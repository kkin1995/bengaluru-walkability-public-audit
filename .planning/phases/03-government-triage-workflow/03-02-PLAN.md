---
phase: 03-government-triage-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/src/db/admin_queries.rs
  - backend/src/db/queries.rs
  - backend/src/handlers/admin.rs
  - backend/src/handlers/reports.rs
  - backend/src/main.rs
  - backend/.sqlx
autonomous: true
requirements:
  - WFLOW-01
  - WFLOW-02
  - WFLOW-03
  - WFLOW-04
  - WFLOW-05
  - MAP-03
user_setup: []
tags:
  - axum
  - multipart
  - sqlx
  - postgres
  - rust

must_haves:
  truths:
    - "POST /api/admin/reports/:id/resolve accepts multipart form-data with status (resolved|closed), resolution_photo file (mandatory per D-13, D-14), optional resolution_notes; rejects missing photo with HTTP 400 'Resolution photo required'"
    - "POST /api/admin/reports/:id/assign-org accepts JSON {org_id} and atomically sets reports.assigned_org_id and reports.status='assigned' plus inserts a status_history audit row in a single transaction (D-09; WFLOW-03)"
    - "GET /api/admin/reports list response (list_admin_reports) includes a corporation field on each row, sourced via LEFT JOIN wards w ON w.id = r.ward_id and SELECT w.corporation — feeds the CORP column on the admin reports list (D-22; WFLOW-01)"
    - "GET /api/reports/:id response includes new top-level keys `history` (array of {status, changed_at} entries — public fields only, NEVER note or changed_by per Pitfall 10) and `ward_hierarchy` (object with zone_name, ro_division, aro_sub_division, assembly_constituency, assembly_constituency_no, parliamentary_constituency, mla_name, mp_name, corporation, ward_name) (D-29, D-25)"
    - "Resolve handler writes after-photo to UPLOADS_DIR via the same UUID.jpg pattern as create_report; EXIF-stripped via img-parts before disk write (same anti-pattern guard as create_report); stores only filename (not full path) in reports.resolution_photo_path"
    - "resolve_report DB function and assign_report_org DB function each run in a transaction that rolls back on rows_affected==0 and inserts a status_history row in the same transaction (WFLOW-02)"
    - "Both new routes are registered behind require_auth in main.rs admin_protected_router and accept any role (admin or reviewer) per D-07"
    - "cargo test + cargo clippy -- -D warnings + cargo sqlx prepare all succeed; .sqlx metadata regenerated and committed"
  artifacts:
    - path: "backend/src/handlers/admin.rs"
      provides: "admin_resolve_report + admin_assign_report_org handlers"
      contains: "admin_resolve_report"
    - path: "backend/src/db/admin_queries.rs"
      provides: "resolve_report + assign_report_org DB functions + list_admin_reports extended with corporation via wards JOIN"
      contains: "pub async fn resolve_report"
    - path: "backend/src/db/queries.rs"
      provides: "get_report_with_detail function for public single-report API"
      contains: "get_report_with_detail"
    - path: "backend/src/handlers/reports.rs"
      provides: "get_report handler extended to return history + ward_hierarchy"
      contains: "ward_hierarchy"
    - path: "backend/src/main.rs"
      provides: "two new admin routes registered behind require_auth"
      contains: "/api/admin/reports/:id/resolve"
  key_links:
    - from: "backend/src/handlers/admin.rs::admin_resolve_report"
      to: "validate_resolve_request (from plan 03-01)"
      via: "pure validator called after multipart field collection"
      pattern: "validate_resolve_request"
    - from: "backend/src/handlers/admin.rs::admin_resolve_report"
      to: "backend/src/db/admin_queries.rs::resolve_report"
      via: "DB transaction wrapping reports UPDATE + status_history INSERT"
      pattern: "resolve_report\\(\\&state.pool"
    - from: "backend/src/handlers/admin.rs::admin_assign_report_org"
      to: "backend/src/db/admin_queries.rs::assign_report_org"
      via: "DB transaction wrapping reports UPDATE + status_history INSERT"
      pattern: "assign_report_org\\(\\&state.pool"
    - from: "backend/src/handlers/reports.rs::get_report"
      to: "backend/src/db/queries.rs::get_report_with_detail"
      via: "single function call returning enriched JSON value"
      pattern: "get_report_with_detail"
    - from: "backend/src/main.rs"
      to: "admin_resolve_report + admin_assign_report_org"
      via: ".route(...) within admin_protected_router"
      pattern: "/api/admin/reports/:id/resolve.*post.*admin_resolve_report"
    - from: "backend/src/db/admin_queries.rs::list_admin_reports"
      to: "wards.corporation column"
      via: "LEFT JOIN wards w ON w.id = r.ward_id + SELECT w.corporation AS corporation"
      pattern: "LEFT JOIN wards.*r\\.ward_id"
---

<objective>
Implement the WFLOW-01 / WFLOW-02 / WFLOW-03 / WFLOW-04 / WFLOW-05 backend: two new admin routes (resolve + assign-org), the public GET /api/reports/:id extension to include status history + ward hierarchy, and the extension of list_admin_reports to surface the per-ward corporation (data source for the CORP column added by Plan 03-03). Lands the API contract that plans 03-03 (admin UI) and 03-04 (public single-report page) consume.

Purpose: Without these handlers, admin frontend cannot complete the lifecycle and the public page has no data source. Without the list_admin_reports JOIN, the CORP column added by Plan 03-03 has no source field to render. This is the wave-2 keystone of Phase 03.

Output:
- backend/src/db/admin_queries.rs: `resolve_report` + `assign_report_org` (each in transactional block; both write to status_history); `list_admin_reports` extended with `LEFT JOIN wards` + `SELECT w.corporation`
- backend/src/db/queries.rs: `get_report_with_detail(pool, id, api_base)` returning serde_json::Value enriched with history + ward_hierarchy
- backend/src/handlers/admin.rs: `admin_resolve_report` (multipart) + `admin_assign_report_org` (JSON) handlers
- backend/src/handlers/reports.rs: `get_report` handler swapped to call `get_report_with_detail`
- backend/src/main.rs: two new POST routes registered behind `require_auth` in `admin_protected_router`
- backend/.sqlx regenerated and committed
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-government-triage-workflow/03-CONTEXT.md
@.planning/phases/03-government-triage-workflow/03-RESEARCH.md
@.planning/phases/03-government-triage-workflow/03-VALIDATION.md
@.planning/phases/03-government-triage-workflow/03-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@backend/src/handlers/reports.rs
@backend/src/handlers/admin.rs
@backend/src/db/queries.rs
@backend/src/db/admin_queries.rs
@backend/src/main.rs
@backend/src/models/report.rs
@backend/src/models/admin.rs
@backend/src/errors.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolve_report + assign_report_org DB functions + list_admin_reports corporation extension in admin_queries.rs (transactional, with status_history audit; wards JOIN for CORP)</name>
  <files>backend/src/db/admin_queries.rs, backend/.sqlx</files>
  <read_first>
    - backend/src/db/admin_queries.rs (existing `update_report_status` at line 528 as the template; existing `admin_get_report_by_id`; existing `list_admin_reports` — observe current SELECT list and existing `ward_name` JOIN pattern; identify where to add `corporation`)
    - backend/src/errors.rs (AppError variants — BadRequest, NotFound, Database, Internal)
    - backend/migrations/004_ward_boundaries.sql (wards table — confirms `corporation TEXT` column already exists; no new migration needed for Task 1 of this plan)
    - .planning/phases/03-government-triage-workflow/03-RESEARCH.md (Backend: resolve_report code example and Backend: assign_report_org code example sections; Pattern 3 multipart; Pitfall 7 multipart field order; D-22 corporation auto-derived at query time via JOIN)
    - .planning/phases/03-government-triage-workflow/03-CONTEXT.md (D-09 assign auto-advances to 'assigned'; D-13/D-14 mandatory photo; D-07 any admin can transition; D-22 corporation derived via JOIN not denormalized)
    - .planning/codebase/CONVENTIONS.md (sqlx raw-string-literal patterns; tracing macros; error handling)
  </read_first>
  <behavior>
    - `pub async fn resolve_report(pool: &PgPool, report_id: Uuid, new_status: &str, resolution_photo_path: &str, resolution_notes: Option<&str>, changed_by: Uuid) -> Result<bool, AppError>` exists in admin_queries.rs
    - resolve_report begins a transaction (`pool.begin().await?`), executes one UPDATE on reports setting status, resolution_photo_path, resolution_notes; if rows_affected == 0 rolls back and returns Ok(false); else inserts a status_history row (report_id, new_status, note=resolution_notes, changed_by) and commits; returns Ok(true)
    - resolve_report rejects (via AppError::BadRequest) any new_status that is not `"resolved"` or `"closed"` — guard arm at the top of the function before tx.begin to fail fast (D-13, D-14)
    - `pub async fn assign_report_org(pool: &PgPool, report_id: Uuid, org_id: Uuid, changed_by: Uuid) -> Result<bool, AppError>` exists in admin_queries.rs
    - assign_report_org begins a transaction, UPDATE reports SET assigned_org_id = $1, status = 'assigned'::report_status WHERE id = $2 (per D-09 auto-advance), rolls back on rows_affected == 0, otherwise inserts a status_history row (report_id, 'assigned', note='Assigned to organization', changed_by) and commits; returns Ok(true)
    - `list_admin_reports` SELECT list is extended to include `w.corporation AS corporation` via the existing LEFT JOIN wards (the same JOIN already used to select `w.ward_name`) — no new JOIN needed; per D-22 the corporation is auto-derived at query time, not stored on reports
    - The serde_json::Value (or admin list response row struct) returned by `list_admin_reports` now includes a top-level `corporation` key per row (String or null) — feeds the CORP column rendered by Plan 03-03 ReportsTable
    - Both new resolve/assign functions are exercised by integration in plan 03-02 task 2; this task alone cannot have full unit coverage (DB functions are async with I/O — backend tests historically do not cover async I/O per TESTING.md "What Backend Tests Do NOT Cover")
    - `cargo sqlx prepare --database-url ...` regenerates .sqlx metadata for the two new functions AND the modified list_admin_reports SELECT list; metadata commits cleanly
    - All SQL uses raw string literals `r#"..."#` per CONVENTIONS.md
  </behavior>
  <action>
    Add two functions to backend/src/db/admin_queries.rs immediately after `update_report_status` (around line 567). Use the code template from 03-RESEARCH.md "Backend: resolve_report DB Function" and "Backend: assign_report_org DB Function" sections verbatim, BUT add an early-return guard at the top of resolve_report: `if !matches!(new_status, "resolved" | "closed") { return Err(AppError::BadRequest("resolve_report called with invalid status".to_string())); }` per D-13/D-14 defense-in-depth (validate_resolve_request in handlers/admin.rs is the primary gate from plan 03-01; this is a belt-and-suspenders check at the DB layer).

    Both functions use the same transaction pattern as the existing `update_report_status`: `pool.begin().await?` → UPDATE → if rows_affected == 0 rollback + return Ok(false) → INSERT into status_history → commit → Ok(true). Insert SQL must cast literal status to enum: `'resolved'::report_status` or `$1::report_status`.

    The status_history INSERT for resolve_report uses `note = resolution_notes` (Option<&str> — None becomes SQL NULL via sqlx binding). For assign_report_org, status_history note is the static string `'Assigned to organization'`.

    Add a tracing::info! log line at the end of each function (before Ok(true) returns) per CONVENTIONS.md: `tracing::info!(report_id = %report_id, new_status = "resolved" | "closed", changed_by = %changed_by, "Report resolved");` for resolve_report; analogous for assign with org_id field.

    **CORP column wiring (D-22; feeds Plan 03-03 ReportsTable):** Edit the existing `list_admin_reports` function in the same file. Locate the SELECT list (the wards JOIN already exists for `w.ward_name`). Add `w.corporation AS corporation` to the SELECT list — sibling to the existing `w.ward_name` column. Then add a `corporation` String/Option<String> field to the per-row row struct (or row.try_get extraction if list_admin_reports builds serde_json::Value directly) and include `corporation` as a top-level key in each row's serialized JSON value. The list response remains a flat array of row objects per the existing convention — no shape change beyond the added key. NO new function and NO new JOIN: this is one SELECT-list addition + one row-struct/JSON-builder addition.

    After both new resolve/assign functions land AND list_admin_reports is extended, run from project root: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up db -d` then `cd backend && cargo sqlx prepare --database-url "postgres://walkability:walkability@localhost:5432/walkability"` to regenerate `.sqlx/` JSON metadata files for ALL three changed queries (resolve_report INSERT/UPDATE, assign_report_org INSERT/UPDATE, list_admin_reports extended SELECT). Commit the regenerated JSON files.

    NO route registration, NO handler wiring, NO public API change in this task — only the DB-layer functions and the list_admin_reports SELECT extension.
  </action>
  <verify>
    <automated>cd backend && cargo check 2>&1 | tail -10 && cargo clippy -- -D warnings 2>&1 | tail -10</automated>
    <human-check>After applying plan 03-01 migrations + this task, start the backend (`cd backend && cargo run`) and curl the admin list endpoint with a logged-in admin cookie: `curl -b "admin_token=$TOK" http://localhost:3001/api/admin/reports | jq '.[0] | keys'` — confirm `"corporation"` appears in the returned keys array. Spot-check a row with `jq '.[0].corporation'` and confirm a string value (one of: Central, North, East, South, West) or null appears.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub async fn resolve_report" backend/src/db/admin_queries.rs` returns 1
    - `grep -c "pub async fn assign_report_org" backend/src/db/admin_queries.rs` returns 1
    - `grep -E "matches!\(new_status,\s*\"resolved\"\s*\|\s*\"closed\"\)" backend/src/db/admin_queries.rs` returns at least 1 (the early-return guard in resolve_report)
    - `grep -c "status = 'assigned'::report_status" backend/src/db/admin_queries.rs` returns at least 1 (assign_report_org UPDATE)
    - `grep -c "INSERT INTO status_history" backend/src/db/admin_queries.rs` returns at least 3 (existing update_report_status + 2 new)
    - `grep -c "w.corporation" backend/src/db/admin_queries.rs` returns at least 1 (list_admin_reports SELECT extension)
    - `grep -c "\"corporation\"" backend/src/db/admin_queries.rs` returns at least 1 (list_admin_reports JSON builder includes the key)
    - `cd backend && cargo check 2>&1 | grep -c "error\[E" | head -1` returns 0
    - `cd backend && cargo clippy -- -D warnings 2>&1 | grep -c "^warning:"` returns 0
    - `git status backend/.sqlx/ | grep -E "modified|new file"` shows changes were committed
  </acceptance_criteria>
  <done>
    Both new resolve/assign DB functions exist and pass clippy; list_admin_reports SELECT list now surfaces the per-ward corporation via the existing wards JOIN; sqlx metadata is regenerated and tracked. Plan 03-02 Task 2 can call resolve/assign from handlers; Plan 03-03 ReportsTable CORP column has a backing data field.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin_resolve_report multipart handler + admin_assign_report_org JSON handler + route registration</name>
  <files>backend/src/handlers/admin.rs, backend/src/main.rs, backend/src/models/admin.rs</files>
  <read_first>
    - backend/src/handlers/admin.rs (validate_status, validate_resolve_request from plan 03-01; admin_update_report_status at line 471 as the existing PATCH template; admin_delete_report at line 513 for the canonicalize+starts_with path-safety pattern at lines 526-568)
    - backend/src/handlers/reports.rs (create_report multipart pattern; strip_exif function at line 346; UUID-based filename; uploads_dir write)
    - backend/src/main.rs (admin_protected_router at line 177; existing route registrations at lines 185-199; require_auth wiring at the bottom)
    - backend/src/models/admin.rs (UpdateStatusRequest struct at line 171; pattern to mirror for new AssignOrgRequest)
    - backend/src/middleware/auth.rs (AuthJwtClaims extractor pattern — claims.sub Uuid parsing)
    - .planning/phases/03-government-triage-workflow/03-RESEARCH.md (Pattern 3 Resolution Photo Upload via Multipart; New Routes to Register in main.rs section; Pitfall 6, 7)
    - .planning/phases/03-government-triage-workflow/03-CONTEXT.md (D-07 any admin, D-09 auto-advance, D-13/D-14 mandatory photo, D-16 single submit, D-18 public photo)
  </read_first>
  <behavior>
    - `pub async fn admin_resolve_report(Extension(claims): Extension<AuthJwtClaims>, State(state): State<Arc<AppState>>, Path(id): Path<Uuid>, mut multipart: Multipart) -> Result<Json<serde_json::Value>, AppError>` exists in admin.rs § 3 (async handlers section)
    - Multipart field collection follows the create_report pattern: iterate all fields into local variables BEFORE any validation, so the order-dependent multipart consumer never re-reads (per Pitfall 7). Required fields: `status` (text), `resolution_photo` (file bytes + filename). Optional: `resolution_notes` (text).
    - After collection, call `validate_status(&status)?` then `validate_resolve_request(&status, photo_bytes.len())?` (these pure helpers landed in plan 03-01)
    - Reject any status not in {"resolved", "closed"} with BadRequest — covered by validate_resolve_request behavior
    - Apply `strip_exif` to photo_bytes; if strip_exif returns Err (invalid JPEG), propagate the BadRequest
    - Generate UUID-based filename: `let photo_filename = format!("{}.jpg", Uuid::new_v4())`; write EXIF-stripped bytes to `state.uploads_dir.join(&photo_filename)` via tokio::fs::write (matches create_report pattern)
    - Parse changed_by Uuid from claims.sub; on parse failure return AppError::Unauthorized (matches admin_update_report_status pattern at line 481)
    - Call `admin_queries::resolve_report(&state.pool, id, &status, &photo_filename, resolution_notes.as_deref(), changed_by).await?`; on returned Ok(false) return AppError::NotFound
    - On success, fetch the updated report via `admin_queries::get_admin_report_by_id(&state.pool, id)` and return it as Json(serde_json::Value) — same return shape as admin_update_report_status to match frontend AdminReport contract
    - Add tracing::info!(report_id = %id, new_status = %status, "Report resolved (Phase 03 WFLOW-05)") log line
    - `pub async fn admin_assign_report_org(Extension(claims): Extension<AuthJwtClaims>, State(state): State<Arc<AppState>>, Path(id): Path<Uuid>, Json(payload): Json<AssignOrgRequest>) -> Result<Json<serde_json::Value>, AppError>` exists in admin.rs § 3
    - New struct in backend/src/models/admin.rs: `pub struct AssignOrgRequest { pub org_id: Uuid }` with `#[derive(Debug, Deserialize)]`
    - admin_assign_report_org parses changed_by from claims.sub (Uuid::parse_str), calls `admin_queries::assign_report_org(&state.pool, id, payload.org_id, changed_by).await?`, returns NotFound on Ok(false), otherwise fetches the updated report via admin_get_report_by_id and returns it as Json
    - main.rs admin_protected_router gets two new routes added in the existing chain (between line 191 and line 192): `.route("/api/admin/reports/:id/resolve", post(admin_resolve_report))` and `.route("/api/admin/reports/:id/assign-org", post(admin_assign_report_org))`
    - cargo test, cargo clippy -- -D warnings, cargo build all pass; no new test files (handlers are async I/O — per TESTING.md not covered by current unit tests)
    - validate_resolve_request unit tests from plan 03-01 cover the photo-required gate; this task's handler is a thin orchestration layer over already-tested pure validators + already-tested DB transaction
  </behavior>
  <action>
    Add AssignOrgRequest struct to backend/src/models/admin.rs near UpdateStatusRequest (around line 175). Single field `pub org_id: Uuid` with #[derive(Debug, Deserialize)]. No validator needed — UUID parse is enforced by serde_json. Citation: D-08 (assign target is the org_id).

    Add admin_resolve_report and admin_assign_report_org handlers to backend/src/handlers/admin.rs after admin_update_report_status (around line 510). Follow the multipart pattern from create_report verbatim for resolve: use axum::extract::Multipart, iterate `while let Some(field) = multipart.next_field().await?` and dispatch on field.name() — collect (status: String, resolution_notes: Option<String>, photo_bytes: Vec<u8>, photo_filename: String) into local variables before any validation. After the loop, call validate_status + validate_resolve_request (Pitfall 7: validate AFTER collection, not before).

    For path-traversal safety on the photo write, follow the SAME canonicalize+starts_with pattern used in admin_delete_report at lines 526-568 (state.uploads_dir canonical, joined photo_filename canonical, starts_with check) — but adapted to a write (not delete): canonicalize state.uploads_dir, join photo_filename, then tokio::fs::write the canonical join. Since photo_filename is a freshly-generated UUID we control, the canonicalize check is a defense-in-depth measure not a bug fix; document this rationale with an inline comment referencing FINDING-013.

    Store ONLY photo_filename (not full path) in reports.resolution_photo_path per the existing image_path convention (see backend/src/models/report.rs comment at line 36-ish: "Filename only (UUID.jpg); URL built at runtime from PUBLIC_URL").

    For admin_assign_report_org, the handler is much simpler: parse claims.sub → changed_by, call `assign_report_org`, on miss return NotFound, on hit refetch report and return Json. No multipart, no file write, no validation beyond the implicit serde Uuid parse.

    Register both routes in backend/src/main.rs admin_protected_router (lines 177-199). Insert the two new `.route()` calls in alphabetical-of-path order: assign-org first (`/api/admin/reports/:id/assign-org`) then resolve (`/api/admin/reports/:id/resolve`), each calling `post(admin_resolve_report)` / `post(admin_assign_report_org)` respectively. Both routes inherit the require_auth middleware applied to admin_protected_router at the bottom of main.rs.

    Update the use-import block at the top of admin.rs to include `axum::extract::Multipart` and `crate::handlers::reports::strip_exif` (the strip_exif function in reports.rs is currently private — make it `pub(crate)` so admin.rs can call it; one-line visibility change at line 346 of reports.rs).

    After all edits run: `cd backend && cargo build && cargo clippy -- -D warnings && cargo test`. Then `cargo sqlx prepare --database-url "postgres://walkability:walkability@localhost:5432/walkability"` once more if sqlx surface area changed. Commit any regenerated .sqlx files.

    NO Wave 0 unit tests added — admin async handlers are explicitly NOT covered by the project's existing unit tests per TESTING.md. The validate_status + validate_resolve_request pure tests (from plan 03-01) carry the WFLOW-05 photo gate coverage. Manual UAT covers end-to-end per 03-VALIDATION.md Manual-Only Verifications table.
  </action>
  <verify>
    <automated>cd backend && cargo build 2>&1 | tail -10 && cargo clippy -- -D warnings 2>&1 | tail -10 && cargo test validate_resolve validate_status 2>&1 | tail -20</automated>
    <human-check>Start the backend (`cd backend && cargo run`), POST a test resolve via curl: `curl -X POST -b "admin_token=$(get a real token)" -F status=resolved -F resolution_photo=@test.jpg http://localhost:3001/api/admin/reports/$REAL_REPORT_ID/resolve` and confirm 200 + the response JSON contains a resolution_photo_url; then POST without the photo: `curl -X POST -b "..." -F status=resolved http://localhost:3001/api/admin/reports/$ID/resolve` and confirm 400 + body `{"error":"Resolution photo required"}`. Confirm a row was appended to status_history via `docker compose exec db psql -U walkability -d walkability -c "SELECT report_id, new_status, changed_by FROM status_history ORDER BY changed_at DESC LIMIT 1"`.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub async fn admin_resolve_report" backend/src/handlers/admin.rs` returns 1
    - `grep -c "pub async fn admin_assign_report_org" backend/src/handlers/admin.rs` returns 1
    - `grep -c "pub struct AssignOrgRequest" backend/src/models/admin.rs` returns 1
    - `grep -c "validate_resolve_request" backend/src/handlers/admin.rs` returns at least 1 (called in admin_resolve_report)
    - `grep -c "/api/admin/reports/:id/resolve" backend/src/main.rs` returns 1
    - `grep -c "/api/admin/reports/:id/assign-org" backend/src/main.rs` returns 1
    - `grep -c "post(admin_resolve_report)" backend/src/main.rs` returns 1
    - `grep -c "post(admin_assign_report_org)" backend/src/main.rs` returns 1
    - `grep -c "pub(crate) fn strip_exif\|pub fn strip_exif" backend/src/handlers/reports.rs` returns 1 (visibility expanded)
    - `grep -c "canonicalize" backend/src/handlers/admin.rs` returns at least 2 (existing admin_delete_report + new admin_resolve_report)
    - `cd backend && cargo build 2>&1 | grep -c "^error" | head -1` returns 0
    - `cd backend && cargo clippy -- -D warnings 2>&1 | grep -c "^warning:"` returns 0
    - `cd backend && cargo test 2>&1 | grep -E "test result: ok\."` shows ok (no regressions vs plan 03-01 baseline)
  </acceptance_criteria>
  <done>
    Both new admin routes exist, are wired behind require_auth, compile cleanly with zero clippy warnings, and pass the human-check curl smoke test (200 on valid resolve, 400 on missing photo, status_history row written).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Public GET /api/reports/:id extension — get_report_with_detail (history + ward_hierarchy)</name>
  <files>backend/src/db/queries.rs, backend/src/handlers/reports.rs, backend/.sqlx</files>
  <read_first>
    - backend/src/db/queries.rs (existing get_report_by_id at line 185; existing SELECT list — already extended in plan 03-01 with resolution columns)
    - backend/src/handlers/reports.rs (get_report handler at line 334 — currently calls get_report_by_id then into_response)
    - backend/src/models/report.rs (Report → ReportResponse conversion; resolution_photo_url added in plan 03-01)
    - backend/src/models/ward.rs (Ward struct — column names match wards table)
    - backend/migrations/004_ward_boundaries.sql (wards table base schema) and 009_ward_hierarchy.sql (new columns from plan 03-01)
    - .planning/phases/03-government-triage-workflow/03-RESEARCH.md (Pattern 4 — Extending GET /api/reports/:id; code example for get_report_with_detail; Pitfall 9 wards JOIN; Pitfall 10 public status history must NOT include note or changed_by)
    - .planning/phases/03-government-triage-workflow/03-CONTEXT.md (D-25 display locations; D-29 endpoint extension recommendation; D-17 resolution_notes admin-only — exclude from public; D-18 after-photo IS public)
  </read_first>
  <behavior>
    - New function `pub async fn get_report_with_detail(pool: &PgPool, id: Uuid, api_base: &str) -> Result<serde_json::Value, AppError>` exists in backend/src/db/queries.rs
    - get_report_with_detail executes ONE SQL query joining reports + wards via LEFT JOIN (per Pitfall 9), selecting: r.id, r.created_at, r.image_path, r.latitude, r.longitude, r.category::TEXT, r.severity::TEXT, r.description, r.status::TEXT, r.location_source::TEXT, r.resolution_photo_path, w.ward_name, w.corporation, w.zone_name, w.ro_division, w.aro_sub_division, w.assembly_constituency, w.assembly_constituency_no, w.parliamentary_constituency, w.mla_name, w.mp_name
    - get_report_with_detail executes a SECOND query: `SELECT new_status::TEXT AS status, changed_at FROM status_history WHERE report_id = $1 ORDER BY changed_at ASC` — note + changed_by COLUMNS ARE NEVER SELECTED (per Pitfall 10, D-17)
    - get_report_with_detail returns a serde_json::Value matching this JSON shape: `{id, created_at, image_url, latitude (rounded 3dp), longitude (rounded 3dp), category, severity, description, status, location_source, resolution_photo_url (only if resolution_photo_path is Some), history: [{status, changed_at}, ...], ward_hierarchy: {ward_name, corporation, zone_name, ro_division, aro_sub_division, assembly_constituency, assembly_constituency_no, parliamentary_constituency, mla_name, mp_name}}`
    - resolution_photo_url is built via the same `format!("{}/uploads/{}", api_base, path)` pattern as image_url; only included when resolution_photo_path is Some
    - resolution_notes (admin-only per D-17) is NEVER included in the public response
    - submitter_name, submitter_contact, submitter_ip (existing privacy fields) are NEVER selected or included
    - When the report exists but ward_id is NULL (report stored without ward lookup), ward_hierarchy is still returned as an object with all-null fields — not omitted — so the frontend's UI-SPEC "Ward assignment not available for this report" branch can dispatch on the presence of null values
    - get_report handler in backend/src/handlers/reports.rs is changed to: `let json_value = queries::get_report_with_detail(&state.pool, id, &state.api_base_url).await?; Ok(Json(json_value))` — no into_response call
    - cargo sqlx prepare regenerates metadata for the two new queries; cargo build + cargo clippy -- -D warnings + cargo test all pass
  </behavior>
  <action>
    Add `get_report_with_detail` function to backend/src/db/queries.rs immediately after `get_report_by_id` (at line 213). Implementation outline:

    1. First query: `sqlx::query(r#"SELECT r.id, r.created_at, r.image_path, r.latitude, r.longitude, r.category::TEXT AS category, r.severity::TEXT AS severity, r.description, r.status::TEXT AS status, r.location_source::TEXT AS location_source, r.resolution_photo_path, w.ward_name, w.corporation, w.zone_name, w.ro_division, w.aro_sub_division, w.assembly_constituency, w.assembly_constituency_no, w.parliamentary_constituency, w.mla_name, w.mp_name FROM reports r LEFT JOIN wards w ON w.id = r.ward_id WHERE r.id = $1"#).bind(id).fetch_optional(pool).await?.ok_or(AppError::NotFound)?` — bind via sqlx::Row; extract each column with row.get::<T, _>("col_name") or row.try_get for nullable ward columns.

    2. Second query: `sqlx::query("SELECT new_status::TEXT AS status, changed_at FROM status_history WHERE report_id = $1 ORDER BY changed_at ASC").bind(id).fetch_all(pool).await?` — note + changed_by NEVER selected (D-17, Pitfall 10).

    3. Build the response JSON via `serde_json::json!({...})`: latitude/longitude rounded to 3dp using `(v * 1000.0).round() / 1000.0` to match the existing Report::into_response pattern. image_url built as `format!("{}/uploads/{}", api_base, image_path)`. resolution_photo_url ONLY included when resolution_photo_path is Some — use `serde_json::Map::insert` conditionally OR include the key with serde_json::Value::Null and rely on `#[serde(skip_serializing_if)]`-equivalent JSON serialization... actually simpler: build a `let mut obj = serde_json::Map::new()` and conditionally insert resolution_photo_url. ward_hierarchy is built as a nested json! object — every key always present (even when value is null) so the frontend dispatches on null per UI-SPEC.

    history array: `let history: Vec<serde_json::Value> = history_rows.iter().map(|r| json!({"status": r.get::<String, _>("status"), "changed_at": r.get::<chrono::DateTime<chrono::Utc>, _>("changed_at")})).collect()`.

    Change backend/src/handlers/reports.rs::get_report at line 334 to call get_report_with_detail and return its Value directly:
    `pub async fn get_report(State(state): State<AppState>, Path(id): Path<Uuid>) -> Result<Json<serde_json::Value>, AppError> { let v = queries::get_report_with_detail(&state.pool, id, &state.api_base_url).await?; Ok(Json(v)) }`. The Json<ReportResponse> return type changes to Json<serde_json::Value> — this is a breaking JSON shape addition (we gain `history` and `ward_hierarchy` keys; existing keys preserved), but no existing public-API consumer breaks because the original ReportResponse keys are still present.

    Add an inline comment in get_report_with_detail body: `// PRIVACY (D-17, Pitfall 10): status_history.note and status_history.changed_by are NEVER selected for public response — admin-only fields stay admin-only.`

    Run `cd backend && cargo sqlx prepare --database-url "postgres://walkability:walkability@localhost:5432/walkability"` to regenerate metadata. Run `cargo build && cargo clippy -- -D warnings && cargo test`. Commit regenerated .sqlx files.

    NO change to admin_get_report_by_id — admin endpoint may continue to use the older shape since it already includes admin-only fields (resolution_notes, status_history with notes). This task ONLY touches the public reports.rs get_report path.
  </action>
  <verify>
    <automated>cd backend && cargo build 2>&1 | tail -10 && cargo clippy -- -D warnings 2>&1 | tail -10 && cargo test 2>&1 | tail -20</automated>
    <human-check>Start backend; insert a test report; transition status via PATCH; resolve via the plan 03-02 task 2 handler; then `curl http://localhost:3001/api/reports/$ID | jq` and confirm: (1) `.history` is an array with ≥ 2 entries each having only `status` and `changed_at` keys (NO note or changed_by); (2) `.ward_hierarchy` is an object with keys ward_name, corporation, zone_name, ro_division, aro_sub_division, assembly_constituency, assembly_constituency_no, parliamentary_constituency, mla_name, mp_name; (3) `.resolution_photo_url` is present when the report has been resolved; (4) `.resolution_notes` is ABSENT from the JSON (admin-only per D-17).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub async fn get_report_with_detail" backend/src/db/queries.rs` returns 1
    - `grep -c "LEFT JOIN wards w ON w.id = r.ward_id" backend/src/db/queries.rs` returns at least 1
    - `grep -c "FROM status_history WHERE report_id" backend/src/db/queries.rs` returns at least 1
    - `grep -c "changed_by\|note" backend/src/db/queries.rs | head -1` — within the new get_report_with_detail function block (verifiable manually) these substrings appear ONLY in the privacy comment, never in the SELECT list of the public status_history query
    - `grep -c "get_report_with_detail" backend/src/handlers/reports.rs` returns at least 1 (handler now calls it)
    - `grep -c "ward_hierarchy" backend/src/db/queries.rs` returns at least 1 (JSON build site)
    - `grep -c "resolution_notes" backend/src/db/queries.rs | head -1` — appears ONLY in the privacy-rationale comment, never in a SELECT or json! invocation in get_report_with_detail
    - `cd backend && cargo build 2>&1 | grep -c "^error"` returns 0
    - `cd backend && cargo clippy -- -D warnings 2>&1 | grep -c "^warning:"` returns 0
    - `cd backend && cargo test 2>&1 | grep -E "test result: ok\."` shows ok (full suite green, no regressions)
    - human-check curl confirms all 4 acceptance points
  </acceptance_criteria>
  <done>
    The public GET /api/reports/:id endpoint returns enriched JSON with history (public-safe fields only) and ward_hierarchy (10 keys). resolution_photo_url is present when resolved; resolution_notes is absent. Plan 03-04 has the data contract it needs for the /reports/[id] page.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTPS → /api/admin/reports/:id/resolve | Multipart upload from authenticated admin; photo bytes write to disk; status_history row inserted |
| HTTPS → /api/admin/reports/:id/assign-org | Authenticated admin JSON; updates reports.assigned_org_id and reports.status atomically |
| Authenticated admin → status_history note | Admin-supplied resolution_notes persisted; admin-only — never exposed via public API |
| Public HTTPS → /api/reports/:id | Anyone can fetch ward_hierarchy + status history (public fields only) |
| status_history table → public response | Pitfall 10 — must SELECT only new_status + changed_at, never note or changed_by |
| Admin HTTPS → /api/admin/reports list | list_admin_reports JOIN serves per-ward corporation; admin-only (already gated by require_auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | admin_resolve_report multipart photo | mitigate | strip_exif strips EXIF before disk write (existing pattern from create_report); rejected on invalid JPEG via AppError::BadRequest |
| T-03-02-02 | Tampering | admin_resolve_report path traversal via stored filename | mitigate | UUID-generated filename + canonicalize + starts_with(uploads_dir) check, mirroring FINDING-013 pattern in admin_delete_report at lines 526-568 |
| T-03-02-03 | Tampering | Resolve bypass without photo | mitigate | validate_resolve_request (pure validator from plan 03-01) rejects (status in {resolved, closed} && photo_bytes.is_empty()) → BadRequest "Resolution photo required"; defense-in-depth guard inside resolve_report DB function rejects invalid statuses too |
| T-03-02-04 | Information Disclosure | status_history.note exposure on public /api/reports/:id | mitigate | get_report_with_detail SELECT list explicitly omits note + changed_by; inline rationale comment + acceptance test grep guard (per Pitfall 10, D-17) |
| T-03-02-05 | Information Disclosure | resolution_notes leak via JSON response | mitigate | get_report_with_detail JSON builder never inserts resolution_notes; the field is admin-only on admin_get_report_by_id only (D-17) |
| T-03-02-06 | Tampering | assign_org with nonexistent org_id | mitigate | FK constraint `assigned_org_id REFERENCES organizations(id)` (from migration 008) enforces referential integrity at DB layer; sqlx Postgres FK violation surfaces as AppError::Database; handler may convert to BadRequest with a guard, but FK is the authoritative gate |
| T-03-02-07 | Spoofing | claims.sub forged to inject changed_by audit | accept | require_auth middleware (existing) validates the JWT signature; claims.sub is the authenticated UUID; no additional check needed at handler layer |
| T-03-02-08 | Elevation of Privilege | reviewer-role admin closing reports | accept | D-07 explicitly allows any admin or reviewer to perform any transition; no require_role gate added — matches existing admin_update_report_status pattern |
| T-03-02-09 | Information Disclosure | list_admin_reports corporation exposure to non-admins | mitigate | admin_protected_router require_auth gate already in place; corporation is published civic data (5 GBA corporation names) and is shown publicly per D-25 on the map popup anyway — no privacy concern |
| T-03-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan introduces zero new dependencies. No Package Legitimacy Gate required. |

</threat_model>

<verification>
After all 3 tasks complete:
- `cd backend && cargo build && cargo clippy -- -D warnings && cargo test` all pass green
- `cd backend && cargo sqlx prepare --check --database-url "postgres://walkability:walkability@localhost:5432/walkability"` confirms .sqlx is in sync
- Start the stack, create a test report, then:
  - `curl -X POST -F status=resolved -F resolution_photo=@test.jpg -b admin_token=... http://localhost:3001/api/admin/reports/$ID/resolve` returns 200
  - Same call without `-F resolution_photo` returns 400 with body `{"error":"Resolution photo required"}`
  - `curl -X POST -H "Content-Type: application/json" -d '{"org_id":"$ORG_UUID"}' -b admin_token=... http://localhost:3001/api/admin/reports/$ID/assign-org` returns 200; report.status afterward is 'assigned'
  - `curl http://localhost:3001/api/reports/$ID | jq '.history, .ward_hierarchy, .resolution_photo_url, .resolution_notes // "ABSENT"'` shows: history array, hierarchy object, photo URL present after resolve, resolution_notes is "ABSENT"
  - `curl -b admin_token=... http://localhost:3001/api/admin/reports | jq '.[0] | has("corporation")'` returns `true`; `jq '.[0].corporation'` returns a string (one of: Central, North, East, South, West) or null
</verification>

<success_criteria>
- POST /api/admin/reports/:id/resolve accepts multipart {status, resolution_photo, optional resolution_notes}; rejects missing photo with 400
- POST /api/admin/reports/:id/assign-org accepts JSON {org_id}; UPDATE reports + INSERT status_history happen atomically
- GET /api/reports/:id returns history array (public fields only) + ward_hierarchy object
- GET /api/admin/reports list returns a `corporation` field per row (sourced via the existing wards LEFT JOIN), unblocking the CORP column added by Plan 03-03 ReportsTable
- resolution_notes (admin-only per D-17) never appears in any /api/reports/:id JSON
- resolution_photo_url (public per D-18) appears when resolution_photo_path is set
- All 9 SQL/handler-layer security threats from <threat_model> are mitigated by code paths and acceptance criteria
- cargo test, cargo clippy -- -D warnings green; .sqlx metadata in sync
</success_criteria>

<output>
Create `.planning/phases/03-government-triage-workflow/03-02-SUMMARY.md` when done, listing: routes registered, DB functions added, public API JSON shape diff, the curl-based smoke results, and the list_admin_reports corporation extension (sample JSON row with corporation key included).
</output>
