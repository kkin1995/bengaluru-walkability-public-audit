---
phase: 03-government-triage-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - frontend/app/admin/admin.css
  - frontend/app/admin/components/StatusBadge.tsx
  - frontend/app/admin/components/StatusActionPanel.tsx
  - frontend/app/admin/components/OrgAssignPanel.tsx
  - frontend/app/admin/components/GbaHierarchyPanel.tsx
  - frontend/app/admin/components/ResolveModal.tsx
  - frontend/app/admin/components/__tests__/StatusBadge.test.tsx
  - frontend/app/admin/components/__tests__/StatusActionPanel.test.tsx
  - frontend/app/admin/components/__tests__/OrgAssignPanel.test.tsx
  - frontend/app/admin/components/__tests__/ResolveModal.test.tsx
  - frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx
  - frontend/app/admin/lib/adminApi.ts
  - frontend/app/admin/reports/[id]/page.tsx
  - frontend/app/admin/reports/page.tsx
  - frontend/app/admin/components/ReportsTable.tsx
autonomous: false
requirements:
  - WFLOW-01
  - WFLOW-03
  - WFLOW-04
  - WFLOW-05
user_setup: []
tags:
  - react
  - nextjs
  - typescript
  - admin
  - direction-b

must_haves:
  truths:
    - "StatusBadge renders all 6 status values with the dot treatment + label + color tokens defined in UI-SPEC §Status Color System (open filled-teal, acknowledged ring-teal, assigned filled-amber, in_progress pulsing-amber, resolved filled-green, closed filled-grey) per D-37, D-38"
    - "admin.css gains the 5 new --status-* token triplets in BOTH the .dir-b light block AND the .dir-b.dark block exactly as listed in UI-SPEC §E (open, acknowledged, assigned, in_progress, closed — each with -bg + -border suffixes)"
    - "StatusActionPanel renders the per-status button set from UI-SPEC §A copy table: open→Acknowledge, acknowledged→Assign to organisation + Mark as in progress, assigned→Mark as in progress + Resolve, in_progress→Resolve, resolved→Close report, closed→locked panel 'This report is closed.' (WFLOW-01)"
    - "OrgAssignPanel renders unassigned state + cascading Corporation→Ward Office picker per D-11 and UI-SPEC §B; calls assignReportOrg API on save which auto-advances status to assigned (D-09; WFLOW-03)"
    - "ResolveModal renders mandatory after-photo dropzone with REQUIRED indicator; submit button is disabled until photo provided; resolution_notes textarea is optional; submits via FormData multipart to the new POST /api/admin/reports/:id/resolve route (D-13, D-14, D-15, D-16; WFLOW-04, WFLOW-05)"
    - "GbaHierarchyPanel renders the bureaucratic chain (Ward → ARO Sub Division → RO Division → Zone → Corporation → GBA) plus the elected chain (Assembly + AC number + MLA name + Parliamentary constituency + MP name) from the ward_hierarchy data fetched via admin API per D-23, D-42, D-45"
    - "/admin/reports/[id] page integrates StatusActionPanel + OrgAssignPanel + GbaHierarchyPanel + ResolveModal — the existing inline status PATCH buttons are replaced with the new contextual panel"
    - "/admin/reports page (reports list) gains a CORP column between WARD and SEV per UI-SPEC §F"
    - "adminApi.ts gains resolveReport (multipart) + assignReportOrg (JSON) functions matching the plan 03-02 backend contract; AdminReport interface gains resolution_photo_url, resolution_notes, assigned_org_id, ward_hierarchy fields; AdminStats by_status keys updated to 6 values"
    - "All previously-skipped describe.skip blocks from plan 03-01 task 3 are un-skipped and implemented; cd frontend && npm test passes green with new assertions"
  artifacts:
    - path: "frontend/app/admin/components/StatusActionPanel.tsx"
      provides: "Per-status contextual action button bar"
      min_lines: 80
    - path: "frontend/app/admin/components/OrgAssignPanel.tsx"
      provides: "Cascading corporation→ward-office assignment picker"
      min_lines: 100
    - path: "frontend/app/admin/components/GbaHierarchyPanel.tsx"
      provides: "Bureaucratic + elected chain display"
      min_lines: 80
    - path: "frontend/app/admin/components/ResolveModal.tsx"
      provides: "Combined resolve/close modal with mandatory photo + optional notes"
      min_lines: 120
    - path: "frontend/app/admin/admin.css"
      provides: "5 new --status-* token triplets in light + dark blocks"
      contains: "--status-closed"
  key_links:
    - from: "frontend/app/admin/components/ResolveModal.tsx"
      to: "frontend/app/admin/lib/adminApi.ts::resolveReport"
      via: "FormData multipart POST to /api/admin/reports/:id/resolve"
      pattern: "resolveReport\\("
    - from: "frontend/app/admin/components/OrgAssignPanel.tsx"
      to: "frontend/app/admin/lib/adminApi.ts::assignReportOrg + listOrganizations"
      via: "fetch calls returning typed responses"
      pattern: "assignReportOrg\\("
    - from: "frontend/app/admin/reports/[id]/page.tsx"
      to: "StatusActionPanel + OrgAssignPanel + GbaHierarchyPanel + ResolveModal"
      via: "JSX composition at the top of the report detail layout"
      pattern: "<StatusActionPanel"
    - from: "frontend/app/admin/components/StatusBadge.tsx"
      to: "frontend/app/admin/admin.css --status-* tokens"
      via: "STATUS_MAP entries reference var(--status-*) tokens"
      pattern: "var\\(--status-(open|acknowledged|assigned|in-progress|closed)\\)"
---

<objective>
Ship the Phase 03 admin frontend per UI-SPEC §A–F (Direction B): expand StatusBadge to 6 values, build StatusActionPanel + OrgAssignPanel + GbaHierarchyPanel + ResolveModal, wire them into /admin/reports/[id], add CORP column to /admin/reports, extend adminApi.ts for the new endpoints.

Purpose: WFLOW-01 (6-state lifecycle UI), WFLOW-03 (org assignment), WFLOW-04 (resolution notes), WFLOW-05 (mandatory after-photo upload) are entirely admin-UI requirements. This plan delivers all four to the Nammadaari admin team per the GBA pivot (D-01, D-02).

Output:
- 5 new admin Direction-B components (4 new files + StatusBadge extension)
- 5 new --status-* token triplets in admin.css (light + dark)
- Extended adminApi.ts with resolveReport, assignReportOrg, updated AdminReport/AdminStats types
- Extended /admin/reports/[id] page composing the 4 new panels
- CORP column added to /admin/reports list (frontend-only, derived from ward.corporation already on the JSON)
- All Wave 0 describe.skip tests un-skipped and implemented
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-government-triage-workflow/03-CONTEXT.md
@.planning/phases/03-government-triage-workflow/03-RESEARCH.md
@.planning/phases/03-government-triage-workflow/03-UI-SPEC.md
@.planning/phases/03-government-triage-workflow/03-VALIDATION.md
@.planning/phases/03-government-triage-workflow/03-01-SUMMARY.md
@.planning/phases/03-government-triage-workflow/03-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@frontend/app/admin/admin.css
@frontend/app/admin/components/StatusBadge.tsx
@frontend/app/admin/components/Btn.tsx
@frontend/app/admin/components/Input.tsx
@frontend/app/admin/components/Select.tsx
@frontend/app/admin/components/Card.tsx
@frontend/app/admin/components/Icon.tsx
@frontend/app/admin/components/SectionLabel.tsx
@frontend/app/admin/components/Pill.tsx
@frontend/app/admin/lib/adminApi.ts
@frontend/app/admin/reports/[id]/page.tsx
@frontend/app/admin/reports/page.tsx
@frontend/app/admin/components/ReportsTable.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tokens + StatusBadge 6-value extension + adminApi.ts contract extension</name>
  <files>frontend/app/admin/admin.css, frontend/app/admin/components/StatusBadge.tsx, frontend/app/admin/components/__tests__/StatusBadge.test.tsx, frontend/app/admin/lib/adminApi.ts</files>
  <read_first>
    - frontend/app/admin/admin.css (existing .dir-b light + .dir-b.dark blocks — observe how --status-submitted, --status-review, --status-resolved are defined; the new tokens must mirror the same key naming convention exactly)
    - frontend/app/admin/components/StatusBadge.tsx (full file; current 3-entry STATUS_MAP at line 21; toneStyle at lines 26-33; SIZE_STYLE at line 57; fallback unknown-status branch at lines 65-70)
    - frontend/app/admin/components/__tests__/StatusBadge.test.tsx (existing tests + the new describe blocks for the 5 new statuses scaffolded by plan 03-01 task 3 — un-skip and finish)
    - frontend/app/admin/lib/adminApi.ts (current AdminReport interface at line 36; AdminStats interface at line 77; updateReportStatus function at line 171; full file structure)
    - .planning/phases/03-government-triage-workflow/03-UI-SPEC.md (sections: §Status Color System 6-state table; §E StatusBadge Extension with the exact CSS token block; §Copywriting Contract for exact label strings)
    - .planning/phases/03-government-triage-workflow/03-RESEARCH.md (Pattern 6 StatusBadge 6-Value Extension; Frontend: AdminReport Interface Extended; Frontend: resolveReport API Function code blocks)
  </read_first>
  <behavior>
    - admin.css `.dir-b` light block contains the 5 new token triplets (15 new CSS variables total): `--status-open`, `--status-open-bg`, `--status-open-border`, `--status-acknowledged`, `--status-acknowledged-bg`, `--status-acknowledged-border`, `--status-assigned`, `--status-assigned-bg`, `--status-assigned-border`, `--status-in-progress`, `--status-in-progress-bg`, `--status-in-progress-border`, `--status-closed`, `--status-closed-bg`, `--status-closed-border` — values exactly as listed in UI-SPEC §E light block (e.g. `--status-open: oklch(0.56 0.13 200)`)
    - admin.css `.dir-b.dark` block contains the SAME 15 variables with the dark-mode values from UI-SPEC §E dark block (e.g. `--status-open: oklch(0.72 0.13 200)`)
    - StatusValue type expanded to `"open" | "acknowledged" | "assigned" | "in_progress" | "resolved" | "closed"`; STATUS_MAP gains 5 new entries with tone + dot + label + ariaLabel + toneStyle per UI-SPEC §Status Color System table
    - StatusBadge accepts optional `dotTreatment?: "filled" | "ring" | "pulse"` prop with default "filled"; "ring" renders `background: transparent; box-shadow: inset 0 0 0 1.5px <color>` per UI-SPEC §Status Color System ring-dot implementation; "pulse" renders the dot with className `pulse-dot` (the CSS keyframe pulse-dot must already exist in admin.css — if not, add it: `@keyframes pulse-dot { 0%,100% { opacity: 1 } 50% { opacity: 0.4 } }` with `.pulse-dot { animation: pulse-dot 1.6s ease-in-out infinite }`)
    - STATUS_MAP entries set dotTreatment defaults: open→filled, acknowledged→ring, assigned→filled, in_progress→pulse, resolved→filled, closed→filled (per UI-SPEC table)
    - adminApi.ts AdminReport interface gains 4 new fields: `resolution_photo_url: string | null`, `resolution_notes: string | null`, `assigned_org_id: string | null`, `ward_hierarchy?: WardHierarchy | null` (WardHierarchy is a new exported interface with the 10 fields ward_name, corporation, zone_name, ro_division, aro_sub_division, assembly_constituency, assembly_constituency_no, parliamentary_constituency, mla_name, mp_name — all nullable strings except assembly_constituency_no which is `number | null`)
    - adminApi.ts AdminStats interface `by_status` field type changed from `{submitted: number, under_review: number, resolved: number}` to `{open: number, acknowledged: number, assigned: number, in_progress: number, resolved: number, closed: number}` per D-03
    - New exported function `resolveReport(id: string, status: "resolved" | "closed", photo: File, notes?: string): Promise<AdminReport>` — uses FormData with fields status + resolution_photo + (optional) resolution_notes; POSTs to `${BASE}/api/admin/reports/${id}/resolve` with `credentials: "include"` and DOES NOT set Content-Type header (browser sets boundary automatically — Pitfall 7 in research)
    - New exported function `assignReportOrg(id: string, orgId: string): Promise<AdminReport>` — JSON POST to `${BASE}/api/admin/reports/${id}/assign-org` with body `{org_id: orgId}`; uses apiFetch helper
    - StatusBadge.test.tsx — un-skip the 5 describe.skip blocks added in plan 03-01 task 3 and assert: (a) the rendered text content matches UI-SPEC labels for each status, (b) the data-tone attribute matches the tone mapping (open/acknowledged→info, assigned/in_progress→warn, resolved→accent, closed→muted), (c) acknowledged renders the ring dot (assert via box-shadow inline style or a data-dot-treatment attribute), (d) in_progress renders a dot with className containing pulse-dot
  </behavior>
  <action>
    Edit frontend/app/admin/admin.css. Find the .dir-b block (light mode) and locate where existing --status-* tokens are defined (`--status-submitted`, `--status-review`, `--status-resolved`). Add the 15 new variables immediately below the existing ones using the exact oklch values from UI-SPEC §E light block. Do NOT remove the old `--status-submitted` and `--status-review` tokens — they remain available for backwards compat if any legacy component still references them. Then in the .dir-b.dark block, add the same 15 variables with the dark values from UI-SPEC §E dark block. Also add the `@keyframes pulse-dot` and `.pulse-dot` class at the bottom of the .dir-b block if not already present (search for "pulse-dot" first; only add if missing). Citation: D-37, D-38.

    Edit frontend/app/admin/components/StatusBadge.tsx:
    - Expand StatusValue type to the union of 6 string literals
    - Update StatusMeta interface: add `dotTreatment: "filled" | "ring" | "pulse"`
    - Expand STATUS_MAP with 5 new entries (keys: open, acknowledged, assigned, in_progress, closed) — each entry uses tone + var(--status-*-bg) + var(--*-ink) per UI-SPEC §Status Color System table. The existing `resolved` entry is kept. The existing `submitted` and `under_review` entries are RENAMED in place: `submitted` is replaced with `open`'s entry (same key, new label "Open"); `under_review` is replaced with `acknowledged`'s entry. After this rename, the STATUS_MAP keys are exactly the 6 new values.
    - Update the rendered dot span: when `m.dotTreatment === "ring"`, override `background: transparent; boxShadow: inset 0 0 0 1.5px ${m.dot}`; when `m.dotTreatment === "pulse"`, add `className="pulse-dot"` to the dot span
    - Update the unknown-status fallback at line 65 to use the `open` entry as the fallback base (not the removed `submitted`)
    - Add tone="muted" support — the existing tone union is "info" | "warn" | "accent"; expand to "info" | "warn" | "accent" | "muted". For tone="muted", toneStyle uses `background: var(--surface-2); color: var(--muted); border: 1px solid var(--border)` — `closed` is the only muted entry

    Edit frontend/app/admin/lib/adminApi.ts:
    - Add new exported interface `WardHierarchy` with the 10 fields (per behavior section)
    - Extend AdminReport with the 4 new fields
    - Change AdminStats.by_status type to the 6-key shape per D-03
    - Add `export async function resolveReport(...)` using fetch directly (cannot use apiFetch helper because we must NOT set Content-Type — see Pattern 3 in research):
      ```
      const form = new FormData();
      form.append("status", status);
      form.append("resolution_photo", photo);
      if (notes) form.append("resolution_notes", notes);
      const res = await fetch(`${BASE}/api/admin/reports/${id}/resolve`, { method: "POST", credentials: "include", body: form });
      if (!res.ok) throw new Error(`HTTP ${res.status}`);
      return res.json();
      ```
    - Add `export async function assignReportOrg(id, orgId)` using the apiFetch helper (JSON POST)

    Edit frontend/app/admin/components/__tests__/StatusBadge.test.tsx:
    - Un-skip the 5 describe blocks added by plan 03-01 task 3 (`describe.skip` → `describe`)
    - Fill in `it("renders Open label and info tone", ...)`-style tests per behavior section. Assert via `screen.getByTestId('status-badge')` then check `toHaveAttribute('data-tone', '...')`, `toHaveTextContent('...')`, and the dot treatment via `data-dot-treatment` attribute on the StatusBadge (add this data attribute to the dot span via `data-dot-treatment={m.dotTreatment}` so it is testable)

    Run `cd frontend && npm test -- --watchAll=false StatusBadge adminApi 2>&1`. All StatusBadge tests (existing + 5 new) and adminApi tests must remain green. NO regressions in existing test counts.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --watchAll=false --passWithNoTests StatusBadge adminApi 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\-\\-status-closed:" frontend/app/admin/admin.css` returns at least 2 (light + dark blocks)
    - `grep -c "\\-\\-status-in-progress:" frontend/app/admin/admin.css` returns at least 2
    - `grep -c "\\-\\-status-acknowledged:" frontend/app/admin/admin.css` returns at least 2
    - `grep -c "@keyframes pulse-dot\|\\.pulse-dot" frontend/app/admin/admin.css` returns at least 2
    - `grep -c "open\\|acknowledged\\|assigned\\|in_progress\\|resolved\\|closed" frontend/app/admin/components/StatusBadge.tsx | head -1` — STATUS_MAP keys are all 6 values (verifiable manually via inspection)
    - `grep -c "dotTreatment" frontend/app/admin/components/StatusBadge.tsx` returns at least 3 (interface, STATUS_MAP entries, rendered span)
    - `grep -c "muted" frontend/app/admin/components/StatusBadge.tsx` returns at least 1 (tone="muted" branch for closed)
    - `grep -c "export async function resolveReport" frontend/app/admin/lib/adminApi.ts` returns 1
    - `grep -c "export async function assignReportOrg" frontend/app/admin/lib/adminApi.ts` returns 1
    - `grep -c "export interface WardHierarchy" frontend/app/admin/lib/adminApi.ts` returns 1
    - `grep -c "resolution_photo_url" frontend/app/admin/lib/adminApi.ts` returns at least 1
    - `grep -c "in_progress: number" frontend/app/admin/lib/adminApi.ts` returns 1 (AdminStats.by_status shape)
    - `cd frontend && npm test -- --watchAll=false --passWithNoTests StatusBadge 2>&1 | grep -E "Tests:.*passed"` shows pass count higher than before plan 03-03 (≥ existing + 5 new)
    - `cd frontend && npm run lint 2>&1 | grep -c "error" | head -1` returns 0
  </acceptance_criteria>
  <done>
    Tokens + StatusBadge + adminApi all reflect the 6-value enum and the new endpoints. Test suite green; StatusBadge tests cover all 6 values + 3 dot treatments. Plan 03-03 tasks 2-3 can compose against these primitives.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusActionPanel + OrgAssignPanel + ResolveModal + GbaHierarchyPanel components (tested)</name>
  <files>frontend/app/admin/components/StatusActionPanel.tsx, frontend/app/admin/components/OrgAssignPanel.tsx, frontend/app/admin/components/ResolveModal.tsx, frontend/app/admin/components/GbaHierarchyPanel.tsx, frontend/app/admin/components/__tests__/StatusActionPanel.test.tsx, frontend/app/admin/components/__tests__/OrgAssignPanel.test.tsx, frontend/app/admin/components/__tests__/ResolveModal.test.tsx, frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx</files>
  <read_first>
    - frontend/app/admin/components/Btn.tsx (variant prop API: "primary"|"secondary"|"ghost"|"accent"|"danger"; size prop)
    - frontend/app/admin/components/Input.tsx (textarea support; aria-* attributes)
    - frontend/app/admin/components/Select.tsx (Select primitive with options prop, value, onChange)
    - frontend/app/admin/components/Card.tsx (Card primitive — title, footer slots)
    - frontend/app/admin/components/Icon.tsx (Icon primitive with name prop)
    - frontend/app/admin/components/SectionLabel.tsx (eyebrow label primitive)
    - frontend/app/admin/lib/adminApi.ts (listOrganizations, assignReportOrg, resolveReport, updateReportStatus, AdminReport, WardHierarchy types — all from task 1)
    - .planning/phases/03-government-triage-workflow/03-UI-SPEC.md (sections §A StatusActionPanel, §B OrgAssignPanel, §C GbaHierarchyPanel, §D Resolve/Close Modal, §Copywriting Contract for exact button + alert + placeholder strings)
    - frontend/app/admin/components/__tests__/StatusActionPanel.test.tsx (Wave 0 scaffold from plan 03-01 task 3 — un-skip and implement the 6 status-state describe blocks)
    - frontend/app/admin/components/__tests__/OrgAssignPanel.test.tsx (Wave 0 scaffold — 3 describe blocks)
    - frontend/app/admin/components/__tests__/ResolveModal.test.tsx (Wave 0 scaffold — 3 describe blocks)
    - .planning/codebase/TESTING.md (jest.mock pattern for adminApi; data-testid pattern; userEvent fireEvent pattern)
  </read_first>
  <behavior>
    - StatusActionPanel.tsx (client component, "use client" directive) props: `{report: AdminReport, onStatusChange: (newStatus: string) => Promise<void>, onResolveClick: () => void, onAssignClick: () => void, onCloseClick: () => void, disabled?: boolean}`. Renders contextual button row based on `report.status`:
      - open → `<Btn variant="accent" onClick={() => onStatusChange("acknowledged")}>Acknowledge</Btn>`
      - acknowledged → `<Btn variant="secondary" onClick={onAssignClick}>Assign to organisation</Btn><Btn variant="accent" onClick={() => onStatusChange("in_progress")}>Mark as in progress</Btn>`
      - assigned → `<Btn variant="secondary" onClick={() => onStatusChange("in_progress")}>Mark as in progress</Btn><Btn variant="accent" onClick={onResolveClick}>Resolve</Btn>`
      - in_progress → `<Btn variant="accent" onClick={onResolveClick}>Resolve</Btn>`
      - resolved → `<Btn variant="secondary" onClick={onCloseClick}>Close report</Btn>`
      - closed → `<div role="status">This report is closed.</div>` (static, no buttons)
      - Layout: horizontal flex, gap 8px, right-aligned inside a Card per UI-SPEC §A
      - Each button has data-testid for the test suite: `data-testid="action-acknowledge"`, `action-assign`, `action-mark-in-progress`, `action-resolve`, `action-close`
    - OrgAssignPanel.tsx props: `{report: AdminReport, onAssigned: (updated: AdminReport) => void}`. State: `mode: "view" | "edit"`, `selectedCorpId: string | null`, `selectedWardOfficeId: string | null`, `orgs: Organization[] | null`, `loading: boolean`, `error: string | null`. On mount and on mode→edit transition, calls `listOrganizations()` to populate orgs. Filters orgs by org_type per Pattern 7 in research: `corporations = orgs.filter(o => o.org_type === 'corporation')`, `wardOffices = selectedCorpId ? orgs.filter(o => o.org_type === 'ward_office' && o.parent_id === selectedCorpId) : []`. View state: "No organisation assigned" with danger tag when assigned_org_id is null, else the corporation + ward office names with a "Change" ghost button. Edit state: two Select dropdowns side by side (Corporation required, Ward Office filtered) + "Save assignment" accent button + "Discard changes" ghost. On save: call `assignReportOrg(report.id, wardOfficeId || corpId)` (per D-11 ward_office id preferred, fall back to corp id); on success call `onAssigned(updatedReport)` and revert to view mode. data-testid attributes: `org-corp-select`, `org-ward-office-select`, `org-save`, `org-discard`, `org-status` (the displayed currently-assigned text).
    - ResolveModal.tsx props: `{open: boolean, mode: "resolve" | "close", report: AdminReport, onClose: () => void, onResolved: (updated: AdminReport) => void}`. State: `photoFile: File | null`, `notes: string`, `submitting: boolean`, `error: string | null`. Renders only when `open === true`. Title: "Resolve report" or "Close report" per mode. Photo dropzone: native `<input type="file" accept="image/jpeg,image/jpg">` with REQUIRED tag visible when photoFile is null; show preview thumbnail (via URL.createObjectURL) when photoFile is set; "Remove" button clears photoFile. Notes: `<Input as="textarea" placeholder="Optional — describe what was done" value={notes} onChange={...}>`. Audit echo: small muted block showing `WLK-{report.id.slice(0,8)} · {report.status} → {mode === "resolve" ? "Resolved" : "Closed"}` (no real WLK ID system exists yet — use first 8 hex chars of report.id). Submit button: `<Btn variant="accent" disabled={photoFile == null || submitting}>Mark as resolved</Btn>` or "Mark as closed". For mode==="close" also render an amber warning text "Closing is permanent. The report will be archived and removed from the active queue." (per UI-SPEC §D close variant warning). On submit: call `resolveReport(report.id, mode === "resolve" ? "resolved" : "closed", photoFile!, notes || undefined)`, on success call `onResolved(updated)` then `onClose()`. data-testid attributes: `resolve-photo-input`, `resolve-photo-preview`, `resolve-photo-remove`, `resolve-notes-input`, `resolve-submit`, `resolve-cancel`, `resolve-required-alert`, `close-warning` (mode-conditional).
    - GbaHierarchyPanel.tsx props: `{hierarchy: WardHierarchy | null}`. Renders nothing-state ("Ward assignment not available for this report." muted text) when hierarchy is null OR when hierarchy.ward_name is null. Otherwise renders two sub-sections inside a Card:
      - "BUREAUCRATIC CHAIN" eyebrow (SectionLabel mono uppercase). Then 6 rows in stacked order: Ward (label "Ward" + value `{ward_number} · {ward_name}` + title "Ward Engineer"), then ARO Sub Division (label "ARO Sub Division" + value `hierarchy.aro_sub_division` + title "Asst. Revenue Officer"), then RO Division (`hierarchy.ro_division` + "Revenue Officer"), then Zone (`hierarchy.zone_name` + "Zonal Commissioner"), then Corporation (`hierarchy.corporation + " Corporation, GBA"` + "Chief Commissioner"), then GBA ("Greater Bengaluru Authority" + "Chief Commissioner, GBA"). Each row uses `1px solid var(--border)` row separator and the layout per UI-SPEC §C bureaucratic chain layout block.
      - "ELECTED · ASSEMBLY" eyebrow. Below: `{hierarchy.assembly_constituency_no} – {hierarchy.assembly_constituency}` + MLA label `MLA: {hierarchy.mla_name}` + Parliamentary line `{hierarchy.parliamentary_constituency}` + MP label `MP: {hierarchy.mp_name}` + disclaimer footnote "Constituency boundaries may differ from ward boundaries." (per UI-SPEC §C elected chain + §Copywriting Contract).
      - When mla_name or mp_name is null, render the row but display the value as "—" muted; do not crash. ward_number is from AdminReport NOT from WardHierarchy — pass it as a separate prop OR include it in WardHierarchy from the adminApi types (cleanest: extend WardHierarchy to include ward_number: number | null).
    - All 4 component test files are un-skipped from describe.skip → describe and the placeholder `it` bodies are filled in. Use jest.mock for adminApi where needed (listOrganizations + assignReportOrg + resolveReport mocked).
    - cd frontend && npm test passes green with new test counts visible
  </behavior>
  <action>
    Create the 4 new component files in frontend/app/admin/components/. Each MUST:
    - Start with `"use client";` (per CONVENTIONS.md)
    - Import Btn, Input, Select, Card, Icon, SectionLabel from `./` (same directory)
    - Import types from `../lib/adminApi` (AdminReport, Organization, WardHierarchy)
    - Export as default (page-level scope) OR named (utility scope) — use NAMED exports per CONVENTIONS.md "UI library components use named exports"
    - Use CSS custom properties via inline `style` objects — NEVER Tailwind (per CONVENTIONS.md "all new work")
    - Use the exact copy strings from UI-SPEC §Copywriting Contract for every visible string

    For StatusActionPanel, the contextual button set is fully determined by report.status — implement a `switch (report.status)` block returning the appropriate JSX fragment. Pull copy strings from UI-SPEC §Copywriting Contract verbatim (sentence case, no CAPS_LOCK).

    For OrgAssignPanel, the cascade is implemented as Pattern 7 from 03-RESEARCH.md. UseEffect to fetch orgs on first render OR on mode→edit transition. When user picks a corporation, selectedWardOfficeId is reset to null. Save button is disabled until selectedCorpId is non-null. The save handler calls `assignReportOrg(report.id, selectedWardOfficeId ?? selectedCorpId!)` per D-11 (ward office preferred when chosen, corporation as fallback).

    For ResolveModal, use a controlled file input (do NOT use react-dropzone — no new deps allowed). The preview uses `URL.createObjectURL(photoFile)` and is cleaned up via URL.revokeObjectURL in a useEffect cleanup. The submit button calls `resolveReport(...)` and handles error+success states. The Submit click MUST be blocked when `photoFile === null` (WFLOW-05 frontend gate) — assert this in the test via `expect(submitBtn).toBeDisabled()`.

    For GbaHierarchyPanel, render is purely presentational — accept the hierarchy prop and the report.ward_id-derived ward_number (extend WardHierarchy in adminApi.ts to include `ward_number: number | null` — update task 1's interface definition if not already done). The "Bureaucratic chain" section is 6 rows with the (level label, name, title) tuple per UI-SPEC §C; the "Elected chain" section shows MLA + MP per D-42.

    Write the test files in frontend/app/admin/components/__tests__/. Un-skip the existing describe.skip blocks and implement them with @testing-library/react:
    - StatusActionPanel.test.tsx: 6 describe blocks, one per status value. Each renders `<StatusActionPanel report={...} ... />` with a stub report having that status, then asserts which buttons are present (`screen.getByTestId('action-acknowledge')` etc) and which are absent.
    - OrgAssignPanel.test.tsx: 3 describe blocks — (a) renders unassigned text when assigned_org_id is null + clicking Assign triggers cascade picker; (b) cascade: selecting a corporation populates ward office dropdown filtered by parent_id; (c) save button is disabled when no corporation selected, enabled after.
    - ResolveModal.test.tsx: 3 describe blocks — (a) renders required-alert when no photo; (b) submit button disabled until file is uploaded; (c) submitting calls resolveReport with FormData-equivalent args.
    - GbaHierarchyPanel.test.tsx (NEW — not scaffolded in plan 03-01 because UI-SPEC arrived later; create from scratch): (a) renders "Ward assignment not available for this report." when hierarchy is null; (b) renders all 6 bureaucratic rows when hierarchy populated; (c) renders MLA + MP names when present; (d) renders "—" placeholder when mla_name is null.

    Run `cd frontend && npm test -- --watchAll=false --passWithNoTests 2>&1` — all suites must pass.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --watchAll=false --passWithNoTests StatusActionPanel OrgAssignPanel ResolveModal GbaHierarchyPanel 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 4 component files exist: StatusActionPanel.tsx, OrgAssignPanel.tsx, ResolveModal.tsx, GbaHierarchyPanel.tsx
    - 4 test files exist with no remaining describe.skip in any of them: `grep -c "describe.skip" frontend/app/admin/components/__tests__/{StatusActionPanel,OrgAssignPanel,ResolveModal,GbaHierarchyPanel}.test.tsx` returns 0
    - `grep -c "data-testid=\"action-acknowledge\"" frontend/app/admin/components/StatusActionPanel.tsx` returns 1
    - `grep -c "data-testid=\"action-resolve\"" frontend/app/admin/components/StatusActionPanel.tsx` returns 1
    - `grep -c "data-testid=\"org-corp-select\"" frontend/app/admin/components/OrgAssignPanel.tsx` returns 1
    - `grep -c "data-testid=\"resolve-submit\"" frontend/app/admin/components/ResolveModal.tsx` returns 1
    - `grep -c "data-testid=\"resolve-photo-input\"" frontend/app/admin/components/ResolveModal.tsx` returns 1
    - `grep -c "resolveReport\\|assignReportOrg" frontend/app/admin/components/{StatusActionPanel,OrgAssignPanel,ResolveModal,GbaHierarchyPanel}.tsx 2>/dev/null` returns at least 2 (each external API is called in its respective component)
    - `grep -c "Closing is permanent" frontend/app/admin/components/ResolveModal.tsx` returns 1 (close variant warning per UI-SPEC §D)
    - `grep -c "Constituency boundaries may differ" frontend/app/admin/components/GbaHierarchyPanel.tsx` returns 1
    - `grep -c "Ward assignment not available" frontend/app/admin/components/GbaHierarchyPanel.tsx` returns 1
    - `cd frontend && npm test -- --watchAll=false --passWithNoTests 2>&1 | grep -c "Test Suites:.* failed"` returns 0
    - `cd frontend && npm run lint 2>&1 | grep -c "error"` returns 0
  </acceptance_criteria>
  <done>
    All 4 new admin components exist with comprehensive test coverage. WFLOW-03 (org assignment) and WFLOW-04 + WFLOW-05 (resolution notes + mandatory photo) are testable in isolation. Plan task 3 can compose these into the page.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire panels into /admin/reports/[id] page and add CORP column to /admin/reports list</name>
  <files>frontend/app/admin/reports/[id]/page.tsx, frontend/app/admin/reports/page.tsx, frontend/app/admin/components/ReportsTable.tsx</files>
  <read_first>
    - frontend/app/admin/reports/[id]/page.tsx (full file — current detail page; existing inline status PATCH buttons; existing layout sections; handleStatusMove function around line 95)
    - frontend/app/admin/reports/page.tsx (current list page top-level — observe how it passes columns to ReportsTable)
    - frontend/app/admin/components/ReportsTable.tsx (column headers array around line 894 — currently `["ID", "TIME", "CATEGORY", "WARD", "SEV", "STATUS", "DUP", "ACTIONS"]`; ward rendering at line 1011; identify where to insert CORP between WARD and SEV)
    - .planning/phases/03-government-triage-workflow/03-UI-SPEC.md (§F Reports List — Corporation Column; §A StatusActionPanel layout; §B OrgAssignPanel; §C GbaHierarchyPanel; §D ResolveModal trigger pattern)
    - frontend/app/admin/lib/adminApi.ts (AdminReport.ward_hierarchy from task 1 — used by GbaHierarchyPanel)
  </read_first>
  <behavior>
    - /admin/reports/[id]/page.tsx integrates the 4 new components:
      - At the top of the layout (after the header bar, before the existing photo + meta section): render `<StatusActionPanel report={report} onStatusChange={handleStatusMove} onResolveClick={() => setResolveModalState({open: true, mode: "resolve"})} onAssignClick={() => orgAssignPanelRef.current?.scrollIntoView()} onCloseClick={() => setResolveModalState({open: true, mode: "close"})} disabled={statusUpdateLoading} />`
      - Below: `<OrgAssignPanel report={report} onAssigned={setReport} />` (ref forwarded via React.forwardRef OR scroll-into-view via id="org-assign-panel" and DOM API)
      - Below that: `<GbaHierarchyPanel hierarchy={report.ward_hierarchy ?? null} />`
      - At the page root: `<ResolveModal open={resolveModalState.open} mode={resolveModalState.mode} report={report} onClose={() => setResolveModalState({open: false, mode: "resolve"})} onResolved={(updated) => { setReport(updated); }} />`
    - The existing inline status-PATCH button row (the legacy "Mark Under Review" / "Mark Resolved" buttons that currently call updateReportStatus directly) is REMOVED and replaced entirely by StatusActionPanel — only ONE source of status transitions remains
    - The existing handleStatusMove function continues to exist and is passed to StatusActionPanel as the onStatusChange prop — it already wraps updateReportStatus + setReport + error handling
    - The existing delete modal and PII display (admin_get_report_by_id may return submitter_name etc.) remain untouched
    - /admin/reports/page.tsx OR ReportsTable.tsx: add CORP column header between WARD and SEV per UI-SPEC §F. Header label "CORP" uppercase 10px mono (same style as other columns). Cell value: derived from `report.ward_hierarchy?.corporation` (admin endpoint returns ward.corporation via JOIN — verify via task 2 plan 03-02 SUMMARY that admin_get_report_by_id JSON includes corporation) OR computed from a static WARD_TO_CORP map at module level if ward_hierarchy is not yet on the admin endpoint shape. Use short labels: "Central", "North", "East", "South", "West". When corporation is null/missing, render "—".
    - ReportsTable column count adjustment: any colSpan calculations referencing the 8-column count must update to 9
    - `cd frontend && npm test` passes green with no regressions in existing tests
    - `cd frontend && npm run build` succeeds with zero TypeScript errors
  </behavior>
  <action>
    Edit frontend/app/admin/reports/[id]/page.tsx:
    - Import the 4 new components at the top: `import StatusActionPanel from "../../components/StatusActionPanel"; import OrgAssignPanel from "../../components/OrgAssignPanel"; import GbaHierarchyPanel from "../../components/GbaHierarchyPanel"; import ResolveModal from "../../components/ResolveModal";`
    - Add new state hook: `const [resolveModalState, setResolveModalState] = useState<{open: boolean, mode: "resolve" | "close"}>({open: false, mode: "resolve"});`
    - In the JSX return body, find the existing inline status-action button row (look for the buttons that call `handleStatusMove("under_review")` or `handleStatusMove("resolved")` — these are the legacy buttons). Replace that entire button row with `<StatusActionPanel report={report} onStatusChange={handleStatusMove} onResolveClick={() => setResolveModalState({open: true, mode: "resolve"})} onCloseClick={() => setResolveModalState({open: true, mode: "close"})} onAssignClick={() => { document.getElementById("org-assign-panel")?.scrollIntoView({behavior: "smooth"}); }} disabled={statusUpdateLoading} />`
    - Below StatusActionPanel: `<div id="org-assign-panel"><OrgAssignPanel report={report} onAssigned={(updated) => setReport(updated)} /></div>`
    - Below OrgAssignPanel: `<GbaHierarchyPanel hierarchy={report.ward_hierarchy ?? null} />`
    - At the JSX root (sibling of the main page content, alongside the existing delete modal): `<ResolveModal open={resolveModalState.open} mode={resolveModalState.mode} report={report} onClose={() => setResolveModalState({open: false, mode: resolveModalState.mode})} onResolved={(updated) => { setReport(updated); setResolveModalState({open: false, mode: resolveModalState.mode}); }} />`
    - Update handleStatusMove to gracefully no-op when newStatus is "resolved" or "closed" (because these now flow through ResolveModal) — add an early `if (newStatus === "resolved" || newStatus === "closed") { setResolveModalState({open: true, mode: newStatus === "resolved" ? "resolve" : "close"}); return; }` guard at the top of handleStatusMove

    Edit frontend/app/admin/components/ReportsTable.tsx:
    - Find the column header array at line 894: `["ID", "TIME", "CATEGORY", "WARD", "SEV", "STATUS", "DUP", "ACTIONS"]`
    - Insert "CORP" between "WARD" and "SEV" → `["ID", "TIME", "CATEGORY", "WARD", "CORP", "SEV", "STATUS", "DUP", "ACTIONS"]`
    - Find the WARD cell render at line 1011 (`{report.ward_name ?? "—"}`); immediately after that `<td>`, insert a new `<td>` for CORP cell: `<td style={{...}}>{report.ward_hierarchy?.corporation ?? "—"}</td>` (admin endpoint may not yet include ward_hierarchy on the list response — if so, derive corporation from a separate field; verify by inspecting plan 03-02 SUMMARY for the admin_list_reports JSON shape). Use mono uppercase 10px font style matching the column header per UI-SPEC §F.
    - Adjust any column-count-dependent calc (such as the colSpan on expanded duplicate rows around line 54) from 8 to 9
    - Repeat for the card layout variant if it exists (look for ward rendering at the per-card level around line 318-321 or 463 — add a corresponding CORP line beneath ward in the card view)

    Edit frontend/app/admin/reports/page.tsx if it passes column config to ReportsTable explicitly — ensure no hardcoded "8-column" assertion remains; otherwise no change needed.

    Run `cd frontend && npm test -- --watchAll=false --passWithNoTests`. All admin/reports/[id] tests and ReportsTable tests must pass. Then `cd frontend && npm run build` and confirm zero TypeScript errors.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --watchAll=false --passWithNoTests 2>&1 | tail -10 && cd /home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/frontend && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<StatusActionPanel" frontend/app/admin/reports/[id]/page.tsx` returns 1
    - `grep -c "<OrgAssignPanel" frontend/app/admin/reports/[id]/page.tsx` returns 1
    - `grep -c "<GbaHierarchyPanel" frontend/app/admin/reports/[id]/page.tsx` returns 1
    - `grep -c "<ResolveModal" frontend/app/admin/reports/[id]/page.tsx` returns 1
    - `grep -c "resolveModalState" frontend/app/admin/reports/[id]/page.tsx` returns at least 3 (state declaration + setter + JSX)
    - `grep -E '"CORP"' frontend/app/admin/components/ReportsTable.tsx` returns at least 1 (column header)
    - `cd frontend && npm test -- --watchAll=false --passWithNoTests 2>&1 | grep -c "Test Suites:.* failed"` returns 0
    - `cd frontend && npm run build 2>&1 | grep -c "error TS"` returns 0
    - `cd frontend && npm run lint 2>&1 | grep -c "error"` returns 0
  </acceptance_criteria>
  <done>
    /admin/reports/[id] page composes all 4 new panels; /admin/reports list shows CORP column. Frontend build is green; admin UX is fully wired to the new backend endpoints from plan 03-02.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual + interaction UAT of admin lifecycle on local stack</name>
  <what-built>
    The full admin Phase 03 frontend stack: StatusBadge (6 states with dot treatments), StatusActionPanel (contextual buttons per status), OrgAssignPanel (cascading picker), ResolveModal (mandatory photo + optional notes), GbaHierarchyPanel (bureaucratic + elected chain), CORP column on admin reports list, and the full lifecycle wired through /admin/reports/[id] against the plan 03-02 backend.
  </what-built>
  <how-to-verify>
    1. Start the stack: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up db -d`, then `cd backend && cargo run` in one terminal, `cd frontend && npm run dev` in another
    2. Open http://localhost:3000/admin/login and sign in with the seed admin credentials from backend/.env
    3. Submit a test report at http://localhost:3000/report (any photo + Bengaluru pin)
    4. Navigate to http://localhost:3000/admin/reports — confirm the new CORP column appears between WARD and SEV; confirm the test report's CORP cell shows the correct corporation name
    5. Click into the test report at /admin/reports/{id} — confirm:
       - StatusActionPanel shows ONLY "Acknowledge" button (since status is "open")
       - OrgAssignPanel shows "No organisation assigned" in danger styling
       - GbaHierarchyPanel shows the 6-row bureaucratic chain (Ward → ARO Sub Division → RO Division → Zone → Corporation → GBA) with real backfilled values
       - GbaHierarchyPanel elected section shows the AC name + AC number + MLA name + Parliamentary constituency + MP name from migration 009 backfill
    6. Click "Acknowledge" — confirm status updates inline, badge changes to "Acknowledged" with ring (hollow) teal dot, StatusActionPanel switches to "Assign to organisation" + "Mark as in progress" buttons
    7. Click "Mark as in progress" — badge changes to amber pulsing dot, panel shows only "Resolve"
    8. Click "Resolve" — ResolveModal opens, title is "Resolve report", REQUIRED tag visible on dropzone, Submit button is DISABLED with `opacity: 0.5`
    9. Try to submit without a photo — submit button should not respond (disabled)
    10. Add a JPEG photo via the dropzone — preview appears, Submit button enables
    11. Optionally type a note in the textarea — "Footpath repaired by ward office contractor"
    12. Click "Mark as resolved" — modal closes, status badge changes to green "Resolved", the resolution photo + notes are now stored in DB
    13. Verify in db: `docker compose exec db psql -U walkability -d walkability -c "SELECT status, resolution_photo_path, resolution_notes FROM reports WHERE id = 'YOUR_ID'"`
    14. Click "Close report" — modal opens with title "Close report", amber close-warning visible ("Closing is permanent..."), submit button disabled until photo
    15. Provide photo and submit — status changes to muted/grey "Closed" badge; StatusActionPanel shows the locked "This report is closed." panel with no buttons
    16. Test OrgAssignPanel separately on another report: click "Assign" → cascading picker opens → select a Corporation → confirm Ward Office dropdown filters to children of that corporation → click Save → confirm status auto-advances to "Assigned" (D-09) and OrgAssignPanel switches to view mode showing `Corp ↳ Ward Office` names
    17. Verify both light AND dark mode: toggle theme via the existing admin theme toggle button — all 6 status badge colors render correctly in both modes; modal overlay readable; hierarchy panel readable
    18. Verify on mobile width (DevTools 375px): buttons remain ≥ 40px tap target; modal scrolls if needed; CORP column scrolls horizontally with the rest of the table
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 18 checkpoints pass.
    Otherwise: describe the issue (e.g. "Step 10 dropzone preview not showing on iOS Safari", "Step 16 ward office filter shows all wards instead of children of selected corp") — Claude will fix and re-checkpoint.
  </resume-signal>
  <files>n/a — checkpoint task; no files are edited by the executor; the previous 3 tasks produced all artifacts</files>
  <action>Execute the 18-point how-to-verify checklist above against the running local stack (docker compose db + cargo run backend + npm run dev frontend). Confirm every checklist item passes; on any failure, stop and surface the failing point to the user with reproduction steps. The executor performs NO code edits in this task — fixes are made via a separate follow-up edit and the checkpoint re-runs.</action>
  <verify>
    <human-check>All 18 manual verification points above PASS. The user types "approved" at the resume-signal prompt. If any point fails the executor surfaces the failure and pauses for the user to provide a fix direction.</human-check>
  </verify>
  <done>User types "approved" at the resume-signal; all 18 verification points have been visually + functionally confirmed against the running local stack.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → /api/admin/reports/:id/resolve (multipart) | Photo + status + optional notes from authenticated admin |
| Admin browser → /api/admin/reports/:id/assign-org (JSON) | org_id from authenticated admin |
| Admin browser → /api/admin/organizations | Reads org tree for cascade picker |
| ResolveModal → backend | Mandatory photo gate enforced both client (disabled submit) and server (validate_resolve_request) |
| Status mutation buttons → backend | Free transitions allowed per D-06; no client-side state-machine enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | ResolveModal submit without photo (client bypass via devtools) | accept | Client-side disabled button is UX guard only; server-side validate_resolve_request (plan 03-01) is the authoritative gate. Bypass via devtools is non-issue because server still rejects with 400. |
| T-03-03-02 | Information Disclosure | resolution_notes visible to all admins | accept | D-17 explicitly admin-only — visible to all authenticated admins, never to public. Phase 3 GBA-pivot scope. |
| T-03-03-03 | XSS via resolution_notes | resolution_notes textarea rendered back in detail page | mitigate | React's default JSX escaping prevents HTML injection; notes rendered as text content via `{report.resolution_notes}` — never via dangerouslySetInnerHTML; existing project pattern |
| T-03-03-04 | XSS via MLA/MP name display | wards.mla_name / mp_name from migration backfill rendered in GbaHierarchyPanel | mitigate | React JSX escaping; backfill data sourced from migration SQL (no user-controlled input); names are static published election data |
| T-03-03-05 | Tampering | Admin clicking "Close" twice before modal mounts | mitigate | resolveModalState single-source-of-truth pattern; clicking again re-opens the same modal in close mode (idempotent); submitting state disables Submit during in-flight request |
| T-03-03-06 | Spoofing | OrgAssignPanel submits with corporation that does not exist | mitigate | Backend FK constraint on assigned_org_id (plan 03-02 task 1) rejects nonexistent org_id with AppError::Database which surfaces as user-visible error toast |
| T-03-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan introduces zero new dependencies — all primitives (Btn, Input, Select, Card, Icon, SectionLabel) exist from Phase 02.5. No Package Legitimacy Gate required. |

</threat_model>

<verification>
After all tasks + checkpoint complete:
- `cd frontend && npm test -- --watchAll=false --passWithNoTests` is green with all 4 new test files passing
- `cd frontend && npm run build` is green with zero TypeScript errors
- `cd frontend && npm run lint` is green with zero ESLint errors
- Human-verify checkpoint signed off with all 18 verification points
- /admin/reports list shows CORP column
- /admin/reports/[id] shows full Phase 03 admin lifecycle UI
</verification>

<success_criteria>
- StatusBadge renders all 6 status values with correct dot treatments (filled/ring/pulse) and color tokens
- StatusActionPanel renders the exact button set per status per UI-SPEC §A
- OrgAssignPanel renders cascade picker and persists org assignment + auto-advances status (D-09)
- ResolveModal blocks submit until photo is uploaded (WFLOW-05 client gate)
- GbaHierarchyPanel renders bureaucratic + elected chains with named MLA/MP from migration 009
- CORP column visible on admin reports list
- Full lifecycle (Open → Acknowledged → Assigned → In Progress → Resolved → Closed) operable through the UI
- All frontend tests green; build clean; lint clean
- Human-verify checkpoint passes
</success_criteria>

<output>
Create `.planning/phases/03-government-triage-workflow/03-03-SUMMARY.md` when done, listing: components created, tokens added, tests added (counts before/after), screenshots of light + dark mode at /admin/reports/[id], and the curl smoke verification of the resolve endpoint against the running stack.
</output>
