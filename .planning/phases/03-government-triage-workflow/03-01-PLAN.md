---
phase: 03-government-triage-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/008_workflow.sql
  - backend/migrations/009_ward_hierarchy.sql
  - backend/src/models/report.rs
  - backend/src/models/admin.rs
  - backend/src/db/admin_queries.rs
  - backend/src/handlers/admin.rs
  - backend/src/db/queries.rs
  - backend/.sqlx
autonomous: true
requirements:
  - WFLOW-01
  - WFLOW-02
user_setup: []
tags:
  - postgres
  - migration
  - rust
  - sqlx
  - enum

must_haves:
  truths:
    - "Migration 008 renames report_status values submitted→open and under_review→acknowledged and adds new values assigned, in_progress, closed (D-03, D-04, D-05)"
    - "Migration 008 adds resolution_photo_path, resolution_notes, assigned_org_id columns to reports table (D-13, D-15, D-08)"
    - "Migration 009 adds zone_name, ro_division, aro_sub_division, assembly_constituency, assembly_constituency_no, parliamentary_constituency, mla_name, mp_name columns to wards table and backfills all 369 rows (D-21, D-41, D-43, D-44)"
    - "validate_status accepts all 6 new values (open, acknowledged, assigned, in_progress, resolved, closed) and rejects the renamed values submitted and under_review (WFLOW-01)"
    - "get_report_stats seed array contains 6 status keys (open, acknowledged, assigned, in_progress, resolved, closed) — no stale submitted/under_review keys remain"
    - "Report DB struct, ReportResponse, StatsResponse, and UpdateStatusRequest reflect 6-value status enum and new resolution + assignment columns"
    - "Wave 0 test scaffolds exist for validate_status (6 values + reject old names), Resolve modal photo gate (Rust), StatusBadge (6 entries), ResolveModal.tsx, OrgAssignPanel.tsx, StatusActionPanel.tsx, ReportsMap STATUS_COLORS"
    - "cargo test + cargo clippy -- -D warnings pass with green output after all migrations apply on the local docker-compose db"
  artifacts:
    - path: "backend/migrations/008_workflow.sql"
      provides: "report_status ENUM rename + add values + new reports columns"
      contains: "no-transaction"
    - path: "backend/migrations/009_ward_hierarchy.sql"
      provides: "wards hierarchy columns + 369-row backfill"
      contains: "ALTER TABLE wards"
    - path: "backend/src/models/report.rs"
      provides: "Report + ReportResponse extended with resolution_photo_path, resolution_notes, assigned_org_id, resolution_photo_url"
      contains: "resolution_photo_path"
    - path: "backend/src/db/queries.rs"
      provides: "get_report_by_id SELECT extended with new columns"
      contains: "resolution_photo_path"
    - path: "frontend/app/admin/components/__tests__/StatusBadge.test.tsx"
      provides: "6-status badge entry test scaffold"
      contains: "in_progress"
    - path: "frontend/app/admin/components/__tests__/ResolveModal.test.tsx"
      provides: "Wave 0 ResolveModal test scaffold"
      contains: "ResolveModal"
    - path: "frontend/app/admin/components/__tests__/OrgAssignPanel.test.tsx"
      provides: "Wave 0 OrgAssignPanel test scaffold"
      contains: "OrgAssignPanel"
    - path: "frontend/app/admin/components/__tests__/StatusActionPanel.test.tsx"
      provides: "Wave 0 StatusActionPanel test scaffold"
      contains: "StatusActionPanel"
  key_links:
    - from: "backend/src/db/queries.rs::get_report_by_id"
      to: "Report struct in backend/src/models/report.rs"
      via: "sqlx::query_as::<_, Report> SELECT list"
      pattern: "resolution_photo_path"
    - from: "backend/src/db/admin_queries.rs::get_report_stats"
      to: "StatsResponse seed key list"
      via: "hard-coded array literal"
      pattern: "\\[\"open\", \"acknowledged\", \"assigned\", \"in_progress\", \"resolved\", \"closed\"\\]"
    - from: "backend/src/handlers/admin.rs::validate_status"
      to: "match arm for 6 status values"
      via: "Rust match expression"
      pattern: "open.*acknowledged.*assigned.*in_progress.*resolved.*closed"
---

<objective>
Land the schema and type foundation for Phase 03 government triage workflow per D-03, D-04, D-05, D-08, D-13, D-15, D-21, D-41, D-43, D-44. Two migrations (008 + 009), Rust struct extensions, validate_status + stats seed updates, plus Wave 0 test scaffolds for downstream plans.

Purpose: WFLOW-01 (6-value status lifecycle) and the data foundation for WFLOW-02..WFLOW-05 are entirely backend-schema-bound. Plans 03-02, 03-03, 03-04 cannot proceed until this plan ships.

Output:
- Migration 008_workflow.sql (no-transaction; ENUM rename + add values + new reports columns)
- Migration 009_ward_hierarchy.sql (wards hierarchy columns + 369-row backfill)
- backend/src/models/report.rs (Report + ReportResponse extension)
- backend/src/models/admin.rs (UpdateStatusRequest + StatsResponse seed list update; is_valid_status() match arms)
- backend/src/db/admin_queries.rs (get_report_stats seed array + admin_get_report_by_id SELECT extension)
- backend/src/db/queries.rs (get_report_by_id SELECT extension)
- backend/src/handlers/admin.rs (validate_status match arms; new validate_resolve_request pure helper)
- backend/.sqlx (regenerated metadata via cargo sqlx prepare)
- Wave 0 frontend test scaffolds for plans 03-03 and 03-04
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-government-triage-workflow/03-CONTEXT.md
@.planning/phases/03-government-triage-workflow/03-RESEARCH.md
@.planning/phases/03-government-triage-workflow/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@backend/migrations/001_init.sql
@backend/migrations/002_admin.sql
@backend/migrations/004_ward_boundaries.sql
@backend/migrations/005_organizations.sql
@backend/migrations/007_anti_abuse.sql
@backend/src/models/report.rs
@backend/src/models/admin.rs
@backend/src/db/queries.rs
@backend/src/db/admin_queries.rs
@backend/src/handlers/admin.rs
@frontend/app/admin/components/StatusBadge.tsx
@data/gba_wards_2025.geojson
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 008 (ENUM rename + reports columns) + Migration 009 (wards hierarchy + backfill)</name>
  <files>backend/migrations/008_workflow.sql, backend/migrations/009_ward_hierarchy.sql, scripts/generate_009_backfill.py</files>
  <read_first>
    - backend/migrations/001_init.sql (current report_status enum definition + idx_reports_submitted_created partial index)
    - backend/migrations/004_ward_boundaries.sql (current wards table schema and per-ward INSERT pattern to mirror in 009)
    - backend/migrations/005_organizations.sql (organizations table — FK target for assigned_org_id)
    - backend/migrations/007_anti_abuse.sql (most recent migration; demonstrates transactional ALTER TABLE pattern)
    - data/gba_wards_2025.geojson (369 features with Corporation, zone_name, RO_Division, ARO_ Sub Division, ac, ac_no, ward_id — source for 009 backfill)
    - .planning/phases/03-government-triage-workflow/03-RESEARCH.md (Pattern 1, Migration 008 + 009 templates; Migration 009 backfill section with Option A 369-UPDATE recommendation; AC→Lok Sabha mapping; MLA names table; MP names table)
    - .planning/phases/03-government-triage-workflow/03-CONTEXT.md (D-03..D-05 ENUM rename rules, D-21, D-41, D-43, D-44 hierarchy columns)
  </read_first>
  <behavior>
    - Migration 008 first non-empty line is exactly `-- no-transaction` (pragma sqlx requires to allow ALTER TYPE ADD VALUE)
    - Migration 008 renames `submitted`→`open` and `under_review`→`acknowledged` on report_status via two `ALTER TYPE report_status RENAME VALUE` statements
    - Migration 008 adds new enum values `assigned` (after `acknowledged`), `in_progress` (after `assigned`), `closed` (after `resolved`)
    - Migration 008 DROPs the old `idx_reports_submitted_created` partial index and recreates it as `idx_reports_open_created ON reports(created_at DESC) WHERE status = 'open'`
    - Migration 008 adds columns to `reports`: `resolution_photo_path TEXT`, `resolution_notes TEXT`, `assigned_org_id UUID REFERENCES organizations(id) ON DELETE SET NULL`
    - Migration 008 creates `idx_reports_assigned_org ON reports(assigned_org_id) WHERE assigned_org_id IS NOT NULL`
    - Migration 009 has no `-- no-transaction` pragma (default transaction)
    - Migration 009 adds 8 columns to `wards`: `zone_name TEXT`, `ro_division TEXT`, `aro_sub_division TEXT`, `assembly_constituency TEXT`, `assembly_constituency_no INT`, `parliamentary_constituency TEXT`, `mla_name TEXT`, `mp_name TEXT`
    - Migration 009 contains 369 individual `UPDATE wards SET ... WHERE ward_number = N` statements (one per ward in GeoJSON) with non-null values for all 8 columns where source data is available
    - Migration 009 creates 3 indexes: `idx_wards_assembly_constituency_no ON wards(assembly_constituency_no)`, `idx_wards_parliamentary_constituency ON wards(parliamentary_constituency)`, `idx_wards_zone_name ON wards(zone_name)`
    - scripts/generate_009_backfill.py emits the 369 UPDATE statements to stdout — reads data/gba_wards_2025.geojson, joins each feature's ac_no to the hardcoded AC→Lok Sabha + MLA + MP tables from 03-RESEARCH.md (sections "D-24 Research Results: MLA Names" and "D-24 Research Results: MP Names", and "Migration 009: AC→Lok Sabha Mapping")
    - After applying both migrations on the local docker-compose db, `SELECT COUNT(*) FROM wards WHERE mla_name IS NOT NULL` returns 369 and `SELECT DISTINCT parliamentary_constituency FROM wards ORDER BY 1` returns 4 rows (Bangalore Central, Bangalore North, Bangalore Rural, Bangalore South)
    - Test: `SELECT unnest(enum_range(NULL::report_status))::TEXT` returns exactly the 6 ordered values open, acknowledged, assigned, in_progress, resolved, closed
  </behavior>
  <action>
    Generate 008_workflow.sql by hand following the exact template in 03-RESEARCH.md "Pattern 1, Migration 008 (no-transaction)" block. Strip `'submitted'` literal references everywhere — partial index recreated to use `WHERE status = 'open'` and renamed to `idx_reports_open_created` (per pitfall 2). FK reference for `assigned_org_id` is `organizations(id)` from migration 005.

    Write scripts/generate_009_backfill.py as a one-shot Python script (no new deps; uses stdlib json) that:
    1) Reads `data/gba_wards_2025.geojson`
    2) Hardcodes the AC_NO→PC dict, AC_NO→MLA dict, PC→MP dict from 03-RESEARCH.md tables verbatim (use exact ac_no integer keys, e.g. 150..177; PC values are "Bangalore Central", "Bangalore North", "Bangalore Rural", "Bangalore South"; MLA names exactly as listed; MP names exactly as listed)
    3) For each feature, extracts ward_number=int(properties["ward_id"]), zone_name=properties["zone_name"], ro_division=properties["RO_Division"], aro_sub_division=properties["ARO_ Sub Division"], assembly_constituency=properties["ac"], assembly_constituency_no=int(properties["ac_no"])
    4) Looks up parliamentary_constituency = AC_TO_PC[ac_no], mla_name = AC_TO_MLA[ac_no], mp_name = PC_TO_MP[parliamentary_constituency]
    5) Emits one UPDATE statement per ward with single-quoted string values (escape inner apostrophes by doubling them)

    Run the script: `python3 scripts/generate_009_backfill.py > /tmp/009_backfill.sql`. Hand-assemble 009_ward_hierarchy.sql with: (a) ALTER TABLE wards ADD COLUMN block (8 columns), (b) the 369 UPDATE block from the script output, (c) the 3 CREATE INDEX statements. The script remains in the repo so future re-runs (after MLA/MP data refresh per D-43, D-44) can regenerate the SQL.

    All 8 ALTER TABLE wards ADD COLUMN statements MUST NOT include NOT NULL — backfill fills them in the same migration, but new ward rows added later may legitimately leave them NULL (per D-43, D-44 manual-update cycle every ~5 years).

    Verify by running both migrations on the local stack per .planning/codebase/STACK.md: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up db -d` then `cd backend && cargo run` (sqlx::migrate! applies both on startup). Confirm log output shows "applied migration 008_workflow" and "applied migration 009_ward_hierarchy" without error. Citations: D-03, D-04, D-05, D-08, D-13, D-15, D-21, D-41, D-43, D-44 (locked decisions).
  </action>
  <verify>
    <automated>cd backend && cargo test --no-run 2>&1 | tail -5</automated>
    <human-check>Start docker compose db service, run `cd backend && cargo run` and confirm both `008_workflow` and `009_ward_hierarchy` apply with no errors; then run `docker compose exec db psql -U walkability -d walkability -c "SELECT unnest(enum_range(NULL::report_status))::TEXT"` and confirm output is exactly: open, acknowledged, assigned, in_progress, resolved, closed (in that order). Run `docker compose exec db psql -U walkability -d walkability -c "SELECT COUNT(*) FROM wards WHERE mla_name IS NOT NULL"` and confirm output is 369.</human-check>
  </verify>
  <acceptance_criteria>
    - File `backend/migrations/008_workflow.sql` exists and its first non-empty, non-comment line is `-- no-transaction`
    - `grep -c "ALTER TYPE report_status RENAME VALUE 'submitted'" backend/migrations/008_workflow.sql` returns 1
    - `grep -c "ALTER TYPE report_status RENAME VALUE 'under_review'" backend/migrations/008_workflow.sql` returns 1
    - `grep -c "ADD VALUE 'assigned'" backend/migrations/008_workflow.sql` returns 1; same for `'in_progress'` and `'closed'`
    - `grep -c "DROP INDEX IF EXISTS idx_reports_submitted_created" backend/migrations/008_workflow.sql` returns 1
    - `grep -c "WHERE status = 'open'" backend/migrations/008_workflow.sql` returns at least 1
    - `grep -c "resolution_photo_path TEXT" backend/migrations/008_workflow.sql` returns 1; same for `resolution_notes TEXT`
    - `grep -c "assigned_org_id UUID REFERENCES organizations(id) ON DELETE SET NULL" backend/migrations/008_workflow.sql` returns 1
    - File `backend/migrations/009_ward_hierarchy.sql` exists; `grep -E "^UPDATE wards SET" backend/migrations/009_ward_hierarchy.sql | wc -l` returns 369
    - `grep -c "ALTER TABLE wards" backend/migrations/009_ward_hierarchy.sql` returns at least 1; statement adds all 8 columns (zone_name, ro_division, aro_sub_division, assembly_constituency, assembly_constituency_no, parliamentary_constituency, mla_name, mp_name)
    - `grep -c "CREATE INDEX idx_wards_assembly_constituency_no" backend/migrations/009_ward_hierarchy.sql` returns 1; same for `idx_wards_parliamentary_constituency` and `idx_wards_zone_name`
    - `grep -c "mp_name = 'Tejasvi Surya'" backend/migrations/009_ward_hierarchy.sql` returns at least 1 (verifies Bangalore South backfill)
    - `grep -c "mla_name = 'Munirathna'" backend/migrations/009_ward_hierarchy.sql` returns at least 1 (verifies AC 154 backfill)
    - File `scripts/generate_009_backfill.py` exists; `python3 scripts/generate_009_backfill.py | grep -E "^UPDATE wards SET" | wc -l` outputs 369
    - Both migrations apply cleanly per human-check; no PostgreSQL syntax errors
  </acceptance_criteria>
  <done>
    Migrations 008 and 009 land on disk, both apply on a fresh docker-compose db, generator script is in the repo, and the 6-value enum + 369-row hierarchy backfill is observable in the live db.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rust struct extensions, validate_status + UpdateStatusRequest update, StatsResponse seed fix, validate_resolve_request helper</name>
  <files>backend/src/models/report.rs, backend/src/models/admin.rs, backend/src/db/queries.rs, backend/src/db/admin_queries.rs, backend/src/handlers/admin.rs, backend/.sqlx</files>
  <read_first>
    - backend/src/models/report.rs (Report struct lines 7-56; ReportResponse struct lines 59-74; into_response impl lines 76-94; existing tests at bottom of file as the contract baseline)
    - backend/src/models/admin.rs (UpdateStatusRequest at line 171; is_valid_status() at line 190; StatsResponse at line 231; existing tests in `Suite 7 — UpdateStatusRequest validation` at line 1127 and `Suite 6 — StatsResponse serialization` at line 1031)
    - backend/src/db/queries.rs (get_report_by_id at line 185 — current SELECT list)
    - backend/src/db/admin_queries.rs (get_report_stats at line 586; seed array at lines 592-596; update_report_status at line 528)
    - backend/src/handlers/admin.rs (validate_status at line 115; existing test module starting at line 882: test_validate_status_submitted, test_validate_status_under_review, test_validate_status_resolved, test_validate_status_rejected, test_validate_status_uppercase, test_validate_status_empty, test_validate_status_whitespace)
    - .planning/codebase/CONVENTIONS.md (error handling section: AppError::BadRequest pattern; tracing macros; sqlx raw string literals; #[allow(dead_code)] pattern; require_role pattern)
    - .planning/codebase/TESTING.md (backend pure-function unit test approach; #[cfg(test)] mod tests block; make_report fixture pattern in report.rs)
  </read_first>
  <behavior>
    - Report struct gains 3 new fields: `resolution_photo_path: Option<String>`, `resolution_notes: Option<String>`, `assigned_org_id: Option<Uuid>` (each tagged with `#[allow(dead_code)]` with a one-line comment per CONVENTIONS.md if not yet read in production handlers)
    - ReportResponse struct gains 1 new field: `resolution_photo_url: Option<String>` (serialized only when present per `#[serde(skip_serializing_if = "Option::is_none")]`) per D-18 (public after-photo visibility) and Pitfall 6 in 03-RESEARCH.md
    - Report::into_response builds `resolution_photo_url = self.resolution_photo_path.map(|p| format!("{}/uploads/{}", api_base, p))` — same URL construction pattern as image_url (consistent with existing test test_into_response_builds_correct_image_url)
    - make_report fixture in `#[cfg(test)] mod tests` is extended to initialise the 3 new fields to None / Option default — all existing tests continue to compile and pass
    - validate_status match arm is replaced with: `"open" | "acknowledged" | "assigned" | "in_progress" | "resolved" | "closed" => Ok(())`
    - UpdateStatusRequest::is_valid_status matches the same 6 values
    - StatsResponse seed array in admin_queries.rs::get_report_stats becomes `["open", "acknowledged", "assigned", "in_progress", "resolved", "closed"]` per Pitfall 4
    - get_report_by_id SELECT list adds `resolution_photo_path, resolution_notes, assigned_org_id` to the SELECT
    - New pure helper `validate_resolve_request(new_status: &str, photo_bytes_len: usize) -> Result<(), AppError>` in handlers/admin.rs returns Err(BadRequest("Resolution photo required")) when new_status is "resolved" or "closed" and photo_bytes_len is 0 (per D-13, D-14, Pitfall in research; WFLOW-05 backend gate)
    - Existing tests `test_validate_status_submitted`, `test_validate_status_under_review` get inverted assertions (these renamed values now MUST be rejected) and renamed to `test_validate_status_rejects_renamed_submitted` and `test_validate_status_rejects_renamed_under_review`
    - New tests added in handlers/admin.rs test module: `test_validate_status_accepts_open`, `test_validate_status_accepts_acknowledged`, `test_validate_status_accepts_assigned`, `test_validate_status_accepts_in_progress`, `test_validate_status_accepts_closed` (resolved already exists), plus `test_validate_resolve_requires_photo_for_resolved`, `test_validate_resolve_requires_photo_for_closed`, `test_validate_resolve_allows_non_terminal_without_photo`
    - cargo test, cargo clippy -- -D warnings, cargo sqlx prepare --database-url "postgres://walkability:walkability@localhost:5432/walkability" all succeed
  </behavior>
  <action>
    Edit backend/src/models/report.rs: add 3 new fields to Report struct (after submitter_ip on line 55) with `#[allow(dead_code)]` and brief comment "Phase 03 (D-08, D-13, D-15): set by resolve/assign handlers". Add `resolution_photo_url: Option<String>` field to ReportResponse with `#[serde(skip_serializing_if = "Option::is_none")]`. Extend `Report::into_response` to compute `resolution_photo_url` via the same `format!("{}/uploads/{}", api_base, ...)` pattern as image_url. Extend the inline test fixture `make_report` to initialise the 3 new fields to None / Default. Existing rounding and privacy tests must continue to pass unmodified.

    Edit backend/src/models/admin.rs: change `is_valid_status` match arms to the 6 new values (per D-03). Update each `Suite 7 — UpdateStatusRequest validation` test to use the new value strings — replace the 3 existing "valid" tests (submitted, under_review, resolved) with 6 tests covering the 6 new values, and update the "rejected" tests so the renamed values now fall on the rejection path. Update StatsResponse doc comment "Keys: \"submitted\", \"under_review\", \"resolved\"" to "Keys: \"open\", \"acknowledged\", \"assigned\", \"in_progress\", \"resolved\", \"closed\"". Update `Suite 6 — StatsResponse serialization` tests to assert all 6 new keys appear in the by_status map.

    Edit backend/src/db/queries.rs::get_report_by_id: extend the raw SQL string literal SELECT list to include `resolution_photo_path, resolution_notes, assigned_org_id` (per Pitfall 9 — JOIN comes in plan 03-02; this task only extends the SELECT against `reports` table since these columns are on reports, not wards). The static test `get_ward_for_point_uses_correct_coordinate_order` is unaffected.

    Edit backend/src/db/admin_queries.rs::get_report_stats: replace the seed array at lines 592-596 with `["open", "acknowledged", "assigned", "in_progress", "resolved", "closed"]`. If admin_get_report_by_id also serializes report fields, extend its SELECT to include the 3 new columns and add them to the returned JSON value (per CONVENTIONS.md AdminReport JSON shape pattern). Citation: D-03..D-05.

    Edit backend/src/handlers/admin.rs::validate_status: replace match arms at line 117 with `"open" | "acknowledged" | "assigned" | "in_progress" | "resolved" | "closed" => Ok(())`. Add new pure helper `validate_resolve_request` directly below validate_status — signature `pub fn validate_resolve_request(new_status: &str, photo_bytes_len: usize) -> Result<(), AppError>`, returns BadRequest("Resolution photo required") when new_status in {resolved, closed} and photo_bytes_len == 0 (per D-13, D-14; WFLOW-05). All 7 existing validate_status tests are rewritten per the behavior section; new tests added per the same spec.

    After all edits, run from repo root: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up db -d` (only db needs to be up), then `cd backend && cargo run` once (to apply migrations 008+009), Ctrl+C, then `cargo sqlx prepare --database-url "postgres://walkability:walkability@localhost:5432/walkability"` to regenerate `.sqlx/` metadata for the new SELECT in get_report_by_id. Commit the regenerated `.sqlx/` JSON files alongside the Rust changes.

    NO new handler routes are added in this task — the resolve/assign-org route registration in main.rs and their handler bodies are scoped to plan 03-02. This task delivers ONLY: schema, struct types, pure validators, stats seed list, the get_report_by_id SELECT extension, and Wave 0 test rewrites.
  </action>
  <verify>
    <automated>cd backend && cargo test validate_status validate_resolve into_response is_valid_status stats_response --no-fail-fast 2>&1 | tail -40</automated>
    <human-check>Run `cd backend && cargo clippy -- -D warnings` and confirm zero warnings; run `cd backend && cargo test` and confirm 100% pass; spot-check `backend/.sqlx/` was regenerated by checking git status.</human-check>
  </verify>
  <acceptance_criteria>
    - `cd backend && cargo test validate_status 2>&1 | grep -E "test result.*ok\. [0-9]+ passed"` outputs `passed` with count ≥ 8 (3 happy + 5 rejection-renamed + new helper tests)
    - `cd backend && cargo test validate_resolve` outputs `test result: ok` with at least 3 tests passed
    - `grep -E '^\s*\"open\" \| \"acknowledged\" \| \"assigned\" \| \"in_progress\" \| \"resolved\" \| \"closed\"' backend/src/handlers/admin.rs` returns at least 1 line (validate_status match arm)
    - `grep -c "\\\"open\\\", \\\"acknowledged\\\", \\\"assigned\\\", \\\"in_progress\\\", \\\"resolved\\\", \\\"closed\\\"" backend/src/db/admin_queries.rs` returns at least 1 (StatsResponse seed)
    - `grep -c "resolution_photo_path" backend/src/models/report.rs` returns at least 2 (field + into_response usage)
    - `grep -c "resolution_photo_url" backend/src/models/report.rs` returns at least 2 (field declaration + assignment in into_response)
    - `grep -c "resolution_photo_path" backend/src/db/queries.rs` returns at least 1 (SELECT list extension)
    - `grep -c "pub fn validate_resolve_request" backend/src/handlers/admin.rs` returns 1
    - `cd backend && cargo clippy -- -D warnings 2>&1 | grep -c "warning:"` returns 0
    - `git status backend/.sqlx/ | grep -E "modified|new file"` shows changes (sqlx prepare ran)
    - `cd backend && cargo test 2>&1 | grep -E "test result: ok\. [0-9]+ passed; 0 failed"` matches (full suite green)
  </acceptance_criteria>
  <done>
    All Rust types and pure validators reflect the 6-value enum and the new resolve/assign fields. cargo test green, cargo clippy clean, .sqlx metadata regenerated. Plans 03-02 and 03-03 can build on this foundation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 frontend test scaffolds for StatusBadge, ResolveModal, OrgAssignPanel, StatusActionPanel, ReportsMap</name>
  <files>frontend/app/admin/components/__tests__/StatusBadge.test.tsx, frontend/app/admin/components/__tests__/ResolveModal.test.tsx, frontend/app/admin/components/__tests__/OrgAssignPanel.test.tsx, frontend/app/admin/components/__tests__/StatusActionPanel.test.tsx, frontend/app/components/__tests__/ReportsMap.test.tsx</files>
  <read_first>
    - frontend/app/admin/components/__tests__/StatusBadge.test.tsx (existing tests; understand current 3-status coverage and assertion style)
    - frontend/app/components/__tests__/ReportsMap.test.tsx (existing test scaffold; understand react-leaflet mock at frontend/__mocks__/reactLeaflet.js — CircleMarker/Popup are mocked)
    - frontend/app/admin/components/StatusBadge.tsx (current STATUS_MAP at line 21; current StatusValue type at line 5)
    - .planning/phases/03-government-triage-workflow/03-UI-SPEC.md (sections: Status Color System — 6-state mapping table; StatusActionPanel A; OrgAssignPanel B; Resolve/Close Modal D; copywriting contract for exact button labels)
    - .planning/phases/03-government-triage-workflow/03-VALIDATION.md (Wave 0 Requirements section)
    - .planning/codebase/TESTING.md (Frontend Test Approach section — describe pattern, jest.mock pattern for adminApi, react-leaflet via moduleNameMapper)
  </read_first>
  <behavior>
    - StatusBadge.test.tsx is EXTENDED (not replaced) — every existing test still passes verbatim, and 6 new describe blocks are appended, one per new status entry: open, acknowledged, assigned, in_progress, closed (resolved already covered). Each new describe block asserts the rendered text label matches UI-SPEC table (e.g. "Open", "Acknowledged", "Assigned", "In Progress", "Closed") and the data-status attribute matches the status string passed as prop. The "Closed" describe block asserts data-tone="muted" (the new tone introduced by D-37/D-38).
    - ResolveModal.test.tsx is NEW with a describe.skip wrapper around the actual implementation tests — three describe.skip blocks for: photo-required-blocks-submit (WFLOW-05), photo-provided-enables-submit, optional-notes-field. Each describe.skip contains placeholder `it("...", () => { /* implemented in plan 03-03 */ })` calls so jest reports them as todo / skipped and the file is wired for npm test discovery. Use the project's existing FormData / file-mock helpers (see PhotoCapture.test.tsx makeFile).
    - OrgAssignPanel.test.tsx is NEW with describe.skip wrapper around cascade-rendering tests: corporation-dropdown-rendered, ward-office-filtered-by-corp, save-button-disabled-without-corp.
    - StatusActionPanel.test.tsx is NEW with describe.skip wrapper around per-status-button-presence tests: open-shows-acknowledge, acknowledged-shows-assign-and-mark-in-progress, assigned-shows-mark-in-progress-and-resolve, in-progress-shows-resolve, resolved-shows-close, closed-shows-locked-panel.
    - ReportsMap.test.tsx is EXTENDED (not replaced) — every existing test still passes verbatim, and 2 new describe.skip blocks are appended: status-colors-applied-to-circle-marker (MAP-01) and popup-status-label-rendered (MAP-03). describe.skip is used because the implementation lands in plan 03-04; this task only provides the scaffold to satisfy the Nyquist Wave 0 gate for those requirements.
    - All Wave 0 scaffold tests use describe.skip (NOT it.skip) so plan 03-03/03-04 executors must un-skip and implement, making the gate visible.
    - `cd frontend && npm test -- --watchAll=false --passWithNoTests 2>&1` exits 0 (the full suite remains green; describe.skip blocks are reported as skipped not failed)
  </behavior>
  <action>
    Append 6 new describe blocks to StatusBadge.test.tsx using the existing test file's `describe`/`it`/`render`/`screen.getByTestId('status-badge')` style. Assert: `expect(badge).toHaveAttribute('data-status', 'open')`, `expect(badge).toHaveTextContent('Open')` for each of open, acknowledged, assigned, in_progress, closed. For closed specifically, also assert `data-tone="muted"` (the new 6th tone introduced by D-38). Use UI-SPEC §"Status Color System" 6-state table verbatim for label text strings.

    Create ResolveModal.test.tsx with a file-level JSDoc block referencing WFLOW-05 (D-13, D-14, D-16). Define three describe.skip blocks per the behavior section. Inside each, leave placeholder `it("...", () => {})` calls so the test file is well-formed and tracked by jest.

    Create OrgAssignPanel.test.tsx with a JSDoc referencing WFLOW-03 (D-08, D-09, D-10, D-11). Define three describe.skip blocks per the behavior section.

    Create StatusActionPanel.test.tsx with a JSDoc referencing WFLOW-01 button-bar contract (D-37). Define six describe.skip blocks per the behavior section (one per status state).

    Append 2 describe.skip blocks to ReportsMap.test.tsx with JSDoc references to MAP-01 + MAP-03 (D-30, D-31). Each placeholder describes the assertion plan a future executor will implement (e.g. "When report.status='in_progress', CircleMarker fillColor matches STATUS_COLORS.in_progress").

    All new files MUST start with the test file conventions described in .planning/codebase/TESTING.md (file-level JSDoc, beforeEach clearAllMocks, describe headings tied to requirement IDs). No production imports yet — Wave 0 scaffolds are intentionally `describe.skip` so they compile without the future implementation files existing.

    Run `cd frontend && npm test -- --watchAll=false --passWithNoTests` and confirm: (a) existing test count is unchanged-or-increased (the StatusBadge new tests + ReportsMap unchanged), (b) the new files report as having "skipped" describes, (c) exit code 0.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --watchAll=false --passWithNoTests StatusBadge ReportsMap ResolveModal OrgAssignPanel StatusActionPanel 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/admin/components/__tests__/StatusBadge.test.tsx` contains `data-status="open"` and `data-status="closed"` string literals
    - `grep -c "data-status=\"acknowledged\"" frontend/app/admin/components/__tests__/StatusBadge.test.tsx` returns at least 1
    - `grep -c "data-status=\"in_progress\"" frontend/app/admin/components/__tests__/StatusBadge.test.tsx` returns at least 1
    - File `frontend/app/admin/components/__tests__/ResolveModal.test.tsx` exists; `grep -c "describe.skip" frontend/app/admin/components/__tests__/ResolveModal.test.tsx` returns at least 3
    - File `frontend/app/admin/components/__tests__/OrgAssignPanel.test.tsx` exists; `grep -c "describe.skip" frontend/app/admin/components/__tests__/OrgAssignPanel.test.tsx` returns at least 3
    - File `frontend/app/admin/components/__tests__/StatusActionPanel.test.tsx` exists; `grep -c "describe.skip" frontend/app/admin/components/__tests__/StatusActionPanel.test.tsx` returns at least 6
    - `grep -c "describe.skip" frontend/app/components/__tests__/ReportsMap.test.tsx` returns at least 2
    - `cd frontend && npm test -- --watchAll=false --passWithNoTests 2>&1 | grep -E "Tests:.*passed"` shows ≥ existing pass count (no regressions)
    - `cd frontend && npm test -- --watchAll=false --passWithNoTests 2>&1 | grep -c "Test Suites:.* failed"` returns 0 (no failed suites)
    - `cd frontend && npm run lint 2>&1 | grep -c "error"` returns 0 (test files excluded per .eslintrc — but the new files must be syntactically valid)
  </acceptance_criteria>
  <done>
    All 5 Wave 0 test files exist with appropriate describe.skip blocks tied to requirement IDs, npm test passes, and plans 03-03/03-04 have a green Nyquist scaffold to un-skip and fill in.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live database | DDL changes (ALTER TYPE, ALTER TABLE, UPDATE) execute against production data on next deploy; untrusted only in the sense that data shape mismatch crashes startup |
| GeoJSON file → migration SQL | data/gba_wards_2025.geojson is the data source for 369 UPDATE statements; a malformed entry could produce invalid SQL |
| backfill data (MLA/MP names) → public response | wards.mla_name and wards.mp_name will be served to citizens on /reports/[id] (plan 03-04); typos or stale data become public-facing misinformation |
| validate_resolve_request → reports table | New pure validator gates uploads to the file system; weakness here lets a no-photo PATCH bypass D-13/D-14 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Migration 008 ALTER TYPE | mitigate | `-- no-transaction` pragma at top; ENUM rename is atomic in Postgres 10+; idx_reports_submitted_created DROP+CREATE in same migration prevents stale partial-index references (Pitfall 2) |
| T-03-01-02 | Information Disclosure | wards.mla_name + mp_name as public-facing data | accept | Names are publicly published election results (Karnataka Vidhan Sabha 2023 + ECI 2024); citation chain in 03-RESEARCH.md; treated as published civic data, not PII |
| T-03-01-03 | Tampering | scripts/generate_009_backfill.py untrusted ac_no parsing | mitigate | Python int() cast wraps each ac_no read; out-of-range ac_no (not 150..177) raises KeyError on AC_TO_PC dict lookup at generation time, halting SQL emission before deployment |
| T-03-01-04 | Repudiation | ALTER TYPE rename without keep-old-plus-add-new | accept | D-05 explicitly chose rename approach; legacy data with status='submitted' is updated atomically to 'open' by the rename itself (Postgres rewrites pg_enum); no per-row UPDATE needed; commit hash from this plan serves as audit trail |
| T-03-01-05 | Denial of Service | 369-row UPDATE inside transaction | mitigate | Single transaction acceptable for 369 rows on indexed PK (ward_number); estimated <100ms on Postgres 16; statement_timeout default is unbounded; no DoS risk at this scale |
| T-03-01-06 | Tampering | validate_resolve_request bypass via empty photo bytes | mitigate | Pure function rejects (photo_bytes_len == 0 && status ∈ {resolved, closed}) → BadRequest("Resolution photo required"); enforced in plan 03-02 handler before any disk write |
| T-03-01-SC | Tampering | npm/pip/cargo installs | mitigate | This plan introduces zero new dependencies (scripts/generate_009_backfill.py uses Python stdlib json only). No Package Legitimacy Gate checkpoint required. |

</threat_model>

<verification>
After all 3 tasks complete:
- Run `cd backend && cargo clippy -- -D warnings && cargo test 2>&1 | tail -10` — must end with `test result: ok` and zero clippy warnings
- Run `cd frontend && npm test -- --watchAll=false --passWithNoTests 2>&1 | tail -10` — must show all suites green, new Wave 0 suites reporting as "skipped" not failed
- Run `docker compose -f docker-compose.yml -f docker-compose.dev.yml up db -d` then `cd backend && cargo run` once and confirm "applied migration 008_workflow" + "applied migration 009_ward_hierarchy" log lines before Ctrl+C; running again must show NO migration-apply output (idempotency)
- Run `docker compose exec db psql -U walkability -d walkability -c "\dT+ report_status"` and confirm 6 values in order: open, acknowledged, assigned, in_progress, resolved, closed
- Run `docker compose exec db psql -U walkability -d walkability -c "SELECT ward_number, mla_name, mp_name, parliamentary_constituency FROM wards WHERE ward_number IN (1, 100, 200, 300, 369) ORDER BY ward_number"` — every row has all 3 names populated
</verification>

<success_criteria>
- Migration 008 + 009 apply cleanly on a fresh docker-compose db
- report_status ENUM is exactly the 6 ordered values open, acknowledged, assigned, in_progress, resolved, closed
- 369 wards each have non-null mla_name, mp_name, parliamentary_constituency, zone_name, ro_division, aro_sub_division, assembly_constituency, assembly_constituency_no
- validate_status / UpdateStatusRequest::is_valid_status / StatsResponse seed all reflect the 6 values
- ReportResponse exposes resolution_photo_url only when present (per D-18)
- validate_resolve_request gates resolved/closed transitions on photo presence (foundation for WFLOW-05)
- All 5 Wave 0 frontend test files exist with describe.skip blocks for the components plans 03-03 / 03-04 will implement
- cargo test, cargo clippy -- -D warnings, npm test all pass green
- backend/.sqlx metadata is regenerated and committed
</success_criteria>

<output>
Create `.planning/phases/03-government-triage-workflow/03-01-SUMMARY.md` when done, listing: migrations created with file sizes, struct fields added, test counts before/after, and the exact 6-value enum order verified in postgres.
</output>
