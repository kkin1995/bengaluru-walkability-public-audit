---
phase: 03-government-triage-workflow
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - frontend/app/components/ReportsMap.tsx
  - frontend/app/components/__tests__/ReportsMap.test.tsx
  - frontend/app/map/page.tsx
  - frontend/app/reports/[id]/page.tsx
  - frontend/app/reports/[id]/__tests__/page.test.tsx
  - frontend/app/lib/config.ts
autonomous: false
requirements:
  - MAP-01
  - MAP-03
user_setup: []
tags:
  - react
  - nextjs
  - leaflet
  - direction-a
  - server-component

must_haves:
  truths:
    - "ReportsMap CircleMarker fillColor is sourced from STATUS_COLORS keyed by report.status, NOT from CATEGORY_COLORS — 3-color public mapping per D-30 and UI-SPEC §I (red for open/acknowledged/assigned, amber for in_progress, green for resolved/closed) (MAP-01)"
    - "ReportsMap Popup includes: corporation name, ward name, current status label with colored dot, and a 'Read More →' link to /reports/{id} per D-31 and UI-SPEC §G (MAP-03)"
    - "Map legend in /map page replaces the category-based legend with a status-based 3-row legend (Open, In progress, Resolved) per UI-SPEC §H"
    - "/reports/[id] page is a Next.js server component (per RESEARCH.md open-question recommendation) that fetches GET /api/reports/:id server-side via INTERNAL_API_URL and renders the full Direction-A layout per UI-SPEC §J (MAP-03 details surface)"
    - "/reports/[id] hero photo, status badge, meta grid (date + ward + corporation), status history timeline, conditional Resolution section (after-photo + notes) when status is resolved or closed, full GBA Responsibility Hierarchy section (bureaucratic + elected), back-to-map link"
    - "Pin colors render correctly in jest tests via the existing react-leaflet mock pattern; fillColor prop passed to CircleMarker is asserted in tests"
    - "cd frontend && npm test passes green with the new + un-skipped ReportsMap tests and the new server-component page test"
  artifacts:
    - path: "frontend/app/reports/[id]/page.tsx"
      provides: "Public single-report page (Direction-A server component)"
      min_lines: 150
    - path: "frontend/app/components/ReportsMap.tsx"
      provides: "ReportsMap with STATUS_COLORS + extended popup including status + corporation + Read More link"
      contains: "STATUS_COLORS"
    - path: "frontend/app/map/page.tsx"
      provides: "Map page with status-based legend"
      contains: "Open\\|In progress\\|Resolved"
  key_links:
    - from: "frontend/app/reports/[id]/page.tsx"
      to: "INTERNAL_API_URL/api/reports/:id"
      via: "server-side fetch in async page component"
      pattern: "fetch\\(.*INTERNAL_API_URL.*api/reports"
    - from: "frontend/app/components/ReportsMap.tsx"
      to: "report.status → STATUS_COLORS lookup"
      via: "fillColor prop on CircleMarker"
      pattern: "STATUS_COLORS\\[report\\.status\\]"
    - from: "frontend/app/components/ReportsMap.tsx Popup"
      to: "/reports/{report.id} Next.js route"
      via: "<a href={`/reports/${report.id}`}> link"
      pattern: "href=\\{`/reports/\\$\\{report\\.id\\}`\\}"
---

<objective>
Ship the Phase 03 public citizen frontend per UI-SPEC §G–J (Direction A): switch map pin coloring from category-based to status-based (3-color scheme), extend the map popup with corporation + ward + status + Read More link, replace the map legend with a status legend, and introduce the new public /reports/[id] single-report page as a Next.js server component.

Purpose: MAP-01 (status-colored pins) and MAP-03 (status in popup + dedicated public report page per D-25) — the public-visibility half of the GBA-pivot scope (D-01, D-02). Plan 03-03 (admin frontend) runs in parallel; this plan does not depend on 03-03 because the public surface only consumes the plan 03-02 public API endpoints.

Output:
- ReportsMap.tsx: STATUS_COLORS replaces CATEGORY_COLORS for fillColor; popup gains corporation/ward/status/Read More
- map/page.tsx: status-based 3-row legend replacing category legend
- reports/[id]/page.tsx: new Direction-A server component
- Updated ReportsMap.test.tsx (un-skip the 2 describe.skip blocks from plan 03-01 task 3 and implement)
- New reports/[id]/__tests__/page.test.tsx integration test
- config.ts: confirm INTERNAL_API_URL is exported (it already is — no change expected)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-government-triage-workflow/03-CONTEXT.md
@.planning/phases/03-government-triage-workflow/03-RESEARCH.md
@.planning/phases/03-government-triage-workflow/03-UI-SPEC.md
@.planning/phases/03-government-triage-workflow/03-VALIDATION.md
@.planning/phases/03-government-triage-workflow/03-01-SUMMARY.md
@.planning/phases/03-government-triage-workflow/03-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@frontend/app/components/ReportsMap.tsx
@frontend/app/components/__tests__/ReportsMap.test.tsx
@frontend/app/map/page.tsx
@frontend/app/lib/config.ts
@frontend/app/lib/translations.ts
@frontend/__mocks__/reactLeaflet.js
@frontend/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReportsMap STATUS_COLORS + extended popup + map page legend update</name>
  <files>frontend/app/components/ReportsMap.tsx, frontend/app/components/__tests__/ReportsMap.test.tsx, frontend/app/map/page.tsx</files>
  <read_first>
    - frontend/app/components/ReportsMap.tsx (full file — CATEGORY_COLORS at line 10; Report interface at line 20; CircleMarker fillColor at line 112; Popup JSX at lines 117-145)
    - frontend/app/components/__tests__/ReportsMap.test.tsx (existing tests + the 2 describe.skip blocks added by plan 03-01 task 3 — un-skip and implement)
    - frontend/__mocks__/reactLeaflet.js (the mock implementation — CircleMarker is mocked to expose its props as data attributes; Popup is mocked similarly)
    - frontend/app/map/page.tsx (full file — current category-based legend at the top; chip filter strip)
    - .planning/phases/03-government-triage-workflow/03-UI-SPEC.md (§I Map Pin Colors STATUS_COLORS block; §G Map Popup Update — exact additive elements; §H Map Legend Update — 3-item layout)
    - .planning/phases/03-government-triage-workflow/03-CONTEXT.md (D-30 pin colors, D-31 popup additions, D-33 resolved stays on map)
  </read_first>
  <behavior>
    - ReportsMap.tsx Report interface gains 2 new fields: `corporation?: string | null`, `ward_name?: string | null` — both populated from the existing GET /api/reports?limit=200 response (the public list endpoint shape already includes ward_name via Report struct in plan 03-01 if extended, OR is added in this task's verification step). Note: the public list endpoint /api/reports is a separate handler from /api/reports/:id; the list endpoint's JSON shape needs to include ward_name + corporation for the popup. Verify by reading the current `list_reports` handler at backend/src/handlers/reports.rs to confirm shape — if missing, this task's scope expands to include extending list_reports' JSON output (a small SELECT addition); otherwise this is purely a frontend change.
    - STATUS_COLORS Record<string, string> is defined at module-level above the component, exactly per UI-SPEC §I block: `{open: "var(--danger)", acknowledged: "var(--danger)", assigned: "var(--danger)", in_progress: "var(--warn)", resolved: "var(--accent)", closed: "var(--accent)"}`. CSS tokens `--danger`, `--warn`, `--accent` are defined in globals.css (Direction A — read globals.css to confirm; per UI-SPEC §Public Portal Color Direction-A these are the civic palette: danger=oklch(0.62 0.14 30), warn=oklch(0.72 0.14 75), accent=oklch(0.62 0.14 145))
    - CATEGORY_COLORS constant is DELETED (no longer needed)
    - CircleMarker fillColor prop changed from `CATEGORY_COLORS[report.category] ?? "#3b82f6"` to `STATUS_COLORS[report.status] ?? "var(--danger)"` per UI-SPEC §I fallback policy ("treats unknown status as unresolved")
    - Popup JSX is extended with 3 new elements (additive — preserve existing photo, category, severity, description, date):
      1. Status chip (inline next to category title): renders `<span>` with colored dot + status label text per UI-SPEC §G; uses 3-state public public mapping (open/acknowledged/assigned → "Open" red, in_progress → "In progress" amber, resolved/closed → "Resolved" green)
      2. GBA jurisdiction line (between description and date): renders `<div>` with a small `GBA` mono tag + corporation name bold + middle-dot + ward name per UI-SPEC §G styling spec
      3. "Read More →" link (replacing the existing chevron in the footer or appended below the date): `<a href={`/reports/${report.id}`} style={{color: "var(--accent-ink)", fontWeight: 600, fontSize: 12}}>Read More →</a>` — links to the new public /reports/[id] page (per D-27, D-28)
    - The GBA jurisdiction line and status chip handle null ward_name + null corporation gracefully (omit the line / use fallback text "—")
    - map/page.tsx legend is replaced: find the existing category legend rendering (look for a `<div>` containing the category color samples), replace with a 3-item status legend:
      - `[red dot] Open` (8px circle, red color via var(--danger))
      - `[amber dot] In progress`
      - `[green dot] Resolved`
      Container styled per UI-SPEC §H: `background: rgba(255,255,255,0.95); border: 1px solid var(--border); border-radius: var(--r-md); padding: 8px 12px; font-size: 10px; font-weight: 400; backdrop-filter: blur(8px)`. Position: top-left below filter chips (per UI-SPEC §H position spec). Add `aria-label="Status legend"` for accessibility (per UI-SPEC §Accessibility map legend point).
    - ReportsMap.test.tsx un-skips the 2 describe.skip blocks from plan 03-01 task 3 and asserts:
      - (MAP-01) When a report with status="in_progress" is rendered, the CircleMarker mock's data attribute for fillColor (or the prop captured via the mock) matches `var(--warn)` or the literal amber color; when status="resolved", matches `var(--accent)`; when status="open", matches `var(--danger)`. Use the existing react-leaflet mock pattern (frontend/__mocks__/reactLeaflet.js exposes props as data-* attributes on a stub element).
      - (MAP-03) The Popup mock renders the report.status as a text label within the popup body; render a report with status="resolved" and assert `screen.getByText(/Resolved/i)` is present in the popup; render with status="in_progress" and assert `getByText(/In progress/i)`.
      - Read More link assertion: `screen.getByRole('link', {name: /read more/i})` has href `/reports/${report.id}`.
    - cd frontend && npm test passes green; the 2 newly-implemented describe blocks plus existing tests all pass
  </behavior>
  <action>
    Edit frontend/app/components/ReportsMap.tsx:
    - At the top (line 10), DELETE the `CATEGORY_COLORS` constant and REPLACE with `STATUS_COLORS` per UI-SPEC §I (use the var(--*) tokens, not hex literals, so Direction-A palette is honored). Add a brief inline comment `// MAP-01 (D-30): status-based public pin colors`.
    - Extend the Report interface (around line 20) with `corporation?: string | null` and `ward_name?: string | null` fields. The list endpoint may not yet return these — check backend/src/handlers/reports.rs::list_reports and backend/src/models/report.rs::ReportResponse to confirm the public list shape. If ward_name/corporation are NOT on ReportResponse, extend ReportResponse to include them (matches the admin pattern that already does the JOIN) — see plan 03-02 task 3 which already did the JOIN for /api/reports/:id. The list endpoint may need a parallel JOIN; if so, this task adds the JOIN. Use the existing `#[serde(skip_serializing_if = "Option::is_none")]` pattern so when ward_id is null these fields are simply absent from the JSON.
    - Change the CircleMarker fillColor prop at line 112 to `fillColor={STATUS_COLORS[report.status] ?? "var(--danger)"}`
    - Inside the Popup JSX (line 117-145), add the 3 new elements per behavior section:
      - Replace the line `<p className="font-semibold text-sm">{getCategoryLabel(report.category).en}</p>` with a flex container that renders the category label AND a status chip side by side. Status chip: `<span style={{display: "inline-flex", alignItems: "center", gap: 4, marginLeft: 8}}><span style={{width: 6, height: 6, borderRadius: "50%", background: STATUS_COLORS[report.status]}}/><span style={{fontSize: 11}}>{publicStatusLabel(report.status)}</span></span>`. Helper `publicStatusLabel` is a pure function in the file mapping the 6-status enum to 3 public labels: `open|acknowledged|assigned -> "Open"; in_progress -> "In progress"; resolved|closed -> "Resolved"`.
      - Between the description block and the date line, add: `<div style={{marginTop: 8, display: "flex", alignItems: "center", gap: 6, fontSize: 11}}><span style={{fontFamily: "var(--font-mono)", fontSize: 9, letterSpacing: "0.06em", textTransform: "uppercase", color: "var(--muted)", background: "var(--surface-2)", padding: "4px 8px", borderRadius: 4}}>GBA</span><span style={{fontWeight: 600}}>{report.corporation ?? "—"}</span><span>·</span><span>{report.ward_name ?? "—"}</span></div>` (per UI-SPEC §G GBA jurisdiction line + GBA tag styling)
      - After the date `<p>`, append: `<a href={`/reports/${report.id}`} style={{display: "inline-flex", alignItems: "center", gap: 4, marginTop: 8, color: "var(--accent-ink)", fontWeight: 600, fontSize: 12}}>Read More →</a>`

    Edit frontend/app/map/page.tsx:
    - Locate the existing category-based legend (search for `CATEGORY_COLORS` references or category color samples in the JSX — the legend may be inline or in a sub-component). Replace it with a status-based 3-row legend per UI-SPEC §H. Use the exact container style block from UI-SPEC §H. Place it at position `top: 108px, left: 12px` (absolute, below the filter chip row).
    - Each row: `<div style={{display: "flex", alignItems: "center", gap: 6}}><span style={{width: 8, height: 8, borderRadius: "50%", background: "var(--danger)"}}/><span>Open</span></div>` for the red row; repeat for amber/var(--warn)/"In progress" and green/var(--accent)/"Resolved"
    - Add `<div role="region" aria-label="Status legend">` wrapping the whole legend block per UI-SPEC §Accessibility
    - The chip filter strip (filter by category) — KEEP as-is. UI-SPEC §H replaces the LEGEND, not the filter chips. Filter chips remain category-based per Phase 02.3.2 UI-MAP-FILTER-CHIPS requirement.

    Edit frontend/app/components/__tests__/ReportsMap.test.tsx:
    - Un-skip the 2 describe.skip blocks from plan 03-01 task 3 (`describe.skip` → `describe`)
    - Implement the assertions per behavior section. Use the existing test file's render pattern: `render(<ReportsMap apiUrl="..."/>)`, mock `fetch` to return `{items: [{id, status, category, ...}]}`, `await waitFor` for marker to render. Assert CircleMarker fillColor prop matches expected via the react-leaflet mock's exposed data attributes. Read frontend/__mocks__/reactLeaflet.js to understand how CircleMarker props are exposed — likely as `data-fill-color` attribute on a stub `<div>`.
    - For the Read More link: `expect(screen.getByRole('link', {name: /read more/i})).toHaveAttribute('href', '/reports/test-report-id')`

    IF list_reports JSON shape needs updating: edit backend/src/handlers/reports.rs::list_reports SELECT to include ward_name + corporation via LEFT JOIN with wards (same pattern as get_report_with_detail in plan 03-02 task 3); extend Report→ReportResponse mapping to include these fields with #[serde(skip_serializing_if = "Option::is_none")]. Run `cargo sqlx prepare --database-url ...` afterward. If the shape is already complete, skip this paragraph.

    Run `cd frontend && npm test -- --watchAll=false --passWithNoTests ReportsMap` and confirm all tests pass.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --watchAll=false --passWithNoTests ReportsMap 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "STATUS_COLORS" frontend/app/components/ReportsMap.tsx` returns at least 2 (declaration + usage)
    - `grep -c "CATEGORY_COLORS" frontend/app/components/ReportsMap.tsx` returns 0 (deleted)
    - `grep -c "Read More" frontend/app/components/ReportsMap.tsx` returns at least 1
    - `grep -c "publicStatusLabel\\|in_progress.*In progress\\|resolved.*Resolved" frontend/app/components/ReportsMap.tsx | head -1` returns at least 1
    - `grep -c "describe.skip" frontend/app/components/__tests__/ReportsMap.test.tsx` returns 0 (all un-skipped)
    - `grep -c "fillColor\\|fill-color" frontend/app/components/__tests__/ReportsMap.test.tsx` returns at least 1 (MAP-01 assertion)
    - `grep -c "/reports/" frontend/app/components/__tests__/ReportsMap.test.tsx` returns at least 1 (Read More href assertion)
    - `grep -c "Open\\|In progress\\|Resolved" frontend/app/map/page.tsx` returns at least 3 (3-row legend)
    - `grep -c "var(--danger)\\|var(--warn)\\|var(--accent)" frontend/app/map/page.tsx` returns at least 3 (3 dot colors)
    - `cd frontend && npm test -- --watchAll=false --passWithNoTests ReportsMap 2>&1 | grep -c "Test Suites:.* failed"` returns 0
    - `cd frontend && npm run lint 2>&1 | grep -c "error"` returns 0
  </acceptance_criteria>
  <done>
    Public map renders status-colored pins; popup includes corporation + ward + status + Read More link; legend reflects status not category. ReportsMap tests cover both MAP-01 and MAP-03. Plan task 2 can build the /reports/[id] page that the Read More link targets.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Public /reports/[id] server component page (Direction-A)</name>
  <files>frontend/app/reports/[id]/page.tsx, frontend/app/reports/[id]/__tests__/page.test.tsx</files>
  <read_first>
    - frontend/app/lib/config.ts (full file — INTERNAL_API_URL export pattern for server-side fetch; rule "never inline process.env.*" — must use the config exports)
    - frontend/app/globals.css (Direction-A token names: --bg, --surface, --surface-2, --ink, --ink-2, --muted, --accent, --accent-ink, --accent-bg, --border, --border-strong, --danger, --warn, --r-sm, --r-md, --r-lg, --font-sans, --font-mono)
    - frontend/app/lib/translations.ts (getCategoryLabel helper)
    - frontend/app/page.tsx OR frontend/app/admin/layout.tsx (server component patterns in this codebase — async function with default export; await fetch with INTERNAL_API_URL)
    - frontend/app/components/redesign/ (if it contains any Direction-A primitives — Pill, Btn for citizen UI; review for reuse)
    - .planning/phases/03-government-triage-workflow/03-UI-SPEC.md (§J Public Single-Report Page — full layout spec: floating header, hero photo, scrollable content, category/severity/status chip row, title, meta grid, status history timeline, Resolution section conditional, GBA Responsibility Hierarchy, back-to-map link; §Copywriting Contract for exact strings)
    - .planning/phases/03-government-triage-workflow/03-CONTEXT.md (D-26, D-27, D-28, D-29 — page contract; D-42 elected chain with named officials; D-45 engineering chain note)
    - .planning/phases/03-government-triage-workflow/03-RESEARCH.md (Open Question 4: Recommendation Server Component using INTERNAL_API_URL; Pitfall 10 — public history must exclude note + changed_by — already enforced by plan 03-02 backend)
  </read_first>
  <behavior>
    - File `frontend/app/reports/[id]/page.tsx` exports a default async function `export default async function PublicReportPage({params}: {params: {id: string}})` — Next.js 14 App Router server component (no "use client" directive)
    - Server-side fetch: `const res = await fetch(`${INTERNAL_API_URL}/api/reports/${params.id}`, {cache: "no-store"})` where INTERNAL_API_URL is imported from `@/app/lib/config`; on res.status === 404 call `notFound()` from `next/navigation`; on other non-2xx render an error fallback `<div>Failed to load report.</div>`
    - Page layout follows UI-SPEC §J top-to-bottom exactly:
      1. Floating header bar (position absolute top 12px): `<a href="/map">← Map</a>` pill on left; share button (icon + aria-label="Share report") on right
      2. Hero photo full-width 260px tall, `<img src={report.image_url} alt="" style={{width: "100%", height: 260, objectFit: "cover"}}>`, gradient overlay div, mono report-ID overlay bottom-left
      3. Scrollable content area (padding 16px 16px 24px) containing:
         a. Category + severity + status row (flex wrap, gap 8px): category chip with icon, severity chip ("Severity · {report.severity}" with warn/accent/muted color per level), status badge with colored dot per public 3-state mapping (open/acknowledged/assigned→red "Open", in_progress→amber "In progress", resolved/closed→green "Resolved")
         b. Title `<h1>` (22px/700) + description `<p>` (13px/ink-2/lh 1.55)
         c. Meta grid: 2-column card; Row 1 — Submitted date (formatted via `Intl.DateTimeFormat`) | Ward name; Row 2 (full-width, dashed top border) — GBA tag + corporation name + zone mono
         d. Status history timeline (`<section aria-label="Status history">` with eyebrow "Status history"): vertical timeline with 16px colored dots (red/amber/green per 3-state mapping), connector line between, current step highlighted with ring glow; mono date right-aligned for each entry; map data from `report.history` array (already public-safe per plan 03-02 — no note, no changed_by)
         e. Resolution section (conditional `{(report.status === "resolved" || report.status === "closed") && ...}`): container with `var(--accent-bg)` background + `1px solid var(--accent-border)` border; header "RESOLUTION" eyebrow with check icon; after-photo full-width 180px (from `report.resolution_photo_url` — present per plan 03-02 when resolved); photo caption mono small "After · {date}" + "Field verified"; resolution_notes is ABSENT from the public response per D-17 (plan 03-02 enforces) — DO NOT render notes
         f. GBA Responsibility Hierarchy section: intro 11px muted "Who is accountable for this location, by office."; "BUREAUCRATIC CHAIN" eyebrow; bordered card with 6 numbered rows (Ward → ARO Sub Division → RO Division → Zone → Corporation → GBA) reading from `report.ward_hierarchy.*`; "ELECTED · ASSEMBLY" eyebrow; single card with `{ac_no} – {ac_name}` + MLA name + parliamentary_constituency + MP name + disclaimer footnote "Constituency boundaries may differ from ward boundaries."
         g. Back-to-map link at bottom (centered): `<a href="/map">← Back to map</a>` pill
    - When `report.ward_hierarchy.ward_name === null` (report stored without ward lookup), the bureaucratic + elected sections render the muted fallback "Ward assignment not available for this report." per UI-SPEC §J alternate state
    - When `report.history` is empty or has only 1 entry, the timeline still renders (single-entry timeline is valid — initial "Open" state)
    - The page does NOT include ANY admin-only fields — verify by the implementation never referencing `submitter_name`, `submitter_contact`, `submitter_ip`, or `resolution_notes` (these are not on the public response per plan 03-02; this task confirms the contract by NEVER reading those keys)
    - Test file `frontend/app/reports/[id]/__tests__/page.test.tsx` tests the page as a server component. Server components are async functions — mock the global fetch and call the page function directly, then render the returned JSX with @testing-library/react render. Cover: (a) renders with full hierarchy when API returns all fields, (b) renders muted "Ward assignment not available..." when hierarchy.ward_name is null, (c) renders Resolution section ONLY when status is resolved or closed, (d) renders status history list with public fields only (asserts that the rendered timeline does NOT include any element with text "Acknowledged by admin@example.com" — i.e. no changed_by leak), (e) 404 from API triggers Next.js notFound (mock the navigation import or assert the function throws the expected error)
    - cd frontend && npm test passes green; cd frontend && npm run build passes with zero TypeScript errors
  </behavior>
  <action>
    Create frontend/app/reports/[id]/page.tsx as an async server component:

    ```
    import { notFound } from "next/navigation";
    import { INTERNAL_API_URL } from "@/app/lib/config";
    import { getCategoryLabel } from "@/app/lib/translations";

    export default async function PublicReportPage({params}: {params: {id: string}}) {
      const res = await fetch(`${INTERNAL_API_URL}/api/reports/${params.id}`, { cache: "no-store" });
      if (res.status === 404) notFound();
      if (!res.ok) return <div role="alert">Failed to load report.</div>;
      const report = await res.json();
      // ... full UI-SPEC §J layout
    }
    ```

    Implement the full UI-SPEC §J layout. All styling MUST use inline `style` objects referencing globals.css CSS variables (Direction A tokens) — NO Tailwind, NO CSS modules per CONVENTIONS.md "all new work". The page is mobile-first; container max-width 100% with the padding spec from UI-SPEC §J.

    Helper functions defined at module scope (above the default export):
    - `publicStatusLabel(status: string): "Open" | "In progress" | "Resolved"` — same mapping as in ReportsMap.tsx task 1 (open/acknowledged/assigned→Open, in_progress→In progress, resolved/closed→Resolved). Consider extracting to `@/app/lib/translations.ts` so both ReportsMap.tsx and this page share a single source of truth (per CONVENTIONS.md "single source of truth" guidance and the pattern established by getCategoryLabel from Phase 02.3).
    - `publicStatusColor(status: string): string` — returns the CSS var string ("var(--danger)" / "var(--warn)" / "var(--accent)") for the 3-state mapping
    - `formatDate(isoString: string): string` — uses Intl.DateTimeFormat("en-IN", {day: "numeric", month: "short", year: "numeric"}) — matches the existing pattern in ReportsMap.tsx

    Decision rationale comment near the helper functions: `// MAP-03 / D-42: public 3-state mapping for citizens — 6-state admin enum is collapsed for clarity`.

    Status history timeline rendering: `report.history.map((entry, i) => (<TimelineEntry key={entry.changed_at} entry={entry} isCurrent={i === report.history.length - 1} />))`. Each entry shows: colored dot (per 3-state mapping), status label (publicStatusLabel), date (mono small, right-aligned via flex justify-between). Last entry has the ring-glow + "current" indicator per UI-SPEC §J.

    GBA Responsibility Hierarchy section: receives report.ward_hierarchy as the data source. Bureaucratic chain rows include the WARD label rendering `Ward {ward_number ?? "—"} · {ward_name ?? "—"}` ... ARO Sub Division `{aro_sub_division ?? "—"}` ... etc. Elected chain shows `{assembly_constituency_no ?? "—"} – {assembly_constituency ?? "—"}` and `MLA: {mla_name ?? "—"}` + `Parliamentary: {parliamentary_constituency ?? "—"}` + `MP: {mp_name ?? "—"}`. When `report.ward_hierarchy === null` OR `report.ward_hierarchy.ward_name === null`, replace the entire two-section block with a muted card displaying "Ward assignment not available for this report." (per UI-SPEC §J alternate state).

    Create frontend/app/reports/[id]/__tests__/page.test.tsx. Server component testing is non-trivial — mock global fetch, call the page function directly (it returns JSX), then render that JSX with @testing-library/react. Pattern:

    ```
    import PublicReportPage from "../page";
    // mock next/navigation notFound:
    jest.mock("next/navigation", () => ({ notFound: () => { throw new Error("NEXT_NOT_FOUND") } }));
    // mock @/app/lib/config:
    jest.mock("@/app/lib/config", () => ({ INTERNAL_API_URL: "http://test-api" }));

    describe("PublicReportPage", () => {
      it("renders full report when API returns all fields", async () => {
        (global.fetch as jest.Mock).mockResolvedValueOnce(mockOkResponse({ id: "abc", status: "resolved", ... }));
        const jsx = await PublicReportPage({ params: { id: "abc" }});
        render(jsx);
        expect(screen.getByText(/Resolved/i)).toBeInTheDocument();
        expect(screen.getByText(/Tejasvi Surya/i)).toBeInTheDocument(); // example MP
      });
      // ... other test cases
    });
    ```

    Use the existing `mockOkResponse` helper from jest.setup.ts if available; otherwise inline a small helper that returns `{ok: true, status: 200, json: async () => body}`.

    Run `cd frontend && npm test -- --watchAll=false --passWithNoTests` and `cd frontend && npm run build` — both must be green.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --watchAll=false --passWithNoTests page.test 2>&1 | tail -20 && cd /home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/reports/[id]/page.tsx` exists
    - File `frontend/app/reports/[id]/__tests__/page.test.tsx` exists
    - `grep -c "export default async function" frontend/app/reports/[id]/page.tsx` returns 1
    - `grep -c "INTERNAL_API_URL" frontend/app/reports/[id]/page.tsx` returns at least 1
    - `grep -c "process.env" frontend/app/reports/[id]/page.tsx` returns 0 (must use config.ts export per CONVENTIONS.md)
    - `grep -c "notFound" frontend/app/reports/[id]/page.tsx` returns at least 1
    - `grep -c "publicStatusLabel\\|status === \"resolved\"" frontend/app/reports/[id]/page.tsx` returns at least 1
    - `grep -c "Bureaucratic chain\\|BUREAUCRATIC CHAIN" frontend/app/reports/[id]/page.tsx` returns at least 1
    - `grep -c "Elected\\|ELECTED" frontend/app/reports/[id]/page.tsx` returns at least 1
    - `grep -c "Constituency boundaries may differ" frontend/app/reports/[id]/page.tsx` returns 1
    - `grep -c "Back to map" frontend/app/reports/[id]/page.tsx` returns at least 1
    - `grep -c "resolution_notes" frontend/app/reports/[id]/page.tsx` returns 0 (admin-only per D-17 — must NOT appear in public page)
    - `grep -c "submitter_name\\|submitter_contact\\|submitter_ip" frontend/app/reports/[id]/page.tsx` returns 0 (privacy)
    - `cd frontend && npm test -- --watchAll=false --passWithNoTests 2>&1 | grep -c "Test Suites:.* failed"` returns 0
    - `cd frontend && npm run build 2>&1 | grep -c "error TS"` returns 0
    - `cd frontend && npm run lint 2>&1 | grep -c "error"` returns 0
  </acceptance_criteria>
  <done>
    Public /reports/[id] page renders the full Direction-A layout; status history is public-safe (no note, no changed_by); GBA hierarchy + MLA + MP rendered from migration 009 backfill; the Read More link in the map popup (task 1) now navigates to a working page. Frontend build green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual + interaction UAT of public map + single-report page on local stack</name>
  <what-built>
    The full Phase 03 public citizen frontend: ReportsMap with 3-color status pins (red/amber/green per D-30), extended popup with corporation/ward/status/Read More, status-based map legend, and the new /reports/[id] public page rendering full report details + status history + GBA Responsibility Hierarchy with named MLA/MP from migration 009.
  </what-built>
  <how-to-verify>
    1. Start the stack: db + backend + frontend per the standard local dev flow
    2. Submit several test reports at /report with different categories and locations across Bengaluru
    3. Log in as admin via /admin/login, navigate to a few of those reports, and transition them through different statuses (use the plan 03-03 lifecycle): leave some as Open, mark one Acknowledged, one Assigned (assign to an org via OrgAssignPanel), one In Progress, one Resolved (using ResolveModal with a real photo), one Closed (using ResolveModal close mode)
    4. Open http://localhost:3000/map in a fresh browser tab — confirm:
       - Pins for Open/Acknowledged/Assigned reports render RED (`var(--danger)`)
       - Pin for the In Progress report renders AMBER (`var(--warn)`)
       - Pins for Resolved and Closed reports both render GREEN (`var(--accent)`)
       - The map legend in the top-left shows 3 rows: [red dot] Open, [amber dot] In progress, [green dot] Resolved (NOT the old category-based legend)
       - The category filter chip strip is still present (Phase 02.3.2 functionality preserved)
    5. Click any pin — popup shows:
       - Photo thumbnail
       - Category name + status badge with colored dot inline
       - Description (if present)
       - GBA tag + Corporation name + ward name on a new line
       - Date
       - "Read More →" link in accent green
    6. Click "Read More →" — confirm navigation to /reports/{id} renders the public page
    7. On /reports/{id} for an Open report — verify the UI-SPEC §J layout:
       - Floating header: "← Map" pill (left), share icon (right)
       - Hero photo full-width 260px with mono report-ID overlay bottom-left
       - Category + severity + status chips below the photo
       - Title + description
       - Meta grid: submitted date | ward name; dashed-top row: GBA tag + Corporation + Zone
       - Status history timeline showing only the "Open" entry (date right-aligned)
       - NO Resolution section (status not resolved/closed)
       - GBA Responsibility Hierarchy with 6 bureaucratic rows (Ward → ARO Sub Division → RO Division → Zone → Corporation → GBA), all populated from migration 009 backfill
       - Elected Assembly card: AC number + AC name + MLA name (e.g. "Munirathna" for AC 154); Parliamentary constituency (e.g. "Bangalore Rural") + MP name (e.g. "Dr. C N Manjunath")
       - Disclaimer footnote "Constituency boundaries may differ from ward boundaries."
       - Back-to-map link at bottom
    8. Open /reports/{id} for the Resolved report — verify the Resolution section appears between status history and hierarchy: green accent-bg card, after-photo full-width 180px, caption "After · [date]" + "Field verified"
    9. Verify NO admin-only data leaks: open browser DevTools Network tab, inspect the /api/reports/{id} response JSON — confirm `resolution_notes` is ABSENT, `submitter_name` is ABSENT, `submitter_contact` is ABSENT, `submitter_ip` is ABSENT, status_history entries contain ONLY `status` + `changed_at` keys (no `note`, no `changed_by`)
    10. Open /reports/{id} for a report whose ward_hierarchy.ward_name is null (or manually NULL out a report's ward_id in DB for this test) — confirm the two hierarchy sections collapse to a single muted card "Ward assignment not available for this report."
    11. Open /reports/{nonexistent-id} — confirm Next.js notFound() 404 page renders
    12. Mobile width test (DevTools 375px): all elements stack correctly, hero photo + meta grid + timeline all readable; back-to-map pill remains accessible at the bottom
    13. Light + dark mode (whichever globals.css supports — Direction A may not have dark mode yet; if so, skip this point)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 13 verification points pass.
    Otherwise: describe the failing step — Claude will fix and re-checkpoint.
  </resume-signal>
  <files>n/a — checkpoint task; no files are edited by the executor; the previous 2 tasks produced all artifacts</files>
  <action>Execute the 13-point how-to-verify checklist above against the running local stack (docker compose db + cargo run backend + npm run dev frontend). Confirm every checklist item passes; on any failure, stop and surface the failing point to the user with reproduction steps. The executor performs NO code edits in this task.</action>
  <verify>
    <human-check>All 13 manual verification points above PASS. The user types "approved" at the resume-signal prompt. If any point fails the executor surfaces the failure and pauses for the user to provide a fix direction.</human-check>
  </verify>
  <done>User types "approved" at the resume-signal; all 13 verification points have been visually + functionally confirmed against the running local stack including DevTools confirmation that admin-only fields do not leak via the public API.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public browser → GET /api/reports/:id | Anonymous access; server returns only public fields per D-17, D-18, plan 03-02 task 3 contract |
| Public browser → /reports/[id] page | Server-side fetch via INTERNAL_API_URL; the page is itself public — no auth |
| Public browser → /api/reports?limit=200 (map list) | Anonymous; popup needs ward_name + corporation which join wards table |
| INTERNAL_API_URL → backend | Server-side fetch from Next.js; must NEVER include admin cookies (server component fetches do not propagate browser cookies anyway) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | /reports/[id] leaking admin-only fields | mitigate | Plan 03-02 task 3 enforces public response contract (no resolution_notes, no submitter_*, no status_history.note/changed_by); this plan's page never reads those keys; acceptance tests grep for their absence |
| T-03-04-02 | Information Disclosure | Map popup leaking submitter identity | mitigate | Public /api/reports list endpoint already strips submitter_name/contact (Phase 02.1 SEC-01); this task verifies popup only renders ward_name + corporation + status |
| T-03-04-03 | Tampering | Path traversal in /reports/[arbitrary-id] | mitigate | Next.js routing constrains [id] to a single segment; backend get_report_with_detail parses Uuid via sqlx — non-UUID returns 400; non-existent UUID returns 404; notFound() renders Next.js 404 page |
| T-03-04-04 | Information Disclosure | resolution_photo_url leaked for non-resolved reports | mitigate | Plan 03-02 task 3 only sets resolution_photo_url when resolution_photo_path is Some (typically only after resolve); plan task 2 renders the Resolution section conditionally on status === "resolved" or "closed"; even if photo_url were present on an in-progress report, the JSX would still hide it |
| T-03-04-05 | XSS via report.description on public page | descriptions rendered as JSX text | mitigate | React JSX escapes by default; `{report.description}` is text content, never dangerouslySetInnerHTML; descriptions are constrained by backend bbox + length validation |
| T-03-04-06 | Cache poisoning | server-component fetch cached stale | accept | `cache: "no-store"` on fetch ensures every page render hits the API; OK for v1; future optimization may add ISR per D-29 if needed |
| T-03-04-SC | Tampering | npm/pip/cargo installs | mitigate | This plan introduces zero new dependencies. No Package Legitimacy Gate required. |

</threat_model>

<verification>
After all 3 tasks (2 code + 1 checkpoint) complete:
- `cd frontend && npm test -- --watchAll=false --passWithNoTests` is green with all new tests passing
- `cd frontend && npm run build` is green with zero TypeScript errors
- `cd frontend && npm run lint` is green with zero ESLint errors
- Human-verify checkpoint signed off with all 13 verification points
- /map shows status-colored pins, status legend, extended popup
- /reports/[id] renders the full Direction-A layout for any valid report ID
- DevTools network tab confirms no admin-only fields leak via the public API
</verification>

<success_criteria>
- ReportsMap CircleMarker fillColor sources from STATUS_COLORS (MAP-01 satisfied)
- Map popup contains status label + corporation + ward + Read More link (MAP-03 satisfied)
- Map legend replaced with 3-row status legend
- /reports/[id] public page renders full Direction-A layout per UI-SPEC §J including hierarchy with named MLA/MP
- Public API response contract is honored: NO resolution_notes, NO status_history.note, NO submitter_* in any code path on the public surface
- All frontend tests green; build green; lint green
- Human-verify checkpoint passes
</success_criteria>

<output>
Create `.planning/phases/03-government-triage-workflow/03-04-SUMMARY.md` when done, listing: files modified, pin-color mapping confirmed in browser, /reports/[id] screenshots for Open/In Progress/Resolved/Closed report states, and the DevTools-confirmed JSON shape of the public /api/reports/:id response (showing absent admin-only fields).
</output>
