---
phase: 05-uat-stabilisation
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - frontend/app/admin/reports/[id]/page.tsx
  - frontend/app/admin/page.tsx
  - frontend/app/admin/admin.css
  - frontend/app/admin/login/page.tsx
  - frontend/app/admin/lib/adminApi.ts
  - frontend/app/lib/config.ts
  - frontend/app/lib/translations.ts
autonomous: true
requirements: [FIX-05, FIX-08, FIX-09, FIX-11, FIX-13]
must_haves:
  truths:
    - "Admin report detail page renders a real Leaflet map (not a grey MAP placeholder) when coordinates exist"
    - "Admin dashboard '+N today' counter reads stats.today_count from the API, not a status-derived value"
    - "Admin dashboard page scrolls freely on iOS Safari (no overflow:hidden trap, no overscroll-behavior:none)"
    - "Admin footer and login footer render BUILD_HASH from config, not the hardcoded string 0000000"
    - "Admin report detail LOCATION_SRC label shows GPS_API/MANUAL_ADJUST/EXIF_GPS via a translation helper, never MANUAL_PIN"
  artifacts:
    - path: "frontend/app/admin/reports/[id]/page.tsx"
      provides: "dynamic read-only LocationMap + canonical LOCATION_SRC label"
      contains: "LocationMap"
    - path: "frontend/app/lib/translations.ts"
      provides: "getLocationSourceLabel helper"
      contains: "getLocationSourceLabel"
    - path: "frontend/app/lib/config.ts"
      provides: "BUILD_HASH export"
      contains: "BUILD_HASH"
  key_links:
    - from: "frontend/app/admin/page.tsx"
      to: "AdminStats.today_count"
      via: "render stats.today_count in +N today counter"
      pattern: "today_count"
    - from: "frontend/app/admin/reports/[id]/page.tsx"
      to: "frontend/app/components/LocationMap.tsx"
      via: "nextDynamic ssr:false import"
      pattern: "nextDynamic"
---

<objective>
Fix the admin-portal frontend UAT bugs:
- FIX-05: replace the grey "MAP" placeholder on the admin report detail page with a real read-only Leaflet map (dynamic, ssr:false).
- FIX-08 (frontend portion): render the new `stats.today_count` field in the "+N today" counter and add it to the AdminStats type.
- FIX-09: fix the iOS Safari rubber-band scroll on the admin dashboard.
- FIX-11 (frontend portion): export BUILD_HASH from config and use it in the admin/login footers.
- FIX-13 (admin display): add `getLocationSourceLabel` to translations.ts and use canonical values in the admin detail LOCATION_SRC label and EXIF condition.

Purpose: These are the admin-side findings from the v1.0 live iPhone field test. This plan owns all admin-portal frontend files plus config.ts and translations.ts, so it has no file overlap with plan 05-02 (citizen frontend). It runs in wave 2 because the FIX-08 counter and FIX-13 labels consume the `today_count` field and migrated `location_source` values delivered by plan 05-01.

Output: A working admin map, a correct today counter, free-scrolling dashboard, real build hash in footers, and canonical LOCATION_SRC labels.
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-uat-stabilisation/05-CONTEXT.md
@.planning/phases/05-uat-stabilisation/05-PATTERNS.md
@.planning/phases/05-uat-stabilisation/05-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-05 admin Leaflet map + FIX-13 LOCATION_SRC label</name>
  <files>frontend/app/admin/reports/[id]/page.tsx, frontend/app/lib/translations.ts</files>
  <read_first>
    - frontend/app/admin/reports/[id]/page.tsx (grey placeholder div at lines ~426-437; the EXIF condition at line ~106 `=== "exif"`; LOCATION_SRC label at line ~347; report.latitude/longitude fields)
    - frontend/app/components/LocationMap.tsx (readOnly prop at line ~59; non-draggable Marker at ~163-164; required onChange prop)
    - frontend/app/lib/translations.ts (CATEGORY_LABEL_MAP + getCategoryLabel pattern at lines ~35-46 — mirror it)
    - frontend/app/map/page.tsx (line ~5 `import nextDynamic from "next/dynamic"` — the dynamic-import pattern to copy)
    - .planning/phases/05-uat-stabilisation/05-PATTERNS.md (FIX-05 and FIX-13 admin display sections — exact code) and 05-RESEARCH.md (Pitfall 2 — admin detail needs a map ADDED, not just CSP)
    - .planning/phases/05-uat-stabilisation/05-01-SUMMARY.md (confirm canonical location_source values shipped by backend)
  </read_first>
  <action>
    FIX-05 (D-10, Pitfall 2): Import LocationMap into admin/reports/[id]/page.tsx via `nextDynamic(() => import("@/app/components/LocationMap"), { ssr: false })` per CLAUDE.md's mandatory dynamic-import rule for all Leaflet components. Replace the static grey placeholder div (~lines 426-437) with a conditional: when report.latitude and report.longitude are both non-null, render `<LocationMap lat={report.latitude} lng={report.longitude} onChange={() => {}} readOnly style={{ height: 140 }} />` (onChange is a required no-op for read-only); otherwise render a "No coordinates" placeholder reusing the existing grey-box styling. The invalidateSize fix added to LocationMap in plan 05-02 applies automatically.
    FIX-13 admin display (D-36): Add a `LOCATION_SOURCE_LABEL_MAP` Record and `getLocationSourceLabel(value): { en; kn }` helper to translations.ts following the getCategoryLabel pattern, mapping GPS_API→"GPS (device)", MANUAL_ADJUST→"Manual pin", EXIF_GPS→"Photo GPS", plus legacy fallbacks for the un-migrated `manual_pin`/`exif` strings, with a `{ en: value, kn: value }` default. In admin/reports/[id]/page.tsx change the EXIF condition at ~line 106 from `=== "exif"` to `=== "EXIF_GPS"`, and change the LOCATION_SRC value at ~line 347 from `report.location_source.toUpperCase()` to `getLocationSourceLabel(report.location_source ?? "").en`. Import getLocationSourceLabel from translations.ts.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/admin/reports/[id]/page.tsx contains `nextDynamic` importing LocationMap with `{ ssr: false }` and renders `<LocationMap` with `readOnly`
    - `grep -n '"exif"' frontend/app/admin/reports/[id]/page.tsx` returns 0 lines (condition now uses EXIF_GPS)
    - frontend/app/lib/translations.ts contains `getLocationSourceLabel` and the three canonical keys GPS_API, MANUAL_ADJUST, EXIF_GPS
    - admin/reports/[id]/page.tsx LOCATION_SRC value calls getLocationSourceLabel (no `.toUpperCase()` on location_source)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Admin detail shows a real read-only map when coordinates exist and the LOCATION_SRC label uses canonical values via the translation helper.</done>
</task>

<task type="auto">
  <name>Task 2: FIX-08 counter + FIX-09 scroll + FIX-11 build hash</name>
  <files>frontend/app/admin/page.tsx, frontend/app/admin/admin.css, frontend/app/admin/login/page.tsx, frontend/app/admin/lib/adminApi.ts, frontend/app/lib/config.ts</files>
  <read_first>
    - frontend/app/admin/lib/adminApi.ts (AdminStats TypeScript interface)
    - frontend/app/admin/page.tsx (the "+N today" render at ~line 357 currently using statsForCards.submitted; the outermost wrapper div and any parent with overflow/height styles)
    - frontend/app/admin/admin.css (the admin CSS token/layout layer — audit the scrollable container chain)
    - frontend/app/admin/login/page.tsx (hardcoded "BUILD_HASH: 0000000" at ~line 246)
    - frontend/app/lib/config.ts (APP_VERSION export at ~line 22 — mirror it for BUILD_HASH; lines 18-23)
    - .planning/phases/05-uat-stabilisation/05-PATTERNS.md (FIX-08, FIX-09, FIX-11 sections) and 05-RESEARCH.md (Pattern 3, Pitfall 5)
    - .planning/phases/05-uat-stabilisation/05-01-SUMMARY.md (confirm StatsResponse.today_count shipped)
  </read_first>
  <action>
    FIX-08 (D-20/D-21, Pitfall 5): Add `today_count: number;` to the AdminStats interface in adminApi.ts (matching the backend StatsResponse.today_count delivered by plan 05-01). In admin/page.tsx change the "+N today" counter at ~line 357 to read `stats.today_count` instead of `statsForCards.submitted`. Do NOT change the SUBMITTED/UNDER REVIEW/RESOLVED status cards.
    FIX-09 (D-22/D-23/D-24): Audit the admin dashboard scrollable container chain in admin/page.tsx and admin.css. Ensure the scrollable wrapper uses `overflow-y: auto` (not `hidden`), remove any `overscroll-behavior: none` on that wrapper, and replace any ancestor `height: 100vh` that traps scroll with `min-height: 100dvh`. Do NOT add `-webkit-overflow-scrolling: touch` (deprecated, forbidden by D-24).
    FIX-11 (D-27/D-29): Add `export const BUILD_HASH = process.env.NEXT_PUBLIC_BUILD_HASH ?? "0000000";` to config.ts following the APP_VERSION pattern. In admin/login/page.tsx import BUILD_HASH and replace the hardcoded `0000000` in the footer with `{BUILD_HASH}`. If the admin main footer also hardcodes the hash, replace it the same way. The CI injection of NEXT_PUBLIC_BUILD_HASH is handled in plan 05-04 — this task only wires the variable so the footer reads it at build time.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/admin/lib/adminApi.ts AdminStats interface contains `today_count`
    - frontend/app/admin/page.tsx renders `stats.today_count` for the +N today counter (no longer statsForCards.submitted there)
    - frontend/app/lib/config.ts contains `export const BUILD_HASH = process.env.NEXT_PUBLIC_BUILD_HASH`
    - `grep -rn "0000000" frontend/app/admin` returns 0 lines (footers use the BUILD_HASH variable)
    - `grep -rn "webkit-overflow-scrolling" frontend/app/admin` returns 0 lines (forbidden property not introduced)
    - `grep -rn "overscroll-behavior" frontend/app/admin` returns 0 lines (no trapping value introduced per D-23)
    - `grep -rn "overflow.*hidden" frontend/app/admin/admin.css frontend/app/admin/page.tsx` returns 0 lines on the scrollable dashboard wrapper (overflow:hidden removed)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The +N today counter is date-based, the dashboard scrolls freely on iOS, and footers render the injected build hash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → API (stats/report detail) | Authenticated admin reads stats and report data rendered in the portal |
| Build pipeline → frontend bundle (BUILD_HASH) | NEXT_PUBLIC_BUILD_HASH is baked into the public client bundle at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Information Disclosure | BUILD_HASH exposed in public bundle | accept | A short git SHA is non-sensitive build provenance; intentionally surfaced for support/debugging; no secret material |
| T-05-10 | Tampering | location_source label rendering (XSS via value) | mitigate | getLocationSourceLabel returns mapped strings or the raw value rendered as React text (auto-escaped); no dangerouslySetInnerHTML |
| T-05-11 | Information Disclosure | admin map exposes precise coordinates | accept | Coordinates are already shown to authenticated admins for triage; report data is admin-scoped behind auth |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- `grep -rn "0000000" frontend/app/admin` returns 0 lines
- `grep -rn "webkit-overflow-scrolling" frontend/app/admin` returns 0 lines
- `grep -c "getLocationSourceLabel" frontend/app/lib/translations.ts` returns at least 1
</verification>

<success_criteria>
The admin detail page shows a real read-only map; the +N today counter reflects today's creations regardless of status; the dashboard scrolls freely on iOS Safari; footers show the real build hash; and LOCATION_SRC uses canonical labels.
</success_criteria>

<output>
Create `.planning/phases/05-uat-stabilisation/05-03-SUMMARY.md` when done.
</output>
