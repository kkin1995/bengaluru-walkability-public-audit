---
phase: 05-uat-stabilisation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Cargo.toml
  - backend/src/handlers/reports.rs
  - backend/src/db/queries.rs
  - backend/src/db/admin_queries.rs
  - backend/src/models/admin.rs
  - backend/src/models/report.rs
  - backend/migrations/015_rename_location_source.sql
autonomous: true
requirements: [FIX-06, FIX-07, FIX-08, FIX-13]
nyquist_override: "integration-fix phase — primary verification is manual iOS Safari testing on staging.nammadaari.com; automated verify commands confirm compilation and DB migration, not unit test coverage. Wave 0 test scaffolds for bake_orientation, public history filter, and today_count are intentionally not created; the bake_orientation unit test is authored inline in Task 1 (orientation 1/6) and the SQL-layer FIX-07/FIX-08 changes are verified by cargo build + cargo sqlx prepare + manual staging checks."
must_haves:
  truths:
    - "iPhone-portrait photos (EXIF orientation 6) are stored upright on disk after upload"
    - "Public status history query returns exactly one Open entry per report: it excludes acknowledged rows AND the auto-assign duplicate open row, for both org-assigned and unassigned reports"
    - "Admin stats response includes a today_count field counting all reports created today regardless of status"
    - "New reports store location_source as GPS_API / EXIF_GPS / MANUAL_ADJUST, never manual_pin"
    - "Migration 015 converts every existing manual_pin row to GPS_API and exif row to EXIF_GPS"
  artifacts:
    - path: "backend/migrations/015_rename_location_source.sql"
      provides: "location_source canonical enum values + data migration"
      contains: "ALTER TYPE location_source ADD VALUE"
    - path: "backend/src/handlers/reports.rs"
      provides: "bake_orientation helper + canonical location_source defaults"
      contains: "fn bake_orientation"
    - path: "backend/src/db/queries.rs"
      provides: "public status history filter excluding acknowledged AND auto-assign duplicate open"
      contains: "!= 'acknowledged'"
    - path: "backend/src/models/admin.rs"
      provides: "StatsResponse.today_count field"
      contains: "today_count"
  key_links:
    - from: "backend/src/handlers/reports.rs"
      to: "image crate"
      via: "bake_orientation called before strip_exif in create_report"
      pattern: "bake_orientation"
    - from: "backend/src/db/admin_queries.rs"
      to: "StatsResponse"
      via: "today_count query populated into struct"
      pattern: "today_count"
---

<objective>
Fix the four backend-side UAT bugs in a single wave to minimise `cargo sqlx prepare` runs:
- FIX-06: bake EXIF orientation into pixels before the EXIF strip so iPhone-portrait photos are stored upright.
- FIX-07: filter `acknowledged` rows AND the auto-assign duplicate `open` row out of the public status history query so exactly one Open entry shows per report.
- FIX-08: add a creation-date-based `today_count` to the admin stats response.
- FIX-13 (backend portion): add canonical `location_source` enum values, migrate existing rows, and emit the new values from the report handler.

Purpose: These bugs were confirmed on the v1.0 live iPhone field test (staging.nammadaari.com, iOS 26.5). All four require Rust compilation and/or DB migration; grouping them avoids repeated SQLx metadata regeneration.

Output: A compiling backend with new helper `bake_orientation`, a filtered public history query (acknowledged + auto-assign open), a `today_count` stats field, migration `015_rename_location_source.sql`, and canonical location_source emission.
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-uat-stabilisation/05-CONTEXT.md
@.planning/phases/05-uat-stabilisation/05-RESEARCH.md
@.planning/phases/05-uat-stabilisation/05-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FIX-06 — bake EXIF orientation before strip (image crate)</name>
  <files>backend/Cargo.toml, backend/src/handlers/reports.rs</files>
  <read_first>
    - backend/src/handlers/reports.rs (read the full upload pipeline: validate → SHA256 → bbox/rate-limit → strip_exif at lines ~292-302, and the existing strip_exif helper at lines ~381-396 — replicate its signature style)
    - backend/Cargo.toml (confirm img-parts is present and image is absent before adding)
    - .planning/phases/05-uat-stabilisation/05-RESEARCH.md (Pattern 1, Pitfall 6, Assumptions A1/A2 — image crate API; verify rotate90/rotate180/rotate270 and JpegEncoder::new_with_quality against installed image 0.25 docs before coding)
    - .planning/phases/05-uat-stabilisation/05-PATTERNS.md (FIX-06 section — exact bake_orientation skeleton and read_exif_orientation_tag approach)
  </read_first>
  <behavior>
    - orientation value 1 (or absent / 0): bake_orientation returns the input bytes unchanged (no decode, no re-encode)
    - orientation value 6 (iPhone portrait): output image width/height are swapped relative to input (rotated 90 CW)
    - orientation value 3: pixels rotated 180
    - orientation value 8: rotated 90 CCW
    - malformed / non-JPEG bytes: returns AppError::BadRequest (does not panic)
  </behavior>
  <action>
    FIX-06 (D-12/D-13/D-14/D-16): Add `image = "0.25"` to backend/Cargo.toml dependencies (D-13 — image crate is approved per RESEARCH.md Package Legitimacy Audit: crates.io, image-rs/image, ~12yr old). Add a `bake_orientation(bytes: &[u8]) -> Result<Vec<u8>, crate::errors::AppError>` helper in reports.rs using the same signature/error style as `strip_exif`. Per D-13 it reads the EXIF Orientation tag (TIFF IFD0 tag 0x0112) from the raw EXIF payload returned by `img_parts::jpeg::Jpeg::exif()`; per D-16, if the tag is absent or equals 1, return the original bytes unchanged (no decode, no re-encode). Otherwise decode via `image::load_from_memory`, apply the transform per the orientation→transform table (1=no-op, 3=rotate180, 6=rotate90, 8=rotate270, 2=fliph, 4=flipv, 5=rotate90+fliph, 7=rotate270+fliph, unknown=passthrough), and re-encode to JPEG at quality 85 (D-14) using `image::codecs::jpeg::JpegEncoder::new_with_quality(&mut out, 85)` + `write_with_encoder`. Provide a `read_exif_orientation_tag` helper that parses tag 0x0112 from the EXIF bytes (minimal TIFF IFD parse handling both little- and big-endian byte order markers `II`/`MM`). In `create_report`, insert `let oriented_bytes = bake_orientation(&req.image_bytes)?;` immediately BEFORE the existing `strip_exif` call (D-12), then change the strip call to operate on `&oriented_bytes` instead of `&req.image_bytes`. Do NOT alter the SHA256/bbox/rate-limit steps that run earlier. Scope is new uploads only per D-15 — do not add any migration for existing stored photos. This task changes no SQL query strings, so `cargo sqlx prepare` is not required for Task 1 (the plan-level verification block runs it once after Task 2/Task 3 SQL changes).
  </action>
  <verify>
    <automated>cd backend && cargo test bake_orientation 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/Cargo.toml contains a line matching `image = "0.25`
    - backend/src/handlers/reports.rs contains `fn bake_orientation`
    - backend/src/handlers/reports.rs contains `bake_orientation(&req.image_bytes)` invoked before the strip_exif call in create_report
    - A unit test for bake_orientation exists asserting orientation 1 returns input unchanged and orientation 6 swaps width/height; `cd backend && cargo test bake_orientation` exits 0
    - `cd backend && cargo build` exits 0
  </acceptance_criteria>
  <done>bake_orientation runs before strip_exif, the image crate is a dependency, and unit tests for orientation 1/6 pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIX-07 + FIX-08 — public history filter (acknowledged + auto-assign open) and today_count stat</name>
  <files>backend/src/db/queries.rs, backend/src/db/admin_queries.rs, backend/src/models/admin.rs</files>
  <read_first>
    - backend/src/db/queries.rs (status history query at lines ~381-386, and the lat/lng 3dp rounding at lines ~403-406; confirm the status_history table has a `note` column)
    - backend/src/handlers/reports.rs (~line 308 auto-assign audit insert — confirm the EXACT `note` text it writes for the duplicate `open` status_history row; the LIKE pattern below MUST match this prefix)
    - backend/src/db/admin_queries.rs (get_report_stats at lines ~826-901, query_scalar pattern at line ~827; AND the admin status history query path — confirm it is separate and stays unfiltered)
    - backend/src/models/admin.rs (StatsResponse struct at lines ~242-251)
    - .planning/phases/05-uat-stabilisation/05-PATTERNS.md (FIX-07 and FIX-08 sections — exact SQL and struct edits) and 05-RESEARCH.md (Pattern 2, Pitfall 3, Assumption A6, Open Question 1 RESOLVED)
  </read_first>
  <behavior>
    - public status history for an org-assigned report (auto-assign wrote a second `open` row) returns exactly ONE `open` row, not two
    - public status history for a report that has an 'acknowledged' transition excludes that row
    - public status history still returns the genuine 'open', 'in_progress', 'resolved', 'closed' transition rows in ascending changed_at order
    - the admin status history query path is unchanged and still returns acknowledged AND the auto-assign audit row (admins keep the full timeline)
    - get_report_stats returns a StatsResponse whose today_count equals COUNT(*) of reports with created_at::date = CURRENT_DATE, independent of status
  </behavior>
  <action>
    FIX-07 (D-17/D-18/D-19): In the PUBLIC status history query in queries.rs, add TWO filter clauses to the existing WHERE so the public timeline shows exactly one Open entry per the ROADMAP success criterion ("exactly one Open entry per report"). Keep `WHERE report_id = $1` and `ORDER BY changed_at ASC`, and add: (a) `AND new_status::TEXT != 'acknowledged'` (D-17 — removes the acknowledge-as-Open entry, Source B), and (b) `AND (note IS NULL OR note NOT LIKE 'Auto-assigned%')` (removes the auto-assign duplicate `open` row, Source A). Before finalising clause (b), READ reports.rs ~line 308 and confirm the exact note text the auto-assign audit insert writes; if its prefix is not literally "Auto-assigned", adjust the LIKE pattern to match the actual prefix (Assumption A6). Both clauses are SQL-layer only (D-19) and change NO write logic — the full audit trail stays in status_history. Per D-18, do NOT touch the admin portal status history query — admins must keep the full timeline including acknowledged and the auto-assign audit row; verify the admin history query is a separate code path and leave it unfiltered. Do NOT modify the auto-assign insert in reports.rs (write behaviour preserved).
    FIX-08 (D-20/D-21): Add `pub today_count: i64,` to the StatsResponse struct in models/admin.rs (after total_reports). In admin_queries.rs get_report_stats, add `let today_count: i64 = sqlx::query_scalar("SELECT COUNT(*) FROM reports WHERE created_at::date = CURRENT_DATE").fetch_one(pool).await?;` following the existing query_scalar pattern, and add `today_count,` to the returned `StatsResponse { ... }` literal. Do NOT change the SUBMITTED/UNDER REVIEW/RESOLVED by_status cards (D-21). Run `cargo sqlx prepare --database-url "$DATABASE_URL"` after these query changes so offline SQLx metadata stays valid.
  </action>
  <verify>
    <automated>cd backend && cargo test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/db/queries.rs PUBLIC status history query contains `!= 'acknowledged'`
    - backend/src/db/queries.rs PUBLIC status history query contains a `note NOT LIKE` clause filtering the auto-assign duplicate open row (pattern confirmed against reports.rs auto-assign note text)
    - backend/src/models/admin.rs contains `today_count`
    - backend/src/db/admin_queries.rs contains `created_at::date = CURRENT_DATE`
    - backend/src/db/admin_queries.rs returns a StatsResponse literal containing `today_count`
    - The admin status history query path does NOT contain `!= 'acknowledged'` and does NOT contain the auto-assign `note NOT LIKE` filter (admin keeps full timeline per D-18)
    - `cd backend && cargo build` exits 0
  </acceptance_criteria>
  <done>Public history shows exactly one Open entry (excludes acknowledged AND the auto-assign duplicate open), admin history keeps everything, StatsResponse carries today_count from a date-only COUNT, and the backend compiles with updated SQLx metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FIX-13 backend — canonical location_source migration + emission</name>
  <files>backend/migrations/015_rename_location_source.sql, backend/src/handlers/reports.rs, backend/src/models/report.rs</files>
  <read_first>
    - backend/migrations/014_link_wards_to_organisations.sql (most recent migration — mirror header/comment style)
    - backend/migrations/001_init.sql (confirm the location_source enum currently defines only 'exif' and 'manual_pin')
    - backend/src/handlers/reports.rs (location_source default at line ~216 and fake_success_response at line ~91)
    - backend/src/models/report.rs (test fixture at line ~186 and the test_default_location_source_is_manual_pin test at lines ~583-590)
    - .planning/phases/05-uat-stabilisation/05-PATTERNS.md (FIX-13 backend + migration sections) and 05-RESEARCH.md (Pattern 4, Pitfall 4, Assumption A5)
  </read_first>
  <behavior>
    - after migration runs, no reports row has location_source = 'manual_pin' or 'exif'
    - rows previously 'manual_pin' become 'GPS_API'; rows previously 'exif' become 'EXIF_GPS'
    - create_report default location_source is emitted as 'GPS_API', never 'manual_pin'
  </behavior>
  <action>
    FIX-13 backend (D-32 full-stack fix — this plan owns the backend + migration portion; frontend portions are in plans 05-02 and 05-03). Create backend/migrations/015_rename_location_source.sql. Per D-33/D-35 and RESEARCH.md Pattern 4: add the three canonical values with `ALTER TYPE location_source ADD VALUE IF NOT EXISTS 'GPS_API';`, `... 'MANUAL_ADJUST';`, `... 'EXIF_GPS';`, then `UPDATE reports SET location_source = 'GPS_API' WHERE location_source::TEXT = 'manual_pin';` and `UPDATE reports SET location_source = 'EXIF_GPS' WHERE location_source::TEXT = 'exif';`. Do NOT attempt `ALTER TYPE ... DROP VALUE` (PostgreSQL does not support it — old values stay in the enum but are never emitted). PostgreSQL cannot run `ALTER TYPE ... ADD VALUE` inside a transaction block in older versions; verify the installed SQLx migration pragma for non-transactional migrations and add the appropriate `-- sqlx:` pragma if `cargo run`/`sqlx::migrate!` fails with "ALTER TYPE ... ADD VALUE cannot run inside a transaction block".
    Backend emission (D-34): in reports.rs change the `location_source` default at line ~216 from `"manual_pin"` to `"GPS_API"`, and the fake_success_response value at line ~91 from `"manual_pin"` to `"GPS_API"`. Update the test fixture in models/report.rs line ~186 and rename/adjust `test_default_location_source_is_manual_pin` in reports.rs to expect `"GPS_API"`. The frontend display mapping (D-36) and the frontend value emission (D-33 client side) are handled in plans 05-03 and 05-02 — do NOT edit any frontend files here. Run `cargo sqlx prepare --database-url "$DATABASE_URL"` after the migration is applied so query metadata reflects the new enum values.
  </action>
  <verify>
    <automated>cd backend && cargo test location_source 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/migrations/015_rename_location_source.sql exists and contains `ALTER TYPE location_source ADD VALUE IF NOT EXISTS 'GPS_API'`
    - The migration contains `UPDATE reports SET location_source = 'GPS_API'` and `UPDATE reports SET location_source = 'EXIF_GPS'`
    - The migration contains no `DROP VALUE`
    - backend/src/handlers/reports.rs no longer contains the string `"manual_pin"`
    - The default location_source test expects `GPS_API`; `cd backend && cargo test location_source` exits 0
  </acceptance_criteria>
  <done>Migration 015 adds canonical values and converts existing rows; backend emits GPS_API by default; tests updated and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (multipart upload) | Untrusted image bytes cross into the orientation-baking + EXIF-strip pipeline |
| API → PostgreSQL (location_source) | Enum value bound into a typed column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | bake_orientation (malformed/polyglot JPEG) | mitigate | is_jpeg() magic-byte validation already runs BEFORE bake_orientation; image::load_from_memory returns Err on malformed input — map to AppError::BadRequest, never panic |
| T-05-02 | Denial of Service | image decode of oversized image | accept | Existing upload size limit and rate limiting (ABUSE-03) bound input before decode; civic photos are small; risk low |
| T-05-03 | Tampering | location_source enum injection | mitigate | SQLx typed `$N::location_source` binding rejects invalid enum values at the DB layer; backend code only ever emits GPS_API/EXIF_GPS/MANUAL_ADJUST |
| T-05-04 | Information Disclosure | EXIF GPS retained after orientation bake | mitigate | strip_exif still runs AFTER bake_orientation, removing all EXIF including GPS — pipeline order preserved |
| T-05-SC | Tampering | cargo add image crate | mitigate | image crate approved in RESEARCH.md Package Legitimacy Audit (crates.io, image-rs/image, ~12yr, very high downloads) — no [ASSUMED]/[SUS] verdict, no blocking checkpoint required |
</threat_model>

<verification>
- `cd backend && cargo build` exits 0
- `cd backend && cargo test` passes (orientation, history filter, today_count, location_source tests)
- `cd backend && cargo sqlx prepare --database-url "$DATABASE_URL"` exits 0 (per CLAUDE.md — regenerate offline SQLx metadata after the FIX-07/FIX-08/FIX-13 query and migration changes; required so CI/offline builds verify against the new enum values and today_count query)
- `grep -rn "manual_pin" backend/src/handlers/reports.rs` returns 0 lines
- Migration file present and contains ADD VALUE + UPDATE, no DROP VALUE
- Public status history query contains BOTH `!= 'acknowledged'` and the auto-assign `note NOT LIKE` filter; admin history path contains neither
</verification>

<success_criteria>
All four backend FIX requirements compile and pass unit tests: orientation baked before strip; public history shows exactly one Open entry per report (acknowledged + auto-assign duplicate open both filtered, admin history unchanged); today_count present and date-based; location_source canonical values migrated and emitted.
</success_criteria>

<output>
Create `.planning/phases/05-uat-stabilisation/05-01-SUMMARY.md` when done. In the SUMMARY, record the exact auto-assign note text confirmed at reports.rs ~line 308 and the final `note NOT LIKE` pattern used, so verification can confirm the FIX-07 "exactly one Open entry" criterion holds for org-assigned reports.
</output>
</content>
</invoke>
