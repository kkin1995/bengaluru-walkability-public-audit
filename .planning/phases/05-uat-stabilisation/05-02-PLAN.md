---
phase: 05-uat-stabilisation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/reports/[id]/page.tsx
  - frontend/app/components/redesign/SuccessCard.tsx
  - frontend/app/map/page.tsx
  - frontend/app/report/page.tsx
  - frontend/app/components/LocationMap.tsx
  - frontend/app/components/ReportsMap.tsx
  - frontend/app/components/ReportCTA.tsx
  - frontend/app/lib/photo-store.ts
  - frontend/next.config.mjs
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04, FIX-10, FIX-12, FIX-13]
must_haves:
  truths:
    - "Public report detail page builds the photo src from API_BASE_URL + /uploads/ + filename, never the raw internal image_url"
    - "Report another CTA navigates the browser to / (home)"
    - "Report here FAB on /map links to / (home)"
    - "/report and /reports return a permanent redirect to /"
    - "Every Leaflet map component in the citizen/public flow (LocationMap.tsx AND ReportsMap.tsx) calls map.invalidateSize() shortly after mount"
    - "Citizen form GPS coordinates render at 3 decimal places"
    - "No frontend file renders the string Auto-routed"
    - "Citizen report submission sends location_source as GPS_API or EXIF_GPS, never manual_pin or exif"
  artifacts:
    - path: "frontend/app/reports/[id]/page.tsx"
      provides: "browser-accessible public image URL"
      contains: "API_BASE_URL"
    - path: "frontend/next.config.mjs"
      provides: "301/308 redirects for deprecated routes"
      contains: "redirects"
    - path: "frontend/app/components/LocationMap.tsx"
      provides: "MapSizeUpdater invalidateSize fix"
      contains: "invalidateSize"
    - path: "frontend/app/components/ReportsMap.tsx"
      provides: "invalidateSize fix on the public reports map (D-10 scope)"
      contains: "invalidateSize"
  key_links:
    - from: "frontend/app/reports/[id]/page.tsx"
      to: "frontend/app/lib/config.ts"
      via: "import API_BASE_URL"
      pattern: "API_BASE_URL"
    - from: "frontend/app/components/LocationMap.tsx"
      to: "react-leaflet useMap"
      via: "MapSizeUpdater child of MapContainer"
      pattern: "useMap"
---

<objective>
Fix the citizen-facing and public frontend UAT bugs that do not require admin-portal files:
- FIX-01: reconstruct the public report photo URL from API_BASE_URL (the raw image_url contains an internal Docker hostname unreachable from the browser).
- FIX-02 / FIX-03: point the "Report another" CTA and the "/map" FAB to `/`, and add permanent redirects for the deprecated `/report` and `/reports` routes.
- FIX-04: add `invalidateSize()` to ALL citizen/public Leaflet map components — LocationMap.tsx AND ReportsMap.tsx — so tiles render in iOS Safari (D-10 scope).
- FIX-10: round citizen-form GPS coordinates to 3 decimal places.
- FIX-12: standardise the ward label to "Auto-detected".
- FIX-13 (frontend emission): send canonical location_source values (GPS_API / EXIF_GPS) from the citizen flow.

Purpose: These bugs blocked or degraded the citizen reporting flow during the v1.0 live iPhone field test. All edits live in citizen/public frontend files with no overlap with admin-portal files (owned by plan 05-03) or backend files (plan 05-01).

Output: A working public detail page photo, corrected navigation/redirects, iOS-safe map sizing on both LocationMap and ReportsMap, 3dp coordinates, consistent ward label, and canonical location_source emission.
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-uat-stabilisation/05-CONTEXT.md
@.planning/phases/05-uat-stabilisation/05-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-01 photo URL + FIX-02/03 navigation + redirects</name>
  <files>frontend/app/reports/[id]/page.tsx, frontend/app/components/redesign/SuccessCard.tsx, frontend/app/report/page.tsx, frontend/app/map/page.tsx, frontend/next.config.mjs</files>
  <read_first>
    - frontend/app/reports/[id]/page.tsx (imports at lines ~11-13; the `<img src={report.image_url}>` at line ~298)
    - frontend/app/lib/config.ts (API_BASE_URL export at line ~18 — already exists, do not edit)
    - frontend/app/components/redesign/SuccessCard.tsx (onReportAnother button at lines ~254-256 — callback prop, not a link)
    - frontend/app/report/page.tsx (where SuccessCard is rendered ~line 419; the onClose handler using window.location.href = "/" at ~line 422 — mirror it)
    - frontend/app/map/page.tsx (FAB href="/report" at line ~269)
    - frontend/next.config.mjs (existing rewrites() block at lines ~29-43 — add redirects() alongside it)
    - .planning/phases/05-uat-stabilisation/05-PATTERNS.md (FIX-01, FIX-02/03 sections — exact code)
  </read_first>
  <action>
    FIX-01 (D-01/D-02): In reports/[id]/page.tsx, add `API_BASE_URL` to the existing `@/app/lib/config` import. After the report is fetched, derive imageFilename by splitting report.image_url on "/uploads/" and taking the last segment, then build publicImageUrl as `API_BASE_URL` + "/uploads/" + imageFilename (empty string when no filename). Change the `<img>` src from report.image_url to publicImageUrl. Never inline `process.env.*` in the component (CLAUDE.md config rule). Do NOT touch the AdminReport image_url/image_path tech debt (D-04 — out of scope).
    FIX-02 (D-05): In report/page.tsx, change the onReportAnother prop passed to SuccessCard from the current resetAll-based callback to a handler that sets `window.location.href = "/"`, matching the onClose pattern already at ~line 422. Do not modify SuccessCard.tsx for this fix — the navigation decision lives in the caller.
    FIX-03 (D-06): In map/page.tsx, change the FAB href from `/report` to `/`.
    FIX-02/03 redirects (D-07): In next.config.mjs add an `async redirects()` method on the same nextConfig object as `rewrites()`, returning permanent redirects from source `/report` to `/` and from source `/reports` to `/`, both with `permanent: true`. Do NOT delete the route page files — the redirect intercepts first.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/reports/[id]/page.tsx imports API_BASE_URL and the `<img>` src uses the derived publicImageUrl (not report.image_url directly)
    - frontend/app/report/page.tsx contains `window.location.href = "/"` wired to onReportAnother
    - `grep -n 'href="/report"' frontend/app/map/page.tsx` returns 0 lines (FAB now points to "/")
    - frontend/next.config.mjs contains an `async redirects()` returning source "/report" and "/reports" with `permanent: true`
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Public photo loads from the public URL, both deprecated-route CTAs navigate home, and permanent redirects exist for /report and /reports.</done>
</task>

<task type="auto">
  <name>Task 2: FIX-04 invalidateSize (LocationMap + ReportsMap) + FIX-10 3dp + FIX-12 label + FIX-13 emission</name>
  <files>frontend/app/components/LocationMap.tsx, frontend/app/components/ReportsMap.tsx, frontend/app/report/page.tsx, frontend/app/components/redesign/SuccessCard.tsx, frontend/app/components/ReportCTA.tsx, frontend/app/lib/photo-store.ts</files>
  <read_first>
    - frontend/app/components/LocationMap.tsx (existing useEffect icon-fix at lines ~98-109; the MapContainer + TileLayer JSX; confirm "use client" at line 1)
    - frontend/app/components/ReportsMap.tsx (confirm it renders its own `<MapContainer>` + `<TileLayer>` and is a "use client" component; identify where to add the MapSizeUpdater child — this is the third map named in D-10 scope)
    - frontend/app/report/page.tsx (toFixed(4) at lines ~662 and ~970; locationSource type at ~46, defaults at ~60/156/319/918)
    - frontend/app/components/redesign/SuccessCard.tsx ("Auto-routed" hardcoded at line ~241)
    - frontend/app/components/ReportCTA.tsx (location_source emitted values at lines ~58, 74, 121, 137)
    - frontend/app/lib/photo-store.ts (locationSource type definition "exif" | "manual_pin")
    - .planning/phases/05-uat-stabilisation/05-PATTERNS.md (FIX-04, FIX-10, FIX-12, FIX-13 frontend send-values sections) and 05-RESEARCH.md (Pattern 5, Assumption A3 — confirm react-leaflet useMap exists in the installed version per package.json)
  </read_first>
  <action>
    FIX-04 (D-09/D-10): D-10 scopes the invalidateSize fix to ALL Leaflet map components; this plan owns the two citizen/public maps — LocationMap.tsx AND ReportsMap.tsx (the admin report detail map is owned by plan 05-03). Add a `MapSizeUpdater` child component that imports `useMap` from react-leaflet and, inside a useEffect, calls setTimeout to run `map.invalidateSize()` after ~100ms and clears the timeout on cleanup. Add `<MapSizeUpdater />` as a child of the `<MapContainer>` (sibling to the existing `<TileLayer>`) in BOTH LocationMap.tsx and ReportsMap.tsx. MapSizeUpdater must live inside each already-client ("use client") component — no extra dynamic wrapper. If both files can share one MapSizeUpdater, factor it into a small shared module and import it into both; otherwise define it inline in each. The nginx CSP portion of FIX-04 is handled in plan 05-04; do not touch nginx here.
    FIX-10 (D-25): In report/page.tsx change both GPS coordinate renders from toFixed(4) to toFixed(3) (~lines 662 and 970). Per D-26 the backend already rounds to 3dp (queries.rs ~403-406, verified in RESEARCH.md) — no backend change needed.
    FIX-12 (D-30/D-31): Replace the hardcoded `Auto-routed` in SuccessCard.tsx (~line 241) with `Auto-detected`. Then grep all frontend files for `Auto-routed`, `auto-routed`, `Auto routed` (case variations) and replace every remaining occurrence with `Auto-detected`.
    FIX-13 frontend emission (D-33): Update photo-store.ts locationSource type from `"exif" | "manual_pin"` to `"EXIF_GPS" | "GPS_API" | "MANUAL_ADJUST"`. In report/page.tsx update the type at ~line 46, the default at ~60 to `GPS_API`, the post-geolocation value at ~156 to `GPS_API`, and the post-EXIF value at ~319 to `gps ? "EXIF_GPS" : "GPS_API"`. In ReportCTA.tsx change emitted `"exif"` to `"EXIF_GPS"` and `"manual_pin"` to `"GPS_API"` at lines ~58, 74, 121, 137. The DB migration, backend emission, and admin-portal display mapping are handled in plans 05-01 and 05-03 — do NOT edit translations.ts or admin files here.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/components/LocationMap.tsx contains `invalidateSize` and imports `useMap`
    - frontend/app/components/ReportsMap.tsx contains `invalidateSize` (D-10 scope — the third map)
    - `grep -rni "auto.routed" frontend/app | grep -v node_modules | wc -l` returns 0
    - `grep -n "toFixed(4)" frontend/app/report/page.tsx` returns 0 lines for the GPS coordinate renders (both changed to toFixed(3))
    - `grep -En '"exif"|"manual_pin"' frontend/app/report/page.tsx frontend/app/components/ReportCTA.tsx frontend/app/lib/photo-store.ts | wc -l` returns 0
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both LocationMap and ReportsMap invalidate size on mount, coordinates show 3dp, no Auto-routed strings remain, and the citizen flow emits canonical location_source values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → public image URL | Client constructs an image src from API_BASE_URL + filename derived from API data |
| Browser → API (location_source field) | Client sends a location_source enum string to the backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | publicImageUrl construction (path traversal via image_url) | mitigate | Extract only the basename via `.split("/uploads/").pop()` — never use the full path; backend ServeDir already scopes to the uploads directory |
| T-05-06 | Tampering | client-sent location_source value | mitigate | Client emits only GPS_API/EXIF_GPS/GPS_API; backend SQLx `$N::location_source` typed binding rejects any non-enum value at the DB layer |
| T-05-07 | Information Disclosure | redirect open-redirect via /report, /reports | accept | Redirect destinations are hardcoded to "/" — no user-controlled target; no open-redirect surface |
| T-05-08 | Spoofing | Leaflet tiles loaded over the network | accept | Tiles are public OSM imagery served over HTTPS; CSP allow-listing handled in plan 05-04; no sensitive data |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- `grep -rni "auto.routed" frontend/app | grep -v node_modules` returns 0 lines
- `grep -n 'href="/report"' frontend/app/map/page.tsx` returns 0 lines
- `grep -c "redirects" frontend/next.config.mjs` returns at least 1
- `grep -c "invalidateSize" frontend/app/components/LocationMap.tsx frontend/app/components/ReportsMap.tsx` returns 1 for each file (D-10 — both maps fixed)
</verification>

<success_criteria>
The public photo renders from a browser-reachable URL; deprecated-route CTAs and redirects send users home; both the citizen confirm map (LocationMap) and the public reports map (ReportsMap) size correctly for iOS Safari per D-10; coordinates display at 3dp; the ward label is uniformly "Auto-detected"; and the citizen flow emits canonical location_source values.
</success_criteria>

<output>
Create `.planning/phases/05-uat-stabilisation/05-02-SUMMARY.md` when done. Note whether MapSizeUpdater was factored into a shared module or defined inline in each map component.
</output>
