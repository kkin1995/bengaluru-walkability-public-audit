---
phase: 05-uat-stabilisation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - nginx/nginx.conf
  - .github/workflows/deploy.yml
  - frontend/Dockerfile
autonomous: false
requirements: [FIX-04, FIX-11]
user_setup:
  - service: vercel
    why: "Frontend deploys to Vercel (deploy.yml only builds the backend on the LXC). NEXT_PUBLIC_BUILD_HASH must be injected into the Vercel build, which requires Vercel project access."
    dashboard_config:
      - task: "Set NEXT_PUBLIC_BUILD_HASH env var (or override the build command to inject git short SHA)"
        location: "Vercel Dashboard → Project → Settings → Environment Variables / Build & Development Settings"
must_haves:
  truths:
    - "The nginx public location / block sends a Content-Security-Policy header allowing https://*.tile.openstreetmap.org in both img-src and connect-src"
    - "The deployed frontend build injects a real git short SHA into NEXT_PUBLIC_BUILD_HASH"
  artifacts:
    - path: "nginx/nginx.conf"
      provides: "public-route CSP allowing OSM tiles"
      contains: "tile.openstreetmap.org"
  key_links:
    - from: "nginx/nginx.conf location /"
      to: "OSM tile CDN"
      via: "CSP img-src + connect-src allowlist"
      pattern: "tile.openstreetmap.org"
---

<objective>
Fix the two infrastructure/CI-layer UAT bugs:
- FIX-04 (nginx portion): add a Content-Security-Policy header to the public nginx `location /` block that allows the OSM tile origin in both `img-src` and `connect-src`, so iOS Safari permits Leaflet tile fetches on the citizen confirm map and public `/map`.
- FIX-11 (CI portion): inject a real git short SHA into `NEXT_PUBLIC_BUILD_HASH` at frontend build time.

Purpose: These are the non-application-code fixes from the v1.0 live iPhone field test. iOS Safari strictly enforces CSP — the public routes currently send no CSP header, but Leaflet's XHR tile fetches still fail in some iOS contexts; the admin block already allows OSM tiles, the public block does not. FIX-11 fails on the wrong layer in research's open question: the frontend deploys to Vercel, not the LXC Docker stack, so the injection point must be decided before implementation.

Output: A CSP-protected public nginx block allowing OSM tiles, and a confirmed build-time injection path for the build hash.
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-uat-stabilisation/05-CONTEXT.md
@.planning/phases/05-uat-stabilisation/05-PATTERNS.md
@.planning/phases/05-uat-stabilisation/05-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-04 — public-route CSP allowing OSM tiles</name>
  <files>nginx/nginx.conf</files>
  <read_first>
    - nginx/nginx.conf (the admin location block CSP at line ~198; the public `location /` block at lines ~211-220 which currently has NO CSP header; confirm /uploads/ has no auth guard per D-03)
    - .planning/phases/05-uat-stabilisation/05-PATTERNS.md (FIX-04 nginx section — exact header value) and 05-RESEARCH.md (Pattern 6, Pitfall 7 — add_header does NOT inherit into nested locations)
  </read_first>
  <action>
    FIX-04 (D-08/D-10): Add a `Content-Security-Policy` header directly to the public `location /` block (not the server block — nginx add_header does not inherit into a location that has its own add_header, per Pitfall 7). Use the value: `default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://unpkg.com; img-src 'self' data: blob: https://unpkg.com https://*.tile.openstreetmap.org; connect-src 'self' https://*.tile.openstreetmap.org;` with the `always` flag. The key difference from the existing admin CSP is that `connect-src` must include `https://*.tile.openstreetmap.org` (iOS Safari enforces connect-src for Leaflet's XHR tile fetches). Do NOT switch tile providers (D-11). While in this file, confirm the `/uploads/` location has no auth guard that would block unauthenticated FIX-01 photo requests (D-03) — if one exists, remove it and note it in the SUMMARY.
  </action>
  <verify>
    <automated>grep "Content-Security-Policy" nginx/nginx.conf | grep -o "tile.openstreetmap.org" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - nginx/nginx.conf public `location /` block contains a `Content-Security-Policy` `add_header` with `always`
    - That CSP value contains `https://*.tile.openstreetmap.org` in BOTH `img-src` and `connect-src`
    - `nginx -t` equivalent passes if nginx is available locally; otherwise the config diff is syntactically valid (matching the admin block's add_header form)
    - The /uploads/ location has no auth/deny directive blocking unauthenticated GETs
  </acceptance_criteria>
  <done>The public nginx location sends a CSP that allows OSM tiles for both image loads and XHR connects, matching the working admin block plus the connect-src addition.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: choose the NEXT_PUBLIC_BUILD_HASH injection point</name>
  <action>Pause for the user to choose where NEXT_PUBLIC_BUILD_HASH is injected for the production frontend build, before implementing FIX-11 in Task 2. Present the three options below and wait for the resume signal.</action>
  <decision>Where is NEXT_PUBLIC_BUILD_HASH injected for the production frontend build?</decision>
  <context>
    RESEARCH.md Open Question 2 and a direct read of `.github/workflows/deploy.yml` confirm the production frontend deploys to Vercel (the deploy job only builds the `backend` Docker image on the LXC; the smoke test hits `vars.FRONTEND_URL`, a Vercel URL). The frontend Dockerfile already declares `ARG NEXT_PUBLIC_API_URL` / `ENV` and runs `npm run build`, but that Docker image is NOT built in the production deploy path. So `NEXT_PUBLIC_BUILD_HASH` cannot be injected by editing deploy.yml's SSH step. The injection point must be chosen before implementation because the two paths edit different files and require different access.
    The config.ts BUILD_HASH export and footer wiring are already handled in plan 05-03; this checkpoint only decides the CI/build injection mechanism.
  </context>
  <options>
    <option id="vercel-build-command">
      <label>Vercel build command override (recommended)</label>
      <pros>Matches the actual production deploy path; produces a fresh per-deploy SHA; no Docker build added to CI</pros>
      <cons>Requires the user to set the Vercel project Build Command to `NEXT_PUBLIC_BUILD_HASH=$(git rev-parse --short HEAD) npm run build` (or a Vercel env var) in the dashboard — Claude cannot do this</cons>
    </option>
    <option id="vercel-env-var">
      <label>Vercel project environment variable</label>
      <pros>Simple dashboard toggle; no build-command change</pros>
      <cons>A static env var does not auto-update per deploy unless tied to Vercel's VERCEL_GIT_COMMIT_SHA system var; user must map NEXT_PUBLIC_BUILD_HASH to VERCEL_GIT_COMMIT_SHA in the dashboard</cons>
    </option>
    <option id="docker-build-arg">
      <label>Docker build-arg in deploy.yml (only if frontend moves to LXC Docker)</label>
      <pros>Keeps injection in version-controlled CI; passes `--build-arg NEXT_PUBLIC_BUILD_HASH=$(git rev-parse --short HEAD)` with a matching `ARG`/`ENV` in frontend/Dockerfile</pros>
      <cons>Only valid if the frontend is actually built and served from the LXC Docker stack; currently it is NOT — this would change the deployment architecture (out of scope for a bug-fix phase)</cons>
    </option>
  </options>
  <resume-signal>Select: vercel-build-command, vercel-env-var, or docker-build-arg — and confirm whether you will set the Vercel dashboard config yourself.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: FIX-11 — implement the chosen build-hash injection</name>
  <files>.github/workflows/deploy.yml, frontend/Dockerfile</files>
  <read_first>
    - .github/workflows/deploy.yml (the "Build and deploy on LXC" step ~lines 41-53; the smoke-test "Frontend check — Vercel" step)
    - frontend/Dockerfile (existing `ARG NEXT_PUBLIC_API_URL` / `ENV` declarations and the `RUN npm run build` line — mirror for BUILD_HASH only if the docker-build-arg option was chosen)
    - .planning/phases/05-uat-stabilisation/05-PATTERNS.md (FIX-11 section) and 05-RESEARCH.md (Open Question 2)
  </read_first>
  <action>
    Implement the option selected in the preceding checkpoint (FIX-11, D-27/D-29):
    - If `vercel-build-command` or `vercel-env-var`: the actual injection is a Vercel dashboard action by the user (recorded in user_setup). In this repo, document the chosen mechanism with a comment block at the top of deploy.yml under the existing "Required GitHub Actions configuration" header, noting that NEXT_PUBLIC_BUILD_HASH is injected in Vercel (build command or env var mapped to VERCEL_GIT_COMMIT_SHA). Do NOT add a Docker frontend build to deploy.yml. Make no code change that would set a static hash (D-28).
    - If `docker-build-arg`: add `ARG NEXT_PUBLIC_BUILD_HASH` and `ENV NEXT_PUBLIC_BUILD_HASH=$NEXT_PUBLIC_BUILD_HASH` to frontend/Dockerfile (before `RUN npm run build`, mirroring the NEXT_PUBLIC_API_URL pattern), and pass `--build-arg NEXT_PUBLIC_BUILD_HASH=$(git rev-parse --short HEAD)` in the deploy.yml frontend build step.
    The config.ts BUILD_HASH export and footer reads are already wired in plan 05-03 — do not re-implement them here.
  </action>
  <verify>
    <automated>grep -c "NEXT_PUBLIC_BUILD_HASH" .github/workflows/deploy.yml frontend/Dockerfile 2>/dev/null | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - The chosen injection mechanism is implemented or documented in the repo: either deploy.yml carries a comment documenting the Vercel injection point, or frontend/Dockerfile + deploy.yml carry the build-arg wiring (depending on the checkpoint decision)
    - No static hardcoded hash is introduced anywhere (D-28); `grep -rn "0000000" .github frontend/Dockerfile` returns 0 lines
    - The mechanism produces a per-deploy git short SHA (documented or wired)
  </acceptance_criteria>
  <done>The build-hash injection path chosen at the checkpoint is implemented/documented so the admin footer (wired in 05-03) renders a real SHA on the next production deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS Safari → nginx (CSP enforcement) | Browser enforces the CSP header nginx returns for public routes |
| CI/build → public frontend bundle | Build-time env var baked into the client-served bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | CSP allowlist for OSM tiles | mitigate | CSP scoped to `https://*.tile.openstreetmap.org` only; `default-src 'self'` keeps everything else same-origin; `unsafe-inline` for script/style matches existing admin block (no regression introduced here) |
| T-05-13 | Information Disclosure | BUILD_HASH in public bundle | accept | Git short SHA is non-sensitive build provenance, intentionally surfaced; no secret material |
| T-05-14 | Tampering | over-broad CSP weakening | mitigate | Public CSP mirrors the already-reviewed admin CSP plus a single connect-src tile origin; no wildcard host or `unsafe-eval` added |
</threat_model>

<verification>
- `grep "Content-Security-Policy" nginx/nginx.conf | grep -o "tile.openstreetmap.org" | wc -l` returns at least 2 (one for img-src, one for connect-src on the public location / block CSP line)
- `grep -rn "0000000" .github frontend/Dockerfile` returns 0 lines
- Build-hash injection mechanism is implemented or documented per the checkpoint decision
</verification>

<success_criteria>
iOS Safari can fetch OSM tiles on the public citizen confirm map and /map (CSP allows the tile origin), and the production frontend build path injects a real git short SHA into NEXT_PUBLIC_BUILD_HASH.
</success_criteria>

<output>
Create `.planning/phases/05-uat-stabilisation/05-04-SUMMARY.md` when done. Record the checkpoint decision (which injection path was chosen) and whether the /uploads/ auth-guard check (D-03) found anything.
</output>
