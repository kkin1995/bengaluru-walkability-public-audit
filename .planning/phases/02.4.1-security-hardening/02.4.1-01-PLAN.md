---
phase: 02.4.1-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/handlers/reports.rs
  - backend/src/handlers/admin.rs
  - backend/src/models/admin.rs
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-07
  - D-09
tags:
  - security
  - rust
  - axum

must_haves:
  truths:
    - "POST /api/reports with a non-JPEG body returns HTTP 400 with the exact message 'Only JPEG images are accepted'"
    - "POST /api/reports with bytes starting 0xFF 0xD8 proceeds past magic-bytes validation (no 400 from this check)"
    - "admin_logout builds a removal cookie that carries SameSite=None and is Secure when COOKIE_SECURE is truthy, mirroring admin_login at backend/src/handlers/admin.rs:311-325"
    - "Bengaluru bounding-box constants exist exactly once at module level in backend/src/handlers/reports.rs (not duplicated in the test module or inside create_report)"
    - "No 'TODO: implement' comment lines remain in backend/src/models/admin.rs or backend/src/handlers/admin.rs"
    - "The cross-domain comment near the logout/login cookie in backend/src/handlers/admin.rs no longer references Railway; it names Cloudflare tunnel"
    - "cargo test passes; new tests cover JPEG accept/reject paths"
  artifacts:
    - path: "backend/src/handlers/reports.rs"
      provides: "is_jpeg helper, magic-bytes guard in create_report, module-level Bengaluru bbox consts, jpeg_validation_tests module"
      contains: "fn is_jpeg"
    - path: "backend/src/handlers/admin.rs"
      provides: "Hardened admin_logout with SameSite=None + conditional Secure; updated cross-domain comment"
      contains: "same_site(axum_extra::extract::cookie::SameSite::None)"
    - path: "backend/src/models/admin.rs"
      provides: "Doc comments with 'TODO: implement' lines removed above implemented functions"
  key_links:
    - from: "create_report handler in backend/src/handlers/reports.rs"
      to: "is_jpeg helper (or inline equivalent)"
      via: "guard sequence after image_bytes empty check, before SHA256 dedup block"
      pattern: "Only JPEG images are accepted"
    - from: "admin_logout in backend/src/handlers/admin.rs"
      to: "axum_extra::extract::cookie::SameSite::None and Cookie::set_secure"
      via: "Cookie::build() chain mirroring admin_login at lines 311-325"
      pattern: "same_site\\(.*SameSite::None\\)"
    - from: "mod tests / mod jpeg_validation_tests in backend/src/handlers/reports.rs"
      to: "module-level LAT_MIN/LAT_MAX/LNG_MIN/LNG_MAX consts"
      via: "use super::* in the test module"
      pattern: "use super::\\*"
---

<objective>
Implement the application-level security and code-quality fixes from Phase 02.4.1: reject non-JPEG uploads with HTTP 400, fix the admin logout cookie so it actually clears across the Vercel ↔ Cloudflare-tunnel boundary, deduplicate Bengaluru bounding-box constants, and remove stale `TODO: implement` doc-comment headers (plus the Railway reference) from already-implemented backend code.

Purpose: Close stored-XSS-via-SVG (D-05/D-06/D-07), close the silent-logout bug (D-09), and remove confusion-risk tech debt that would mislead Phase 3 executors reading these files (D-04).

Output: Hardened `reports.rs` + `admin.rs` + `models/admin.rs`, new unit tests for JPEG validation, all existing tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.4.1-security-hardening/02.4.1-CONTEXT.md
@.planning/phases/02.4.1-security-hardening/02.4.1-RESEARCH.md
@.planning/phases/02.4.1-security-hardening/02.4.1-PATTERNS.md
@backend/src/handlers/reports.rs
@backend/src/handlers/admin.rs
@backend/src/models/admin.rs
@backend/src/errors.rs
</context>

<interfaces>
<!-- Key contracts the executor must respect — extracted from codebase via RESEARCH/PATTERNS. -->

From `backend/src/errors.rs`:
- `AppError::BadRequest(String)` maps to HTTP 400 with the contained message as the response body. This is the correct return type for non-JPEG rejection (D-06).

From `backend/src/handlers/reports.rs` (existing pattern — `is_honeypot_triggered`, `build_rate_limit_key`, `extract_client_ip`):
- Pure top-level helpers above the handler body. No async, no I/O, easily unit-tested via `mod tests` with `use super::*;`.

From `backend/src/handlers/admin.rs` lines 311-325 (login cookie pattern — the template to mirror):
- Reads `COOKIE_SECURE` env via `std::env::var("COOKIE_SECURE").map(|v| v == "true" || v == "1").unwrap_or(false)`.
- Uses `axum_extra::extract::cookie::Cookie` with `set_same_site(SameSite::None)` and conditional `set_secure(true)`.

From `backend/src/handlers/reports.rs` lines 210-213 and 342-345 (existing duplicate bbox consts — the dedup target):
- Same four constants (`LAT_MIN`, `LAT_MAX`, `LNG_MIN`, `LNG_MAX`) defined in two places. Must collapse to a single module-level definition; the test module accesses them via `use super::*;` (already present) or `super::LAT_MIN`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JPEG magic-bytes guard + bounding-box constant deduplication</name>
  <files>backend/src/handlers/reports.rs</files>
  <read_first>
    - backend/src/handlers/reports.rs (entire file — to see current guard sequence, existing test modules, and exact line numbers for bbox consts at ~210-213 and ~342-345)
    - backend/src/errors.rs (to confirm AppError::BadRequest signature and HTTP 400 mapping)
    - .planning/phases/02.4.1-security-hardening/02.4.1-RESEARCH.md (Pattern 1, Pattern 5, Code Examples)
    - .planning/phases/02.4.1-security-hardening/02.4.1-PATTERNS.md (handlers/reports.rs section — exact insertion point and analog code)
  </read_first>
  <behavior>
    - Test: `is_jpeg(&[0xFF, 0xD8, 0xFF, 0xE0, 0x00])` returns true (happy path; real JPEG SOI + APP0 prefix).
    - Test: `is_jpeg(b"<?xml version=\"1.0\"")` returns false (SVG rejected).
    - Test: `is_jpeg(&[0x89, 0x50, 0x4E, 0x47])` returns false (PNG rejected).
    - Test: `is_jpeg(&[])` returns false (empty buffer rejected; no panic).
    - Test: `is_jpeg(&[0xFF])` returns false (single byte rejected; no panic).
    - Test: All existing `bengaluru_bounds` boundary tests (`exact_sw_corner_inclusive`, `exact_ne_corner_inclusive`, `out_of_bbox_*`, etc.) continue to pass against the lifted module-level constants.
  </behavior>
  <action>
    Per D-05, D-06, D-07 (JPEG validation) and D-04 (bbox deduplication):

    1. Write the new tests FIRST in a new `#[cfg(test)] mod jpeg_validation_tests { use super::*; ... }` block (mirroring the existing `mod rate_limit_honeypot_tests` style). The tests reference a top-level helper `is_jpeg(bytes: &[u8]) -> bool`. Confirm `cargo test jpeg` FAILS to compile (helper missing) — this is the RED step.
    2. Add the helper as a top-level pure fn above `create_report`, alongside `is_honeypot_triggered`. Signature: `fn is_jpeg(bytes: &[u8]) -> bool`. Returns `bytes.len() >= 2 && bytes[0] == 0xFF && bytes[1] == 0xD8`. Doc comment must explain it checks the JPEG SOI marker. Confirm `cargo test jpeg` GREEN.
    3. Insert the guard in `create_report` immediately AFTER the existing `if req.image_bytes.is_empty()` check and BEFORE the SHA256 dedup block. On reject return `Err(AppError::BadRequest("Only JPEG images are accepted".into()))`. The exact rejection string must match D-06 verbatim. Add a short security comment referencing strip_exif's silent-fallback behavior (per Pitfall 1 in RESEARCH.md).
    4. Lift the four Bengaluru bbox constants (`LAT_MIN`, `LAT_MAX`, `LNG_MIN`, `LNG_MAX` with values `12.7342`, `13.1739`, `77.3791`, `77.8731`) to module level, placed near the other top-level helpers/constants and ABOVE `create_report`. Remove the duplicate `const` items from inside `create_report` (~lines 210-213) and from the test module (~lines 342-345). Ensure the test module uses `use super::*;` so existing assertions referencing the names directly continue to compile (per Pitfall 6 in RESEARCH.md). Do NOT rename the constants.
    5. Run `cargo fmt` and `cargo clippy -- -D warnings` on the touched file. Resolve any warnings introduced by the changes only.
  </action>
  <verify>
    <automated>cd backend && cargo test jpeg_validation_tests && cargo test bengaluru_bounds && cargo test --lib && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "fn is_jpeg" backend/src/handlers/reports.rs` returns exactly one match at a top-level position (not inside a function).
    - Source: `grep -n "Only JPEG images are accepted" backend/src/handlers/reports.rs` returns at least one match inside `create_report`.
    - Source: `grep -c "const LAT_MIN" backend/src/handlers/reports.rs` returns `1` (exactly one definition after dedup).
    - Source: `grep -n "mod jpeg_validation_tests" backend/src/handlers/reports.rs` returns one match.
    - Test: `cd backend && cargo test jpeg_validation_tests` runs 5 tests, all pass.
    - Test: `cd backend && cargo test bengaluru_bounds` runs the existing boundary tests, all pass.
    - Test: `cd backend && cargo test --lib` exits 0 (no other test regressed).
    - Lint: `cd backend && cargo clippy --all-targets -- -D warnings` exits 0.
    - Behavior (handler order): the new guard sits BEFORE the SHA256 dedup block — verifiable by `grep -nA1 "Only JPEG images are accepted" backend/src/handlers/reports.rs` showing a line number less than the line of `Sha256::digest(&req.image_bytes)`.
  </acceptance_criteria>
  <done>
    JPEG SOI guard rejects non-JPEG uploads with HTTP 400 and the exact D-06 message; bbox constants exist exactly once at module level; new tests cover accept/reject paths; existing bbox tests still pass; clippy clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Logout cookie hardening + TODO/Railway-reference cleanup in admin.rs</name>
  <files>backend/src/handlers/admin.rs, backend/src/models/admin.rs</files>
  <read_first>
    - backend/src/handlers/admin.rs (entire file — current `admin_login` cookie construction at lines 311-325 to mirror, current `admin_logout` body at lines 342-352 to fix, stale Railway comment at line 318, TODO blocks at lines 115 and 145)
    - backend/src/models/admin.rs (entire file — TODO blocks at lines 88, 195, 257, 279, 306, 370, 409, 438, 456)
    - .planning/phases/02.4.1-security-hardening/02.4.1-RESEARCH.md (Pattern 2 — logout cookie fix; Pitfall 2)
    - .planning/phases/02.4.1-security-hardening/02.4.1-PATTERNS.md (handlers/admin.rs section — login template, fixed logout snippet, TODO removal pattern)
  </read_first>
  <action>
    Per D-09 (logout cookie) and D-04 (TODO + Railway comment cleanup):

    1. Fix `admin_logout` in `backend/src/handlers/admin.rs` (currently around lines 342-352). Add a `cookie_secure` read using the exact same `std::env::var("COOKIE_SECURE").map(|v| v == "true" || v == "1").unwrap_or(false)` snippet used in `admin_login`. Build the removal cookie via `Cookie::build(("admin_token", "")).path("/").http_only(true).same_site(axum_extra::extract::cookie::SameSite::None).build()`. After build, conditionally `removal.set_secure(true)` when `cookie_secure` is true. Pass the resulting cookie to `jar.remove(removal)`. Preserve the existing `tracing::info!("Admin logout")` log line and the `(StatusCode::OK, jar.remove(removal))` return shape.
    2. Update the cross-domain comment near the cookie code (currently at line 318 in `admin_login`, plus any duplicate in the logout block) so it names the Cloudflare tunnel rather than Railway. Replacement text: `SameSite=None required for cross-domain Vercel + Cloudflare tunnel production setup.` Do not change cookie behavior in `admin_login`; only the comment.
    3. Remove `TODO: implement` doc-comment lines in `backend/src/handlers/admin.rs` at lines 115 and 145. Strategy: delete only the doc-comment lines that contain `TODO: implement` (and any obviously paired `# TDD stub` doc-comment line directly above them, when present); keep any surrounding description/contract doc lines. Do not touch function bodies.
    4. Apply the same `TODO: implement` line removal in `backend/src/models/admin.rs` at lines 88, 195, 257, 279, 306, 370, 409, 438, 456. Same rule: remove only the TODO-bearing doc-comment lines (plus a paired `# TDD stub` doc-comment line directly above when present); do not touch function bodies or other doc content.
    5. Run `cargo fmt` and `cargo clippy --all-targets -- -D warnings`. Resolve any warnings introduced by the changes only.
  </action>
  <verify>
    <automated>cd backend && cargo test --lib && cargo clippy --all-targets -- -D warnings && ! grep -q 'TODO: implement' backend/src/handlers/admin.rs backend/src/models/admin.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "TODO: implement" backend/src/handlers/admin.rs` returns `0`.
    - Source: `grep -c "TODO: implement" backend/src/models/admin.rs` returns `0`.
    - Source: `grep -n "same_site(axum_extra::extract::cookie::SameSite::None)" backend/src/handlers/admin.rs` returns at least two matches (one in `admin_login`, one in `admin_logout`).
    - Source: `grep -nB1 -A6 "pub async fn admin_logout" backend/src/handlers/admin.rs` shows the function reads `COOKIE_SECURE` and conditionally calls `set_secure(true)`.
    - Source: `grep -c "Railway" backend/src/handlers/admin.rs` returns `0`.
    - Source: `grep -n "Cloudflare tunnel" backend/src/handlers/admin.rs` returns at least one match.
    - Test: `cd backend && cargo test --lib` exits 0 — no test regressed (D-04 cleanup must not break compilation or behavior).
    - Lint: `cd backend && cargo clippy --all-targets -- -D warnings` exits 0.
    - Behavior: `admin_logout` still returns `(StatusCode::OK, ...)` and still calls `jar.remove(removal)` (verifiable via grep on the function body).
  </acceptance_criteria>
  <done>
    Logout removal cookie mirrors login cookie attributes (SameSite=None, conditional Secure); zero `TODO: implement` doc-comment lines remain in `admin.rs` or `models/admin.rs`; stale Railway comment is replaced with a Cloudflare tunnel comment; full backend test suite green; clippy clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → `create_report` (POST /api/reports) | Untrusted multipart body crosses into the backend; bytes can be any format the client chooses |
| Browser (vercel domain) → `admin_logout` (cross-domain) | Cookie clearing must succeed across Vercel ↔ Cloudflare tunnel; mismatched cookie attributes leave a valid session cookie behind |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.4.1-01 | Tampering + Elevation of Privilege | `create_report` accepts arbitrary file bytes labelled as a JPEG | mitigate | JPEG SOI magic-bytes guard returns HTTP 400 before any processing; placement BEFORE `strip_exif` ensures non-JPEG cannot reach disk because `img-parts` silently falls back on parse errors (per RESEARCH Pitfall 1). Implemented in Task 1. |
| T-02.4.1-02 | Repudiation | `admin_logout` returns 200 but browser keeps `admin_token` because removal cookie's SameSite/Secure differs from login cookie | mitigate | Mirror login cookie attributes on removal cookie (SameSite=None, conditional Secure). Implemented in Task 2 per D-09. |
| T-02.4.1-03 | Information Disclosure (developer-confusion class) | `TODO: implement` headers above already-implemented functions mislead Phase 3 executors into rewriting working code | mitigate | Remove all `TODO: implement` doc-comment lines per D-04. Implemented in Task 2. |
| T-02.4.1-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — uses only `axum-extra`, `cookie`, `sha2`, all already in `Cargo.lock`. Package Legitimacy Gate not applicable per RESEARCH.md §Package Legitimacy Audit. |
</threat_model>

<verification>
End-of-plan checks:
- `cd backend && cargo test` exits 0 (full backend suite green).
- `cd backend && cargo clippy --all-targets -- -D warnings` exits 0.
- `grep -c "TODO: implement" backend/src/handlers/admin.rs backend/src/models/admin.rs` reports `0` for both files.
- `grep -c "const LAT_MIN" backend/src/handlers/reports.rs` returns `1`.
- `grep -c "fn is_jpeg" backend/src/handlers/reports.rs` returns `1`.
- `grep -c "Only JPEG images are accepted" backend/src/handlers/reports.rs` returns at least `1` (handler) plus possible test references.
</verification>

<success_criteria>
- JPEG magic-bytes guard rejects non-JPEG with HTTP 400 and the exact D-06 message.
- `is_jpeg` helper exists as a pure top-level fn and is exercised by 5 unit tests covering accept/reject/empty/single-byte cases.
- Bengaluru bounding-box constants are defined exactly once at module level; existing `bengaluru_bounds` tests still pass.
- `admin_logout` builds a removal cookie with `SameSite=None` and conditional `Secure=true`, mirroring `admin_login`.
- Zero `TODO: implement` doc-comment lines remain in `backend/src/handlers/admin.rs` or `backend/src/models/admin.rs`.
- Stale Railway comment near the cookie code is replaced with the Cloudflare-tunnel wording specified in D-04.
- Full `cargo test` suite green; `cargo clippy --all-targets -- -D warnings` exits 0.
</success_criteria>

<output>
Create `.planning/phases/02.4.1-security-hardening/02.4.1-01-SUMMARY.md` when done.
</output>
</content>
</invoke>