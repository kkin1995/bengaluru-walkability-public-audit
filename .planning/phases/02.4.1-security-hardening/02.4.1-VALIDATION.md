---
phase: 02.4.1
slug: security-hardening
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-20
---

# Phase 02.4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test harness (`cargo test`) |
| **Config file** | none (built into cargo) |
| **Quick run command** | `cd backend && cargo test` |
| **Full suite command** | `cd backend && cargo test && cd ../frontend && npm test` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && cargo test`
- **After every plan wave:** Run `cd backend && cargo test && cd ../frontend && npm test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| JPEG-01 | app-security | 1 | D-05/D-06 | SVG stored XSS | Non-JPEG returns HTTP 400 `Only JPEG images are accepted` | Unit | `cd backend && cargo test jpeg` | ✅ | ✅ green |
| JPEG-02 | app-security | 1 | D-05 | SVG stored XSS | Valid JPEG (FF D8) accepted without error | Unit | `cd backend && cargo test jpeg` | ✅ | ✅ green |
| LOGOUT-01 | app-security | 1 | D-09 | Session not cleared on logout | Manual: browser dev tools confirms `admin_token` cleared on cross-domain logout | Manual | Browser dev tools | N/A | 🔵 manual |
| NGINX-01 | app-security | 1 | D-08 | SVG MIME sniffing | `X-Content-Type-Options: nosniff` in response for `/uploads/` paths (nosniff replaces Content-Type override per d725e2a — RFC 7230 compliance) | Manual | `curl -I <url>/uploads/<file>` | N/A | 🔵 manual |
| BBOX-01 | tech-debt | 1 | D-04 | — | All existing bounding box tests still pass after const lift | Unit | `cd backend && cargo test bengaluru_bounds` | ✅ | ✅ green |
| TODO-01 | tech-debt | 1 | D-04 | — | `cargo test` still passes after TODO comment removal | Unit | `cd backend && cargo test` | ✅ | ✅ green |
| BACKUP-01 | ops-security | 2 | D-10–D-15 | Disk exhaustion | Backup script creates files > 10 KB; retention deletes files > 30 days | Manual | `ls -lh /data/backups/db && ls -lh /data/backups/uploads` | N/A | 🔵 manual |
| DOCS-01 | ops-security | 2 | D-16/D-17 | — | `DEPLOYMENT.md` contains `## 10. Secret Rotation` and `## 11. Backup and Restore` sections | Source | `grep -c "Secret Rotation\|Backup and Restore" DEPLOYMENT.md` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `backend/src/handlers/reports.rs` — add `mod jpeg_validation_tests` or extend `mod rate_limit_honeypot_tests` with JPEG magic bytes unit tests (stubs for JPEG-01 and JPEG-02)
  - **Resolution:** Plan 01 Task 1 is a `tdd="true"` task whose RED step (action step 1) creates `mod jpeg_validation_tests` with the failing tests BEFORE the helper exists. Wave 0 scaffolding is folded into the first executable task, so no separate Wave 0 plan is required.

*Existing infrastructure covers all other phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cross-domain logout clears `admin_token` cookie | D-09 | Cookie attribute correctness is a browser runtime behavior; not unit-testable without full HTTP harness | Log in via admin panel, check cookie in DevTools → Application → Cookies; click Logout; confirm `admin_token` is removed |
| nginx `/uploads/` returns `X-Content-Type-Options: nosniff` | D-08 | nginx config change; no Rust unit test possible. **Note:** original `add_header Content-Type "image/jpeg" always;` was replaced in commit d725e2a — adding a second Content-Type violates RFC 7230. nosniff achieves same anti-MIME-sniff protection. | `curl -sI <tunnel-url>/uploads/<any-existing-file>` — confirm `x-content-type-options: nosniff` header present |
| Weekly backup produces valid files > 10 KB | D-10–D-14 | systemd timer runs on host; not testable in CI | `sudo systemctl start walkability-backup.service && ls -lh /data/backups/db/` — confirm file created and size > 10 KB |
| 30-day retention deletes old files | D-15 | Requires time passage or manual date manipulation | `touch -d "35 days ago" /data/backups/db/test.sql.gz && ./backup/backup.sh && ls /data/backups/db/` — confirm old file deleted |
| UptimeRobot/Cloudflare monitor active on `/health` | D-18 | External service configuration | Log into UptimeRobot dashboard; confirm monitor shows green for `<tunnel-url>/health` |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
  - Plan 01 Task 1: `cd backend && cargo test jpeg_validation_tests && cargo test bengaluru_bounds && cargo test --lib && cargo clippy --all-targets -- -D warnings`
  - Plan 01 Task 2: `cd backend && cargo test --lib && cargo clippy --all-targets -- -D warnings && ! grep -q 'TODO: implement' backend/src/handlers/admin.rs backend/src/models/admin.rs`
  - Plan 02 Task 1: grep on both nginx configs + `docker run ... nginx -t -c /etc/nginx/nginx.conf` for each
  - Plan 02 Task 2: `<verify><human-check>` — checkpoint:human-verify per workflow rule
  - Plan 03 Task 1: `test -x backup/backup.sh && bash -n backup/backup.sh && grep ...` (covers all required directives across the four files)
  - Plan 03 Task 2: composite grep verifying §10 and §11 headings, subsections, and required commands in DEPLOYMENT.md
  - Plan 03 Task 3: `<verify><human-check>` — checkpoint:human-action per workflow rule (host-only steps)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
  - Wave 1 task order (Plan 01 → Plan 02 → Plan 03): auto, auto, auto, human-verify, auto, auto, human-action — never 3 consecutive checkpoints.
- [x] Wave 0 covers all MISSING references
  - No `MISSING` tags appear in any plan's `<automated>` block. The TDD RED step in Plan 01 Task 1 itself creates `mod jpeg_validation_tests`, so the listed Wave 0 requirement is satisfied by the first task's RED step.
- [x] No watch-mode flags
  - No `--watch`, `--watchAll`, or `cargo watch` invocations in any plan's verify block.
- [x] Feedback latency < 30s
  - Per the test-infrastructure table: full backend `cargo test` runtime is ~30 seconds. Per-task verify blocks scope tests (`cargo test jpeg`, `cargo test bengaluru_bounds`) for sub-30s feedback during the inner loop.
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved (revision iteration 1 — flipped from draft after checker confirmed automated verify coverage on every auto-task, no MISSING tags, no watch flags, sampling continuity green).

---

## Validation Audit 2026-06-01

| Metric | Count |
|--------|-------|
| Gaps found | 1 |
| Resolved | 1 |
| Escalated | 0 |

**Audit findings:**

- All 5 automated tasks (JPEG-01, JPEG-02, BBOX-01, TODO-01, DOCS-01) confirmed COVERED and green:
  - `cargo test jpeg` → 5/5 pass (`mod jpeg_validation_tests`)
  - `cargo test bengaluru_bounds` → 14/14 pass
  - `grep "Secret Rotation\|Backup and Restore" DEPLOYMENT.md` → 2 matches
  - `grep "TODO: implement" admin.rs models/admin.rs` → 0 matches

- **NGINX-01 stale instruction (resolved):** Commit `d725e2a` (`fix(02.4.1): replace duplicate Content-Type with X-Content-Type-Options nosniff`) replaced `add_header Content-Type "image/jpeg" always;` with `add_header X-Content-Type-Options "nosniff" always;` on both nginx configs. Adding a second `Content-Type` header violates RFC 7230. The MIME-sniffing protection (D-08) is preserved via `nosniff`. Per-task map and manual-only table updated to reflect current implementation.

- Per-task map statuses updated from `⬜ pending` → `✅ green` (automated) / `🔵 manual` (manual-only items).
