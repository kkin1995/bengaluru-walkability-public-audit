---
phase: 02.4.1
slug: security-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-20
---

# Phase 02.4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test harness (`cargo test`) |
| **Config file** | none (built into cargo) |
| **Quick run command** | `cd backend && cargo test` |
| **Full suite command** | `cd backend && cargo test && cd ../frontend && npm test` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && cargo test`
- **After every plan wave:** Run `cd backend && cargo test && cd ../frontend && npm test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| JPEG-01 | app-security | 1 | D-05/D-06 | SVG stored XSS | Non-JPEG returns HTTP 400 `Only JPEG images are accepted` | Unit | `cd backend && cargo test jpeg` | ❌ W0 | ⬜ pending |
| JPEG-02 | app-security | 1 | D-05 | SVG stored XSS | Valid JPEG (FF D8) accepted without error | Unit | `cd backend && cargo test jpeg` | ❌ W0 | ⬜ pending |
| LOGOUT-01 | app-security | 1 | D-09 | Session not cleared on logout | Manual: browser dev tools confirms `admin_token` cleared on cross-domain logout | Manual | Browser dev tools | N/A | ⬜ pending |
| NGINX-01 | app-security | 1 | D-08 | SVG MIME sniffing | `Content-Type: image/jpeg` in response for `/uploads/` paths | Manual | `curl -I <url>/uploads/<file>` | N/A | ⬜ pending |
| BBOX-01 | tech-debt | 1 | D-04 | — | All existing bounding box tests still pass after const lift | Unit | `cd backend && cargo test bengaluru_bounds` | ✅ | ⬜ pending |
| TODO-01 | tech-debt | 1 | D-04 | — | `cargo test` still passes after TODO comment removal | Unit | `cd backend && cargo test` | ✅ | ⬜ pending |
| BACKUP-01 | ops-security | 2 | D-10–D-15 | Disk exhaustion | Backup script creates files > 10 KB; retention deletes files > 30 days | Manual | `ls -lh /data/backups/db && ls -lh /data/backups/uploads` | N/A | ⬜ pending |
| DOCS-01 | ops-security | 2 | D-16/D-17 | — | `DEPLOYMENT.md` contains `## 10. Secret Rotation` and `## 11. Backup and Restore` sections | Source | `grep -c "Secret Rotation\|Backup and Restore" DEPLOYMENT.md` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/src/handlers/reports.rs` — add `mod jpeg_validation_tests` or extend `mod rate_limit_honeypot_tests` with JPEG magic bytes unit tests (stubs for JPEG-01 and JPEG-02)

*Existing infrastructure covers all other phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cross-domain logout clears `admin_token` cookie | D-09 | Cookie attribute correctness is a browser runtime behavior; not unit-testable without full HTTP harness | Log in via admin panel, check cookie in DevTools → Application → Cookies; click Logout; confirm `admin_token` is removed |
| nginx `/uploads/` returns `Content-Type: image/jpeg` | D-08 | nginx config change; no Rust unit test possible | `curl -I <tunnel-url>/uploads/<any-existing-file>` — confirm single `Content-Type: image/jpeg` header, no duplicate |
| Weekly backup produces valid files > 10 KB | D-10–D-14 | systemd timer runs on host; not testable in CI | `sudo systemctl start walkability-backup.service && ls -lh /data/backups/db/` — confirm file created and size > 10 KB |
| 30-day retention deletes old files | D-15 | Requires time passage or manual date manipulation | `touch -d "35 days ago" /data/backups/db/test.sql.gz && ./backup/backup.sh && ls /data/backups/db/` — confirm old file deleted |
| UptimeRobot/Cloudflare monitor active on `/health` | D-18 | External service configuration | Log into UptimeRobot dashboard; confirm monitor shows green for `<tunnel-url>/health` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
