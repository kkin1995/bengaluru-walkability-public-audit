---
phase: 02.4.1-security-hardening
plan: "02"
subsystem: infra
tags: [nginx, security, content-type, mime, stored-xss]

# Dependency graph
requires:
  - phase: 02.4.1-security-hardening
    provides: "Plan 01 magic-bytes JPEG guard on Axum backend — first layer preventing non-JPEG uploads"
provides:
  - "nginx Content-Type: image/jpeg override on /uploads/ location block (D-08) — second independent layer"
  - "Defense-in-depth: even if a non-JPEG bypasses Plan 01, nginx forces browser to treat asset as image/jpeg, not executable script"
affects:
  - 02.4.1-security-hardening
  - any phase that adds nginx /uploads/ config

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "nginx add_header with 'always' flag for security overrides — consistent with existing X-Frame-Options convention in codebase"

key-files:
  created: []
  modified:
    - nginx/nginx.conf
    - nginx/nginx.server.conf

key-decisions:
  - "No proxy_hide_header Content-Type needed — curl -sI on deployed tunnel confirmed exactly one Content-Type: image/jpeg header (cf-cache-status: MISS, fresh uncached response)"
  - "add_header appended after existing Cache-Control directive, not before, matching codebase convention order"

patterns-established:
  - "nginx add_header security directives inside /uploads/ block always carry 'always' flag"
  - "Deployment verification: curl -sI against cf-cache-status: MISS URL confirms single-header behaviour without Cloudflare cache interference"

requirements-completed:
  - D-08

# Metrics
duration: 15min
completed: "2026-05-20"
---

# Phase 02.4.1 Plan 02: nginx Content-Type Override Summary

**nginx `/uploads/` location block in both configs now forces `Content-Type: image/jpeg` via `add_header ... always;`, verified live against deployed tunnel returning a single canonical header with no `proxy_hide_header` needed**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-20T17:58:39Z
- **Completed:** 2026-05-20T18:13:00Z
- **Tasks:** 2 (1 auto + 1 checkpoint:human-verify)
- **Files modified:** 2

## Accomplishments

- Added `add_header Content-Type "image/jpeg" always;` inside the `/uploads/` location block of `nginx/nginx.conf` and `nginx/nginx.server.conf` — byte-identical line in both files
- Existing `add_header Cache-Control "public, no-transform";` directive preserved untouched in both files
- Deployed to production tunnel; `curl -sI https://api-walkability.nammadaari.com/uploads/f1130106-7b83-4c36-bee8-0e4300d496f3.jpg` (cf-cache-status: MISS) returned exactly one `content-type: image/jpeg` header — no duplicate, no `proxy_hide_header` required
- Requirement D-08 closed: second independent layer of stored-XSS defense active alongside Plan 01 magic-bytes guard

## Task Commits

Each task was committed atomically:

1. **Task 1: Add Content-Type override to /uploads/ block in both nginx configs** - `381e59f` (feat)
2. **Task 2: Checkpoint human-verify** - resolved `approved` (no code commit — human confirmation only)

**Plan metadata:** (this docs commit)

## Files Created/Modified

- `nginx/nginx.conf` - Added `add_header Content-Type "image/jpeg" always;` inside `location /uploads/` block (~line 134)
- `nginx/nginx.server.conf` - Identical directive added inside `location /uploads/` block (~line 122)

## Decisions Made

- No `proxy_hide_header Content-Type;` added — the `add_header ... always;` directive replaces rather than appends the upstream Content-Type from Axum's `ServeDir`. Confirmed by fresh `curl -sI` on a real uploaded file with Cloudflare cache bypassed (`cf-cache-status: MISS`).
- Used `always` flag consistent with the existing security header convention in the codebase (`add_header X-Frame-Options "DENY" always;` in `/admin` block).

## Deviations from Plan

None - plan executed exactly as written. The open question (Assumption A2: whether `proxy_hide_header` was needed) was resolved at the checkpoint: single header confirmed, no amendment required.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. nginx config deployed by pushing to the branch and restarting the nginx container on the server.

## Next Phase Readiness

- D-08 satisfied; both nginx configs are correct and deployed
- Plan 03 (CSP / security headers hardening) can proceed independently
- No blockers

---

## Self-Check: PASSED

- `grep -c 'add_header Content-Type "image/jpeg" always;' nginx/nginx.conf` → 1 (FOUND)
- `grep -c 'add_header Content-Type "image/jpeg" always;' nginx/nginx.server.conf` → 1 (FOUND)
- `awk '/location \/uploads\//,/^[[:space:]]*}/' nginx/nginx.conf | grep -c 'add_header Content-Type'` → 1 (FOUND)
- `grep -c 'add_header Cache-Control "public, no-transform"' nginx/nginx.conf` → 1 (FOUND)
- Commit `381e59f` → FOUND in git log
- Checkpoint Task 2 → resolved `approved` by human verification

---
*Phase: 02.4.1-security-hardening*
*Completed: 2026-05-20*
