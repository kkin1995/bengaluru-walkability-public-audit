---
phase: 06-production-launch-git-branching-workflow
plan: 05
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - frontend/app/page.tsx
  - frontend/app/coming-soon.module.css
autonomous: false
requirements:
  - LAUNCH-01
user_setup:
  - service: vercel
    why: "Branch-to-domain mappings must be verified in Vercel dashboard — no CLI alternative for branch domain routing"
    dashboard_config:
      - task: "Verify nammadaari.com is assigned to the 'main' branch"
        location: "Vercel → Project → Settings → Domains → nammadaari.com → confirm branch: main"
      - task: "Verify staging.nammadaari.com is assigned to the 'staging' branch"
        location: "Vercel → Project → Settings → Domains → staging.nammadaari.com → confirm branch: staging (add if missing)"
      - task: "Set Vercel env vars for main branch"
        location: "Vercel → Project → Settings → Environment Variables → Production (main branch): NEXT_PUBLIC_API_URL=https://api.nammadaari.com, INTERNAL_API_URL=https://api.nammadaari.com"
      - task: "Set Vercel env vars for staging branch"
        location: "Vercel → Project → Settings → Environment Variables → Preview (staging branch specifically): NEXT_PUBLIC_API_URL=https://staging-api.nammadaari.com, INTERNAL_API_URL=https://staging-api.nammadaari.com"

must_haves:
  truths:
    - "Visiting nammadaari.com shows the coming soon page with brand wordmark, tagline, description, and Instagram CTA"
    - "The coming soon page uses only CSS variable tokens from globals.css, no hardcoded color values"
    - "The Instagram CTA opens https://instagram.com/nammadaariblr in a new tab"
    - "staging.nammadaari.com still shows the full citizen app (page.tsx change is main-only)"
    - "The page renders as a Server Component with no 'use client' directive and no API calls"
    - "The page title is 'Namma Daari — Coming Soon'"
    - "Page-specific CSS is scoped in coming-soon.module.css (not in globals.css)"
    - "D-21: One Vercel project has two branch-to-domain mappings — nammadaari.com assigned to the main branch, staging.nammadaari.com assigned to the staging branch (verified in Vercel Project Settings → Domains)"
    - "D-22: Vercel env vars are set per branch — main branch: NEXT_PUBLIC_API_URL=https://api.nammadaari.com and INTERNAL_API_URL=https://api.nammadaari.com; staging branch: NEXT_PUBLIC_API_URL=https://staging-api.nammadaari.com and INTERNAL_API_URL=https://staging-api.nammadaari.com"
  artifacts:
    - path: "frontend/app/page.tsx"
      provides: "Coming soon page — Server Component with wordmark, tagline, description, Instagram CTA, footer"
      contains: "nammadaariblr"
    - path: "frontend/app/coming-soon.module.css"
      provides: "Scoped CSS for coming soon page — all layout, typography, and color classes"
      contains: "ig-btn"
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/app/globals.css"
      via: "Reuses global classes .press, .pulse, .kn, .mono without re-declaring them"
      pattern: "press|pulse"
    - from: "frontend/app/page.tsx (metadata export)"
      to: "frontend/app/layout.tsx"
      via: "Page-level metadata.title overrides root layout metadata for this route"
      pattern: "export const metadata"
---

<objective>
Replace frontend/app/page.tsx on the main branch with the coming soon page designed in
06-UI-SPEC.md. Create the scoped CSS module coming-soon.module.css.

Per D-23, D-24, D-25, D-26, LAUNCH-01 from CONTEXT.md. Full design spec in 06-UI-SPEC.md.

CRITICAL BRANCH DISCIPLINE (per D-23 and RESEARCH.md Pitfall 4):
This change targets the 'main' branch ONLY. The working branch for this plan must PR to 'main',
NOT to 'staging'. The correct sequence is:
1. Checkout a new branch from main: git checkout main && git checkout -b phase/06-coming-soon
2. Implement the coming soon page
3. Create PR from phase/06-coming-soon → main
4. Once merged to main, Vercel auto-deploys to nammadaari.com

If this change accidentally lands on staging, staging.nammadaari.com will show "Coming soon"
instead of the full citizen app — which breaks UAT and Phase 7 testing.

Purpose: nammadaari.com must show the coming soon page to establish the brand presence while
the app is finalized. The staging branch retains the full citizen app.

Output:
- frontend/app/page.tsx: replaced with coming soon page (Server Component, no API calls)
- frontend/app/coming-soon.module.css: scoped CSS module with all page-specific styles
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-production-launch-git-branching-workflow/06-CONTEXT.md
@.planning/phases/06-production-launch-git-branching-workflow/06-RESEARCH.md
@.planning/phases/06-production-launch-git-branching-workflow/06-UI-SPEC.md
@frontend/app/page.tsx
@frontend/app/globals.css
@frontend/app/layout.tsx
@frontend/app/components/ui/Bi.tsx
@frontend/app/components/ui/Btn.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch setup — confirm working on phase/06-coming-soon targeting main</name>
  <files>(git refs — no source files modified)</files>
  <read_first>
    - Run: git branch --show-current (must confirm current branch context)
    - Run: git log --oneline -3 (confirm you are at main HEAD before creating branch)
    - .planning/phases/06-production-launch-git-branching-workflow/06-RESEARCH.md (Pitfall 4 — coming soon page must not land on staging)
  </read_first>
  <action>
    CRITICAL: Ensure you are on the correct branch before any code changes.

    Required sequence:
    1. git checkout main (ensure starting from main, not from the infra working branch)
    2. git pull origin main (ensure main is up to date)
    3. git checkout -b phase/06-coming-soon
    4. Confirm: git branch --show-current prints "phase/06-coming-soon"

    This branch will PR to 'main' — NOT to 'staging'. This is an exception to the normal
    feature/fix → staging flow because the coming soon page must live on main only.

    Do NOT merge or cherry-pick from the infra working branch (Plans 02-04). This branch
    is a clean branch from main containing only the coming soon page change.
  </action>
  <verify>
    <automated>git branch --show-current | grep -c "phase/06-coming-soon"</automated>
  </verify>
  <acceptance_criteria>
    - git branch --show-current prints "phase/06-coming-soon"
    - git log --oneline -3 shows the same commits as main (no infra changes from Plans 02-04)
  </acceptance_criteria>
  <done>
    Working on phase/06-coming-soon branch, branched from main HEAD with no infra changes included.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create coming-soon.module.css with all scoped page styles</name>
  <files>frontend/app/coming-soon.module.css</files>
  <read_first>
    - frontend/app/globals.css (read in full — identify: CSS variable tokens (--color-*, --font-*, --radius-*), global classes .press, .pulse, .kn, .mono that must NOT be re-declared in the module; understand background, spacing baseline)
    - .planning/phases/06-production-launch-git-branching-workflow/06-UI-SPEC.md (read in full — this is the complete locked design contract; every CSS value must match it exactly)
    - frontend/app/layout.tsx (read — understand what styles root layout applies to body; coming soon page works within body without nav/sidebar)
  </read_first>
  <action>
    Create frontend/app/coming-soon.module.css. All class names from 06-UI-SPEC.md §Implementation
    Notes item 1 must appear as CSS module classes: .page, .topbar, .wordmark, .en, .kn,
    .status-chip, .dot, .hero, .eyebrow, .tagline, .tagline-kn, .translit, .desc, .cta-row,
    .ig-btn, .handle, .cta-note, .footer, .tags, .tag.

    DO NOT declare .press, .pulse, .kn (global), .mono (global) in this module — they already
    exist in globals.css. The .kn class in this module is for the wordmark Kannada span styling
    (display, font properties) — it shadows the global .kn class only within the module scope,
    which is intentional since CSS Modules scope by default.

    Key CSS values from UI-SPEC.md to implement precisely:

    LAYOUT:
    - .page: max-width 1080px, margin 0 auto, display flex, flex-direction column, min-height 100vh,
      padding 32px 28px 28px, box-sizing border-box
    - Background on page or body: var(--bg) with two radial-gradient overlays (fixed attachment)
      per UI-SPEC.md Layout section (circles at 18% 22% and 84% 78%)
    - .topbar: display flex, justify-content space-between, align-items center, gap 16px (md token)
    - .hero: flex 1, max-width 760px, padding 56px 0 (hero-pad token), display flex, flex-direction column

    WORDMARK:
    - .wordmark: display inline-flex, align-items baseline, gap 8px
    - .en (scoped): font-size 17px, font-weight 700, font-family var(--font-sans), color var(--ink), letter-spacing -0.01em
    - .kn (scoped — wordmark only): font-size 16px, font-weight 600, font-family var(--font-kn), color var(--accent-ink)

    STATUS CHIP:
    - .status-chip: display inline-flex, align-items center, gap 8px (sm token), font-size 10px,
      color var(--muted), background var(--surface), border 1px solid var(--border),
      border-radius 999px, padding 5px 10px, white-space nowrap
    - .dot: width 6px, height 6px, border-radius 50%, background var(--accent), flex-shrink 0

    EYEBROW:
    - .eyebrow: font-size 12px, font-weight 400, color var(--muted), letter-spacing 0.01em,
      text-transform none, margin-bottom 22px (locked design exception from UI-SPEC.md)
    - .eyebrow span (accent): color var(--accent-ink)

    TAGLINE:
    - h1.tagline: font-size clamp(38px, 6.4vw, 68px), font-weight 700, line-height 1.04,
      letter-spacing -0.03em, color var(--ink), margin 0
    - .tagline .soft: color var(--muted-2)
    - .tagline-kn: font-size clamp(18px, 2.4vw, 24px), font-weight 500, font-family var(--font-kn),
      color var(--accent-ink), margin 0, margin-top 22px (locked design exception)
    - .translit: font-family var(--font-mono), font-size 0.62em, letter-spacing 0.04em, color var(--muted)

    DESCRIPTION:
    - .desc: font-size clamp(15px, 1.9vw, 18px), font-weight 400, line-height 1.62,
      color var(--ink-2), margin 0, margin-top 34px (locked design exception)
    - .desc b: font-weight 600, color var(--ink)

    CTA ROW:
    - .cta-row: display flex, flex-direction column, gap 18px (locked design exception), margin-top 40px (cta-mt)
    - .ig-btn: display inline-flex, align-items center, gap 10px, background var(--ink), color var(--bg),
      border none, border-radius var(--r-full, 9999px), padding 14px 22px, font-size 15px,
      font-weight 600, font-family var(--font-sans), text-decoration none, cursor pointer,
      align-self flex-start
    - .handle: font-family var(--font-mono), font-size 14px, font-weight 600
    - .cta-note: font-size 13px, font-weight 400, color var(--muted), line-height 1.5, max-width 380px

    FOOTER:
    - footer.footer: display flex, justify-content space-between, align-items center, flex-wrap wrap,
      gap 12px, border-top 1px solid var(--border), padding-top 20px, margin-top auto
    - footer text: font-size 11px, color var(--muted-2)
    - .tags: display flex, gap 8px (sm token), flex-wrap wrap
    - .tag: font-size 11px, color var(--muted), background var(--surface), border 1px solid var(--border),
      border-radius 4px, padding 4px 9px (xs spacing)

    RESPONSIVE (per UI-SPEC.md Responsive Behaviour table):
    At max-width 560px:
    - .page: padding 22px 20px 24px
    - .topbar: align-items flex-start
    - .hero: padding 36px 0
    - h1.tagline: letter-spacing -0.02em
    - .desc: margin-top 28px
    - .cta-row: gap 16px, margin-top 32px
    - .ig-btn: width 100%, justify-content center
    - .cta-note: max-width none

    At max-width 380px:
    - .wordmark: flex-direction column, gap 2px
    - .wordmark .kn: font-size 14px

    ANIMATION: Do NOT redeclare the pulse animation (it is in globals.css). The .dot class uses
    the .pulse global class applied via className in JSX — the module only styles the dot's
    base appearance (size, color, border-radius).
  </action>
  <verify>
    <automated>cd frontend && npx --no-install tsc --noEmit 2>&1 | head -5 || true</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/coming-soon.module.css exists
    - Module contains class selectors for all 20 class names from UI-SPEC.md §Implementation Notes item 1
    - .page has max-width 1080px
    - .ig-btn has background var(--ink) and color var(--bg) (inverted button)
    - .ig-btn has display inline-flex (anchor element, not button)
    - No hardcoded hex color values in the module (all references use var(--...))
    - No re-declaration of .press, .pulse (those are in globals.css)
    - @media (max-width: 560px) block exists with all responsive overrides from UI-SPEC.md
    - @media (max-width: 380px) block exists with wordmark column layout
  </acceptance_criteria>
  <done>
    frontend/app/coming-soon.module.css exists with all classes from UI-SPEC.md, correct CSS
    variable tokens, and responsive breakpoints. No global class re-declarations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace frontend/app/page.tsx with coming soon page</name>
  <files>frontend/app/page.tsx</files>
  <read_first>
    - frontend/app/page.tsx (read current full content — this is being replaced entirely)
    - frontend/app/layout.tsx (confirm root metadata shape so page-level export overrides correctly)
    - frontend/app/globals.css (confirm .press and .pulse class names for use in JSX)
    - .planning/phases/06-production-launch-git-branching-workflow/06-UI-SPEC.md (read in full — Copywriting Contract (exact copy), Component Mapping, Implementation Notes, Interaction Contract, Accessibility table)
    - frontend/app/coming-soon.module.css (just created — import path and class names)
  </read_first>
  <action>
    Replace the entire content of frontend/app/page.tsx with the coming soon page.

    IMPORTS (only what is needed):
    - import type { Metadata } from "next" (for metadata export)
    - import styles from "./coming-soon.module.css" (CSS module)
    - No other imports — no Link, no Icon, no Bi, no Btn, no SectionLabel, no INTERNAL_API_URL
      (per UI-SPEC.md Component Mapping: Bi and Btn are NOT used; SectionLabel is NOT used)

    METADATA EXPORT (per UI-SPEC.md Copywriting Contract):
      export const metadata: Metadata = {
        title: "Namma Daari — Coming Soon",
      };

    COMPONENT (Server Component — NO "use client" directive):
    - Export default function ComingSoonPage() — async is NOT needed (no await calls)
    - Return a single outer div with className={styles.page}
    - Background radial gradients applied via inline style on the outer div or via CSS module

    TOPBAR section (className={styles.topbar}):
    - div.wordmark (className={styles.wordmark}):
      - span className={styles.en}: "Namma Daari"
      - span className={`${styles.kn} kn`}: "ನಮ್ಮ ದಾರಿ" (kn global class for font; styles.kn for color/size)
    - div.status-chip (className={styles.statusChip} — note CSS modules camelCase):
      - span className={`${styles.dot} pulse`} (pulse is the global class for animation)
      - text node: "Coming soon · Bengaluru"

    HERO section (className={styles.hero}):
    - p.eyebrow (className={styles.eyebrow}):
      "Citizen reporting for " + span(className=accented span inline): "walkable streets"
    - h1 className={styles.tagline}:
      "Snap a broken footpath. " + span className={styles.soft}: "Put it on Bengaluru's map."
    - p className={styles.taglineKn}:
      "ನಮ್ಮ ದಾರಿ" + " " + span className={`${styles.translit} mono`}: "namma daari · our path"
    - p className={styles.desc}:
      "Namma Daari turns a quick phone photo into a tracked civic report — cracked footpaths,
      blocked crossings, missing kerb ramps and dark stretches along the Namma Metro corridor
      and beyond. " + b: "Every report lands in front of BBMP / GBA" + " with a location, a
      ward and a status attached, so nothing quietly disappears. Walkable streets start with
      what we choose to notice."
    - div.cta-row (className={styles.ctaRow}):
      - a href="https://instagram.com/nammadaariblr" target="_blank" rel="noopener"
        className={`${styles.igBtn} press`} (press is the global class for scale-on-active):
        - SVG Instagram icon (20x20, viewBox="0 0 24 24", aria-hidden="true"):
          Use the standard Instagram rounded-square outline icon path. The exact SVG paths
          are documented in the design HTML file at
          .planning/phases/06-production-launch-git-branching-workflow/design/Namma Daari - Coming Soon.html
          Read that file for the exact SVG path data before writing the icon.
        - "Follow "
        - span className={styles.handle}: "@nammadaariblr"
      - p className={styles.ctaNote}:
        "We're not live yet. Follow along for the launch and the first wards we're mapping."

    FOOTER (footer element, className={styles.footer}):
    - span: "Built for Bengaluru · ಬೆಂಗಳೂರಿಗಾಗಿ"
    - div className={styles.tags}:
      Four spans each with className={styles.tag}: "Footpaths", "Crossings", "Lighting", "BBMP / GBA"

    IMPORTANT: Copy text must match 06-UI-SPEC.md §Copywriting Contract EXACTLY.
    Do not alter punctuation, capitalisation, or word order.

    CSS MODULE CLASS REFERENCES:
    All JSX className references to CSS module classes use styles.camelCaseVersion where
    CSS uses kebab-case (e.g., .ig-btn → styles.igBtn, .cta-row → styles.ctaRow,
    .cta-note → styles.ctaNote, .status-chip → styles.statusChip, .tagline-kn → styles.taglineKn).
    Update coming-soon.module.css class names to use camelCase if needed to match JSX usage.

    After writing, run lint check:
      cd frontend && npm run lint -- --max-warnings 0 2>&1 | tail -5

    Commit both Task 2 and Task 3:
      git add frontend/app/page.tsx frontend/app/coming-soon.module.css
      git commit -m "feat(06): add coming soon page on main branch (LAUNCH-01)"
  </action>
  <verify>
    <automated>cd frontend && npm run lint -- --max-warnings 0 2>&1 | tail -5 && npm test -- --passWithNoTests --watchAll=false 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/page.tsx does not contain "use client" directive
    - frontend/app/page.tsx does not contain import of INTERNAL_API_URL or fetch calls
    - frontend/app/page.tsx does not import Bi, Btn, or SectionLabel components
    - frontend/app/page.tsx exports: const metadata = { title: "Namma Daari — Coming Soon" }
    - frontend/app/page.tsx contains: href="https://instagram.com/nammadaariblr"
    - frontend/app/page.tsx contains: target="_blank" and rel="noopener" on the CTA anchor
    - frontend/app/page.tsx contains: aria-hidden="true" on the Instagram SVG icon
    - frontend/app/page.tsx imports styles from "./coming-soon.module.css"
    - npm run lint exits 0 (no ESLint errors)
    - npm test -- --passWithNoTests --watchAll=false exits 0
    - The page component function has no async keyword (no await — no API calls)
    - All copy matches 06-UI-SPEC.md §Copywriting Contract exactly (check wordmark, tagline, body text, CTA text, footer text)
  </acceptance_criteria>
  <done>
    frontend/app/page.tsx is a Server Component implementing the coming soon design per UI-SPEC.md.
    Lint and tests pass. Committed on phase/06-coming-soon branch.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Push branch, open PR to main, merge, and verify nammadaari.com</name>
  <files>(git push + GitHub PR merge + Vercel deploy — no local file changes)</files>
  <what-built>
    Tasks 1-3 created the coming soon page on the phase/06-coming-soon branch (branched from
    main). The page implements the full UI-SPEC.md design with all copy from the Copywriting
    Contract. Now you must: verify Vercel settings, push the branch, create a PR to main,
    approve it, and merge it.
  </what-built>
  <how-to-verify>
    **Step 1 — Verify Vercel domain and env var settings (before pushing)**
    Visit Vercel Project → Settings → Domains:
    - Confirm nammadaari.com is assigned to branch: main
    - Confirm staging.nammadaari.com is assigned to branch: staging
    If either is missing, add the domain assignment now (see user_setup at top of plan).

    Visit Vercel Project → Settings → Environment Variables:
    - Production (main branch): set NEXT_PUBLIC_API_URL=https://api.nammadaari.com and INTERNAL_API_URL=https://api.nammadaari.com
    - Preview → staging branch specifically: set NEXT_PUBLIC_API_URL=https://staging-api.nammadaari.com and INTERNAL_API_URL=https://staging-api.nammadaari.com

    **Step 2 — Push branch and open PR to main**
    From the project root:
      git push -u origin phase/06-coming-soon
      gh pr create --title "feat(06): coming soon page on main branch (LAUNCH-01)" --body "Replaces page.tsx on main with the coming soon page per 06-UI-SPEC.md design contract. Targets main — NOT staging." --base main

    **Step 3 — Approve and merge the PR**
    Since branch protection allows self-approval (D-06):
      gh pr review --approve
      gh pr merge --squash

    **Step 4 — Verify Vercel deployment**
    Wait for Vercel to deploy main branch (typically 1-3 minutes after merge).
    Visit https://nammadaari.com in a browser:
    Expected: Coming soon page shows Namma Daari wordmark, "Coming soon · Bengaluru" chip,
    "Snap a broken footpath." tagline, description paragraph, and Instagram CTA.

    **Step 5 — Verify staging still shows full app**
    Visit https://staging.nammadaari.com in a browser:
    Expected: Full citizen app home page (NOT the coming soon page).

    **Step 6 — Visual checklist**
    - [ ] Page title in browser tab: "Namma Daari — Coming Soon"
    - [ ] Wordmark shows: "Namma Daari" (EN) + "ನಮ್ಮ ದಾರಿ" (KN in green accent color)
    - [ ] Status chip: "Coming soon · Bengaluru" with pulsing green dot
    - [ ] Tagline: "Snap a broken footpath." on first line, "Put it on Bengaluru's map." in muted color
    - [ ] Description paragraph visible with bold "Every report lands in front of BBMP / GBA"
    - [ ] Instagram CTA button visible with Instagram icon and "@nammadaariblr" handle text
    - [ ] Clicking CTA opens https://instagram.com/nammadaariblr in a new tab
    - [ ] Footer shows: "Built for Bengaluru · ಬೆಂಗಳೂರಿಗಾಗಿ" and four category tags
    - [ ] On mobile (≤560px): CTA button spans full width
    - [ ] staging.nammadaari.com shows the citizen report form (not coming soon)
  </how-to-verify>
  <resume-signal>Type "coming soon live" after verifying nammadaari.com shows the coming soon page and staging.nammadaari.com shows the full app, or describe any issues.</resume-signal>
  <action>Human-only: verify Vercel settings, push branch, open PR to main, approve it (gh pr review --approve), merge it (gh pr merge --squash), and confirm Vercel deployment at nammadaari.com.</action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://nammadaari.com | grep -c "200"</automated>
  </verify>
  <done>nammadaari.com returns HTTP 200 and shows the coming soon page. staging.nammadaari.com shows the full citizen app. PR merged to main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → nammadaari.com (coming soon page) | No auth, no user input, no API calls — static Server Component |
| Instagram CTA anchor | Opens external URL in new tab; rel="noopener" prevents tab-napping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | Coming soon page accidentally merged to staging | mitigate | Pitfall 4 (RESEARCH.md): Task 1 enforces branch from main; checkpoint verifies staging still shows full app |
| T-06-05-02 | Information Disclosure | Instagram CTA tab-napping (window.opener access) | mitigate | rel="noopener" on CTA anchor prevents window.opener access from instagram.com |
| T-06-05-03 | Tampering | CSS variables not resolving (custom properties undefined) | mitigate | All var(--...) tokens verified in globals.css before use; acceptance criteria requires no hardcoded hex values |
| T-06-05-04 | Information Disclosure | Vercel env vars leaking production API URL to staging | mitigate | D-22: separate env var values per Vercel branch; staging branch env points to staging-api.nammadaari.com only |
| T-06-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages — coming soon page uses only existing project components and CSS |
</threat_model>

<verification>
After checkpoint confirms nammadaari.com is live:
1. curl -s -o /dev/null -w "%{http_code}" https://nammadaari.com returns 200
2. curl -s https://nammadaari.com | grep -c "nammadaariblr" returns 1 or more (Instagram handle in page)
3. curl -s https://staging.nammadaari.com | grep -c "nammadaariblr" returns 0 (staging shows full app)
4. cd frontend && npm run lint -- --max-warnings 0 exits 0
5. cd frontend && npm test -- --passWithNoTests --watchAll=false exits 0
</verification>

<success_criteria>
- nammadaari.com serves the coming soon page with all UI-SPEC.md elements
- staging.nammadaari.com continues to serve the full citizen app
- page.tsx is a Server Component with no API calls, no "use client", no Bi/Btn/SectionLabel imports
- coming-soon.module.css contains all 20 scoped class names from UI-SPEC.md
- Lint and tests pass
- Change is merged to main (not staging)
</success_criteria>

## Artifacts this phase produces

New files:
- `frontend/app/coming-soon.module.css` — scoped CSS module with all coming soon page styles

Modified files (main branch only):
- `frontend/app/page.tsx` — replaced with coming soon page Server Component

New Vercel configuration (manual):
- Domain: `nammadaari.com` → main branch
- Domain: `staging.nammadaari.com` → staging branch
- Env var (Production): `NEXT_PUBLIC_API_URL=https://api.nammadaari.com`
- Env var (Production): `INTERNAL_API_URL=https://api.nammadaari.com`
- Env var (Preview/staging): `NEXT_PUBLIC_API_URL=https://staging-api.nammadaari.com`
- Env var (Preview/staging): `INTERNAL_API_URL=https://staging-api.nammadaari.com`

<output>
Create `.planning/phases/06-production-launch-git-branching-workflow/06-05-SUMMARY.md` when done
</output>
