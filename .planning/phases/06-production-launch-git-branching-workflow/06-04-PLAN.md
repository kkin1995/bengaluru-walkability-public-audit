---
phase: 06-production-launch-git-branching-workflow
plan: 04
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - DEPLOYMENT.md
  - .planning/config.json
  - CLAUDE.md
autonomous: true
requirements:
  - LAUNCH-04
  - LAUNCH-05

must_haves:
  truths:
    - "DEPLOYMENT.md reflects the new branching model: feature/fix branches → staging → main at milestone"
    - "DEPLOYMENT.md references docker-compose.production-server.yml (not docker-compose.server.yml)"
    - "DEPLOYMENT.md documents the staging stack setup (port 3011, staging volumes, nginx.staging-server.conf)"
    - "DEPLOYMENT.md domain table shows api.nammadaari.com (not api-walkability.nammadaari.com)"
    - "DEPLOYMENT.md documents the Cloudflare Tunnel manual steps for staging-api.nammadaari.com"
    - ".planning/config.json has a 'branching' section matching the exact JSON from D-28"
    - "CLAUDE.md references docker-compose.production-server.yml wherever docker-compose.server.yml was mentioned"
  artifacts:
    - path: "DEPLOYMENT.md"
      provides: "Deployment runbook with updated branching model, domains, and staging stack"
      contains: "staging"
    - path: ".planning/config.json"
      provides: "GSD config with branching section"
      contains: "working_branch"
  key_links:
    - from: ".planning/config.json branching.working_branch"
      to: "git remote origin/staging"
      via: "GSD /gsd-ship reads branching.working_branch to determine PR target"
      pattern: "working_branch.*staging"
    - from: "DEPLOYMENT.md §Branching model"
      to: "GitHub branch protection rules (Plan 01)"
      via: "Documents the rule: feature/fix → staging (PR) → main (milestone merge only)"
      pattern: "staging.*main.*milestone"
---

<objective>
Update all documentation and configuration to reflect the new branching model and staging
infrastructure created in Plans 01-03.

Per D-20, D-28, D-29 (cleanup), D-04, D-05, D-06, D-07, LAUNCH-04, LAUNCH-05 from CONTEXT.md.

Purpose: Plans 02 and 03 create infrastructure files. This plan ensures the documentation and
GSD config accurately reflect what was built, so any contributor can follow the branching
workflow without asking.

Output:
- DEPLOYMENT.md: rewritten to document the new branching model, staging stack, updated domains,
  and Cloudflare Tunnel manual configuration steps
- .planning/config.json: branching section added per D-28 exact JSON
- CLAUDE.md: docker-compose.server.yml references updated to docker-compose.production-server.yml
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-production-launch-git-branching-workflow/06-CONTEXT.md
@.planning/phases/06-production-launch-git-branching-workflow/06-RESEARCH.md
@DEPLOYMENT.md
@.planning/config.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DEPLOYMENT.md for new branching model, staging stack, and domains</name>
  <files>DEPLOYMENT.md</files>
  <read_first>
    - DEPLOYMENT.md (read full content — 14+ references to docker-compose.server.yml must be updated; domain names must be updated)
    - .planning/phases/06-production-launch-git-branching-workflow/06-CONTEXT.md (D-16 domain rename, D-17 staging domain, D-18 CORS_ORIGIN values, D-20 DEPLOYMENT.md CORS_ORIGIN example, D-27 seed secret removal)
    - .planning/phases/06-production-launch-git-branching-workflow/06-RESEARCH.md (Runtime State Inventory, Architecture Diagram, system topology section)
    - docker-compose.staging-server.yml (just created — read to confirm port/volume values for documentation accuracy)
    - nginx/nginx.staging-server.conf (just created — confirm upstream port for documentation)
  </read_first>
  <action>
    Update DEPLOYMENT.md to reflect the Phase 6 infrastructure. Key changes:

    GLOBAL REPLACEMENTS (use Edit for each occurrence — do not write the whole file):
    - Replace every occurrence of "docker-compose.server.yml" with "docker-compose.production-server.yml"
      (confirm grep -c "docker-compose.server.yml" DEPLOYMENT.md to count before updating)
    - Replace "api-walkability.kinariwala.com" → "api.nammadaari.com" (per D-16)
    - Replace "staging-walkability.kinariwala.com" → "nammadaari.com" (production frontend)
    - Update the "What runs where" table: docker-compose.server.yml → docker-compose.production-server.yml

    ADD NEW SECTION — Branching model (insert after the Overview section, before Prerequisites):
    Title: "Branching Workflow"
    Content:
    - Three-tier model: feature/fix branches → staging → main
    - feature/fix branches: created from staging; name format: feat/*, fix/*, phase/*
    - PR to staging: all feature work merges to staging first via PR; requires CI pass + 1 approval
    - Merge to main: only via /gsd-complete-milestone at milestone completion; staging → main
    - Direct pushes to staging or main are blocked by GitHub branch protection rules
    - /gsd-ship creates PRs targeting staging (per GSD config.json branching.working_branch)

    ADD NEW SECTION — Staging stack (insert after the Branching Workflow section):
    Title: "Staging Stack Setup"
    Content:
    - Staging runs on same LXC (192.168.1.152) in /opt/nammadaari-staging/
    - docker compose -f docker-compose.yml -f docker-compose.staging-server.yml up -d db backend nginx
    - nginx publishes on host port 3011 (not port 80 — production owns port 80)
    - Cloudflare Tunnel second ingress: staging-api.nammadaari.com → http://localhost:3011
    - Staging volumes: postgres_staging_data (separate from postgres_data), uploads_staging (separate from uploads)
    - Backend PUBLIC_URL: https://staging-api.nammadaari.com
    - Backend CORS_ORIGIN (GitHub Environment staging secret): https://staging.nammadaari.com
    - Vercel branch domain: staging.nammadaari.com → staging branch

    ADD NEW SECTION — Cloudflare Tunnel manual configuration:
    Title: "Cloudflare Tunnel: Hostname Rename and Staging Ingress"
    Content (per D-16, D-17 — these are manual steps, not automated):
    - Step 1: Edit /etc/cloudflared/config.yml on the Arch Linux VM (or LXC)
    - Step 2: Rename existing ingress rule from api-walkability.nammadaari.com to api.nammadaari.com
    - Step 3: Add second ingress rule: staging-api.nammadaari.com → http://localhost:3011
    - Step 4: Update Cloudflare DNS: add CNAME api.nammadaari.com → tunnel UUID; add CNAME staging-api.nammadaari.com → tunnel UUID; optionally remove api-walkability CNAME
    - Step 5: sudo systemctl restart cloudflared
    - Verification: curl https://staging-api.nammadaari.com/health should return {"status":"ok"} after staging stack is running

    UPDATE §Environment Variables table (per D-20):
    - CORS_ORIGIN example: change from "https://staging-walkability.kinariwala.com" to "https://nammadaari.com"
    - Add row for PUBLIC_URL: value for production = https://api.nammadaari.com, for staging = https://staging-api.nammadaari.com

    UPDATE §Admin seed secret removal (per D-27):
    - Add reminder: After first successful admin login on the production stack, remove
      ADMIN_SEED_EMAIL and ADMIN_SEED_PASSWORD from the GitHub Environment 'production' secrets
      page. The seed function is idempotent but removing the secrets is good hygiene.

    UPDATE §Common operations quick-reference table:
    - Replace all docker-compose.server.yml references with docker-compose.production-server.yml
    - Add staging stack equivalents for: deploy manually, view logs, restart service, stop all, check health
      (using docker-compose.staging-server.yml and /opt/nammadaari-staging/)

    UPDATE last-updated timestamp at top of file: 2026-06-22

    Use multiple Edit calls (scoped replacements) to make these changes. Do NOT rewrite DEPLOYMENT.md
    wholesale with Write — that destroys sections outside the diff window.
  </action>
  <verify>
    <automated>grep -c "docker-compose.server.yml" DEPLOYMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "docker-compose.server.yml" DEPLOYMENT.md returns 0 (all replaced with production-server.yml)
    - grep -c "docker-compose.production-server.yml" DEPLOYMENT.md returns 14 or more (matching original count)
    - grep -c "api-walkability" DEPLOYMENT.md returns 0 (all replaced with api.nammadaari.com)
    - grep -c "api.nammadaari.com" DEPLOYMENT.md returns 1 or more
    - grep -c "staging" DEPLOYMENT.md returns 10 or more (new staging sections added)
    - DEPLOYMENT.md contains section heading "Branching Workflow" or "Branching model"
    - DEPLOYMENT.md contains section heading "Staging Stack" or "Staging stack"
    - DEPLOYMENT.md contains section heading "Cloudflare Tunnel" with staging ingress instructions
    - grep "staging-api.nammadaari.com" DEPLOYMENT.md returns at least one result
    - CORS_ORIGIN example shows https://nammadaari.com (not staging-walkability.kinariwala.com)
  </acceptance_criteria>
  <done>
    DEPLOYMENT.md has zero references to docker-compose.server.yml, documents the three-tier
    branching model, staging stack setup, and Cloudflare Tunnel manual steps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add branching section to .planning/config.json and update CLAUDE.md</name>
  <files>.planning/config.json, CLAUDE.md</files>
  <read_first>
    - .planning/config.json (read current content — must not alter existing keys: mode, granularity, parallelization, commit_docs, model_profile, workflow, graphify, ui)
    - .planning/phases/06-production-launch-git-branching-workflow/06-CONTEXT.md (D-28 — exact JSON for branching section)
    - CLAUDE.md (read in full — check for references to docker-compose.server.yml that must be updated to docker-compose.production-server.yml)
  </read_first>
  <action>
    UPDATE .planning/config.json (per D-28, LAUNCH-05):
    Add a "branching" key at the top level of the JSON object. The exact value from D-28:
      "branching": {
        "working_branch": "staging",
        "protected": ["main", "staging"],
        "merge_targets": {
          "feat": "staging",
          "fix": "staging",
          "phase": "staging"
        },
        "milestone_merge": {
          "from": "staging",
          "to": "main"
        }
      }
    All existing keys must remain unchanged. Use Edit (scoped replacement) to insert the
    branching key after the last existing key before the closing brace.
    After editing, verify the JSON is valid: python3 -c "import json; json.load(open('.planning/config.json'))"

    UPDATE CLAUDE.md:
    Run: grep -n "docker-compose.server.yml" CLAUDE.md
    If any references exist (current state: RESEARCH.md research shows 0 occurrences, but verify),
    replace them with docker-compose.production-server.yml using Edit (scoped replacement).
    If zero references, no change needed — note in commit message.

    Also update the API endpoint table in CLAUDE.md if it references the old staging domain
    (staging-walkability.kinariwala.com). Update to staging.nammadaari.com.

    Commit:
      git add .planning/config.json CLAUDE.md DEPLOYMENT.md
      git commit -m "docs(06): update DEPLOYMENT.md branching model, staging stack, domains; add GSD branching config (LAUNCH-04, LAUNCH-05)"
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('.planning/config.json') as f:
    d = json.load(f)
assert 'branching' in d, 'branching key missing'
b = d['branching']
assert b.get('working_branch') == 'staging', 'working_branch must be staging'
assert 'main' in b.get('protected', []), 'main must be in protected'
assert 'staging' in b.get('protected', []), 'staging must be in protected'
assert b.get('merge_targets', {}).get('feat') == 'staging', 'feat must target staging'
assert b.get('merge_targets', {}).get('fix') == 'staging', 'fix must target staging'
assert b.get('milestone_merge', {}).get('from') == 'staging', 'milestone_merge.from must be staging'
assert b.get('milestone_merge', {}).get('to') == 'main', 'milestone_merge.to must be main'
print('config.json branching section PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - python3 -c "import json; d=json.load(open('.planning/config.json')); assert 'branching' in d; print('OK')" exits 0
    - .planning/config.json branching.working_branch = "staging"
    - .planning/config.json branching.protected = ["main", "staging"]
    - .planning/config.json branching.merge_targets.feat = "staging"
    - .planning/config.json branching.merge_targets.fix = "staging"
    - .planning/config.json branching.milestone_merge = {"from": "staging", "to": "main"}
    - All pre-existing config.json keys (mode, granularity, parallelization, commit_docs, model_profile, workflow, graphify, ui) are unchanged
    - grep -c "docker-compose.server.yml" CLAUDE.md returns 0
    - JSON is valid: python3 -c "import json; json.load(open('.planning/config.json'))" exits 0
  </acceptance_criteria>
  <done>
    .planning/config.json has the branching section exactly matching D-28. CLAUDE.md has no
    references to the old docker-compose.server.yml filename.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → GSD tooling | GSD reads branching.working_branch to route /gsd-ship PRs; incorrect value would send PRs to wrong branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | config.json branching section malformed JSON | mitigate | Acceptance criteria requires python3 json.load validation exits 0 |
| T-06-04-02 | Information Disclosure | DEPLOYMENT.md still showing old domain api-walkability | mitigate | Acceptance criteria: grep -c "api-walkability" DEPLOYMENT.md returns 0 |
| T-06-04-03 | Tampering | Partial DEPLOYMENT.md update leaving inconsistent docker-compose references | mitigate | Acceptance criteria: grep -c "docker-compose.server.yml" DEPLOYMENT.md returns 0; using Edit (scoped) not Write |
| T-06-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — documentation and config changes only |
</threat_model>

<verification>
After both tasks commit:
1. `grep -c "docker-compose.server.yml" DEPLOYMENT.md` returns 0
2. `grep -c "docker-compose.production-server.yml" DEPLOYMENT.md` returns 14 or more
3. `python3 -c "import json; d=json.load(open('.planning/config.json')); print(d['branching']['working_branch'])"` prints "staging"
4. `grep -c "api-walkability" DEPLOYMENT.md` returns 0
5. `grep -c "staging" DEPLOYMENT.md` returns significant matches (new staging sections present)
6. `python3 -c "import json; json.load(open('.planning/config.json')); print('valid JSON')"` exits 0
</verification>

<success_criteria>
- DEPLOYMENT.md: zero docker-compose.server.yml references; new sections for branching model,
  staging stack, and Cloudflare Tunnel manual steps; domains updated to nammadaari.com topology
- .planning/config.json: branching section present with exact D-28 values
- CLAUDE.md: no stale docker-compose.server.yml references
- All changes committed on the working branch targeting staging
</success_criteria>

## Artifacts this phase produces

Modified files:
- `DEPLOYMENT.md` — updated: branching model, staging stack setup, Cloudflare Tunnel steps, all docker-compose.production-server.yml references, domain table (api.nammadaari.com, staging-api.nammadaari.com)
- `.planning/config.json` — added: branching section (working_branch, protected, merge_targets, milestone_merge)
- `CLAUDE.md` — updated: docker-compose.server.yml → docker-compose.production-server.yml (if any references existed)

New GSD config keys:
- `branching.working_branch` = "staging"
- `branching.protected` = ["main", "staging"]
- `branching.merge_targets.feat` = "staging"
- `branching.merge_targets.fix` = "staging"
- `branching.milestone_merge.from` = "staging"
- `branching.milestone_merge.to` = "main"

<output>
Create `.planning/phases/06-production-launch-git-branching-workflow/06-04-SUMMARY.md` when done
</output>
