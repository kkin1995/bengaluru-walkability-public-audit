---
phase: 03.4-admin-uat-gap-fixes-geographic-org-auto-assignment-and-compa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/admin/components/ReportsTable.tsx
  - frontend/app/admin/components/__tests__/ReportsTable.test.tsx
autonomous: true
requirements: [NF-03-A]

must_haves:
  truths:
    - "In the mobile compact-rows view, tapping anywhere on a report card body navigates to /admin/reports/:id"
    - "Tapping the Status button changes status without navigating to the detail page"
    - "Tapping the Delete button deletes without navigating to the detail page"
  artifacts:
    - path: "frontend/app/admin/components/ReportsTable.tsx"
      provides: "CompactRow with full-card navigation (onClick + sr-only anchor) and stopPropagation on Status/Delete buttons"
      contains: "window.location.assign"
    - path: "frontend/app/admin/components/__tests__/ReportsTable.test.tsx"
      provides: "Assertion that CompactRow renders a link to /admin/reports/:id"
      contains: "getByRole"
  key_links:
    - from: "frontend/app/admin/components/ReportsTable.tsx CompactRow outer div"
      to: "/admin/reports/:id"
      via: "onClick window.location.assign + sr-only anchor"
      pattern: "window\\.location\\.assign\\(`/admin/reports/"
    - from: "CompactRow Status/Delete Btn onClick"
      to: "card navigation suppression"
      via: "e.stopPropagation()"
      pattern: "stopPropagation"
---

<objective>
Implement NF-03-A: make the entire mobile compact-row report card tappable so that tapping anywhere outside the Status and Delete buttons navigates to /admin/reports/:id, matching the navigable-row pattern already used by DupeSubTable in the same file.

Purpose: Closes the confirmed Phase 3 UAT gap NF-03-A (minor). On mobile, only the WLK-XXXXX chip is currently tappable; admins expect the whole card to open the report. This brings the compact-rows view in line with the desktop table and the dupe sub-table.
Output: A CompactRow whose outer container navigates on click (with an sr-only anchor for accessibility), Status/Delete buttons that call stopPropagation so they never trigger card navigation, and a new test assertion verifying the navigation link.
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.4-admin-uat-gap-fixes-geographic-org-auto-assignment-and-compa/03.4-RESEARCH.md
@.planning/phases/03.4-admin-uat-gap-fixes-geographic-org-auto-assignment-and-compa/03.4-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CompactRow full-card navigation + button stopPropagation + test assertion</name>
  <files>frontend/app/admin/components/ReportsTable.tsx, frontend/app/admin/components/__tests__/ReportsTable.test.tsx</files>
  <read_first>
    - frontend/app/admin/components/ReportsTable.tsx (CompactRow function at line 425; its outer div around lines 431–439; the Status Btn around lines 497–503; the Delete Btn around lines 505–514; the DupeSubTable navigable row at lines 184–207 which is the exact established pattern — window.location.assign on the row container + an sr-only anchor inside; confirm the `report` prop shape exposes `report.id`)
    - frontend/app/admin/components/__tests__/ReportsTable.test.tsx (existing imports lines 33–36: React, render/screen/within from @testing-library/react, userEvent, ReportsTable; existing fixture objects e.g. REPORT_A; existing delete-button test structure for the rendering/prop pattern to mirror)
    - .planning/phases/03.4-admin-uat-gap-fixes-geographic-org-auto-assignment-and-compa/03.4-PATTERNS.md (CompactRow modified outer div, Status/Delete stopPropagation edits, and the new test `it` block — exact code provided)
  </read_first>
  <behavior>
    - Test "CompactRow renders an anchor linking to /admin/reports/:id covering the card body": rendering ReportsTable with a single report in compact-rows mode produces a link (role="link", accessible name matching /view report/i) whose href equals `/admin/reports/${REPORT_A.id}`. (RED before the sr-only anchor is added.)
    - Behavior preserved: the existing delete-button test (clicking Delete invokes onDelete) must still pass — stopPropagation must not break the Delete handler.
  </behavior>
  <action>
    Modify the CompactRow outer `<div>` (lines 431–439): add `cursor: "pointer"` to its inline style object and add `onClick={() => window.location.assign(`/admin/reports/${report.id}`)}`. Immediately inside the div, as the first child, add an sr-only anchor: `<a href={`/admin/reports/${report.id}`} className="sr-only">View report {report.id.slice(0, 5).toUpperCase()}</a>`. This is the exact DupeSubTable pattern (window.location.assign on the container + sr-only anchor) — do NOT use useRouter; window.location.assign is the established convention in this file and is recorded as a STATE.md decision.

    Modify the Status Btn onClick (lines 497–503) from `onClick={() => (onUpdateStatus ?? onStatusChange)(report.id)}` to `onClick={(e) => { e.stopPropagation(); (onUpdateStatus ?? onStatusChange)(report.id); }}`. Preserve the existing variant="ghost", size="xs", and aria-label.

    Modify the Delete Btn onClick (lines 505–514) from `onClick={() => onDelete(report.id)}` to `onClick={(e) => { e.stopPropagation(); onDelete(report.id); }}`. Preserve variant="danger-soft", size="xs", aria-label, and data-testid="delete-button". Keep the existing `role === 'admin'` guard if present.

    Use `e.stopPropagation()` (NOT `e.preventDefault()`) — stopPropagation prevents the click bubbling to the card's onClick while still letting the button's own handler run. Do not modify the Btn component itself; the stopPropagation lives at the call site only.

    In frontend/app/admin/components/__tests__/ReportsTable.test.tsx, add a new describe block "CompactRow: full-card navigation" with one `it` that renders `<ReportsTable reports={[REPORT_A]} role="admin" onStatusChange={jest.fn()} onDelete={jest.fn()} />` (match the prop names used by the existing tests — verify against the existing render calls), then `const link = screen.getByRole("link", { name: /view report/i }); expect(link).toHaveAttribute("href", `/admin/reports/${REPORT_A.id}`);`. If the component renders both card-stream and compact rows depending on a prop, pass whatever prop the existing tests use to force the compact-rows view (read the existing test setup to match it).

    Use the exact code from 03.4-PATTERNS.md sections "CompactRow modified outer div", the Status/Delete Btn AFTER snippets, and the new test `it` block.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --watchAll=false --testPathPattern=ReportsTable 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "window.location.assign(\`/admin/reports/" frontend/app/admin/components/ReportsTable.tsx` returns a match inside CompactRow (in addition to any pre-existing DupeSubTable match).
    - The CompactRow outer div has `cursor: "pointer"` in its style and an `onClick` calling `window.location.assign` with the report id.
    - An sr-only anchor `<a ... className="sr-only">View report ...</a>` with href `/admin/reports/${report.id}` is the first child of the CompactRow div.
    - `grep -c "stopPropagation" frontend/app/admin/components/ReportsTable.tsx` shows at least 2 matches (Status + Delete button handlers in CompactRow).
    - The new test asserts `getByRole("link", { name: /view report/i })` has href `/admin/reports/${REPORT_A.id}` and passes.
    - The pre-existing CompactRow delete-button test still passes (Delete still invokes onDelete; stopPropagation did not break it).
    - `npm test -- --watchAll=false --testPathPattern=ReportsTable` passes with zero failures.
  </acceptance_criteria>
  <done>The entire CompactRow card body navigates to /admin/reports/:id on tap; Status and Delete buttons stopPropagation so they act without navigating; the new navigation test and all existing ReportsTable tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Full frontend build + full test suite regression check</name>
  <files>frontend/app/admin/components/ReportsTable.tsx</files>
  <read_first>
    - frontend/app/admin/components/ReportsTable.tsx (the file modified in Task 1 — confirm no stray syntax from the edits)
  </read_first>
  <action>
    Run the full frontend production build and the complete Jest suite to confirm the CompactRow changes introduced no TypeScript errors and no regressions in any other admin component test. No code changes are expected in this task; if the build or a previously-green test fails because of the Task 1 edits, fix the offending edit in ReportsTable.tsx (e.g. an event-handler type signature `(e) =>` that TypeScript wants typed as `(e: React.MouseEvent) =>` — add the explicit type only if the build requires it). Do not change unrelated files.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --watchAll=false 2>&amp;1 | tail -15 &amp;&amp; npm run build 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --watchAll=false` reports all suites passing with zero failures (and the runner does NOT report "No tests found").
    - `npm run build` completes with zero TypeScript errors and no new ESLint warnings attributable to ReportsTable.tsx.
    - No files other than ReportsTable.tsx and its test file were modified (`git diff --name-only` shows only the two Plan 02 files plus planning docs).
  </acceptance_criteria>
  <done>`npm run build` passes with zero TypeScript errors and the full Jest suite is green — NF-03-A introduced no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin user → admin UI (browser) | Authenticated admin interacts with the compact-rows list; all logic is client-side React/DOM |
| CompactRow click → navigation | A click event determines whether the card navigates or a button action fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4-05 | Spoofing (tap-jacking / mis-tap) | CompactRow card vs Status/Delete buttons | mitigate | Status and Delete Btn onClick call e.stopPropagation() so the destructive/state-changing buttons are never accidentally triggered by, nor accidentally trigger, the card navigation. |
| T-03.4-06 | Information disclosure | navigation target /admin/reports/:id | accept | The route is already admin-authenticated server-side (existing detail page); making the card tappable does not change access control — it only changes the click target client-side. |
| T-03.4-07 | Tampering | report.id interpolated into URL | accept | report.id is a server-issued UUID rendered in the admin list; not user-editable in this view. No injection vector in a same-origin navigation. |
| T-03.4-SC | Tampering | npm installs | accept | Zero new packages this phase (RESEARCH: "installs zero new packages"; window.location.assign is a browser API, not an import); no package legitimacy gate required. |
</threat_model>

<verification>
- `cd frontend && npm test -- --watchAll=false --testPathPattern=ReportsTable` — CompactRow navigation test and all existing ReportsTable tests pass.
- `cd frontend && npm test -- --watchAll=false` — full suite green.
- `cd frontend && npm run build` — zero TypeScript errors, no new ESLint warnings.
- Manual (optional, on mobile viewport): in the admin reports compact-rows view, tapping the card body opens the detail page; tapping Status opens the status control without navigating; tapping Delete deletes without navigating.
</verification>

<success_criteria>
- NF-03-A success criterion #4: tapping anywhere on a compact-row card (outside Status and Delete) navigates to /admin/reports/:id.
- Status and Delete buttons remain independently functional and do not trigger card navigation.
- ROADMAP success criterion #5 (this plan's portion): `npm run build` and all existing frontend tests pass with no regressions.
</success_criteria>

<output>
Create `.planning/phases/03.4-admin-uat-gap-fixes-geographic-org-auto-assignment-and-compa/03.4-02-SUMMARY.md` when done.
</output>
