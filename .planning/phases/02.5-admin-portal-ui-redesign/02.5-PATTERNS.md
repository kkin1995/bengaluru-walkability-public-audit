# Phase 02.5: Admin Portal UI Redesign — Pattern Map

**Mapped:** 2026-05-22
**Files analyzed:** 28 (14 new + 14 modified)
**Analogs found:** 28 / 28

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `frontend/app/admin/admin.css` | config/token | transform | `frontend/app/globals.css` | role-match |
| `frontend/app/admin/components/Icon.tsx` | component | request-response | `frontend/app/components/ui/Icon.tsx` | exact |
| `frontend/app/admin/components/Btn.tsx` | component | request-response | `frontend/app/components/ui/Btn.tsx` | exact |
| `frontend/app/admin/components/SeverityIndicator.tsx` | component | request-response | `frontend/app/components/redesign/SeverityGrid.tsx` | role-match |
| `frontend/app/admin/components/Pill.tsx` | component | request-response | `frontend/app/components/ui/Pill.tsx` | exact |
| `frontend/app/admin/components/Card.tsx` | component | request-response | `frontend/app/components/ui/Btn.tsx` | role-match |
| `frontend/app/admin/lib/useOnlineStatus.ts` | hook | event-driven | (no existing hook — see No Analog section) | none |
| `frontend/app/admin/components/__tests__/Icon.test.tsx` | test | — | `frontend/app/admin/components/__tests__/StatusBadge.test.tsx` | role-match |
| `frontend/app/admin/components/__tests__/Btn.test.tsx` | test | — | `frontend/app/admin/components/__tests__/StatsCards.test.tsx` | role-match |
| `frontend/app/admin/components/__tests__/SeverityIndicator.test.tsx` | test | — | `frontend/app/admin/components/__tests__/StatusBadge.test.tsx` | role-match |
| `frontend/app/admin/components/__tests__/Pill.test.tsx` | test | — | `frontend/app/admin/components/__tests__/StatusBadge.test.tsx` | role-match |
| `frontend/app/admin/components/__tests__/Card.test.tsx` | test | — | `frontend/app/admin/components/__tests__/StatsCards.test.tsx` | role-match |
| `frontend/app/admin/lib/__tests__/useOnlineStatus.test.ts` | test | — | `frontend/app/lib/__tests__/constants.test.ts` | partial |
| `frontend/app/layout.tsx` | config | request-response | self | exact |
| `frontend/app/admin/layout.tsx` | layout/middleware | request-response | self | exact |
| `frontend/app/admin/components/AdminSidebar.tsx` | component | event-driven | self (current Tailwind version) | exact |
| `frontend/app/admin/components/StatusBadge.tsx` | component | request-response | `frontend/app/components/ui/Pill.tsx` | role-match |
| `frontend/app/admin/components/ReportsTable.tsx` | component | CRUD | self (current Tailwind version) | exact |
| `frontend/app/admin/components/StatsCards.tsx` | component | CRUD | self (current Tailwind version) | exact |
| `frontend/app/admin/components/UserManagementTable.tsx` | component | CRUD | self (current Tailwind version) | exact |
| `frontend/app/admin/components/CreateUserModal.tsx` | component | request-response | self (current Tailwind version) | exact |
| `frontend/app/admin/page.tsx` | page | CRUD | self (current Tailwind version) | exact |
| `frontend/app/admin/reports/page.tsx` | page | CRUD | `frontend/app/admin/page.tsx` | role-match |
| `frontend/app/admin/reports/[id]/page.tsx` | page | CRUD | `frontend/app/admin/page.tsx` | role-match |
| `frontend/app/admin/reports/map/page.tsx` | page | CRUD | `frontend/app/admin/page.tsx` | role-match |
| `frontend/app/admin/users/page.tsx` | page | CRUD | `frontend/app/admin/page.tsx` | role-match |
| `frontend/app/admin/profile/page.tsx` | page | request-response | `frontend/app/admin/page.tsx` | role-match |

---

## Pattern Assignments

### `frontend/app/admin/admin.css` (config/token, transform)

**Analog:** `frontend/app/globals.css`

**How globals.css scopes citizen tokens (lines 16-56) — admin.css must NOT touch this block:**
```css
:root {
  --bg: #fafaf9;
  --surface: #ffffff;
  /* ... all citizen Direction A values ... */
  --r-sm: 8px; --r-md: 12px; --r-lg: 16px; --r-xl: 20px; --r-full: 999px;
  --shadow-sm: 0 1px 2px rgba(28, 25, 23, 0.04), 0 1px 3px rgba(28, 25, 23, 0.06);
}
```

**admin.css must use this scope structure (from D-02, transcribing `design-ref/admin-portal/tokens.css` lines 117-235):**
```css
/* File: frontend/app/admin/admin.css */
/* Import is handled by layout.tsx — do NOT add @import here */

.admin-portal {
  /* Neutrals — cool, slightly olive-tinted stone */
  --bg: #f3f3f1;
  --surface: #ffffff;
  --surface-2: #eaeae6;
  --surface-3: #d8d8d4;
  --border: #d4d4d1;
  --border-strong: #a8a8a3;
  --ink: #0a0a0a;
  --ink-2: #3a3a37;
  --muted: #6b6b66;
  --muted-2: #9a9a94;

  /* Accent — teal-cyan */
  --accent: oklch(0.56 0.13 200);
  --accent-ink: oklch(0.38 0.13 200);
  --accent-bg: oklch(0.95 0.03 200);
  --accent-border: oklch(0.86 0.06 200);
  --on-accent: #ffffff;

  /* Semantic */
  --danger: oklch(0.56 0.20 25);
  --danger-ink: oklch(0.40 0.18 25);
  --danger-bg: oklch(0.95 0.04 25);
  --danger-border: oklch(0.84 0.10 25);
  --warn: oklch(0.70 0.15 70);
  --warn-ink: oklch(0.44 0.15 70);
  --warn-bg: oklch(0.96 0.04 70);
  --warn-border: oklch(0.84 0.12 70);
  --info: oklch(0.56 0.13 200);
  --info-ink: oklch(0.38 0.13 200);
  --info-bg: oklch(0.95 0.03 200);
  --info-border: oklch(0.86 0.06 200);

  /* Status tokens */
  --status-submitted: oklch(0.56 0.13 200);
  --status-submitted-bg: oklch(0.95 0.03 200);
  --status-review: oklch(0.66 0.16 60);
  --status-review-bg: oklch(0.96 0.04 60);
  --status-resolved: oklch(0.58 0.14 145);
  --status-resolved-bg: oklch(0.95 0.03 145);

  /* Severity */
  --sev-low: oklch(0.66 0.06 200);
  --sev-medium: oklch(0.66 0.16 60);
  --sev-high: oklch(0.56 0.20 25);

  /* Type — font vars from layout.tsx, same names, still usable */
  --font-display: 'JetBrains Mono', ui-monospace, monospace;

  /* Radii — sharper than citizen Direction A */
  --r-xs: 4px;
  --r-sm: 6px;
  --r-md: 8px;
  --r-lg: 10px;
  --r-xl: 14px;
  --r-full: 999px;

  /* Shadows — flat, border-reliant */
  --shadow-sm: 0 0 0 1px rgba(10,10,10,0.04);
  --shadow-md: 0 1px 2px rgba(10,10,10,0.04), 0 0 0 1px rgba(10,10,10,0.04);
  --shadow-lg: 0 8px 24px rgba(10,10,10,0.08), 0 0 0 1px rgba(10,10,10,0.04);

  /* Hero text — not in globals.css, admin-only */
  --text-hero-mobile: 56px;
  --text-hero-desktop: 84px;
}

.dark .admin-portal {
  --bg: #0a0a0a;
  --surface: #131313;
  --surface-2: #1d1d1c;
  --surface-3: #2a2a28;
  --border: #262624;
  --border-strong: #3a3a36;
  --ink: #fafaf9;
  --ink-2: #d4d4d1;
  --muted: #8a8a85;
  --muted-2: #595955;
  --accent: oklch(0.72 0.13 200);
  --accent-ink: oklch(0.84 0.10 200);
  --accent-bg: oklch(0.26 0.06 200);
  --accent-border: oklch(0.36 0.08 200);
  --on-accent: #0a0a0a;
  --status-submitted: oklch(0.72 0.13 200);
  --status-submitted-bg: oklch(0.26 0.05 200);
  --status-review: oklch(0.76 0.15 60);
  --status-review-bg: oklch(0.26 0.05 60);
  --status-resolved: oklch(0.70 0.14 145);
  --status-resolved-bg: oklch(0.26 0.05 145);
}

/* Universal resets for .admin-portal scope */
.admin-portal {
  font-family: var(--font-sans);
  color: var(--ink);
  background: var(--bg);
  font-feature-settings: "cv02", "cv03", "cv04", "cv11", "ss01";
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}
.admin-portal * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
.admin-portal button { font-family: inherit; cursor: pointer; }

/* Utility classes — used by primitives */
.press { transition: transform 80ms ease, opacity 80ms ease; }
.press:active { transform: scale(0.97); opacity: 0.85; }
.no-scrollbar { scrollbar-width: none; }
.no-scrollbar::-webkit-scrollbar { display: none; }
```

---

### `frontend/app/admin/components/Icon.tsx` (component, request-response)

**Analog:** `frontend/app/components/ui/Icon.tsx` (exact match — same role, same pattern)

**Imports pattern (lines 1-2):**
```typescript
import type { CSSProperties, ReactElement } from "react";
// Note: no "use client" needed — Icon is pure rendering, no state/effects
```

**IconName union type pattern (lines 3-11):**
```typescript
export type IconName =
  | "menu" | "close" | "search" | "filter" | "chevron_right" | "chevron_down"
  | "chevron_left" | "arrow_right" | "arrow_left" | "arrow_up_right" | "plus"
  | "check" | "check_circle" | "dots" | "bell" | "pin" | "map" | "image"
  | "camera" | "user" | "users" | "shield" | "grid" | "list" | "table"
  | "clock" | "eye" | "download" | "upload" | "trash" | "edit" | "logout"
  | "lock" | "mail" | "org" | "sun" | "moon" | "activity" | "alert"
  | "warn_tri" | "info" | "refresh" | "inbox" | "flag" | "zoom_in"
  | "phone" | "external" | "duplicate" | "settings" | "sort"
  | "cat_no_path" | "cat_broken" | "cat_blocked" | "cat_crossing"
  | "cat_lighting" | "cat_other";
// Add remaining admin-specific names from primitives.jsx
```

**SVG path constant pattern (lines 20-27 of analog — copy this exact structure):**
```typescript
const P = {
  fill: "none" as const,
  stroke: "currentColor",
  strokeWidth: 1.75,
  strokeLinecap: "round" as const,
  strokeLinejoin: "round" as const,
};

const ICONS: Record<IconName, ReactElement> = {
  menu: (<g {...P}><path d="M3 6h18M3 12h18M3 18h18"/></g>),
  close: (<g {...P}><path d="M6 6l12 12M18 6L6 18"/></g>),
  // ... all paths from design-ref/admin-portal/primitives.jsx lines 8-66
};
```

**Component function pattern (lines 63-73 of analog):**
```typescript
export function Icon({ name, size = 24, className, style, "aria-label": ariaLabel }: IconProps) {
  const sizeStyle: CSSProperties = { width: size, height: size, display: "block", flexShrink: 0, ...style };
  const ariaProps = ariaLabel
    ? { role: "img" as const, "aria-label": ariaLabel }
    : { "aria-hidden": true as const };
  return (
    <svg viewBox="0 0 24 24" className={className} style={sizeStyle} {...ariaProps}>
      {ICONS[name] ?? ICONS.dots}
    </svg>
  );
}
```

**Key difference from citizen Icon:** Admin Icon has more icon names (50+ vs ~30), default size is 24 (same), fallback is `ICONS.dots` (from primitives.jsx line 69). Must be a named export (D-08).

---

### `frontend/app/admin/components/Btn.tsx` (component, request-response)

**Analog:** `frontend/app/components/ui/Btn.tsx` (exact match)

**Full pattern to extend (citizen Btn, lines 1-60):**
```typescript
import type { ButtonHTMLAttributes, CSSProperties } from "react";

export type BtnVariant = "primary" | "accent" | "secondary" | "ghost" | "soft" | "danger" | "danger-soft";
export type BtnSize = "xs" | "sm" | "md" | "lg";

export interface BtnProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: BtnVariant;
  size?: BtnSize;
  icon?: IconName;       // NEW: optional leading icon
  iconRight?: IconName;  // NEW: optional trailing icon
  children?: React.ReactNode;
}
```

**Admin sizes from primitives.jsx lines 76-81 (Direction B is more compact than citizen):**
```typescript
const SIZES: Record<BtnSize, CSSProperties> = {
  xs: { padding: "6px 10px",  fontSize: 12, borderRadius: "var(--r-sm)", minHeight: 28, gap: 6 },
  sm: { padding: "8px 12px",  fontSize: 13, borderRadius: "var(--r-sm)", minHeight: 34, gap: 8 },
  md: { padding: "10px 14px", fontSize: 14, borderRadius: "var(--r-md)", minHeight: 40, gap: 8 },
  lg: { padding: "14px 18px", fontSize: 15, borderRadius: "var(--r-md)", minHeight: 48, gap: 10 },
};
```

**Admin variants from primitives.jsx lines 82-90 (7 variants — extends citizen's 4):**
```typescript
const VARIANTS: Record<BtnVariant, CSSProperties> = {
  primary:      { background: "var(--ink)",        color: "var(--bg)",           fontWeight: 600 },
  accent:       { background: "var(--accent)",     color: "var(--on-accent)",    fontWeight: 600 },
  secondary:    { background: "var(--surface)",    color: "var(--ink)",          fontWeight: 500, boxShadow: "inset 0 0 0 1px var(--border-strong)" },
  ghost:        { background: "transparent",       color: "var(--ink-2)",        fontWeight: 500 },
  soft:         { background: "var(--surface-2)",  color: "var(--ink)",          fontWeight: 500 },
  danger:       { background: "var(--danger)",     color: "#fff",                fontWeight: 600 },
  "danger-soft":{ background: "var(--danger-bg)",  color: "var(--danger-ink)",   fontWeight: 600, boxShadow: "inset 0 0 0 1px var(--danger-border)" },
};
```

**Button render pattern (citizen Btn lines 36-59 — copy this structure exactly):**
```typescript
export function Btn({ variant = "primary", size = "md", icon, iconRight, disabled, style, children, ...rest }: BtnProps) {
  const merged: CSSProperties = {
    ...SIZES[size],
    ...VARIANTS[variant],
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    border: VARIANTS[variant].boxShadow ? "none" : "none",
    cursor: disabled ? "not-allowed" : "pointer",
    opacity: disabled ? 0.4 : 1,
    pointerEvents: disabled ? "none" : "auto",
    ...style,
  };
  return (
    <button
      className="press"
      data-variant={variant}
      data-size={size}
      data-component="btn"
      style={merged}
      disabled={disabled}
      {...rest}
    >
      {icon && <Icon name={icon} size={size === "xs" ? 13 : size === "sm" ? 14 : 16} aria-hidden />}
      {children}
      {iconRight && <Icon name={iconRight} size={size === "xs" ? 13 : size === "sm" ? 14 : 16} aria-hidden />}
    </button>
  );
}
```

---

### `frontend/app/admin/components/Pill.tsx` (component, request-response)

**Analog:** `frontend/app/components/ui/Pill.tsx` (exact match)

**Extended tone set from primitives.jsx lines 107-114 (admin has 7 tones + size prop):**
```typescript
export type PillTone = "neutral" | "outline" | "accent" | "danger" | "warn" | "info" | "ink";
export type PillSize = "sm" | "md" | "lg";

const TONES: Record<PillTone, CSSProperties> = {
  neutral:  { background: "var(--surface-2)",    color: "var(--ink-2)",     border: "1px solid var(--border)" },
  outline:  { background: "transparent",         color: "var(--ink-2)",     border: "1px solid var(--border-strong)" },
  accent:   { background: "var(--accent-bg)",    color: "var(--accent-ink)",border: "1px solid var(--accent-border)" },
  danger:   { background: "var(--danger-bg)",    color: "var(--danger-ink)",border: "1px solid var(--danger-border)" },
  warn:     { background: "var(--warn-bg)",      color: "var(--warn-ink)",  border: "1px solid var(--warn-border)" },
  info:     { background: "var(--info-bg)",      color: "var(--info-ink)",  border: "1px solid var(--info-border)" },
  ink:      { background: "var(--ink)",          color: "var(--bg)",        border: "1px solid var(--ink)" },
};

const SIZES: Record<PillSize, CSSProperties> = {
  sm: { padding: "2px 8px",  fontSize: 11, gap: 5, borderRadius: "var(--r-full)" },
  md: { padding: "4px 10px", fontSize: 12, gap: 6, borderRadius: "var(--r-full)" },
  lg: { padding: "6px 12px", fontSize: 13, gap: 7, borderRadius: "var(--r-full)" },
};
```

**Render pattern (copy from citizen Pill lines 20-39, extend with size + dot + icon props):**
```typescript
export function Pill({ tone = "neutral", size = "md", dot, icon, children, style }: PillProps) {
  return (
    <span
      data-tone={tone}
      data-size={size}
      data-component="pill"
      style={{
        display: "inline-flex",
        alignItems: "center",
        fontWeight: 500,
        lineHeight: 1,
        whiteSpace: "nowrap",
        ...SIZES[size],
        ...TONES[tone],
        ...style,
      }}
    >
      {dot && <span style={{ width: 6, height: 6, borderRadius: 999, background: dot, flexShrink: 0 }} aria-hidden="true" />}
      {icon && <Icon name={icon} size={12} aria-hidden />}
      {children}
    </span>
  );
}
```

---

### `frontend/app/admin/components/Card.tsx` (component, request-response)

**Analog:** `frontend/app/components/ui/Btn.tsx` (role-match — same inline-style pattern)

**Pattern from primitives.jsx lines 169-184:**
```typescript
"use client";
import type { CSSProperties, HTMLAttributes, ReactNode } from "react";

export interface CardProps extends HTMLAttributes<HTMLDivElement> {
  padded?: boolean;
  hoverable?: boolean;
  children: ReactNode;
  style?: CSSProperties;
  className?: string;
}

export function Card({ padded = true, hoverable = false, children, style, className = "", ...rest }: CardProps) {
  return (
    <div
      className={`${hoverable ? "press" : ""} ${className}`.trim()}
      data-component="card"
      data-padded={padded}
      data-hoverable={hoverable}
      style={{
        background: "var(--surface)",
        borderRadius: "var(--r-lg)",
        boxShadow: "var(--shadow-sm)",
        border: "1px solid var(--border)",
        padding: padded ? 16 : 0,
        ...style,
      }}
      {...rest}
    >
      {children}
    </div>
  );
}
```

---

### `frontend/app/admin/components/SeverityIndicator.tsx` (component, request-response)

**Analog:** `frontend/app/components/redesign/SeverityGrid.tsx` (role-match — inline CSS var + accessibility pattern)

**Triple-encoding bars pattern from primitives.jsx lines 148-166 (admin always uses "bars" style):**
```typescript
"use client";
import type { CSSProperties } from "react";

export type SeverityLevel = "low" | "medium" | "high";

export interface SeverityIndicatorProps {
  severity: SeverityLevel;
  style?: CSSProperties;
}

const SEV_MAP = {
  low:    { color: "var(--sev-low)",    label: "Low",    level: 1 },
  medium: { color: "var(--sev-medium)", label: "Medium", level: 2 },
  high:   { color: "var(--sev-high)",   label: "High",   level: 3 },
} as const;

const BAR_HEIGHTS = [4, 8, 12] as const;

export function SeverityIndicator({ severity, style }: SeverityIndicatorProps) {
  const s = SEV_MAP[severity] ?? SEV_MAP.medium;
  return (
    <span
      data-component="severity-indicator"
      data-severity={severity}
      style={{ display: "inline-flex", alignItems: "center", gap: 6, ...style }}
    >
      {/* Decorative bars — aria-hidden, text label carries the semantic meaning */}
      <span
        aria-hidden="true"
        style={{ display: "inline-flex", alignItems: "flex-end", gap: 2, height: 12 }}
      >
        {BAR_HEIGHTS.map((h, i) => (
          <span
            key={i}
            style={{
              width: 3,
              height: h,
              background: i < s.level ? s.color : "var(--border-strong)",
              borderRadius: 1,
            }}
          />
        ))}
      </span>
      {/* Text label — carries WCAG 1.4.1 non-color encoding */}
      <span style={{
        fontSize: 11,
        fontFamily: "var(--font-mono)",
        textTransform: "uppercase",
        letterSpacing: "0.04em",
        color: "var(--ink-2)",
      }}>
        {s.label}
      </span>
    </span>
  );
}
```

---

### `frontend/app/admin/lib/useOnlineStatus.ts` (hook, event-driven)

**Analog:** No existing hook analog in codebase. Pattern from CONTEXT.md D-15 and RESEARCH.md Pattern 5.

See "No Analog Found" section below. Use the exact specification pattern from RESEARCH.md.

---

### `frontend/app/layout.tsx` (config, request-response) — MODIFY

**Analog:** Self — read current file at lines 14-18

**Current JetBrains Mono declaration (lines 14-18) — requires weight "600" addition:**
```typescript
// CURRENT (missing weight "600"):
const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  weight: ["400", "500"],           // ← Add "600" here
  variable: "--font-jetbrains-mono",
  display: "swap",
});

// TARGET after modification:
const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  weight: ["400", "500", "600"],    // Weight 600 required by UI-SPEC section headers
  variable: "--font-jetbrains-mono",
  display: "swap",
});
```

**`<html>` element requires `suppressHydrationWarning` (line 49):**
```typescript
// CURRENT:
<html lang="en" className={fontVars}>

// TARGET:
<html lang="en" className={fontVars} suppressHydrationWarning>
```

---

### `frontend/app/admin/layout.tsx` (layout/middleware, request-response) — MODIFY

**Analog:** Self — read current file at lines 1-59

**Three additions required to current layout:**

1. **Add `import './admin.css'` at top** (after existing imports):
```typescript
import { cookies, headers } from 'next/headers';
import { redirect } from 'next/navigation';
import { INTERNAL_API_URL } from '@/app/lib/config';
import AdminSidebar from './components/AdminSidebar';
import './admin.css';   // Direction B token layer — add this line
```

2. **Add FOTWT blocking script** (from CONTEXT.md D-10 — placed as first element before `.admin-portal` div):
```typescript
// In the return for authenticated routes (after auth guard resolves):
return (
  <>
    <script
      dangerouslySetInnerHTML={{
        __html: `(function(){var d=localStorage.getItem('admin-theme');if(d==='dark')document.documentElement.classList.add('dark');})();`,
      }}
    />
    <div className="admin-portal" style={{ minHeight: '100vh', display: 'flex', background: 'var(--bg)' }}>
      <AdminSidebar role={role} />
      <main style={{ flex: 1 }}>{children}</main>
    </div>
  </>
);
```

3. **Replace Tailwind wrapper** — current line 47:
```typescript
// CURRENT (line 47):
<div className="min-h-screen bg-gray-50">

// TARGET:
<div className="admin-portal" style={{ minHeight: '100vh', background: 'var(--bg)' }}>
```

---

### `frontend/app/admin/components/AdminSidebar.tsx` (component, event-driven) — REWRITE

**Analog:** Self — current file lines 1-111

**Preserved structure (keep from current file):**
- `"use client"` directive (line 1)
- `useState` for mobile drawer open/close (line 5)
- `interface AdminSidebarProps { role: string }` (lines 6-8)
- `navLinks` JSX fragment pattern with role-gated Users link (lines 13-63)
- All `href` values unchanged: `/admin`, `/admin/reports`, `/admin/reports/map`, `/admin/profile`, `/admin/users`
- Mobile overlay and drawer pattern (lines 77-108)

**What changes (replace Tailwind with CSS vars + swap lucide-react for Icon):**
```typescript
// OLD (lines 4-5):
import { Menu, X } from "lucide-react";

// NEW:
import { Icon } from "./Icon";

// OLD nav link (lines 15-22):
<a href="/admin" className="block px-3 py-2 rounded-lg hover:bg-gray-100 text-sm font-medium">
  Dashboard
</a>

// NEW nav link:
<a
  href="/admin"
  style={{
    display: "flex", alignItems: "center", gap: 8,
    padding: "8px 12px", borderRadius: "var(--r-md)",
    fontSize: 14, fontWeight: 400, color: "var(--ink-2)",
    textDecoration: "none",
  }}
>
  <Icon name="grid" size={16} aria-hidden />
  Dashboard
</a>

// OLD hamburger (line 74):
<Menu className="w-5 h-5 text-gray-700" />

// NEW hamburger:
<Icon name="menu" size={20} aria-hidden />

// OLD close button (line 104):
<X className="w-5 h-5 text-gray-500" />

// NEW close button:
<Icon name="close" size={20} aria-hidden />

// OLD sidebar container (line 87):
className="fixed top-0 left-0 z-50 h-full w-64 bg-white border-r border-gray-200 p-4 ..."

// NEW sidebar container:
style={{
  position: "fixed", top: 0, left: 0, zIndex: 50,
  height: "100%", width: 220,
  background: "var(--surface)",
  borderRight: "1px solid var(--border)",
  padding: 16,
  transform: isOpen ? "translateX(0)" : "translateX(-100%)",
  transition: "transform 200ms ease",
}}
```

---

### `frontend/app/admin/components/StatusBadge.tsx` (component, request-response) — REWRITE

**Analog:** `frontend/app/components/ui/Pill.tsx` (role-match — Pill-based badge with dot)

**Full rewrite pattern from RESEARCH.md Code Examples:**
```typescript
"use client";
import type { CSSProperties } from "react";

export type StatusValue = "submitted" | "under_review" | "resolved";

export interface StatusBadgeProps {
  status: string;
  monoLabel?: boolean;
  size?: "sm" | "md";
}

const STATUS_MAP = {
  submitted:    { tone: "info",   dot: "var(--status-submitted)", label: "Submitted" },
  under_review: { tone: "warn",   dot: "var(--status-review)",    label: "Under Review" },
  resolved:     { tone: "accent", dot: "var(--status-resolved)",  label: "Resolved" },
} as const;

export function StatusBadge({ status, monoLabel = false, size = "md" }: StatusBadgeProps) {
  const m = STATUS_MAP[status as keyof typeof STATUS_MAP] ?? STATUS_MAP.submitted;
  return (
    <span
      data-testid="status-badge"
      data-tone={m.tone}
      data-status={status}
      data-component="status-badge"
      role="status"
      aria-label={`Status: ${m.label.toLowerCase()}`}
      style={{
        display: "inline-flex",
        alignItems: "center",
        padding: size === "sm" ? "2px 8px" : "4px 10px",
        borderRadius: "var(--r-full)",
        fontSize: monoLabel ? 11 : 12,
        fontWeight: 500,
        fontFamily: monoLabel ? "var(--font-mono)" : "inherit",
        letterSpacing: monoLabel ? "0.02em" : undefined,
        textTransform: monoLabel ? "uppercase" : undefined,
        background: `var(--${m.tone}-bg)`,
        color: `var(--${m.tone}-ink)`,
        border: `1px solid var(--${m.tone}-border)`,
        gap: 6,
        lineHeight: 1,
        whiteSpace: "nowrap",
      }}
    >
      <span style={{ width: 6, height: 6, borderRadius: 999, background: m.dot, flexShrink: 0 }} aria-hidden="true" />
      {m.label}
    </span>
  );
}
export default StatusBadge;
```

**Test update for StatusBadge.test.tsx** — replace `classesContaining(el, "amber")` with:
```typescript
// Replace ALL classesContaining() assertions with data-tone:
expect(badge.getAttribute('data-tone')).toBe('info');     // submitted
expect(badge.getAttribute('data-tone')).toBe('warn');     // under_review
expect(badge.getAttribute('data-tone')).toBe('accent');   // resolved
// aria-label assertions remain unchanged (still valid):
expect(el.getAttribute('aria-label')!.toLowerCase()).toContain('under review');
```

---

### `frontend/app/admin/components/StatsCards.tsx` (component, CRUD) — REWRITE

**Analog:** Self — current file lines 1-155

**Preserved from current file:**
- All prop interface shapes (both `data`/`loading` and `stats`/`isLoading`/`isError`/`onRetry`)
- All `data-testid` attributes (`stats-cards`, `stats-cards-loading`, `stats-cards-error`, `stat-total`, `stat-submitted`, `stat-under-review`, `stat-resolved`, `skeleton`, `stats-retry-button`)
- Logic for resolving values from either prop shape (lines 87-103)
- Loading skeleton rendering (lines 44-62)
- Error state rendering (lines 63-79)

**What changes — Tailwind class strings replaced with inline style:**
```typescript
// OLD SkeletonCard (line 32):
<div data-testid="skeleton" className="animate-pulse bg-white rounded-2xl shadow-sm p-6">
  <div className="h-4 bg-gray-200 rounded w-1/2 mb-3" />
  <div className="h-8 bg-gray-200 rounded w-1/3" />
</div>

// NEW SkeletonCard — data-testid preserved, className removed:
<div
  data-testid="skeleton"
  style={{
    background: "var(--surface)", borderRadius: "var(--r-lg)",
    border: "1px solid var(--border)", padding: 16,
    animation: "pulse 1.5s ease-in-out infinite",
  }}
>
  <div style={{ height: 10, background: "var(--surface-2)", borderRadius: "var(--r-sm)", width: "50%", marginBottom: 12 }} />
  <div style={{ height: 32, background: "var(--surface-2)", borderRadius: "var(--r-sm)", width: "33%" }} />
</div>

// OLD card grid container (line 107):
<div data-testid="stats-cards" className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">

// NEW (responsive grid via inline style — no media query needed for basic layout):
<div
  data-testid="stats-cards"
  style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(180px, 1fr))", gap: 16 }}
>
```

---

### `frontend/app/admin/components/ReportsTable.tsx` (component, CRUD) — REWRITE

**Analog:** Self — current file lines 1-60+

**Preserved from current file:**
- `"use client"` directive
- All imports (`adminApi`, `getCategoryLabel`, etc.)
- `Report` interface and `ReportsTableProps` interface
- `expandedRows` and `duplicateRows` useState pattern (lines 46-49)
- `toggleExpand` async function (lines 51-60+)
- All `role`-gated delete button logic
- All `onStatusChange`, `onDelete`, `onUpdateStatus` callback wiring

**What changes:**
```typescript
// OLD table header row:
<thead className="bg-gray-50">
  <tr>
    <th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">

// NEW — inline style, no className:
<thead style={{ background: "var(--surface-2)" }}>
  <tr>
    <th style={{
      padding: "8px 12px", textAlign: "left",
      fontSize: 10, fontWeight: 600, fontFamily: "var(--font-mono)",
      color: "var(--muted)", textTransform: "uppercase", letterSpacing: "0.10em",
    }}>

// OLD table row:
<tr className="hover:bg-gray-50 border-b border-gray-200">

// NEW:
<tr style={{ borderBottom: "1px solid var(--border)" }}>
  {/* Each <td> uses style={{ padding: "10px 12px", color: "var(--ink-2)", fontSize: 14 }} */}
```

---

### `frontend/app/admin/components/UserManagementTable.tsx` (component, CRUD) — REWRITE

**Analog:** Self — current file lines 1-50+

**Preserved from current file:**
- `"use client"` directive
- `AdminUser` interface and `UserManagementTableProps`
- Empty state render (line 23-26)
- All `currentUserId` comparison for self-row guard
- `onDeactivate` callback wiring

**What changes:**
```typescript
// OLD role badge (Tailwind color classes):
// role === "admin" → className "bg-blue-100 text-blue-700"
// role === "reviewer" → className "bg-gray-100 text-gray-700"

// NEW — use Pill component with tone:
import { Pill } from "./Pill";
// role === "admin" → <Pill tone="accent" size="sm">{user.role}</Pill>
// role === "reviewer" → <Pill tone="neutral" size="sm">{user.role}</Pill>

// OLD table th:
<th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">

// NEW:
<th style={{ padding: "8px 12px", fontSize: 10, fontWeight: 600, fontFamily: "var(--font-mono)",
             color: "var(--muted)", textTransform: "uppercase", letterSpacing: "0.10em" }}>

// Inactive row — add opacity:
// row style: is_active === false ? { opacity: 0.5 } : {}
```

---

### `frontend/app/admin/components/CreateUserModal.tsx` (component, request-response) — REWRITE

**Analog:** Self — current file lines 1-50+

**Preserved from current file:**
- `"use client"` directive
- All state declarations (`email`, `password`, `role`, `displayName`, `errors`, `isSubmitting`)
- `validate()` function logic (lines 33-45)
- `handleSubmit` async logic with `createUser()` call
- Early return `if (!isOpen) return null`
- All `interface` definitions

**What changes — Direction B modal styling:**
```typescript
// OLD backdrop:
<div className="fixed inset-0 z-50 bg-black/50 flex items-center justify-center p-4">

// NEW:
<div
  style={{
    position: "fixed", inset: 0, zIndex: 50,
    background: "rgba(10,10,10,0.5)",
    display: "flex", alignItems: "center", justifyContent: "center", padding: 16,
  }}
  onClick={onClose}
>

// OLD modal container:
<div className="bg-white rounded-2xl shadow-xl w-full max-w-md p-6">

// NEW (480px width per UI-SPEC):
<div
  style={{
    background: "var(--surface)", borderRadius: "var(--r-xl)",
    boxShadow: "var(--shadow-lg)", width: "100%", maxWidth: 480, padding: 24,
  }}
  onClick={e => e.stopPropagation()}
>

// OLD form inputs:
<input className="w-full px-3 py-2 border border-gray-300 rounded-lg ...">

// NEW — use Input component:
import { Input } from "./Input";
<Input icon="mail" type="email" value={email} onChange={e => setEmail(e.target.value)} />
```

---

### Admin Page Files (all use same pattern)

**Analog:** `frontend/app/admin/page.tsx` (current file — lines 1-50+)

**Preserved in all page rewrites:**
- `"use client"` directive as first line
- All `useState` + `useCallback` + `useEffect` fetch patterns (lines 3-27)
- All `adminApi` method calls
- All `data-testid` attributes required by existing tests
- Error state rendering with retry button

**What changes in all pages:**
```typescript
// OLD page wrapper:
<div className="min-h-screen bg-gray-50 p-6">
  <div className="max-w-7xl mx-auto">
    <h1 className="text-2xl font-bold text-gray-900 mb-6">Dashboard</h1>

// NEW page wrapper — consume CSS vars, add useOnlineStatus:
import { useOnlineStatus } from "../lib/useOnlineStatus";

const isOnline = useOnlineStatus();

return (
  <div style={{ padding: "24px 32px", maxWidth: 1200 }}>
    {!isOnline && (
      <div role="status" aria-live="polite" style={{ /* offline banner */ }}>
        You are offline
      </div>
    )}
    <h1 style={{ fontFamily: "var(--font-mono)", fontSize: 14, fontWeight: 600, color: "var(--ink)", marginBottom: 24 }}>
      DASHBOARD
    </h1>
```

**Profile page specific — dark mode toggle pattern (D-12):**
```typescript
// Dark mode toggle (reads/writes localStorage directly — no React state needed):
function toggleDarkMode() {
  const html = document.documentElement;
  if (html.classList.contains("dark")) {
    html.classList.remove("dark");
    localStorage.setItem("admin-theme", "light");
  } else {
    html.classList.add("dark");
    localStorage.setItem("admin-theme", "dark");
  }
}
```

---

## Test File Patterns

### New test file structure (applies to all `__tests__/*.test.tsx` files)

**Analog:** `frontend/app/admin/components/__tests__/StatusBadge.test.tsx` (full test)

**File header pattern (lines 1-20 of StatusBadge.test.tsx):**
```typescript
/**
 * Tests for frontend/app/admin/components/[ComponentName].tsx
 *
 * Requirements covered:
 *   ADMIN-UI-02 — [component] renders without crashing, data-* attributes present
 *   ADMIN-UI-07 — [accessibility requirements]
 */

import React from "react";
import { render, screen } from "@testing-library/react";
import { [ComponentName] } from "../[ComponentName]";
```

**data-* attribute assertion pattern (replaces Tailwind class assertions — from RESEARCH.md Pattern 3):**
```typescript
// NEVER assert on className (Tailwind removed):
// expect(el).toHaveClass("bg-amber-100")  ← WRONG

// ALWAYS assert on data-* attributes:
expect(el.getAttribute("data-tone")).toBe("warn");
expect(el.getAttribute("data-variant")).toBe("accent");
expect(el.getAttribute("data-size")).toBe("md");
expect(el.getAttribute("data-component")).toBe("btn");
```

### `__tests__/Icon.test.tsx` specific pattern
```typescript
it("renders aria-hidden on decorative icon (no aria-label)", () => {
  render(<Icon name="menu" />);
  const svg = document.querySelector("svg")!;
  expect(svg.getAttribute("aria-hidden")).toBe("true");
});

it("renders role=img + aria-label when aria-label prop provided", () => {
  render(<Icon name="menu" aria-label="Open menu" />);
  const svg = document.querySelector("svg")!;
  expect(svg.getAttribute("role")).toBe("img");
  expect(svg.getAttribute("aria-label")).toBe("Open menu");
});

it("falls back to dots icon for unknown icon name", () => {
  // @ts-expect-error — intentional unknown name test
  render(<Icon name="unknown_icon_xyz" />);
  expect(document.querySelector("svg")).not.toBeNull();
});
```

### `__tests__/Btn.test.tsx` specific pattern
```typescript
it("renders with data-variant and data-size attributes", () => {
  render(<Btn variant="accent" size="lg">Click</Btn>);
  const btn = screen.getByRole("button");
  expect(btn.getAttribute("data-variant")).toBe("accent");
  expect(btn.getAttribute("data-size")).toBe("lg");
});

it("md and lg sizes have minHeight >= 40px (via data-size — component contract)", () => {
  render(<Btn size="md">Click</Btn>);
  const btn = screen.getByRole("button");
  // Contract: data-size="md" means minHeight 40px per SIZES map
  expect(btn.getAttribute("data-size")).toBe("md");
});
```

### `lib/__tests__/useOnlineStatus.test.ts` specific pattern

**Analog:** `frontend/app/lib/__tests__/constants.test.ts` (structure reference — no hook analog exists)

```typescript
import { renderHook, act } from "@testing-library/react";
import { useOnlineStatus } from "../useOnlineStatus";

describe("useOnlineStatus", () => {
  beforeEach(() => {
    Object.defineProperty(navigator, "onLine", { writable: true, value: true });
  });

  it("returns true when navigator.onLine is true on mount", () => {
    const { result } = renderHook(() => useOnlineStatus());
    expect(result.current).toBe(true);
  });

  it("returns false after 'offline' event fires", () => {
    const { result } = renderHook(() => useOnlineStatus());
    act(() => { window.dispatchEvent(new Event("offline")); });
    expect(result.current).toBe(false);
  });

  it("returns true after 'online' event fires following offline", () => {
    const { result } = renderHook(() => useOnlineStatus());
    act(() => { window.dispatchEvent(new Event("offline")); });
    act(() => { window.dispatchEvent(new Event("online")); });
    expect(result.current).toBe(true);
  });
});
```

---

## Shared Patterns

### CSS Custom Property Inline Style (universal — all new/rewritten components)

**Source:** `frontend/app/components/ui/Btn.tsx` + `frontend/app/components/redesign/SeverityGrid.tsx`
**Apply to:** Every new admin primitive and every rewritten admin component

```typescript
// The canonical pattern — never hard-code hex/oklch values in component files:
style={{ background: "var(--surface)", color: "var(--ink)", border: "1px solid var(--border)" }}

// Not this (hardcoded values break dark mode):
style={{ background: "#ffffff", color: "#0a0a0a" }}

// Not this (Tailwind classes — banned by D-09):
className="bg-white text-gray-900"
```

### data-component + data-variant Testability Pattern

**Source:** `frontend/app/components/ui/Btn.tsx` (lines 49-57), `frontend/app/components/ui/Pill.tsx` (lines 23-25)
**Apply to:** All new admin primitive components

```typescript
// Every primitive root element must carry:
data-component="[component-name]"     // e.g. "btn", "pill", "card", "status-badge"
data-variant={variant}                // when component has variant prop
data-tone={tone}                      // when component has tone prop
data-size={size}                      // when component has size prop
data-severity={severity}              // when component has severity prop
```

### Named Export (no default export)

**Source:** `frontend/app/components/ui/Btn.tsx` (line 27), `frontend/app/components/ui/Pill.tsx` (line 20)
**Apply to:** All NEW admin primitive components (Icon, Btn, Pill, Card, SeverityIndicator, etc.)

```typescript
// Correct — named export:
export function Btn({ ... }: BtnProps) { ... }

// Incorrect — default export (reserved for page components and rewritten existing components):
export default function Btn() { ... }
```

**Exception:** Rewritten existing components (AdminSidebar, StatusBadge, StatsCards, ReportsTable, UserManagementTable, CreateUserModal) keep `export default` to avoid breaking existing imports.

### "use client" Placement

**Source:** `frontend/app/admin/components/AdminSidebar.tsx` (line 1), `frontend/app/admin/page.tsx` (line 1)
**Apply to:** All components that use useState, useEffect, event handlers, or browser APIs

```typescript
"use client";

import type { ... } from "react";
// "use client" is always the very first line — before all imports
```

### aria-hidden on Decorative Icons

**Source:** `frontend/app/components/ui/Icon.tsx` (lines 66-68)
**Apply to:** Every Icon usage that is decorative (has no aria-label)

```typescript
// Decorative icon (navigation, button embellishment):
<Icon name="grid" size={16} aria-hidden />
// or: aria-hidden="true" as a prop (Icon component handles both)

// Interactive icon-only button — must have aria-label on the button:
<button aria-label="Delete report">
  <Icon name="trash" size={16} aria-hidden />
</button>
```

---

## No Analog Found

| File | Role | Data Flow | Reason |
|---|---|---|---|
| `frontend/app/admin/lib/useOnlineStatus.ts` | hook | event-driven | No custom hooks exist in the codebase. No `use*.ts` files found in `frontend/app/lib/` or `frontend/app/admin/lib/`. Use the exact specification from RESEARCH.md Pattern 5 (navigator.onLine + window 'offline'/'online' events). |

---

## Metadata

**Analog search scope:** `frontend/app/components/ui/`, `frontend/app/components/redesign/`, `frontend/app/admin/components/`, `frontend/app/admin/__tests__/`, `frontend/app/admin/components/__tests__/`, `frontend/app/lib/`, `design-ref/admin-portal/`
**Files scanned:** 35
**Pattern extraction date:** 2026-05-22

**Key lookup table for executor:**

| If creating... | Copy structure from | Lines |
|---|---|---|
| New admin primitive (Btn/Pill/Card) | `frontend/app/components/ui/Btn.tsx` | 1-60 |
| New admin icon component | `frontend/app/components/ui/Icon.tsx` | 1-73 |
| Admin inline-style component with CSS vars | `frontend/app/components/redesign/SeverityGrid.tsx` | 50-98 |
| Test for new primitive | `frontend/app/admin/components/__tests__/StatusBadge.test.tsx` | 1-35 (header/imports), 42-67 (describe/it pattern) |
| Test assertions for data-* (not Tailwind) | `frontend/app/admin/components/__tests__/StatusBadge.test.tsx` | All — this is the migration pattern |
| Admin page with fetch + loading/error states | `frontend/app/admin/page.tsx` | 1-50 |
| Token CSS scope block | `frontend/app/globals.css` | 16-56 (structure to mirror, NOT to modify) |
| admin.css Direction B tokens (verbatim) | `design-ref/admin-portal/tokens.css` | 117-186 (`.dir-b`), 189-214 (`.dir-b.dark`) |
| Primitive prop API spec | `design-ref/admin-portal/primitives.jsx` | per component (Icon:4-72, Btn:75-102, Pill:105-127, StatusBadge:130-142, SeverityIndicator:145-166, Card:169-184, SectionLabel:187-191, Input:194-203, Select:206-212, PhotoTile:232-236, MapPin:239-244, Sparkbars:247-258, Avatar:261-274, ConfidencePill:277-282, Kbd:293-295) |
