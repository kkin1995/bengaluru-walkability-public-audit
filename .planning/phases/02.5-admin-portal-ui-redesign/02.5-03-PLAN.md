---
phase: 02.5-admin-portal-ui-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 02.5-01
files_modified:
  - frontend/app/admin/components/ReportsTable.tsx
  - frontend/app/admin/reports/page.tsx
  - frontend/app/admin/reports/[id]/page.tsx
  - frontend/app/admin/components/__tests__/ReportsTable.test.tsx
  - frontend/app/admin/components/__tests__/ReportsTable.subtable.test.tsx
autonomous: true
requirements:
  - ADMIN-UI-03
  - ADMIN-UI-04
  - ADMIN-UI-07
  - ADMIN-UI-08
decisions_implemented:
  - D-06
  - D-07
  - D-09

must_haves:
  truths:
    - "Reports list mobile view shows the card stream layout — one card per report with photo tile, category title, status badge, ward·time meta, duplicate badge when present (success criterion #7)"
    - "Reports list also supports compact rows view (day-grouped) toggled via filter strip"
    - "Reports list desktop view shows the columnar table per UI-SPEC Reports List section (ID + thumbnail | TIME | CATEGORY+DESC | WARD | SEV | STATUS | DUP | actions)"
    - "Filter strip with chips (ALL · N / SUBMITTED · N / REVIEW · N / SEV: HIGH) renders above the list with active state visually distinct"
    - "Duplicate expand row preserves existing toggle behavior — fetches duplicates on first expand, shows warn-bg sub-row with linked reports"
    - "Report detail page renders mobile layout (photo + status badges + telemetry block + actions + citizen description + map + timeline) and desktop layout (split panel)"
    - "ReportsTable existing tests pass after rewrite — all data-testid attributes, expand/collapse logic, role-gated delete button preserved"
    - "SeverityIndicator usage on report rows always shows bars + label + color (never color alone) per WCAG 1.4.1"
  artifacts:
    - path: "frontend/app/admin/components/ReportsTable.tsx"
      provides: "Two-mode reports view (card stream + compact rows) on mobile, columnar table on desktop"
      contains: "expandedRows"
    - path: "frontend/app/admin/reports/page.tsx"
      provides: "Reports list page with filter strip, search, and view-mode toggle"
      contains: "useOnlineStatus"
    - path: "frontend/app/admin/reports/[id]/page.tsx"
      provides: "Report detail page (mobile + desktop layouts)"
      contains: "STATUS_HISTORY"
  key_links:
    - from: "frontend/app/admin/components/ReportsTable.tsx"
      to: "frontend/app/admin/components/StatusBadge.tsx"
      via: "import StatusBadge"
      pattern: "import\\s+StatusBadge"
    - from: "frontend/app/admin/components/ReportsTable.tsx"
      to: "frontend/app/admin/components/SeverityIndicator.tsx"
      via: "import { SeverityIndicator }"
      pattern: "SeverityIndicator"
    - from: "frontend/app/admin/reports/page.tsx"
      to: "frontend/app/admin/components/ReportsTable.tsx"
      via: "table render"
      pattern: "ReportsTable"
    - from: "frontend/app/admin/reports/[id]/page.tsx"
      to: "frontend/app/lib/translations.ts"
      via: "getCategoryLabel"
      pattern: "getCategoryLabel"
---

<objective>
Rewrite the Reports list (card stream + compact rows on mobile, columnar table on desktop) and Report detail (mobile single-column + desktop split panel) using the Plan 01 token layer and primitives. Migrate ReportsTable tests off Tailwind class assertions.

Purpose: Reports list is the admin's primary workflow surface. The card stream layout (A-pattern in B-voice) is the headline visual feature of the redesign — photo tile + category title + status badge + ward·time meta line is the canonical card shape called out in success criterion #7. The detail page is where status transitions and resolution happen; the telemetry block, action buttons, and status timeline define the high-touch interaction surface.

Output:
- `frontend/app/admin/components/ReportsTable.tsx` — two-mode mobile view (card stream + compact rows) + desktop columnar table, with filter strip
- `frontend/app/admin/reports/page.tsx` — reports list page with view-mode toggle, filter strip, search input, pagination
- `frontend/app/admin/reports/[id]/page.tsx` — report detail with telemetry block, action buttons, status timeline, citizen description (English + Kannada per language boundary)
- `frontend/app/admin/components/__tests__/ReportsTable.test.tsx` updated — Tailwind assertions replaced with data-* / data-testid assertions
- `frontend/app/admin/components/__tests__/ReportsTable.subtable.test.tsx` updated — same migration for duplicate expand row tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-CONTEXT.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-RESEARCH.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-01-SUMMARY.md

# Design source of truth
@design-ref/admin-portal/screens-b-mobile.jsx
@design-ref/admin-portal/screens-b-desktop.jsx
@design-ref/admin-portal/primitives.jsx

# Files being modified (current state)
@frontend/app/admin/components/ReportsTable.tsx
@frontend/app/admin/reports/page.tsx
@frontend/app/admin/reports/[id]/page.tsx
@frontend/app/admin/components/__tests__/ReportsTable.test.tsx
@frontend/app/admin/components/__tests__/ReportsTable.subtable.test.tsx

# Plan 01 outputs consumed by this plan
@frontend/app/admin/admin.css
@frontend/app/admin/lib/useOnlineStatus.ts
@frontend/app/admin/components/StatusBadge.tsx
@frontend/app/admin/components/SeverityIndicator.tsx
@frontend/app/admin/components/Pill.tsx
@frontend/app/admin/components/Btn.tsx
@frontend/app/admin/components/Card.tsx
@frontend/app/admin/components/PhotoTile.tsx
@frontend/app/admin/components/SectionLabel.tsx
@frontend/app/admin/components/Icon.tsx
@frontend/app/admin/components/Input.tsx
@frontend/app/admin/components/Select.tsx
@frontend/app/admin/components/ConfidencePill.tsx

# Domain helpers
@frontend/app/lib/translations.ts
@frontend/app/admin/lib/adminApi.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite ReportsTable with two-mode mobile view + desktop columnar table; update tests</name>
  <files>
    frontend/app/admin/components/ReportsTable.tsx,
    frontend/app/admin/components/__tests__/ReportsTable.test.tsx,
    frontend/app/admin/components/__tests__/ReportsTable.subtable.test.tsx
  </files>
  <read_first>
    frontend/app/admin/components/ReportsTable.tsx,
    frontend/app/admin/components/__tests__/ReportsTable.test.tsx,
    frontend/app/admin/components/__tests__/ReportsTable.subtable.test.tsx,
    design-ref/admin-portal/screens-b-mobile.jsx,
    design-ref/admin-portal/screens-b-desktop.jsx,
    design-ref/admin-portal/primitives.jsx,
    frontend/app/admin/components/StatusBadge.tsx,
    frontend/app/admin/components/SeverityIndicator.tsx,
    frontend/app/admin/components/Pill.tsx,
    frontend/app/admin/components/PhotoTile.tsx,
    frontend/app/admin/components/Card.tsx,
    frontend/app/admin/components/ConfidencePill.tsx,
    frontend/app/admin/lib/adminApi.ts,
    frontend/app/lib/translations.ts,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
  </read_first>
  <behavior>
    - ReportsTable.tsx: Preserve every existing piece per PATTERNS.md "preserved from current file" list:
      - `"use client"` directive
      - All imports for `adminApi`, `getCategoryLabel`, types
      - `Report` interface and `ReportsTableProps` interface (current shape)
      - `expandedRows` + `duplicateRows` `useState` pattern
      - `toggleExpand` async function that calls `adminApi.getAdminReports({ duplicate_of_id })` on first expand
      - All `role`-gated delete button logic (super admin only)
      - All `onStatusChange`, `onDelete`, `onUpdateStatus` callback wiring
      - All existing `data-testid` attributes (verify by reading current file and the existing tests — preserve every one)
      - `data-testid="status-badge"` on every StatusBadge usage (re-rendered via the rewritten StatusBadge from Plan 01)
    - Add a `viewMode` prop (or internal state) supporting `"card-stream" | "compact-rows" | "table"`. The page (Task 2) controls this via prop or via a child toggle component — leaving the choice to ReportsTable internal state with a sensible default keeps the prop API of this component backwards-compatible.
    - Render three layout modes:
      a) **Card stream (mobile default)**: vertical list of Card primitives, one per report. Each card has three rows per UI-SPEC Reports List card structure:
         - Header row: report ID (mono 10px weight 600 `color: var(--ink-2)`) + status dot + relative timestamp (uppercase mono)
         - Body row: 64px PhotoTile (radius var(--r-xs)) + category title (Inter 14px weight 600 — use `getCategoryLabel(report.category)`) + ward·location_source meta (10px mono uppercase muted) + 2-line description excerpt
         - Footer row (dashed border-top via `borderTop: "1px dashed var(--border)"`): SeverityIndicator (bars style — from Plan 01) + duplicate badge (Pill tone="warn" with `DUP +N · CONF` when duplicate_count > 0, else neutral Pill `NO_DUPES`)
      b) **Compact rows (mobile alt)**: Day-grouped list. Day separator (mono 10px uppercase muted label "TODAY · MAY 22" or relative day string). Each row: 44px PhotoTile + category title (Inter 14px weight 600) + StatusBadge (with `monoLabel` prop) right-aligned. Meta line below: ID (mono weight 600) + ward (uppercase) + relative time (10px mono). Trailing: duplicate count "+N" in warn-ink + SeverityIndicator bars.
      c) **Desktop columnar table (≥1024px)**: HTML `<table>` with `<thead>` and `<th scope="col">` per UI-SPEC accessibility item #8. Columns: ID (thumbnail + id) | TIME | CATEGORY+DESC | WARD | SEV | STATUS | DUP | actions. Header row: `background: var(--surface-2)`, 10px mono uppercase muted weight 600 letter-spacing 0.10em. Row hover: press state. 1px border between rows. Status cell in table renders dot + uppercase status string (NOT a pill badge) — per UI-SPEC "StatusBadge in reports table" exception. Expandable duplicate group: existing warn-bg sub-row with the linked reports table — preserve the inner `<tr>` markup that the current test expects.
    - Filter strip (rendered above the list by ReportsTable — or by the parent page; for testability keep in ReportsTable): horizontal scrollable Pill chips with `no-scrollbar` class — ALL · N / SUBMITTED · N / REVIEW · N / SEV: HIGH. Active chip: tone="ink", inactive: tone="outline". Chip radius var(--r-xs) per UI-SPEC. Multiple chips can be active (category + severity combinations).
    - Search Input below filter strip per UI-SPEC: 34px height (matches Plan 01 Input — note Input default is 44px but desktop search per UI-SPEC is 34px; pass `style={{ minHeight: 34, height: 34 }}` to Input as a size override on the desktop search field). Placeholder: "Filter · ward · id · text…" with `icon="search"` and a Sort Btn variant="ghost" size="sm" on the right.
    - Pagination footer: "1–7 / 218" + prev/next Btn variant="ghost" size="sm" + page number Btn variant="ghost" size="xs" buttons. Wire to existing pagination props.
    - Zero Tailwind classes. Zero lucide-react. Default export preserved.

    - ReportsTable.test.tsx UPDATE: Read the current test file end-to-end. For every assertion that uses `classesContaining(...)`, `toHaveClass("bg-..."`, `toHaveClass("text-..."`, `toHaveClass("border-..."`, `toHaveClass("hover:..."`, replace with one of:
      - `data-testid` lookup (preserved per PATTERNS.md)
      - `data-tone` / `data-variant` / `data-status` / `data-severity` / `data-component` attribute getAttribute assertion
      - Behavioral assertion (e.g. `screen.getByRole("table")`, `screen.getAllByRole("row")` count, `fireEvent.click` then `await screen.findByText(...)`)
    - For role-gated delete button tests: keep the `data-testid="delete-button"` assertion (preserved per PATTERNS.md) — verify presence/absence by role.
    - For expand/collapse tests in ReportsTable.subtable.test.tsx: keep the `data-testid="expand-button"` lookups (or whatever the current file uses); verify the warn-bg sub-row by querying for `data-testid="duplicate-group"` or `[data-warn-row="true"]` (executor adds this data attribute to the sub-row in the rewrite if not already present).
    - Do NOT change describe/it block titles. Do NOT delete tests. The migration is mechanical — assertion shape changes, behavior coverage stays.

    - ReportsTable.subtable.test.tsx UPDATE: Same Tailwind-class-to-data-attribute migration. Preserve all expand/collapse behavior assertions.
  </behavior>
  <action>
    Rewrite ReportsTable.tsx in-place. Preserve every existing prop, useState, callback, data-testid, and the toggleExpand fetch logic from the current file (read it first, identify the preserved blocks per PATTERNS.md). Layer in the three render modes (card stream, compact rows, desktop table) using Plan 01 primitives (Card, PhotoTile, Pill, StatusBadge, SeverityIndicator, Btn, Icon, Input, ConfidencePill).

    Use `useState` for `viewMode` with default based on `matchMedia("(min-width: 1024px)")`: desktop → "table", mobile → "card-stream". Provide a view-mode toggle (two Pill chips: GRID / LIST or CARDS / ROWS) above the list when on mobile.

    For the desktop table, the status cell uses the dot+uppercase pattern, NOT a `<StatusBadge>` component (UI-SPEC exception). But for card stream + compact rows, render the StatusBadge primitive (with monoLabel prop on compact rows). Both must satisfy existing tests that look up `data-testid="status-badge"`.

    Duplicate expand row: preserve existing onClick handler + duplicateRows state. Render the expanded warn-bg sub-row using `background: var(--warn-bg)`, `border: 1px dashed var(--warn-border)` per UI-SPEC interaction patterns. Inner table for linked reports shows ID / category / ward / time / distance / status / severity columns (no photo thumbnails). Add `data-testid="duplicate-group"` to the expanded row container if not already present (verify against existing test assertions; preserve whatever testid the current tests expect).

    Update ReportsTable.test.tsx and ReportsTable.subtable.test.tsx in-place: Tailwind-class assertions → data-* attribute or data-testid behavioral assertions. Read both test files, identify every offending assertion, replace surgically. Do not delete tests; do not change describe/it titles.

    Use getCategoryLabel from `frontend/app/lib/translations.ts` for every category display per UI-SPEC Language Boundary (BUG-01 retained from Phase 02.3).

    Do not modify the page that consumes this component yet (Task 2). Do not touch detail page (Task 3). Do not modify package.json.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern="ReportsTable" --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/admin/components/ReportsTable.tsx` does NOT contain `lucide-react`
    - File `frontend/app/admin/components/ReportsTable.tsx` does NOT contain Tailwind class names (grep `grep -nE 'className="(bg-|text-|border-|hover:|md:|p-[0-9]|rounded-)' frontend/app/admin/components/ReportsTable.tsx` returns no matches)
    - File `frontend/app/admin/components/ReportsTable.tsx` contains `getCategoryLabel`
    - File `frontend/app/admin/components/ReportsTable.tsx` contains `expandedRows` (preserved logic)
    - File `frontend/app/admin/components/ReportsTable.tsx` contains `toggleExpand` (preserved logic)
    - File `frontend/app/admin/components/ReportsTable.tsx` imports from `./StatusBadge` and `./SeverityIndicator` and `./PhotoTile`
    - File `frontend/app/admin/components/ReportsTable.tsx` contains an HTML `<table` element (for desktop columnar mode)
    - File `frontend/app/admin/components/ReportsTable.tsx` contains `scope="col"` (UI-SPEC accessibility item #8)
    - File `frontend/app/admin/components/__tests__/ReportsTable.test.tsx` contains zero `classesContaining(` references
    - File `frontend/app/admin/components/__tests__/ReportsTable.test.tsx` contains zero `toHaveClass("bg-` or `toHaveClass("text-` or `toHaveClass("border-` or `toHaveClass("hover:` references (grep `grep -nE 'toHaveClass\("(bg-|text-|border-|hover:)' frontend/app/admin/components/__tests__/ReportsTable.test.tsx` returns no matches)
    - File `frontend/app/admin/components/__tests__/ReportsTable.subtable.test.tsx` contains zero `toHaveClass("bg-` or similar Tailwind-class assertions (same grep)
    - File `frontend/app/admin/components/__tests__/ReportsTable.test.tsx` retains `data-testid` lookups (`grep -c "data-testid" frontend/app/admin/components/__tests__/ReportsTable.test.tsx` ≥ 3)
    - `cd frontend && npm test -- --testPathPattern="ReportsTable" --watchAll=false` exits 0
  </acceptance_criteria>
  <done>
    ReportsTable renders three modes (card stream + compact rows mobile, columnar table desktop) using Plan 01 primitives. All existing preserved behavior intact: expand/collapse, role-gated delete, status change, getCategoryLabel for categories. Both test files migrated off Tailwind assertions to data-* / behavioral assertions; all tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite Reports list page and Report detail page</name>
  <files>
    frontend/app/admin/reports/page.tsx,
    frontend/app/admin/reports/[id]/page.tsx
  </files>
  <read_first>
    frontend/app/admin/reports/page.tsx,
    frontend/app/admin/reports/[id]/page.tsx,
    design-ref/admin-portal/screens-b-mobile.jsx,
    design-ref/admin-portal/screens-b-desktop.jsx,
    frontend/app/admin/components/ReportsTable.tsx,
    frontend/app/admin/components/SeverityIndicator.tsx,
    frontend/app/admin/components/StatusBadge.tsx,
    frontend/app/admin/components/Btn.tsx,
    frontend/app/admin/components/Card.tsx,
    frontend/app/admin/components/SectionLabel.tsx,
    frontend/app/admin/components/PhotoTile.tsx,
    frontend/app/admin/components/Pill.tsx,
    frontend/app/admin/components/Icon.tsx,
    frontend/app/admin/components/ConfidencePill.tsx,
    frontend/app/admin/lib/useOnlineStatus.ts,
    frontend/app/admin/lib/adminApi.ts,
    frontend/app/lib/translations.ts,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
  </read_first>
  <behavior>
    - reports/page.tsx: `"use client"` first line. Preserve all existing data-fetching (adminApi.getAdminReports), useState/useCallback/useEffect, pagination state, filters, error handling, retry button — per PATTERNS.md "preserved in all page rewrites" list.
    - Heading: `<h1>` with mono uppercase "QUEUE" per UI-SPEC Reports List section + count chip showing total reports (Pill tone="neutral" size="sm" with mono count).
    - useOnlineStatus + offline banner using UI-SPEC offline copy (same as Plan 02 dashboard pattern): heading "You're offline right now", body string verbatim from UI-SPEC.
    - Render `<ReportsTable>` (from Task 1) wired to the page's filter/search/pagination state. The page owns API state; ReportsTable owns view-mode toggling.
    - Generic API error: render an error Card using the EXACT string from UI-SPEC Copywriting "API error (reports list)": `"Could not load reports. Check your connection and try again."` with a retry Btn variant="ghost".
    - Page wrapper: `<div style={{ padding: "24px 32px", maxWidth: 1400, marginLeft: "auto", marginRight: "auto" }}>`.
    - Zero Tailwind classes.

    - reports/[id]/page.tsx: `"use client"` first line. Preserve all existing logic (adminApi.getAdminReport(id), adminApi.updateReportStatus, delete confirm, error/404 handling).
    - 404 handling: when report fetch returns 404, render error Card with EXACT string `"This report no longer exists or was removed."` per UI-SPEC Copywriting.
    - Delete confirmation: inline modal (not browser confirm()) per UI-SPEC interaction patterns. Modal renders with backdrop `rgba(10,10,10,0.5)`, 480px Card, message EXACT string `"Delete this report? This cannot be undone."`, Cancel Btn variant="ghost" + Delete Btn variant="danger".
    - Mobile layout per UI-SPEC "Report Detail" mobile spec:
      - Top: photo area `height: 280px`, full-width, with absolute-positioned overlays: photo count chip (top-left, Pill tone="outline" size="sm" mono), EXIF confirmation chip (top-right, Pill tone="accent" size="sm" "EXIF_OK"), zoom Btn (bottom-right, icon-only with `aria-label="Zoom photo"`), SHA chip (bottom-left, Pill tone="neutral" size="sm" mono "SHA: 7AC30…")
      - Status badges row: StatusBadge + SeverityIndicator + ConfidencePill (when duplicate_confidence is set)
      - Category title: Inter 14px weight 600 — `getCategoryLabel(report.category)`
      - Telemetry block: bordered Card padded false, internal grid `display: grid; gridTemplateColumns: 1fr 1fr` with rows showing LAT/LNG, WARD, LOCATION_SRC, SUBMITTED_AT, DUP_CONF, UUID. Each cell: SectionLabel for the key + mono value below. Row separators `borderTop: 1px dashed var(--border)`.
      - Action buttons: 2-column grid — "Move to review" (Btn variant="accent" size="md") + "Mark resolved" (Btn variant="secondary" size="md"). Both min-height 44px (UI-SPEC accessibility). Wire to existing adminApi.updateReportStatus.
      - Citizen description Card: SectionLabel "CITIZEN_DESCRIPTION", body text in Inter 14px (English description). If the report has a Kannada description field (translation/locale-tagged), render below in `var(--font-kn)` weight 400 `color: var(--muted)` fontSize 0.9em per UI-SPEC Language Boundary. (If no Kannada field exists in the current Report type, render English only — no synthesis.)
      - Map placeholder: 140px map-tile div (use PhotoTile primitive with custom gradient) + ward label chip + coordinates line + "Open in Maps" link (anchor to `https://www.google.com/maps?q={lat},{lng}` — opens in new tab with `rel="noopener noreferrer"`).
      - Status timeline Card: SectionLabel "STATUS_HISTORY · TAIL". Vertical list of past status changes (8px dot + status label uppercase mono + timestamp + "BY · admin" line + optional note). When status_history data is unavailable from current API, render a single entry from the report's current status as a fallback (do not synthesize fake history).
    - Desktop layout (≥1024px viewport): Split panel — `display: grid; gridTemplateColumns: 1.3fr 1fr`. Left panel: photo (large, dark surface-2 bg). Right panel: scrollable, same content as mobile right panel.
    - Desktop top action bar: "View public" (Btn variant="ghost") + "Delete" (Btn variant="danger-soft", role-gated to super_admin per existing logic) + "Resolve" (Btn variant="accent"). All min-height 40px.
    - Page wrapper: same `padding 24px 32px` mobile, full-bleed on desktop.
    - Zero Tailwind classes. Default export preserved.
  </behavior>
  <action>
    Rewrite both pages in-place. For each, read the current file end-to-end and preserve every fetch, error handler, useState, useCallback, useEffect, and adminApi call.

    reports/page.tsx: Replace the Tailwind layout shell with the Direction B mono "QUEUE" heading + total count Pill, wire `useOnlineStatus` for the offline banner (UI-SPEC heading "You're offline right now", body exact string), render the rewritten ReportsTable (Task 1) with filter/search/pagination props from existing page state. Error state uses the EXACT UI-SPEC string "Could not load reports. Check your connection and try again." Page wrapper padding 24px 32px, maxWidth 1400.

    reports/[id]/page.tsx: Replace layout entirely with mobile (default) + desktop split panel via matchMedia. Preserve adminApi.getAdminReport(id), updateReportStatus, delete confirmation. Replace browser confirm() (if currently used) with inline modal using EXACT UI-SPEC string "Delete this report? This cannot be undone." 404 handling shows EXACT string "This report no longer exists or was removed." Build the photo + overlays, status badges row, category title, telemetry block (2-col grid), action buttons (2-col grid for mobile, 3 buttons in desktop top bar), citizen description (English + optional Kannada per language boundary — render Kannada only if the current API/Report type exposes it), map placeholder + "Open in Maps" link with `rel="noopener noreferrer"`, status timeline.

    Use Plan 01 primitives throughout (Card, Pill, Btn, SectionLabel, SeverityIndicator, StatusBadge, PhotoTile, Icon, ConfidencePill). Use `getCategoryLabel` for every category display.

    Zero Tailwind classes. Zero lucide-react. Default exports preserved.

    Do not modify ReportsTable.tsx (Task 1 owns it) or any test file (Task 1 covered test migration). Do not touch the map page, users page, profile page, or organizations page (Plan 04).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern="(reports|ReportsTable)" --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/admin/reports/page.tsx` does NOT contain Tailwind class names (grep `grep -nE 'className="(bg-|text-|border-|hover:|md:|p-[0-9]|rounded-)' frontend/app/admin/reports/page.tsx` returns no matches)
    - File `frontend/app/admin/reports/page.tsx` contains `useOnlineStatus`
    - File `frontend/app/admin/reports/page.tsx` contains the EXACT string `Could not load reports. Check your connection and try again.`
    - File `frontend/app/admin/reports/page.tsx` contains `You're offline right now`
    - File `frontend/app/admin/reports/page.tsx` imports `ReportsTable`
    - File `frontend/app/admin/reports/[id]/page.tsx` does NOT contain Tailwind class names (same grep)
    - File `frontend/app/admin/reports/[id]/page.tsx` contains the EXACT string `This report no longer exists or was removed.`
    - File `frontend/app/admin/reports/[id]/page.tsx` contains the EXACT string `Delete this report? This cannot be undone.`
    - File `frontend/app/admin/reports/[id]/page.tsx` contains `STATUS_HISTORY` (section label literal)
    - File `frontend/app/admin/reports/[id]/page.tsx` contains `getCategoryLabel`
    - File `frontend/app/admin/reports/[id]/page.tsx` contains `rel="noopener noreferrer"` on the "Open in Maps" link
    - File `frontend/app/admin/reports/[id]/page.tsx` does NOT contain `window.confirm(` (inline modal replaces browser confirm per UI-SPEC)
    - File `frontend/app/admin/reports/[id]/page.tsx` does NOT contain `lucide-react`
    - File `frontend/app/admin/reports/page.tsx` does NOT contain `lucide-react`
    - `cd frontend && npm test -- --testPathPattern="(reports|ReportsTable)" --watchAll=false` exits 0
  </acceptance_criteria>
  <done>
    Reports list page renders the QUEUE heading, total count Pill, filter strip, search input, view-mode toggle, ReportsTable in card-stream/compact-rows/table modes, pagination, offline banner via useOnlineStatus. Report detail renders mobile single-column (photo + overlays + status badges + telemetry + actions + description + map + timeline) and desktop split panel. Delete confirmation uses inline modal with locked UI-SPEC copy. All existing reports/detail tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/api/admin/reports*` | Reports list + detail fetch authenticated reports; existing auth guard from layout.tsx unchanged in this plan |
| User-controlled URL params (filter, pagination, [id]) → URL | Existing query param handling preserved; no new injection surface introduced |
| External link "Open in Maps" → google.com/maps | New anchor — must use `rel="noopener noreferrer"` to prevent reverse tabnabbing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.5-03-01 | Tampering | "Open in Maps" external anchor in report detail | mitigate | All external links use `target="_blank" rel="noopener noreferrer"` to prevent the opened tab from manipulating `window.opener`. Coordinates passed via URL are already public data on the citizen map. |
| T-02.5-03-02 | Information Disclosure | API error display on reports list and detail | mitigate | Use only UI-SPEC locked generic strings ("Could not load reports...", "This report no longer exists..."); never render the raw server error message. Preserves SEC-06 contract beyond the login surface. |
| T-02.5-03-03 | Information Disclosure | Submitter PII in admin views | accept | Admin views legitimately show submitter_name (admin role required by layout.tsx auth guard) — SEC-01 mandates removal from PUBLIC API only. No change to access control in this plan. |
| T-02.5-03-04 | Tampering | Inline delete confirmation modal | mitigate | Modal renders entirely client-side; the actual delete is an authenticated DELETE call already gated by adminApi + backend role check. Inline modal replaces browser `confirm()` for UX consistency, not as a security boundary. |
</threat_model>

<verification>
After both tasks complete:

1. `cd frontend && npm test -- --testPathPattern="ReportsTable" --watchAll=false` exits 0
2. `cd frontend && npm test -- --testPathPattern="reports" --watchAll=false` exits 0
3. `cd frontend && npm test -- --testPathPattern="admin" --watchAll=false` exits 0 (full admin suite still green after Plan 03 lands)
4. Manual smoke (human-check): visit `/admin/reports` on mobile viewport — see card stream with photo tiles. Toggle to compact rows view. On desktop viewport — see columnar table with proper column headers. Click a report → see detail page with telemetry block + action buttons. Click delete (as super admin) → see inline modal with the UI-SPEC string.
</verification>

<success_criteria>
- Reports list shows card stream cards with photo tile + category label (getCategoryLabel) + StatusBadge + ward·time meta + duplicate badge (success criterion #7)
- Compact rows mode works (day-grouped, monoLabel StatusBadge variant)
- Desktop columnar table preserves expand/collapse + delete role gate + status change behavior
- Report detail renders mobile + desktop layouts with telemetry block, action buttons ≥ 44px (ADMIN-UI-07), citizen description language boundary, status timeline
- All API error display strings match UI-SPEC Copywriting Contract exactly (SEC-06 extended)
- External links use noopener noreferrer
- All ReportsTable + ReportsTable.subtable tests pass after Tailwind→data-attribute migration
- Zero Tailwind classes in any rewritten file; zero lucide-react imports
</success_criteria>

<output>
Create `.planning/phases/02.5-admin-portal-ui-redesign/02.5-03-SUMMARY.md` capturing: files modified, ReportsTable view-mode prop shape, test migration table (assertion-by-assertion: old Tailwind class → new data-attribute or behavioral check), UI-SPEC copy strings used verbatim, any deviations from the prototype (e.g. if certain overlay chips on the detail photo were not implemented due to missing API fields — note them as carry-over to Plan 04 or future phase).
</output>
</content>
</invoke>