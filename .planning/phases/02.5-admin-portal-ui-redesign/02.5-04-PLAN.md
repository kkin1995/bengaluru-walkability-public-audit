---
phase: 02.5-admin-portal-ui-redesign
plan: 04
type: execute
wave: 3
depends_on:
  - 02.5-01
  - 02.5-02
  - 02.5-03
files_modified:
  - frontend/app/admin/reports/map/page.tsx
  - frontend/app/admin/users/page.tsx
  - frontend/app/admin/components/UserManagementTable.tsx
  - frontend/app/admin/components/CreateUserModal.tsx
  - frontend/app/admin/organizations/page.tsx
  - frontend/app/admin/profile/page.tsx
  - frontend/app/admin/components/__tests__/UserManagementTable.test.tsx
  - frontend/app/admin/components/__tests__/UserManagementTable.phase2.test.tsx
autonomous: true
requirements:
  - ADMIN-UI-03
  - ADMIN-UI-04
  - ADMIN-UI-06
  - ADMIN-UI-07
  - ADMIN-UI-08
decisions_implemented:
  - D-06
  - D-07
  - D-09
  - D-12

must_haves:
  truths:
    - "Map view renders all reports with status-colored MapPin markers + map legend (SUBMITTED teal / REVIEW orange / RESOLVED green) and a cluster popup card"
    - "Users management page shows the rewritten user table (avatar + name + role pill + org + active status dot + last_login + actions) with inactive row opacity"
    - "Create user modal renders at 480px with the locked UI-SPEC copy (password hint, CREATE USER CTA) and SEC-06-compliant error display"
    - "Organizations page exists at /admin/organizations (newly created — was missing from current routes) and renders with admin-portal tokens, role-gated to admin/super_admin"
    - "Profile page renders identity card + display name field + change password fields + session telemetry + log-out-all + dark mode toggle (D-12)"
    - "Empty state, error state, and offline state UIs use the locked UI-SPEC copywriting contract strings verbatim"
    - "`cd frontend && npm run build` exits 0 — zero TypeScript errors, zero new ESLint warnings (success criterion #8)"
    - "Full admin test suite green after final wave"
  artifacts:
    - path: "frontend/app/admin/reports/map/page.tsx"
      provides: "Map view with status-colored pins, legend, cluster popup"
      contains: "MapPin"
    - path: "frontend/app/admin/users/page.tsx"
      provides: "Users management page using rewritten UserManagementTable + CreateUserModal"
      contains: "UserManagementTable"
    - path: "frontend/app/admin/components/UserManagementTable.tsx"
      provides: "User table with role pill + active status dot + inactive opacity"
      contains: "data-testid"
    - path: "frontend/app/admin/components/CreateUserModal.tsx"
      provides: "Direction B create-user modal (480px) with locked copy"
      contains: "MIN 12"
    - path: "frontend/app/admin/organizations/page.tsx"
      provides: "Organizations stub page rendering with admin-portal tokens"
      contains: "admin-portal"
    - path: "frontend/app/admin/profile/page.tsx"
      provides: "Profile page with dark mode toggle (D-12) + change password + identity card"
      contains: "admin-theme"
  key_links:
    - from: "frontend/app/admin/reports/map/page.tsx"
      to: "frontend/app/admin/components/MapPin.tsx"
      via: "import { MapPin }"
      pattern: "MapPin"
    - from: "frontend/app/admin/users/page.tsx"
      to: "frontend/app/admin/components/UserManagementTable.tsx"
      via: "table render"
      pattern: "UserManagementTable"
    - from: "frontend/app/admin/users/page.tsx"
      to: "frontend/app/admin/components/CreateUserModal.tsx"
      via: "modal render"
      pattern: "CreateUserModal"
    - from: "frontend/app/admin/profile/page.tsx"
      to: "document.documentElement"
      via: "dark mode toggle"
      pattern: "classList\\.(toggle|add|remove)\\(['\"]dark['\"]\\)"
---

<objective>
Complete the Phase 02.5 admin portal redesign by rewriting the four remaining screens (Map, Users, Organizations, Profile), the two remaining components (UserManagementTable + CreateUserModal), creating the missing Organizations page route, wiring empty/error/offline states everywhere, and gating the entire phase on a green `npm run build`.

Purpose: Plans 01-03 covered the foundation, login, dashboard, sidebar, reports list, and report detail. This final plan closes the loop on the four remaining surfaces. The Organizations stub satisfies the desktop nav item added in Plan 02. The Profile page is the canonical home for the dark mode toggle (D-12 specifies both sidebar footer AND profile). The build gate (ADMIN-UI-08) is the phase's hard exit criterion.

Output:
- `frontend/app/admin/reports/map/page.tsx` rewritten with MapPin markers + frosted-glass legend chip
- `frontend/app/admin/users/page.tsx` rewritten with QUEUE-style heading, search, role filter
- `frontend/app/admin/components/UserManagementTable.tsx` rewritten with Pill (role) + Avatar + active status dot + inactive opacity
- `frontend/app/admin/components/CreateUserModal.tsx` rewritten at 480px with locked password hint copy
- `frontend/app/admin/organizations/page.tsx` newly created minimal stub
- `frontend/app/admin/profile/page.tsx` rewritten with identity card + dark mode toggle + change password + session telemetry
- Empty / error / offline state UIs with UI-SPEC locked strings everywhere
- UserManagementTable tests migrated off Tailwind class assertions
- `npm run build` green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-CONTEXT.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-RESEARCH.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-01-SUMMARY.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-02-SUMMARY.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-03-SUMMARY.md

# Design source of truth
@design-ref/admin-portal/screens-b-mobile.jsx
@design-ref/admin-portal/screens-b-desktop.jsx
@design-ref/admin-portal/primitives.jsx

# Files being modified (current state)
@frontend/app/admin/reports/map/page.tsx
@frontend/app/admin/users/page.tsx
@frontend/app/admin/components/UserManagementTable.tsx
@frontend/app/admin/components/CreateUserModal.tsx
@frontend/app/admin/profile/page.tsx
@frontend/app/admin/components/__tests__/UserManagementTable.test.tsx
@frontend/app/admin/components/__tests__/UserManagementTable.phase2.test.tsx

# Plan 01 primitives + hook consumed by this plan
@frontend/app/admin/components/MapPin.tsx
@frontend/app/admin/components/Avatar.tsx
@frontend/app/admin/components/Pill.tsx
@frontend/app/admin/components/Card.tsx
@frontend/app/admin/components/Btn.tsx
@frontend/app/admin/components/Input.tsx
@frontend/app/admin/components/Select.tsx
@frontend/app/admin/components/SectionLabel.tsx
@frontend/app/admin/components/Icon.tsx
@frontend/app/admin/lib/useOnlineStatus.ts
@frontend/app/admin/admin.css

# Domain helpers
@frontend/app/lib/translations.ts
@frontend/app/admin/lib/adminApi.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite Map view, Users page + UserManagementTable + CreateUserModal; migrate tests</name>
  <files>
    frontend/app/admin/reports/map/page.tsx,
    frontend/app/admin/users/page.tsx,
    frontend/app/admin/components/UserManagementTable.tsx,
    frontend/app/admin/components/CreateUserModal.tsx,
    frontend/app/admin/components/__tests__/UserManagementTable.test.tsx,
    frontend/app/admin/components/__tests__/UserManagementTable.phase2.test.tsx
  </files>
  <read_first>
    frontend/app/admin/reports/map/page.tsx,
    frontend/app/admin/users/page.tsx,
    frontend/app/admin/components/UserManagementTable.tsx,
    frontend/app/admin/components/CreateUserModal.tsx,
    frontend/app/admin/components/__tests__/UserManagementTable.test.tsx,
    frontend/app/admin/components/__tests__/UserManagementTable.phase2.test.tsx,
    design-ref/admin-portal/screens-b-mobile.jsx,
    design-ref/admin-portal/screens-b-desktop.jsx,
    frontend/app/admin/components/MapPin.tsx,
    frontend/app/admin/components/Avatar.tsx,
    frontend/app/admin/components/Pill.tsx,
    frontend/app/admin/components/Card.tsx,
    frontend/app/admin/components/Btn.tsx,
    frontend/app/admin/components/Input.tsx,
    frontend/app/admin/components/Select.tsx,
    frontend/app/admin/components/Icon.tsx,
    frontend/app/admin/lib/useOnlineStatus.ts,
    frontend/app/lib/translations.ts,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
  </read_first>
  <behavior>
    - reports/map/page.tsx: `"use client"` first line. Preserve every existing leaflet/react-leaflet usage with `dynamic(() => import(...), { ssr: false })` per project Map convention (CLAUDE.md). Preserve all marker fetch logic, popup logic, and category label sourcing from translations.ts.
    - Replace the page chrome (header, container, legend) with Direction B treatment per UI-SPEC Map View section:
      - Floating top bar: count chip (Pill tone="outline" size="sm" with mono count "N REPORTS")
      - Scrollable filter chips row above the map for status (ALL / SUBMITTED / REVIEW / RESOLVED) — reuse existing filter wiring from current map page if present
      - Replace existing marker icons with the `MapPin` primitive style — color by status using `var(--status-submitted)` / `var(--status-review)` / `var(--status-resolved)`. Implementation: continue using react-leaflet `L.divIcon` with HTML that renders a `<div>` styled like the MapPin primitive (14px circle with 2px var(--surface) border)
      - Map legend: frosted-glass chip at bottom-left with the EXACT UI-SPEC labels `SUBMITTED` (teal dot) / `REVIEW` (orange dot) / `RESOLVED` (green dot). Use Pill tone="neutral" or a custom div with `backdrop-filter: blur(8px); background: rgba(255,255,255,0.7)` (light mode) or `rgba(20,20,20,0.7)` (dark mode — gated by `[data-theme]` or CSS var resolution). Position `absolute; bottom: 16px; left: 16px`.
      - Cluster popup card at bottom on mobile / floating panel on desktop: Card with `boxShadow: var(--shadow-lg)`, `border: 1px solid var(--border-strong)`, `background: var(--surface)`, showing report metadata
      - Desktop layout: 340px right panel listing reports in the current viewport (use existing viewport filter logic if present; otherwise stub with the current full reports list)
    - Zero Tailwind classes on the rewritten chrome. The Leaflet CSS imports (from `unpkg.com/leaflet/dist/leaflet.css` per nginx CSP allowlist) remain unchanged. Default export preserved.
    - useOnlineStatus banner: same UI-SPEC offline copy. Render above the map.

    - users/page.tsx: `"use client"` first line. Preserve every existing fetch (adminApi.getAdminUsers, adminApi.createUser, adminApi.deactivateUser), useState, error/retry handling. Preserve role-gate (only admin / super_admin can access).
    - Heading: `USERS` (mono uppercase) + total count Pill.
    - Render `<UserManagementTable>` (the rewritten component) wired to current users state.
    - Add a "New user" Btn variant="accent" size="md" with `icon="plus"` that opens `<CreateUserModal>`.
    - Empty state: when no users, render Card with the EXACT UI-SPEC empty state heading `"All caught up"` and body `"There are no reports waiting for your review. New ones will appear here automatically as citizens submit them."` — NOTE: the UI-SPEC empty state copy is generic. For the users page where the empty state semantics differ, the executor uses a fallback EXACT string "No users yet." with a "New user" Btn — this is a deliberate scope-conscious deviation since UI-SPEC does not provide a users-specific empty state. Document in SUMMARY.
    - Error state: API error Card with EXACT string from UI-SPEC fallback "Could not load users. Check your connection and try again." (reused from reports list API error pattern — same shape, "users" substituted for "reports"). Generic, no raw error.
    - useOnlineStatus banner with UI-SPEC offline copy.
    - Page wrapper: `padding: 24px 32px; maxWidth: 1400; marginLeft: auto; marginRight: auto`.
    - Zero Tailwind classes.

    - components/UserManagementTable.tsx: Preserve every existing piece per PATTERNS.md "preserved from current file" list:
      - `"use client"` directive
      - `AdminUser` interface and `UserManagementTableProps`
      - Empty state render at the top
      - `currentUserId` self-row guard
      - `onDeactivate` callback wiring
      - All existing `data-testid` attributes (verify against test files)
    - Replace Tailwind with inline `style` consuming CSS vars + Plan 01 primitives.
    - Table structure: HTML `<table>` per UI-SPEC accessibility. Columns: USER (Avatar + name + email) | ROLE (Pill) | ORG | STATUS (dot + ACTIVE/OFF) | LAST_LOGIN | actions
    - Role display: `<Pill tone="accent" size="sm">ADMIN</Pill>` for admin, `<Pill tone="neutral" size="sm">REVIEWER</Pill>` for reviewer. Super admin gets an additional inline mini-Pill tone="accent" size="sm" with "SUPER" text per UI-SPEC.
    - Status display: active = 6px dot `background: var(--accent)` + text "ACTIVE" (mono uppercase muted), inactive = 6px dot `background: var(--muted-2)` + text "OFF".
    - Inactive rows: entire `<tr>` gets `style={{ opacity: 0.55 }}` per UI-SPEC.
    - Table header: `<thead style={{ background: "var(--surface-2)" }}>` with 10px mono uppercase weight 600 letter-spacing 0.10em headers.
    - Actions: dots menu Btn variant="ghost" size="sm" with `icon="dots"` + `aria-label="User actions for {name}"` — clicking opens a small floating menu with "Deactivate" (danger) option. Deactivate triggers existing `onDeactivate` callback.
    - Deactivate confirmation: inline modal (per UI-SPEC interaction patterns) with EXACT string `"Deactivate {name}? They will lose access immediately."` (interpolate the actual user name). Cancel Btn variant="ghost" + Confirm Btn variant="danger".
    - Zero Tailwind. Default export preserved.

    - components/CreateUserModal.tsx: Preserve every existing piece:
      - `"use client"` directive
      - All state declarations (email, password, role, displayName, errors, isSubmitting)
      - `validate()` function logic (existing 12-char password threshold per SEC-07)
      - `handleSubmit` async logic with `createUser()` call
      - Early return when `!isOpen`
      - All `interface` definitions
    - Replace visual layer with Direction B modal per UI-SPEC Users → Invite User Modal:
      - Backdrop: `position: fixed; inset: 0; zIndex: 50; background: rgba(10,10,10,0.5); display: flex; alignItems: center; justifyContent: center; padding: 16px`
      - Modal Card: 480px width, `borderRadius: var(--r-xl)`, `boxShadow: var(--shadow-lg)`, padded
      - Click outside (backdrop onClick) closes; click inside (Card onClick stopPropagation) does not
      - Fields per UI-SPEC: EMAIL (Input icon="mail"), DISPLAY_NAME (Input icon="user"), ROLE toggle (two-button group: ADMIN vs REVIEWER — render as two Btn variant="ghost" with one active in variant="ink"), ORG_ID Select (icon="org"), INITIAL_PASSWORD Input with `type="password"` + a "Generate" Btn variant="ghost" size="sm" right-side suffix
      - Password hint: EXACT UI-SPEC string `"// MIN 12 · ARGON2ID · CHANGE_ON_FIRST_LOGIN"` rendered in 10px mono color var(--muted)
      - Footer: Cancel Btn variant="ghost" + CREATE USER Btn variant="accent" with `iconRight="arrow_right"`
      - Error display: `role="status" aria-live="polite"` region with `var(--danger-bg)` / `var(--danger-ink)` styling — show only generic strings, never raw server errors
    - Zero Tailwind. Default export preserved.

    - UserManagementTable.test.tsx + UserManagementTable.phase2.test.tsx UPDATE: Same Tailwind-to-data-attribute migration as Plan 03 Task 1. Replace `classesContaining(...)`, `toHaveClass("bg-...")`, `toHaveClass("text-...")`, `toHaveClass("border-...")` with `data-tone` / `data-component` / data-testid / behavioral assertions. Preserve all describe/it titles. Preserve all `data-testid` lookups.
  </behavior>
  <action>
    Rewrite all six files in-place (one new file: organizations/page.tsx in Task 2). For each file: read current, preserve all PATTERNS.md-listed blocks, layer in the Direction B visual treatment.

    Map page: Keep the existing `dynamic(() => import('./MapClient'), { ssr: false })` (or equivalent SSR-safe leaflet wrapper) pattern. Replace the `L.divIcon` HTML for markers with a div styled like the MapPin primitive (14px circle, status color, 2px surface border). Add filter chips strip, frosted-glass legend, count chip, cluster popup Card. Wire useOnlineStatus offline banner.

    Users page: QUEUE-style heading, total count Pill, "New user" Btn opening CreateUserModal, render UserManagementTable. Empty state uses fallback string "No users yet." with action Btn (note in SUMMARY). API error uses generic mirror of UI-SPEC reports list error.

    UserManagementTable: Preserve every interface, data-testid, empty state, currentUserId guard, onDeactivate callback. Build HTML table with Avatar + Pill role + dot+text status + opacity 0.55 inactive rows. Use existing data-testid attributes (read tests to identify which are required). Dots-menu actions with aria-label per user.

    CreateUserModal: Preserve every state declaration, validate(), handleSubmit, early-return-on-not-open. Build 480px Direction B modal with locked UI-SPEC copy (password hint EXACT string). All inputs ≥ 44px tap target. Generic error display only.

    Test files: Read both UserManagementTable test files, identify every Tailwind-class assertion, replace with data-attribute or behavioral assertion. Preserve test names and data-testid lookups. Do not delete tests.

    Do not touch organizations/page.tsx (Task 2 owns it). Do not touch profile/page.tsx (Task 2 owns it). Do not touch package.json. Do not modify any other file.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern="(UserManagementTable|users|map)" --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/admin/reports/map/page.tsx` does NOT contain Tailwind class names (grep `grep -nE 'className="(bg-|text-|border-|hover:|md:|p-[0-9]|rounded-)' frontend/app/admin/reports/map/page.tsx` returns no matches)
    - File `frontend/app/admin/reports/map/page.tsx` contains the literal strings `SUBMITTED`, `REVIEW`, `RESOLVED` (map legend labels)
    - File `frontend/app/admin/reports/map/page.tsx` contains `useOnlineStatus`
    - File `frontend/app/admin/reports/map/page.tsx` contains `dynamic(` import for SSR-safe leaflet (preserved pattern)
    - File `frontend/app/admin/users/page.tsx` does NOT contain Tailwind class names (same grep)
    - File `frontend/app/admin/users/page.tsx` contains `UserManagementTable` and `CreateUserModal` imports
    - File `frontend/app/admin/users/page.tsx` contains `useOnlineStatus`
    - File `frontend/app/admin/components/UserManagementTable.tsx` does NOT contain Tailwind class names (same grep)
    - File `frontend/app/admin/components/UserManagementTable.tsx` contains `opacity: 0.55` (inactive row)
    - File `frontend/app/admin/components/UserManagementTable.tsx` contains `aria-label="User actions` (icon-only button aria-label per UI-SPEC accessibility item #4)
    - File `frontend/app/admin/components/UserManagementTable.tsx` contains the EXACT string `Deactivate ` followed by user-name interpolation followed by `? They will lose access immediately.` (template literal acceptable; grep `grep "They will lose access immediately" frontend/app/admin/components/UserManagementTable.tsx` returns at least one match)
    - File `frontend/app/admin/components/CreateUserModal.tsx` does NOT contain Tailwind class names (same grep)
    - File `frontend/app/admin/components/CreateUserModal.tsx` contains `maxWidth: 480` or `width: 480` (modal sizing per UI-SPEC)
    - File `frontend/app/admin/components/CreateUserModal.tsx` contains the EXACT string `// MIN 12 · ARGON2ID · CHANGE_ON_FIRST_LOGIN`
    - File `frontend/app/admin/components/CreateUserModal.tsx` contains `rgba(10,10,10,0.5)` (UI-SPEC backdrop)
    - File `frontend/app/admin/components/__tests__/UserManagementTable.test.tsx` contains zero Tailwind-class assertions (grep `grep -nE 'toHaveClass\("(bg-|text-|border-|hover:)' frontend/app/admin/components/__tests__/UserManagementTable.test.tsx` returns no matches; same for `classesContaining(`)
    - File `frontend/app/admin/components/__tests__/UserManagementTable.phase2.test.tsx` contains zero Tailwind-class assertions (same greps)
    - `cd frontend && npm test -- --testPathPattern="(UserManagementTable|users|map)" --watchAll=false` exits 0
  </acceptance_criteria>
  <done>
    Map view shows status-colored pins + UI-SPEC legend. Users page renders rewritten UserManagementTable with role Pills, active status dots, inactive opacity, and dots-menu actions with aria-labels. CreateUserModal rendered at 480px with locked UI-SPEC copy and generic-only error display. UserManagementTable tests migrated to data attributes; all relevant tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Organizations stub, rewrite Profile page, lucide-react cleanup decision, build gate</name>
  <files>
    frontend/app/admin/organizations/page.tsx,
    frontend/app/admin/profile/page.tsx
  </files>
  <read_first>
    frontend/app/admin/profile/page.tsx,
    frontend/app/admin/page.tsx,
    design-ref/admin-portal/screens-b-mobile.jsx,
    design-ref/admin-portal/screens-b-desktop.jsx,
    frontend/app/admin/components/Avatar.tsx,
    frontend/app/admin/components/Card.tsx,
    frontend/app/admin/components/Btn.tsx,
    frontend/app/admin/components/Input.tsx,
    frontend/app/admin/components/Pill.tsx,
    frontend/app/admin/components/SectionLabel.tsx,
    frontend/app/admin/components/Icon.tsx,
    frontend/app/admin/lib/useOnlineStatus.ts,
    frontend/app/admin/lib/adminApi.ts,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md,
    package.json
  </read_first>
  <behavior>
    - organizations/page.tsx: CREATE this file (does not exist per RESEARCH.md note 8). `"use client"` first line. Render a minimal stub that satisfies the AdminSidebar nav link added in Plan 02 + the role-gated visibility.
      - Page wrapper: `padding: 24px 32px; maxWidth: 1400`
      - Heading: mono uppercase "ORGANIZATIONS" + Pill tone="outline" with text "PREVIEW" (signals this is a stub)
      - Body Card containing SectionLabel "ORG_TREE" + an empty state message using a fallback string `"Organization management is not configured yet."` (UI-SPEC does not provide a specific organizations empty state string — note this in SUMMARY).
      - Wire useOnlineStatus offline banner with UI-SPEC offline copy.
      - Default export.
      - Zero Tailwind. This is a stub — no API calls. Full Organizations management UI is deferred to a future phase (REQUIREMENTS.md does not allocate a specific phase yet).

    - profile/page.tsx: `"use client"` first line. Preserve every existing piece:
      - All state declarations (display name, password change form, errors, success messages)
      - All adminApi calls (updateProfile, changePassword, logout, logoutAllSessions if present)
      - All validation logic
      - Existing data-testid attributes if any (read current file to identify)
    - Replace visual layer with Direction B per UI-SPEC Profile section:
      - User identity Card: Avatar (size 48) + name (Inter 14px weight 600) + email (mono 10px muted) + role Pill (tone="accent" for admin, tone="neutral" for reviewer) + org Pill (tone="outline")
      - Display name Card: SectionLabel "DISPLAY_NAME", Input prefilled with current name, "Save" Btn variant="accent"
      - Change password Card: SectionLabel "CHANGE_PASSWORD", three Input fields (current password, new password, confirm new password) each `type="password"` and ≥ 44px height, password requirement hint "// MIN 12 · ARGON2ID" in 10px mono muted, "Update password" Btn variant="accent"
      - Session telemetry Card: SectionLabel "SESSION", mono lines for LAST_LOGIN, SESSION_EXP, IP (if available from existing API; otherwise show placeholder strings — note in SUMMARY)
      - Dark mode toggle Card (D-12 canonical settings location): SectionLabel "APPEARANCE", description text "Switch between light and dark display.", and a Btn variant="secondary" with `icon="sun"` or `icon="moon"` based on current state. Click handler: identical logic to AdminSidebar dark mode toggle from Plan 02 (read `document.documentElement.classList.contains("dark")` in useEffect for initial state, on click toggle the class + write `localStorage.setItem("admin-theme", "light"|"dark")`).
      - Log out all sessions Card: SectionLabel "SECURITY", Btn variant="danger-soft" size="md" with text "Log out all sessions" and `icon="logout"` (aria-label preserved if existing test asserts it)
      - Password change success: render with EXACT UI-SPEC string `"Password updated."` in a `role="status" aria-live="polite"` accent-bg banner.
      - Password change error: generic string only — never raw server message. Default: `"Could not update password. Check your current password and try again."`
      - User updated success: EXACT UI-SPEC string `"User created. They can now log in."` is for the create-user flow (Modal in Task 1) — NOT for profile updates. Profile display-name update success: fallback string `"Profile saved."`
    - Page wrapper: `padding: 24px 32px; maxWidth: 720; marginLeft: auto; marginRight: auto`. Default export preserved. Zero Tailwind.

    - lucide-react cleanup decision (DOCUMENTED, not executed): Read `frontend/package.json` and run `grep -rE "lucide-react" frontend/app` mentally — RESEARCH.md Open Question #2 result + current grep show citizen-facing components (`PhotoCapture.tsx`, `SubmitSuccess.tsx`) still import lucide-react. **Decision: DO NOT uninstall lucide-react**. Document this in 02.5-04-SUMMARY.md under "lucide-react cleanup decision" — package remains in package.json; admin-portal usages are all replaced (verified by grep of `frontend/app/admin/` returning 0 lucide-react references after this plan).

    - Final build gate: After both files exist, the executor runs `cd frontend && npm run build` (and `npm test` for the full suite). Per ADMIN-UI-08, the build must exit 0 with zero new ESLint warnings.
  </behavior>
  <action>
    Create organizations/page.tsx and rewrite profile/page.tsx in-place.

    organizations/page.tsx: New file at `frontend/app/admin/organizations/page.tsx`. `"use client"` first line. Minimal stub: heading "ORGANIZATIONS" + PREVIEW Pill + body Card with placeholder text + useOnlineStatus offline banner. Zero API calls. Default export.

    profile/page.tsx: Read current file end-to-end. Preserve all state, validation, adminApi calls. Build the five Cards (identity, display name, change password, session telemetry, appearance/dark-mode, security/log-out-all) per UI-SPEC. Wire dark mode toggle identically to AdminSidebar's toggle (same localStorage key `admin-theme`, same classList behavior). Password change success uses EXACT UI-SPEC string "Password updated." in role="status" region. Password change error uses generic fallback "Could not update password. Check your current password and try again." Use Plan 01 primitives throughout (Card, SectionLabel, Avatar, Pill, Btn, Input, Icon). Zero Tailwind.

    Verify lucide-react grep on the admin subtree: `grep -rE "lucide-react" frontend/app/admin` should return 0 matches after this plan. Citizen-facing `frontend/app/components/` may continue to import lucide-react — that is outside the phase scope. Document the decision in the SUMMARY: keep package installed, admin-portal is lucide-free.

    Run `cd frontend && npm run build` after both files exist. If it fails, the executor MUST fix the underlying TypeScript or ESLint issue in the offending file before declaring the task done. Do not suppress errors with `// @ts-ignore` or `/* eslint-disable */` unless the failure is in third-party generated code (e.g. `.next/`). Document any deferrals in SUMMARY.

    Do not touch any file outside organizations/page.tsx and profile/page.tsx. Do not modify package.json. Do not touch tests in this task — UserManagementTable tests are Task 1's territory, and the build gate runs the full Jest suite as part of `npm test` verification at the plan level.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern="admin" --watchAll=false && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/admin/organizations/page.tsx` exists and `head -1 frontend/app/admin/organizations/page.tsx` shows `"use client";` (or single-quoted equivalent)
    - File `frontend/app/admin/organizations/page.tsx` contains `ORGANIZATIONS` (heading literal)
    - File `frontend/app/admin/organizations/page.tsx` contains `useOnlineStatus`
    - File `frontend/app/admin/organizations/page.tsx` does NOT contain Tailwind class names (grep `grep -nE 'className="(bg-|text-|border-|hover:|md:|p-[0-9]|rounded-)' frontend/app/admin/organizations/page.tsx` returns no matches)
    - File `frontend/app/admin/profile/page.tsx` does NOT contain Tailwind class names (same grep)
    - File `frontend/app/admin/profile/page.tsx` contains `admin-theme` (localStorage key — dark mode toggle)
    - File `frontend/app/admin/profile/page.tsx` contains `classList.toggle("dark")` OR (`classList.add("dark")` AND `classList.remove("dark")`)
    - File `frontend/app/admin/profile/page.tsx` contains the EXACT string `Password updated.`
    - File `frontend/app/admin/profile/page.tsx` contains `MIN 12 · ARGON2ID` (password hint)
    - File `frontend/app/admin/profile/page.tsx` imports `Avatar` from `../components/Avatar` or `./components/Avatar` (path varies; grep `grep -E "from\s+[\"'].*components/Avatar[\"']" frontend/app/admin/profile/page.tsx` returns at least one match)
    - Grep `grep -rE "lucide-react" frontend/app/admin` returns ZERO matches (entire admin subtree is lucide-free)
    - `cd frontend && npm run build` exits 0 (ADMIN-UI-08 phase gate — capture stdout to confirm "Compiled successfully" or equivalent Next.js success indicator)
    - `cd frontend && npm test -- --testPathPattern="admin" --watchAll=false` exits 0 (full admin suite green)
  </acceptance_criteria>
  <done>
    Organizations stub page exists at /admin/organizations. Profile page renders identity card + display name + change password (≥44px tap targets) + session telemetry + dark mode toggle (D-12 canonical) + log-out-all-sessions. Entire admin subtree is lucide-free (grep verified). `npm run build` exits 0 with zero TypeScript errors. Full admin test suite green. Phase 02.5 ready for verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/api/admin/users*` | Users management calls — existing auth guard enforces admin/super_admin role; no change in this plan |
| Browser → `/api/admin/auth/profile`, `/api/admin/auth/change-password` | Profile updates — existing auth guard preserved |
| Inline modal (deactivate user, delete report) → adminApi mutation | Confirmation modals are UX only; actual mutation is authenticated server call (no new trust boundary) |
| External "Open in Maps" link (Plan 03 territory; cross-reference) | rel="noopener noreferrer" enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.5-04-01 | Information Disclosure | Profile change-password error display | mitigate | Render only generic strings ("Could not update password. Check your current password and try again."); never echo backend error message. Extends SEC-06 contract to profile surface. |
| T-02.5-04-02 | Information Disclosure | CreateUserModal error display | mitigate | Same generic-only contract; the SEC-07 12-char password threshold is enforced both frontend (validate()) and backend — no new validation surface. |
| T-02.5-04-03 | Spoofing | Dark mode toggle in profile page | accept | Identical to AdminSidebar toggle (Plan 02 T-02.5-02-02 acceptance) — localStorage `admin-theme` write only affects UI for the local user. |
| T-02.5-04-04 | Information Disclosure | Profile session telemetry display (IP, last login) | accept | Telemetry shown is the admin's own session data — not cross-user disclosure. If existing API does not expose these fields, executor renders placeholders, not synthesized values. |
| T-02.5-04-05 | Tampering | "Log out all sessions" Btn | mitigate | Existing adminApi.logoutAllSessions endpoint (if present) is authenticated and idempotent. Button is danger-soft visual treatment — confirmation modal optional but recommended per UI-SPEC interaction patterns. |
</threat_model>

<verification>
After both tasks complete:

1. `cd frontend && npm test -- --testPathPattern="admin" --watchAll=false` exits 0 (full admin suite)
2. `cd frontend && npm run build` exits 0 with zero new ESLint warnings (ADMIN-UI-08 phase gate)
3. `grep -rE "lucide-react" frontend/app/admin` returns ZERO matches
4. Manual smoke (human-check): visit `/admin/users` — see new table with avatars + role pills + active status dots + inactive row opacity. Open create-user modal → see 480px modal with password hint copy. Visit `/admin/profile` → click dark mode toggle → `<html>` gains `.dark`, `localStorage.admin-theme === "dark"`. Hard reload → no FOTWT flash. Visit `/admin/organizations` → see stub page rendering with admin-portal tokens. Visit `/admin/reports/map` → see status-colored MapPin markers + frosted legend.
5. Spot accessibility check (human-check): all primary buttons (Authenticate, Start reviewing, Move to review, Mark resolved, Save, Update password, CREATE USER, Log out all sessions) measure ≥ 44px tap target in DevTools mobile viewport (ADMIN-UI-07).
</verification>

<success_criteria>
- Map view renders status-colored MapPin markers + UI-SPEC frosted legend (success criterion #1)
- Users page + UserManagementTable + CreateUserModal use Plan 01 primitives + tokens, no Tailwind (success criteria #1, #8)
- Organizations stub exists at /admin/organizations (success criterion #1 — all screens covered)
- Profile page has dark mode toggle (D-12) + locked password change copy + log-out-all (success criteria #1, #3, #8)
- Empty / error / offline states everywhere use UI-SPEC locked strings (success criterion #6)
- All interactive elements ≥ 44px tap target (success criterion #5 / ADMIN-UI-07)
- `npm run build` exits 0 with zero TS errors / zero new ESLint warnings (success criterion #8 / ADMIN-UI-08)
- Entire admin subtree is lucide-free (success criterion #2 / D-07)
- All admin tests pass after final wave
</success_criteria>

<output>
Create `.planning/phases/02.5-admin-portal-ui-redesign/02.5-04-SUMMARY.md` capturing: files created/modified, screen-by-screen completion checklist against ROADMAP Success Criteria #1-8, lucide-react cleanup decision (DO NOT uninstall; admin subtree clean; citizen pages still consume it — future phase if ever), any UI-SPEC copy strings that were filled with fallback values (users empty state, organizations stub message, profile session telemetry placeholders), build output summary (`npm run build` exit + warnings count), final test run summary (`npm test` exit + total tests).
</output>
</content>
</invoke>