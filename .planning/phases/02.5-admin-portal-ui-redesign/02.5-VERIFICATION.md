---
phase: 02.5-admin-portal-ui-redesign
verified: 2026-05-23T00:00:00Z
status: passed
score: 7/8 must-haves verified
overrides_applied: 1
uat_completed: 2026-05-23T00:00:00Z
uat_source: 02.5-UAT.md
uat_summary: "10/12 tests passed; 5 bugs found and fixed in fix/phase-02.5-uat-polish commits"
human_verification_results:
  - test: "Visit /admin/login in a browser. Hard-reload with localStorage.admin-theme = 'dark' set. Observe no white flash before dark mode applies."
    result: pass
    note: "FOTWT confirmed working. See UAT.md test #1."
  - test: "Visit /admin (dashboard) on mobile viewport. Verify hero card uses teal accent border + large mono count, severity composition shows bars+text+color per row."
    result: pass
    note: "Layout confirmed. Stats cards show real API data. See UAT.md test #2."
  - test: "Visit /admin/reports on mobile viewport. Confirm card stream shows: photo tile + human-readable category label + StatusBadge + ward·time meta line + duplicate badge."
    result: partial
    note: "BUG-T3-01: photo tiles showed placeholder (image_url undefined). Fixed in fix commit e07368a — image_path fallback added. Elements 2-5 confirmed."
  - test: "Visit /admin/reports/map. Confirm map pins are status-colored circles and the frosted legend shows SUBMITTED / REVIEW / RESOLVED labels."
    result: pass
    note: "Map loads, colored pins and frosted legend confirmed. Submitted color is teal (intentional redesign). See UAT.md test #4."
  - test: "Click the dark mode toggle in the admin sidebar footer. Confirm html.classList gains/loses 'dark', localStorage.admin-theme is written, and the portal switches color scheme without a full reload."
    result: pass
    note: "Toggle confirmed on desktop sidebar. classList + localStorage both correct. See UAT.md test #5."
  - test: "Check all primary interactive buttons for 44px tap targets."
    result: partial
    note: "Authenticate (48px), Start reviewing (48px), Save (44px), Change Password (44px), CREATE USER (44px) all pass. BUG-T6-02 (Log out all sessions hidden by mobile nav) fixed in fix commit e07368a."
overrides:
  - must_have: "Offline/error state shows pending-changes card with queued action descriptions"
    reason: "Admin portal is read-mostly with no offline mutation queue (D-13, D-14). Reassurance copy is present on all screens. Queued-action descriptions require a persistence layer that is out of scope for this phase."
    accepted_by: "kkin1995"
    accepted_at: "2026-05-23T00:00:00Z"
---

# Phase 02.5: Admin Portal UI Redesign Verification Report

**Phase Goal:** Implement the hybrid "B-voice × A-structure" design system for the admin portal at /admin — teal Console palette, JetBrains Mono chrome, A-pattern report cards, light+dark mode, full accessibility pass
**Verified:** 2026-05-22T10:14:12Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All admin screens render with hybrid design system tokens | ✓ VERIFIED | All 8 screens (Login, Dashboard, Reports list, Report detail, Map, Users, Organizations, Profile) exist and render with `.admin-portal` CSS var scope; all import Plan 01 primitives; zero Tailwind in any screen |
| 2 | `admin.css` is the single source of truth; no hardcoded hex/oklch in component files | ⚠ PARTIAL | `admin.css` exists and scopes all tokens. Minor violations: `Btn.tsx` uses `color: "#fff"` for danger variant text; `reports/[id]/page.tsx` has `color: "#fff"` on the zoom overlay button; `reports/map/page.tsx` uses hex literals in `getPinColor()` for Leaflet divIcon (documented intentional per Leaflet API constraint — CSS vars unavailable inside L.divIcon HTML injection) |
| 3 | Light + dark mode both work via `.dark` class on `<html>` with no FOTWT flash | ? UNCERTAIN | FOTWT script verified in admin layout (IIFE reads `localStorage.admin-theme` and calls `document.documentElement.classList.add('dark')`). `suppressHydrationWarning` on `<html>` confirmed. Flash-free behavior requires human browser test |
| 4 | Severity never color-only: bars + text label + color per WCAG 1.4.1 | ✓ VERIFIED | `SeverityIndicator.tsx` BAR_HEIGHTS=[4,8,12], bars wrapper `aria-hidden="true"`, text label "Low"/"Medium"/"High" rendered separately. Triple-encoding confirmed in code. Used in ReportsTable card stream, dashboard severity composition, and report detail |
| 5 | All interactive elements ≥ 44px tap target on mobile | ? UNCERTAIN | `Btn.tsx` md size has `minHeight: 44`. Action buttons in report detail have explicit `style={{ minHeight: 44 }}`. Authenticate button is `size="lg"` (48px per plan). Requires human DevTools measurement for complete confirmation |
| 6 | Offline/error state shows pending-changes card with queued action descriptions | ✗ FAILED | Offline banners with reassurance copy exist on all screens. But no "pending-changes card with queued action descriptions" is implemented — D-13 (plan decision) deliberately scoped to UI-indicator only with pendingCount=0. The UI-SPEC "Offline pending card label: Saved on this device" and "queued-actions list" are absent. The ROADMAP SC#6 requires queued action descriptions; the plan explicitly chose not to implement them. |
| 7 | Report cards show: photo thumbnail, category label (human-readable), status badge, ward · time meta, duplicate badge when present | ✓ VERIFIED | `ReportsTable.tsx` CardStreamRow: `PhotoTile` (64px), `getCategoryLabel(report.category).en`, `StatusBadge`, ward span in separate `<span>` elements, `Pill tone="warn"` when dupCount>0 / `Pill tone="neutral"` "NO_DUPES" when 0 |
| 8 | `npm run build` passes with zero TypeScript errors and zero new ESLint warnings | ✓ VERIFIED | `npm run build` exits 0, "Compiled successfully", all 13 app routes compiled. Test suite 407/407 passing across 26 suites |

**Score:** 6/8 truths fully verified; 1 failed (SC#6), 1 partial/warning (SC#2)

---

### Deferred Items

None identified — SC#6 failure is a genuine gap, not deferred to a later phase.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/app/admin/admin.css` | Direction B token layer | ✓ VERIFIED | 4882 bytes; `.admin-portal {}` (light) + `.dark .admin-portal {}` (dark); no `:root`; `--text-hero-mobile`, `--r-xs:4px` through `--r-xl:14px`; shadows with `rgba(10,10,10,...)` |
| `frontend/app/admin/layout.tsx` | FOTWT + admin-portal class + admin.css import | ✓ VERIFIED | `import './admin.css'`, IIFE with `localStorage.getItem('admin-theme')`, `className="admin-portal"` |
| `frontend/app/layout.tsx` | JetBrains Mono 600 + suppressHydrationWarning | ✓ VERIFIED | weight array `["400","500","600","700"]`; `suppressHydrationWarning` on `<html>` |
| `frontend/app/admin/lib/useOnlineStatus.ts` | Online status hook | ✓ VERIFIED | `"use client"` first line; named export `useOnlineStatus(): boolean`; navigator.onLine + window events |
| `frontend/app/admin/components/Icon.tsx` | Custom Icon replacing lucide-react | ✓ VERIFIED | 53 icon names in `IconName` union; ICONS record with SVG paths; aria contract (role="img" when aria-label provided, aria-hidden otherwise) |
| `frontend/app/admin/components/Btn.tsx` | 7 variants × 4 sizes primitive | ✓ VERIFIED | BtnVariant union with 7 values; SIZES.md.minHeight=44; data-variant, data-size, data-component="btn" |
| `frontend/app/admin/components/SeverityIndicator.tsx` | WCAG 1.4.1 triple-encoding | ✓ VERIFIED | BAR_HEIGHTS=[4,8,12]; bars aria-hidden; text labels "Low"/"Medium"/"High" |
| `frontend/app/admin/components/StatusBadge.tsx` | Rewritten with data-tone | ✓ VERIFIED | `data-tone={m.tone}`, `data-testid="status-badge"`, no Tailwind, default export preserved |
| All 14 other primitives (Pill, Card, SectionLabel, Input, Select, Avatar, ConfidencePill, PhotoTile, MapPin, Sparkbars, Kbd) | Named-export "use client" components | ✓ VERIFIED | All 14 exist at `frontend/app/admin/components/`; all `"use client"` first line; no Tailwind; CSS vars only |
| `frontend/app/admin/components/AdminSidebar.tsx` | Desktop sidebar + mobile tab bar + dark mode toggle | ✓ VERIFIED | `admin-theme` localStorage key; `classList.add/remove("dark")`; `aria-label="Toggle dark mode"`; `usePathname()`; `matchMedia` for responsive; no lucide-react |
| `frontend/app/admin/login/page.tsx` | Direction B aesthetic + SEC-06 strings | ✓ VERIFIED | `$ login`, `WLK.CONSOLE`, ASCII banner, `role="status" aria-live="polite"`; 3 exact SEC-06 strings as constants |
| `frontend/app/admin/page.tsx` | Dashboard with offline banner + responsive | ✓ VERIFIED | `useOnlineStatus()`, "You're offline right now" copy, `role="list" aria-label="Open reports by severity"`, `var(--text-hero-mobile)` + `var(--text-hero-desktop)`, `getCategoryLabel` |
| `frontend/app/admin/components/StatsCards.tsx` | Rewritten with CSS vars | ✓ VERIFIED | `data-testid="stats-cards"`, `data-testid="skeleton"`, `data-testid="stats-retry-button"`; `var(--surface)`; no Tailwind |
| `frontend/app/admin/components/ReportsTable.tsx` | Three-mode table | ✓ VERIFIED | `expandedRows`, `toggleExpand`, `getCategoryLabel`, imports StatusBadge+SeverityIndicator+PhotoTile, `<table>` element, `scope="col"` |
| `frontend/app/admin/reports/page.tsx` | Reports list page | ✓ VERIFIED | `useOnlineStatus`, "You're offline right now", "Could not load reports. Check your connection and try again.", `import ReportsTable` |
| `frontend/app/admin/reports/[id]/page.tsx` | Report detail page | ✓ VERIFIED | `STATUS_HISTORY` const, `getCategoryLabel`, `rel="noopener noreferrer"`, "This report no longer exists or was removed.", "Delete this report? This cannot be undone.", no `window.confirm()` |
| `frontend/app/admin/reports/map/page.tsx` | Map view | ✓ VERIFIED | `dynamic(` for Leaflet; SUBMITTED/REVIEW/RESOLVED legend labels; `useOnlineStatus`; MapPin-style via `L.divIcon` with hex (documented Leaflet constraint) |
| `frontend/app/admin/users/page.tsx` | Users page | ✓ VERIFIED | `import UserManagementTable`, `import CreateUserModal`, `useOnlineStatus` |
| `frontend/app/admin/components/UserManagementTable.tsx` | User table | ✓ VERIFIED | `opacity: user.is_active ? 1 : 0.55`, `aria-label="User actions for ${name}"`, deactivation copy with "They will lose access immediately." |
| `frontend/app/admin/components/CreateUserModal.tsx` | Create user modal | ✓ VERIFIED | `maxWidth: 480`, `"// MIN 12 · ARGON2ID · CHANGE_ON_FIRST_LOGIN"`, `rgba(10,10,10,0.5)` backdrop |
| `frontend/app/admin/organizations/page.tsx` | Organizations stub | ✓ VERIFIED | "ORGANIZATIONS" heading, `useOnlineStatus`, `"use client"` first line, no Tailwind |
| `frontend/app/admin/profile/page.tsx` | Profile page | ✓ VERIFIED | `admin-theme` localStorage key, `classList.add/remove("dark")`, "Password updated successfully." (satisfies grep for "Password updated."), `// MIN 12 · ARGON2ID`, imports Avatar |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `admin/layout.tsx` | `admin/admin.css` | `import './admin.css'` | ✓ WIRED | Confirmed: `import './admin.css';` line 1 of imports |
| `admin/layout.tsx` | `document.documentElement` | FOTWT blocking script | ✓ WIRED | `localStorage.getItem('admin-theme')` IIFE present |
| `app/layout.tsx` | `<html>` | `suppressHydrationWarning` | ✓ WIRED | Confirmed on `<html>` element |
| `AdminSidebar.tsx` | `Icon.tsx` | `import { Icon }` | ✓ WIRED | `import { Icon } from "./Icon"` |
| `AdminSidebar.tsx` | `document.documentElement` | dark mode toggle handler | ✓ WIRED | `classList.add("dark")` / `classList.remove("dark")` + localStorage write |
| `page.tsx` (dashboard) | `useOnlineStatus.ts` | `import { useOnlineStatus }` | ✓ WIRED | `import { useOnlineStatus } from "./lib/useOnlineStatus"` |
| `login/page.tsx` | `components/Btn.tsx` | `import { Btn }` | ✓ WIRED | Btn used for Authenticate CTA |
| `ReportsTable.tsx` | `StatusBadge.tsx` | `import StatusBadge` | ✓ WIRED | `import StatusBadge from "./StatusBadge"` |
| `ReportsTable.tsx` | `SeverityIndicator.tsx` | `import { SeverityIndicator }` | ✓ WIRED | Confirmed; used in CardStreamRow |
| `reports/page.tsx` | `ReportsTable.tsx` | table render | ✓ WIRED | `import ReportsTable from "../components/ReportsTable"` |
| `reports/[id]/page.tsx` | `translations.ts` | `getCategoryLabel` | ✓ WIRED | `import { getCategoryLabel } from "@/app/lib/translations"` |
| `reports/map/page.tsx` | `components/MapPin.tsx` | `import { MapPin }` | ✗ NOT_WIRED | Map page does NOT import the `MapPin` component. Instead uses `L.divIcon` with custom HTML and `getPinColor()` returning hex values. Plan 04 key_link specified this import but it was not implemented this way. MapPin.tsx exists as a primitive but is unused in the map page. |
| `users/page.tsx` | `UserManagementTable.tsx` | table render | ✓ WIRED | `import UserManagementTable from "../components/UserManagementTable"` |
| `users/page.tsx` | `CreateUserModal.tsx` | modal render | ✓ WIRED | `import CreateUserModal from "../components/CreateUserModal"` |
| `profile/page.tsx` | `document.documentElement` | dark mode toggle | ✓ WIRED | `classList.add/remove("dark")` + localStorage write |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `page.tsx` (dashboard) stats | `stats: AdminStats` | `getStats()` via adminApi | Yes — `adminApi.getStats()` calls `/api/admin/stats` | ✓ FLOWING |
| `page.tsx` (dashboard) activity feed | `STUB_ACTIVITY` | Module-level hardcoded constant | No — 4 hardcoded activity items | ⚠ STATIC (intentional stub per SUMMARY-02, D-13) |
| `page.tsx` (dashboard) sparkbars | `STUB_SPARKBARS` | Module-level hardcoded constant | No — 14 hardcoded values | ⚠ STATIC (intentional stub per SUMMARY-02, D-13) |
| `ReportsTable.tsx` | `reports` prop | reports/page.tsx → adminApi.getAdminReports() | Yes | ✓ FLOWING |
| `reports/[id]/page.tsx` | `report` state | adminApi.getAdminReport(id) | Yes | ✓ FLOWING |
| `reports/map/page.tsx` | `reports` state | adminApi.getAdminReports() | Yes | ✓ FLOWING |
| `StatsCards.tsx` | `stats` prop | dashboard page → getStats() | Yes | ✓ FLOWING |
| `UserManagementTable.tsx` | `users` prop | users/page → adminApi.getAdminUsers() | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Admin test suite (407 tests, 26 suites) | `npm test -- --testPathPattern="admin" --watchAll=false` | 407/407 passed, 0 failed | ✓ PASS |
| npm run build (TypeScript + Next.js compile) | `npm run build` | "Compiled successfully", exit 0, 13 routes | ✓ PASS |
| admin.css token scope | `grep -c ".admin-portal {" frontend/app/admin/admin.css` | 3 (light block, dark comment ref, reset block) | ✓ PASS |
| No lucide-react in admin subtree | `grep -rE "lucide-react" frontend/app/admin` (non-test) | 0 matches | ✓ PASS |
| No Tailwind in admin components | grep for `className="(bg-|text-|border-|hover:...)` across admin screens | 0 matches in all page and component files | ✓ PASS |
| FOTWT script present | grep for `localStorage.getItem.*admin-theme` in admin/layout.tsx | 1 match | ✓ PASS |

### Requirements Coverage

ADMIN-UI-01..08 are phase-local requirements defined in `02.5-UI-SPEC.md` — they are not present in `.planning/REQUIREMENTS.md`. All phase plans declare them in their frontmatter `requirements:` fields. Cross-reference by ID against implemented behavior:

| Requirement | Plans | Description (per UI-SPEC/PLAN context) | Status | Evidence |
|-------------|-------|----------------------------------------|--------|----------|
| ADMIN-UI-01 | 01,02,03,04 | Direction B token layer + .admin-portal CSS scope | ✓ SATISFIED | `admin.css` exists; all screens use CSS vars; layout applies `.admin-portal` class |
| ADMIN-UI-02 | 01 | 15 admin primitives library | ✓ SATISFIED | All 15 primitives present; no Tailwind; CSS vars throughout |
| ADMIN-UI-03 | 02,03,04 | Screen rewrites (Login, Dashboard, Reports, Map, Users, Orgs, Profile) | ✓ SATISFIED | All 8 screen routes rewritten with Direction B treatment |
| ADMIN-UI-04 | 02,03,04 | A-pattern card structure for report cards | ✓ SATISFIED | CardStreamRow: PhotoTile + category (getCategoryLabel) + StatusBadge + ward/time + duplicate badge |
| ADMIN-UI-05 | 01,02 | Light + dark mode via .dark class on html | ✓ SATISFIED | FOTWT script + dark mode toggle in sidebar + profile |
| ADMIN-UI-06 | 01,02,03,04 | Offline/error states | ⚠ PARTIAL | Offline banners with UI-SPEC copy present on all screens; "pending-changes card with queued actions" (ROADMAP SC#6) absent per plan decision D-13 |
| ADMIN-UI-07 | 01,02,03,04 | ≥44px tap targets on all interactive elements | ? NEEDS HUMAN | `Btn.tsx` md.minHeight=44; explicit minHeight 44 on action buttons in report detail; human measurement required for completeness |
| ADMIN-UI-08 | 04 | npm run build exits 0, zero TypeScript errors | ✓ SATISFIED | Build exits 0, "Compiled successfully" |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `frontend/app/admin/components/Btn.tsx` | 30 | `color: "#fff"` in danger variant | ⚠ WARNING | Minor SC#2 deviation; `#fff` is equivalent to `var(--on-accent)` / `var(--bg)` dark mode; does not adapt to dark mode token but white text on red is safe regardless |
| `frontend/app/admin/reports/[id]/page.tsx` | 236 | `color: "#fff"` on zoom overlay button | ⚠ WARNING | Minor SC#2 deviation; button overlays photo (dark bg always); functionally correct but not tokenized |
| `frontend/app/admin/reports/map/page.tsx` | 25-31 | Hex literals in `getPinColor()` | ℹ INFO | Intentional and documented — Leaflet's `L.divIcon` creates HTML strings outside React's render tree and cannot access CSS custom properties. Colors match status hues but are not the exact design tokens (e.g., `#6B7280` for submitted vs. the CSS `oklch(0.56 0.13 200)` teal). Visual discrepancy. |
| `frontend/app/admin/page.tsx` | 20-28 | `STUB_ACTIVITY` (hardcoded array) + `STUB_SPARKBARS` (hardcoded values) | ⚠ WARNING | Dashboard activity feed and 14-day sparkbars show placeholder data, not real API data. Documented intentional per SUMMARY-02 (deferred from Plan 03); Plan 03 SUMMARY does not wire these either. These stubs remain at phase end. |
| `frontend/app/admin/reports/map/page.tsx` | key_link | `MapPin` component not imported | ℹ INFO | Plan 04 specified `import { MapPin }` link from map page → MapPin component. Actual implementation uses `L.divIcon` HTML with inline styles instead. The visual result is equivalent (14px colored circle dot) but the MapPin component itself is unused in the map page. |

No `TBD`, `FIXME`, or `XXX` markers found in any admin file.

### Human Verification Required

#### 1. No Flash-of-Wrong-Theme on Dark Mode Reload

**Test:** Set `localStorage.admin-theme = 'dark'` in DevTools. Hard-reload `/admin/login`. Observe the first paint.
**Expected:** Page renders immediately in dark mode (dark background visible before React hydrates). No white flash.
**Why human:** FOTWT script execution timing and first-paint behavior cannot be verified from source code alone.

#### 2. Dashboard Mobile Layout (Visual)

**Test:** Open `/admin` in Chrome DevTools mobile viewport (375px). Verify hero card, severity composition, and activity feed layout.
**Expected:** Hero card has teal `var(--accent-bg)` background, large mono count. Severity list shows colored bars + text labels. 2×2 metric grid visible below.
**Why human:** CSS custom property rendering and responsive layout require browser.

#### 3. Report Card Stream Layout (Visual)

**Test:** Visit `/admin/reports` on mobile viewport. Confirm card stream view.
**Expected:** Each report card shows: PhotoTile (gradient placeholder if no src), category human-readable label, StatusBadge, ward·time meta line, duplicate badge (Pill warn or Pill neutral).
**Why human:** Visual card structure requires browser rendering.

#### 4. Map View Rendering

**Test:** Visit `/admin/reports/map`. Confirm colored markers and legend.
**Expected:** Pins are colored circles (gray/orange/green per status). Frosted legend at bottom-left with SUBMITTED/REVIEW/RESOLVED labels.
**Why human:** Leaflet map requires browser; SSR-off dynamic import cannot be tested statically.

#### 5. Dark Mode Toggle — End-to-End

**Test:** Log in. Click the moon/sun icon in the sidebar footer. Verify `html.classList` and `localStorage.admin-theme`.
**Expected:** `document.documentElement.classList.contains("dark")` toggles true/false. `localStorage.getItem("admin-theme")` returns "dark" or "light" accordingly. Full portal color scheme switches without reload.
**Why human:** DOM mutation and localStorage state require browser interaction.

#### 6. Tap Target Sizes — All Primary CTAs

**Test:** In DevTools mobile viewport, measure button heights: Authenticate (login), Start reviewing (dashboard), Move to review/Mark resolved (report detail), Save (profile), Update password (profile), CREATE USER (modal), Log out all sessions (profile).
**Expected:** All measure ≥ 44px height.
**Why human:** Tap target verification requires DevTools element inspection.

---

## Gaps Summary

### SC#6 Gap — Pending-Changes Card Missing (BLOCKER or Override Candidate)

**ROADMAP Success Criterion #6:** "The offline/error state shows a pending-changes card with queued action descriptions and a reassurance copy string that does not use technical jargon"

**What exists:** Offline banners with reassurance copy ("You're offline right now" + "Don't worry — everything you've changed has been saved on this device...") are present on all admin screens. This covers the "reassurance copy" half of SC#6.

**What is missing:** "pending-changes card with queued action descriptions" — a card listing the specific actions that are queued for sync (the UI-SPEC specifies `role="list"` on a queued-actions list with "Saved on this device" label). The plan explicitly chose not to implement this via design decisions D-13 (offline UI-states only) and D-14 (pendingCount always 0). There is no queue — the admin portal is read-mostly with minimal mutations, and the SUMMARY documents this as intentional.

**Assessment:** The plan scoped SC#6 down from "queued action descriptions" to "reassurance copy only." The reassurance copy is present and non-jargon. The queued-action list is absent because there is no queuing mechanism. This is a planned deviation, not an oversight.

**This looks intentional.** To accept this deviation, add to VERIFICATION.md frontmatter:

```yaml
overrides:
  - must_have: "Offline/error state shows pending-changes card with queued action descriptions"
    reason: "Admin portal is read-mostly with no offline mutation queue (D-13, D-14). Reassurance copy is present on all screens. Queued-action descriptions require a persistence layer that is out of scope for this phase."
    accepted_by: "<your-name>"
    accepted_at: "<ISO timestamp>"
```

### SC#2 Minor Deviation — Three Hardcoded Hex Values in Component Files

Three `#fff` / `#6B7280` / `#F59E0B` / `#22C55E` occurrences in component files:

1. `Btn.tsx` line 30 — `color: "#fff"` for danger variant (white text on red — always correct regardless of mode)
2. `reports/[id]/page.tsx` line 236 — `color: "#fff"` on zoom overlay button (overlays a photo — always dark bg)
3. `reports/map/page.tsx` lines 25-31 + 419 — hex colors in `getPinColor()` for Leaflet `L.divIcon` HTML, documented as intentional Leaflet API constraint

These are functional but deviate from the "no hardcoded hex in component files" requirement. The map page violation is technically justified (Leaflet API limitation). The two `#fff` values are minor. All are WARNING level — they do not prevent the portal from working correctly.

---

_Verified: 2026-05-22T10:14:12Z_
_Verifier: Claude (gsd-verifier)_
