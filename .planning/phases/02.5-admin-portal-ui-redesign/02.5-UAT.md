---
status: complete
phase: 02.5-admin-portal-ui-redesign
source: [02.5-01-SUMMARY.md, 02.5-02-SUMMARY.md, 02.5-03-SUMMARY.md, 02.5-04-SUMMARY.md, fix/phase-02.5-uat-polish]
started: 2026-05-23T00:00:00Z
updated: 2026-05-23T00:00:00Z
---

## Current Test

complete: true

## Tests

### 1. No-flash dark mode (FOTWT) on hard reload
expected: |
  In browser DevTools Console, run: localStorage.setItem('admin-theme', 'dark')
  Then hard-reload /admin/login (Cmd+Shift+R / Ctrl+Shift+R).
  The very first paint should be dark — no white background flash before dark styles apply.
  The teal Console palette is visible immediately on load.
result: pass

### 2. Dashboard mobile layout
expected: |
  Log in to /admin. Resize to 375px width (DevTools mobile viewport).
  Hero card should show a teal accent border + large JetBrains Mono count number.
  Below it: three severity rows, each showing colored bars (aria-hidden) alongside
  text labels "High" / "Medium" / "Low" with a color dot. Bars and text appear together
  (not just bars alone, not just text alone).
result: pass
note: Layout confirmed. Stats cards show real API data (Total: 2, Under Review: 0, Resolved: 0). Recent Activity feed and sparkbars show stub placeholder data — documented intentional limitation (STUB_ACTIVITY, STUB_SPARKBARS per plan decision D-13, logged as IN-03 in REVIEW.md).

### 3. Reports list card stream
expected: |
  Navigate to /admin/reports at 375px mobile viewport.
  Each report card must show all 5 elements:
  1. Photo tile (thumbnail or gradient placeholder if no photo)
  2. Human-readable category label (e.g. "Damaged Footpath", not "broken_footpath")
  3. Status badge (e.g. SUBMITTED / UNDER REVIEW / RESOLVED)
  4. Ward name + timestamp meta line (e.g. "Shivajinagar · 15 Apr 2026")
  5. Duplicate badge (orange Pill with count) when duplicates exist — no badge when none
result: fail
issue: BUG-T3-01
note: Elements 2–5 confirmed present. Element 1 (photo tile) shows gradient placeholder for all reports even though photos were uploaded. Root cause: admin API (/api/admin/reports) returns image_path (filename only) but frontend reads report.image_url which is undefined at runtime. ReportsTable.tsx:286 passes undefined to PhotoTile → placeholder renders. Fix: admin query must include image_url, or frontend must construct it from image_path. Confirmed bug — specific file/line, reproducible.

### 4. Admin map view — colored pins + legend
expected: |
  Navigate to /admin/reports/map.
  Map loads with colored circle markers:
  - Gray circles for Submitted reports
  - Orange circles for Under Review
  - Green circles for Resolved
  Frosted-glass legend at bottom-left shows three labeled entries:
  SUBMITTED (gray dot) / REVIEW (orange dot) / RESOLVED (green dot)
result: pass
note: Map loads, two pins visible, frosted legend at bottom-left with three colored dots. Status filter bar shows SUBMITTED/REVIEW/RESOLVED with colored dots. Submitted color is teal (not gray) — intentional redesign decision, not a bug.

### 5. Dark mode toggle — DOM + localStorage
expected: |
  While logged in to any admin page, click the moon/sun icon in the sidebar footer.
  Check: document.documentElement.classList.contains("dark") → true
  Check: localStorage.getItem("admin-theme") → "dark"
  The full portal switches to dark palette without a full page reload.
  Clicking the toggle again reverses both: classList loses "dark", localStorage → "light".
result: pass
note: Toggle confirmed working on desktop sidebar. classList.contains("dark") → true, localStorage.getItem("admin-theme") → "dark". Visual palette switches without page reload. Gap: toggle is inaccessible on mobile (375px) — mobile tab bar has no theme control. Separate gap: icon-192.png returns 404 (missing PWA manifest icon, unrelated to dark mode).

### 6. Tap targets ≥ 44px — primary CTAs
expected: |
  In DevTools mobile viewport, use the element inspector to measure button heights.
  All of these must be ≥ 44px:
  - /admin/login → "Authenticate" button
  - /admin → "Start reviewing" button
  - /admin/reports/[id] → "Move to review" + "Mark resolved" buttons
  - /admin/profile → "Save" + "Update password" + "Log out all sessions" buttons
  - /admin/users → "CREATE USER" button (inside the create modal)
result: partial
note: |
  Measured buttons all pass ≥44px:
  - Authenticate: 335×48 ✓
  - Start reviewing: 277×48 ✓
  - Save: 61×44 ✓
  - Change Password: 150×44 ✓
  - CREATE USER: 147×44 ✓
  Two buttons could not be measured due to bugs:
  - BUG-T6-01: "Move to review"/"Mark resolved" unreachable — report cards have no link/navigation to /admin/reports/[id]; detail page exists but is orphaned from the queue UI
  - BUG-T6-02: "Log out all sessions" inaccessible on mobile — fixed bottom tab bar covers bottom page content; button exists in code (profile/page.tsx:577) but cannot be scrolled to
  Additional UI issues found during this test:
  - BUG-T6-03: Profile page shows raw code-style labels as visible UI text: section header "CHANGE_PASSWORD", field labels "SESSION_EXP"/"LAST_LOGIN", hint "// MIN 12 · ARGON2ID"
  - BUG-T6-04: Users page has large gray blank area below user row — broken modal or unrendered component

### 7. "Start reviewing" button navigates to /admin/reports (F-01)
expected: |
  On /admin (dashboard), there is a "Start reviewing" button visible.
  Clicking it navigates to /admin/reports.
  (Previously this button was a dead placeholder.)
result: pass

### 8. Ward name truncated in compact row — no overflow (F-02)
expected: |
  In /admin/reports compact row view (if available), reports with long ward names
  (e.g. "Sagayapura") should show a truncated label with ellipsis, not overflow
  outside the column boundaries or wrap to a second line.
result: pass

### 9. Report counter singular — "1 report" not "1 reports" (COPY-01)
expected: |
  When the public home page shows exactly 1 submitted report, the counter reads
  "1 report" (singular). When there are 2 or more, it reads "2 reports" (plural).
  (This is on the citizen-facing home page at /, not the admin panel.)
result: pass
note: Confirmed both states in one session — started with 2 reports ("2 reports"), deleted one via admin portal, counter updated to "1 report". Singular and plural both correct.

### 10. NO_DUPES badge removed — zero-dupe reports show no badge (F-04)
expected: |
  In /admin/reports, expand a report that has zero duplicates (duplicate_count = 0).
  No "NO_DUPES" or "0 dupes" badge should appear anywhere on that row.
  The duplicate badge only shows when duplicate_count > 0.
result: pass

### 11. Online sync indicator is green (F-05)
expected: |
  In the admin sidebar (any admin page), there is an online/connectivity indicator.
  When connected to the internet, the indicator dot is GREEN.
  (Previously it was reported as white/gray.)
result: pass
note: Green dot confirmed next to "Synced just now" text on dashboard.

### 12. Danger variant text readable in dark mode — SC#2 token fix
expected: |
  In dark mode (html.dark class active), navigate to a report detail page
  and trigger the delete confirmation (or inspect the "Delete" button).
  The danger button text color should be clearly readable (white or light)
  against the red/danger background — using the --on-danger CSS token, not
  hardcoded #fff. In light mode it should also render correctly.
result: pass
note: Delete button renders as outline/ghost danger variant — red text on white-red tinted background with red border. Clearly readable in dark mode. SC#2 token fix confirmed working.

## Summary

total: 12
passed: 10
issues: 5
pending: 0
skipped: 0

## Gaps

### BUG-T3-01 — Photo tiles blank in reports queue (confirmed-bug)
Admin API returns `image_path` (filename only) but frontend reads `report.image_url` (undefined at runtime). PhotoTile shows gradient placeholder for all reports even when photos exist. Fix: add `image_url` to admin query response, or construct it from `image_path` in the frontend.

### ~~BUG-T6-01 — Report detail page unreachable from queue~~ RESOLVED
`frontend/app/admin/reports/[id]/page.tsx` now exists — implemented in Phase 03.1 (admin report detail split layout). Report cards link to the detail page; "Move to review" and "Mark resolved" are accessible.

### BUG-T6-02 — "Log out all sessions" hidden behind mobile nav bar (confirmed-bug)
Fixed bottom tab bar on mobile overlays bottom page content on `/admin/profile`. The SECURITY card with "Log out all sessions" button is in the DOM (profile/page.tsx:577) but cannot be scrolled to on mobile.

### ~~BUG-T6-03 — Profile page exposes raw code-style labels~~ RESOLVED
Verified stale: `grep` for CHANGE_PASSWORD, SESSION_EXP, LAST_LOGIN in `profile/page.tsx` returns 0 matches — raw labels are absent from the current profile page UI.

### ~~BUG-T6-04 — Users page has large gray blank area~~ RESOLVED
`CreateUserModal` now has `if (!isOpen) return null` guard at line 35 — modal renders nothing when closed, eliminating any phantom overlay below the user row.

### GAP-T5-01 — Dark mode toggle inaccessible on mobile (design gap)
Theme toggle is only in the desktop sidebar footer. Mobile users have no way to switch themes from the UI. Not blocking but a usability gap.

### ~~GAP-T12-01 — Missing PWA manifest icon~~ RESOLVED
`frontend/public/icon-192.png` exists — file confirmed present, 404 no longer applies.
