---
phase: 02.5-admin-portal-ui-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 02.5-01
files_modified:
  - frontend/app/admin/components/AdminSidebar.tsx
  - frontend/app/admin/components/StatsCards.tsx
  - frontend/app/admin/login/page.tsx
  - frontend/app/admin/page.tsx
  - frontend/app/admin/components/__tests__/StatsCards.test.tsx
autonomous: true
requirements:
  - ADMIN-UI-03
  - ADMIN-UI-04
  - ADMIN-UI-05
  - ADMIN-UI-06
decisions_implemented:
  - D-06
  - D-07
  - D-09
  - D-11
  - D-12

must_haves:
  truths:
    - "Login screen renders the Direction B terminal aesthetic with `$ login` headline, ASCII banner, security hint, and Authenticate CTA — generic error messages per SEC-06 preserved"
    - "Mobile dashboard shows hero open-reports card (teal border, large mono count) + 2×2 metric grid + severity composition list + recent activity feed"
    - "Desktop dashboard shows hero metrics row (inverted ink card + 3 metric cards) + 14-day sparkbars + two-column bottom split"
    - "AdminSidebar renders a desktop sidebar (220px fixed left) AND a mobile bottom tab bar (4 tabs: OPS, QUEUE, MAP, USERS), using the new Icon component (no lucide-react import)"
    - "Sidebar footer includes a dark mode toggle button that reads/writes `localStorage.admin-theme` and toggles `.dark` on `<html>`"
    - "All interactive elements on the rewritten dashboard, login, and sidebar are ≥ 44px tap target"
    - "StatsCards renders using the rewritten StatusBadge data contract + Pill/Card primitives with no Tailwind class strings remaining"
  artifacts:
    - path: "frontend/app/admin/components/AdminSidebar.tsx"
      provides: "Desktop sidebar + mobile bottom tab bar + dark mode toggle"
      contains: "admin-theme"
    - path: "frontend/app/admin/components/StatsCards.tsx"
      provides: "Dashboard metric cards (mobile 2×2 + desktop hero layout)"
      contains: "data-testid=\"stats-cards\""
    - path: "frontend/app/admin/login/page.tsx"
      provides: "Direction B login screen with generic SEC-06 error copy"
      contains: "Incorrect email or password"
    - path: "frontend/app/admin/page.tsx"
      provides: "Dashboard page with hero card, severity composition, activity feed"
      contains: "useOnlineStatus"
  key_links:
    - from: "frontend/app/admin/components/AdminSidebar.tsx"
      to: "frontend/app/admin/components/Icon.tsx"
      via: "import { Icon }"
      pattern: "from\\s+[\"']\\./Icon[\"']"
    - from: "frontend/app/admin/components/AdminSidebar.tsx"
      to: "document.documentElement"
      via: "dark mode toggle handler"
      pattern: "classList\\.(toggle|add|remove)\\(['\"]dark['\"]\\)"
    - from: "frontend/app/admin/page.tsx"
      to: "frontend/app/admin/lib/useOnlineStatus.ts"
      via: "import { useOnlineStatus }"
      pattern: "useOnlineStatus"
    - from: "frontend/app/admin/login/page.tsx"
      to: "frontend/app/admin/components/Btn.tsx"
      via: "import { Btn }"
      pattern: "from\\s+[\"'].*components/Btn[\"']"
---

<objective>
Rewrite the AdminSidebar (desktop + mobile bottom tab bar + dark mode toggle), the Login screen, the Dashboard (mobile + desktop), and the StatsCards component using the Plan 01 token layer and primitives.

Purpose: Login is the user's first contact with the admin portal — the FOTWT script must visibly work here, and the SEC-06 error contract must be preserved. The dashboard is the most-visited admin surface; the hero card, severity composition, and metric grid set the visual tone for the rest of the rewrite. The AdminSidebar dark mode toggle (D-12) is the only practical way to verify dark mode end-to-end in this phase.

Output:
- `frontend/app/admin/components/AdminSidebar.tsx` — full rewrite with Icon (no lucide-react), CSS variables, mobile bottom tab bar, dark mode toggle in sidebar footer
- `frontend/app/admin/login/page.tsx` — Direction B terminal aesthetic, generic error copy, Authenticate CTA
- `frontend/app/admin/page.tsx` — Dashboard with responsive mobile/desktop layout
- `frontend/app/admin/components/StatsCards.tsx` — metric cards using Card + Pill primitives + CSS vars
- `frontend/app/admin/components/__tests__/StatsCards.test.tsx` updated to drop Tailwind class assertions and keep `data-testid` assertions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-CONTEXT.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-RESEARCH.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-01-SUMMARY.md

# Design source of truth
@design-ref/admin-portal/screens-b-mobile.jsx
@design-ref/admin-portal/screens-b-desktop.jsx
@design-ref/admin-portal/primitives.jsx

# Files being modified (current state)
@frontend/app/admin/components/AdminSidebar.tsx
@frontend/app/admin/components/StatsCards.tsx
@frontend/app/admin/login/page.tsx
@frontend/app/admin/page.tsx
@frontend/app/admin/components/__tests__/StatsCards.test.tsx

# Plan 01 outputs (primitives + hook + tokens)
@frontend/app/admin/admin.css
@frontend/app/admin/lib/useOnlineStatus.ts
@frontend/app/admin/components/Icon.tsx
@frontend/app/admin/components/Btn.tsx
@frontend/app/admin/components/Pill.tsx
@frontend/app/admin/components/Card.tsx
@frontend/app/admin/components/SectionLabel.tsx
@frontend/app/admin/components/SeverityIndicator.tsx
@frontend/app/admin/components/Input.tsx
@frontend/app/admin/components/Sparkbars.tsx
@frontend/app/admin/components/StatusBadge.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite AdminSidebar (desktop sidebar + mobile bottom tab bar + dark mode toggle) and Login page</name>
  <files>
    frontend/app/admin/components/AdminSidebar.tsx,
    frontend/app/admin/login/page.tsx
  </files>
  <read_first>
    frontend/app/admin/components/AdminSidebar.tsx,
    frontend/app/admin/login/page.tsx,
    design-ref/admin-portal/screens-b-mobile.jsx,
    design-ref/admin-portal/screens-b-desktop.jsx,
    design-ref/admin-portal/primitives.jsx,
    frontend/app/admin/components/Icon.tsx,
    frontend/app/admin/components/Btn.tsx,
    frontend/app/admin/components/Input.tsx,
    frontend/app/admin/components/SectionLabel.tsx,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
  </read_first>
  <behavior>
    - AdminSidebar.tsx: `"use client"` first line. Keep `interface AdminSidebarProps { role: string }` and `useState` for mobile drawer (rename or repurpose as needed for bottom tab bar visibility — the previous slide-in drawer pattern is replaced by always-visible bottom tab bar on mobile per UI-SPEC Navigation section). Keep all nav `href` values identical: `/admin`, `/admin/reports`, `/admin/reports/map`, `/admin/profile`, `/admin/users`, plus add `/admin/organizations` (desktop-only nav item per UI-SPEC, see note below — link target exists in Plan 04).
    - Replace `import { Menu, X } from "lucide-react"` with `import { Icon } from "./Icon"`. Replace every lucide-react JSX with `<Icon name="..." size={N} aria-hidden />`.
    - Render two distinct structures inside one component:
      a) **Desktop sidebar (≥768px viewport)**: 220px fixed left. Brand mark area at top (28×28 ink-tile "W" + `WLK.CONSOLE` + `BENGALURU · v2.4.1` label). 7 nav items in order: OPS (activity icon), QUEUE (inbox icon), MAP (map icon), USERS (users icon, role-gated to admin/super_admin same as current), ORGS (org icon, role-gated and desktop-only per UI-SPEC), spacer, QUICK CMD ⌘K (Kbd primitive — UI hint only, not interactive). User panel pinned at sidebar bottom with Avatar + name + role + logout button + DARK MODE TOGGLE button.
      b) **Mobile bottom tab bar (<768px viewport)**: 4 tabs (OPS, QUEUE, MAP, USERS — Organizations omitted on mobile per UI-SPEC), `position: fixed; bottom: 0; left: 0; right: 0`. Active tab uses `accent-bg` + `accent-ink` + `1px solid accent-border`. Padding: 8px top, 14px bottom (safe area). Tab labels JetBrains Mono 10px weight 600 letter-spacing 0.06em.
    - Active state detection: read `usePathname()` from `next/navigation` to determine which nav item matches the current route — exact match for `/admin`, prefix match for `/admin/reports`, `/admin/users`, etc.
    - Dark mode toggle (D-12): a button in the sidebar footer with `aria-label="Toggle dark mode"`. Clicking: if `document.documentElement.classList.contains("dark")`, remove `.dark` + write `localStorage.setItem("admin-theme", "light")`; else add `.dark` + write `localStorage.setItem("admin-theme", "dark")`. Icon swap: sun when in dark mode, moon when in light mode. Read initial state from `document.documentElement.classList.contains("dark")` inside a `useEffect` to avoid hydration mismatch.
    - All nav items and buttons have `min-height: 44px` (tap target). Sidebar uses `background: var(--surface)`, `border-right: 1px solid var(--border)`. Bottom tab bar uses `background: var(--surface)`, `border-top: 1px solid var(--border)`.
    - Responsive switching: use a media query approach acceptable in CSS-in-JS — render BOTH structures and gate visibility via inline `style={{ display: ... }}` driven by a `useEffect` + `window.matchMedia('(min-width: 768px)')` listener. Initial render before useEffect: render both (desktop visible by default for SSR consistency).
    - Default export preserved: `export default function AdminSidebar`.
    - Zero Tailwind class names anywhere in the file (grep gate).
    - Zero `lucide-react` imports.

    - login/page.tsx: `"use client"` first line. Preserve every piece of existing logic: fetch POST to `/api/admin/auth/login`, error handling for 401/429/5xx with the EXACT generic error strings from UI-SPEC Copywriting Contract (SEC-06 lock):
      - 401 → "Incorrect email or password."
      - 429 → "Too many attempts. Please wait a few minutes before trying again."
      - 5xx → "Something went wrong. Please try again."
    - Replace existing layout with Direction B terminal aesthetic per UI-SPEC "Login" section:
      - Outer: full viewport min-height, `background: var(--bg)`, centered single-column layout
      - Brand: 36×36 ink-tile "W" + `WLK.CONSOLE` (10px mono 600) + `BENGALURU · v2.4.1` (10px mono muted)
      - ASCII banner box (decorative `<pre aria-hidden="true">`, mono 10px, `color: var(--muted)`) — content is a small terminal-style banner from screens-b-mobile.jsx login screen reference (executor picks a 4-6 line ASCII art consistent with the prototype)
      - Headline: `$ login` — 14px JetBrains Mono weight 600 letter-spacing -0.01em
      - Sub-headline: `// authenticate to the triage queue` — 14px JetBrains Mono weight 400 `color: var(--muted)`
      - Two Input primitives (from Plan 01) for USER_EMAIL and PASSWORD with 44px height, `border-radius: var(--r-xs)`. Use `icon="mail"` for email input, `icon="lock"` for password input.
      - Authenticate Btn: `variant="accent"`, `size="lg"`, full width, mono font label "AUTHENTICATE", min-height 48px
      - Security hint row: Icon name="shield" + text "ARGON2ID · 24H_SESSION · IP_RATELIMITED" (10px mono `var(--muted)`)
      - Footer: build hash · date / STATUS: OK (10px mono — use placeholder strings; build hash sourcing is out of scope)
    - Error display: render below the password input in a `role="status" aria-live="polite"` region using `color: var(--danger-ink)`, `background: var(--danger-bg)`, `border: 1px solid var(--danger-border)`, `padding: 8px 12px`, `border-radius: var(--r-sm)`. Never display the raw server message — always one of the three generic strings.
    - Form submit: keep the existing fetch + `router.push` flow; only the visual layer changes.
    - Zero Tailwind classes. Zero lucide-react. Default export preserved.
  </behavior>
  <action>
    Rewrite both files in-place. Read the current AdminSidebar (preserved structure: `"use client"`, `useState`, role-gated Users link, all hrefs) and the current login page (preserved logic: fetch, error mapping, router.push) — keep their preserved blocks per PATTERNS.md lines 567-631 and login screen UI-SPEC section.

    AdminSidebar: Replace lucide-react with Icon. Build the desktop sidebar (220px, 7 nav items with role-gates: USERS and ORGS visible only when role is admin/super_admin matching existing pattern) and the mobile bottom tab bar (4 tabs always visible regardless of role for mobile — Users tab is hidden when role does not permit, matching existing Users link role-gate). Wire dark mode toggle per D-10/D-12: read initial state from `document.documentElement.classList.contains("dark")` in useEffect (avoids SSR hydration), toggle via `classList.toggle("dark")` + `localStorage.setItem("admin-theme", ...)`. Use Icon `sun` / `moon` for toggle state. Add `aria-label="Toggle dark mode"` to the button. All interactive elements get `min-height: 44px`. Use Avatar primitive for the user panel. Render Kbd primitive for the QUICK CMD ⌘K hint. Detect active route via `usePathname()`.

    For responsive switching: render both desktop sidebar and mobile bottom tab bar in the same component output, with `display` driven by a `useState`+`useEffect`+`matchMedia` pattern. Default to desktop visible on SSR. After mount, evaluate `matchMedia("(min-width: 768px)").matches` and toggle display. Subscribe to the media-query change event for runtime resize.

    login/page.tsx: Preserve fetch logic, error mapping, and redirect entirely from the current file. Replace the visual layer per UI-SPEC Login section (D-09: no Tailwind). Use the Plan 01 Input primitive (with icon="mail" / icon="lock"), Btn primitive (variant="accent", size="lg"), and Icon primitive ("shield" for security hint). Error display in a `role="status" aria-live="polite"` region using only the three SEC-06 generic strings — never the raw server response text. Generic message mapping by HTTP status: 401 → "Incorrect email or password.", 429 → "Too many attempts. Please wait a few minutes before trying again.", 5xx → "Something went wrong. Please try again." Network/fetch failure falls back to the 5xx generic string.

    Do not modify any other file. Do not touch tests in this task (StatsCards.test.tsx updates happen in Task 2). Do not delete lucide-react from package.json (Plan 04 cleanup).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern="login-page" --watchAll=false && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/admin/components/AdminSidebar.tsx` does NOT contain `lucide-react` (grep `grep "lucide-react" frontend/app/admin/components/AdminSidebar.tsx` returns no matches)
    - File `frontend/app/admin/components/AdminSidebar.tsx` contains `from "./Icon"` or `from './Icon'`
    - File `frontend/app/admin/components/AdminSidebar.tsx` contains `admin-theme` (localStorage key for dark mode toggle)
    - File `frontend/app/admin/components/AdminSidebar.tsx` contains `classList.toggle("dark")` OR (`classList.add("dark")` AND `classList.remove("dark")`)
    - File `frontend/app/admin/components/AdminSidebar.tsx` contains `aria-label="Toggle dark mode"` (or single-quoted equivalent)
    - File `frontend/app/admin/components/AdminSidebar.tsx` contains `usePathname` import
    - File `frontend/app/admin/components/AdminSidebar.tsx` contains `matchMedia` reference (for responsive switching)
    - File `frontend/app/admin/components/AdminSidebar.tsx` does NOT contain Tailwind class names (grep `grep -nE 'className="(bg-|text-|border-|hover:|md:|p-[0-9]|rounded-)' frontend/app/admin/components/AdminSidebar.tsx` returns no matches)
    - File `frontend/app/admin/login/page.tsx` contains the EXACT string `Incorrect email or password.`
    - File `frontend/app/admin/login/page.tsx` contains the EXACT string `Too many attempts. Please wait a few minutes before trying again.`
    - File `frontend/app/admin/login/page.tsx` contains the EXACT string `Something went wrong. Please try again.`
    - File `frontend/app/admin/login/page.tsx` contains `role="status"` and `aria-live="polite"` (or single-quoted equivalents)
    - File `frontend/app/admin/login/page.tsx` does NOT contain `lucide-react`
    - File `frontend/app/admin/login/page.tsx` does NOT contain Tailwind class names (grep `grep -nE 'className="(bg-|text-|border-|hover:|md:|p-[0-9]|rounded-)' frontend/app/admin/login/page.tsx` returns no matches)
    - File `frontend/app/admin/login/page.tsx` imports `Btn` from the admin components directory (grep `grep -E "from\s+[\"'].*admin/components/Btn[\"']" frontend/app/admin/login/page.tsx` returns at least one match)
    - `cd frontend && npm test -- --testPathPattern="login-page" --watchAll=false` exits 0 (existing login tests pass against new copy; if any existing test asserts a specific error string verify it matches the SEC-06 contract above)
  </acceptance_criteria>
  <done>
    AdminSidebar rendered with desktop sidebar + mobile bottom tab bar (no slide-in drawer needed), dark mode toggle wired to localStorage `admin-theme`, no lucide-react. Login page uses Direction B terminal aesthetic with SEC-06-compliant generic errors only. Both files Tailwind-free. Existing login-page tests pass (or test copy updated alongside).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite Dashboard page + StatsCards (mobile 2×2 + desktop hero) with offline indicator</name>
  <files>
    frontend/app/admin/page.tsx,
    frontend/app/admin/components/StatsCards.tsx,
    frontend/app/admin/components/__tests__/StatsCards.test.tsx
  </files>
  <read_first>
    frontend/app/admin/page.tsx,
    frontend/app/admin/components/StatsCards.tsx,
    frontend/app/admin/components/__tests__/StatsCards.test.tsx,
    design-ref/admin-portal/screens-b-mobile.jsx,
    design-ref/admin-portal/screens-b-desktop.jsx,
    frontend/app/admin/lib/useOnlineStatus.ts,
    frontend/app/admin/components/Card.tsx,
    frontend/app/admin/components/Pill.tsx,
    frontend/app/admin/components/Btn.tsx,
    frontend/app/admin/components/SectionLabel.tsx,
    frontend/app/admin/components/SeverityIndicator.tsx,
    frontend/app/admin/components/Sparkbars.tsx,
    frontend/app/admin/components/StatusBadge.tsx,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
  </read_first>
  <behavior>
    - StatsCards.tsx: Preserve every existing prop interface shape, every `data-testid` (`stats-cards`, `stats-cards-loading`, `stats-cards-error`, `stat-total`, `stat-submitted`, `stat-under-review`, `stat-resolved`, `skeleton`, `stats-retry-button`), the dual `data`/`loading` and `stats`/`isLoading`/`isError`/`onRetry` prop signature, and the loading + error + onRetry callback wiring per PATTERNS.md "preserved from current file" list.
    - Replace Tailwind class strings with inline `style={{...}}` consuming CSS vars. Use `Card` primitive (from Plan 01) for each metric card. Use Pill primitive for status counts where the design shows pill-style displays.
    - Loading skeletons: data-testid="skeleton" preserved; use `background: var(--surface)`, `border: 1px solid var(--border)`, `borderRadius: var(--r-lg)`, `animation: pulse 1.5s ease-in-out infinite`.
    - Error state: `Btn variant="ghost"` with the `data-testid="stats-retry-button"` preserved + Pill `tone="danger"` displaying the error message.
    - Grid layout: `display: grid; gridTemplateColumns: repeat(auto-fill, minmax(180px, 1fr)); gap: 16px` (CSS-native responsive — no Tailwind media queries).
    - Each card renders: a SectionLabel for the metric name (e.g. "TOTAL REPORTS"), a large mono count (14px Inter weight 600 for non-hero per UI-SPEC Typography), optional delta line. The hero "Open reports" count uses `fontSize: var(--text-hero-mobile)` on mobile (default) — desktop hero count override goes in page.tsx via Card customization, not StatsCards.
    - Default export preserved.

    - page.tsx (Dashboard): `"use client"` first line. Preserve every existing prop pattern (useState + useCallback + useEffect + adminApi.getStats() fetch + error/retry handling) per PATTERNS.md "preserved in all page rewrites" list.
    - Import and call `useOnlineStatus()` (Plan 01 hook). When `!isOnline`, render an offline banner at the top of the page in a `role="status" aria-live="polite"` region using `background: var(--warn-bg)`, `color: var(--warn-ink)`, `border: 1px solid var(--warn-border)`, `padding: 10px 14px`, `border-radius: var(--r-md)`, with the UI-SPEC offline state heading `"You're offline right now"` and body `"Don't worry — everything you've changed has been saved on this device. We'll send it through automatically as soon as you're back online."` (D-13).
    - Mobile layout (default, single-column):
      1. Sync state bar: live pulse dot + `Synced just now` (JetBrains Mono 10px muted), `role="status" aria-live="polite"`
      2. Hero open-reports Card: teal `border: 1px solid var(--accent-border)`, `background: var(--accent-bg)`, large mono count using `fontSize: var(--text-hero-mobile)` (56px), label "OPEN REPORTS", delta line ("+N today"), "Start reviewing" Btn (variant="accent" size="lg")
      3. 2×2 metric grid: 4 metric Cards (Under review / Resolved this week / Total reports / Duplicates merged) — use StatsCards component output, customized for the 2x2 mobile layout via `gridTemplateColumns: "repeat(2, 1fr)"` wrapper
      4. Severity composition Card: `role="list" aria-label="Open reports by severity"`. Three rows (High / Medium / Low) each with SeverityIndicator + thin proportion bar + count + percentage. Each row is `role="listitem"`.
      5. Recent activity feed Card: SectionLabel "RECENT ACTIVITY". List of items (mono timestamp + action word "New" / "Under review" + category label from `getCategoryLabel()` from `app/lib/translations.ts`). Render from existing /api/admin/reports data or stub empty state if no data wired.
    - Desktop layout (≥1024px viewport — use matchMedia + useState pattern same as AdminSidebar):
      1. Hero metrics row: 4-column grid. First Card is the "inverted ink" hero (background: var(--ink), color: var(--bg)) with `fontSize: var(--text-hero-desktop)` (84px) count. Three smaller metric Cards beside it.
      2. 14-day intake timeseries: SectionLabel "INTAKE · 14_DAY", Sparkbars component full-width, period toggle chips (7D / 14D / 30D — render as Pill tone="outline" size="sm" buttons)
      3. Two-column bottom: Severity distribution Card | Activity tail Card
    - Page wrapper: `<div style={{ padding: "24px 32px", maxWidth: 1200, marginLeft: "auto", marginRight: "auto" }}>`. Wrap entire return so it sits to the right of the desktop sidebar (handled by layout.tsx flex container from Plan 01).
    - Heading: `<h1 style={{ fontFamily: "var(--font-mono)", fontSize: 14, fontWeight: 600, color: "var(--ink)", marginBottom: 24, textTransform: "uppercase" }}>DASHBOARD</h1>` per UI-SPEC.
    - Default export preserved.
    - Zero Tailwind classes. Zero lucide-react.

    - StatsCards.test.tsx UPDATE: Remove every assertion that checks Tailwind class strings on the skeleton or card containers (e.g. `expect(...).toHaveClass("animate-pulse")` or `classesContaining(...)`). Keep all `data-testid` lookups and value assertions. If a removed assertion leaves a test empty, replace with `expect(skeleton.getAttribute("data-testid")).toBe("skeleton")` as a minimal still-meaningful assertion.
  </behavior>
  <action>
    Rewrite StatsCards and the dashboard page in-place. Update StatsCards tests to drop Tailwind class assertions.

    StatsCards: Mirror the analog (current file) preserving every interface, data-testid, loading/error/onRetry pattern. Replace all Tailwind class strings with inline `style={{...}}` using CSS vars. Use the Card primitive from Plan 01 as the outer wrapper for each metric. Grid: `gridTemplateColumns: "repeat(auto-fill, minmax(180px, 1fr))"`. Loading skeletons keep `data-testid="skeleton"` and use surface-2 backgrounds with the existing pulse animation (define inline keyframes if needed via a CSS animation property — `animation: "pulse 1.5s ease-in-out infinite"` and rely on the existing `@keyframes pulse` already defined in globals.css or add to admin.css as part of Plan 01's utility section if missing — verify and add if absent).

    page.tsx (Dashboard): Preserve every fetch, error-handling, and useCallback pattern from the current file (PATTERNS.md "preserved in all page rewrites"). Import `useOnlineStatus` from `../lib/useOnlineStatus`. Add an offline banner with the exact UI-SPEC offline state heading and body strings, role="status" aria-live="polite", warn-tone styling. Implement mobile layout (single column with hero card, 2x2 metrics, severity composition, activity feed) AND desktop layout (4-col hero row, 14-day sparkbars, two-col bottom) gated by `matchMedia("(min-width: 1024px)")` + useEffect + useState. SSR-initial defaults to mobile layout to avoid hydration mismatches (or render both with display gating like AdminSidebar — executor picks the cleaner approach but must avoid hydration warnings).

    Use Plan 01 primitives throughout: Card, Pill, Btn, SectionLabel, SeverityIndicator, Sparkbars, Icon. Use `getCategoryLabel()` from `app/lib/translations.ts` per UI-SPEC Language Boundary (do not invent new category labels).

    StatsCards.test.tsx update: remove every Tailwind-class assertion (toHaveClass with bg-/text-/animate-/rounded-/p- patterns). Preserve all data-testid lookups and numeric/text value assertions. Replace empty tests (if any) with minimal `data-testid` presence assertions.

    Do not touch ReportsTable, report detail, or any page outside dashboard. Do not modify package.json. Do not remove lucide-react yet.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern="(StatsCards|dashboard|login-page)" --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/admin/components/StatsCards.tsx` does NOT contain Tailwind class names (grep `grep -nE 'className="(bg-|text-|border-|hover:|md:|p-[0-9]|rounded-|animate-)' frontend/app/admin/components/StatsCards.tsx` returns no matches)
    - File `frontend/app/admin/components/StatsCards.tsx` contains `data-testid="stats-cards"` and `data-testid="skeleton"` and `data-testid="stats-retry-button"` (each present)
    - File `frontend/app/admin/components/StatsCards.tsx` contains `var(--surface)` (grep `grep "var(--surface" frontend/app/admin/components/StatsCards.tsx` returns at least one match)
    - File `frontend/app/admin/page.tsx` contains `useOnlineStatus`
    - File `frontend/app/admin/page.tsx` contains the EXACT string `You're offline right now`
    - File `frontend/app/admin/page.tsx` contains the EXACT string `Don't worry — everything you've changed has been saved on this device. We'll send it through automatically as soon as you're back online.`
    - File `frontend/app/admin/page.tsx` contains `role="list"` and `aria-label="Open reports by severity"` (or single-quoted equivalents) — severity composition accessibility per UI-SPEC item #6
    - File `frontend/app/admin/page.tsx` contains `var(--text-hero-mobile)` or `var(--text-hero-desktop)` (at least one hero count reference)
    - File `frontend/app/admin/page.tsx` contains `getCategoryLabel` import (or equivalent translations.ts reference)
    - File `frontend/app/admin/page.tsx` does NOT contain Tailwind class names (grep `grep -nE 'className="(bg-|text-|border-|hover:|md:|p-[0-9]|rounded-)' frontend/app/admin/page.tsx` returns no matches)
    - File `frontend/app/admin/components/__tests__/StatsCards.test.tsx` does NOT contain any of: `toHaveClass("bg-`, `toHaveClass("text-`, `toHaveClass("animate-`, `classesContaining(` (grep returns 0 for each pattern)
    - File `frontend/app/admin/components/__tests__/StatsCards.test.tsx` retains `data-testid` assertions (grep `grep -c "data-testid" frontend/app/admin/components/__tests__/StatsCards.test.tsx` returns ≥ 5)
    - `cd frontend && npm test -- --testPathPattern="(StatsCards|dashboard|login-page)" --watchAll=false` exits 0
  </acceptance_criteria>
  <done>
    Dashboard renders responsively (mobile hero card + 2×2 metrics + severity composition; desktop hero row + sparkbars + two-col bottom). Offline banner appears via `useOnlineStatus` with UI-SPEC copy. StatsCards rewritten using Card primitive + CSS vars. StatsCards tests dropped Tailwind assertions, retained data-testid coverage. All relevant tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/api/admin/auth/login` | Login form submits credentials; backend returns HTTP status; frontend MUST map to generic strings per SEC-06 |
| Browser → localStorage | Dark mode toggle reads/writes `admin-theme` |
| Server response body → User-visible error string | Raw server message MUST NOT be displayed (SEC-06 information disclosure mitigation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.5-02-01 | Information Disclosure | Login error display | mitigate | Map HTTP status → one of three locked UI-SPEC strings ("Incorrect email or password.", "Too many attempts. Please wait a few minutes before trying again.", "Something went wrong. Please try again."). Never render `response.json().error` or `response.statusText`. SEC-06 already implemented; this plan preserves the contract verbatim. |
| T-02.5-02-02 | Tampering | Dark mode toggle button onClick | accept | localStorage write is scoped to `admin-theme` key; no privilege boundary; no secret material; tampering with this key only changes UI appearance for the local user. |
| T-02.5-02-03 | Spoofing | useOnlineStatus offline banner | accept | navigator.onLine and window 'offline' events are user-controllable; spoofing the offline state only changes UI visibility of a reassurance banner — no security boundary crossed (D-13 explicitly scopes UI-only). |
| T-02.5-02-04 | Tampering | localStorage `admin-theme` read by FOTWT script (Plan 01 territory; cross-reference) | accept | Plan 01's FOTWT script only adds/removes a CSS class; the dark mode toggle in AdminSidebar is the canonical writer of this key. No XSS surface. |
</threat_model>

<verification>
After both tasks complete:

1. `cd frontend && npm test -- --testPathPattern="(StatsCards|dashboard|login-page|AdminSidebar)" --watchAll=false` exits 0
2. Full admin test suite green: `cd frontend && npm test -- --testPathPattern="admin" --watchAll=false` exits 0
3. Manual smoke (run by executor or human-check): `npm run dev`, visit `/admin/login` — confirm `$ login` headline, no white flash on hard reload with `admin-theme=dark` set. Log in, observe dashboard — hero card teal accent, severity composition list, sidebar toggle flips between light/dark.
4. Lighthouse / accessibility spot check (human-check): all login form inputs and Authenticate button ≥ 44px tap target.
</verification>

<success_criteria>
- AdminSidebar renders desktop sidebar (220px) + mobile bottom tab bar (4 tabs) with dark mode toggle (success criteria #1, #5)
- Login screen uses Direction B terminal aesthetic with SEC-06-compliant error copy (success criterion #1, threat T-02.5-02-01 mitigated)
- Dashboard renders responsively with hero card (light + dark mode correct), severity composition with WCAG 1.4.1 triple encoding, offline banner via useOnlineStatus (success criteria #1, #3, #4, #5, #6)
- StatsCards rewritten using Card primitive + CSS vars; all data-testids preserved (success criterion #1)
- Zero Tailwind classes in rewritten files; zero lucide-react imports (success criterion #8 partial)
- All updated tests pass; existing test coverage preserved (success criterion #8 partial)
</success_criteria>

<output>
Create `.planning/phases/02.5-admin-portal-ui-redesign/02.5-02-SUMMARY.md` capturing: files modified, AdminSidebar dark mode toggle implementation notes (localStorage key, classList target), Login error string mapping table (status → SEC-06 string), dashboard responsive breakpoint approach (matchMedia + display gating), any deviation from PATTERNS.md (e.g. if executor chose different approach for SSR-safe matchMedia).
</output>
</content>
</invoke>