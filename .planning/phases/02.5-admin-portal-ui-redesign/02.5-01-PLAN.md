---
phase: 02.5-admin-portal-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/admin/admin.css
  - frontend/app/layout.tsx
  - frontend/app/admin/layout.tsx
  - frontend/app/admin/lib/useOnlineStatus.ts
  - frontend/app/admin/lib/__tests__/useOnlineStatus.test.ts
  - frontend/app/admin/components/Icon.tsx
  - frontend/app/admin/components/Btn.tsx
  - frontend/app/admin/components/Pill.tsx
  - frontend/app/admin/components/Card.tsx
  - frontend/app/admin/components/SectionLabel.tsx
  - frontend/app/admin/components/SeverityIndicator.tsx
  - frontend/app/admin/components/Input.tsx
  - frontend/app/admin/components/Select.tsx
  - frontend/app/admin/components/Avatar.tsx
  - frontend/app/admin/components/ConfidencePill.tsx
  - frontend/app/admin/components/PhotoTile.tsx
  - frontend/app/admin/components/MapPin.tsx
  - frontend/app/admin/components/Sparkbars.tsx
  - frontend/app/admin/components/Kbd.tsx
  - frontend/app/admin/components/StatusBadge.tsx
  - frontend/app/admin/components/__tests__/Icon.test.tsx
  - frontend/app/admin/components/__tests__/Btn.test.tsx
  - frontend/app/admin/components/__tests__/Pill.test.tsx
  - frontend/app/admin/components/__tests__/Card.test.tsx
  - frontend/app/admin/components/__tests__/SeverityIndicator.test.tsx
  - frontend/app/admin/components/__tests__/StatusBadge.test.tsx
  - frontend/app/admin/__tests__/layout.phase2.test.tsx
autonomous: true
requirements:
  - ADMIN-UI-01
  - ADMIN-UI-02
  - ADMIN-UI-05
  - ADMIN-UI-06
  - ADMIN-UI-07
  - ADMIN-UI-08
decisions_implemented:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-13
  - D-14
  - D-15

must_haves:
  truths:
    - "admin.css exists at frontend/app/admin/admin.css (D-01) and is the single source of truth for all admin-portal colors, fonts, radii, and shadows — tokens scoped under .admin-portal class (D-02), verbatim .dir-b transcription (D-03), radii overrides (D-04), shadow overrides (D-05) — no hardcoded hex/oklch values in component files (success criterion #2)"
    - "Light mode and dark mode are both available, toggled by a .dark class on the <html> element with FOTWT blocking script (D-10) and localStorage key admin-theme (D-11), with no flash-of-wrong-theme on reload (success criterion #3)"
    - "Severity is never color-only: every SeverityIndicator render shows bars (1/2/3 lit) + text label + color (success criterion #4)"
    - "All 15 admin primitives in frontend/app/admin/components/ (D-08) render without crashing and expose data-* attributes for testability; all rewritten in-place (D-06) using CSS vars + inline style, no Tailwind (D-09); lucide-react replaced by Icon (D-07)"
    - "useOnlineStatus hook (D-15) created; offline UI-states only (D-13), pendingCount always 0 (D-14)"
    - "JetBrains Mono weight 600 is loaded by next/font/google (no synthesized bold)"
    - "StatusBadge tests assert on data-tone attributes (not Tailwind class names) and pass green"
  artifacts:
    - path: "frontend/app/admin/admin.css"
      provides: "Direction B token layer scoped under .admin-portal and .dark .admin-portal"
      contains: ".admin-portal {"
    - path: "frontend/app/admin/layout.tsx"
      provides: "admin layout that imports admin.css, applies .admin-portal class, injects FOTWT script"
      contains: "admin-portal"
    - path: "frontend/app/admin/lib/useOnlineStatus.ts"
      provides: "shared online status hook used across admin pages"
      exports: ["useOnlineStatus"]
    - path: "frontend/app/admin/components/Icon.tsx"
      provides: "Custom Icon component replacing lucide-react"
      exports: ["Icon"]
    - path: "frontend/app/admin/components/Btn.tsx"
      provides: "Btn primitive with 7 variants × 4 sizes"
      exports: ["Btn"]
    - path: "frontend/app/admin/components/SeverityIndicator.tsx"
      provides: "WCAG 1.4.1 severity bars + text label component"
      exports: ["SeverityIndicator"]
    - path: "frontend/app/admin/components/StatusBadge.tsx"
      provides: "Rewritten StatusBadge using CSS vars + data-tone attribute"
      contains: "data-tone"
  key_links:
    - from: "frontend/app/admin/layout.tsx"
      to: "frontend/app/admin/admin.css"
      via: "import './admin.css'"
      pattern: "import.*admin\\.css"
    - from: "frontend/app/admin/layout.tsx"
      to: "document.documentElement"
      via: "blocking FOTWT script in <script dangerouslySetInnerHTML>"
      pattern: "localStorage.getItem.*admin-theme"
    - from: "frontend/app/layout.tsx"
      to: "<html>"
      via: "suppressHydrationWarning attribute"
      pattern: "suppressHydrationWarning"
    - from: "frontend/app/admin/components/Btn.tsx"
      to: "frontend/app/admin/components/Icon.tsx"
      via: "icon/iconRight props"
      pattern: "import.*Icon.*from.*Icon"
---

<objective>
Establish the Direction B token foundation and complete primitive library that every other plan in Phase 02.5 builds on.

Purpose: Plans 02, 03, and 04 cannot rewrite any admin screen until `admin.css`, the FOTWT script, the 15 primitives, and `useOnlineStatus` exist. This plan also closes the JetBrains Mono weight 600 gap and rewrites StatusBadge — the only existing component whose contract (data-tone) is consumed across multiple downstream plans (ReportsTable, StatsCards, dashboard, report detail).

Output:
- `frontend/app/admin/admin.css` — Direction B tokens transcribed verbatim from `design-ref/admin-portal/tokens.css` (.dir-b and .dir-b.dark blocks), scoped under `.admin-portal` and `.dark .admin-portal`
- `frontend/app/layout.tsx` — JetBrains Mono weight `"600"` added, `suppressHydrationWarning` on `<html>`
- `frontend/app/admin/layout.tsx` — imports admin.css, wraps in `.admin-portal` div, injects FOTWT blocking script
- `frontend/app/admin/lib/useOnlineStatus.ts` — shared offline detection hook
- 15 admin primitives in `frontend/app/admin/components/`: Icon, Btn, Pill, Card, SectionLabel, SeverityIndicator, Input, Select, Avatar, ConfidencePill, PhotoTile, MapPin, Sparkbars, Kbd, plus the rewritten StatusBadge
- All Wave 0 test stubs covering the new primitives + StatusBadge migration to data-tone assertions + layout assertions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-CONTEXT.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-RESEARCH.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
@.planning/phases/02.5-admin-portal-ui-redesign/02.5-VALIDATION.md

# Design source of truth (executor reads these directly)
@design-ref/admin-portal/tokens.css
@design-ref/admin-portal/primitives.jsx

# Token baseline (must NOT be modified)
@frontend/app/globals.css

# Files being modified (executor reads current state first)
@frontend/app/layout.tsx
@frontend/app/admin/layout.tsx

# Analog patterns
@frontend/app/components/ui/Icon.tsx
@frontend/app/components/ui/Btn.tsx
@frontend/app/components/ui/Pill.tsx

<interfaces>
<!-- Contracts established by this plan that downstream plans (02, 03, 04) consume. -->

From frontend/app/admin/components/Icon.tsx (NEW):
- export type IconName (union of 50+ names from design-ref/admin-portal/primitives.jsx)
- export interface IconProps { name: IconName; size?: number; className?: string; style?: CSSProperties; "aria-label"?: string; "aria-hidden"?: boolean }
- export function Icon(props: IconProps): JSX.Element

From frontend/app/admin/components/Btn.tsx (NEW):
- export type BtnVariant = "primary" | "accent" | "secondary" | "ghost" | "soft" | "danger" | "danger-soft"
- export type BtnSize = "xs" | "sm" | "md" | "lg"
- export interface BtnProps extends ButtonHTMLAttributes<HTMLButtonElement> { variant?: BtnVariant; size?: BtnSize; icon?: IconName; iconRight?: IconName }
- export function Btn(props: BtnProps): JSX.Element

From frontend/app/admin/components/Pill.tsx (NEW):
- export type PillTone = "neutral" | "outline" | "accent" | "danger" | "warn" | "info" | "ink"
- export type PillSize = "sm" | "md" | "lg"
- export interface PillProps { tone?: PillTone; size?: PillSize; dot?: string; icon?: IconName; children: ReactNode; style?: CSSProperties }
- export function Pill(props: PillProps): JSX.Element

From frontend/app/admin/components/Card.tsx (NEW):
- export interface CardProps extends HTMLAttributes<HTMLDivElement> { padded?: boolean; hoverable?: boolean; children: ReactNode }
- export function Card(props: CardProps): JSX.Element

From frontend/app/admin/components/SectionLabel.tsx (NEW):
- export interface SectionLabelProps { children: ReactNode; style?: CSSProperties }
- export function SectionLabel(props: SectionLabelProps): JSX.Element

From frontend/app/admin/components/SeverityIndicator.tsx (NEW):
- export type SeverityLevel = "low" | "medium" | "high"
- export interface SeverityIndicatorProps { severity: SeverityLevel; style?: CSSProperties }
- export function SeverityIndicator(props: SeverityIndicatorProps): JSX.Element

From frontend/app/admin/components/Input.tsx (NEW):
- export interface InputProps extends InputHTMLAttributes<HTMLInputElement> { icon?: IconName; suffix?: ReactNode }
- export function Input(props: InputProps): JSX.Element

From frontend/app/admin/components/Select.tsx (NEW):
- export interface SelectProps extends SelectHTMLAttributes<HTMLSelectElement> { icon?: IconName }
- export function Select(props: SelectProps): JSX.Element

From frontend/app/admin/components/Avatar.tsx (NEW):
- export type AvatarTone = "neutral" | "accent" | "ink"
- export interface AvatarProps { name: string; tone?: AvatarTone; size?: number }
- export function Avatar(props: AvatarProps): JSX.Element

From frontend/app/admin/components/ConfidencePill.tsx (NEW):
- export interface ConfidencePillProps { confidence: "high" | "low" }
- export function ConfidencePill(props: ConfidencePillProps): JSX.Element

From frontend/app/admin/components/PhotoTile.tsx (NEW):
- export interface PhotoTileProps { size?: number; radius?: number; alt?: string; src?: string }
- export function PhotoTile(props: PhotoTileProps): JSX.Element

From frontend/app/admin/components/MapPin.tsx (NEW):
- export interface MapPinProps { status: "submitted" | "under_review" | "resolved"; size?: number }
- export function MapPin(props: MapPinProps): JSX.Element

From frontend/app/admin/components/Sparkbars.tsx (NEW):
- export interface SparkbarsProps { values: number[]; color?: string; height?: number; width?: number }
- export function Sparkbars(props: SparkbarsProps): JSX.Element

From frontend/app/admin/components/Kbd.tsx (NEW):
- export interface KbdProps { children: ReactNode }
- export function Kbd(props: KbdProps): JSX.Element

From frontend/app/admin/components/StatusBadge.tsx (REWRITTEN):
- export type StatusValue = "submitted" | "under_review" | "resolved"
- export interface StatusBadgeProps { status: string; monoLabel?: boolean; size?: "sm" | "md" }
- export default function StatusBadge(props: StatusBadgeProps): JSX.Element  (default export preserved for existing imports)

From frontend/app/admin/lib/useOnlineStatus.ts (NEW):
- export function useOnlineStatus(): boolean
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Token foundation, root layout font/hydration fixes, admin layout, and test stubs</name>
  <files>
    frontend/app/admin/admin.css,
    frontend/app/layout.tsx,
    frontend/app/admin/layout.tsx,
    frontend/app/admin/lib/useOnlineStatus.ts,
    frontend/app/admin/lib/__tests__/useOnlineStatus.test.ts,
    frontend/app/admin/__tests__/layout.phase2.test.tsx,
    frontend/app/admin/components/__tests__/Icon.test.tsx,
    frontend/app/admin/components/__tests__/Btn.test.tsx,
    frontend/app/admin/components/__tests__/Pill.test.tsx,
    frontend/app/admin/components/__tests__/Card.test.tsx,
    frontend/app/admin/components/__tests__/SeverityIndicator.test.tsx,
    frontend/app/admin/components/__tests__/StatusBadge.test.tsx
  </files>
  <read_first>
    design-ref/admin-portal/tokens.css,
    design-ref/admin-portal/primitives.jsx,
    frontend/app/globals.css,
    frontend/app/layout.tsx,
    frontend/app/admin/layout.tsx,
    frontend/app/admin/components/StatusBadge.tsx,
    frontend/app/admin/components/__tests__/StatusBadge.test.tsx,
    frontend/app/admin/__tests__/layout.phase2.test.tsx,
    frontend/app/lib/__tests__/constants.test.ts,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
  </read_first>
  <behavior>
    - admin.css: Transcribe `.dir-b` block (light) and `.dir-b.dark` block (dark) from `design-ref/admin-portal/tokens.css` verbatim under selectors `.admin-portal { }` and `.dark .admin-portal { }` respectively. Add hero text tokens `--text-hero-mobile: 56px` and `--text-hero-desktop: 84px` to the light block per D-03. Add radii overrides (`--r-xs: 4px`, `--r-sm: 6px`, `--r-md: 8px`, `--r-lg: 10px`, `--r-xl: 14px`, `--r-full: 999px`) per D-04. Add shadow overrides (`--shadow-sm`, `--shadow-md`, `--shadow-lg` with rgba(10,10,10,...) values) per D-05. Add universal resets and `.press` / `.no-scrollbar` utility classes inside `.admin-portal` scope per PATTERNS.md token block reference (lines 157-173 of admin.css pattern). Do NOT modify `:root` selectors anywhere.
    - frontend/app/layout.tsx: Add `"600"` to the JetBrains Mono `weight` array (currently `["400", "500"]` per RESEARCH.md Pitfall 1) → target `["400", "500", "600"]`. Add `suppressHydrationWarning` prop to the `<html>` element per D-10 / Pitfall 2.
    - frontend/app/admin/layout.tsx: Add `import './admin.css'` at top of file. After auth guard resolves, wrap the returned JSX in a Fragment whose first child is a `<script>` tag using `dangerouslySetInnerHTML` running the literal IIFE `(function(){var d=localStorage.getItem('admin-theme');if(d==='dark')document.documentElement.classList.add('dark');})();` per D-10. Replace the current outermost wrapper `<div className="min-h-screen bg-gray-50">` with `<div className="admin-portal" style={{ minHeight: '100vh', display: 'flex', background: 'var(--bg)' }}>` per D-02 and PATTERNS.md. Keep the existing auth guard logic and `AdminSidebar` import unchanged (AdminSidebar rewrite happens in Plan 02).
    - useOnlineStatus.ts: `"use client"` first line, then export a hook reading `navigator.onLine` in a `useState` initial guard (typeof window check for SSR safety, default true), set initial value in a `useEffect` from `navigator.onLine`, register `'online'` and `'offline'` window event listeners that update state, return cleanup that removes both listeners. Return type `boolean`.
    - useOnlineStatus.test.ts: Test 1 — returns true when `navigator.onLine` is true on mount. Test 2 — returns false after dispatching `window.dispatchEvent(new Event('offline'))` wrapped in `act()`. Test 3 — returns true again after dispatching `'online'` event following an `'offline'` event.
    - layout.phase2.test.tsx UPDATE: Add a test asserting the rendered admin layout output contains a `<script>` element whose `dangerouslySetInnerHTML.__html` string includes the substring `localStorage.getItem('admin-theme')`. Add a test asserting an element with `className === 'admin-portal'` is present in the rendered output.
    - StatusBadge.test.tsx UPDATE: Replace every `classesContaining(el, ...)` assertion with `expect(el.getAttribute('data-tone')).toBe(<expected>)` where the mapping is submitted→`'info'`, under_review→`'warn'`, resolved→`'accent'`. Keep all `aria-label` assertions unchanged. Keep `data-testid="status-badge"` lookup.
    - Five test stubs (Icon, Btn, Pill, Card, SeverityIndicator) — each file: header comment naming requirement (ADMIN-UI-02 or ADMIN-UI-07), import the component, write 2-3 `it()` blocks asserting the data-* contract from the `<interfaces>` block above. For Icon: aria-hidden default true when no aria-label, role=img + aria-label when prop provided. For Btn: data-variant + data-size present, data-component="btn". For Pill: data-tone + data-size present, data-component="pill". For Card: data-padded + data-hoverable booleans reflect props. For SeverityIndicator: three `<span>` bar children exist inside the aria-hidden bars span, text label string matches "Low" / "Medium" / "High" per severity prop. All stubs MUST pass once Task 2 implements the components.
  </behavior>
  <action>
    Wave 0 task: establish admin token scope, fix root layout chrome, wire admin layout to the new tokens + FOTWT script, create useOnlineStatus hook, and seed all test stubs.

    admin.css: Create new file at frontend/app/admin/admin.css. Transcribe `.dir-b` and `.dir-b.dark` blocks verbatim from design-ref/admin-portal/tokens.css under selectors `.admin-portal` and `.dark .admin-portal`. Add hero text tokens per D-03, radii per D-04, shadows per D-05. Append universal resets, `.press` and `.no-scrollbar` utility classes inside `.admin-portal` scope. Never touch `:root`.

    frontend/app/layout.tsx: Modify the existing JetBrains_Mono call — extend weight array to include "600" (current: ["400","500"], target: ["400","500","600"]) per RESEARCH.md Pitfall 1. Add `suppressHydrationWarning` attribute to the existing `<html>` element per D-10 / Pitfall 2. Do not change Inter, Noto_Sans_Kannada, fontVars, or any auth/SEO logic.

    frontend/app/admin/layout.tsx: Add `import './admin.css'` immediately after existing imports. In the returned JSX for the authenticated branch (replace current wrapper at the bg-gray-50 line), wrap children in a React Fragment whose first child is a `<script dangerouslySetInnerHTML={{__html: ...}}/>` with the IIFE literal from D-10, and whose second child is `<div className="admin-portal" style={{ minHeight: '100vh', display: 'flex', background: 'var(--bg)' }}>` containing `<AdminSidebar role={role} />` and `<main style={{ flex: 1 }}>{children}</main>` exactly as PATTERNS.md specifies. Keep auth guard, cookies(), /auth/me fetch, and redirect logic identical to current.

    useOnlineStatus.ts: Create at frontend/app/admin/lib/useOnlineStatus.ts. `"use client"` first line, default-export-free named export `useOnlineStatus` returning `boolean`. Initialize `useState(true)`. In `useEffect`: set state to `navigator.onLine`, attach `'online'` and `'offline'` event listeners on `window`, return cleanup removing both. Per RESEARCH.md Pattern 5.

    useOnlineStatus.test.ts: Three tests per behavior block. Use `renderHook` and `act` from `@testing-library/react`. Mock navigator.onLine via `Object.defineProperty(navigator, "onLine", { writable: true, value: true })` in beforeEach.

    layout.phase2.test.tsx: Augment existing test file — keep all current tests, add two new tests as specified in behavior block. Use `container.querySelector('script')` and `document.querySelector('.admin-portal')` patterns. Do not delete any existing test.

    StatusBadge.test.tsx: In-place rewrite of every assertion that uses `classesContaining` — replace with `data-tone` getAttribute assertions per behavior block. Keep test names, describe blocks, and aria-label checks unchanged.

    Icon.test.tsx, Btn.test.tsx, Pill.test.tsx, Card.test.tsx, SeverityIndicator.test.tsx: New files. Each starts with header comment block naming the requirement(s) covered. Import the (not-yet-implemented) component from `../<Name>`. Write 2-3 it() blocks per the behavior block. These stubs will fail with import errors until Task 2 lands — that is the Wave 0 contract (`MISSING — Wave 0 must create {test_file} first` is satisfied by this task).

    Do not implement any of the 15 primitive components yet — Task 2 owns that. Do not rewrite AdminSidebar (Plan 02), StatsCards/dashboard (Plan 02), ReportsTable (Plan 03), or any page (Plans 02/03/04).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern="useOnlineStatus" --passWithNoTests --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/admin/admin.css` exists and `grep -c "\.admin-portal {" frontend/app/admin/admin.css` returns at least 1
    - File `frontend/app/admin/admin.css` contains the selector `.dark .admin-portal` (grep `grep "\.dark \.admin-portal" frontend/app/admin/admin.css` returns at least one match)
    - File `frontend/app/admin/admin.css` does NOT contain the selector `:root` (grep `grep "^:root" frontend/app/admin/admin.css` returns no matches)
    - File `frontend/app/admin/admin.css` contains `--text-hero-mobile` and `--text-hero-desktop` tokens
    - File `frontend/app/admin/admin.css` contains `--r-xs: 4px` and `--r-xl: 14px`
    - File `frontend/app/admin/admin.css` contains `--shadow-sm` with `rgba(10,10,10` substring
    - File `frontend/app/layout.tsx` JetBrains_Mono weight array includes the string literal `"600"` (grep `grep -E '"400".*"500".*"600"' frontend/app/layout.tsx` succeeds)
    - File `frontend/app/layout.tsx` `<html` opening tag contains `suppressHydrationWarning` (grep `grep "suppressHydrationWarning" frontend/app/layout.tsx` returns at least one match)
    - File `frontend/app/admin/layout.tsx` contains `import './admin.css'` (or `import "./admin.css"`)
    - File `frontend/app/admin/layout.tsx` contains a `<script` tag with `dangerouslySetInnerHTML` and the string `localStorage.getItem('admin-theme')` (or double-quoted equivalent)
    - File `frontend/app/admin/layout.tsx` contains `className="admin-portal"`
    - File `frontend/app/admin/layout.tsx` does NOT contain `className="min-h-screen bg-gray-50"` anymore
    - File `frontend/app/admin/lib/useOnlineStatus.ts` exists; first line is `"use client";`; exports a named function `useOnlineStatus`
    - File `frontend/app/admin/lib/__tests__/useOnlineStatus.test.ts` exists and contains at least three `it(` blocks
    - File `frontend/app/admin/__tests__/layout.phase2.test.tsx` contains a new assertion mentioning `admin-theme` or `dangerouslySetInnerHTML`
    - File `frontend/app/admin/__tests__/layout.phase2.test.tsx` contains a new assertion mentioning `admin-portal`
    - File `frontend/app/admin/components/__tests__/StatusBadge.test.tsx` contains at least three `getAttribute('data-tone')` references
    - File `frontend/app/admin/components/__tests__/StatusBadge.test.tsx` contains zero `classesContaining(` references (grep returns 0)
    - Each of Icon.test.tsx, Btn.test.tsx, Pill.test.tsx, Card.test.tsx, SeverityIndicator.test.tsx exists under `frontend/app/admin/components/__tests__/` and contains at least 2 `it(` blocks
    - `cd frontend && npm test -- --testPathPattern="useOnlineStatus" --passWithNoTests --watchAll=false` exits 0
  </acceptance_criteria>
  <done>
    Token foundation and chrome fixes shipped. Root layout loads JetBrains Mono 600 with suppressHydrationWarning. Admin layout imports admin.css, applies `.admin-portal` class, injects the FOTWT script. useOnlineStatus hook + its test pass. All Wave 0 test stubs exist (will go green once Task 2 implements the primitives). StatusBadge test file migrated from classesContaining to data-tone assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 15 admin primitives + rewrite StatusBadge</name>
  <files>
    frontend/app/admin/components/Icon.tsx,
    frontend/app/admin/components/Btn.tsx,
    frontend/app/admin/components/Pill.tsx,
    frontend/app/admin/components/Card.tsx,
    frontend/app/admin/components/SectionLabel.tsx,
    frontend/app/admin/components/SeverityIndicator.tsx,
    frontend/app/admin/components/Input.tsx,
    frontend/app/admin/components/Select.tsx,
    frontend/app/admin/components/Avatar.tsx,
    frontend/app/admin/components/ConfidencePill.tsx,
    frontend/app/admin/components/PhotoTile.tsx,
    frontend/app/admin/components/MapPin.tsx,
    frontend/app/admin/components/Sparkbars.tsx,
    frontend/app/admin/components/Kbd.tsx,
    frontend/app/admin/components/StatusBadge.tsx
  </files>
  <read_first>
    design-ref/admin-portal/primitives.jsx,
    design-ref/admin-portal/tokens.css,
    frontend/app/components/ui/Icon.tsx,
    frontend/app/components/ui/Btn.tsx,
    frontend/app/components/ui/Pill.tsx,
    frontend/app/components/redesign/SeverityGrid.tsx,
    frontend/app/admin/components/StatusBadge.tsx,
    frontend/app/admin/admin.css,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-PATTERNS.md,
    .planning/phases/02.5-admin-portal-ui-redesign/02.5-UI-SPEC.md
  </read_first>
  <behavior>
    - Every primitive file starts with `"use client";` and uses **inline `style={{...}}` consuming `var(--token)` references only** — zero Tailwind class names, zero hardcoded hex/oklch literals in style objects (only references like `var(--surface)`, `var(--ink)`, `var(--accent-bg)`).
    - Every primitive's root element carries the data attributes specified in the `<interfaces>` block (data-component="<name>", plus data-variant / data-tone / data-size / data-severity / data-padded / data-hoverable as applicable).
    - Icon: Implements `IconName` union covering at minimum every path defined in `design-ref/admin-portal/primitives.jsx` Icon component (50+ names). Uses SVG paths from primitives.jsx verbatim, viewBox 24×24, stroke="currentColor", strokeWidth 1.75, strokeLinecap/Linejoin "round". Default size 24. Aria contract: when caller passes `aria-label`, render `role="img"` + the aria-label; otherwise render `aria-hidden="true"`. Fallback to `ICONS.dots` for unknown names. Per PATTERNS.md pattern (lines 178-237).
    - Btn: 7 variants (primary/accent/secondary/ghost/soft/danger/danger-soft) × 4 sizes (xs/sm/md/lg). Size minHeights per PATTERNS.md table: xs=28, sm=34, md=40, lg=48. Per UI-SPEC accessibility: when Btn is a primary action it MUST be ≥44px tap target — implementation: md size minHeight=40 by default but UI-SPEC overrides md→44 when md is sole interactive element; encode this by setting md.minHeight=44 (not 40) so all md+lg buttons satisfy ADMIN-UI-07. Renders optional leading and trailing Icon at size 13/14/16 per Btn size. data-variant, data-size, data-component="btn" required. className "press" for press animation.
    - Pill: 7 tones, 3 sizes per PATTERNS.md lines 322-341. Optional `dot` color string renders a 6×6 aria-hidden span at start. Optional `icon` renders Icon size=12 at start.
    - Card: padded (default true) and hoverable (default false) booleans. Renders `<div data-component="card" data-padded data-hoverable>` with `background: var(--surface)`, `border: 1px solid var(--border)`, `borderRadius: var(--r-lg)`, `boxShadow: var(--shadow-sm)`. When hoverable, adds className "press".
    - SectionLabel: 10px JetBrains Mono uppercase weight 600 with letter-spacing `0.10em`, color `var(--muted)`. Per UI-SPEC Typography "Section header" row.
    - SeverityIndicator: Always `style="bars"` style (admin-only). Three bars heights `[4, 8, 12]`, width 3, gap 2, level 1 for low, 2 for medium, 3 for high, lit bars use `var(--sev-{severity})`, unlit use `var(--border-strong)`. Bars wrapper `aria-hidden="true"`. Text label child renders the sentence-case label `"Low"` / `"Medium"` / `"High"` in 11px JetBrains Mono uppercase, weight 600, letter-spacing 0.04em, color `var(--ink-2)`. Per PATTERNS.md lines 415-477 and UI-SPEC component inventory.
    - Input: 44px min-height (per UI-SPEC), `border-radius: var(--r-xs)` per Login section, `border: 1px solid var(--border-strong)`. Optional `icon` prefix renders Icon size=14 inside left padding. Optional `suffix` slot. Background `var(--surface)`, color `var(--ink)`.
    - Select: 36px height per UI-SPEC component inventory, chevron icon renders right side, `border-radius: var(--r-md)`.
    - Avatar: Renders initials (up to 2 chars from `name` prop). Background by tone: neutral→`var(--surface-3)`, accent→`var(--accent-bg)`, ink→`var(--ink)`. Color by tone: neutral→`var(--ink-2)`, accent→`var(--accent-ink)`, ink→`var(--bg)`. Default size 36. Circular (`var(--r-full)`).
    - ConfidencePill: `confidence="high"` → renders `HIGH CONF` text in warn tone Pill (Pill tone="warn"); `confidence="low"` → renders `LOW CONF` in neutral Pill. monoLabel-styled (JetBrains Mono uppercase 10px).
    - PhotoTile: Abstract gradient placeholder div. When no src, renders `aria-hidden="true"` div with `background: linear-gradient(135deg, var(--surface-2), var(--surface-3))`. When src is present, renders an `<img>` with the provided `alt` text per UI-SPEC accessibility item #7. Default size 64, default radius `var(--r-xs)`.
    - MapPin: Colored circle div, `var(--status-{status})` background, default size 14, border `2px solid var(--surface)`.
    - Sparkbars: Inline SVG with `<rect>` per value, color defaults to `var(--ink-2)`. Width default 240, height default 40. Maps values to bar heights proportional to max(values).
    - Kbd: 10px JetBrains Mono uppercase chip, `border: 1px solid var(--border)`, `border-bottom-width: 2px`, padding `2px 6px`, `border-radius: var(--r-sm)`.
    - StatusBadge (REWRITE): Replace current Tailwind-classed implementation with the spec from PATTERNS.md lines 640-691. Default export preserved (existing imports use `import StatusBadge from "./StatusBadge"` per existing code). Props: `status: string`, `monoLabel?: boolean`, `size?: "sm" | "md"`. STATUS_MAP: submitted→tone=info, under_review→tone=warn, resolved→tone=accent. Renders `data-testid="status-badge"`, `data-tone`, `data-status`, `data-component="status-badge"`, `role="status"`, `aria-label`. Includes 6×6 dot span before label.
    - Every component file: Named export `export function ComponentName` per PATTERNS.md "Named Export" rule. **Exception: StatusBadge keeps `export default`** to avoid breaking existing imports.
  </behavior>
  <action>
    Build the 14 new primitives + rewrite StatusBadge. Each component lives at `frontend/app/admin/components/<Name>.tsx`. For each component, mirror the analog file listed in PATTERNS.md "Key lookup table" and apply the prop API spec from `design-ref/admin-portal/primitives.jsx`.

    Implementation order (per dependency: Icon has no deps; Btn/Pill consume Icon; Card has no deps; SeverityIndicator/SectionLabel/Avatar/Kbd/PhotoTile/MapPin/Sparkbars/ConfidencePill/Input/Select consume Icon and/or Pill):
    1. Icon.tsx — transcribe ICONS record from design-ref/admin-portal/primitives.jsx, named export
    2. SectionLabel.tsx, Card.tsx, Kbd.tsx, MapPin.tsx, PhotoTile.tsx, Sparkbars.tsx, Avatar.tsx — leaf components with no Icon/Pill dep
    3. Btn.tsx, Pill.tsx — consume Icon
    4. Input.tsx, Select.tsx — consume Icon
    5. SeverityIndicator.tsx — leaf, references --sev-* CSS vars
    6. ConfidencePill.tsx — consumes Pill
    7. StatusBadge.tsx — REWRITE existing file in-place per PATTERNS.md StatusBadge full rewrite pattern (lines 640-691); keep `export default StatusBadge` at end

    Btn size table per PATTERNS.md lines 261-267, but **override md minHeight from 40 to 44** to satisfy UI-SPEC accessibility item #2 ("all buttons ≥ 44px tap target ... override md→44 when md is sole interactive element"). md remains visually unchanged otherwise (padding/fontSize stay at PATTERNS.md values).

    Btn variant table per PATTERNS.md lines 271-280.

    Pill tone + size tables per PATTERNS.md lines 327-341. Render dot span when `dot` prop provided, icon when `icon` prop provided.

    SeverityIndicator: triple-encoding mandatory — bars aria-hidden, text label carries semantic meaning. SEV_MAP per PATTERNS.md lines 431-435. BAR_HEIGHTS = [4, 8, 12].

    For every component: assign `data-component="<lowercase-name>"` plus the data-* attributes that match the test stubs from Task 1. Use named export `export function Name` except StatusBadge keeps `export default`.

    Do not modify package.json (lucide-react removal happens in Plan 04 after final grep). Do not touch any existing admin component besides StatusBadge.tsx. Do not write or modify any test file in this task — Task 1 already seeded the stubs; this task makes them green.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern="admin/components/__tests__" --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - All 14 new primitive files exist under `frontend/app/admin/components/` (Icon.tsx, Btn.tsx, Pill.tsx, Card.tsx, SectionLabel.tsx, SeverityIndicator.tsx, Input.tsx, Select.tsx, Avatar.tsx, ConfidencePill.tsx, PhotoTile.tsx, MapPin.tsx, Sparkbars.tsx, Kbd.tsx)
    - Every new primitive file's first non-comment line is `"use client";`
    - Every new primitive file uses `export function <Name>` (grep `grep "^export function" frontend/app/admin/components/<Name>.tsx` returns at least one match for each of the 14 files)
    - Zero new primitive file contains the string `className="bg-` or `className="text-` or `className="border-` (grep `grep -nE 'className="(bg-|text-|border-|hover:)' frontend/app/admin/components/<Name>.tsx` returns no matches for any of the 14 files)
    - Zero new primitive file contains a `lucide-react` import (grep `grep "lucide-react" frontend/app/admin/components/Icon.tsx Btn.tsx Pill.tsx Card.tsx SectionLabel.tsx SeverityIndicator.tsx Input.tsx Select.tsx Avatar.tsx ConfidencePill.tsx PhotoTile.tsx MapPin.tsx Sparkbars.tsx Kbd.tsx` returns no matches)
    - File `frontend/app/admin/components/Icon.tsx` contains `IconName` type export with at least 40 named members (grep `grep -c '"|"' frontend/app/admin/components/Icon.tsx` ≥ 40, accounting for union pipes; equivalent assertion: file size > 2KB and contains the names `menu`, `close`, `inbox`, `map`, `users`, `activity`, `org`, `sun`, `moon`)
    - File `frontend/app/admin/components/Btn.tsx` exports `BtnVariant` union containing `"primary"`, `"accent"`, `"secondary"`, `"ghost"`, `"soft"`, `"danger"`, `"danger-soft"`
    - File `frontend/app/admin/components/Btn.tsx` SIZES record has `md` with `minHeight: 44` (grep `grep -nE 'md:.*minHeight:\s*44' frontend/app/admin/components/Btn.tsx` returns at least one match)
    - File `frontend/app/admin/components/SeverityIndicator.tsx` contains the literal `[4, 8, 12]` (or `[4,8,12]`) BAR_HEIGHTS reference
    - File `frontend/app/admin/components/SeverityIndicator.tsx` text label child renders the literal strings `"Low"`, `"Medium"`, `"High"` (grep `grep -E '"Low"|"Medium"|"High"' frontend/app/admin/components/SeverityIndicator.tsx` returns matches)
    - File `frontend/app/admin/components/StatusBadge.tsx` contains `data-tone={m.tone}` (or equivalent template literal) and `data-testid="status-badge"`
    - File `frontend/app/admin/components/StatusBadge.tsx` last line includes `export default StatusBadge` (grep `grep "^export default StatusBadge" frontend/app/admin/components/StatusBadge.tsx` returns 1 match)
    - File `frontend/app/admin/components/StatusBadge.tsx` does NOT contain Tailwind class names (grep `grep -nE 'className="(bg-|text-|border-)' frontend/app/admin/components/StatusBadge.tsx` returns no matches)
    - `cd frontend && npm test -- --testPathPattern="admin/components/__tests__/(Icon|Btn|Pill|Card|SeverityIndicator|StatusBadge)" --watchAll=false` exits 0
    - `frontend/app/admin/components/Icon.tsx` contains all icon names used by downstream plans: `grep -E '"logout"' frontend/app/admin/components/Icon.tsx` returns a match; `grep -E '"arrow_right"' frontend/app/admin/components/Icon.tsx` returns a match; `grep -E '"org"' frontend/app/admin/components/Icon.tsx` returns a match; `grep -E '"shield"' frontend/app/admin/components/Icon.tsx` returns a match
  </acceptance_criteria>
  <done>
    All 15 admin primitives (14 new + StatusBadge rewrite) exist as named-export `"use client"` React components consuming CSS variables exclusively. Every Wave 0 test stub from Task 1 is now green. StatusBadge tests pass under data-tone assertions. No lucide-react imports in any primitive. No Tailwind class strings in any primitive.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → localStorage | FOTWT script reads `admin-theme` key from localStorage before React hydrates |
| Server-rendered HTML → Client hydration | `<html>` class state set by client script before React mount; mitigated by `suppressHydrationWarning` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.5-01-01 | Tampering | FOTWT inline `<script>` in `app/admin/layout.tsx` | mitigate | Script body is a static string literal (D-10 IIFE); no user input concatenated; no `eval`; no DOM properties read from URL/cookies/network. Only reads `localStorage.getItem('admin-theme')` and writes `document.documentElement.classList`. XSS vector closed at source. |
| T-02.5-01-02 | Information Disclosure | admin.css token file | accept | Direction B token values are public design constants (palette, radii, shadows); no secrets; no PII; ships in browser bundle today via tokens.css source already. Tracked under SEC-05 expectations (already PASS). |
| T-02.5-01-03 | Tampering | StatusBadge data-tone attribute | accept | data-* attributes are read-only from external JS perspective; test-only contract; no privilege boundary crossed. |
</threat_model>

<verification>
After both tasks complete:

1. `cd frontend && npm test -- --testPathPattern="admin" --passWithNoTests --watchAll=false` exits 0
2. `cd frontend && npm test -- --testPathPattern="useOnlineStatus" --watchAll=false` exits 0
3. `cd frontend && npm test -- --testPathPattern="admin/components/__tests__" --watchAll=false` exits 0 (all 6 stubs + StatusBadge migrated tests green)
4. Manual smoke (executor may defer to human-check): run `cd frontend && npm run dev`, visit `http://localhost:3000/admin/login`, set `localStorage.admin-theme = 'dark'` in DevTools, hard reload — observe no white flash before dark mode applies.
5. `grep -rE "lucide-react" frontend/app/admin/components/{Icon,Btn,Pill,Card,SectionLabel,SeverityIndicator,Input,Select,Avatar,ConfidencePill,PhotoTile,MapPin,Sparkbars,Kbd}.tsx` returns no matches.
</verification>

<success_criteria>
- admin.css exists and scopes Direction B tokens under `.admin-portal` (light) and `.dark .admin-portal` (dark); `:root` untouched (success criteria #2, #3)
- JetBrains Mono weight 600 loaded by next/font; `suppressHydrationWarning` on root `<html>` (FOTWT prerequisite — success criterion #3)
- admin layout imports admin.css, applies `.admin-portal` class, injects FOTWT script (success criteria #1, #3)
- 14 new admin primitives + rewritten StatusBadge ship as `"use client"` named-export components consuming CSS vars (success criteria #1, #4, #7)
- SeverityIndicator renders bars (aria-hidden) + text label + color — never color-only (success criterion #4)
- All Wave 0 test stubs green; StatusBadge tests migrated to data-tone assertions; no Tailwind class names in any primitive (success criterion #8 partial)
- useOnlineStatus hook exists and its tests pass (foundation for Plans 02-04 offline states — success criterion #6)
</success_criteria>

<output>
Create `.planning/phases/02.5-admin-portal-ui-redesign/02.5-01-SUMMARY.md` capturing: files created/modified counts, primitive list with prop API summary, test stub list (passing), known gotchas for downstream plans (e.g. lucide-react still in package.json — Plan 04 cleanup, AdminSidebar still imports lucide-react — Plan 02 rewrite).
</output>
</content>
</invoke>