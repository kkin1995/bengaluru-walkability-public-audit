---
phase: 03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db/admin_queries.rs
autonomous: true
requirements: [BUG-03.2-B]
must_haves:
  truths:
    - "Status history entries on /admin/reports/[id] show BY · <admin email> when display_name is NULL (no longer BY · —)"
    - "The status history SQL selects changed_by_name via COALESCE(au.display_name, au.email)"
    - "A unit test asserts the status history SQL fragment contains COALESCE and au.email without a live database"
  artifacts:
    - path: "backend/src/db/admin_queries.rs"
      provides: "COALESCE attribution fallback in status history query + attribution_sql_fragment() test helper"
      contains: "COALESCE(au.display_name, au.email)"
  key_links:
    - from: "backend/src/db/admin_queries.rs status history query"
      to: "admin_users.email column"
      via: "COALESCE fallback inside LEFT JOIN on au.id = sh.changed_by"
      pattern: "COALESCE\\(au\\.display_name, au\\.email\\)"
---

<objective>
Fix BUG-03.2-B: status history attribution on /admin/reports/[id] always renders `BY · —` because the seeded admin has `display_name = NULL` and the status history query selects `au.display_name AS changed_by_name` with no fallback. Change that one column expression to `COALESCE(au.display_name, au.email) AS changed_by_name` so the admin email is surfaced when no display name is set. Add a test-only SQL-string helper plus a unit test so the fix is verifiable without a live database (consistent with the runtime-`sqlx::query` pattern this file deliberately uses).

Purpose: Restores real admin attribution in the status history timeline — a UAT-confirmed regression from Phase 03.2.
Output: A modified status history query in admin_queries.rs, a `attribution_sql_fragment()` test helper, and a passing unit test `attribution_fallback_sql_uses_coalesce`.
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback/03.3-UAT-ISSUES.md
@.planning/phases/03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback/03.3-RESEARCH.md
@.planning/phases/03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback/03.3-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: COALESCE attribution fallback + Wave 0 SQL-string test helper and unit test</name>
  <files>backend/src/db/admin_queries.rs</files>
  <read_first>
    - backend/src/db/admin_queries.rs — read the status history query block (around lines 537–560, the `let history_rows = sqlx::query(...)` block that LEFT JOINs admin_users on au.id = sh.changed_by) AND the existing test-only SQL-string helper pattern (around lines 1026–1038, `admin_user_cols_sql()` / `deactivate_admin_user_sql()`) AND the test module structure (the `#[cfg(test)] mod tests` block, around line 1054+) to mirror the existing assertion style.
  </read_first>
  <behavior>
    - Test `attribution_fallback_sql_uses_coalesce`: the string returned by `attribution_sql_fragment()` contains the substring "COALESCE" AND the substring "au.email" AND the substring "changed_by_name".
    - Behavior: the status history query column expression now reads `COALESCE(au.display_name, au.email) AS changed_by_name` (table alias qualified, per RESEARCH.md Pitfall 3).
  </behavior>
  <action>
    Wave 0 first: add a test-only `#[allow(dead_code)] pub fn attribution_sql_fragment() -> &'static str` that returns the verbatim status history SQL string (the same `r#" ... "#` literal used in the `history_rows` query), following the existing `admin_user_cols_sql()` helper convention already present in this file. Then in the `#[cfg(test)] mod tests` block add `#[test] fn attribution_fallback_sql_uses_coalesce()` asserting the fragment `.contains("COALESCE")`, `.contains("au.email")`, and `.contains("changed_by_name")`.
    Then make the fix: in the status history `sqlx::query(...)` block (the one whose `WHERE sh.report_id = $1 ORDER BY sh.changed_at DESC`), change the single selected column line from `au.display_name AS changed_by_name` to `COALESCE(au.display_name, au.email) AS changed_by_name`. Keep the alias qualified as `au.` on both columns. Change nothing else in the query — same FROM, LEFT JOIN, WHERE, ORDER BY, same `.bind(report_id)`.
    To avoid query-string duplication drift between the live query and the test helper, define the SQL once as a `const` (or have `attribution_sql_fragment()` return the exact same literal the query uses) so the test asserts on the string the runtime query actually executes.
    Do NOT switch to `query!`/`query_as!` macros — this file deliberately uses runtime `sqlx::query(...)` so `cargo test` runs without a live DB (RESEARCH.md Anti-Patterns). No migration. No frontend change — the frontend `changed_by_name || "—"` fallback stops firing once the column is non-NULL.
  </action>
  <verify>
    <automated>cd backend && cargo test attribution_fallback_sql_uses_coalesce -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && cargo test attribution_fallback` exits 0 with the test `attribution_fallback_sql_uses_coalesce` reported as passed (not "0 tests run").
    - `grep -n "COALESCE(au.display_name, au.email) AS changed_by_name" backend/src/db/admin_queries.rs` returns at least one match.
    - `grep -c "au.display_name AS changed_by_name" backend/src/db/admin_queries.rs` returns 0 (the un-fixed expression no longer exists as a bare select).
    - `cd backend && cargo check` exits 0 (compiles).
    - `grep -n "fn attribution_sql_fragment" backend/src/db/admin_queries.rs` returns a match (test helper exists).
    - No occurrence of `query!` or `query_as!` introduced in the status history block (`grep -c "query_as!" backend/src/db/admin_queries.rs` is unchanged from before the edit).
  </acceptance_criteria>
  <done>Status history query selects COALESCE(au.display_name, au.email) AS changed_by_name; attribution_sql_fragment() helper exists; attribution_fallback_sql_uses_coalesce unit test passes; cargo check and cargo test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /api/admin/reports/:id | Authenticated admin requests report detail incl. status history; behind require_auth middleware |
| backend → PostgreSQL | Parameterised query with `.bind(report_id)`; report_id is a path-extracted UUID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.3B-01 | Information Disclosure | changed_by_name now exposes admin email instead of display name | accept | Endpoint is inside admin_protected_router (require_auth); admin email is already visible to authenticated admins on the Users page. No new PII exposure to unauthenticated users. |
| T-03.3B-02 | Tampering | report_id bind in status history query | mitigate | Already parameterised via `.bind(report_id)` (UUID); no string interpolation. This plan does not alter binding. |
| T-03.3B-SC | Tampering | npm/cargo installs | mitigate | No new packages installed this plan (RESEARCH.md Package Legitimacy Audit: not applicable). No install tasks present. |
</threat_model>

<verification>
- `cd backend && cargo check` exits 0.
- `cd backend && cargo test` exits 0 (full backend suite green, including the new attribution test).
- `grep -n "COALESCE(au.display_name, au.email)" backend/src/db/admin_queries.rs` confirms the fix is present.
</verification>

<success_criteria>
- Status history query column is `COALESCE(au.display_name, au.email) AS changed_by_name`.
- `attribution_sql_fragment()` test helper exists and `attribution_fallback_sql_uses_coalesce` passes.
- Full backend `cargo test` suite is green; `cargo check` compiles.
- No new migration, no frontend change, no new dependency.
</success_criteria>

<output>
Create `.planning/phases/03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback/03.3-01-SUMMARY.md` when done.
</output>
