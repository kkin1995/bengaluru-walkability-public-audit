---
phase: 03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback
plan: 02
type: execute
wave: 2
depends_on: ["03.3-01"]
files_modified:
  - backend/src/models/admin.rs
  - backend/src/db/admin_queries.rs
  - backend/src/handlers/admin.rs
  - backend/src/main.rs
  - frontend/app/admin/lib/adminApi.ts
  - frontend/app/admin/page.tsx
  - frontend/app/admin/__tests__/dashboard.test.tsx
  - frontend/app/admin/lib/__tests__/adminApi.intake.test.ts
autonomous: true
requirements: [BUG-03.2-A]
must_haves:
  truths:
    - "GET /api/admin/stats/intake?days=N returns per-day report counts as JSON [{day, count}], one row per day that has submissions"
    - "The handler clamps days to [1,90] and defaults to 14 when absent or invalid"
    - "On the admin dashboard, clicking 7D/14D/30D fetches real intake data for that window and the sparkbars reflect actual per-day report counts (empty days render height 0)"
    - "A system with 1 total report shows at most 1 bar with height > 0 (no hardcoded STUB_SPARKBARS pattern remains)"
    - "Backend SQL and frontend wiring are unit-tested without a live DB / live backend"
  artifacts:
    - path: "backend/src/models/admin.rs"
      provides: "IntakeDayCount struct (day: String, count: i64) with Serialize/Deserialize"
      contains: "struct IntakeDayCount"
    - path: "backend/src/db/admin_queries.rs"
      provides: "get_intake_stats(pool, days) per-day aggregate query + intake_sql_fragment() test helper"
      contains: "date_trunc"
    - path: "backend/src/handlers/admin.rs"
      provides: "admin_get_intake_stats handler with days clamp [1,90]"
      contains: "clamp(1, 90)"
    - path: "backend/src/main.rs"
      provides: "/api/admin/stats/intake route in admin_protected_router"
      contains: "/api/admin/stats/intake"
    - path: "frontend/app/admin/lib/adminApi.ts"
      provides: "getIntakeStats(days) + IntakeDayCount interface"
      contains: "getIntakeStats"
    - path: "frontend/app/admin/page.tsx"
      provides: "useEffect fetching real intake data on activePeriod change; STUB_SPARKBARS removed"
      contains: "getIntakeStats"
    - path: "frontend/app/admin/lib/__tests__/adminApi.intake.test.ts"
      provides: "URL/credentials/parsing tests for getIntakeStats"
      contains: "stats/intake"
  key_links:
    - from: "frontend/app/admin/page.tsx useEffect [activePeriod]"
      to: "GET /api/admin/stats/intake?days=N"
      via: "getIntakeStats(dayCount) in adminApi.ts"
      pattern: "getIntakeStats\\("
    - from: "backend/src/handlers/admin.rs admin_get_intake_stats"
      to: "backend/src/db/admin_queries.rs get_intake_stats"
      via: "admin_queries::get_intake_stats(&state.pool, days)"
      pattern: "get_intake_stats\\(&state\\.pool"
    - from: "backend/src/main.rs admin_protected_router"
      to: "admin_get_intake_stats handler"
      via: ".route(\"/api/admin/stats/intake\", get(admin_get_intake_stats))"
      pattern: "/api/admin/stats/intake"
---

<objective>
Fix BUG-03.2-A: the dashboard INTAKE sparkbars render hardcoded `STUB_SPARKBARS` data unrelated to real reports. Build the missing data path end-to-end: a new backend endpoint `GET /api/admin/stats/intake?days=N` that returns per-day report counts, and frontend wiring that fetches it whenever the 7D/14D/30D period changes and zero-fills sparse days before passing the dense array to `<Sparkbars>`. Backend and frontend are one coupled plan because the frontend cannot be exercised without the endpoint.

Purpose: The dashboard intake chart shows real submission volume per day instead of meaningless stub bars — a UAT-confirmed bug from Phase 03.2.
Output: IntakeDayCount struct, get_intake_stats DB fn, admin_get_intake_stats handler, intake route, getIntakeStats adminApi fn, dashboard useEffect wiring, and unit tests across backend SQL string + frontend API + dashboard React.
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback/03.3-UAT-ISSUES.md
@.planning/phases/03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback/03.3-RESEARCH.md
@.planning/phases/03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback/03.3-PATTERNS.md
@.planning/phases/03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback/03.3-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend intake endpoint — struct, DB fn (+ SQL test helper), handler with days clamp, router wiring</name>
  <files>backend/src/models/admin.rs, backend/src/db/admin_queries.rs, backend/src/handlers/admin.rs, backend/src/main.rs</files>
  <read_first>
    - backend/src/models/admin.rs — the `StatsResponse` struct (around lines 241–251) for the `#[derive(Debug, Serialize, Deserialize)]` convention and where the Stats response section lives.
    - backend/src/db/admin_queries.rs — `get_report_stats` (around lines 804–877) for the runtime `sqlx::query(...).bind(...).fetch_all(pool).await?` + `row.get::<T, _>("col")` mapping pattern; the existing SQL-string test helper convention (around lines 1026–1038); the `#[cfg(test)] mod tests` block for assertion style. (Note: this file is also touched by 03.3-01 — read current state, the attribution fix and its helper will already be present.)
    - backend/src/handlers/admin.rs — `admin_get_stats` (around lines 778–786) for handler shape, and `admin_list_reports` (around line 410) for the `Query(params): Query<...>` extractor pattern.
    - backend/src/main.rs — the `handlers::admin::{...}` import block (around lines 160–165) and the `admin_protected_router` route list with `.route("/api/admin/stats", get(admin_get_stats))` (around line 200).
  </read_first>
  <behavior>
    - Test `intake_sql_uses_date_trunc_and_group_by`: the string returned by `intake_sql_fragment()` contains "date_trunc" AND "GROUP BY".
    - Behavior (verified by inspection / cargo check): handler clamps `days` to [1,90] via `params.days.unwrap_or(14).clamp(1, 90)`; query binds `days` as a parameter (no string interpolation); per-day counts grouped by truncated UTC day.
  </behavior>
  <action>
    In `backend/src/models/admin.rs`, after the `StatsResponse` block in the Stats response section, add `pub struct IntakeDayCount { pub day: String, pub count: i64 }` with `#[derive(Debug, Serialize, Deserialize)]`. `day` is an ISO date string "YYYY-MM-DD" (UTC); `count` is i64.
    In `backend/src/db/admin_queries.rs`, append after `get_report_stats` (before the Phase 2 comment near line 879) an async fn `get_intake_stats(pool: &PgPool, days: i32) -> Result<Vec<IntakeDayCount>, AppError>`. Use runtime `sqlx::query(...)` (NOT `query!`/`query_as!`). SQL: select `date_trunc('day', created_at AT TIME ZONE 'UTC')::DATE::TEXT AS day`, `COUNT(*)::BIGINT AS count` from reports where `created_at >= NOW() - ($1 || ' days')::INTERVAL`, `GROUP BY 1 ORDER BY 1`. Bind `days` (i32) as `$1`. Map rows with `r.get::<String, _>("day")` and `r.get::<i64, _>("count")`. Import `crate::models::admin::IntakeDayCount`. Wave 0: also add a `#[allow(dead_code)] pub fn intake_sql_fragment() -> &'static str` returning the exact same SQL literal (define the literal once as a const reused by both the query and the helper to prevent drift), and a `#[test] fn intake_sql_uses_date_trunc_and_group_by` asserting `.contains("date_trunc")` and `.contains("GROUP BY")`.
    INTERVAL note (RESEARCH.md Pitfall 1 / Open Question 1): `($1 || ' days')::INTERVAL` is [ASSUMED]; verify with `cargo check`. If Postgres rejects it at runtime, the fallback is `INTERVAL '1 day' * $1`. Do not use `format!`/string interpolation for the interval — it must stay a bound parameter (T-intake-sqli mitigation).
    In `backend/src/handlers/admin.rs`, add `#[derive(serde::Deserialize)] pub struct IntakeParams { pub days: Option<i32> }`, then append after `admin_get_stats` (before `admin_list_users`) an `admin_get_intake_stats` handler with signature `Extension(_claims): Extension<AuthJwtClaims>, State(state): State<Arc<AppState>>, Query(params): Query<IntakeParams>` returning `Result<Json<serde_json::Value>, AppError>`. Compute `let days = params.days.unwrap_or(14).clamp(1, 90);` (T-intake-dos mitigation — clamp BEFORE the DB call), call `admin_queries::get_intake_stats(&state.pool, days).await?`, return `Ok(Json(serde_json::json!(rows)))`.
    In `backend/src/main.rs`, add `admin_get_intake_stats` to the `handlers::admin::{...}` import list, and add `.route("/api/admin/stats/intake", get(admin_get_intake_stats))` immediately after the existing `.route("/api/admin/stats", get(admin_get_stats))` line inside `admin_protected_router` so it inherits `require_auth` (V4 access control).
  </action>
  <verify>
    <automated>cd backend && cargo test intake_sql_uses_date_trunc_and_group_by -- --nocapture && cargo check</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && cargo check` exits 0 (all four files compile together).
    - `cd backend && cargo test intake_sql` exits 0 with `intake_sql_uses_date_trunc_and_group_by` reported passed (not "0 tests run").
    - `grep -n "struct IntakeDayCount" backend/src/models/admin.rs` returns a match.
    - `grep -n "clamp(1, 90)" backend/src/handlers/admin.rs` returns a match (days bounded).
    - `grep -n "/api/admin/stats/intake" backend/src/main.rs` returns a match (route wired).
    - `grep -n "date_trunc" backend/src/db/admin_queries.rs` returns a match AND `grep -c "format!" backend/src/db/admin_queries.rs` shows the intake query does not use string interpolation for the interval (manual confirm the `($1 || ' days')::INTERVAL` bound form is used).
    - `grep -c "query_as!" backend/src/db/admin_queries.rs` is unchanged (no compile-time macros introduced).
  </acceptance_criteria>
  <done>IntakeDayCount struct exists; get_intake_stats + intake_sql_fragment() helper exist; admin_get_intake_stats handler clamps days [1,90]; intake route wired into admin_protected_router; cargo check + intake_sql test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend adminApi getIntakeStats + new API unit test (Wave 0)</name>
  <files>frontend/app/admin/lib/adminApi.ts, frontend/app/admin/lib/__tests__/adminApi.intake.test.ts</files>
  <read_first>
    - frontend/app/admin/lib/adminApi.ts — the `ADMIN_API_BASE_URL as BASE` import (around line 11), the `apiFetch<T>` helper (around lines 139–158, injects `credentials: "include"`), and the `getStats()` function (around lines 242–244) which is the exact analog.
    - frontend/app/admin/lib/__tests__/adminApi.phase2.test.ts — the full file: copy the `mockOkResponse`, `lastFetchUrl`, `lastFetchOptions` helpers and the `beforeEach(() => { global.fetch = jest.fn(); })` setup verbatim as the established testing pattern.
  </read_first>
  <behavior>
    - `getIntakeStats(14)` sends a GET whose URL contains `/api/admin/stats/intake?days=14`.
    - `getIntakeStats(7)` sends a URL containing `days=7`.
    - On HTTP 200 with body `[{day:"2026-05-30",count:5}]`, `getIntakeStats` returns that parsed array.
    - The fetch is issued with `credentials: "include"` (inherited from apiFetch).
  </behavior>
  <action>
    In `frontend/app/admin/lib/adminApi.ts`, append to the Stats section (after `getStats()`) an exported `interface IntakeDayCount { day: string; count: number }` and an exported async `getIntakeStats(days: number): Promise<IntakeDayCount[]>` that returns `apiFetch<IntakeDayCount[]>(\`${BASE}/api/admin/stats/intake?days=${days}\`)`. Do not call `fetch()` directly — route through `apiFetch` so `credentials: "include"` is applied (shared pattern).
    Create `frontend/app/admin/lib/__tests__/adminApi.intake.test.ts` (new file). Copy the `mockOkResponse`/`lastFetchUrl`/`lastFetchOptions` helpers and `beforeEach` fetch mock from `adminApi.phase2.test.ts`. Add tests: (a) `getIntakeStats(14)` URL matches `/\/api\/admin\/stats\/intake\?days=14/`; (b) `getIntakeStats(7)` URL contains `days=7`; (c) returns parsed `IntakeDayCount[]` on 200 (assert `result` deep-equals the fixture `[{day:"2026-05-30",count:5},{day:"2026-05-29",count:3}]`); (d) request options include `credentials: "include"`.
  </action>
  <verify>
    <automated>cd frontend && npx jest app/admin/lib/__tests__/adminApi.intake.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx jest app/admin/lib/__tests__/adminApi.intake.test.ts` exits 0 with all four tests passing (not "No tests found").
    - `grep -n "getIntakeStats" frontend/app/admin/lib/adminApi.ts` returns a match.
    - `grep -n "interface IntakeDayCount" frontend/app/admin/lib/adminApi.ts` returns a match.
    - `grep -c "fetch(" frontend/app/admin/lib/adminApi.ts` shows no new direct `fetch(` call added in getIntakeStats (it routes through apiFetch).
  </acceptance_criteria>
  <done>getIntakeStats + IntakeDayCount exported from adminApi.ts; adminApi.intake.test.ts created; all four API tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dashboard page wiring — remove STUB_SPARKBARS, fetch real intake on period change + dashboard tests (Wave 0)</name>
  <files>frontend/app/admin/page.tsx, frontend/app/admin/__tests__/dashboard.test.tsx</files>
  <read_first>
    - frontend/app/admin/page.tsx — the adminApi import line (around line 5), the `STUB_SPARKBARS` constant (around line 30), the `activePeriod` useState (around line 41), the `sparkbarsValues` IIFE (around lines 103–115), the existing non-critical-fetch useEffect (around lines 56–61) and responsive useEffect (around lines 65–72) for the established effect pattern, and the `<Sparkbars values={...} />` JSX (around lines 480–485).
    - frontend/app/admin/__tests__/dashboard.test.tsx — the `jest.mock("../lib/adminApi", ...)` factory (around lines 31–43), the `beforeEach` default-mock block, and the existing period button tests (around lines 406–473) for the click/`waitFor`/`act` pattern.
    - frontend/app/admin/components/Sparkbars.tsx — confirm the `values: number[]` prop type (the new wiring passes a `number[]`).
  </read_first>
  <behavior>
    - On mount (default 14D), the dashboard calls `getIntakeStats(14)`.
    - Clicking the 7D button calls `getIntakeStats(7)`.
    - Clicking the 30D button calls `getIntakeStats(30)`.
    - The Sparkbars `values` array length equals the selected window (7/14/30) with sparse days zero-filled oldest→newest; an empty/error fetch yields an all-zero array of the correct length (no crash, no STUB pattern).
  </behavior>
  <action>
    In `frontend/app/admin/page.tsx`: update the adminApi import to also import `getIntakeStats` and `type IntakeDayCount`. Remove the `STUB_SPARKBARS` constant and the entire `sparkbarsValues` IIFE. Add state `const [intakeData, setIntakeData] = useState<number[]>([]);` (and optionally `isIntakeLoading`) after the `activePeriod` state. Add a `useEffect` with dependency array `[activePeriod]` that: computes `dayCount` = 7 for "7D", 30 for "30D", else 14; calls `getIntakeStats(dayCount)`; on success builds a dense `number[]` of length `dayCount` ordered oldest→newest by iterating `i` from `dayCount-1` down to 0, computing the UTC date key via `d.setUTCDate(d.getUTCDate() - i)` + `d.toISOString().slice(0,10)`, and pushing the matching `count` or 0 (zero-fill); on error sets an all-zero array of length `dayCount`. Do NOT call `setActivePeriod` inside this effect (infinite-loop anti-pattern). Update the `<Sparkbars>` `values` prop to `intakeData.length > 0 ? intakeData : Array(dayCount).fill(0)` (compute the same dayCount inline) so flat-zero bars show during first load instead of a blank chart (RESEARCH.md Pitfall 5).
    In `frontend/app/admin/__tests__/dashboard.test.tsx`: add `getIntakeStats: jest.fn()` to the `jest.mock("../lib/adminApi", ...)` factory object, and add `(adminApi.getIntakeStats as jest.Mock).mockResolvedValue([]);` to the `beforeEach` default mocks (Pitfall 4 — missing mock throws "not a function"). Add a describe block with three tests modelled on the existing period button tests: default mount calls `getIntakeStats(14)`; clicking 7D calls `getIntakeStats(7)`; clicking 30D calls `getIntakeStats(30)` — each using `render(<AdminDashboard />)`, `waitFor`, `userEvent.click` on the button's `.closest("button")`, and `expect(adminApi.getIntakeStats).toHaveBeenCalledWith(N)`.
  </action>
  <verify>
    <automated>cd frontend && npx jest app/admin/__tests__/dashboard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx jest app/admin/__tests__/dashboard.test.tsx` exits 0 with the three new intake tests passing AND all pre-existing dashboard tests still green (not "No tests found").
    - `grep -c "STUB_SPARKBARS" frontend/app/admin/page.tsx` returns 0 (stub fully removed).
    - `grep -n "getIntakeStats" frontend/app/admin/page.tsx` returns a match (real fetch wired).
    - `grep -n "getIntakeStats: jest.fn()" frontend/app/admin/__tests__/dashboard.test.tsx` returns a match (mock added).
    - `grep -c "setActivePeriod" frontend/app/admin/page.tsx` shows no call to setActivePeriod inside the `[activePeriod]` intake useEffect (manual confirm — no infinite loop).
    - `cd frontend && npm run build` exits 0 (zero TypeScript errors).
  </acceptance_criteria>
  <done>STUB_SPARKBARS removed; useEffect fetches real intake on activePeriod change with zero-fill; dashboard mock + three intake tests added and passing; npm run build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → GET /api/admin/stats/intake?days=N | Authenticated admin supplies `days` query param; behind require_auth middleware in admin_protected_router |
| backend → PostgreSQL | `days` (i32) reaches the aggregate query; must be bound, not interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-intake-dos | Denial of Service | unbounded `days` query param → extreme-range scan | mitigate | Handler clamps `params.days.unwrap_or(14).clamp(1, 90)` BEFORE the DB call (Task 1). Verified by `grep clamp(1, 90)` and the date-bounded WHERE clause. |
| T-intake-sqli | Tampering | `days` injected into SQL via the INTERVAL expression | mitigate | `days` passed as a bound parameter `($1 || ' days')::INTERVAL` via `.bind(days)` — never `format!`/string-interpolated (Task 1, RESEARCH.md Pitfall 1 + Don't Hand-Roll). |
| T-intake-authz | Elevation of Privilege | unauthenticated access to intake stats | mitigate | Route added inside `admin_protected_router` which applies `require_auth`; handler requires `Extension<AuthJwtClaims>` (Task 1). |
| T-intake-SC | Tampering | npm/cargo installs | mitigate | No new packages this phase (RESEARCH.md Package Legitimacy Audit: not applicable; no install tasks present). |
</threat_model>

<verification>
- `cd backend && cargo check` exits 0; `cd backend && cargo test` full suite green (includes intake_sql + attribution tests).
- `cd frontend && npx jest app/admin/lib/__tests__/adminApi.intake.test.ts app/admin/__tests__/dashboard.test.tsx` all pass.
- `cd frontend && npm run build` exits 0 with zero TypeScript errors.
- Manual/integration confidence: with the backend running, `GET /api/admin/stats/intake?days=7` returns JSON `[{day,count}]`; a 1-report system shows at most one bar > 0 on the dashboard across 7D/14D/30D (UAT BUG-03.2-A expected behavior).
</verification>

<success_criteria>
- `GET /api/admin/stats/intake?days=N` exists in admin_protected_router, clamps days to [1,90], returns per-day counts.
- Dashboard fetches real intake data on each 7D/14D/30D change; STUB_SPARKBARS is gone; sparse days zero-fill; a 1-report system shows ≤1 non-zero bar.
- All backend (`cargo test`) and frontend (intake API + dashboard) unit tests pass; `npm run build` and `cargo check` succeed.
- No new dependency, no migration.
</success_criteria>

<output>
Create `.planning/phases/03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback/03.3-02-SUMMARY.md` when done.
</output>
