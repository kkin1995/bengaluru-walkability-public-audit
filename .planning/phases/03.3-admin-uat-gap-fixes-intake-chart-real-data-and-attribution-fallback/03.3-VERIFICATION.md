---
phase: 03.3-admin-uat-gap-fixes-intake-chart-real-data-and-attribution-fallback
verified: 2026-05-30T10:45:00Z
status: human_needed
score: 4/4 must-haves verified
overrides_applied: 0
human_verification:
  - test: "On a running instance with at least one report, open /admin and cycle through 7D/14D/30D tabs"
    expected: "Sparkbars change height based on the selected window and reflect actual per-day report counts — the 14D and 30D views differ, and a system with 1 total report shows at most 1 bar with height > 0 across all windows"
    why_human: "Zero-fill logic and Sparkbars rendering require a browser with real data; automated tests mock getIntakeStats and assert call arguments, not visual bar heights"
  - test: "On a running instance, open /admin/reports/[id] for any report with at least one status history entry"
    expected: "Each status history entry shows 'BY · <admin email>' (e.g. BY · admin@example.com), not 'BY · —'"
    why_human: "The COALESCE fix affects DB output that requires a live PostgreSQL connection; unit tests verify the SQL fragment contains COALESCE but cannot execute against real data"
---

# Phase 03.3: UAT Gap Fixes — Intake Chart Real Data and Attribution Fallback — Verification Report

**Phase Goal:** Fix two UAT-confirmed regressions from Phase 03.2 — BUG-03.2-A (dashboard INTAKE sparkbars show hardcoded stub data instead of real per-day report counts) and BUG-03.2-B (status history attribution always shows "—" because the admin display_name is NULL with no fallback).
**Verified:** 2026-05-30T10:45:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                                                                                                        | Status     | Evidence                                                                                                                                                             |
|----|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1  | The dashboard INTAKE sparkbars reflect actual per-day report intake counts for the selected 7D/14D/30D window — empty days show height 0, and a system with 1 total report shows at most 1 bar with height > 0 (no STUB_SPARKBARS pattern) | ✓ VERIFIED | `grep -c "STUB_SPARKBARS" page.tsx` = 0; `useEffect([activePeriod])` calls `getIntakeStats(dayCount)` at page.tsx:80; dashboard test suite asserts `getIntakeStats(14/7/30)` on each period click |
| 2  | GET /api/admin/stats/intake?days=N returns per-day report counts, clamps days to [1,90], and is behind admin auth                                                                           | ✓ VERIFIED | Route at main.rs:202 inside `admin_protected_router`; handler `admin_get_intake_stats` at handlers/admin.rs:799 with `clamp(1,90)` at line 804; calls `admin_queries::get_intake_stats(&state.pool, days)` |
| 3  | Status history entries on /admin/reports/[id] show "BY · <admin email>" when no display name is set (no longer "BY · —")                                                                    | ✓ VERIFIED | `STATUS_HISTORY_SQL` const at admin_queries.rs:1055 contains `COALESCE(au.display_name, au.email) AS changed_by_name`; bare `au.display_name AS changed_by_name` count = 0; unit test `attribution_fallback_sql_uses_coalesce` passes |
| 4  | `cargo test` and the frontend intake + dashboard test suites pass; `npm run build` and `cargo check` succeed with no new dependency or migration                                            | ✓ VERIFIED | `cargo test`: 241 passed, 0 failed; `cargo check`: exit 0; frontend intake + dashboard: 30 passed, 0 failed; `npm run build`: "Compiled successfully"; no new packages introduced |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact                                                                 | Expected                                                                    | Status     | Details                                                                                                                                      |
|--------------------------------------------------------------------------|-----------------------------------------------------------------------------|------------|----------------------------------------------------------------------------------------------------------------------------------------------|
| `backend/src/models/admin.rs`                                            | IntakeDayCount struct (day: String, count: i64) with Serialize/Deserialize  | ✓ VERIFIED | `struct IntakeDayCount` at line 264 with `#[derive(Debug, Serialize, Deserialize)]`                                                          |
| `backend/src/db/admin_queries.rs`                                        | get_intake_stats(pool, days) + date_trunc + attribution COALESCE + helpers  | ✓ VERIFIED | `INTAKE_SQL` const at line 877 uses `date_trunc`; `get_intake_stats` at line 891; `STATUS_HISTORY_SQL` const at line 1055 uses COALESCE; both test helpers present |
| `backend/src/handlers/admin.rs`                                          | admin_get_intake_stats handler with days clamp [1,90]                       | ✓ VERIFIED | `admin_get_intake_stats` at line 799; `clamp(1, 90)` at line 804                                                                             |
| `backend/src/main.rs`                                                    | /api/admin/stats/intake route in admin_protected_router                     | ✓ VERIFIED | `.route("/api/admin/stats/intake", get(admin_get_intake_stats))` at line 202                                                                  |
| `frontend/app/admin/lib/adminApi.ts`                                     | getIntakeStats(days) + IntakeDayCount interface                              | ✓ VERIFIED | `interface IntakeDayCount` at line 249; `getIntakeStats` at line 258 routing through `apiFetch`                                               |
| `frontend/app/admin/page.tsx`                                            | useEffect fetching real intake data on activePeriod change; STUB_SPARKBARS removed | ✓ VERIFIED | `useEffect([activePeriod])` at line 77; `getIntakeStats(dayCount)` at line 80; `STUB_SPARKBARS` count = 0                                    |
| `frontend/app/admin/lib/__tests__/adminApi.intake.test.ts`               | URL/credentials/parsing tests for getIntakeStats                            | ✓ VERIFIED | 4 tests covering URL pattern (days=14, days=7), parsed IntakeDayCount[] on 200, credentials: include                                         |
| `frontend/app/admin/__tests__/dashboard.test.tsx`                        | 3 new BUG-03.2-A intake tests + getIntakeStats mock added                   | ✓ VERIFIED | `getIntakeStats: jest.fn()` in mock factory (line 34); `beforeEach` default `mockResolvedValue([])` (line 151); 3 intake tests at lines 483–528 |

### Key Link Verification

| From                                                      | To                                        | Via                                                    | Status     | Details                                                                                              |
|-----------------------------------------------------------|-------------------------------------------|--------------------------------------------------------|------------|------------------------------------------------------------------------------------------------------|
| `frontend/app/admin/page.tsx useEffect [activePeriod]`    | GET /api/admin/stats/intake?days=N        | `getIntakeStats(dayCount)` in adminApi.ts              | ✓ WIRED    | page.tsx:80 calls `getIntakeStats(dayCount)`; adminApi.ts:258 builds URL `${BASE}/api/admin/stats/intake?days=${days}` |
| `backend/src/handlers/admin.rs admin_get_intake_stats`    | backend/src/db/admin_queries.rs           | `admin_queries::get_intake_stats(&state.pool, days)`   | ✓ WIRED    | handlers/admin.rs:805 calls `admin_queries::get_intake_stats(&state.pool, days).await?`              |
| `backend/src/main.rs admin_protected_router`              | admin_get_intake_stats handler             | `.route("/api/admin/stats/intake", get(admin_get_intake_stats))` | ✓ WIRED | main.rs:202 — inside admin_protected_router which applies require_auth middleware                    |
| `backend/src/db/admin_queries.rs status history query`    | admin_users.email column                  | COALESCE fallback inside LEFT JOIN on au.id = sh.changed_by | ✓ WIRED | `STATUS_HISTORY_SQL` const uses `COALESCE(au.display_name, au.email) AS changed_by_name`; runtime query at line 1179 references `STATUS_HISTORY_SQL` |

### Data-Flow Trace (Level 4)

| Artifact                              | Data Variable | Source                                       | Produces Real Data | Status     |
|---------------------------------------|---------------|----------------------------------------------|--------------------|------------|
| `frontend/app/admin/page.tsx`         | `intakeData`  | `getIntakeStats(dayCount)` → `/api/admin/stats/intake?days=N` → `get_intake_stats(pool, days)` → DB aggregate on `reports.created_at` | Yes (DB query with date_trunc + GROUP BY on `reports` table) | ✓ FLOWING  |
| `backend/src/db/admin_queries.rs`     | status history `changed_by_name` | `STATUS_HISTORY_SQL` with `COALESCE(au.display_name, au.email)` over LEFT JOIN to `admin_users` | Yes (joins real admin_users table, falls back to email) | ✓ FLOWING  |

### Behavioral Spot-Checks

| Behavior                                            | Command                                                                 | Result                                | Status  |
|-----------------------------------------------------|-------------------------------------------------------------------------|---------------------------------------|---------|
| attribution_fallback test passes                    | `cd backend && cargo test attribution_fallback`                         | 1 passed; 0 failed                    | ✓ PASS  |
| intake_sql test passes                              | `cd backend && cargo test intake_sql`                                   | 1 passed; 0 failed                    | ✓ PASS  |
| Full backend test suite passes                      | `cd backend && cargo test`                                              | 241 passed; 0 failed                  | ✓ PASS  |
| cargo check exits 0                                 | `cd backend && cargo check`                                             | exit 0 ("Finished")                   | ✓ PASS  |
| Frontend intake + dashboard tests pass              | `npx jest adminApi.intake.test.ts dashboard.test.tsx --no-coverage`    | 30 passed; 0 failed                   | ✓ PASS  |
| Frontend build compiles                             | `cd frontend && npm run build`                                          | "Compiled successfully"               | ✓ PASS  |
| STUB_SPARKBARS fully removed                        | `grep -c "STUB_SPARKBARS" frontend/app/admin/page.tsx`                 | 0                                     | ✓ PASS  |
| COALESCE fix present, bare form absent              | grep for COALESCE + grep -c for bare form                              | 1 match (in SQL const); 0 bare forms  | ✓ PASS  |

### Probe Execution

No probe scripts declared or conventionally present for this phase. Behavioral spot-checks above serve as the functional verification layer. Step 7c: SKIPPED (no probe scripts found).

### Requirements Coverage

| Requirement | Source Plan  | Description                                                                                          | Status      | Evidence                                                                                         |
|-------------|--------------|------------------------------------------------------------------------------------------------------|-------------|--------------------------------------------------------------------------------------------------|
| BUG-03.2-B  | 03.3-01-PLAN | Status history attribution always shows "—" because display_name is NULL with no fallback           | ✓ SATISFIED | `COALESCE(au.display_name, au.email) AS changed_by_name` in STATUS_HISTORY_SQL; unit test passes |
| BUG-03.2-A  | 03.3-02-PLAN | Dashboard INTAKE sparkbars show hardcoded stub data instead of real per-day report counts            | ✓ SATISFIED | STUB_SPARKBARS removed; useEffect fetches real data via getIntakeStats; endpoint clamps days to [1,90]; 3 dashboard tests pass |

### Anti-Patterns Found

| File                                          | Line | Pattern                        | Severity | Impact                                                                 |
|-----------------------------------------------|------|--------------------------------|----------|------------------------------------------------------------------------|
| (none)                                        | —    | —                              | —        | No TBD/FIXME/XXX markers, no stub patterns, no hardcoded empty data in any modified file |

React `act()` warnings appeared in the dashboard test output but are pre-existing (they originate from a non-critical `setRecentReports` call in a sibling useEffect, not from the newly added intake useEffect). All 30 tests pass regardless; warnings are not blockers.

**Implementation deviation from plan (informational, not a gap):** The INTAKE_SQL uses `make_interval(days => $1)` rather than the plan's `($1 || ' days')::INTERVAL`. The SUMMARY documents this as an intentional choice (verified by `cargo check`). Both forms are bound parameters — the security properties (T-intake-sqli) are identical. This is not a gap.

### Human Verification Required

Two integration-level checks require a running instance and real data:

#### 1. Sparkbars Reflect Real Data — Visual/Behavioral Check

**Test:** Start the full stack with at least one submitted report. Open `/admin`. Note the bar heights for 14D. Click 7D and observe bar count change to 7. Click 30D and observe bar count change to 30. Verify that bar heights vary based on actual submission dates (not the same pattern repeated). If the system has exactly 1 report, verify that only 1 bar shows a non-zero height across all period windows.
**Expected:** Bar heights correspond to actual daily submission volumes. The 14D and 30D views differ. A 1-report system shows at most 1 bar above zero height. No visual artifact from the former STUB_SPARKBARS pattern (3–9 heights repeating).
**Why human:** Zero-fill logic and Sparkbars rendering require a browser with live data. Automated tests mock `getIntakeStats` and assert call arguments, not bar visual output.

#### 2. Status History Attribution Shows Admin Email — Live DB Check

**Test:** Start the full stack. Log in as the seeded admin (whose `display_name` is NULL). Open any report at `/admin/reports/[id]` that has at least one status history entry. Inspect the "BY ·" text next to each history entry.
**Expected:** Each entry shows `BY · admin@example.com` (the admin's email address), not `BY · —`. If the admin has set a display name, it shows the display name instead.
**Why human:** The COALESCE fix is a SQL change that requires a live PostgreSQL connection with real status_history rows. Unit tests verify the SQL fragment structure but cannot query a real database.

### Gaps Summary

No automated gaps found. All 4 roadmap success criteria are verifiably met in the codebase. Two human verification items require a running instance with real data to confirm integration-level behavior — this is expected for database and UI rendering checks.

---

_Verified: 2026-05-30T10:45:00Z_
_Verifier: Claude (gsd-verifier)_
