---
phase: 02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch
verified: 2026-04-25T21:00:00Z
status: human_needed
score: 13/13 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 8/8
  gaps_closed:
    - "WARNING resolved: Confirm review card now shows getCategoryLabel(form.category).en instead of raw enum"
    - "GAP 1: zoomControl={false} on MapContainer — Leaflet +/- controls no longer overlap back button"
    - "GAP 2: ReportCTA + photo-store — home page CTA opens camera directly; /report skips photo step when photo stored"
    - "GAP 3: Confirm review card shows human-readable category label via getCategoryLabel"
    - "GAP 4: Contact accordion is interactive (role=button, onClick, showContact, expand/collapse with name/email)"
    - "GAP 5: SuccessCard wrapped in maxWidth: 428 container consistent with other report steps"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Render all 5 screens at 390px mobile width and compare against CONTEXT.md design spec"
    expected: "Colors match CSS variable spec (--bg: #fafaf9, --accent: oklch(0.62 0.14 145)), typography is Inter/JetBrains Mono/Noto Sans Kannada, layout matches design file"
    why_human: "CSS visual regression not automated; jsdom cannot evaluate oklch() colors, next/font font loading, or tap-target pixel dimensions"
  - test: "Open the report flow on iOS Safari — tap Take Photo label on home page (ReportCTA)"
    expected: "OS camera opens directly (capture=environment on home-page file input); photo captured; /report auto-advances to category step with thumbnail visible; Upload from Gallery on /report opens photo library directly"
    why_human: "capture=environment behavior is device/OS-specific; cross-page photo-store handoff requires real navigation (router.push cannot be simulated end-to-end in Jest)"
  - test: "Submit a report end-to-end on ui-redesign branch (home CTA photo, category, confirm, submit)"
    expected: "SuccessCard renders with a real report ID returned by the backend; report appears on the public map at /map; confirm review card shows human-readable category label (e.g. 'Damaged Footpath') not raw enum"
    why_human: "Requires live Rust backend + PostgreSQL/PostGIS; Jest mocks the fetch call; getCategoryLabel fix is tested in Jest but confirmation in live flow requires browser"
  - test: "Verify GPS pulsing pill animation on category screen after photo capture with EXIF GPS data"
    expected: "Pulsing green dot animates (pulse CSS class, @keyframes pulse-soft) alongside coordinates in mono font; no modal prompt"
    why_human: "CSS animation cannot be evaluated in jsdom"
  - test: "Verify filter chips on map page are scrollable and active chip toggles ink fill"
    expected: "Horizontal chip strip scrolls on small screens without visible scrollbar (.no-scrollbar); active chip background transitions to var(--ink) with white text; no Leaflet +/- zoom buttons visible (zoomControl={false})"
    why_human: "Scrollbar-width:none behavior, CSS transitions, and Leaflet rendering require real browser"
---

# Phase 02.3.1: Implement Walkable BLR UI Redesign — Re-Verification Report (After Plan 04 Gap Closure)

**Phase Goal:** On a dedicated `ui-redesign` branch, implement a pixel-faithful Next.js redesign of the 5 citizen-facing screens (Home, Category step, Confirm step, Success, Public Map) per the Walkable BLR design file — introducing a CSS-variable design system, Google Fonts via next/font, and 5 TSX primitives (Bi, Icon, Btn, Pill, SectionLabel) — without merging to main.

**Verified:** 2026-04-25T21:00:00Z
**Status:** human_needed
**Re-verification:** Yes — after Plan 04 UAT gap closure

---

## Goal Achievement

### Re-verification Scope

Previous verification (2026-04-25T20:00:00Z) returned `human_needed` with 8/8 automated truths VERIFIED and one WARNING (raw enum in confirm review card). Plan 04 (UAT gap closure) has since been executed. This re-verification:

- Performs full 3-level verification on all 5 Plan 04 must-haves (new items that failed UAT)
- Confirms the previous WARNING was resolved
- Does quick regression checks on the 8 previously-VERIFIED truths

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A dedicated `ui-redesign` branch exists and is NOT merged to main | VERIFIED | `git branch --show-current` = `ui-redesign`; branch is active |
| 2 | `globals.css` contains the full CSS variable design system (28 variables, oklch accent) and 10 utility classes | VERIFIED | `:root` block with --bg, --accent oklch(0.62 0.14 145), all radii, shadows, font vars; .bi, .press, .no-scrollbar, .pulse, .map-tile, .img-placeholder present |
| 3 | `layout.tsx` loads Inter, JetBrains Mono, Noto Sans Kannada via next/font/google in CSS variable mode | VERIFIED | Inter, JetBrains_Mono, Noto_Sans_Kannada imports confirmed with variable mode and correct weights |
| 4 | 5 TSX primitives (Bi, Icon, Btn, Pill, SectionLabel) exist with correct interfaces and all 32 icons | VERIFIED | All 5 primitives exist; Icon confirmed 32 names; Btn 4 variants × 4 sizes; Pill 5 tones |
| 5 | Home page (`/`) delivers Namma ದಾರಿ brand, "Fix the footpath." hero, map preview, CTA, trust sub-copy — now via ReportCTA (file input, not Link+Btn) | VERIFIED | "Namma", "Fix the footpath.", "Report an issue"/"ಸಮಸ್ಯೆ ವರದಿ ಮಾಡಿ" present; ReportCTA imported and rendered at line 212; Btn/Bi imports removed from home page as unused |
| 6 | Map page (`/map`) delivers full-bleed ReportsMap with overlay bar, filter chips, floating CTA; Leaflet zoom buttons disabled | VERIFIED | `zoomControl={false}` at ReportsMap.tsx line 111; overlay bar, 6 FILTERS, "Report here" CTA all present |
| 7 | Report flow is a 2-step flow (photo → category → confirm → success) preserving all functional behavior; confirm review card shows human-readable category label; contact accordion is interactive; SuccessCard in 428px container | VERIFIED | Step union type confirmed; getCategoryLabel(form.category).en at line 695; showContact state + role="button" + aria-expanded + expand/collapse + name/email fields at lines 837-900; SuccessCard wrapped in main maxWidth:428 at lines 266-283; honeypot present on all 3 steps (lines 424, 591, 968); exifr.gps before compress; isInBengaluru bounds guard |
| 8 | CategoryGrid, SeverityGrid, SuccessCard exist under redesign/ and are wired into report page | VERIFIED | All 3 files present; all imported in report/page.tsx; CategoryGrid/SeverityGrid wired with value+onChange; SuccessCard rendered on submittedReportId !== null |
| 9 | ReportsMap.tsx has zoomControl={false} on MapContainer (GAP 1) | VERIFIED | Line 111: `zoomControl={false}` on MapContainer element |
| 10 | photo-store.ts browser singleton exports storePendingPhoto and consumePendingPhoto with correct PendingPhoto interface (GAP 2 — new file) | VERIFIED | File exists at frontend/app/lib/photo-store.ts; exports storePendingPhoto (line 16), consumePendingPhoto (line 20); PendingPhoto interface has all 6 fields: file, previewUrl, lat, lng, locationSource, gpsConfirmed |
| 11 | ReportCTA.tsx is a client component with hidden file input (capture=environment), exifr extraction, compression, photo-store write, and router.push (GAP 2 — new file) | VERIFIED | "use client" at line 1; import storePendingPhoto at line 8; capture="environment" at line 123; exifr require with UMD compat guard (exifrModule.default ?? exifrModule) at lines 57-58; quality loop [0.85, 0.75, 0.65, 0.55, 0.45, 0.4] at line 29; router.push("/report") at lines 79 and 93; storePendingPhoto called at line 91 |
| 12 | consumePendingPhoto() called in report/page.tsx mount useEffect; setStep("category") called if pending photo found (GAP 2 — wiring) | VERIFIED | useEffect at lines 127-141 with empty dep array; consumePendingPhoto() at line 128; setStep("category") at line 139 |
| 13 | All 736 tests pass with no regressions after Plan 04 changes | VERIFIED | `npm test -- --passWithNoTests`: 736/736 pass, 41 suites, no failures |

**Score:** 13/13 truths verified (automated)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/app/globals.css` | Full :root CSS variable block + 10 utility classes | VERIFIED | 28 custom properties, additive (existing @tailwind + .leaflet-container preserved) |
| `frontend/app/layout.tsx` | next/font/google loading 3 fonts in variable mode | VERIFIED | Inter, JetBrains_Mono, Noto_Sans_Kannada with weights and --font-* CSS variable names |
| `frontend/app/components/ui/Bi.tsx` | Bilingual stacked text primitive | VERIFIED | .bi/.bi-en/.bi-kn classes; optional kn prop |
| `frontend/app/components/ui/Icon.tsx` | 32-icon inline SVG primitive | VERIFIED | 32 icons including all cat_* category icons |
| `frontend/app/components/ui/Btn.tsx` | 4 variants × 4 sizes button primitive | VERIFIED | .press class; disabled states; correct min-heights |
| `frontend/app/components/ui/Pill.tsx` | 5-tone pill primitive | VERIFIED | neutral/accent/ink/glass/warn tones |
| `frontend/app/components/ui/SectionLabel.tsx` | Uppercase mono section header | VERIFIED | 11px, 0.08em letter-spacing, uppercase |
| `frontend/app/page.tsx` | Redesigned home page using ReportCTA (not Link+Btn) | VERIFIED | ReportCTA at line 212; Btn/Bi imports removed; Link (for /map) and Icon/Pill/SectionLabel retained |
| `frontend/app/map/page.tsx` | Redesigned map page with overlay and zoomControl disabled | VERIFIED | Full-bleed ReportsMap, overlay bar, 6 filter chips, floating CTA; zoomControl={false} in ReportsMap |
| `frontend/app/report/page.tsx` | 2-step report flow with GAP 2/3/4/5 fixes | VERIFIED | consumePendingPhoto mount effect; getCategoryLabel in review card; interactive contact accordion; SuccessCard maxWidth 428 wrapper; 3 honeypot inputs |
| `frontend/app/components/redesign/CategoryGrid.tsx` | 3-column 6-chip radio grid | VERIFIED | 6 enum-matching categories, active ink fill, check badge, Bi labels |
| `frontend/app/components/redesign/SeverityGrid.tsx` | 3-severity radio grid with semantic colors | VERIFIED | low/medium/high with accent/warn/danger tokens |
| `frontend/app/components/redesign/SuccessCard.tsx` | Success screen with share + report-another | VERIFIED | check_circle, "Thank you. It's on the map.", Share/Report another CTAs |
| `frontend/app/lib/photo-store.ts` | Browser singleton for photo handoff | VERIFIED | storePendingPhoto, consumePendingPhoto, PendingPhoto interface |
| `frontend/app/components/ReportCTA.tsx` | Client component with file input CTA | VERIFIED | "use client", capture=environment, storePendingPhoto, router.push |
| `design-ref/` directory | 5 design source files preserved | VERIFIED | primitives.jsx, styles.css, screens-capture.jsx, screen-category.jsx, screens-confirm.jsx, README.md |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `globals.css :root` | `layout.tsx` fonts | --font-inter/--font-jetbrains-mono/--font-noto-sans-kannada CSS vars | WIRED | var(--font-sans)=var(--font-inter); fonts loaded in layout.tsx applied to html className |
| `page.tsx` (Home) | `ReportCTA` | import + render in CTA section | WIRED | import at line 5; `<ReportCTA />` at line 212 |
| `ReportCTA.tsx` | `photo-store.ts` | storePendingPhoto() on file selection | WIRED | import at line 8; storePendingPhoto called at line 91 before router.push |
| `report/page.tsx` | `photo-store.ts` | consumePendingPhoto() in useEffect on mount | WIRED | import at line 9; useEffect at lines 127-141 with empty dep array; setStep("category") if pending |
| `ReportsMap.tsx` | Leaflet MapContainer | zoomControl={false} prop | WIRED | Line 111 confirmed; Leaflet +/- controls suppressed |
| `report/page.tsx` review card | `getCategoryLabel` | getCategoryLabel(form.category).en | WIRED | import at line 8; used at line 695 |
| Contact row | `showContact` state | role=button onClick setShowContact | WIRED | aria-expanded={showContact} at line 841; name/email fields shown when showContact=true at line 867 |
| `map/page.tsx` | `ReportsMap` | nextDynamic(ssr:false) | WIRED | ReportsMap receives apiUrl={API_BASE_URL}; renders in absolute inset:0 div |
| `report/page.tsx` | API backend | fetch(${API_BASE_URL}/api/reports, FormData) | WIRED | FormData with photo, lat, lng, category, severity, location_source, website honeypot |
| `ui-redesign` branch | main branch | NOT merged (branch isolation goal) | WIRED | Branch active; not in `git branch --merged main` output |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `report/page.tsx` SuccessCard | `submittedReportId` | fetch /api/reports response body `.id` | Yes — real backend response | FLOWING |
| `report/page.tsx` GPS pill | `form.gpsConfirmed`, `form.lat/lng` | exifr.gps(originalFile) before compression | Yes — real EXIF data from user device | FLOWING |
| `report/page.tsx` review card | `getCategoryLabel(form.category).en` | Translations lookup — static map keyed by enum | Yes — human-readable string, not raw enum | FLOWING |
| `ReportCTA.tsx` → `report/page.tsx` | `pending.file`, `pending.lat/lng` | photo-store singleton (module variable) | Yes — real File object from user's file input; EXIF GPS from exifr | FLOWING |
| `map/page.tsx` filter chips | `activeFilter` useState | User onClick interaction | N/A — display state; filter→ReportsMap wiring documented as deferred | WIRED (visual state only) |
| `page.tsx` map preview dots | MAP_DOTS | Constant (10 positions) | N/A — preview scatter; intentional (avoids hydration mismatch) | INFO |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite (736 tests) | `cd frontend && npm test -- --passWithNoTests` | 736/736 pass, 41 suites | PASS |
| zoomControl={false} in ReportsMap | `grep "zoomControl={false}" frontend/app/components/ReportsMap.tsx` | Line 111 | PASS |
| photo-store exports both functions | `grep "storePendingPhoto\|consumePendingPhoto" frontend/app/lib/photo-store.ts` | Lines 16, 20 | PASS |
| ReportCTA has camera capture | `grep 'capture="environment"' frontend/app/components/ReportCTA.tsx` | Line 123 | PASS |
| Home page uses ReportCTA | `grep "ReportCTA" frontend/app/page.tsx` | Lines 5, 212 | PASS |
| getCategoryLabel in confirm review card | `grep "getCategoryLabel(form.category).en" frontend/app/report/page.tsx` | Line 695 | PASS |
| Contact accordion interactive | `grep "role=\"button\"\|aria-expanded\|showContact" frontend/app/report/page.tsx` | Lines 113, 837, 841, 867 | PASS |
| SuccessCard in maxWidth:428 wrapper | `grep "maxWidth: 428" report/page.tsx` near `submittedReportId !== null` | Lines 269, 293, 441, 607 (multiple steps each have their own wrapper) | PASS |
| Honeypot intact on all 3 steps | `grep -n 'name="website"' frontend/app/report/page.tsx` | Lines 239 (querySelector), 424, 591, 968 (3 input elements) | PASS |
| consumePendingPhoto mount useEffect | `useEffect` with empty dep array at lines 127-141 | setStep("category") called when pending photo found | PASS |
| Branch not merged to main | `git branch --show-current` | `ui-redesign` | PASS |

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| UI-FOUNDATION | Plan 01 | CSS-variable design system + next/font + 5 primitives on ui-redesign branch | SATISFIED | globals.css :root block with 28 vars, layout.tsx next/font/google wiring, all 5 primitives exist and substantive |
| UI-HOME | Plans 02, 04 | Home page redesign per design spec + ReportCTA file-input CTA | SATISFIED | page.tsx: Namma ದಾರಿ brand, "Fix the footpath." h1, map preview scatter, ReportCTA file input (not Link+Btn), trust sub-copy |
| UI-MAP | Plans 02, 04 | Map page overlay redesign + zoom controls disabled | SATISFIED | map/page.tsx: full-bleed ReportsMap, overlay bar, 6-chip filter strip, floating "Report here" CTA; zoomControl={false} in ReportsMap |
| UI-REPORT-FLOW | Plans 03, 04 | 2-step report flow with functional behavior + UAT gaps closed | SATISFIED | report/page.tsx: photo-store mount bypass, getCategoryLabel human label, interactive contact accordion, SuccessCard 428px wrapper, honeypot on all steps, EXIF extraction, bounds guard, FormData submission |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `map/page.tsx` | 44-56 | Filter chips not wired to ReportsMap `category` prop | INFO | Active filter state exists but does not propagate to ReportsMap. Explicitly documented as display-only in Plan 02 SUMMARY and CONTEXT.md deferred section. Does not block phase goal. |
| `page.tsx` | 9-20 | MAP_DOTS: hardcoded 10-dot scatter for home page preview | INFO | Not real report data. Design-correct — home page shows a preview; live map is at /map. Prevents hydration mismatch (documented decision). |

**Previous WARNING resolved:** The raw category enum display in the confirm review card (WARNING in previous verification) is now fixed. `getCategoryLabel(form.category).en` at line 695 shows "Damaged Footpath" instead of "broken_footpath". Test `Damaged Footpath` assertion in report-page.test.tsx confirms.

**No blocker anti-patterns found.**

---

### Human Verification Required

All automated checks confirm structural and behavioral requirements are met. The following items require a real device or browser:

**1. Visual design fidelity across all 5 screens**

**Test:** Open `http://localhost:3000` (with frontend running on ui-redesign branch) at 390px viewport width. Navigate through Home → tap the camera CTA (ReportCTA) → Category step → Confirm step → Success screen → `/map`.
**Expected:** Colors match CSS variable spec (#fafaf9 background, oklch green accent), typography is Inter for body/headings, JetBrains Mono for coordinates and mono labels, Noto Sans Kannada for ಕನ್ನಡ text; warm-toned shadows and correct border radii throughout all 5 screens.
**Why human:** CSS visual regression is not automated; jsdom cannot render oklch() colors, evaluate next/font font loading, or measure rendered pixel dimensions.

**2. iOS Safari camera UX (home CTA + report page)**

**Test:** On an iPhone, tap the "Report an issue" button on the home page (ReportCTA). Also navigate directly to `/report` and tap "Take Photo" and "Upload from Gallery".
**Expected:** Home-page CTA opens the OS camera directly (capture=environment); after capture, /report auto-starts at the category step with the photo thumbnail visible (photo-store handoff). On /report directly: "Take Photo" opens camera; "Upload from Gallery" opens photo library (no capture attribute).
**Why human:** capture=environment device behavior and cross-page photo-store handoff require real navigation through the App Router; cannot be simulated end-to-end in Jest.

**3. End-to-end report submission with category label verification**

**Test:** Submit a complete report (home CTA photo capture → Damaged category → Confirm step check → submit).
**Expected:** Confirm step review card shows "Damaged Footpath" (not "broken_footpath"). SuccessCard renders inside a centered 428px max-width container with a real UUID report ID. Report appears on `/map` as a colored pin.
**Why human:** Requires live Rust backend + PostgreSQL/PostGIS; Jest mocks fetch; the maxWidth container is grep-verified in source but visual centering requires browser.

**4. GPS pulsing dot animation on category screen**

**Test:** Capture a photo with EXIF GPS data (on a device that embeds GPS), proceed to category screen.
**Expected:** GPS pill shows a pulsing green dot (.pulse class, @keyframes pulse-soft, 2s ease-in-out infinite) alongside coordinates in JetBrains Mono. No modal or GPS confirmation step.
**Why human:** CSS animation cannot be evaluated in jsdom.

**5. Map page: no zoom controls, scrollable chips, active chip state**

**Test:** Open `/map` at 375px width. Verify Leaflet map area. Swipe the filter chip strip horizontally. Tap a category chip.
**Expected:** No Leaflet +/- zoom buttons visible (zoomControl={false}). No scrollbar visible on chip strip (.no-scrollbar). Scrolling works via touch. Active chip background transitions to var(--ink) with white text.
**Why human:** Leaflet rendering, scrollbar-width:none behavior, and CSS transitions require real browser rendering.

---

### Gaps Summary

No structural gaps. All 13 observable truths verified. All required artifacts exist, are substantive, and are wired. All 5 Plan 04 UAT gaps are closed in the codebase. Full test suite passes 736/736.

Status is `human_needed` because visual design fidelity, iOS camera behavior, cross-page photo-store handoff, GPS animation, and live end-to-end submission cannot be verified without a real browser and device.

The phase goal is structurally achieved: the `ui-redesign` branch contains a complete Next.js implementation of all 5 citizen-facing screens with the CSS-variable design system, Google Fonts via next/font, 5 TSX primitives, and all UAT gaps closed — without merging to main.

---

_Verified: 2026-04-25T21:00:00Z_
_Verifier: Claude (gsd-verifier)_
