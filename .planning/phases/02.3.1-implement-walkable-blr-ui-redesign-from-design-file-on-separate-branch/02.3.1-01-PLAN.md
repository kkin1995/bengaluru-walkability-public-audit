---
phase: 02.3.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/globals.css
  - frontend/app/layout.tsx
  - frontend/app/components/ui/Bi.tsx
  - frontend/app/components/ui/Icon.tsx
  - frontend/app/components/ui/Btn.tsx
  - frontend/app/components/ui/Pill.tsx
  - frontend/app/components/ui/SectionLabel.tsx
  - frontend/app/components/ui/__tests__/Bi.test.tsx
  - frontend/app/components/ui/__tests__/Icon.test.tsx
  - frontend/app/components/ui/__tests__/Btn.test.tsx
  - frontend/app/components/ui/__tests__/Pill.test.tsx
  - frontend/app/components/ui/__tests__/SectionLabel.test.tsx
  - design-ref/primitives.jsx
  - design-ref/styles.css
  - design-ref/screens-capture.jsx
  - design-ref/screen-category.jsx
  - design-ref/screens-confirm.jsx
autonomous: true
requirements:
  - UI-FOUNDATION (phase-local: CSS variables, fonts, UI primitives — sourced from CONTEXT.md decisions)
must_haves:
  truths:
    - "Working on branch `ui-redesign` created from `phase-02.3-uat-fixes`"
    - "Design source files preserved in repo under `design-ref/` so /tmp/ clearing does not lose them"
    - "`globals.css` contains the full `:root` block with --bg, --accent, --ink, radii, shadows, and font variables"
    - "`layout.tsx` loads Inter, JetBrains_Mono, Noto_Sans_Kannada via next/font/google with variable mode"
    - "`body { font-family: var(--font-sans) }` is applied globally so pages inherit the redesign typography"
    - "5 UI primitives exist in `frontend/app/components/ui/` (Bi, Icon, Btn, Pill, SectionLabel) with typed exports"
    - "Icon component contains all 32 required icon names transcribed from primitives.jsx"
    - "Unit tests for every primitive pass (`npm test -- --testPathPattern='components/ui' --passWithNoTests`)"
    - "`cd frontend && npm run build` succeeds on branch ui-redesign (no TypeScript / build regressions)"
  artifacts:
    - path: "design-ref/primitives.jsx"
      provides: "Preserved design source — icon SVG paths reference"
      min_lines: 140
    - path: "frontend/app/globals.css"
      provides: "CSS variables, typography, utility classes for redesign"
      contains: "--accent"
    - path: "frontend/app/layout.tsx"
      provides: "next/font loading and html className wiring"
      contains: "next/font/google"
    - path: "frontend/app/components/ui/Bi.tsx"
      provides: "Bilingual stacked text span"
      exports: ["Bi"]
    - path: "frontend/app/components/ui/Icon.tsx"
      provides: "Inline SVG icon primitive"
      exports: ["Icon", "IconName"]
    - path: "frontend/app/components/ui/Btn.tsx"
      provides: "Button primitive with variant and size"
      exports: ["Btn"]
    - path: "frontend/app/components/ui/Pill.tsx"
      provides: "Pill/chip with tone variants"
      exports: ["Pill"]
    - path: "frontend/app/components/ui/SectionLabel.tsx"
      provides: "Uppercase mono section header"
      exports: ["SectionLabel"]
  key_links:
    - from: "frontend/app/layout.tsx"
      to: "globals.css :root font variables"
      via: "inter.variable / jetbrains_mono.variable / noto_sans_kannada.variable applied to <html> className"
      pattern: "inter\\.variable"
    - from: "frontend/app/globals.css"
      to: "next/font variables"
      via: "--font-sans: var(--font-inter) ..."
      pattern: "var\\(--font-inter\\)"
---

<objective>
Establish the Walkable BLR design system foundation on a new `ui-redesign` branch — CSS variables, Google Fonts via next/font, and 5 TSX primitives (Bi, Icon, Btn, Pill, SectionLabel) that every subsequent screen redesign will compose.

Purpose: Downstream plans (02 Home+Map, 03 Report flow) must build screens using a consistent set of primitives. Without the primitives and design system, every screen plan would duplicate CSS variable definitions and reinvent the same button/icon/pill markup. Foundation first = clean downstream consumption.

Output:
- Branch `ui-redesign` created (checked out from `phase-02.3-uat-fixes`)
- Design source files copied into `design-ref/` (survives /tmp/ wipe)
- `globals.css` extended additively with `:root` design system block
- `layout.tsx` loads three Google Fonts via `next/font/google`
- 5 primitives in `frontend/app/components/ui/` with tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch/02.3.1-CONTEXT.md
@.planning/phases/02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch/02.3.1-RESEARCH.md
@.planning/phases/02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch/02.3.1-UI-SPEC.md

<interfaces>
<!-- Key types the executor needs. Executor uses these directly — no codebase exploration needed. -->

### Existing primitives to MATCH (from BilingualText — keep contract similar)
```typescript
// frontend/app/components/BilingualText.tsx (existing, do NOT modify)
interface BilingualTextProps {
  en: string;
  kn: string;
  enClass?: string;
  knClass?: string;
  containerClass?: string;
}
```

### NEW primitives this plan creates (exact signatures — executor must match)

```typescript
// frontend/app/components/ui/Bi.tsx
interface BiProps {
  en: string;
  kn?: string;
  className?: string;
  style?: React.CSSProperties;
}
export function Bi(props: BiProps): JSX.Element;
```

```typescript
// frontend/app/components/ui/Icon.tsx
export type IconName =
  | 'camera' | 'map' | 'pin' | 'arrow_right' | 'arrow_left'
  | 'check' | 'check_circle' | 'close' | 'image' | 'flash'
  | 'flip' | 'crosshair' | 'edit' | 'send' | 'chevron_right'
  | 'chevron_down' | 'share' | 'grid' | 'list' | 'filter'
  | 'globe' | 'menu' | 'shield' | 'mic'
  | 'cat_no_path' | 'cat_broken' | 'cat_blocked' | 'cat_crossing'
  | 'cat_lighting' | 'cat_other' | 'cat_ramp' | 'cat_encroach';

interface IconProps {
  name: IconName;
  size?: number;           // default 24
  className?: string;
  style?: React.CSSProperties;
  'aria-label'?: string;   // optional — if provided, remove aria-hidden
}
export function Icon(props: IconProps): JSX.Element;
```

```typescript
// frontend/app/components/ui/Btn.tsx
type BtnVariant = 'primary' | 'accent' | 'secondary' | 'ghost';
type BtnSize = 'sm' | 'md' | 'lg' | 'xl';
interface BtnProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: BtnVariant;  // default 'primary'
  size?: BtnSize;         // default 'md'
  children: React.ReactNode;
}
export function Btn(props: BtnProps): JSX.Element;
```

```typescript
// frontend/app/components/ui/Pill.tsx
type PillTone = 'neutral' | 'accent' | 'ink' | 'glass' | 'warn';
interface PillProps {
  tone?: PillTone;       // default 'neutral'
  children: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
}
export function Pill(props: PillProps): JSX.Element;
```

```typescript
// frontend/app/components/ui/SectionLabel.tsx
interface SectionLabelProps {
  children: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
}
export function SectionLabel(props: SectionLabelProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ui-redesign branch and preserve design source files</name>
  <read_first>
    - CLAUDE.md (project structure)
    - .planning/phases/02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch/02.3.1-CONTEXT.md (branch strategy section)
    - .planning/phases/02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch/02.3.1-RESEARCH.md (Environment Availability section — warns /tmp/ is ephemeral)
  </read_first>
  <files>
    - design-ref/primitives.jsx
    - design-ref/styles.css
    - design-ref/screens-capture.jsx
    - design-ref/screen-category.jsx
    - design-ref/screens-confirm.jsx
    - design-ref/README.md
  </files>
  <action>
    Create a new git branch for all UI redesign work and copy design source files from /tmp/ into the repo so they survive tmp wipes and are available to downstream plans.

    1. Create branch and switch to it (from current branch `phase-02.3-uat-fixes`):
       ```
       git checkout -b ui-redesign
       ```
       Verify: `git branch --show-current` returns `ui-redesign`.

    2. Create `design-ref/` directory at repo root.

    3. Copy the 5 design source files from /tmp/ into design-ref/:
       ```
       mkdir -p design-ref
       cp /tmp/tmp.IzVB7XLmLV/bengaluru-walkability-audit-app/project/primitives.jsx     design-ref/primitives.jsx
       cp /tmp/tmp.IzVB7XLmLV/bengaluru-walkability-audit-app/project/styles.css         design-ref/styles.css
       cp /tmp/tmp.IzVB7XLmLV/bengaluru-walkability-audit-app/project/screens-capture.jsx design-ref/screens-capture.jsx
       cp /tmp/tmp.IzVB7XLmLV/bengaluru-walkability-audit-app/project/screen-category.jsx design-ref/screen-category.jsx
       cp /tmp/tmp.IzVB7XLmLV/bengaluru-walkability-audit-app/project/screens-confirm.jsx design-ref/screens-confirm.jsx
       ```

    4. Create `design-ref/README.md` with this exact content:
       ```markdown
       # Walkable BLR — Design Source Files

       These are the canonical JSX/CSS sources for the Phase 02.3.1 UI redesign.
       Originally exported from claude.ai/design as `Walkable BLR Redesign.html`.

       Files preserved here because the original lived under `/tmp/` which is ephemeral.

       - `styles.css` — full CSS variable system (the design system)
       - `primitives.jsx` — Bi, Icon, Btn, Pill, SectionLabel source + all 32 icon SVG paths
       - `screens-capture.jsx` — HomeScreen + CaptureScreen prototype
       - `screen-category.jsx` — CategoryScreen prototype (Step 1 of 2)
       - `screens-confirm.jsx` — ConfirmScreen + SuccessScreen + MapScreen prototypes

       The Next.js implementation lives under `frontend/app/` — these files are reference only.
       ```

    5. Commit the design-ref directory:
       ```
       git add design-ref/
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(02.3.1): preserve design source files in design-ref/" --files design-ref/
       ```

    IMPORTANT:
    - Do NOT merge to main at any point during this phase (CONTEXT.md Branch Strategy locks this).
    - Do NOT delete or modify files in `frontend/app/admin/*` — admin pages are explicitly OUT OF SCOPE.
  </action>
  <verify>
    <automated>git branch --show-current | grep -x "ui-redesign" &amp;&amp; test -f design-ref/primitives.jsx &amp;&amp; test -f design-ref/styles.css &amp;&amp; test -f design-ref/screens-capture.jsx &amp;&amp; test -f design-ref/screen-category.jsx &amp;&amp; test -f design-ref/screens-confirm.jsx &amp;&amp; test -f design-ref/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` outputs exactly `ui-redesign`
    - `test -f design-ref/primitives.jsx` exits 0
    - `test -f design-ref/styles.css` exits 0
    - `test -f design-ref/screens-capture.jsx` exits 0
    - `test -f design-ref/screen-category.jsx` exits 0
    - `test -f design-ref/screens-confirm.jsx` exits 0
    - `test -f design-ref/README.md` exits 0
    - `wc -l design-ref/primitives.jsx` reports >= 140 lines
    - `grep -q "Walkable BLR" design-ref/README.md` exits 0
    - `git log --oneline -1` contains the message "docs(02.3.1): preserve design source files in design-ref/"
  </acceptance_criteria>
  <done>Branch ui-redesign is active and design source files are committed under design-ref/</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend globals.css with design system + wire next/font in layout.tsx</name>
  <read_first>
    - frontend/app/globals.css (current state — only 9 lines, @tailwind directives + leaflet overrides)
    - frontend/app/layout.tsx (current state — no custom fonts)
    - design-ref/styles.css (canonical source — copy CSS variable values verbatim)
    - .planning/phases/02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch/02.3.1-UI-SPEC.md (Design System Variables, Font Loading Contract, CSS utility classes sections)
    - .planning/phases/02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch/02.3.1-RESEARCH.md (Pattern 1 and Pattern 2 — additive injection and next/font variable mode)
    - frontend/app/__tests__/layout.test.ts (verify no regressions)
  </read_first>
  <files>
    - frontend/app/globals.css
    - frontend/app/layout.tsx
  </files>
  <behavior>
    - Test 1: `globals.css` contains `--accent: oklch(0.62 0.14 145)` on the :root block
    - Test 2: `globals.css` contains `--bg: #fafaf9` on the :root block
    - Test 3: `globals.css` defines `body { font-family: var(--font-sans); ... }`
    - Test 4: `globals.css` contains all 5 utility classes (.press, .no-scrollbar, .pulse, .mono, .kn, .bi, .bi-kn, .map-tile, .img-placeholder)
    - Test 5: `layout.tsx` imports `Inter`, `JetBrains_Mono`, `Noto_Sans_Kannada` from `next/font/google`
    - Test 6: `layout.tsx` applies `inter.variable`, `jetbrainsMono.variable`, `notoSansKannada.variable` to `<html>` className
    - Test 7: `cd frontend && npm run build` succeeds with no TypeScript errors
  </behavior>
  <action>
    Extend the frontend design system non-breakingly. CSS variables and utility classes are additive — existing Tailwind classes are unaffected.

    ### Step 1 — Append to `frontend/app/globals.css` (preserve existing Tailwind directives and Leaflet block at top)

    Append AFTER the existing content (do NOT remove `@tailwind base/components/utilities` or the `.leaflet-container` rule). Append this block verbatim:

    ```css
    /* ─────────────────────────────────────────────────────────────────────
       Walkable BLR Design System (Phase 02.3.1 — citizen-facing redesign)
       Additive: CSS custom properties do not affect existing Tailwind classes.
       Source of truth: design-ref/styles.css
       ───────────────────────────────────────────────────────────────────── */
    :root {
      /* Neutrals — warm, subtly toned */
      --bg: #fafaf9;
      --surface: #ffffff;
      --surface-2: #f5f5f4;
      --border: #e7e5e4;
      --border-strong: #d6d3d1;
      --ink: #1c1917;
      --ink-2: #44403c;
      --muted: #78716c;
      --muted-2: #a8a29e;

      /* Single civic accent — muted green */
      --accent: oklch(0.62 0.14 145);
      --accent-ink: oklch(0.42 0.14 145);
      --accent-bg: oklch(0.96 0.03 145);
      --accent-border: oklch(0.88 0.06 145);

      /* Semantic — shared chroma/lightness, only hue varies */
      --danger: oklch(0.62 0.14 30);
      --danger-bg: oklch(0.96 0.03 30);
      --warn: oklch(0.72 0.14 75);
      --warn-bg: oklch(0.96 0.03 75);

      /* Type — references next/font CSS variables set in layout.tsx */
      --font-sans: var(--font-inter), -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      --font-mono: var(--font-jetbrains-mono), ui-monospace, SFMono-Regular, monospace;
      --font-kn: var(--font-noto-sans-kannada), var(--font-sans);

      /* Radii */
      --r-sm: 8px;
      --r-md: 12px;
      --r-lg: 16px;
      --r-xl: 20px;
      --r-full: 999px;

      /* Shadows — warm-toned, subtle */
      --shadow-sm: 0 1px 2px rgba(28, 25, 23, 0.04), 0 1px 3px rgba(28, 25, 23, 0.06);
      --shadow-md: 0 4px 12px rgba(28, 25, 23, 0.06), 0 2px 6px rgba(28, 25, 23, 0.04);
      --shadow-lg: 0 12px 32px rgba(28, 25, 23, 0.10), 0 4px 12px rgba(28, 25, 23, 0.06);
    }

    /* Apply design-system typography globally. Admin pages also inherit Inter —
       this is acceptable (existing Tailwind system stack is replaced by a better sans). */
    body {
      font-family: var(--font-sans);
      color: var(--ink);
      background: var(--bg);
      font-feature-settings: "cv02", "cv03", "cv04", "cv11";
      -webkit-font-smoothing: antialiased;
    }

    /* Utility classes — transcribed from design-ref/styles.css */
    .mono { font-family: var(--font-mono); letter-spacing: -0.01em; }
    .kn   { font-family: var(--font-kn); }

    .bi {
      display: flex;
      flex-direction: column;
      gap: 2px;
      line-height: 1.15;
      align-items: inherit;
      white-space: nowrap;
      min-width: 0;
    }
    .bi-en { font-weight: inherit; white-space: nowrap; }
    .bi-kn {
      font-family: var(--font-kn);
      font-size: 0.72em;
      opacity: 0.7;
      font-weight: 400;
      white-space: nowrap;
      line-height: 1.4;
    }

    .press {
      transition: transform 0.12s ease, background-color 0.12s ease, border-color 0.12s ease;
    }
    .press:active { transform: scale(0.97); }

    .no-scrollbar::-webkit-scrollbar { display: none; }
    .no-scrollbar { scrollbar-width: none; -ms-overflow-style: none; }

    @keyframes pulse-soft {
      0%, 100% { opacity: 1; transform: scale(1); }
      50%      { opacity: 0.7; transform: scale(1.1); }
    }
    .pulse { animation: pulse-soft 2s ease-in-out infinite; }

    .map-tile {
      background-color: #e8ece6;
      background-image:
        linear-gradient(120deg, rgba(120, 113, 108, 0.08) 1px, transparent 1px),
        linear-gradient(60deg, rgba(120, 113, 108, 0.08) 1px, transparent 1px),
        radial-gradient(circle at 30% 40%, rgba(134, 164, 134, 0.25) 0%, transparent 40%),
        radial-gradient(circle at 70% 60%, rgba(180, 165, 140, 0.2) 0%, transparent 45%);
      background-size: 60px 60px, 60px 60px, 100% 100%, 100% 100%;
    }

    .img-placeholder {
      background-image: repeating-linear-gradient(
        -45deg,
        #e7e5e4 0px, #e7e5e4 1px,
        transparent 1px, transparent 8px
      );
      background-color: #f5f5f4;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--muted);
      font-family: var(--font-mono);
      font-size: 10px;
      letter-spacing: 0.02em;
      text-transform: uppercase;
    }
    ```

    ### Step 2 — Rewrite `frontend/app/layout.tsx` to load Google Fonts via next/font

    REPLACE the current file content (keep PWA manifest + Leaflet `<link>` + metadata + viewport) with:

    ```typescript
    import type { Metadata, Viewport } from "next";
    import { Inter, JetBrains_Mono, Noto_Sans_Kannada } from "next/font/google";
    import "./globals.css";

    // Self-hosted Google Fonts via next/font — exposed as CSS variables
    // for the design system to reference in globals.css (--font-sans etc.).
    const inter = Inter({
      subsets: ["latin"],
      weight: ["400", "500", "600", "700"],
      variable: "--font-inter",
      display: "swap",
    });

    const jetbrainsMono = JetBrains_Mono({
      subsets: ["latin"],
      weight: ["400", "500"],
      variable: "--font-jetbrains-mono",
      display: "swap",
    });

    const notoSansKannada = Noto_Sans_Kannada({
      subsets: ["kannada"],
      weight: ["400", "500", "600"],
      variable: "--font-noto-sans-kannada",
      display: "swap",
    });

    export const metadata: Metadata = {
      title: "Bengaluru Walkability Audit",
      description:
        "Help improve pedestrian infrastructure in Bengaluru by reporting issues near you.",
      manifest: "/manifest.json",
    };

    export const viewport: Viewport = {
      themeColor: "#2563eb",
      width: "device-width",
      initialScale: 1,
      maximumScale: 1,
    };

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      const fontVars = `${inter.variable} ${jetbrainsMono.variable} ${notoSansKannada.variable}`;
      return (
        <html lang="en" className={fontVars}>
          <head>
            <link rel="manifest" href="/manifest.json" />
            <meta name="mobile-web-app-capable" content="yes" />
            <meta name="apple-mobile-web-app-capable" content="yes" />
            <meta name="apple-mobile-web-app-status-bar-style" content="default" />
            {/* Leaflet CSS — loaded globally so SSR-disabled map components still style correctly */}
            <link
              rel="stylesheet"
              href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
              integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
              crossOrigin=""
            />
          </head>
          <body className="min-h-screen antialiased">{children}</body>
        </html>
      );
    }
    ```

    Notes on the change:
    - REMOVED `bg-gray-50 text-gray-900` Tailwind classes from `<body>` — the `body { background: var(--bg); color: var(--ink); }` rule in globals.css now sets those. This prevents Tailwind from overriding the design system.
    - KEPT the `min-h-screen antialiased` Tailwind classes — those do not conflict.
    - KEPT the Leaflet `<link>` and the PWA manifest/meta tags.

    ### Step 3 — Run build + existing layout test

    ```
    cd frontend && npm run build
    cd frontend && npm test -- --testPathPattern="layout" --passWithNoTests
    ```

    If `layout.test.ts` fails because it asserts specific body className content, update the expectation to `min-h-screen antialiased` (the only two Tailwind classes remaining on `<body>`).

    IMPORTANT — Do NOT add `oklch()` values to tailwind.config.ts (RESEARCH.md Pitfall 2). All oklch values stay in `globals.css` only.

    Reference: CONTEXT.md `<decisions>` — Design System CSS Variables section (lines 36–56).
  </action>
  <verify>
    <automated>grep -q "\\-\\-accent: oklch(0.62 0.14 145)" frontend/app/globals.css &amp;&amp; grep -q "\\-\\-bg: #fafaf9" frontend/app/globals.css &amp;&amp; grep -q "font-family: var(--font-sans)" frontend/app/globals.css &amp;&amp; grep -q "next/font/google" frontend/app/layout.tsx &amp;&amp; grep -q "inter.variable" frontend/app/layout.tsx &amp;&amp; cd frontend &amp;&amp; npm run build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\\s*--accent:" frontend/app/globals.css` returns >= 1
    - `grep -q "\\-\\-bg: #fafaf9" frontend/app/globals.css` exits 0
    - `grep -q "\\-\\-accent: oklch(0.62 0.14 145)" frontend/app/globals.css` exits 0
    - `grep -q "\\-\\-font-sans: var(--font-inter)" frontend/app/globals.css` exits 0
    - `grep -q "font-family: var(--font-sans)" frontend/app/globals.css` exits 0
    - `grep -q "\\.press:active" frontend/app/globals.css` exits 0
    - `grep -q "\\.bi-kn" frontend/app/globals.css` exits 0
    - `grep -q "\\.map-tile" frontend/app/globals.css` exits 0
    - `grep -q "import { Inter, JetBrains_Mono, Noto_Sans_Kannada } from \"next/font/google\"" frontend/app/layout.tsx` exits 0
    - `grep -q "variable: \"--font-inter\"" frontend/app/layout.tsx` exits 0
    - `grep -q "variable: \"--font-jetbrains-mono\"" frontend/app/layout.tsx` exits 0
    - `grep -q "variable: \"--font-noto-sans-kannada\"" frontend/app/layout.tsx` exits 0
    - `grep -q "@tailwind base" frontend/app/globals.css` exits 0 (existing Tailwind directives preserved)
    - `grep -q "leaflet.css" frontend/app/layout.tsx` exits 0 (existing Leaflet link preserved)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Design system tokens and fonts are loaded globally; `npm run build` succeeds with no regressions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 5 UI primitives (Bi, Icon, Btn, Pill, SectionLabel) with unit tests</name>
  <read_first>
    - design-ref/primitives.jsx (canonical source — transcribe icons and variant styles from here)
    - .planning/phases/02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch/02.3.1-UI-SPEC.md (Component Inventory section — lines 190–260 define props, sizes, variants, tones)
    - frontend/app/components/BilingualText.tsx (existing bilingual component — do NOT modify; new Bi is in `ui/` subfolder)
    - frontend/jest.setup.ts (test setup to confirm how tests are configured)
    - frontend/app/components/__tests__/BilingualText.test.tsx (example test pattern for bilingual components in this project)
  </read_first>
  <files>
    - frontend/app/components/ui/Bi.tsx
    - frontend/app/components/ui/Icon.tsx
    - frontend/app/components/ui/Btn.tsx
    - frontend/app/components/ui/Pill.tsx
    - frontend/app/components/ui/SectionLabel.tsx
    - frontend/app/components/ui/__tests__/Bi.test.tsx
    - frontend/app/components/ui/__tests__/Icon.test.tsx
    - frontend/app/components/ui/__tests__/Btn.test.tsx
    - frontend/app/components/ui/__tests__/Pill.test.tsx
    - frontend/app/components/ui/__tests__/SectionLabel.test.tsx
  </files>
  <behavior>
    - Test 1: `Bi` renders English in `.bi-en` span and Kannada in `.bi-kn` span
    - Test 2: `Bi` omits the `.bi-kn` span when `kn` prop is undefined
    - Test 3: `Icon name="camera"` renders `<svg viewBox="0 0 24 24">` with a `<g>` and at least one `<path>` or `<circle>`
    - Test 4: `Icon name="cat_broken"` renders without throwing (all 32 icon names exist in the map)
    - Test 5: `Icon` default size is 24; `Icon size=32` sets width and height to 32
    - Test 6: `Icon` sets `aria-hidden="true"` by default; when `aria-label` is provided, aria-hidden is removed
    - Test 7: `Btn variant="accent" size="xl"` renders button with `min-height: 64px` (inline style) and `background: var(--accent)` (inline style)
    - Test 8: `Btn variant="secondary"` renders with `background: var(--surface)` and `border: 1px solid var(--border-strong)` (inline style)
    - Test 9: `Btn size="sm"` renders with `min-height: 44px` (44 is the floor for sm/md)
    - Test 10: `Btn disabled={true}` renders with `disabled` attribute and `opacity: 0.4` (or pointer-events: none)
    - Test 11: `Btn` includes `.press` className for active-state animation
    - Test 12: `Pill tone="accent"` renders with `background: var(--accent-bg)` and `border: 1px solid var(--accent-border)`
    - Test 13: `Pill tone="glass"` renders with backdrop-filter blur style
    - Test 14: `SectionLabel` renders `<div>` (or `<p>`) with uppercase transform, mono font-family, muted color, 11px font-size inline style
    - Test 15: `npm test -- --testPathPattern='components/ui' --passWithNoTests` exits 0 with all new tests passing
  </behavior>
  <action>
    Create 5 primitives under `frontend/app/components/ui/` as pure TSX components. Use inline `style` props (not Tailwind) so colors/sizes reference CSS variables directly. Write Jest tests for each.

    ### File 1: `frontend/app/components/ui/Bi.tsx`

    ```typescript
    import type { CSSProperties } from "react";

    export interface BiProps {
      en: string;
      kn?: string;
      className?: string;
      style?: CSSProperties;
    }

    /**
     * Bilingual stacked text span. Renders English above Kannada (if provided).
     * CSS classes `.bi`, `.bi-en`, `.bi-kn` are defined in globals.css.
     */
    export function Bi({ en, kn, className = "", style }: BiProps) {
      return (
        <span className={`bi ${className}`.trim()} style={style}>
          <span className="bi-en">{en}</span>
          {kn && <span className="bi-kn">{kn}</span>}
        </span>
      );
    }
    ```

    ### File 2: `frontend/app/components/ui/Icon.tsx`

    Transcribe all 32 icon SVG paths from `design-ref/primitives.jsx` (lines 14–47). Use the exact same `strokeWidth: 1.75`, `strokeLinecap: "round"`, `strokeLinejoin: "round"`, `fill: "none"` parameters. The full icons object:

    ```typescript
    import type { CSSProperties, ReactElement } from "react";

    export type IconName =
      | "camera" | "map" | "pin" | "arrow_right" | "arrow_left"
      | "check" | "check_circle" | "close" | "image" | "flash"
      | "flip" | "crosshair" | "edit" | "send" | "chevron_right"
      | "chevron_down" | "share" | "grid" | "list" | "filter"
      | "globe" | "menu" | "shield" | "mic"
      | "cat_no_path" | "cat_broken" | "cat_blocked" | "cat_crossing"
      | "cat_lighting" | "cat_other" | "cat_ramp" | "cat_encroach";

    export interface IconProps {
      name: IconName;
      size?: number;
      className?: string;
      style?: CSSProperties;
      "aria-label"?: string;
    }

    const P = {
      fill: "none" as const,
      stroke: "currentColor",
      strokeWidth: 1.75,
      strokeLinecap: "round" as const,
      strokeLinejoin: "round" as const,
    };

    const ICONS: Record<IconName, ReactElement> = {
      camera: (<g {...P}><path d="M3 8a2 2 0 0 1 2-2h3l2-2h4l2 2h3a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8z"/><circle cx="12" cy="13" r="4"/></g>),
      map: (<g {...P}><path d="M9 4l-6 2v14l6-2 6 2 6-2V4l-6 2-6-2z"/><path d="M9 4v14M15 6v14"/></g>),
      pin: (<g {...P}><path d="M12 22s-7-7.5-7-13a7 7 0 0 1 14 0c0 5.5-7 13-7 13z"/><circle cx="12" cy="9" r="2.5"/></g>),
      arrow_right: (<g {...P}><path d="M5 12h14M13 6l6 6-6 6"/></g>),
      arrow_left: (<g {...P}><path d="M19 12H5M11 6l-6 6 6 6"/></g>),
      check: (<g {...P}><path d="M4 12l5 5L20 6"/></g>),
      check_circle: (<g {...P}><circle cx="12" cy="12" r="9"/><path d="M8 12l3 3 5-6"/></g>),
      close: (<g {...P}><path d="M6 6l12 12M18 6L6 18"/></g>),
      image: (<g {...P}><rect x="3" y="4" width="18" height="16" rx="2"/><circle cx="8.5" cy="9.5" r="1.5"/><path d="M3 17l5-5 4 4 3-3 6 6"/></g>),
      flash: (<g {...P}><path d="M13 2L5 14h6l-1 8 8-12h-6l1-8z"/></g>),
      flip: (<g {...P}><path d="M21 8V5a2 2 0 0 0-2-2h-3M3 16v3a2 2 0 0 0 2 2h3M3 8V5a2 2 0 0 1 2-2h3M21 16v3a2 2 0 0 1-2 2h-3"/><circle cx="12" cy="12" r="3.5"/></g>),
      crosshair: (<g {...P}><circle cx="12" cy="12" r="8"/><path d="M12 2v4M12 18v4M2 12h4M18 12h4"/></g>),
      edit: (<g {...P}><path d="M12 20h9"/><path d="M16.5 3.5a2.1 2.1 0 1 1 3 3L7 19l-4 1 1-4 12.5-12.5z"/></g>),
      send: (<g {...P}><path d="M22 2L11 13M22 2l-7 20-4-9-9-4 20-7z"/></g>),
      chevron_right: (<g {...P}><path d="M9 6l6 6-6 6"/></g>),
      chevron_down: (<g {...P}><path d="M6 9l6 6 6-6"/></g>),
      share: (<g {...P}><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><path d="M8.6 13.5l6.8 4M15.4 6.5l-6.8 4"/></g>),
      grid: (<g {...P}><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></g>),
      list: (<g {...P}><path d="M8 6h13M8 12h13M8 18h13M3 6h.01M3 12h.01M3 18h.01"/></g>),
      filter: (<g {...P}><path d="M22 3H2l8 9.5V19l4 2v-8.5L22 3z"/></g>),
      globe: (<g {...P}><circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3a14 14 0 0 1 0 18M12 3a14 14 0 0 0 0 18"/></g>),
      menu: (<g {...P}><path d="M3 6h18M3 12h18M3 18h18"/></g>),
      shield: (<g {...P}><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></g>),
      mic: (<g {...P}><rect x="9" y="2" width="6" height="12" rx="3"/><path d="M5 11a7 7 0 0 0 14 0M12 18v4"/></g>),
      cat_no_path: (<g {...P}><path d="M4 20h16"/><path d="M8 20l2-14M16 20l-2-14" strokeDasharray="2 3"/><circle cx="12" cy="4" r="2"/></g>),
      cat_broken: (<g {...P}><path d="M3 18l4-2 3 3 4-4 3 2 4-3"/><path d="M3 18v2h18v-4"/></g>),
      cat_blocked: (<g {...P}><rect x="3" y="16" width="18" height="4" rx="1"/><path d="M7 16V8l3-4h4l3 4v8"/><path d="M10 8h4"/></g>),
      cat_crossing: (<g {...P}><path d="M3 6h4M9 6h4M15 6h4M3 12h4M9 12h4M15 12h4M3 18h4M9 18h4M15 18h4"/></g>),
      cat_lighting: (<g {...P}><path d="M12 3v2M5.6 5.6l1.4 1.4M3 12h2M5.6 18.4l1.4-1.4M12 19v2M18.4 18.4l-1.4-1.4M19 12h2M18.4 5.6l-1.4 1.4"/><circle cx="12" cy="12" r="3.5"/></g>),
      cat_other: (<g {...P}><circle cx="12" cy="12" r="9"/><path d="M9.5 9a2.5 2.5 0 1 1 3.5 2.3c-.8.3-1 .9-1 1.7M12 17h.01"/></g>),
      cat_ramp: (<g {...P}><path d="M3 20h18"/><path d="M3 20L18 6h3v14"/></g>),
      cat_encroach: (<g {...P}><rect x="3" y="14" width="18" height="6" rx="1"/><path d="M7 14V9h4v5M13 14V6h5v8"/></g>),
    };

    export function Icon({ name, size = 24, className, style, "aria-label": ariaLabel }: IconProps) {
      const sizeStyle: CSSProperties = { width: size, height: size, ...style };
      const ariaProps = ariaLabel
        ? { role: "img", "aria-label": ariaLabel }
        : { "aria-hidden": true as const };
      return (
        <svg viewBox="0 0 24 24" className={className} style={sizeStyle} {...ariaProps}>
          {ICONS[name]}
        </svg>
      );
    }
    ```

    ### File 3: `frontend/app/components/ui/Btn.tsx`

    ```typescript
    import type { ButtonHTMLAttributes, CSSProperties } from "react";

    export type BtnVariant = "primary" | "accent" | "secondary" | "ghost";
    export type BtnSize = "sm" | "md" | "lg" | "xl";

    export interface BtnProps extends ButtonHTMLAttributes<HTMLButtonElement> {
      variant?: BtnVariant;
      size?: BtnSize;
      children: React.ReactNode;
    }

    const SIZES: Record<BtnSize, CSSProperties> = {
      sm: { padding: "10px 14px", fontSize: 13, borderRadius: "var(--r-md)", minHeight: 44 },
      md: { padding: "14px 20px", fontSize: 15, borderRadius: "var(--r-lg)", minHeight: 44 },
      lg: { padding: "18px 24px", fontSize: 16, borderRadius: "var(--r-xl)", minHeight: 56 },
      xl: { padding: "22px 24px", fontSize: 17, borderRadius: "var(--r-xl)", minHeight: 64 },
    };

    const VARIANTS: Record<BtnVariant, CSSProperties> = {
      primary:   { background: "var(--ink)", color: "#fafaf9", fontWeight: 600 },
      accent:    { background: "var(--accent)", color: "#fff", fontWeight: 600 },
      secondary: { background: "var(--surface)", color: "var(--ink)", fontWeight: 500, border: "1px solid var(--border-strong)" },
      ghost:     { background: "transparent", color: "var(--ink)", fontWeight: 500 },
    };

    export function Btn({
      variant = "primary",
      size = "md",
      className = "",
      disabled,
      style,
      children,
      ...rest
    }: BtnProps) {
      const merged: CSSProperties = {
        ...SIZES[size],
        ...VARIANTS[variant],
        display: "inline-flex",
        alignItems: "center",
        justifyContent: "center",
        gap: 10,
        border: VARIANTS[variant].border ?? "none",
        cursor: disabled ? "not-allowed" : "pointer",
        opacity: disabled ? 0.4 : 1,
        pointerEvents: disabled ? "none" : "auto",
        ...style,
      };
      return (
        <button
          className={`press ${className}`.trim()}
          style={merged}
          disabled={disabled}
          {...rest}
        >
          {children}
        </button>
      );
    }
    ```

    ### File 4: `frontend/app/components/ui/Pill.tsx`

    ```typescript
    import type { CSSProperties, ReactNode } from "react";

    export type PillTone = "neutral" | "accent" | "ink" | "glass" | "warn";

    export interface PillProps {
      tone?: PillTone;
      children: ReactNode;
      className?: string;
      style?: CSSProperties;
    }

    const TONES: Record<PillTone, CSSProperties> = {
      neutral: { background: "var(--surface)", color: "var(--ink-2)", border: "1px solid var(--border)" },
      accent:  { background: "var(--accent-bg)", color: "var(--accent-ink)", border: "1px solid var(--accent-border)" },
      ink:     { background: "var(--ink)", color: "#fafaf9", border: "1px solid var(--ink)" },
      glass:   { background: "rgba(255,255,255,0.92)", color: "var(--ink)", border: "1px solid rgba(28,25,23,0.08)", backdropFilter: "blur(12px)" },
      warn:    { background: "var(--warn-bg)", color: "oklch(0.4 0.14 75)", border: "1px solid oklch(0.85 0.08 75)" },
    };

    export function Pill({ tone = "neutral", children, className, style }: PillProps) {
      return (
        <span
          className={className}
          style={{
            display: "inline-flex",
            alignItems: "center",
            gap: 6,
            padding: "6px 12px",
            borderRadius: "var(--r-full)",
            fontSize: 12,
            fontWeight: 500,
            ...TONES[tone],
            ...style,
          }}
        >
          {children}
        </span>
      );
    }
    ```

    ### File 5: `frontend/app/components/ui/SectionLabel.tsx`

    ```typescript
    import type { CSSProperties, ReactNode } from "react";

    export interface SectionLabelProps {
      children: ReactNode;
      className?: string;
      style?: CSSProperties;
    }

    /**
     * Uppercase mono section header. 11px is a component-level override
     * (NOT a 5th typography scale size — see UI-SPEC Typography section).
     */
    export function SectionLabel({ children, className, style }: SectionLabelProps) {
      return (
        <div
          className={className}
          style={{
            fontSize: 11,
            fontWeight: 500,
            letterSpacing: "0.08em",
            textTransform: "uppercase",
            color: "var(--muted)",
            fontFamily: "var(--font-mono)",
            ...style,
          }}
        >
          {children}
        </div>
      );
    }
    ```

    ### Tests — create under `frontend/app/components/ui/__tests__/`

    Follow the existing test style in `frontend/app/components/__tests__/BilingualText.test.tsx`. Example for `Bi.test.tsx`:

    ```typescript
    import { render, screen } from "@testing-library/react";
    import { Bi } from "../Bi";

    describe("Bi", () => {
      it("renders English in .bi-en span", () => {
        const { container } = render(<Bi en="Report" kn="ವರದಿ" />);
        const en = container.querySelector(".bi-en");
        expect(en).toHaveTextContent("Report");
      });

      it("renders Kannada in .bi-kn span when kn prop is provided", () => {
        const { container } = render(<Bi en="Report" kn="ವರದಿ" />);
        const kn = container.querySelector(".bi-kn");
        expect(kn).toHaveTextContent("ವರದಿ");
      });

      it("omits .bi-kn span when kn prop is undefined", () => {
        const { container } = render(<Bi en="Report" />);
        expect(container.querySelector(".bi-kn")).toBeNull();
      });

      it("appends custom className after .bi", () => {
        const { container } = render(<Bi en="X" className="custom" />);
        const span = container.querySelector("span.bi");
        expect(span?.className).toContain("custom");
      });
    });
    ```

    Write similar test files:
    - `Icon.test.tsx`: verifies viewBox="0 0 24 24", default size 24, aria-hidden by default, aria-label replaces aria-hidden, each of 32 names renders without throwing (loop over names array)
    - `Btn.test.tsx`: verifies size=xl sets minHeight 64, size=sm sets minHeight 44, variant=accent sets accent bg, disabled sets opacity 0.4 and disabled attr, includes "press" className
    - `Pill.test.tsx`: verifies tone=accent sets accent-bg inline, tone=glass sets backdropFilter blur, default tone is neutral
    - `SectionLabel.test.tsx`: verifies fontSize 11, textTransform uppercase, fontFamily mono, color muted

    Run tests:
    ```
    cd frontend && npm test -- --testPathPattern="components/ui" --passWithNoTests
    ```

    All tests MUST pass before moving on.
  </action>
  <verify>
    <automated>test -f frontend/app/components/ui/Bi.tsx &amp;&amp; test -f frontend/app/components/ui/Icon.tsx &amp;&amp; test -f frontend/app/components/ui/Btn.tsx &amp;&amp; test -f frontend/app/components/ui/Pill.tsx &amp;&amp; test -f frontend/app/components/ui/SectionLabel.tsx &amp;&amp; cd frontend &amp;&amp; npm test -- --testPathPattern="components/ui" --passWithNoTests 2&gt;&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/app/components/ui/Bi.tsx` exits 0
    - `test -f frontend/app/components/ui/Icon.tsx` exits 0
    - `test -f frontend/app/components/ui/Btn.tsx` exits 0
    - `test -f frontend/app/components/ui/Pill.tsx` exits 0
    - `test -f frontend/app/components/ui/SectionLabel.tsx` exits 0
    - `test -f frontend/app/components/ui/__tests__/Bi.test.tsx` exits 0
    - `test -f frontend/app/components/ui/__tests__/Icon.test.tsx` exits 0
    - `test -f frontend/app/components/ui/__tests__/Btn.test.tsx` exits 0
    - `test -f frontend/app/components/ui/__tests__/Pill.test.tsx` exits 0
    - `test -f frontend/app/components/ui/__tests__/SectionLabel.test.tsx` exits 0
    - `grep -q "export function Bi" frontend/app/components/ui/Bi.tsx` exits 0
    - `grep -q "export type IconName" frontend/app/components/ui/Icon.tsx` exits 0
    - `grep -q "cat_no_path" frontend/app/components/ui/Icon.tsx` exits 0
    - `grep -q "cat_broken" frontend/app/components/ui/Icon.tsx` exits 0
    - `grep -q "cat_blocked" frontend/app/components/ui/Icon.tsx` exits 0
    - `grep -q "cat_crossing" frontend/app/components/ui/Icon.tsx` exits 0
    - `grep -q "cat_lighting" frontend/app/components/ui/Icon.tsx` exits 0
    - `grep -q "cat_other" frontend/app/components/ui/Icon.tsx` exits 0
    - `grep -cE "^\s+[a-z_]+: \(<g" frontend/app/components/ui/Icon.tsx` returns 32 (exactly 32 icons)
    - `grep -q "export function Btn" frontend/app/components/ui/Btn.tsx` exits 0
    - `grep -q "minHeight: 64" frontend/app/components/ui/Btn.tsx` exits 0
    - `grep -q "minHeight: 44" frontend/app/components/ui/Btn.tsx` exits 0
    - `grep -q "var(--accent)" frontend/app/components/ui/Btn.tsx` exits 0
    - `grep -q "export function Pill" frontend/app/components/ui/Pill.tsx` exits 0
    - `grep -q "backdropFilter" frontend/app/components/ui/Pill.tsx` exits 0
    - `grep -q "export function SectionLabel" frontend/app/components/ui/SectionLabel.tsx` exits 0
    - `cd frontend && npm test -- --testPathPattern="components/ui" --passWithNoTests` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>All 5 primitives exist under `frontend/app/components/ui/` with tests green; production build passes</done>
</task>

</tasks>

<verification>
**Phase foundation gate** — must all pass before moving to Wave 2 (Plans 02 and 03):

1. Branch and source preservation
   ```
   git branch --show-current | grep -x "ui-redesign"
   test -f design-ref/primitives.jsx
   test -f design-ref/styles.css
   ```

2. Design system wired
   ```
   grep -q "\\-\\-accent: oklch" frontend/app/globals.css
   grep -q "next/font/google" frontend/app/layout.tsx
   ```

3. Primitives exist and are tested
   ```
   ls frontend/app/components/ui/{Bi,Icon,Btn,Pill,SectionLabel}.tsx
   cd frontend && npm test -- --testPathPattern="components/ui" --passWithNoTests
   ```

4. No regressions in existing tests
   ```
   cd frontend && npm test -- --passWithNoTests
   ```
   All existing tests (PhotoCapture, report-page, home-page, BilingualText, etc.) must still pass. Any failures indicate the foundation changes broke existing admin/citizen pages — investigate before proceeding.

5. Build passes
   ```
   cd frontend && npm run build
   ```
</verification>

<success_criteria>
- [ ] Branch `ui-redesign` is active (`git branch --show-current` == `ui-redesign`)
- [ ] `design-ref/` contains 5 source files + README.md, committed to the branch
- [ ] `frontend/app/globals.css` contains the full `:root` design system block and body typography rules
- [ ] `frontend/app/layout.tsx` loads Inter + JetBrains Mono + Noto Sans Kannada via next/font/google with variable mode
- [ ] 5 primitives (Bi, Icon, Btn, Pill, SectionLabel) exist under `frontend/app/components/ui/` with TypeScript exports matching interfaces in this plan
- [ ] Icon component contains exactly 32 icon names with SVG paths transcribed from design-ref/primitives.jsx
- [ ] Unit tests for all 5 primitives pass (`npm test -- --testPathPattern="components/ui"`)
- [ ] Production build succeeds (`npm run build`)
- [ ] Existing test suite passes with no regressions (`npm test`)
- [ ] No `oklch()` values added to `tailwind.config.ts` (Research Pitfall 2)
- [ ] Admin pages (`frontend/app/admin/*`) untouched
</success_criteria>

<output>
After completion, create `.planning/phases/02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch/02.3.1-01-SUMMARY.md` documenting:
- Branch `ui-redesign` creation and base commit
- CSS variable scope (what was added, what was left untouched)
- Font loading strategy (next/font variable mode)
- Primitive exports (Bi, Icon, Btn, Pill, SectionLabel — signatures)
- Any deviations from UI-SPEC (e.g., if jest.config needed adjustment for oklch in testing)
- Confirmation that the 5 primitives pass their unit tests and `npm run build` succeeds
</output>
