---
phase: 02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch
plan: 04
subsystem: ui
tags: [react, nextjs, leaflet, exifr, photo-store, file-input, accordion, tdd]

# Dependency graph
requires:
  - phase: 02.3.1-P01
    provides: design system primitives (Btn, Bi, Icon, Pill, SectionLabel)
  - phase: 02.3.1-P02
    provides: CategoryGrid, SeverityGrid, SuccessCard components
  - phase: 02.3.1-P03
    provides: report/page.tsx 2-step redesign, ReportsMap, home page redesign
provides:
  - photo-store.ts browser singleton for home CTA → /report photo handoff
  - ReportCTA.tsx client component replacing home page Link+Btn
  - Map zoom control disabled (no overlap with back button)
  - Functional contact accordion in confirm step
  - Human-readable category label in confirm review card
  - SuccessCard wrapped in 428px max-width container
affects: [02.4, future-phases-using-report-flow]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - photo-store module singleton pattern for cross-page state without URL params or localStorage
    - label-wrapped hidden file input pattern for iOS camera capture on home page
    - useEffect mount-once pattern with eslint-disable comment for photo-store consumption

key-files:
  created:
    - frontend/app/lib/photo-store.ts
    - frontend/app/components/ReportCTA.tsx
  modified:
    - frontend/app/components/ReportsMap.tsx
    - frontend/app/page.tsx
    - frontend/app/report/page.tsx
    - frontend/app/__tests__/report-page.test.tsx
    - frontend/app/__tests__/home-page.test.tsx

key-decisions:
  - "photo-store uses module-level variable (not localStorage) — cleared on consumePendingPhoto(), never persisted across page loads, no PII risk"
  - "ReportCTA label wraps hidden file input (position absolute, inset 0, opacity 0) so entire button area is tappable — same iOS camera pattern as report/page.tsx Phase 02.3"
  - "Contact accordion UAT deferral overridden: GAP 4 evidence showed major severity failure; accordion expansion implemented per original UI-SPEC intent"
  - "home-page.test.tsx CTA test updated from link href check to file input check — CTA is now a label+input, not a Link"
  - "next/navigation mock added to home-page.test.tsx — ReportCTA uses useRouter which requires App Router context in Jest"

patterns-established:
  - "photo-store singleton: storePendingPhoto() on CTA, consumePendingPhoto() in useEffect on mount — pattern for passing processed files across route boundaries without re-upload"
  - "useEffect with empty dep array + eslint-disable for one-time mount effects consuming transient store"

requirements-completed: [UI-MAP, UI-HOME, UI-REPORT-FLOW]

# Metrics
duration: 4min
completed: 2026-04-25
---

# Phase 02.3.1 Plan 04: UAT Gap Closure Summary

**5 UAT gaps closed: map zoom overlap fixed, home-page file-input CTA with photo-store bypass, human category label in confirm card, functional contact accordion, SuccessCard max-width wrapper — all 736 tests pass**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-25T04:25:19Z
- **Completed:** 2026-04-25T04:29:00Z
- **Tasks:** 2 (Task A + Task B TDD)
- **Files modified:** 7

## Accomplishments

- GAP 1 (minor): `zoomControl={false}` on ReportsMap MapContainer — Leaflet +/− buttons no longer render, eliminating overlap with back button on /map
- GAP 2 (major): Home page CTA is now a `ReportCTA` client component — label-wrapped hidden file input opens camera directly; processed photo stored in `photo-store.ts` singleton; `/report` page `useEffect` on mount calls `consumePendingPhoto()` and jumps to category step bypassing the photo step
- GAP 3 (major): Confirm review card now shows `getCategoryLabel(form.category).en` (e.g. "Damaged Footpath") instead of raw enum (`broken_footpath`)
- GAP 4 (major): Contact row upgraded from display-only div to interactive accordion with `role="button"`, `onClick`, `aria-expanded`, chevron rotation, and name/email fields that appear on expand
- GAP 5 (cosmetic): SuccessCard wrapped in `<main style={{ maxWidth: 428, ... }}>` — consistent with all other report step containers

## Task Commits

1. **Task A: Map zoom + photo-store + ReportCTA** - `84556cc` (feat)
2. **Task B RED: Failing tests** - `bbe781d` (test)
3. **Task B GREEN: All implementations** - `ed04a6e` (feat)

## Files Created/Modified

- `frontend/app/lib/photo-store.ts` — new browser singleton: `storePendingPhoto` / `consumePendingPhoto` with `PendingPhoto` interface
- `frontend/app/components/ReportCTA.tsx` — new client component: label+hidden file input, EXIF extraction, compression, photo-store write, `router.push("/report")`
- `frontend/app/components/ReportsMap.tsx` — added `zoomControl={false}` to MapContainer
- `frontend/app/page.tsx` — replaced `Link+Btn` CTA with `<ReportCTA />`, removed unused `Bi` and `Btn` imports
- `frontend/app/report/page.tsx` — added imports (`getCategoryLabel`, `consumePendingPhoto`), `showContact` state, mount `useEffect` for photo-store, human label in review card, interactive contact accordion with expand/collapse, SuccessCard max-width wrapper
- `frontend/app/__tests__/report-page.test.tsx` — added `jest.mock` for photo-store; new describes: contact accordion (3 tests), category label (1 test), photo-store mount (2 tests)
- `frontend/app/__tests__/home-page.test.tsx` — added `next/navigation` mock; updated CTA test from link check to file input check

## Decisions Made

- photo-store uses module-level variable (not localStorage) — cleared on `consumePendingPhoto()`, never persisted across page loads, no PII risk
- ReportCTA label wraps hidden file input (position absolute, inset 0, opacity 0) — same iOS camera pattern established in Phase 02.3
- Contact accordion UAT deferral overridden: CONTEXT.md said "display only", but GAP 4 UAT evidence showed major severity failure; accordion expansion implemented per UAT evidence
- home-page.test.tsx CTA test changed from checking `href="/report"` to checking `input[type="file"][capture="environment"]` — CTA is now a label+input, not a `<Link>`
- `next/navigation` mock added to home-page.test.tsx because `ReportCTA` calls `useRouter()` which requires App Router context in Jest

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] home-page.test.tsx: `ReportCTA` use of `useRouter` crashes test suite**
- **Found during:** Task B GREEN (running full test suite)
- **Issue:** `home-page.test.tsx` renders `<HomePage>` which now includes `<ReportCTA>`. `ReportCTA` calls `useRouter()` from `next/navigation`, throwing "invariant expected app router to be mounted" in Jest without a mock. 17 home-page tests failed.
- **Fix:** Added `jest.mock("next/navigation", ...)` at top of `home-page.test.tsx`. Updated the CTA test from checking a `Link` with `href="/report"` to checking for a `<input type="file" capture="environment">` (the new ReportCTA file input).
- **Files modified:** `frontend/app/__tests__/home-page.test.tsx`
- **Verification:** All 17 home-page tests pass; full suite 736/736 green
- **Committed in:** `ed04a6e` (Task B GREEN commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — Bug)
**Impact on plan:** Auto-fix necessary because home-page test was rendered non-functional by the ReportCTA integration. The test behavior was updated to match the new CTA design (file input, not link).

### Note on honeypot `grep -c` count

The plan acceptance criterion stated `grep -c 'name="website"' frontend/app/report/page.tsx` should return `3`. The actual count is `4` because `grep -c` counts all lines containing the string, including the `document.querySelector('input[name="website"]')` string in `handleSubmit` (line 239 — pre-existing before this plan). The actual `<input name="website">` honeypot elements are exactly 3 (lines 424, 591, 968 — photo step, category step, confirm step). Honeypot integrity is intact.

## Issues Encountered

None beyond the auto-fixed bug above.

## Next Phase Readiness

- All 5 UAT gaps from Phase 02.3.1 field testing are closed
- The `ui-redesign` branch is ready for final review and merge to `main` when explicitly requested
- No blockers

---
*Phase: 02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch*
*Completed: 2026-04-25*
