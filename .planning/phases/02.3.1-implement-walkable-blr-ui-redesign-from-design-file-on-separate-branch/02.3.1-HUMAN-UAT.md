---
status: partial
phase: 02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch
source: [02.3.1-VERIFICATION.md]
started: 2026-04-25T21:00:00Z
updated: 2026-04-25T21:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Visual design fidelity — all 5 screens at 390px
expected: Render home, category step, confirm step, success, and map pages at 390px mobile width. Colors match CSS variable spec (--bg: #fafaf9, --accent: oklch(0.62 0.14 145)), typography is Inter/JetBrains Mono/Noto Sans Kannada, layout matches CONTEXT.md design file.
result: [pending]

### 2. iOS Safari camera UX — ReportCTA home page flow
expected: Tap the "Report an issue" button on the home page (now a file-input label). OS camera opens directly (capture=environment). After taking a photo, /report auto-advances to the Category step with the photo thumbnail visible at top — no separate photo-capture step shown. "Upload from Gallery" on /report (direct URL navigation) still opens photo library directly.
result: [pending]

### 3. End-to-end submission with live backend
expected: Complete full flow (home CTA photo → category → confirm → submit) with live Rust backend running. SuccessCard renders with a real report ID. Confirm review card shows human-readable label (e.g. "Damaged Footpath") not raw enum. Report appears on the public map at /map.
result: [pending]

### 4. GPS pulsing animation on category screen
expected: After selecting a photo with EXIF GPS data, the category screen shows a pulsing green dot (pulse CSS class, @keyframes pulse-soft animating) alongside coordinates in mono font. No modal prompt appears.
result: [pending]

### 5. Map page — no zoom controls, scrollable chips, active chip ink fill
expected: No Leaflet +/- zoom buttons visible (zoomControl={false}). Horizontal category chip strip scrolls on small screens without a visible scrollbar (.no-scrollbar). Tapping an active chip transitions background to var(--ink) with white text.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
