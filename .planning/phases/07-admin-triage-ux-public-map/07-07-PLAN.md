---
phase: 07-admin-triage-ux-public-map
plan: 07
type: execute
wave: 4
depends_on: ["07-02", "07-04"]
files_modified:
  - frontend/app/admin/admin.css
  - frontend/app/admin/page.tsx
  - frontend/app/admin/reports/page.tsx
  - frontend/app/admin/analytics/page.tsx
  - frontend/app/admin/analytics/ChoroplethMap.tsx
  - frontend/app/admin/reports/map/page.tsx
autonomous: true
requirements: [MOB-01, MOB-02, MOB-03, MOB-04, MOB-05, MOB-06, MOB-07]
must_haves:
  truths:
    - "On mobile Safari the admin ops dashboard and queue pages scroll to full content without being clipped behind the bottom nav (MOB-01, MOB-02)"
    - "The analytics Reports-per-week chart renders its data lines on mobile (MOB-03)"
    - "The analytics chart legend shows human-readable category labels, not raw DB enum strings (MOB-04)"
    - "The ward choropleth is fully visible without horizontal scroll and handles null GeoJSON gracefully (MOB-05, MOB-06)"
    - "On the admin /map the Leaflet attribution + legend sit above the bottom nav height + safe-area-inset-bottom (MOB-07)"
  artifacts:
    - path: "frontend/app/admin/admin.css"
      provides: "safe-area bottom padding utility for admin scroll containers"
      contains: "safe-area-inset-bottom"
  key_links:
    - from: "frontend/app/admin/analytics/page.tsx"
      to: "frontend/app/lib/translations.ts"
      via: "Recharts Legend formatter uses getCategoryLabel"
      pattern: "getCategoryLabel"
---

<objective>
Fix the seven admin-portal mobile Safari layout bugs (MOB-01 through MOB-07). CSS and Recharts/Leaflet config only — no behavior or data changes.

Purpose: The admin portal is used daily on mobile; these bugs clip content behind the bottom nav, hide charts, and show raw enum strings. This plan is independent of all other Phase 7 work (distinct files).
Output: safe-area bottom padding on the ops + queue scroll containers, explicit chart dimensions, a legend formatter using translations, explicit choropleth container height with null-GeoJSON handling, and Leaflet-control offset on the admin map.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md
@.planning/phases/07-admin-triage-ux-public-map/07-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bottom-nav safe-area padding for ops + queue pages (MOB-01, MOB-02, MOB-07)</name>
  <files>frontend/app/admin/admin.css, frontend/app/admin/page.tsx, frontend/app/admin/reports/page.tsx, frontend/app/admin/reports/map/page.tsx</files>
  <read_first>
    - frontend/app/admin/admin.css (read existing tokens + any existing env(safe-area-inset-bottom) usage; the bottom nav height is 56px per UI-SPEC)
    - frontend/app/admin/page.tsx (read the scrollable dashboard container to find where to add bottom padding)
    - frontend/app/admin/reports/page.tsx (read the outer main container style — note: Plan 04 also edits this file; this is a different region — the page-level scroll container, not the filter bar)
    - frontend/app/admin/reports/map/page.tsx (read the Leaflet map wrapper)
    - .planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md (read Mobile Safari Constraints table; bottom nav = 56px)
  </read_first>
  <action>
    In admin.css add a reusable utility (e.g. `.admin-safe-bottom`) setting `padding-bottom: calc(56px + env(safe-area-inset-bottom))` (56px = admin bottom nav height per UI-SPEC). Apply it (or the equivalent inline padding-bottom) to the scrollable container on the ops dashboard (MOB-01) and the reports queue page-level container (MOB-02) so content clears the bottom nav.

    MOB-07: on the admin /map wrapper (reports/map/page.tsx) offset the Leaflet attribution + legend above the bottom nav — add `padding-bottom: calc(56px + env(safe-area-inset-bottom))` to the map wrapper, or shift the Leaflet attribution control / legend panel by that amount so nav tabs stay tappable.

    Coordinate with Plan 04: in reports/page.tsx edit ONLY the page-level scroll container region, not the filter bar Plan 04 owns. This plan is Wave 4 (after Plan 04 in Wave 3), so the filter bar already exists — add the safe-area padding to the page-level container without disturbing the filter-bar markup.
  </action>
  <verify>
    <automated>cd frontend && grep -rc "safe-area-inset-bottom" frontend/app/admin/admin.css frontend/app/admin/page.tsx frontend/app/admin/reports/map/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "safe-area-inset-bottom" frontend/app/admin/admin.css` returns >= 1
    - Source: ops dashboard container and queue page-level container apply the safe-area bottom padding
    - Source: admin /map wrapper offsets Leaflet controls by `calc(56px + env(safe-area-inset-bottom))`
    - Behavior: `npm run build` succeeds (verified in Task 3)
  </acceptance_criteria>
  <done>Ops, queue, and admin-map containers clear the 56px bottom nav + safe area on mobile Safari.</done>
</task>

<task type="auto">
  <name>Task 2: Fix analytics chart rendering + legend labels + choropleth visibility (MOB-03, MOB-04, MOB-05, MOB-06)</name>
  <files>frontend/app/admin/analytics/page.tsx, frontend/app/admin/analytics/ChoroplethMap.tsx</files>
  <read_first>
    - frontend/app/admin/analytics/page.tsx (read the TrendChart / Recharts usage, the ResponsiveContainer or chart sizing, and the Legend element; read how the choropleth is dynamically imported and the wrapper div sizing)
    - frontend/app/admin/analytics/ChoroplethMap.tsx (read the MapContainer height:400 inline style, the fetchError handling, and the GeoJSON null handling)
    - frontend/app/lib/translations.ts (read getCategoryLabel — its return shape, e.g. { en, kn }, to use the correct field in the formatter)
  </read_first>
  <action>
    MOB-03: ensure the Reports-per-week (12 weeks) chart has explicit dimensions so data lines render on mobile Safari — wrap in a Recharts `ResponsiveContainer` with an explicit numeric `height` (e.g. 300) and ensure the parent has a defined width, OR pass fixed width+height. The known failure is a 100%-only container collapsing to zero height on iOS.

    MOB-04: add a `formatter` to the chart's Recharts `<Legend>` that maps the raw category enum to a human label via `getCategoryLabel(value).en` (use the actual English field name returned by getCategoryLabel) so the legend shows readable labels instead of DB enum strings.

    MOB-05/MOB-06: ensure the ChoroplethMap's parent wrapper div has an explicit height (matching the MapContainer's height:400) so the map is visible on mobile and does not require horizontal scroll; confirm the existing fetchError / null-GeoJSON branch renders a graceful fallback (no crash) when boundaries fail to load. Do not change the data source — the admin choropleth still uses the renamed admin endpoint (Plan 02).
  </action>
  <verify>
    <automated>cd frontend && grep -c "getCategoryLabel" frontend/app/admin/analytics/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "getCategoryLabel" frontend/app/admin/analytics/page.tsx` returns >= 1 (legend formatter)
    - Source: the chart uses an explicit numeric height (ResponsiveContainer height or fixed dimensions), not width/height 100% alone
    - Source: ChoroplethMap parent wrapper has an explicit height and a null/error GeoJSON fallback branch
    - Behavior: `npm run build` succeeds (Task 3)
  </acceptance_criteria>
  <done>Chart data lines render with human-readable legend labels; choropleth visible with graceful null handling.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the admin frontend builds and lints clean</name>
  <files>frontend/app/admin/admin.css</files>
  <read_first>
    - CLAUDE.md (re-read the frontend build/lint commands)
  </read_first>
  <action>
    Run `npm run lint` then `npm run build` from frontend/. Both must pass with no new errors. Do not claim success if either command reports errors or is truncated — fix and re-run.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `npm run lint` passes with no new errors
    - CLI: `npm run build` exits successfully
  </acceptance_criteria>
  <done>Admin frontend lints and builds cleanly after all MOB fixes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS + chart/Leaflet config changes; no new data flow or endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | (n/a) | admin CSS + chart config | accept | Presentation-only changes inside the already-authenticated admin portal; no security surface |
| T-07-SC | Tampering | npm installs | mitigate | Recharts + react-leaflet already present — no new package, no install task |
</threat_model>

<verification>
- safe-area padding present on ops, queue, and admin-map containers.
- Legend formatter uses getCategoryLabel; chart has explicit height; choropleth wrapper has explicit height.
- `npm run lint` + `npm run build` pass.
</verification>

<success_criteria>
- All seven MOB bugs are addressed: content clears the bottom nav, chart renders with readable legend, choropleth is visible, and admin-map controls sit above the nav.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 07)

| Symbol / File | Kind | File |
|---------------|------|------|
| `.admin-safe-bottom` (or inline safe-area padding) | CSS utility | frontend/app/admin/admin.css |
| Legend formatter via getCategoryLabel | chart config | frontend/app/admin/analytics/page.tsx |
| explicit choropleth wrapper height + null handling | CSS/config | frontend/app/admin/analytics/ChoroplethMap.tsx |
| Leaflet control safe-area offset | CSS | frontend/app/admin/reports/map/page.tsx |
</artifacts>

<output>
Create `.planning/phases/07-admin-triage-ux-public-map/07-07-SUMMARY.md` when done
</output>
