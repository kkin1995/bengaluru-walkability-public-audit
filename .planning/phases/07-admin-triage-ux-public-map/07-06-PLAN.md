---
phase: 07-admin-triage-ux-public-map
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/reports/[id]/page.tsx
autonomous: true
requirements: [TRIAGE-05]
must_haves:
  truths:
    - "When a report has a resolution_photo_url, the public detail page shows the original (Before) and resolution (After) photos side by side on desktop (>=768px) and stacked on mobile (<768px) (D-24)"
    - "The After photo carries a floating RESOLUTION badge (D-25 design spec)"
    - "Each photo shows a label (Before/After/Photo) and a mono sub-label of date · source (D-26)"
    - "When no resolution_photo_url exists, only the original shows, labeled Photo, maxWidth 520px, no After slot (D-25)"
    - "Photos are display-only — no lightbox or tap interaction (D-28)"
  artifacts:
    - path: "frontend/app/reports/[id]/page.tsx"
      provides: "PhotoFrame + BeforeAfterGrid + ResolutionBadge before/after layout"
      contains: "RESOLUTION"
  key_links:
    - from: "frontend/app/reports/[id]/page.tsx"
      to: "PublicReport.resolution_photo_url"
      via: "conditional two-column vs single-photo render"
      pattern: "resolution_photo_url"
---

<objective>
Replace the existing simple resolution photo section on the public report detail page with a before/after photo layout (TRIAGE-05, per D-24–D-28). No backend or API changes — `resolution_photo_url` is already typed and returned.

Purpose: Citizens viewing a resolved report see the original and the admin-uploaded resolution photo side by side, making the fix visible and credible.
Output: `PhotoFrame`, `BeforeAfterGrid`, `ResolutionBadge` (inline in the page) with responsive two-column / stacked / single-photo states.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-triage-ux-public-map/07-CONTEXT.md
@.planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md
@.planning/phases/07-admin-triage-ux-public-map/07-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build before/after photo layout on the public report detail page</name>
  <files>frontend/app/reports/[id]/page.tsx</files>
  <read_first>
    - frontend/app/reports/[id]/page.tsx (read: the PublicReport interface incl. resolution_photo_url ~line 46 and the history field; the existing publicImageUrl derivation via split("/uploads/").pop() ~line 75; the existing resolution section ~lines 557-607 that this plan replaces; the existing formatDate/date helper; the ward_hierarchy field shape)
    - .planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md (read TRIAGE-05 Layout + Interaction + Color + Typography contracts: PhotoFrame label 14px/600 + sub-label 10px mono muted, grid 1fr 1fr gap 20px desktop, flex column gap 16px mobile, single photo maxWidth 520px, RESOLUTION badge --accent + #ffffff)
  </read_first>
  <action>
    Replace the existing resolution photo section (~lines 557-607) with a before/after layout.

    Derive the resolution photo URL with the SAME extraction the page uses for the original (Risk: Docker hostname in stored URL): `resolutionFilename = (report.resolution_photo_url ?? "").split("/uploads/").pop() ?? ""` then build the public URL from the same API base + "/uploads/" prefix used for publicImageUrl. Only treat a resolution photo as present when resolution_photo_url is non-empty.

    Sub-label data (D-26): original sub-label = `{DD MMM} · CITIZEN` using the existing date helper on report.created_at. Resolution sub-label = `{DD MMM} · {CORP_NAME}` where CORP_NAME comes from `report.ward_hierarchy?.corporation` (fallback "GBA" — assigned_org_name is not in PublicReport, Assumption A2). The resolution DATE comes from scanning `report.history` for the last entry with status resolved or closed and using its changed_at; fall back to report.created_at if none (Risk 6).

    Add an inline `PhotoFrame({ label, subLabel, src, badge })` component: a header row with label (14px, weight 600, var(--ink)) on the left and sub-label (10px, var(--font-mono), var(--muted), right-aligned) on the right with 4px gap; below it the `<img>` with `width:100%`, `aspectRatio:"16/9"`, `objectFit:"cover"`, `borderRadius: var(--r-md)`, `display:block`. When `badge`, render an inline `ResolutionBadge` — a floating pill `position:absolute`, `top:8px`, `left:8px`, `zIndex:1`, `background: var(--accent)`, `color:#ffffff`, 10px mono, all-caps text "RESOLUTION" (a check icon from the existing Icon component may precede it). Mark the badge `aria-hidden`.

    Render logic: when a resolution photo is present, render a `BeforeAfterGrid` — `display:grid`, `gridTemplateColumns: 1fr 1fr`, `gap: 20px` at >=768px and `display:flex`, `flexDirection:column`, `gap:16px` below 768px (use the page's existing responsive mechanism — media query / CSS class — not a JS width check that breaks SSR). Left/top = PhotoFrame label "Before" (original), right/bottom = PhotoFrame label "After" with badge (resolution). When absent, render a single PhotoFrame label "Photo" (NOT "Before") inside a wrapper with `maxWidth:520px`, `margin:0 auto`, no badge, no After slot (D-25). The section heading does not change between states (D-27).

    No click/tap/lightbox behavior (D-28). Images use `alt=""` (decorative); the `<section>` gets a descriptive aria-label (UI-SPEC Accessibility). Token scope: globals.css citizen tokens only (D-29).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "RESOLUTION" frontend/app/reports/[id]/page.tsx` returns >= 1 (badge text)
    - Source: `grep -c "Before\|After\|Photo" frontend/app/reports/[id]/page.tsx` returns >= 1 covering all three labels
    - Source: single-photo branch uses maxWidth 520 and label "Photo"; two-photo branch uses grid 1fr 1fr
    - Source: resolution URL derived with the same split("/uploads/").pop() extraction as the original
    - Source: resolution sub-label corp name sourced from ward_hierarchy?.corporation with a fallback
    - Source: no onClick/lightbox handler attached to the photos
    - Behavior: `npm run build` succeeds
  </acceptance_criteria>
  <done>Before/after responsive layout replaces the old resolution section; single-photo state correct; RESOLUTION badge present; display-only; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Server component reads an already-fetched, already-public report; layout is CSS only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Information Disclosure | resolution photo URL on public page | accept | Resolution photos are intended for public display on resolved reports; same /uploads serving path as the already-public original photo |
| T-07-14 | Tampering | malformed/missing resolution_photo_url | mitigate | Empty/absent URL falls back to single-photo "Photo" layout; no crash |
| T-07-SC | Tampering | npm installs | mitigate | No new npm package — no install task |
</threat_model>

<verification>
- Two-photo, stacked, and single-photo states all render per UI-SPEC.
- `npm run build` passes.
</verification>

<success_criteria>
- A citizen viewing a resolved report sees Before and After photos side by side (or stacked on mobile); unresolved reports show a single "Photo".
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 06)

| Symbol / File | Kind | File |
|---------------|------|------|
| `PhotoFrame` | component | frontend/app/reports/[id]/page.tsx |
| `BeforeAfterGrid` (layout) | UI | frontend/app/reports/[id]/page.tsx |
| `ResolutionBadge` | component | frontend/app/reports/[id]/page.tsx |
</artifacts>

<output>
Create `.planning/phases/07-admin-triage-ux-public-map/07-06-SUMMARY.md` when done
</output>
