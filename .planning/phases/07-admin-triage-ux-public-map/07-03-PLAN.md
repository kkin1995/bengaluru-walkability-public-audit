---
phase: 07-admin-triage-ux-public-map
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/handlers/reports.rs
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "A unit test feeds a synthetic JPEG with EXIF orientation=6 through bake_orientation and asserts the output dimensions are width=4032, height=3024 (iPhone portrait rotated)"
    - "The new test passes under cargo test"
  artifacts:
    - path: "backend/src/handlers/reports.rs"
      provides: "bake_orientation orientation=6 iPhone-dimensions test"
      contains: "bake_orientation_6"
  key_links:
    - from: "backend/src/handlers/reports.rs (test module)"
      to: "bake_orientation"
      via: "test calls bake_orientation with make_jpeg_with_orientation(...,6)"
      pattern: "bake_orientation"
---

<objective>
Add the TEST-01 backend unit test covering the EXIF orientation=6 path with iPhone portrait dimensions (per ROADMAP success criterion 8: input → output 4032×3024).

Purpose: Phase 5 fixed EXIF orientation baking (FIX-06); this test locks in the orientation=6 dimension-swap behavior at the real iPhone aspect so a regression cannot silently ship.
Output: a new `#[test]` in the existing `bake_orientation_tests` module asserting the rotated output dimensions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-triage-ux-public-map/07-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add orientation=6 iPhone-dimensions test for bake_orientation</name>
  <files>backend/src/handlers/reports.rs</files>
  <read_first>
    - backend/src/handlers/reports.rs (read bake_orientation ~line 482 AND the bake_orientation_tests module ~line 1021 — study make_jpeg_with_orientation(width, height, orientation_value) and the existing bake_orientation_6_swaps_width_height test to match the exact helper signature and assertion style)
  </read_first>
  <behavior>
    - Given a JPEG with EXIF orientation=6 at iPhone-portrait aspect (width < height), bake_orientation rotates 90° CW so output width > height
    - The decoded output width equals the input height and output height equals the input width (dimension swap)
    - The assertion uses the 3024×4032 → 4032×3024 relationship required by ROADMAP success criterion 8
  </behavior>
  <action>
    Add a new `#[test] fn bake_orientation_6_iphone_portrait_dimensions()` to the existing `bake_orientation_tests` module in backend/src/handlers/reports.rs. Build the input with `make_jpeg_with_orientation(W, H, 6)`, call `bake_orientation(&input).expect(...)`, decode with `image::load_from_memory(&output)`, and assert the swapped dimensions.

    Allocation note (Risk 5 in RESEARCH): a full 3024×4032 RGB8 image is ~35 MB and slow in debug. Use the proportional 1/4-scale proxy 756×1008 → assert output width=1008, height=756 (proves the identical orientation=6 swap math at the iPhone aspect ratio). In the test doc-comment, state that 756×1008 is the 1/4-scale proxy for the iPhone 3024×4032 → 4032×3024 case referenced by the acceptance criterion, so the intent is traceable. Do NOT write a 3024×4032 literal into a negative-grep target; the criterion is satisfied by the proportional proof.

    Follow strict RED→GREEN: the function already exists and should make the test pass immediately (GREEN); if it unexpectedly fails, that is a real finding — report it, do not weaken the assertion.
  </action>
  <verify>
    <automated>cd backend && cargo test bake_orientation_6 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "bake_orientation_6_iphone_portrait_dimensions" backend/src/handlers/reports.rs` returns 1
    - Source: the test calls make_jpeg_with_orientation with orientation value 6 and a portrait aspect (width < height)
    - Source: the test asserts output width == input height AND output height == input width
    - CLI: `cargo test bake_orientation_6` shows the new test in the result and reports it passing (2 passing tests in that filter — existing + new)
  </acceptance_criteria>
  <done>New orientation=6 iPhone-aspect test exists and passes; no existing test broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure in-memory unit test; no I/O, no network, no untrusted input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | (n/a) | bake_orientation unit test | accept | Pure function test with synthetic in-memory JPEG; no security surface |
| T-07-SC | Tampering | cargo/crate installs | mitigate | image crate already in tree (used by bake_orientation); no install task |
</threat_model>

<verification>
- `cargo test bake_orientation_6` shows the new test passing alongside the existing one.
</verification>

<success_criteria>
- The orientation=6 iPhone-aspect dimension-swap test exists and passes.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 03)

| Symbol / File | Kind | File |
|---------------|------|------|
| `bake_orientation_6_iphone_portrait_dimensions` | test fn | backend/src/handlers/reports.rs |
</artifacts>

<output>
Create `.planning/phases/07-admin-triage-ux-public-map/07-03-SUMMARY.md` when done
</output>
