---
phase: 07-admin-triage-ux-public-map
plan: 08
type: execute
wave: 5
depends_on: ["07-04", "07-05", "07-06", "07-07"]
files_modified:
  - .planning/phases/07-admin-triage-ux-public-map/07-UAT-CHECKLIST.md
autonomous: false
requirements: [TRIAGE-02]
must_haves:
  truths:
    - "TRIAGE-02 (public map category chips) is confirmed present and functional — verified, not reimplemented"
    - "A cross-requirement UAT checklist covers all 13 Phase 7 requirements with explicit mobile-Safari and desktop steps"
    - "The human verifier signs off each requirement as pass or records a defect"
  artifacts:
    - path: ".planning/phases/07-admin-triage-ux-public-map/07-UAT-CHECKLIST.md"
      provides: "executable UAT checklist for all 13 requirements"
      contains: "TRIAGE-02"
  key_links:
    - from: "07-UAT-CHECKLIST.md"
      to: "staging.nammadaari.com"
      via: "HTTPS-required mobile Safari verification of map + admin features"
      pattern: "staging"
---

<objective>
Verify TRIAGE-02 (category chips already implemented — do NOT reimplement) and produce + execute a cross-requirement UAT checklist covering all 13 Phase 7 requirements.

Purpose: TRIAGE-02 is already satisfied in map/page.tsx (RESEARCH confirms the CHIPS array + chip rendering exist). Several Phase 7 requirements (the MOB bugs, ward overlay, status chips) require HTTPS + real mobile Safari and cannot be auto-tested. This plan closes the loop with structured human verification on staging.
Output: a UAT checklist file + a blocking human-verify checkpoint capturing pass/defect per requirement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-admin-triage-ux-public-map/07-CONTEXT.md
@.planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify TRIAGE-02 category chips exist and author the UAT checklist</name>
  <files>.planning/phases/07-admin-triage-ux-public-map/07-UAT-CHECKLIST.md</files>
  <read_first>
    - frontend/app/map/page.tsx (confirm the existing CHIPS array + chipLabel helper + category chip rendering — TRIAGE-02; this is a verification read, NOT an edit)
    - .planning/REQUIREMENTS.md (read the TRIAGE acceptance criteria)
    - .planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md (read the visual contracts to phrase verifiable UAT steps)
    - $HOME/.claude/projects/.../memory/reference_staging_url.md context: staging URL is https://staging.nammadaari.com (HTTPS-required tests run there)
  </read_first>
  <action>
    First, confirm TRIAGE-02 is already implemented: locate the category CHIPS array and chip rendering in map/page.tsx. Record the file + line evidence in the checklist as "TRIAGE-02: already implemented — smoke-verify only" (do NOT add or modify any chip code; TRIAGE-02 is implementation-complete per RESEARCH).

    Author `.planning/phases/07-admin-triage-ux-public-map/07-UAT-CHECKLIST.md` with one checklist entry per requirement (all 13: TRIAGE-01..05, MOB-01..07, TEST-01). Each entry: the requirement id + one-line intent, the exact device/environment (desktop Chrome vs iOS Safari on staging.nammadaari.com — HTTPS-required items: ward overlay, geolocation-adjacent map, all MOB items), numbered steps, and the expected observable result drawn from the ROADMAP success criteria + UI-SPEC contracts. Mark TEST-01 as covered by `cargo test bake_orientation_6` (automated, already green from Plan 03) and the rest as manual. Leave a pass/defect column for the verifier.
  </action>
  <verify>
    <automated>grep -c "TRIAGE-01\|TRIAGE-02\|TRIAGE-03\|TRIAGE-04\|TRIAGE-05\|MOB-01\|MOB-07\|TEST-01" .planning/phases/07-admin-triage-ux-public-map/07-UAT-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "TRIAGE-02" .planning/phases/07-admin-triage-ux-public-map/07-UAT-CHECKLIST.md` returns >= 1 with the "already implemented — smoke-verify only" note and file/line evidence
    - Source: the checklist contains an entry for every one of the 13 requirements
    - Source: HTTPS-required items reference staging.nammadaari.com and iOS Safari
    - Source: NO change was made to map/page.tsx category chip code (TRIAGE-02 verified, not reimplemented)
  </acceptance_criteria>
  <done>TRIAGE-02 confirmed present (no code change); a complete 13-requirement UAT checklist exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT sign-off across all 13 Phase 7 requirements</name>
  <files>.planning/phases/07-admin-triage-ux-public-map/07-UAT-CHECKLIST.md</files>
  <action>Drive the human verifier through 07-UAT-CHECKLIST.md on staging.nammadaari.com (iOS Safari + desktop) per the how-to-verify steps below; record pass/defect for each of the 13 requirements.</action>
  <what-built>Phase 7 delivered: admin ward/corp filter (Plan 01+04), public status chips + ward overlay (Plan 05), before/after photo (Plan 06), MOB-01..07 mobile Safari fixes (Plan 07), and the TEST-01 unit test (Plan 03). TRIAGE-02 category chips were already present and verified, not rebuilt.</what-built>
  <how-to-verify>
    Use 07-UAT-CHECKLIST.md and work through every requirement.
    1. Deploy the branch to staging (staging.nammadaari.com) per DEPLOYMENT.md.
    2. On iOS Safari: open /map — confirm category chips (TRIAGE-02) AND the new status chip row (TRIAGE-03) appear; tap a status chip and confirm pins filter; tap "WARDS" and confirm teal ward outlines draw and a ward name shows on tap (TRIAGE-04); confirm the toggle sits above the Report-here FAB.
    3. Open a resolved report's public detail page — confirm Before/After photos with the RESOLUTION badge (TRIAGE-05); open an unresolved report — confirm a single "Photo".
    4. Log into the admin portal on iOS Safari: on the reports queue, confirm the Corp + searchable Ward selects, confirm selecting a corp narrows the ward list, and confirm filtering changes the report list (TRIAGE-01).
    5. Confirm MOB fixes: ops + queue pages scroll fully past the bottom nav (MOB-01/02); analytics chart renders data lines with readable legend labels (MOB-03/04); choropleth is fully visible (MOB-05/06); admin /map controls sit above the nav (MOB-07).
    6. Confirm `cd backend && cargo test bake_orientation_6` passes (TEST-01).
    Record pass or a defect for each of the 13 requirements in the checklist.
  </how-to-verify>
  <resume-signal>Type "approved" when all 13 requirements pass, or list the requirement ids with defects so a gap-closure plan can be created.</resume-signal>
  <verify><human-check>Verifier records pass or a defect for each of the 13 requirements in 07-UAT-CHECKLIST.md.</human-check></verify>
  <done>All 13 requirements marked pass, or defects logged with requirement ids for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification + documentation only; no code or endpoint change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16 | (n/a) | UAT checklist + sign-off | accept | Documentation/verification plan; no executable surface |
| T-07-SC | Tampering | npm/cargo installs | mitigate | No installs in this plan — no install task |
</threat_model>

<verification>
- TRIAGE-02 confirmed present with evidence; no chip code changed.
- 13-requirement checklist authored; human sign-off captures pass/defect per requirement.
</verification>

<success_criteria>
- All 13 Phase 7 requirements are verified (or defects recorded for gap closure), with TRIAGE-02 confirmed as already-implemented.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 08)

| Symbol / File | Kind | File |
|---------------|------|------|
| `07-UAT-CHECKLIST.md` | doc | .planning/phases/07-admin-triage-ux-public-map/07-UAT-CHECKLIST.md |
</artifacts>

<output>
Create `.planning/phases/07-admin-triage-ux-public-map/07-08-SUMMARY.md` when done
</output>
