---
phase: 07-admin-triage-ux-public-map
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - frontend/app/admin/lib/adminApi.ts
  - frontend/app/admin/reports/page.tsx
autonomous: true
requirements: [TRIAGE-01]
must_haves:
  truths:
    - "On the admin reports queue, a Corporation select and a searchable Ward select appear inline in the filter bar"
    - "Selecting a corporation narrows the ward list to that corp's wards and resets the ward selection (D-02)"
    - "Selecting a ward refetches the report list with a ward_id param; selecting a corp refetches with corporation_id (D-04)"
    - "While options load, the selects show a Loading state; on fetch error they show Unavailable"
    - "Typing in the ward search filters the ward list client-side and shows Showing N / 369"
  artifacts:
    - path: "frontend/app/admin/reports/page.tsx"
      provides: "corp select + searchable ward popover wired to filters"
      contains: "corporation"
    - path: "frontend/app/admin/lib/adminApi.ts"
      provides: "getAdminCorporations + getAdminWards + extended AdminReportFilters"
      contains: "getAdminCorporations"
  key_links:
    - from: "frontend/app/admin/reports/page.tsx"
      to: "backend /api/admin/corporations + /api/admin/wards"
      via: "getAdminCorporations() / getAdminWards(corpId) on mount + corp change"
      pattern: "getAdminWards"
    - from: "frontend/app/admin/reports/page.tsx"
      to: "backend /api/admin/reports"
      via: "fetchReports passes ward_id + corporation_id params"
      pattern: "corporation_id"
---

<objective>
Build the admin reports-queue ward/corporation filter UI (TRIAGE-01, per D-01/D-02/D-03/D-04) on top of the Plan 01 backend endpoints.

Purpose: Admins filter the queue by geographic scope. This is the UI tier; Plan 01 supplied the endpoints and query support.
Output: extended `AdminReportFilters` TS interface, `getAdminCorporations()` + `getAdminWards(corpId?)` API functions, a Corporation select and a searchable Ward popover inline in the existing filter bar, wired to refetch reports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-triage-ux-public-map/07-CONTEXT.md
@.planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md
@.planning/phases/07-admin-triage-ux-public-map/07-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend adminApi with corp/ward types, fetchers, and filter params</name>
  <files>frontend/app/admin/lib/adminApi.ts</files>
  <read_first>
    - frontend/app/admin/lib/adminApi.ts (read the AdminReportFilters interface ~line 127, the getAdminReports function and how it serializes filter params into the query string, and an existing apiFetch GET helper to match the call signature)
  </read_first>
  <action>
    Add `ward_id?: string` and `corporation_id?: string` to the `AdminReportFilters` interface (per D-04). Update `getAdminReports()` to append `ward_id` and `corporation_id` to the query string when present (same conditional-append pattern as existing category/status params).

    Add two functions calling the Plan 01 endpoints through the existing admin proxy/BASE (these auto-proxy via frontend/app/api/admin/[...path]/route.ts — no proxy code change needed):
    `getAdminCorporations(): Promise<{ id: string; name: string }[]>` → GET /api/admin/corporations.
    `getAdminWards(corpId?: string): Promise<{ id: string; ward_name: string; ward_number: number }[]>` → GET /api/admin/wards, appending `?corp_id=<corpId>` when corpId is provided.
    Define small exported types for the corp and ward option shapes.
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "getAdminCorporations\|getAdminWards" frontend/app/admin/lib/adminApi.ts` returns >= 2
    - Source: AdminReportFilters interface includes both `ward_id?` and `corporation_id?`
    - Source: getAdminReports serializes ward_id and corporation_id into the request
    - Behavior: `npm run lint` passes with no new errors
  </acceptance_criteria>
  <done>Interface extended; two fetchers added; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add Corporation select + searchable Ward popover to the filter bar</name>
  <files>frontend/app/admin/reports/page.tsx</files>
  <read_first>
    - frontend/app/admin/reports/page.tsx (read the full filter bar region: the existing category/status/severity select elements, the useState filter variables, the ref pattern for current filter values, and fetchReports() — replicate this pattern exactly for the two new selects)
    - .planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md (read the TRIAGE-01 sections: Layout Contracts, Interaction Contracts, Color admin tokens, Typography admin table, Copywriting contract — use these EXACT values)
    - frontend/app/admin/admin.css (read the admin token names referenced by the UI-SPEC: --accent-bg, --accent-ink, --accent-border, --surface-2, --r-xs, --muted, --ink, --border)
  </read_first>
  <action>
    Add a Corporation select and a searchable Ward select inline in the existing filter bar, after a vertical `1px solid var(--border)` separator following the existing category/status/severity selects (per D-01 + UI-SPEC Layout Contract). On tablet/narrow widths the select group scrolls horizontally via `overflow-x: auto` + `.no-scrollbar` — it must NOT wrap (D-01).

    State + data: add `corporationId` and `wardId` useState; add `corporations` and `wards` option arrays; add `isLoadingFilters` and `filterError` states. On mount, call `getAdminCorporations()` and `getAdminWards()`; show the "Loading…" disabled placeholder while pending and "Unavailable" disabled on error (UI-SPEC Copywriting + Interaction contracts). Mirror the existing categoryRef/statusRef pattern with wardIdRef and corpIdRef so fetchReports reads current values.

    Corporation trigger + popover (236px, per UI-SPEC): height 32px, JetBrains Mono 12px, borderRadius var(--r-xs); muted label prefix "CORP:" + value in ink; active state background var(--accent-bg) / color var(--accent-ink); popover header "CORPORATION · {total_corps}"; rows show corp name (13px bold) + zone sub-label + selected-row highlight var(--accent-bg). Placeholder "All corps".

    Ward trigger + popover (296px, per UI-SPEC): same trigger styling with "WARD:" prefix; popover has a search `<input type="text">` with placeholder `grep ward name or no…`; header `Showing {N} / 369` (append `· {corp_name}` when a corp is selected); rows show ward number (mono, tabular, muted) + ward name (truncated); no-match state `no ward matches '{q}'` (muted mono, centered). Client-side filter: `ward_name.toLowerCase().includes(q.toLowerCase()) || String(ward_number).includes(q)`. Placeholder "All wards".

    Searchability approach (Claude's discretion per CONTEXT + Risk 4): custom popover, NO third-party library. Use `position: absolute` relative to a `position: relative` wrapper (NOT position: fixed — iOS Safari bug); z-index above other filter content; ward list `overflow-y: auto` with `-webkit-overflow-scrolling: touch`.

    Behavior wiring (D-02, D-04): on corp change → setCorporationId, reset wardId to empty, call getAdminWards(corpId) to narrow the ward list, then fetchReports with corporation_id and no ward_id. On corp cleared ("All corps") → reset ward list to all 369, refetch without corporation_id. On ward change → fetchReports with ward_id. Both triggers get the "NEW" indicator ring `outline: 1.5px solid var(--accent-border)` (UI-SPEC Layout Contract).

    Accessibility (UI-SPEC): corp/ward selects get `aria-label="Filter by corporation"` / `aria-label="Filter by ward"` and `aria-busy={isLoadingFilters}`.

    Token scope: admin.css tokens only (D-29) — do NOT use globals.css citizen tokens here.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "grep ward name or no" frontend/app/admin/reports/page.tsx` returns 1 (exact search placeholder)
    - Source: `grep -c "All corps\|All wards" frontend/app/admin/reports/page.tsx` returns >= 2
    - Source: `grep -c "Showing" frontend/app/admin/reports/page.tsx` returns >= 1 (ward popover header)
    - Source: corp-change handler resets wardId and calls getAdminWards with the corp id
    - Source: fetchReports passes corporation_id and ward_id
    - Source: `grep -c "position: fixed\|position:fixed" frontend/app/admin/reports/page.tsx` for the new popover region uses absolute, not fixed (verify popover wrapper uses position: relative/absolute)
    - Behavior: `npm run build` succeeds
  </acceptance_criteria>
  <done>Corp + searchable ward selects render inline, narrow on corp change, refetch reports by scope, with loading/error/no-match states per UI-SPEC; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→admin API | Filter selections become corp_id/ward_id params on authenticated requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | corp_id/ward_id sent from client | mitigate | Backend (Plan 01) types both as Option<Uuid> and binds them as parameters; UI only sends ids returned by the admin option endpoints |
| T-07-09 | Information Disclosure | scoped admin querying other corps | accept | Ward option list and report query are both org-scoped server-side; out-of-scope selections yield empty results |
| T-07-SC | Tampering | npm installs | mitigate | No new npm package (custom popover, no third-party dropdown per D-01) — no install task |
</threat_model>

<verification>
- Filter bar renders corp + searchable ward selects with the exact UI-SPEC copy and tokens.
- Corp change narrows + resets ward; selections refetch reports by scope.
- `npm run build` passes.
</verification>

<success_criteria>
- An admin can filter the reports queue by ward or corporation from inline selects, with searchable ward selection and correct loading/error states.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 04)

| Symbol / File | Kind | File |
|---------------|------|------|
| `getAdminCorporations` | fn | frontend/app/admin/lib/adminApi.ts |
| `getAdminWards` | fn | frontend/app/admin/lib/adminApi.ts |
| `AdminReportFilters.ward_id`, `.corporation_id` | TS fields | frontend/app/admin/lib/adminApi.ts |
| Corp select + searchable Ward popover | UI | frontend/app/admin/reports/page.tsx |
</artifacts>

<output>
Create `.planning/phases/07-admin-triage-ux-public-map/07-04-SUMMARY.md` when done
</output>
