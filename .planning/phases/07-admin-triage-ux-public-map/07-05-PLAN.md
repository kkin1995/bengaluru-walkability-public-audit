---
phase: 07-admin-triage-ux-public-map
plan: 05
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - frontend/app/lib/config.ts
  - frontend/app/map/page.tsx
  - frontend/app/components/ReportsMap.tsx
autonomous: true
requirements: [TRIAGE-03, TRIAGE-04]
must_haves:
  truths:
    - "The public /map shows a second chip row with All statuses / Open / In progress / Resolved chips, each with a count and colored dot (D-07, D-08)"
    - "Tapping a status chip filters the visible report pins client-side; category and status filters combine with AND logic (D-09)"
    - "A 52x52 WARDS toggle button sits bottom-right stacked above the Report here FAB (D-18)"
    - "Toggling WARDS ON lazily fetches /api/wards/boundaries and draws stroke-only teal ward polygons; tapping/hovering a polygon shows the ward name (D-16, D-17, D-20)"
    - "On fetch failure the toggle silently greys out with no error message (D-21); re-toggling ON reuses the cached GeoJSON (D-20)"
  artifacts:
    - path: "frontend/app/map/page.tsx"
      provides: "StatusChipRow + WardToggleButton + ward overlay state + FAB column"
      contains: "All statuses"
    - path: "frontend/app/components/ReportsMap.tsx"
      provides: "statusFilter prop + WardBoundaryLayer inside ssr:false boundary"
      contains: "WardBoundaryLayer"
  key_links:
    - from: "frontend/app/map/page.tsx"
      to: "backend /api/wards/boundaries (public)"
      via: "lazy fetch on first WARDS toggle ON"
      pattern: "wards/boundaries"
    - from: "frontend/app/map/page.tsx"
      to: "frontend/app/components/ReportsMap.tsx"
      via: "statusFilter + showWardBoundaries + wardBoundariesGeojson props"
      pattern: "statusFilter"
---

<objective>
Add the public map status filter chips (TRIAGE-03, per D-06–D-13) and the toggleable ward boundary overlay (TRIAGE-04, per D-16–D-22) on top of the Plan 02 public endpoint.

Purpose: Citizens filter pins by status and can switch on a ward reference layer. Both features live on map/page.tsx + ReportsMap.tsx, so they ship together (shared files = one plan).
Output: `STATUS_CHIPS` + status filter state + `StatusChipRow`, a `WardToggleButton` in a FAB column, lazy GeoJSON fetch with caching/silent-fail, and a `WardBoundaryLayer` rendered inside the existing ssr:false ReportsMap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-triage-ux-public-map/07-CONTEXT.md
@.planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md
@.planning/phases/07-admin-triage-ux-public-map/07-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status filter chips with client-side AND filtering</name>
  <files>frontend/app/map/page.tsx, frontend/app/components/ReportsMap.tsx</files>
  <read_first>
    - frontend/app/map/page.tsx (read the existing CHIPS array, chipLabel() helper, activeFilter useState, the ReportLite type, the chip row JSX, and how categoryFilter is passed to ReportsMap)
    - frontend/app/components/ReportsMap.tsx (read the props interface, STATUS_COLORS, onReportsLoaded callback, and the CircleMarker .filter() expression where categoryFilter is applied)
    - frontend/app/lib/translations.ts (read publicStatusLabel + publicStatusColor — status chip labels and dot colors MUST come from these, not hardcoded per D-07)
    - .planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md (read TRIAGE-02/03 Layout + Interaction + Color contracts: chip 36px height, row 2 top:120, status dot 7x7, dot colors, AND logic, total-count rule)
  </read_first>
  <action>
    Add a `STATUS_CHIPS` array (per D-07): "All statuses" (value all), "Open" (matches open/acknowledged/assigned), "In progress" (matches in_progress), "Resolved" (matches resolved/closed). Chip display labels must derive from `publicStatusLabel` where applicable (do not hardcode citizen-facing status text). Add `activeStatusFilter` useState defaulting to "all" (in-memory only, NOT persisted to URL — D-13).

    Compute status counts from the full `allReports` array (TOTAL counts, NOT cross-filtered by category — D-10): open = count of open/acknowledged/assigned; in_progress = count of in_progress; resolved = count of resolved/closed; all = allReports.length.

    Render a second chip row (StatusChipRow) below the existing category row per UI-SPEC Layout Contract: `position: absolute`, `top: 120`, `padding: 0 16px`, `overflow-x: auto`, `.no-scrollbar`. Each chip height 36px, borderRadius var(--r-full), inactive `rgba(255,255,255,0.9)` + `1px solid var(--border)` + `backdropFilter: blur(8px)`, active `background: var(--ink)` + `color: #fafaf9`. Each non-"all" chip shows a 7x7px status dot before the label colored from publicStatusColor (Open=--danger, In progress=--warn, Resolved=--accent per UI-SPEC); active dot gets `boxShadow: 0 0 0 1.5px rgba(255,255,255,0.25)`. Chip copy: `All statuses · {total}`, `Open · {N}`, `In progress · {N}`, `Resolved · {N}`.
    Accessibility: row `role="toolbar"` `aria-label="Filter by status"`; each chip `type="button"` `aria-pressed={active}`.

    Add `status: string` to the `ReportLite` type in map/page.tsx (it currently only carries category). The onReportsLoaded callback already receives Report[] which includes status — populate it.

    Pass `statusFilter={activeStatusFilter}` to ReportsMap. In ReportsMap, add a `statusFilter?: string` prop and extend the marker `.filter()` to apply BOTH categoryFilter and statusFilter with AND logic (D-09): statusMatch is true when statusFilter is unset/"all", or the report status is in the matching group. Zero matches = empty map, no message (D-12) — do not add an empty-state overlay.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "All statuses" frontend/app/map/page.tsx` returns >= 1
    - Source: status chip labels reference publicStatusLabel (not hardcoded "Open"/"Resolved" literals for the mapping) — `grep -c "publicStatusLabel" frontend/app/map/page.tsx` returns >= 1
    - Source: status groups match open/acknowledged/assigned, in_progress, resolved/closed
    - Source: ReportsMap filter expression applies categoryFilter AND statusFilter
    - Source: ReportLite type includes a status field
    - Behavior: `npm run build` succeeds
  </acceptance_criteria>
  <done>Second status chip row renders with counts + dots; client-side AND filtering works; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add ward boundary overlay toggle + lazy GeoJSON layer</name>
  <files>frontend/app/lib/config.ts, frontend/app/map/page.tsx, frontend/app/components/ReportsMap.tsx</files>
  <read_first>
    - frontend/app/lib/config.ts (read existing API_BASE_URL export — reuse it; per project config rule no inline process.env)
    - frontend/app/components/ReportsMap.tsx (read the imports — confirm GeoJSON can be imported from react-leaflet as ChoroplethMap does; read the existing FAB rendering and the HeatmapLayer conditional-render pattern)
    - frontend/app/components/HeatmapLayer.tsx (read the toggleable-overlay-inside-map pattern to mirror for WardBoundaryLayer)
    - frontend/app/admin/analytics/ChoroplethMap.tsx (read the react-leaflet GeoJSON usage + how it hardcodes a stroke color rather than a CSS var — Leaflet SVG does not inherit CSS custom properties)
    - .planning/phases/07-admin-triage-ux-public-map/07-UI-SPEC.md (read TRIAGE-04 Layout Contract: FAB column position, 52x52 toggle, ward polygon style, OFF/ON/loading/failed visuals, "Ward boundaries · 369 wards" banner, Interaction Contract, Accessibility)
  </read_first>
  <action>
    Config: in frontend/app/lib/config.ts add a `PUBLIC_WARD_BOUNDARIES_URL` derived from the existing API base (or reuse API_BASE_URL + "/api/wards/boundaries"). Do NOT inline process.env in the component (project config rule).

    ReportsMap: add `showWardBoundaries?: boolean` and `wardBoundariesGeojson?: FeatureCollection | null` props. Import `GeoJSON` from react-leaflet (same import ChoroplethMap uses — safe inside this ssr:false component) and `FeatureCollection` from geojson. Add a `WardBoundaryLayer` component DEFINED INSIDE ReportsMap.tsx (inside the ssr:false boundary per CLAUDE.md Leaflet rule) that renders `<GeoJSON>` only when showWardBoundaries is true and geojson is present, styled stroke-only (D-16): `fill: false`, `color` = resolved --accent value, `opacity: 0.5`, `weight: 1.5`, `lineJoin: "round"`. Resolve the accent color via `getComputedStyle(document.documentElement).getPropertyValue('--accent').trim()` with hardcoded fallback `oklch(0.62 0.14 145)` (Risk 2 — Leaflet SVG ignores CSS vars). In `onEachFeature`, bind a Leaflet tooltip showing `feature.properties?.ward_name` (D-17). Mark the layer `aria-hidden` (decorative).

    map/page.tsx: add `showWardBoundaries` useState (default false — D-19), `wardBoundariesGeojson` useState (default null), and `wardBoundariesStatus: 'idle'|'loading'|'loaded'|'error'` useState. Add a `WardToggleButton` (52x52px, borderRadius var(--r-lg), ward icon + "WARDS" mono label, flex column) per UI-SPEC. On toggle OFF→ON: if geojson not yet cached, set status 'loading' (button opacity 0.5 disabled), fetch PUBLIC_WARD_BOUNDARIES_URL; on success cache geojson + status 'loaded' + setShowWardBoundaries(true); on failure set status 'error' and silently grey/disable the button with NO error message shown (D-21). If geojson already cached, just toggle on without refetch (D-20). On ON→OFF: setShowWardBoundaries(false) and hide the "Ward boundaries · 369 wards" banner.

    Move the existing "Report here" FAB and the new WardToggleButton into a shared flex-column container at `position: absolute`, `bottom: calc(24px + env(safe-area-inset-bottom))`, `right: 16px` (mobile) / `right: 24px` (desktop), `flexDirection: column`, `gap: 12px`, `zIndex: 500`, toggle ABOVE the FAB (D-18). Show the "Ward boundaries · 369 wards" banner below the search bar when overlay is ON (UI-SPEC copy). Pass `showWardBoundaries` + `wardBoundariesGeojson` to ReportsMap.
    Accessibility: toggle `type="button"`, `aria-pressed={showWardBoundaries}`, `aria-label="Toggle ward boundaries"`.
    Token scope: globals.css citizen tokens only (D-29).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "WardBoundaryLayer" frontend/app/components/ReportsMap.tsx` returns >= 1 and the component is defined inside this file (inside the ssr:false module)
    - Source: `grep -c "wards/boundaries" frontend/app/lib/config.ts frontend/app/map/page.tsx` returns >= 1
    - Source: ward overlay fetch is lazy (only inside the toggle handler, not on mount) and caches geojson in state
    - Source: failure path sets an error/disabled state and renders NO user-facing error text
    - Source: `grep -c "Ward boundaries · 369 wards" frontend/app/map/page.tsx` returns 1
    - Source: FAB column container uses bottom/right offsets with env(safe-area-inset-bottom) and toggle stacked above FAB
    - Source: no inline `process.env` in map/page.tsx for the boundaries URL (uses config.ts)
    - Behavior: `npm run build` succeeds
  </acceptance_criteria>
  <done>WARDS toggle renders above the FAB, lazily fetches + caches the public GeoJSON, draws stroke-only teal polygons with ward-name tooltips, and silently fails on error; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→public API | Unauthenticated ward GeoJSON fetch; response is public geographic data only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Information Disclosure | public ward GeoJSON consumed by map | accept | Endpoint (Plan 02) returns only ward_name/ward_number + geometry — no PII; safe to expose |
| T-07-11 | Denial of Service | repeated overlay toggles | mitigate | GeoJSON cached in component state after first success (D-20); no refetch on re-toggle; 24h edge cache from Plan 02 |
| T-07-12 | Tampering | malformed GeoJSON response | mitigate | Fetch failure path silently disables the toggle (D-21); no unhandled exception surfaced to user |
| T-07-SC | Tampering | npm installs | mitigate | No new npm package (react-leaflet + geojson already present) — no install task |
</threat_model>

<verification>
- Two chip rows render; status filtering combines with category via AND; zero matches = empty map.
- WARDS toggle lazily fetches + caches, draws stroke-only polygons with tooltips, silent-fails.
- `npm run build` passes.
</verification>

<success_criteria>
- A citizen can filter pins by status and toggle a ward boundary overlay with ward-name tooltips on the public map.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 05)

| Symbol / File | Kind | File |
|---------------|------|------|
| `STATUS_CHIPS` + `StatusChipRow` | UI/data | frontend/app/map/page.tsx |
| `WardToggleButton` + ward overlay state | UI | frontend/app/map/page.tsx |
| `WardBoundaryLayer` | component | frontend/app/components/ReportsMap.tsx |
| `statusFilter` prop on ReportsMap | prop | frontend/app/components/ReportsMap.tsx |
| `PUBLIC_WARD_BOUNDARIES_URL` | config | frontend/app/lib/config.ts |
</artifacts>

<output>
Create `.planning/phases/07-admin-triage-ux-public-map/07-05-SUMMARY.md` when done
</output>
