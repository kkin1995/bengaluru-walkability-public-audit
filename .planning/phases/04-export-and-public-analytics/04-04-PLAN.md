---
phase: 04-export-and-public-analytics
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/package.json
  - frontend/app/components/HeatmapLayer.tsx
  - frontend/app/components/HeatmapLayer/__tests__/HeatmapLayer.test.tsx
  - frontend/app/components/ReportsMap.tsx
  - frontend/app/map/page.tsx
autonomous: true
requirements: [MAP-02]
must_haves:
  truths:
    - "The public /map page shows a toggleable issue-density heatmap layer driven by open/unresolved reports only"
    - "The heatmap toggle lives in the native Leaflet layer control (top-right), not the filter chip strip"
    - "The heatmap layer mounts without a server-side window error (ssr:false dynamic import)"
  artifacts:
    - path: "frontend/app/components/HeatmapLayer.tsx"
      provides: "leaflet.heat layer component filtering open reports"
      contains: "leaflet.heat"
    - path: "frontend/package.json"
      provides: "leaflet.heat + @types/leaflet.heat dependencies"
      contains: "leaflet.heat"
  key_links:
    - from: "frontend/app/components/HeatmapLayer.tsx"
      to: "leaflet heatLayer"
      via: "side-effect import 'leaflet.heat' + L.heatLayer on open reports"
      pattern: "heatLayer"
    - from: "frontend/app/map/page.tsx"
      to: "HeatmapLayer"
      via: "dynamic import ssr:false + L.control.layers overlay toggle"
      pattern: "control.layers|HeatmapLayer"
---

<objective>
Add a toggleable point-density heatmap layer to the public `/map` page using `leaflet.heat`, showing only open/unresolved reports (D-02), toggled via the native Leaflet layer control top-right (D-03).

Purpose: MAP-02 — citizens and planners see issue density geographically. This plan is frontend-only. It carries `depends_on: ["04-01"]` to formalize its Wave 2 ordering; it shares no source files with 04-01 (04-01 is backend export work), so there is no code conflict — the dependency is an ordering formality only.
Output: leaflet.heat dependency, HeatmapLayer component, layer-control wiring in the map, Wave 0 RTL test.
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-export-and-public-analytics/04-CONTEXT.md
@.planning/phases/04-export-and-public-analytics/04-RESEARCH.md
@.planning/phases/04-export-and-public-analytics/04-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install leaflet.heat and create HeatmapLayer component + Wave 0 test</name>
  <files>frontend/package.json, frontend/app/components/HeatmapLayer.tsx, frontend/app/components/HeatmapLayer/__tests__/HeatmapLayer.test.tsx</files>
  <read_first>
    - frontend/app/components/ReportsMap.tsx (existing Leaflet component: useRef for map/layers, useEffect lifecycle, how reports + status are typed, how the map instance is obtained)
    - frontend/app/map/page.tsx (ReportLite type, dynamic import ssr:false pattern, how reports flow to ReportsMap)
    - .planning/phases/04-export-and-public-analytics/04-RESEARCH.md Pattern 6, Pitfall 3 (TypeScript namespace), Standard Stack install command, Package Legitimacy Audit (all three packages Approved — cross-ecosystem slopcheck false positives, manually verified on npm)
    - .planning/phases/04-export-and-public-analytics/04-PATTERNS.md "HeatmapLayer.tsx" section
  </read_first>
  <action>
    Run `cd frontend && npm install leaflet.heat@0.2.0 @types/leaflet.heat@0.2.5` (versions verified on npm registry 2026-05-31; recharts is installed in 04-03b not here). If @types/leaflet.heat does not merge cleanly with the leaflet namespace, add a local ambient declaration or cast with `(L as any).heatLayer(...)` per Pitfall 3.
    Create frontend/app/components/HeatmapLayer.tsx. It does a side-effect import `import "leaflet.heat";` and uses the react-leaflet `useMap()` hook to get the Leaflet map instance. Props: `reports: Array<{ latitude: number; longitude: number; status: string }>` and `visible: boolean`. In a useEffect keyed on [map, reports, visible]: remove any existing layer ref; if not visible return; filter reports to `status === "open"` (D-02 — open/unresolved only); map to `[lat, lng, 1.0]` triples; create `(L as any).heatLayer(points, { radius: 25, blur: 15, maxZoom: 14, gradient: {...} }).addTo(map)`; store in a useRef; return a cleanup that removes the layer. Component returns null (Leaflet manages the canvas). Tune radius/blur/gradient for Bengaluru city zoom (Claude's discretion per CONTEXT.md).
    CLAUDE.md SSR rule: HeatmapLayer imports `leaflet.heat` (window-dependent). This file must NOT carry its own `dynamic(ssr:false)` wrapper and must NEVER be imported directly by a page or server component. It is window-safe ONLY because it is rendered exclusively as a child of ReportsMap, which map/page.tsx already imports via `dynamic(() => import(...), { ssr: false })`. Add a top-of-file comment stating this constraint explicitly so future maintainers do not import it elsewhere.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern=HeatmapLayer --passWithNoTests 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json dependencies contain `leaflet.heat` and `@types/leaflet.heat`
    - frontend/app/components/HeatmapLayer.tsx exists, contains `import "leaflet.heat"` and `heatLayer`, and filters on `status === "open"`
    - HeatmapLayer.tsx has a top-of-file comment documenting that it must only be rendered as a child of ReportsMap (the ssr:false boundary) and never imported directly
    - HeatmapLayer test exists and passes; the test proves only open reports reach heatLayer
    - `cd frontend && npm test -- --testPathPattern=HeatmapLayer` exits 0
  </acceptance_criteria>
  <done>leaflet.heat installed; HeatmapLayer renders open-only heat points without crashing; Wave 0 test green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire heatmap into the public map via Leaflet layer control toggle</name>
  <files>frontend/app/components/ReportsMap.tsx, frontend/app/map/page.tsx</files>
  <read_first>
    - frontend/app/components/ReportsMap.tsx (where the MapContainer / map instance is set up, where pin markers render, how it receives reports)
    - frontend/app/map/page.tsx (dynamic import block lines 36-52, reports state, chip filter strip — heatmap must NOT go in the chip strip per D-03)
    - .planning/phases/04-export-and-public-analytics/04-RESEARCH.md Pattern 6 "L.control.layers integration" + Open Question 3 RESOLVED (reuse same /api/reports fetch, filter client-side)
    - .planning/phases/04-export-and-public-analytics/04-PATTERNS.md "HeatmapLayer.tsx" L.control.layers section + "Dynamic import SSR guard" shared pattern
  </read_first>
  <action>
    Render HeatmapLayer inside ReportsMap (it must be a child of the react-leaflet MapContainer so useMap() resolves), or render it as a sibling that receives the map via useMap — whichever matches ReportsMap's structure. Pass the same reports array already fetched for the pin layer (no new API call per Open Question 3 RESOLVED); HeatmapLayer filters to open status internally. Add the heatmap as an overlay in a native `L.control.layers({}, { "Issue Density": <heatLayerRef> }, { position: "topright" })` so the user toggles it on/off (D-03) — wire `visible` from the layer-control overlay add/remove events, or add/remove the heat layer in response to the control's overlayadd/overlayremove events. Do NOT add a heatmap toggle to the existing chip filter strip in map/page.tsx.
    CLAUDE.md SSR rule — explicit: HeatmapLayer is window-dependent and MUST only ever be rendered as a child of ReportsMap. ReportsMap is the dynamic+ssr:false boundary (map/page.tsx imports it via `dynamic(() => import("../components/ReportsMap"), { ssr: false })`), so HeatmapLayer is transitively guarded against server-side `window` access. Do NOT import HeatmapLayer directly from map/page.tsx (which is/contains server-rendered code) or from any other page — that would bypass the ssr:false boundary and reintroduce the `window is not defined` crash. Keep HeatmapLayer's only import site inside ReportsMap.tsx.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -iE "error|window is not defined" | head -5; echo "done"</automated>
  </verify>
  <acceptance_criteria>
    - ReportsMap.tsx references HeatmapLayer and passes the existing reports array (no additional fetch added)
    - HeatmapLayer is imported ONLY by ReportsMap.tsx — grep map/page.tsx confirms it does NOT import HeatmapLayer directly
    - The heatmap toggle is registered via `L.control.layers` (grep ReportsMap.tsx for `control.layers`); the chip strip in map/page.tsx is unchanged regarding heatmap
    - `cd frontend && npm run build` produces zero TypeScript errors and no "window is not defined" SSR error
  </acceptance_criteria>
  <done>The public map shows a top-right layer control that toggles an open-reports density heatmap; build is clean with no SSR window error; HeatmapLayer is reached only through ReportsMap's ssr:false boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → frontend build | New leaflet.heat + @types/leaflet.heat dependencies introduced |
| public report data → browser heat canvas | Already-public report coordinates rendered client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | heatmap data source | accept | Heatmap uses the same already-public /api/reports data the pin layer uses; no new data exposure |
| T-04-SC | Tampering | leaflet.heat / @types/leaflet.heat npm install | mitigate | RESEARCH Package Legitimacy Audit: both packages Approved (Leaflet org / DefinitelyTyped), manually verified on npm registry; slopcheck PyPI flags were cross-ecosystem false positives; no postinstall scripts detected. Pin exact versions 0.2.0 / 0.2.5 |
</threat_model>

<verification>
- `cd frontend && npm test -- --testPathPattern=HeatmapLayer` passes
- `cd frontend && npm run build` zero TypeScript errors, no SSR window error
- leaflet.heat + @types/leaflet.heat present in package.json
</verification>

<success_criteria>
- Toggleable heatmap on /map driven by open/unresolved reports (D-02)
- Toggle in native Leaflet layer control top-right (D-03), not chip strip
- No SSR window crash; build clean
</success_criteria>

<output>
Create `.planning/phases/04-export-and-public-analytics/04-04-SUMMARY.md` when done
</output>
