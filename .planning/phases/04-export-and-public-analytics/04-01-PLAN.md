---
phase: 04-export-and-public-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/011_analytics_mv.sql
  - backend/src/db/admin_queries.rs
  - backend/src/handlers/admin.rs
  - backend/src/main.rs
  - backend/tests/export_tests.rs
  - frontend/app/admin/lib/adminApi.ts
  - frontend/app/admin/reports/page.tsx
autonomous: true
requirements: [EXPORT-01, EXPORT-02]
must_haves:
  truths:
    - "An authenticated admin can download a streaming CSV of filtered reports with DD/MM/YYYY dates and a ward_name column"
    - "An authenticated admin can download a streaming GeoJSON FeatureCollection with [longitude, latitude] coordinate order"
    - "The reports table has a resolved_at column that resolve_report() sets to NOW() when status becomes resolved or closed"
    - "Export endpoints return 401 when called without an admin session cookie"
  artifacts:
    - path: "backend/migrations/011_analytics_mv.sql"
      provides: "resolved_at column on reports + resolve_report dependency"
      contains: "ALTER TABLE reports ADD COLUMN IF NOT EXISTS resolved_at"
    - path: "backend/src/db/admin_queries.rs"
      provides: "EXPORT_CSV_SQL, EXPORT_GEOJSON_SQL constants, csv_escape, format_csv_date helpers"
      contains: "const EXPORT_CSV_SQL"
    - path: "backend/src/handlers/admin.rs"
      provides: "admin_export_csv, admin_export_geojson streaming handlers"
      contains: "pub async fn admin_export_csv"
    - path: "frontend/app/admin/reports/page.tsx"
      provides: "Export CSV + Export GeoJSON download buttons"
      contains: "Export CSV"
  key_links:
    - from: "backend/src/main.rs"
      to: "admin_export_csv / admin_export_geojson"
      via: "route registration inside admin_protected_router"
      pattern: "reports/export/csv"
    - from: "frontend/app/admin/reports/page.tsx"
      to: "downloadCsvExport / downloadGeoJsonExport"
      via: "Blob download via adminApi.ts"
      pattern: "downloadCsvExport"
---

<objective>
Add streaming CSV and GeoJSON export endpoints behind admin auth, plus the migration that adds the `resolved_at` column required by the CSV column set (D-13). Wire export download buttons into the existing `/admin/reports` page filter bar (D-07).

Purpose: Unlocks the GBA planner workflow — filtered audit exports in their preferred format without OOM on large datasets (D-15, D-16). Establishes migration 011 (which 04-02 depends on for the materialized view).
Output: Migration 011 (resolved_at column), streaming CSV/GeoJSON handlers, admin export UI, Wave 0 backend test scaffold.
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-export-and-public-analytics/04-CONTEXT.md
@.planning/phases/04-export-and-public-analytics/04-RESEARCH.md
@.planning/phases/04-export-and-public-analytics/04-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 011 — add resolved_at column and update resolve_report()</name>
  <files>backend/migrations/011_analytics_mv.sql, backend/src/db/admin_queries.rs</files>
  <read_first>
    - backend/migrations/008_workflow.sql (current reports schema, status enum values, resolution_photo_path/resolution_notes columns)
    - backend/migrations/010_org_seed.sql (latest migration; confirm next number is 011)
    - backend/src/db/admin_queries.rs lines 648-700 (resolve_report() current UPDATE statement — must add resolved_at = NOW())
    - .planning/phases/04-export-and-public-analytics/04-RESEARCH.md "Schema Investigation" + "Pitfall 8" sections
  </read_first>
  <action>
    Create backend/migrations/011_analytics_mv.sql. In THIS task, add ONLY the resolved_at column section: `ALTER TABLE reports ADD COLUMN IF NOT EXISTS resolved_at TIMESTAMPTZ;`. The materialized view + trigger sections of migration 011 are added by 04-02 (which depends on this plan) — leave a comment placeholder `-- public_stats_mv + trigger appended by plan 04-02` so the file ordering is preserved and 04-02 appends rather than reorders.
    Modify resolve_report() in admin_queries.rs: extend the existing UPDATE reports SET statement to also SET `resolved_at = NOW()`. The current SET clause sets status, resolution_photo_path, resolution_notes; add `resolved_at = NOW()` so any transition through resolve_report (status resolved or closed, already guarded by the matches! check) stamps the timestamp. Do not add a new bind parameter — NOW() is SQL-side.
  </action>
  <verify>
    <automated>cd backend && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backend/migrations/011_analytics_mv.sql exists and contains the exact string `ALTER TABLE reports ADD COLUMN IF NOT EXISTS resolved_at TIMESTAMPTZ`
    - backend/src/db/admin_queries.rs resolve_report() UPDATE statement contains `resolved_at = NOW()`
    - `cd backend && cargo check` exits 0
  </acceptance_criteria>
  <done>resolved_at column migration exists; resolve_report() stamps resolved_at; backend type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 export test scaffold + streaming export SQL, helpers, and handlers</name>
  <files>backend/tests/export_tests.rs, backend/src/db/admin_queries.rs, backend/src/handlers/admin.rs, backend/src/main.rs</files>
  <read_first>
    - backend/src/db/admin_queries.rs (INTAKE_SQL const pattern ~lines 877-883; build_report_where_clause ~lines 189-228; list_admin_reports ~lines 311-435; test-only pub fn fragment pattern ~lines 1085-1126)
    - backend/src/handlers/admin.rs (admin_list_reports + admin_get_intake_stats handler patterns; AdminReportFilters import and Query usage near line 410)
    - backend/src/main.rs lines 173-214 (admin_protected_router route registration block)
    - .planning/phases/04-export-and-public-analytics/04-RESEARCH.md Pattern 1, Pattern 2, Pattern 8, Pitfall 1, Pitfall 4, Pitfall 5, "CSV injection mitigation" section
    - .planning/phases/04-export-and-public-analytics/04-PATTERNS.md "admin_queries.rs" + "handlers/admin.rs" sections
  </read_first>
  <behavior>
    - Test export_csv_includes_all_columns: EXPORT_CSV_SQL fragment contains id, created_at, category, severity, status, ward_name, assigned_org (or organizations name), latitude, longitude, description, photo_hash, duplicate_count, submitter_contact, resolved_at, resolution_notes
    - Test csv_date_format_dd_mm_yyyy: format_csv_date for a known DateTime yields "%d/%m/%Y" form (e.g. 25/12/2025)
    - Test csv_escape_special_chars: a field with comma+quote becomes a double-quoted field with internal quotes doubled; a field starting with '=' is prefixed with a single quote
    - Test geojson_coordinate_order: the GeoJSON feature builder emits coordinates as [longitude, latitude] (lng first)
    - Test export_geojson_sql_no_select_star: EXPORT_GEOJSON_SQL fragment does not contain "SELECT *"
  </behavior>
  <action>
    Create backend/tests/export_tests.rs with the five tests above, referencing pub fn fragment helpers (export_csv_sql_fragment, format_csv_date, csv_escape) — RED first.
    In admin_queries.rs add: `const EXPORT_CSV_SQL: &str` selecting reports.id, reports.created_at, reports.category::TEXT, reports.severity::TEXT, reports.status::TEXT, wards.ward_name, organizations.name AS assigned_org, reports.latitude, reports.longitude, reports.description, reports.photo_hash, reports.duplicate_count, reports.submitter_contact, reports.resolved_at, reports.resolution_notes FROM reports LEFT JOIN wards ON wards.id = reports.ward_id LEFT JOIN organizations ON organizations.id = reports.assigned_org_id with a {where_clause} placeholder filled by build_report_where_clause and ORDER BY reports.created_at DESC. Add `const EXPORT_GEOJSON_SQL` selecting the same columns needed for D-16 (admin GeoJSON = same filter params, includes latitude/longitude/properties). Add helpers `fn format_csv_date(&DateTime<Utc>) -> String` (format "%d/%m/%Y"), `fn format_csv_date_opt(Option<&DateTime<Utc>>) -> String`, `fn csv_escape(&str) -> String` (wrap in double quotes, double internal quotes, strip \n/\r, prefix Excel formula triggers =,+,-,@ with single quote). Expose `pub fn export_csv_sql_fragment() -> &'static str` and `pub fn export_geojson_sql_fragment() -> &'static str` with #[allow(dead_code)] for tests. Make csv_escape/format_csv_date pub(crate) so tests reach them.
    In handlers/admin.rs add `pub async fn admin_export_csv` and `pub async fn admin_export_geojson`, both using the mpsc channel + `tokio_stream::wrappers::ReceiverStream` + `axum::body::Body::from_stream` streaming pattern (NOT StreamBody — Axum 0.7). CSV: send header line first, stream rows via sqlx fetch() (not fetch_all), format each row with csv_escape on description/notes/submitter_contact, set Content-Type text/csv; charset=utf-8 and Content-Disposition attachment filename "walkability-reports.csv". GeoJSON: stream opening `{"type":"FeatureCollection","features":[`, one comma-delimited Feature per row with coordinates [longitude, latitude], then closing `]}`; Content-Type application/geo+json, filename "walkability-reports.geojson". Both accept Query<AdminReportFilters> and Extension<AuthJwtClaims>.
    In main.rs register inside admin_protected_router: `.route("/api/admin/reports/export/csv", get(admin_export_csv))` and `.route("/api/admin/reports/export/geojson", get(admin_export_geojson))`.
  </action>
  <verify>
    <automated>cd backend && cargo test export 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/export_tests.rs exists with tests export_csv_includes_all_columns, csv_date_format_dd_mm_yyyy, csv_escape_special_chars, geojson_coordinate_order, export_geojson_sql_no_select_star
    - admin_queries.rs contains `const EXPORT_CSV_SQL` and `pub fn export_csv_sql_fragment(`
    - handlers/admin.rs contains `pub async fn admin_export_csv(` and `pub async fn admin_export_geojson(` using `Body::from_stream`
    - main.rs contains the string `reports/export/csv` and `reports/export/geojson`
    - `cd backend && cargo test export` exits 0
  </acceptance_criteria>
  <done>Streaming CSV and GeoJSON export handlers compile, are routed under admin auth, and the Wave 0 export tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: adminApi export functions + export buttons on /admin/reports</name>
  <files>frontend/app/admin/lib/adminApi.ts, frontend/app/admin/reports/page.tsx</files>
  <read_first>
    - frontend/app/admin/lib/adminApi.ts (apiFetch ~lines 139-158, getAdminReports filter-param building ~lines 185-201, AdminReportFilters type)
    - frontend/app/admin/reports/page.tsx (existing filter state + filter bar JSX + ReportsTable usage)
    - frontend/app/admin/components/Btn.tsx (Btn variant/size props)
    - .planning/phases/04-export-and-public-analytics/04-PATTERNS.md "adminApi.ts" + "admin/reports/page.tsx" sections
  </read_first>
  <action>
    In adminApi.ts add `export async function downloadCsvExport(filters?: AdminReportFilters): Promise<Blob>` and `export async function downloadGeoJsonExport(filters?: AdminReportFilters): Promise<Blob>`. Both build a URLSearchParams from the same filter shape used by getAdminReports (category, status, ward, date_from, date_to as applicable), call `fetch(\`${BASE}/api/admin/reports/export/csv?...\`, { credentials: "include" })` (geojson variant for the other), throw on !res.ok, return res.blob(). Use the same BASE constant getAdminReports uses (relative admin base).
    In admin/reports/page.tsx add a flex row of two Btn (variant="ghost" size="sm") below the existing filter controls and above ReportsTable: "Export CSV" and "Export GeoJSON". Add handleCsvDownload / handleGeoJsonDownload that call the adminApi functions with the page's current active filters, create an object URL from the Blob, trigger an anchor click with download="walkability-reports.csv" (/.geojson), then revokeObjectURL. Disable buttons while the page isLoading.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -iE "error|failed" | head -5; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - adminApi.ts contains `export async function downloadCsvExport(` and `export async function downloadGeoJsonExport(`
    - admin/reports/page.tsx contains the strings `Export CSV` and `Export GeoJSON` and references downloadCsvExport
    - `cd frontend && npm run build` completes with no TypeScript errors (grep for "error" yields no compiler error lines)
  </acceptance_criteria>
  <done>Export buttons appear on the admin reports page and trigger Blob downloads using current filters; frontend build is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /api/admin/reports/export/* | Authenticated admin input (filter params) crosses into SQL |
| DB free-text fields → CSV file → Excel | Report description/notes (originally untrusted citizen input) land in CSV cells |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | admin_export_csv / EXPORT_CSV_SQL | mitigate | Export is behind admin_protected_router require_auth; explicit column whitelist, no SELECT * |
| T-04-02 | Tampering | export filter params | mitigate | Filter values bound via sqlx .bind() through build_report_where_clause — no string interpolation |
| T-04-CSV | Tampering | csv_escape on description/resolution_notes | mitigate | CSV-injection mitigation: prefix cells starting with =,+,-,@ with single quote; double internal quotes; strip newlines |
| T-04-SC | Tampering | npm/cargo installs | mitigate | No new backend deps (tokio-util/bytes already present); no new packages in this plan — slopcheck N/A |
</threat_model>

<verification>
- `cd backend && cargo test` passes (export tests green)
- `cd backend && cargo check` exits 0
- `cd frontend && npm run build` has zero TypeScript errors
- Migration 011 file present with resolved_at column
</verification>

<success_criteria>
- Admin CSV export streams all D-13 columns with DD/MM/YYYY dates and ward_name
- Admin GeoJSON export streams a valid FeatureCollection with [lng, lat] coordinates
- resolved_at column exists and is set by resolve_report()
- Export endpoints are admin-only; export buttons wired into /admin/reports
</success_criteria>

<output>
Create `.planning/phases/04-export-and-public-analytics/04-01-SUMMARY.md` when done
</output>
