---
phase: 02.2
slug: vercel-staging-deployment-and-pre-uat-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-14
---

# Phase 02.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust), jest/vitest (Next.js frontend) |
| **Config file** | `backend/Cargo.toml`, `frontend/package.json` |
| **Quick run command** | `cd backend && cargo check` / `cd frontend && npm run build` |
| **Full suite command** | `cd backend && cargo test` / `cd frontend && npm run lint && npm run build` |
| **Estimated runtime** | ~60–90 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo check` (backend tasks) or `npm run build` (frontend tasks)
- **After every plan wave:** Run full suite
- **Before `/gsd:verify-work`:** Full suite must be green + manual smoke test against staging URL
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 02.2-01-01 | 01 | 1 | SameSite fix | unit/compile | `cd backend && cargo test` | ✅ | ⬜ pending |
| 02.2-01-02 | 01 | 1 | Next.js rewrites | build | `cd frontend && npm run build` | ✅ | ⬜ pending |
| 02.2-02-01 | 02 | 1 | deploy.yml smoke tests | CI | `cat .github/workflows/deploy.yml` | ✅ | ⬜ pending |
| 02.2-03-01 | 03 | manual | Railway + Vercel provisioning | manual | staging URL smoke test | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — no new test framework needed. The phase is primarily operational (deployment wiring) with two code-level fixes. Existing `cargo test` and `npm run build` provide sufficient automated feedback.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Admin login works cross-domain on staging | SameSite=None fix | Requires live Railway + Vercel deployment | 1. Open staging-walkability.kinariwala.com/admin/login 2. Enter admin credentials 3. Confirm redirect to /admin/reports and cookie is present |
| /api/admin/* routes reachable via Vercel proxy | Next.js rewrites | Requires live deployment | curl -I https://staging-walkability.kinariwala.com/api/admin/auth/me — should reach Railway backend, not 404 |
| ADMIN_SEED runs on first Railway boot | Seed idempotency | Requires Railway access | Check Railway deploy logs for "Admin seed: seeded admin user" or equivalent |
| Upload ephemeral OK for UAT | Ephemeral uploads | Requires manual review | Submit a test photo; confirm it displays; note it will be lost on redeploy |
| Staging smoke test passes in CI | deploy.yml | Requires staging to be live | Trigger workflow_dispatch on GitHub Actions, confirm deploy job succeeds |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
