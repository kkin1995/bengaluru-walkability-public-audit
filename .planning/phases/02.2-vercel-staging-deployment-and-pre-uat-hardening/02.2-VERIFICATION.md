---
phase: 02.2-vercel-staging-deployment-and-pre-uat-hardening
verified: 2026-04-14T00:00:00Z
status: human_needed
score: 7/7 automated must-haves verified
re_verification: false
human_verification:
  - test: "Admin login works on staging"
    expected: "Navigate to https://staging-walkability.kinariwala.com/admin/login, enter ADMIN_SEED_EMAIL/ADMIN_SEED_PASSWORD, confirm redirect to /admin/reports and that admin_token cookie is set under staging-walkability.kinariwala.com"
    why_human: "Requires Railway and Vercel to be live and DNS to resolve; cannot verify cross-domain cookie behavior programmatically against real services"
  - test: "Public report submission works on staging"
    expected: "Navigate to https://staging-walkability.kinariwala.com/report, submit a test photo with GPS coordinates, confirm success message and report appears on /map"
    why_human: "Requires live Railway backend with PostGIS running and real network request from browser"
  - test: "deploy.yml smoke test passes end-to-end"
    expected: "Go to GitHub Actions -> Deploy -> Run workflow (workflow_dispatch), confirm both the health check and reports endpoint steps succeed against the live Railway URL"
    why_human: "Requires RAILWAY_BACKEND_URL secret to be configured and Railway to be deployed; cannot trigger against live infra programmatically in this context"
---

# Phase 02.2: Vercel Staging Deployment and Pre-UAT Hardening — Verification Report

**Phase Goal:** Get the full stack onto a publicly accessible staging URL (staging-walkability.kinariwala.com) with cross-domain auth working so GBA/Walkaluru stakeholders can do UAT before Phase 3 ships
**Verified:** 2026-04-14
**Status:** human_needed — all automated checks pass; live staging environment verification requires human
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Admin login works on staging: cross-domain auth code is correct (SameSite=None + Vercel rewrite) | ✓ VERIFIED | `admin.rs:320` has `SameSite::None`; `next.config.mjs` rewrites `/api/admin/:path*` to `INTERNAL_API_URL`; `adminApi.ts` and `login/page.tsx` both import `ADMIN_API_BASE_URL` (empty string = relative URL through rewrite) |
| 2 | Public report submission works on staging: non-admin routes still use Railway URL directly | ✓ VERIFIED | `frontend/app/report/page.tsx` imports `API_BASE_URL` (not `ADMIN_API_BASE_URL`); rewrites do NOT proxy `/api/reports` |
| 3 | deploy.yml triggers smoke tests after push-to-main with retry logic for Railway cold-start | ✓ VERIFIED | `deploy.yml` has `workflow_dispatch` trigger, `smoke-test` job with `needs: ci`, retry loop (5 attempts x 15s), `RAILWAY_BACKEND_URL` secret env var, hits `/health` and `/api/reports` |
| 4 | STAGING-SETUP.md exists with step-by-step provisioning | ✓ VERIFIED | `STAGING-SETUP.md` exists at repo root, 360 lines, covers all 9 Railway env vars, 2 Vercel env vars, 1 GitHub Actions secret, deployment ordering constraint, pre-UAT checklist, known limitations, troubleshooting |
| 5 | cargo test and npm run build pass with cross-domain auth changes | ✓ VERIFIED (via SUMMARY) | 02.2-01-SUMMARY.md documents: `cargo check` PASSED, `cargo test` PASSED (225 tests), `npm run build` PASSED (13 pages). 5 pre-existing test failures confirmed pre-date this phase via git stash test. |

**Score:** 5/5 truths verified (3 require live environment confirmation — see Human Verification below)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/src/handlers/admin.rs` | SameSite::None cookie setting | ✓ VERIFIED | Line 320: `cookie.set_same_site(axum_extra::extract::cookie::SameSite::None)` with explanatory comment. `COOKIE_SECURE` guard at lines 311–325 unchanged. |
| `frontend/next.config.mjs` | async rewrites() proxying /api/admin/* | ✓ VERIFIED | Lines 14–29: `async rewrites()` function with `source: "/api/admin/:path*"` and `destination: \`${backendUrl}/api/admin/:path*\``. `/api/reports` is NOT proxied. |
| `frontend/app/lib/config.ts` | Exports API_BASE_URL, INTERNAL_API_URL, ADMIN_API_BASE_URL | ✓ VERIFIED | Line 18: `API_BASE_URL`, line 19: `ADMIN_API_BASE_URL = ""` (hardcoded empty string), lines 20–21: `INTERNAL_API_URL`. All three exports present. |
| `frontend/app/admin/lib/adminApi.ts` | Imports ADMIN_API_BASE_URL (not API_BASE_URL) | ✓ VERIFIED | Line 11: `import { ADMIN_API_BASE_URL as BASE } from "@/app/lib/config"`. All API calls use `${BASE}/api/admin/...` pattern. |
| `frontend/app/admin/login/page.tsx` | Imports ADMIN_API_BASE_URL (not API_BASE_URL) | ✓ VERIFIED | Line 5: `import { ADMIN_API_BASE_URL as BASE } from "@/app/lib/config"`. Login fetch uses `${BASE}/api/admin/auth/login`. |
| `.github/workflows/deploy.yml` | workflow_dispatch trigger + smoke-test job + RAILWAY_BACKEND_URL | ✓ VERIFIED | Lines 7: `workflow_dispatch:`, lines 16–56: `smoke-test` job with `needs: ci`, `RAILWAY_BACKEND_URL` env from secret, 5-attempt retry loop, `/health` check, `/api/reports` check, Vercel frontend check. No placeholder text. |
| `backend/railway.toml` | DOCKERFILE builder + /health healthcheck | ✓ VERIFIED | Lines 5–6: `builder = "DOCKERFILE"`, `dockerfilePath = "Dockerfile"`. Lines 9–13: `healthcheckPath = "/health"`, `healthcheckTimeout = 300`, `restartPolicyType = "ON_FAILURE"`, `restartPolicyMaxRetries = 5`. |
| `STAGING-SETUP.md` | 100+ lines, all env vars, pre-UAT checklist | ✓ VERIFIED | 360 lines. All 9 Railway env vars present in table (DATABASE_URL, JWT_SECRET, COOKIE_SECURE=true, CORS_ORIGIN, UPLOADS_DIR, PORT, RUST_LOG, ADMIN_SEED_EMAIL, ADMIN_SEED_PASSWORD). Both Vercel env vars (NEXT_PUBLIC_API_URL, INTERNAL_API_URL). GitHub secret (RAILWAY_BACKEND_URL). Deployment ordering section. Pre-UAT verification checklist (12 items). Known Limitations section (ephemeral uploads). Troubleshooting table. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `frontend/app/admin/lib/adminApi.ts` | `/api/admin/*` (Vercel rewrite) | `ADMIN_API_BASE_URL = ""` making relative URLs | ✓ WIRED | All 11 admin API calls use `${BASE}/api/admin/...`; BASE = "" means calls go to `/api/admin/...` on Vercel which triggers the rewrite |
| `frontend/next.config.mjs` rewrites | Railway backend via `INTERNAL_API_URL` | Next.js server-side rewrite proxy | ✓ WIRED | `source: "/api/admin/:path*"` -> `destination: \`${backendUrl}/api/admin/:path*\`` where `backendUrl = process.env.INTERNAL_API_URL || "http://localhost:3001"` |
| `frontend/app/admin/layout.tsx` | Railway backend | `INTERNAL_API_URL` server-side fetch | ✓ WIRED | Line 3 imports `INTERNAL_API_URL`, line 28 uses `${INTERNAL_API_URL}/api/admin/auth/me` — unchanged from pre-phase state |
| `frontend/app/report/page.tsx` | Railway backend directly | `API_BASE_URL` (= `NEXT_PUBLIC_API_URL`) | ✓ WIRED | Imports `API_BASE_URL` (not ADMIN variant); calls `${API_BASE_URL}/api/reports`; bypasses Vercel rewrite as intended |
| `.github/workflows/deploy.yml` | Railway backend `/health` | `curl` with retry using `RAILWAY_BACKEND_URL` secret | ✓ WIRED | Pattern: `curl -sf --max-time 10 "$RAILWAY_BACKEND_URL/health"` inside 5-attempt loop |
| `.github/workflows/deploy.yml` | Vercel staging frontend | `curl` to `staging-walkability.kinariwala.com` | ✓ WIRED | Pattern: `curl -sf --max-time 15 -o /dev/null -w "%{http_code}" "$VERCEL_STAGING_URL"` with HTTP 200 assertion |
| `STAGING-SETUP.md` env var checklist | Railway backend service | Variable names matching backend/.env.example | ✓ WIRED | All 9 env var names from backend/.env.example are present in the STAGING-SETUP.md Railway table |
| `STAGING-SETUP.md` env var checklist | Vercel frontend project | NEXT_PUBLIC_API_URL and INTERNAL_API_URL | ✓ WIRED | Both Vercel env vars listed with correct scope (Production + Preview) and note that NEXT_PUBLIC_API_URL is baked at build time |

---

### Data-Flow Trace (Level 4)

Level 4 data-flow trace is not applicable for this phase. The deliverables are configuration files, a CI workflow, and documentation — not components that render dynamic data. No hollow-prop or disconnected data source patterns to check.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `SameSite::None` is set on cookie | `grep "SameSite::None" backend/src/handlers/admin.rs` | Line 320: `cookie.set_same_site(axum_extra::extract::cookie::SameSite::None)` | ✓ PASS |
| `SameSite::Strict` is removed | `grep "SameSite::Strict" backend/src/handlers/admin.rs` | No match | ✓ PASS |
| rewrites() function present and not proxying reports | `grep "source.*api" frontend/next.config.mjs` | Only `/api/admin/:path*` found; no `/api/reports` | ✓ PASS |
| ADMIN_API_BASE_URL is hardcoded `""` | `grep "ADMIN_API_BASE_URL" frontend/app/lib/config.ts` | `export const ADMIN_API_BASE_URL = "";` — hardcoded empty string, not env var | ✓ PASS |
| deploy.yml has no placeholder text | `grep "Staging deploy placeholder" .github/workflows/deploy.yml` | No match | ✓ PASS |
| railway.toml healthcheck configured | `grep "healthcheckPath" backend/railway.toml` | `healthcheckPath = "/health"` | ✓ PASS |
| STAGING-SETUP.md line count >= 100 | `wc -l STAGING-SETUP.md` | 360 lines | ✓ PASS |
| deploy.yml YAML is readable | Node file read | 1804 bytes, no parse error | ✓ PASS |
| admin/layout.tsx unchanged | `grep "INTERNAL_API_URL" frontend/app/admin/layout.tsx` | Still uses `INTERNAL_API_URL` — unchanged | ✓ PASS |

---

### Requirements Coverage

The ROADMAP.md specifies STAGING-01 through STAGING-06 for this phase. These IDs do not appear in REQUIREMENTS.md (the canonical requirements file only tracks domain requirements: WARD-xx, ABUSE-xx, WFLOW-xx, etc.). The STAGING-xx IDs are phase-local deployment requirements documented in the ROADMAP.md and CONTEXT.md (as D-xx decisions). The plans declare D-xx IDs which map directly to the CONTEXT.md decision list.

| Plan Req IDs | Plan | Decision Description | Status |
|-------------|------|---------------------|--------|
| D-05, D-06, D-07, D-08 (as D-12-prereq) | 02.2-01 | Cross-domain auth: ADMIN_API_BASE_URL split, INTERNAL_API_URL for rewrites, COOKIE_SECURE dependency, SameSite=None | ✓ SATISFIED |
| D-09, D-10, D-11, D-12 | 02.2-02 | Railway GitHub auto-deploy, workflow_dispatch trigger, CI gate + smoke tests | ✓ SATISFIED |
| D-01, D-02, D-03, D-04, D-13, D-14, D-15, D-16 | 02.2-03 | Vercel+Railway architecture, nginx dropped, cost target, staging URL, env var checklist, HTTPS-only, admin verification, minimal seeding | ✓ SATISFIED (all documented in STAGING-SETUP.md) |

**STAGING-01 through STAGING-06 mapping:** These ROADMAP.md IDs are not defined in REQUIREMENTS.md, which means they are deployment-specific requirements tracked only at the roadmap level. Based on the ROADMAP.md Success Criteria, the mapping is:

| ROADMAP ID | Mapped To | Status |
|-----------|-----------|--------|
| STAGING-01 | Admin cross-domain auth works (SameSite=None + rewrite proxy) | ✓ Code changes verified; live test is human item |
| STAGING-02 | Public report submission unaffected (API_BASE_URL preserved) | ✓ SATISFIED |
| STAGING-03 | CI smoke tests in deploy.yml | ✓ SATISFIED |
| STAGING-04 | STAGING-SETUP.md operational guide | ✓ SATISFIED |
| STAGING-05 | cargo test and npm run build pass | ✓ SATISFIED (per SUMMARY; SUMMARY documents 225 cargo tests pass, npm build passes 13 pages) |
| STAGING-06 | Railway config-as-code (railway.toml) | ✓ SATISFIED |

No orphaned requirements found — all STAGING-xx IDs are accounted for by the deliverables.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

Scanned: `backend/src/handlers/admin.rs`, `frontend/next.config.mjs`, `frontend/app/lib/config.ts`, `frontend/app/admin/lib/adminApi.ts`, `frontend/app/admin/login/page.tsx`, `.github/workflows/deploy.yml`, `backend/railway.toml`, `STAGING-SETUP.md`. No TODO/FIXME/PLACEHOLDER comments. No empty return stubs. No hardcoded `[]` or `{}` passed to rendering paths. `ADMIN_API_BASE_URL = ""` is the correct intentional hardcoded value (not a stub — it causes relative URL routing through the Vercel rewrite proxy by design).

---

### Human Verification Required

#### 1. Admin Login Works on Staging (STAGING-01)

**Test:** Provision Railway (PostGIS DB + backend with all env vars from STAGING-SETUP.md), Vercel (frontend with NEXT_PUBLIC_API_URL and INTERNAL_API_URL pointing to Railway), DNS (CNAME for staging-walkability.kinariwala.com). Navigate to `https://staging-walkability.kinariwala.com/admin/login`. Enter ADMIN_SEED_EMAIL and ADMIN_SEED_PASSWORD. Check redirect to `/admin/reports`.

**Expected:** Redirect to `/admin/reports` succeeds. Browser DevTools > Application > Cookies shows `admin_token` cookie set under `staging-walkability.kinariwala.com` (not under the Railway domain). Cookie has `Secure` and `HttpOnly` attributes.

**Also test:** Open incognito window, navigate directly to `https://staging-walkability.kinariwala.com/admin/reports` — should redirect to login (middleware guard).

**Why human:** Requires live Railway and Vercel deployments with real DNS resolution. Cookie scoping behavior (SameSite=None + rewrite proxy ensuring cookie is set on Vercel domain) cannot be verified without a browser making actual cross-domain requests.

---

#### 2. Public Report Submission Works on Staging (STAGING-02 cross-check)

**Test:** Navigate to `https://staging-walkability.kinariwala.com/report`. Upload a test photo with GPS coordinates. Submit. Navigate to `https://staging-walkability.kinariwala.com/map` and confirm the report pin appears.

**Expected:** Report submission succeeds (no CORS error, no 404). Report appears on map at submitted GPS location.

**Why human:** Requires live Railway backend with PostGIS running, real network request from browser to Railway URL, and map tile rendering.

---

#### 3. GitHub Actions Smoke Test Passes End-to-End

**Test:** In GitHub repository, go to Actions > Deploy > Run workflow (workflow_dispatch). Confirm the workflow completes. Check that the `smoke-test` job's steps all pass: Railway /health responds, /api/reports responds, Vercel staging returns HTTP 200.

**Expected:** All three smoke-test steps pass. Workflow completes with green checkmark.

**Why human:** Requires `RAILWAY_BACKEND_URL` GitHub Actions secret to be set and Railway backend to be live. Cannot trigger live CI run in this context.

---

### Gaps Summary

No automated gaps found. All code changes are correctly implemented, all files exist with substantive content, all key links are wired. The phase cannot be declared fully "passed" only because the core deliverable — a live staging environment accessible at `https://staging-walkability.kinariwala.com` — requires infrastructure provisioning and browser-based testing to confirm.

The code is ready. Staging provisioning follows the STAGING-SETUP.md runbook.

---

_Verified: 2026-04-14_
_Verifier: Claude (gsd-verifier)_
