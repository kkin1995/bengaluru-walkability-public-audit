---
phase: 03.2-admin-uat-gap-fixes-dashboard-chart-period-filter-and-status
verified: 2026-05-30T09:15:00Z
status: human_needed
score: 9/9 must-haves verified
overrides_applied: 0
human_verification:
  - test: "On /admin, click 7D then 30D — visually confirm the active button highlights change and the sparkbar chart visibly redraws with a different bar count (7 bars vs 30 bars)"
    expected: "7D pill is highlighted accent, chart shows 7 bars; 30D pill is highlighted accent, chart shows 30 bars; 14D reverts to outline in both cases"
    why_human: "Pixel rendering and visual active-state feedback require a browser; automated tests assert data-tone attribute which proxies visual state but the actual rendered appearance needs human confirmation"
  - test: "On /admin/reports/[id] for a report with real status history entries in the DB, verify the timeline renders the changer's real display_name and a valid formatted date (not 'Invalid Date' or 'SYSTEM')"
    expected: "Each history entry shows a date in locale format and the admin's display_name (or '—' if null); no 'SYSTEM' text appears anywhere on the page"
    why_human: "Requires a live DB with real status_history rows written by an authenticated admin session; cannot be tested with grep or static analysis"
---

# Phase 03.2: Admin UAT Gap Fixes — Dashboard Chart Period Filter and Status History Attribution

**Phase Goal:** Close two UAT gaps from Phase 03.1 — make 7D/14D/30D period buttons functional on the admin dashboard (BUG-03.1-B) and replace the hardcoded "BY · SYSTEM / Invalid Date" placeholder with real status_history rendering (BUG-03.1-C).
**Verified:** 2026-05-30T09:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | GET /api/admin/reports/:id JSON response includes a non-null updated_at ISO-8601 timestamp | VERIFIED | `admin_queries.rs` line 495 adds `r.updated_at` to SELECT; line 593 includes `"updated_at": r.get::<DateTime<Utc>, _>("updated_at")` in the JSON object |
| 2 | GET /api/admin/reports/:id JSON response includes a status_history array, newest entry first | VERIFIED | Second query at lines 537–555 fetches `FROM status_history sh ... ORDER BY sh.changed_at DESC`; result collected into `Vec<serde_json::Value>` and added as `"status_history"` field at line 611 |
| 3 | Each status_history entry exposes new_status, old_status, changed_at, note, and changed_by_name joined from admin_users.display_name | VERIFIED | LEFT JOIN `admin_users au ON au.id = sh.changed_by` at line 548; `au.display_name AS changed_by_name` at line 546; all 7 fields (`id`, `old_status`, `new_status`, `changed_at`, `note`, `changed_by`, `changed_by_name`) mapped in serde_json! at lines 560–568 |
| 4 | When status_history.changed_by is NULL or display_name is NULL, changed_by_name is null (never the literal 'SYSTEM') | VERIFIED | `h.try_get::<String, _>("changed_by_name").ok()` at line 567 — `.ok()` converts missing/NULL to `None`, which serializes as JSON `null`; no hardcoded "SYSTEM" string present in admin_queries.rs or admin.rs |
| 5 | On /admin, clicking 7D, 14D, or 30D changes which button is visually active | VERIFIED | `useState<"7D" \| "14D" \| "30D">("14D")` at page.tsx line 41; each period label wrapped in `<button type="button" onClick={() => setActivePeriod(label)}>` at lines 457–476; Pill tone driven by `label === activePeriod ? "accent" : "outline"` at line 471; dashboard test confirms click on 7D sets 7D to `data-tone=accent` and 14D to `data-tone=outline` |
| 6 | On /admin, clicking a period button recomputes the intake sparkbars window for that period | VERIFIED | `sparkbarsValues` computed via IIFE at lines 103–115 — `dayCount` is 7/14/30 based on `activePeriod`; slices or cyclically pads STUB_SPARKBARS; passed as `values={sparkbarsValues}` to `<Sparkbars>` at line 481 |
| 7 | On /admin/reports/[id], the status history section renders one entry per status_history array item from the API | VERIFIED | Lines 477–513 in page.tsx: `report.status_history.map((entry: StatusHistoryEntry) => ...)` renders one `<div>` per entry when array is non-empty |
| 8 | Each status history entry shows formatDate(changed_at) (a valid date, never 'Invalid Date') and changed_by_name, never the literal 'SYSTEM' | VERIFIED | Line 504: `{formatDate(entry.changed_at)} · BY · {entry.changed_by_name \|\| "—"}`; no "SYSTEM" literal exists in page.tsx (grep returned empty); test at line 345 asserts `queryByText(/SYSTEM/)` is null; test at line 361 asserts `queryByText(/Invalid Date/)` is null |
| 9 | When status_history is empty, the section shows the current status as a single fallback entry without 'Invalid Date' or 'SYSTEM' | VERIFIED | Fallback at lines 514–544: uses `report.status` for dot/label and `formatDate(report.updated_at)` for date (now non-null from Plan 01); attribution hardcoded `"—"` (not "SYSTEM"); tests at lines 364 and 377 assert fallback renders without SYSTEM |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/src/db/admin_queries.rs` | get_admin_report_by_id with r.updated_at + status_history JOIN query | VERIFIED | Lines 495 (updated_at in SELECT), 537–555 (status_history query with LEFT JOIN admin_users), 611 (status_history in JSON) |
| `backend/src/handlers/admin.rs` | admin_get_report serializes updated_at and status_history into the response | VERIFIED | Handler at line 479 returns `Ok(Json(report))` where `report` is the full `serde_json::Value` from `get_admin_report_by_id` — no field whitelist, no struct re-mapping |
| `frontend/app/admin/page.tsx` | Clickable period buttons with active-state useState and period-driven sparkbars window | VERIFIED | useState at line 41, button wrappers at lines 457–476, sparkbarsValues computed from activePeriod at lines 103–115 |
| `frontend/app/admin/reports/[id]/page.tsx` | Real status_history rendering replacing the hardcoded 'BY · SYSTEM' placeholder | VERIFIED | Lines 477–546 implement the map/fallback renderer; zero "SYSTEM" occurrences in file |
| `frontend/app/admin/lib/adminApi.ts` | StatusHistoryEntry TypeScript type + status_history field on AdminReport | VERIFIED | `StatusHistoryEntry` interface exported at lines 37–45 with all 7 required fields; `status_history?: StatusHistoryEntry[]` on AdminReport at line 90 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `admin_queries.rs get_admin_report_by_id` | `status_history` table | LEFT JOIN admin_users ON sh.changed_by | WIRED | Line 548: `LEFT JOIN admin_users au ON au.id = sh.changed_by` — uses `changed_by` (not `changed_by_id`) |
| `get_admin_report_by_id JSON` | `reports.updated_at` column | r.updated_at added to SELECT and json | WIRED | Line 495 SELECT, line 593 JSON |
| `page.tsx status history section` | `report.status_history` array | .map over status_history entries | WIRED | Lines 477–513 map over `report.status_history` |
| `page.tsx period buttons` | `activePeriod` state | onClick setActivePeriod | WIRED | Line 460: `onClick={() => setActivePeriod(label)}` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `page.tsx` status history section | `report.status_history` | `getAdminReport()` in adminApi.ts → `/api/admin/reports/:id` → `get_admin_report_by_id` → `status_history` DB table | DB query `FROM status_history sh LEFT JOIN admin_users au` with parameterized `report_id` | FLOWING |
| `page.tsx` sparkbars | `sparkbarsValues` | Derived from `STUB_SPARKBARS` constant sliced/padded by `activePeriod` | Intentional stub — plan explicitly scopes to frontend-only recompute; documented in SUMMARY.md; no real per-day intake endpoint exists yet | STATIC (intentional — documented as known stub in 03.2-02-SUMMARY.md, future phase concern) |
| `admin_queries.rs` | `updated_at` | `r.get::<DateTime<Utc>, _>("updated_at")` from DB row | DB column `reports.updated_at` maintained by trigger `trg_reports_updated_at` | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — requires live DB and running backend server. The backend changes are to SQL queries that cannot be meaningfully spot-checked without a running PostgreSQL instance. Frontend rendering changes are covered by the test suite.

### Probe Execution

Step 7c: No probes declared in PLAN files; no `scripts/*/tests/probe-*.sh` files found for this phase. SKIPPED.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| BUG-03.1-C (backend) | 03.2-01-PLAN.md | status_history + updated_at in GET /api/admin/reports/:id response | SATISFIED | admin_queries.rs get_admin_report_by_id returns both fields; handler passes raw JSON value |
| BUG-03.1-B | 03.2-02-PLAN.md | 7D/14D/30D period buttons functional with active state and chart recompute | SATISFIED | useState activePeriod + onClick wiring + sparkbarsValues derivation all present |
| BUG-03.1-C (frontend) | 03.2-02-PLAN.md | Replace "BY · SYSTEM / Invalid Date" placeholder with real status_history rendering | SATISFIED | map renderer over status_history with `changed_by_name \|\| "—"` fallback; no SYSTEM literal; tests assert non-SYSTEM, non-Invalid-Date |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `frontend/app/admin/page.tsx` | 30 | `STUB_SPARKBARS` constant drives `sparkbarsValues` | Info | Intentional, documented stub per plan scope — D-01 explicitly locks to frontend-only recompute without fabricating backend data. SUMMARY.md documents this as a known stub for a future phase. Not a blocker. |

No TBD, FIXME, or XXX markers found in any of the four modified files.

### Human Verification Required

#### 1. Period Button Visual Feedback (BUG-03.1-B live check)

**Test:** On a running instance, navigate to /admin. Click "7D" — confirm the 7D pill visually highlights (accent style) and the 14D pill reverts to outline. Then click "30D" — confirm the chart redraws with visibly more bars.
**Expected:** Active button is visually distinct (accent/filled); chart bar count changes per period selection.
**Why human:** Automated tests proxy this via `data-tone` attribute on Pill. The actual CSS rendering of `tone="accent"` vs `tone="outline"` requires browser paint to confirm. The chart bar count change is a pixel-level visual difference only observable in a browser.

#### 2. Real Status History Attribution on Report Detail (BUG-03.1-C live check)

**Test:** On a running instance with live DB data, navigate to /admin/reports/[id] for a report that has at least one status change recorded in `status_history` by a named admin user. Confirm the timeline shows: (a) the admin's display_name (not "SYSTEM"), (b) a valid formatted date (not "Invalid Date"), (c) the entry count matches the number of history rows.
**Expected:** Real names appear. No "SYSTEM". No "Invalid Date". Multiple entries visible for reports with multiple history rows.
**Why human:** Requires a live PostgreSQL instance with `status_history` rows populated by actual admin operations. Static analysis and unit tests mock the data shape but cannot verify the full round-trip from DB to render.

### Gaps Summary

No gaps. All 9 must-haves are VERIFIED against the actual codebase. The phase goal — closing BUG-03.1-B (period filter) and BUG-03.1-C (status history attribution) — is achieved in code. The `sparkbarsValues` stub is explicitly scoped and documented, not a defect.

Two human verification items remain for live-browser and live-DB confirmation. These are normal end-of-phase UAT items, not code gaps.

---

_Verified: 2026-05-30T09:15:00Z_
_Verifier: Claude (gsd-verifier)_
