---
phase: 04.1-close-gap-analytics-03-ward-03-link-wards-to-organisations
plan: "02"
subsystem: ui
tags: [typescript, nextjs, adminapi, reportstable, type-safety]

# Dependency graph
requires:
  - phase: 03.4-org-auto-assign-compact-nav
    provides: AdminReport interface with ward_hierarchy field, ReportsTable CORP column

provides:
  - AdminReport interface with typed corporation field (non-optional string | null)
  - ReportsTable CORP column reading report.corporation without unsafe cast

affects:
  - 04.1-close-gap-analytics-03-ward-03-link-wards-to-organisations

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Local interface extension: local Report interface in ReportsTable.tsx extended to mirror relevant AdminReport fields to avoid unsafe casts"

key-files:
  created: []
  modified:
    - frontend/app/admin/lib/adminApi.ts
    - frontend/app/admin/components/ReportsTable.tsx

key-decisions:
  - "Added corporation: string | null (non-optional) to AdminReport because the backend always returns the field from the LEFT JOIN wards query"
  - "Extended the local Report interface in ReportsTable.tsx with corporation? and ward_hierarchy? instead of casting — ensures type safety without changing local interface semantics"
  - "Imported WardHierarchy type into ReportsTable.tsx for local Report interface typing"

patterns-established:
  - "Type safety: when removing unsafe casts from component files, ensure the local interface mirrors the fields needed — do not rely only on the central AdminReport interface"

requirements-completed:
  - NF-04.1-C

# Metrics
duration: 8min
completed: 2026-06-01
---

# Phase 04.1 Plan 02: Add corporation to AdminReport and Remove Unsafe Cast Summary

**Restored TypeScript type safety for CORP column: added `corporation: string | null` to AdminReport interface and replaced double `as unknown as` cast in ReportsTable.tsx with direct `report.corporation` typed field access.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-06-01T00:00:00Z
- **Completed:** 2026-06-01T00:08:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- Added `corporation: string | null` (non-optional) to `AdminReport` interface in adminApi.ts
- Extended local `Report` interface in ReportsTable.tsx with `corporation?` and `ward_hierarchy?` fields (WardHierarchy import added)
- Replaced unsafe `(report as unknown as {corporation?: string | null}).corporation ?? (report as unknown as {...}).ward_hierarchy?.corporation` double-cast with clean `report.corporation ?? report.ward_hierarchy?.corporation ?? "—"` fallback chain
- `npm run build` exits 0 with no TypeScript errors
- `npm run lint` exits 0 on both modified files

## Task Commits

Each task was committed atomically:

1. **Task 1: Add corporation field to AdminReport and clean ReportsTable cast** - `4c43c73` (fix)

**Plan metadata:** (to be committed with SUMMARY)

## Files Created/Modified
- `frontend/app/admin/lib/adminApi.ts` - Added `corporation: string | null` to AdminReport interface after `ward_name`
- `frontend/app/admin/components/ReportsTable.tsx` - Imported WardHierarchy, extended local Report interface, replaced unsafe cast with direct field access

## Decisions Made
- Used non-optional `corporation: string | null` (not `corporation?`) because the backend always includes the field in list_admin_reports JSON via LEFT JOIN wards, defaulting to NULL when no ward is assigned
- Extended the local `Report` interface in ReportsTable.tsx with optional fields (`corporation?`, `ward_hierarchy?`) to match practical API response shape while keeping the interface focused on component needs

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Local Report interface in ReportsTable.tsx missing corporation and ward_hierarchy fields**
- **Found during:** Task 1 (build verification after removing cast)
- **Issue:** The `report` variable in CORP column cell is typed as local `Report` interface, not `AdminReport`. The local `Report` interface only had dedup fields but not `corporation` or `ward_hierarchy`. TypeScript error: `Property 'corporation' does not exist on type 'Report'`.
- **Fix:** Imported `WardHierarchy` from adminApi.ts; added `corporation?: string | null` and `ward_hierarchy?: WardHierarchy | null` to the local `Report` interface
- **Files modified:** `frontend/app/admin/components/ReportsTable.tsx`
- **Verification:** `npm run build` exits 0, `npm run lint` exits 0
- **Committed in:** `4c43c73` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — local interface missing fields required for typed cast removal)
**Impact on plan:** Auto-fix was necessary for correctness — removing the cast without updating the local interface would have made the build fail. No scope creep.

## Issues Encountered
None beyond the deviation documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- AdminReport.corporation is now a typed, non-optional field
- ReportsTable CORP column reads report.corporation cleanly with no unsafe casts
- No downstream blockers for remaining plans in phase 04.1

## Self-Check

### Files exist:
- `frontend/app/admin/lib/adminApi.ts` - modified
- `frontend/app/admin/components/ReportsTable.tsx` - modified

### Verification commands passed:
- `grep "corporation: string | null" frontend/app/admin/lib/adminApi.ts` - 1 match (AdminReport field)
- `grep "as unknown as" frontend/app/admin/components/ReportsTable.tsx` - 0 matches
- `grep "report.corporation" frontend/app/admin/components/ReportsTable.tsx` - 1 match
- `npm run build` - exits 0
- `npm run lint` - exits 0

---
*Phase: 04.1-close-gap-analytics-03-ward-03-link-wards-to-organisations*
*Completed: 2026-06-01*
