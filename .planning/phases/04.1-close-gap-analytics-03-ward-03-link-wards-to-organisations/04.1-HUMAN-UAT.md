---
status: partial
phase: 04.1-close-gap-analytics-03-ward-03-link-wards-to-organisations
source: [04.1-VERIFICATION.md]
started: 2026-06-01T13:30:00Z
updated: 2026-06-01T13:45:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. TypeScript build passes

expected: `cd frontend && npm run build` exits 0 with no TypeScript errors. Verifies that `corporation: string | null` on `AdminReport` and the `report.corporation` access in ReportsTable.tsx are accepted by the compiler.
result: pass
note: "Build exits 0. All 16 routes compile cleanly with zero TypeScript errors."

### 2. Live DB: zero wards with null org_id after migration 014

expected: After starting the stack and running the backend (which applies migration 014), query `SELECT COUNT(*) FROM wards WHERE org_id IS NULL` returns 0. Confirms all 369 ward rows are linked to their parent corporation organization.
result: pass
note: "Confirmed via docker exec: SELECT COUNT(*) FROM wards WHERE org_id IS NULL → 0. All 369 wards linked."

### 3. Runtime analytics endpoint returns non-null resolution rates

expected: Hit `GET /api/admin/analytics/corporations` on staging after migration 014 runs. Expected: corporation rows with numeric (non-null) `resolution_rate_pct` values. Confirms the `::float8` cast resolves the OID mismatch end-to-end in production.
result: [pending]

## Summary

total: 3
passed: 2
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
