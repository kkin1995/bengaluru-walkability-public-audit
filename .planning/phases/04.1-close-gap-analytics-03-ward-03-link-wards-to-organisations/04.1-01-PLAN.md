---
phase: 04.1-close-gap-analytics-03-ward-03-link-wards-to-organisations
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/migrations/014_link_wards_to_organisations.sql
  - backend/tests/analytics_tests.rs
autonomous: true
requirements:
  - NF-04.1-A
  - NF-04.1-B
  - ANALYTICS-03
  - WARD-03

must_haves:
  truths:
    - "All 369 wards have a non-NULL org_id after the migration runs"
    - "CORP_ANALYTICS_SQL returns 5 rows (one per corporation) instead of zero"
    - "Org-scoped admin visibility CTE returns reports for admins assigned to a corporation"
    - "The migration UPDATE SQL uses ILIKE with the same pattern as get_org_for_ward"
    - "SQL-string test for migration consistency passes without a live DB"
  artifacts:
    - path: "backend/migrations/014_link_wards_to_organisations.sql"
      provides: "Idempotent UPDATE linking each ward to its corporation via ILIKE"
      contains: "UPDATE wards SET org_id"
    - path: "backend/tests/analytics_tests.rs"
      provides: "SQL-string tests including migration consistency guard"
      contains: "ward_org_id_ilike_pattern_consistency"
  key_links:
    - from: "backend/migrations/014_link_wards_to_organisations.sql"
      to: "backend/src/db/queries.rs get_org_for_ward"
      via: "ILIKE '%' || w.corporation || '%' pattern must match"
      pattern: "ILIKE.*corporation"
    - from: "backend/migrations/014_link_wards_to_organisations.sql"
      to: "backend/src/db/admin_queries.rs CORP_ANALYTICS_SQL"
      via: "wards.org_id FK populated → JOIN wards w ON w.org_id = o.id returns rows"
      pattern: "w\\.org_id = o\\.id"
---

<objective>
Populate wards.org_id for all 369 ward rows by adding migration 014 that runs a single UPDATE linking each ward to its corporation organization via ILIKE on the `corporation` text column. Add a SQL-string unit test that verifies the migration UPDATE uses the same ILIKE pattern as `get_org_for_ward`, preventing silent pattern drift.

Purpose: CORP_ANALYTICS_SQL and the org-scoped admin visibility CTE both JOIN on `wards.org_id = o.id`. With wards.org_id NULL for all 369 rows, ANALYTICS-03 returns zero rows and WARD-03 org-scoped admins see no reports. A single data migration closes both gaps simultaneously.

Output: migration file 014_link_wards_to_organisations.sql + two new tests in analytics_tests.rs (RED → GREEN)
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.planning/PROJECT.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.planning/ROADMAP.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.planning/STATE.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.planning/phases/04-export-and-public-analytics/04-03a-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Add SQL-string tests for migration consistency and ward linkage</name>
  <read_first>
    - backend/tests/analytics_tests.rs — existing test file; new tests must be appended in the same style (no live DB, pure string assertions, same import pattern)
    - backend/src/db/queries.rs lines 75-100 — get_org_for_ward ILIKE pattern: `o.name ILIKE '%' || w.corporation || '%'` — the migration UPDATE must use the same logical pattern
    - backend/src/db/admin_queries.rs lines 1418-1429 — CORP_ANALYTICS_SQL — the fix in the migration makes this query return data via `JOIN wards w ON w.org_id = o.id`
  </read_first>
  <action>
    Append two new tests to backend/tests/analytics_tests.rs. Do NOT modify existing tests.

    Test 1 — `ward_org_id_ilike_pattern_consistency`:
    Import `corp_analytics_sql_fragment` (already imported). Create a new pub fn in admin_queries.rs called `ward_link_migration_sql_fragment() -> &'static str` that returns a const `WARD_LINK_MIGRATION_SQL` (to be created in Task 2). In this test, call `ward_link_migration_sql_fragment()` and assert:
    - The SQL contains `UPDATE wards` (verifies the migration mutates the wards table)
    - The SQL contains `ILIKE` (verifies the match strategy matches get_org_for_ward)
    - The SQL contains `org_type = 'corporation'` (verifies only corporation rows are matched, not the GBA root)
    - The SQL contains `SET org_id` (verifies the column being populated)

    Test 2 — `corp_analytics_joins_on_org_id`:
    Call `corp_analytics_sql_fragment()` and assert:
    - The SQL contains `w.org_id` (verifies the JOIN path that depends on the migration)
    - The SQL contains `o.org_type = 'corporation'` or `org_type = 'corporation'` (verifies the org filter)

    These tests MUST fail (RED) before Task 2 creates the migration and the fragment helper. Confirm RED by running `cargo test ward_link_migration_sql_fragment` — it should fail with a compile error (unresolved import) since the fn does not exist yet.
  </action>
  <verify>
    <automated>cd /home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/backend && cargo test ward_link 2>&1 | grep -E "error|FAILED|unresolved" | head -10</automated>
  </verify>
  <done>cargo test fails to compile due to missing `ward_link_migration_sql_fragment` — RED gate confirmed</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Create migration 014 and expose fragment helper</name>
  <read_first>
    - backend/migrations/013_fix_delete_trigger.sql — observe naming convention: `-- Migration: NNN_name.sql` header comment, plain SQL body
    - backend/migrations/010_org_seed.sql — idempotency pattern: DO $$ BEGIN IF NOT EXISTS ... END $$
    - backend/src/db/admin_queries.rs lines 1467-1473 — corp_analytics_sql_fragment() pattern to replicate for ward_link_migration_sql_fragment()
    - backend/src/db/queries.rs lines 84-100 — exact ILIKE: `o.name ILIKE '%' || w.corporation || '%'` — migration UPDATE must use the same logical expression
  </read_first>
  <action>
    Step 1 — Create backend/migrations/014_link_wards_to_organisations.sql.

    File must have a header comment block:
    - Migration name line: `-- Migration: 014_link_wards_to_organisations.sql`
    - Purpose: links each ward row to its matching corporation organization via ILIKE on the corporation text column
    - Idempotency note: re-running leaves rows unchanged because a ward already assigned will match the same org and SET produces the same value
    - Safety note: wards.corporation contains only 5 trusted migration-seeded values (Central/North/East/South/West); never user input

    SQL body (per NF-04.1-A):
    UPDATE wards
    SET org_id = o.id
    FROM organizations o
    WHERE o.org_type = 'corporation'
      AND o.name ILIKE '%' || wards.corporation || '%'
      AND wards.org_id IS NULL;

    The WHERE wards.org_id IS NULL clause makes the UPDATE idempotent — already-linked wards are skipped on re-run.

    Step 2 — Add the const and fragment helper to backend/src/db/admin_queries.rs.

    Add near the end of the file (after corp_analytics_sql_fragment at line ~1471):

    const WARD_LINK_MIGRATION_SQL: &str = "UPDATE wards \
        SET org_id = o.id \
        FROM organizations o \
        WHERE o.org_type = 'corporation' \
          AND o.name ILIKE '%' || wards.corporation || '%' \
          AND wards.org_id IS NULL";

    Add the test-only fragment helper immediately after:
    #[allow(dead_code)]
    pub fn ward_link_migration_sql_fragment() -> &'static str {
        WARD_LINK_MIGRATION_SQL
    }

    Note: The actual migration file is what sqlx::migrate! runs at startup — the const in admin_queries.rs is the testable mirror for SQL-string unit tests. Both strings must be kept in sync. Add a comment above the const: `// Mirror of 014_link_wards_to_organisations.sql — kept in sync for SQL-string unit tests.`

    Step 3 — Add the import for ward_link_migration_sql_fragment in backend/tests/analytics_tests.rs (update the existing `use` statement to include it).
  </action>
  <verify>
    <automated>cd /home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/backend && cargo test analytics 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All analytics tests pass (including the 2 new ones): `cargo test analytics` shows 6 passed, 0 failed
    - Full suite still green: `cargo test` exits 0
    - migration file exists at backend/migrations/014_link_wards_to_organisations.sql
    - File contains UPDATE wards SET org_id = o.id FROM organizations o WHERE o.org_type = 'corporation' AND o.name ILIKE
    - WARD_LINK_MIGRATION_SQL const exists in admin_queries.rs
    - ward_link_migration_sql_fragment() exported from admin_queries.rs
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → DB | UPDATE uses server-controlled values only — no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | 014_link_wards_to_organisations.sql UPDATE | accept | Both `organizations.name` and `wards.corporation` are migration-seeded server values — never user input. ILIKE pattern `'%' || wards.corporation || '%'` cannot be injected. sqlx::migrate! tracks checksum and prevents re-execution of modified migrations. |
| T-04.1-02 | Information Disclosure | CORP_ANALYTICS_SQL endpoint | accept | Endpoint is registered under admin_protected_router (require_auth) per T-04-08 in plan 04-03a. No new public surface introduced by this plan. |
| T-04.1-SC | Tampering | npm/pip/cargo installs | accept | No new external dependencies added in this plan — migration SQL and test code use only existing project dependencies. |
</threat_model>

<verification>
cargo test analytics — must show 6 passed (4 existing + 2 new), 0 failed
cargo test — full suite exits 0, all tests pass
ls backend/migrations/014_link_wards_to_organisations.sql — file exists
grep "ILIKE" backend/migrations/014_link_wards_to_organisations.sql — pattern present
grep "ward_link_migration_sql_fragment" backend/src/db/admin_queries.rs — helper exported
</verification>

<success_criteria>
- migration file 014_link_wards_to_organisations.sql exists with UPDATE wards SET org_id using ILIKE on wards.corporation
- WARD_LINK_MIGRATION_SQL const in admin_queries.rs matches migration file SQL
- ward_link_migration_sql_fragment() exported from admin_queries.rs
- 2 new SQL-string tests pass: ward_org_id_ilike_pattern_consistency and corp_analytics_joins_on_org_id
- cargo test analytics: 6 passed, 0 failed
- cargo test: full suite exits 0
- When migration runs against live DB: SELECT COUNT(*) FROM wards WHERE org_id IS NULL returns 0
- When migration runs against live DB: SELECT COUNT(*) FROM wards WHERE org_id IS NOT NULL returns 369
</success_criteria>

<output>
Create `.planning/phases/04.1-close-gap-analytics-03-ward-03-link-wards-to-organisations/04.1-01-SUMMARY.md` when done
</output>
