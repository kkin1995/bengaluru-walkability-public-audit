---
phase: 04.1-close-gap-analytics-03-ward-03-link-wards-to-organisations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/admin/lib/adminApi.ts
  - frontend/app/admin/components/ReportsTable.tsx
autonomous: true
requirements:
  - NF-04.1-C

must_haves:
  truths:
    - "AdminReport TypeScript interface has a top-level `corporation` field typed string | null"
    - "ReportsTable.tsx CORP column accesses report.corporation directly — no `as unknown as` cast"
    - "TypeScript compiler accepts both files without type errors"
  artifacts:
    - path: "frontend/app/admin/lib/adminApi.ts"
      provides: "AdminReport interface with corporation: string | null field"
      contains: "corporation: string | null"
    - path: "frontend/app/admin/components/ReportsTable.tsx"
      provides: "CORP column reads report.corporation without unsafe cast"
      contains: "report.corporation"
  key_links:
    - from: "frontend/app/admin/components/ReportsTable.tsx"
      to: "frontend/app/admin/lib/adminApi.ts AdminReport"
      via: "report.corporation — typed field access, no cast"
      pattern: "report\\.corporation"
---

<objective>
Fix the TypeScript type gap on the `AdminReport` interface: add `corporation: string | null` as a top-level field and remove the `as unknown as {corporation?: string | null}` cast in ReportsTable.tsx that bypasses TypeScript's type checker.

Purpose: WARNING-02 from the milestone audit — the ReportsTable CORP column uses an unsafe cast to access a corporation field that was never added to the AdminReport interface. The backend already returns `corporation` in each report row (it is included via the ward JOIN in list_admin_reports). Adding it to the interface restores type safety without any behavioral change.

Output: adminApi.ts with updated AdminReport interface, ReportsTable.tsx with clean field access
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.planning/PROJECT.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.planning/ROADMAP.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add corporation field to AdminReport and clean ReportsTable cast</name>
  <read_first>
    - frontend/app/admin/lib/adminApi.ts lines 64-94 — full AdminReport interface as it currently exists; `corporation` field is absent; note the grouping comments (Phase 03, Phase 03.2, ABUSE-06) to identify where to insert the new field
    - frontend/app/admin/components/ReportsTable.tsx lines 1038-1050 — the CORP column cell that currently uses `as unknown as {corporation?: string | null}` double-cast; this is the only location to modify in this file
  </read_first>
  <action>
    Change 1 — frontend/app/admin/lib/adminApi.ts:

    Inside the AdminReport interface, after the `ward_name: string | null` field (line ~79) and before the `// Phase 03 (D-13, D-14, D-15)` comment block, add:
      // Phase 03 (WARNING-02): Corporation from ward JOIN — populated by backend list_admin_reports query
      corporation: string | null;

    The field must be non-optional (plain `string | null`, NOT `corporation?: string | null`) because the backend always includes the field in the JSON response (it is selected via `LEFT JOIN wards` in list_admin_reports SQL, defaulting to NULL when no ward is assigned).

    Change 2 — frontend/app/admin/components/ReportsTable.tsx lines ~1047-1049:

    Replace the double-cast expression:
      (report as unknown as {corporation?: string | null}).corporation
        ?? (report as unknown as {ward_hierarchy?: {corporation?: string | null}}).ward_hierarchy?.corporation
        ?? "—"

    With the clean field access:
      report.corporation ?? report.ward_hierarchy?.corporation ?? "—"

    The fallback chain is preserved: direct corporation field first, then ward_hierarchy?.corporation as secondary source, then "—" sentinel. No behavioral change — only the cast is removed.

    No other changes are needed. Do not add or remove any other fields from AdminReport.
  </action>
  <verify>
    <automated>cd /home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - AdminReport interface in adminApi.ts contains `corporation: string | null` (non-optional, not `corporation?`)
    - ReportsTable.tsx CORP cell reads `report.corporation` with no `as unknown as` anywhere on that expression
    - `npm run build` exits 0 with no TypeScript errors
    - `npm run lint` exits 0 (no ESLint errors on the modified files)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → TypeScript type | corporation field is server-provided string from ward JOIN — no user-controlled value displayed without HTML encoding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-03 | Information Disclosure | AdminReport.corporation display in ReportsTable | accept | Corporation name is a server-seeded enum-like value (Central/North/East/South/West from migrations 004+010). It is admin-only (shown in admin table, require_auth enforced at route level). No PII. |
| T-04.1-04 | Tampering | TypeScript type removal of `as unknown as` | accept | Removing the cast tightens type safety — this is a risk reduction, not a risk introduction. The field was already being read at runtime; we are now making the type contract explicit. |
| T-04.1-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages added. Change is pure TypeScript interface + JSX expression update. |
</threat_model>

<verification>
cd frontend && npm run build — exits 0, no TypeScript errors
cd frontend && npm run lint -- --file app/admin/lib/adminApi.ts --file app/admin/components/ReportsTable.tsx — exits 0
grep "corporation: string | null" frontend/app/admin/lib/adminApi.ts — returns 1 match (non-optional field)
grep "as unknown as" frontend/app/admin/components/ReportsTable.tsx — returns 0 matches (cast removed)
grep "report.corporation" frontend/app/admin/components/ReportsTable.tsx — returns at least 1 match (direct access)
</verification>

<success_criteria>
- `corporation: string | null` present in AdminReport interface (non-optional)
- Zero occurrences of `as unknown as` in ReportsTable.tsx CORP column expression
- `report.corporation ?? report.ward_hierarchy?.corporation ?? "—"` is the CORP column expression
- `npm run build` exits 0
- `npm run lint` exits 0
</success_criteria>

<output>
Create `.planning/phases/04.1-close-gap-analytics-03-ward-03-link-wards-to-organisations/04.1-02-SUMMARY.md` when done
</output>
