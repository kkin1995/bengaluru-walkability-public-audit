---
phase: 04.1-close-gap-analytics-03-ward-03-link-wards-to-organisations
plan: "03"
subsystem: database
tags: [rust, sqlx, postgres, analytics, tdd]

requires:
  - phase: 04.1-01
    provides: Migration 014 populating wards.org_id FK enabling CORP_ANALYTICS_SQL JOIN
  - phase: 04.1-02
    provides: REQUIREMENTS.md traceability entries for NF-04.1-A/B/C

provides:
  - "CORP_ANALYTICS_SQL with ::float8 cast on ROUND expression — resolution_rate_pct now decodes as f64 instead of NUMERIC"
  - "corp_analytics_joins_on_org_id test with ::float8 regression assertion (NF-04.1-B permanent guard)"

affects:
  - analytics
  - ANALYTICS-03
  - corporation analytics endpoint

tech-stack:
  added: []
  patterns:
    - "PostgreSQL OID cast pattern: ROUND(..., 1)::float8 forces OID 701 (float8) for sqlx f64 decode without bigdecimal feature"
    - "TDD regression guard: SQL-string test assertion prevents reversion to NUMERIC without failing tests first"

key-files:
  created: []
  modified:
    - backend/src/db/admin_queries.rs
    - backend/tests/analytics_tests.rs

key-decisions:
  - "::float8 cast applied in SQL string (not Cargo.toml change) — no bigdecimal/decimal feature flag needed; one-token fix"
  - "New assertion added inside existing corp_analytics_joins_on_org_id test function — not a new test function; test count stays at 6"

patterns-established:
  - "OID cast guard: when sqlx Cargo.toml lacks bigdecimal/decimal features, use ::float8 cast on ROUND/NUMERIC expressions to ensure f64 decode"
  - "Regression assertion: SQL-string test guards critical type casts permanently — any future removal of ::float8 fails the test suite before hitting the live DB"

requirements-completed:
  - ANALYTICS-03
  - NF-04.1-B

duration: 2min
completed: 2026-06-01
---

# Phase 04.1 Plan 03: ::float8 Cast Fix for CORP_ANALYTICS_SQL Summary

**CORP_ANALYTICS_SQL ROUND expression cast to ::float8 (OID 701) so sqlx decodes resolution_rate_pct as f64 instead of NUMERIC, making ANALYTICS-03 functional**

## Performance

- **Duration:** 2 min
- **Started:** 2026-06-01T12:16:17Z
- **Completed:** 2026-06-01T12:18:17Z
- **Tasks:** 2 (TDD: RED + GREEN)
- **Files modified:** 2

## Accomplishments

- Added `::float8` regression assertion to `corp_analytics_joins_on_org_id` test (RED gate confirmed — test failed before fix)
- Appended `::float8` cast after `ROUND(..., 1)` in `CORP_ANALYTICS_SQL` (GREEN gate confirmed — all 6 analytics tests pass)
- ANALYTICS-03 is now functional: `resolution_rate_pct` returns a non-null `f64` for corporations with reports instead of silently returning `None` due to NUMERIC/f64 OID mismatch

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Add ::float8 regression assertion** - `32dfc88` (test)
2. **Task 2 (GREEN): Append ::float8 cast to ROUND in CORP_ANALYTICS_SQL** - `5b50626` (fix)

**Plan metadata:** (docs commit follows)

_Note: TDD plan — test commit (RED) before fix commit (GREEN)_

## Files Created/Modified

- `backend/src/db/admin_queries.rs` - CORP_ANALYTICS_SQL: `ROUND(..., 1)` changed to `ROUND(..., 1)::float8`
- `backend/tests/analytics_tests.rs` - `corp_analytics_joins_on_org_id`: third assertion added verifying `::float8` presence

## Decisions Made

- The `::float8` cast is applied in the SQL string rather than adding `bigdecimal` or `decimal` features to Cargo.toml — this is a one-token fix with no dependency changes.
- The new `::float8` assertion was appended inside the existing `corp_analytics_joins_on_org_id` test function (not added as a new test function). The analytics test suite reports 6 passed (not 7 as stated in the plan — the plan incorrectly counted assertions as test functions; Rust counts test functions).

## Deviations from Plan

### Documentation Discrepancy (Not a Code Deviation)

**Plan stated "7 passed" but actual result is "6 passed":**
- The plan description says "cargo test --test analytics_tests shows 7 passed, 0 failed (6 prior + 1 new ::float8 assertion)"
- In Rust's test runner, each `#[test]` function counts as one test — not each `assert!` call
- Adding an assertion to `corp_analytics_joins_on_org_id` does not create a new test function
- Actual result: 6 passed, 0 failed (correct and expected)
- The implementation is correct. The plan's test count prediction was inaccurate.

None — plan executed exactly as specified (one-token SQL fix + one test assertion).

## Issues Encountered

None — the fix was straightforward. The RED gate confirmed the cast was absent, the GREEN gate confirmed the fix works.

## Verification

All success criteria met:
- `grep "::float8" backend/src/db/admin_queries.rs` returns 1 match inside CORP_ANALYTICS_SQL
- `grep "float8" backend/tests/analytics_tests.rs` returns matches (regression assertion present)
- `cargo test --test analytics_tests`: 6 passed, 0 failed
- `corp_analytics_joins_on_org_id` test passes (GREEN gate confirmed)

## User Setup Required

None — no external service configuration required. The fix takes effect when the backend is redeployed; no environment variable changes needed.

## Next Phase Readiness

- ANALYTICS-03 is now functional: hitting `GET /api/admin/analytics/corporations` against a live DB with reports will return non-null `resolution_rate_pct` values for corporations
- Phase 04.1-04 (the remaining plan in this phase) can proceed
- Human verification item remains: live DB test against `https://staging.nammadaari.com` to confirm `resolution_rate_pct` returns numeric values (requires active admin session + DB with corporation data)

---
*Phase: 04.1-close-gap-analytics-03-ward-03-link-wards-to-organisations*
*Completed: 2026-06-01*
