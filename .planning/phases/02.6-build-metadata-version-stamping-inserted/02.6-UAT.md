---
status: resolved
phase: 02.6-build-metadata-version-stamping-inserted
source: [02.6-01-SUMMARY.md, 02.6-02-SUMMARY.md]
started: 2026-05-25T10:05:00Z
updated: 2026-05-25T18:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Citizen footer visual on public routes
expected: Thin mono footer at bottom of /, /report, /success showing 'Namma Daari · v0.1.0' with correct CSS var tokens
result: issue
reported: "The text is there but I need to scroll to see it. There is excessive whitespace below the buttons on /report, pushing the footer below the viewport."
severity: major

### 2. /map footer suppression
expected: No footer element on /map; full-screen Leaflet map uninterrupted, floating Report CTA unobstructed
result: pass

### 3. Admin sidebar version + public footer bleed (CR-02)
expected: Admin brand shows 'BENGALURU · v0.1.0' (not v2.4.1). Public SiteFooter does not bleed into /admin/* pages.
result: pass

### 4. Admin logout session clearing (CR-01)
expected: Clicking logout clears session and redirects to standalone /admin/login page immediately
result: issue
reported: "When I press logout, it logs out but the sidebar remains. When I press any link in the sidebar, then it shows the login page on the full screen."
severity: major

## Summary

total: 4
passed: 2
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Footer is visible without scrolling on /report — no excess whitespace below page content"
  status: resolved
  reason: "User reported: excessive whitespace below buttons on /report pushes footer below viewport, requires scroll"
  severity: major
  test: 1
  root_cause: "Each step <main> in report/page.tsx has minHeight: '100dvh' (e.g. line 434). FooterBoundary renders after {children} in body, so total height = 100dvh + footer height > viewport, forcing scroll. Fix: make body a flex-col container (flex flex-col min-h-screen) and wrap children in flex-1 div, then remove minHeight:100dvh from individual step containers."
  artifacts:
    - path: "frontend/app/report/page.tsx"
      issue: "minHeight: '100dvh' on step containers (photo ~line 434, category ~line 567, confirm ~line 762)"
    - path: "frontend/app/layout.tsx"
      issue: "body needs flex flex-col layout for sticky footer pattern"
  missing:
    - "body className needs 'flex flex-col' and children need a flex-1 wrapper"
    - "Remove minHeight: '100dvh' from step <main> containers in report/page.tsx"
  debug_session: ""

- truth: "Clicking logout redirects immediately to standalone /admin/login full-page view"
  status: resolved
  reason: "User reported: logout clears session but sidebar remains rendered; login form appears in content area, not as full-page redirect. Only clicking a sidebar link triggers full-page login."
  severity: major
  test: 4
  root_cause: "AdminSidebar.tsx:79 uses router.push('/admin/login') — Next.js soft navigation keeps admin shell mounted. Login form renders inside the shell's content slot. Fix: use window.location.replace('/admin/login') for a hard navigation that fully unmounts the layout. Same fix needed in profile/page.tsx:166."
  artifacts:
    - path: "frontend/app/admin/components/AdminSidebar.tsx"
      issue: "line 79: router.push('/admin/login') should be window.location.replace('/admin/login')"
    - path: "frontend/app/admin/profile/page.tsx"
      issue: "line 166: routerRef.current.push('/admin/login') should be window.location.replace('/admin/login')"
  missing:
    - "Replace router.push with window.location.replace in both logout handlers"
  debug_session: ""
