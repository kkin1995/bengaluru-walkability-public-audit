# Phase 02.3.2: Walkable BLR — Frontend Gap-fill — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-25
**Phase:** 02.3.2 — Walkable BLR Frontend gap-fill (3 missing design pieces)
**Areas discussed:** Scope identification, Filter chip implementation, Branch strategy

---

## Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Map filter chips — full implementation | Re-add category chip strip to /map with actual filtering | ✓ |
| Filter chip counts (per-category) | Each chip shows "Damaged · 38" etc., derived from fetched reports | ✓ |
| Home page live count | Replace hardcoded "412 reports" with live API count | — |

**User's note:** Home page live count is already implemented on main (fetchReportTotal with 60s cache, added in PR #3). No work needed.

---

## Filter Chip Filtering Approach

| Option | Description | Selected |
|--------|-------------|----------|
| Client-side filter (Recommended) | Filter already-fetched reports array in memory | ✓ |
| Re-fetch from API | Pass category param to GET /api/reports?category= on each chip tap | |
| You decide | Either approach | |

**User's choice:** Client-side filter — zero extra API calls.

---

## Chip Strip Placement

| Option | Description | Selected |
|--------|-------------|----------|
| Always visible below top bar (Recommended) | Permanently visible, horizontally scrollable | ✓ |
| Opens when filter button tapped | Filter button toggles chip strip | |
| You decide | | |

**User's choice:** Always visible below top bar, matching the original design spec.

---

## Chip Count Source

| Option | Description | Selected |
|--------|-------------|----------|
| Derived from fetched reports (Recommended) | Count from ReportsMap's existing 200-report fetch via callback | ✓ |
| Separate API call per category | 6 API calls for exact counts | |
| You decide | | |

**User's choice:** Derived from fetched reports — zero extra API calls.

---

## "All" Chip Design

| Option | Description | Selected |
|--------|-------------|----------|
| Shows total count, selected by default (Recommended) | "All · 147", selected on open | ✓ |
| No count on All chip | "All" only, no number | |
| You decide | | |

**User's choice:** "All · N" with total count, selected by default.

---

## Filter Behavior (Pin Visibility)

| Option | Description | Selected |
|--------|-------------|----------|
| Only that category's pins visible (Recommended) | Pins from other categories disappear | ✓ |
| All pins stay, selected category highlighted | Others fade to 30% opacity | |
| You decide | | |

**User's choice:** Only selected category's pins visible; others hidden completely.

---

## Branch Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Continue on ui-redesign branch | — not available, branch was merged + deleted | |
| New branch from main | Create appropriate feat/02.3.2-* branch | ✓ |

**User's note:** "The earlier ui-redesign branch has been merged and deleted. Now there is only a main branch. Create a new branch with an appropriate name and do not commit to main."

---

## Claude's Discretion

- Exact branch name (suggested: `feat/02.3.2-map-filter-chips`)
- Whether Bengaluru pill in top bar shows a count alongside the city name
- Minimum chip dimensions at very small viewports
- Test strategy for new ReportsMap props

## Deferred Ideas

- Language toggle functionality — display-only, not in scope
- GPS pulsing animation on category screen — staging-device test deferred
- Ward auto-detection from GPS — future phase
- Filter chips triggering map zoom to filtered area — not requested
