---
phase: 02.3.2-walkable-blr-frontend-gap-fill-3-missing-design-pieces
plan: 01
subsystem: ui
tags: [next.js, react, typescript, file-input, exif, photo-store, category-grid, bilingual]

# Dependency graph
requires:
  - phase: 02.3.1-implement-walkable-blr-ui-redesign-from-design-file-on-separate-branch
    provides: ReportCTA.tsx with camera input, photo-store singleton, CategoryGrid.tsx, Bi/Icon/Btn UI primitives
provides:
  - Gallery escape hatch in ReportCTA (galleryRef, handleGalleryChange, gallery button + hidden input)
  - CategoryGrid 2-col x 3-row layout with 96px minHeight, 14x12 padding, 24px icons
  - Privacy/EXIF notice row below CategoryGrid with shield icon
  - page.tsx meta <p> marginTop corrected to 10px (D-05 spacing)
affects:
  - future UI verification — CategoryGrid and ReportCTA are citizen-facing critical path components

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "React Fragment wrapper to emit sibling elements from a component return"
    - "Dual-input pattern: one input with capture=environment inside label (camera), one hidden input without capture (gallery)"
    - "Identical handler pipelines for camera and gallery — same EXIF/compress/storePendingPhoto flow, different input attributes"

key-files:
  created: []
  modified:
    - frontend/app/components/ReportCTA.tsx
    - frontend/app/components/redesign/CategoryGrid.tsx
    - frontend/app/page.tsx

key-decisions:
  - "Gallery input has NO capture attribute — this is the mechanism that forces the OS to show the full photo picker instead of camera-only on iOS and Android"
  - "handleGalleryChange is an exact copy of handleChange — identical EXIF/compress/storePendingPhoto pipeline; behavioral difference is exclusively in the input element attribute (no capture)"
  - "CategoryGrid gridTemplateColumns reverted from 1fr 1fr 1fr to 1fr 1fr — UI-SPEC (approved 2026-04-25) is the authoritative source, overriding prior CONTEXT.md D-08 value"
  - "Privacy notice fontSize set to 13 (Caption scale) per UI-SPEC typography section, not 12 as originally in CONTEXT.md D-10"

patterns-established:
  - "React Fragment wrapper for ReportCTA: allows gallery div + hidden input to be siblings of the camera label without DOM wrapper"
  - "galleryRef.current?.click() inside type=button onClick: user gesture propagates through to file input .click() — browsers allow this; programmatic (non-gesture) .click() on file inputs is blocked"

requirements-completed:
  - UI-GALLERY-ESCAPE-HATCH
  - UI-CATEGORY-GRID
  - UI-LIVE-COUNT-VERIFY

# Metrics
duration: 3min
completed: 2026-04-25
---

# Phase 02.3.2 Plan 01: Gallery Escape Hatch, CategoryGrid 2-col, Privacy Row Summary

**Gallery escape hatch wired to same EXIF/compress/storePendingPhoto pipeline as camera CTA; CategoryGrid reverted to 2-col x 3-row with 96px chips and shield privacy notice row**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-25T09:51:42Z
- **Completed:** 2026-04-25T09:54:39Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Gallery escape hatch fully wired: second `useRef`, second hidden `<input>` (no `capture`), `handleGalleryChange` function with identical EXIF/compress/storePendingPhoto pipeline, gallery button with bilingual Bi label and image icon, 14px margin above
- CategoryGrid corrected from 3-col to 2-col x 3-row per approved UI-SPEC; chip dimensions updated (minHeight 96, padding 14x12, icon 24px); privacy row with shield icon added below grid
- page.tsx confirmed to have `fetchReportTotal` + `next: { revalidate: 60 }` (live count unchanged); meta `<p>` marginTop corrected from 12 to 10

## Task Commits

Each task was committed atomically:

1. **Task 1: Gallery escape hatch in ReportCTA.tsx + page.tsx spacing fix** - `7aef8c1` (feat)
2. **Task 2: CategoryGrid 2-col revert + chip dimension update + privacy row** - `fba0267` (feat)

## Files Created/Modified

- `frontend/app/components/ReportCTA.tsx` - Added `galleryRef` (useRef), `handleGalleryChange` (identical pipeline to `handleChange`), gallery button JSX with image icon + bilingual label, hidden gallery `<input>` (no `capture`); wrapped return in Fragment
- `frontend/app/components/redesign/CategoryGrid.tsx` - `gridTemplateColumns` "1fr 1fr 1fr" -> "1fr 1fr"; `minHeight` 76 -> 96; `padding` "12px 8px" -> "14px 12px"; icon `size` 22 -> 24; wrapped return in Fragment; added privacy/EXIF notice row (shield icon + text, marginTop 16, fontSize 13)
- `frontend/app/page.tsx` - `marginTop` on meta `<p>` changed from 12 to 10; `fetchReportTotal` + `revalidate: 60` confirmed present (no change to data-fetching logic)

## Key Values Confirmed

| Property | File | Value |
|----------|------|-------|
| gridTemplateColumns | CategoryGrid.tsx | "1fr 1fr" (2 columns, 3 rows) |
| minHeight | CategoryGrid.tsx chip | 96 |
| padding | CategoryGrid.tsx chip | "14px 12px" |
| Icon size | CategoryGrid.tsx chip | 24 |
| Privacy row marginTop | CategoryGrid.tsx | 16 |
| Privacy row fontSize | CategoryGrid.tsx | 13 |
| galleryRef occurrences | ReportCTA.tsx | 3 (useRef decl, ref={galleryRef}, galleryRef.current?.click()) |
| storePendingPhoto calls | ReportCTA.tsx | 2 (in handleChange and handleGalleryChange) |
| capture attribute | ReportCTA.tsx | 1 line only (camera input inside label); gallery input has none |
| meta p marginTop | page.tsx | 10 |
| fetchReportTotal | page.tsx | present (line 28) |
| revalidate | page.tsx | 60 |

## Build Status

`npm run build` passed with zero TypeScript errors after both tasks. `/report` bundle size increased from 7.75 kB to 7.83 kB (within expected range for additional JSX).

## Decisions Made

- Gallery input has no `capture` attribute — this is the mechanism that forces the OS full photo picker instead of camera-only
- `handleGalleryChange` is an exact copy of `handleChange` — identical pipeline; behavioral difference is exclusively in the input element attribute
- CategoryGrid `gridTemplateColumns` reverted from "1fr 1fr 1fr" to "1fr 1fr" — UI-SPEC (approved 2026-04-25) is the authoritative source, overriding prior CONTEXT.md D-08 value
- Privacy notice `fontSize` set to 13 (Caption scale) per UI-SPEC typography section, not 12 as originally in CONTEXT.md D-10

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `feat/02.3.2-frontend-gap-fill` branch has both task commits ready for PR/merge
- Gallery escape hatch, CategoryGrid 2-col, and privacy row are complete
- Remaining phase 02.3.2 work (map chip strip, if any) can proceed on the same branch

---
*Phase: 02.3.2-walkable-blr-frontend-gap-fill-3-missing-design-pieces*
*Completed: 2026-04-25*
