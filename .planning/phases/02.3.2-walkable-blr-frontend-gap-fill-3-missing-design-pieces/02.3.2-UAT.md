---
status: complete
phase: 02.3.2-walkable-blr-frontend-gap-fill-3-missing-design-pieces
source: [02.3.2-01-SUMMARY.md, 02.3.2-02-SUMMARY.md, 02.3.2-03-SUMMARY.md]
started: 2026-04-25T00:00:00Z
updated: 2026-04-25T23:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Gallery escape hatch — home page
expected: Below the primary green "Report an issue" CTA, a muted secondary link with an image icon and "or upload a saved photo" label is visible. Clicking it opens a file/photo picker (no camera popup on desktop).
result: pass

### 2. CategoryGrid 2-col × 3-row layout
expected: After selecting a photo (via camera or gallery), the Category step shows chips in 2 columns × 3 rows (6 chips total). Chips are noticeably tall (96px min-height). Below the grid, a small privacy notice with a shield icon reads "Photos are stripped of private metadata before upload".
result: pass
note: "Two bugs found and fixed inline — window.__pendingPhoto replaces module singleton in photo-store.ts; Step 1 hidden inputs switched to full-size transparent overlay. GPS coordinates (12.9753, 77.5910) visible in green pill. Privacy notice confirmed."

### 3. Home page live report count
expected: The home page displays a count of submitted reports in a badge or pill — a live number fetched from the backend (may show a cached value up to 60s old).
result: pass
note: "6 reports shown in pill at bottom-left of mini map. Also: back button on Category step calls resetAll() → shows Step 1 Photo — minor UX issue logged below."

### 4. Map chip strip — labels and counts
expected: Open /map. Directly below the top overlay bar, 7 horizontal category filter chips are visible. After reports load, each chip shows the format "Label · N" (e.g. "Damaged · 3"). Before data loads, chips show label only. Chips scroll horizontally on small viewports without a visible scrollbar.
result: pass
note: "7 chips visible (All, Damaged·0, Blocked·0, No path·0, Crossing·0, Lighting·0, Other·0). Counts show ·0 because backend unreachable (CORS/connectivity issue in dev — not a code bug; same issue seen in phase 02.3.2 plan 03 verification)."

### 5. Chip filtering and active state
expected: On /map, tap any category chip (not "All"). Only map pins for that category remain; all others disappear. The active chip switches to a dark ink background with white text. Tapping "All" brings all pins back.
result: pass
note: "Confirmed with live backend running — tapping a category chip hides all other pins. Tapping All restores them."

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Selecting a photo from home page CTA/gallery navigates to the Category step, not Step 1 Photo"
  status: resolved
  reason: "Two bugs fixed inline — window.__pendingPhoto replaces module singleton; Step 1 inputs use full-size transparent overlay."
  severity: major
  test: 2

- truth: "Back button on Category step (when entered from home page CTA) navigates home, not to Step 1 Photo"
  status: resolved
  reason: "Fixed — cameFromCTA flag added to state; set in consumePendingPhoto useEffect; category back calls router.push('/') when true. User confirmed working."
  severity: minor
  test: 3
  root_cause: "Category back button calls resetAll() which always sets step=photo. No awareness of whether photo came from home page CTA (should go home) vs direct /report navigation (showing Step 1 is correct). Fix: track cameFromCTA flag in state; if true, router.push('/') on back instead of resetAll()."
  artifacts:
    - path: "frontend/app/report/page.tsx"
      issue: "resetAll() on category back does not distinguish home-CTA flow from direct /report flow"
  missing:
    - "Add cameFromCTA boolean to form state, set true in consumePendingPhoto useEffect"
    - "Category back: if cameFromCTA, router.push('/') else resetAll()"
  debug_session: ""
