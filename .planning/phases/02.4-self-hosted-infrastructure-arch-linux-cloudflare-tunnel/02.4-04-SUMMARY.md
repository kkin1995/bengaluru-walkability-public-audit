---
phase: 02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel
plan: "04"
subsystem: documentation
tags:
  - checklist
  - cutover
  - github-secrets
  - vercel
  - operations
dependency_graph:
  requires:
    - 02.4-01
    - 02.4-02
  provides:
    - .planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md
  affects:
    - operator runbook
    - production cutover
tech_stack:
  added: []
  patterns:
    - "GitHub-flavored Markdown checkbox list (- [ ])"
    - "5-section numbered checklist"
key_files:
  created:
    - .planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md
  modified: []
decisions:
  - "CORS_ORIGIN mismatch warning added as blockquote — trailing-slash divergence from FRONTEND_URL is the most common CORS failure mode"
  - "Build Cache OFF callout added for Vercel redeploy — easy to miss, causes stale bundle"
  - "Rollback caveat explicit: once Railway project is deleted, rollback is impossible"
  - "9 E2E checkboxes cover: tunnel health, API, frontend load, admin login, cookie attrs, auth guard, report submission, CI deploy, docker ps"
metrics:
  duration: "~8 minutes (inline execution)"
  completed_date: "2026-05-20"
  tasks_completed: 1
  files_changed: 1
---

# Phase 02.4 Plan 04: Cutover Checklist Summary

**One-liner:** 159-line operator checklist for completing the Railway → self-hosted desktop cutover via external dashboard changes (GitHub Secrets/Variables, GitHub Environment, Vercel env vars + redeploy).

## What Was Built

`.planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md` — a 5-section, 21-checkbox runbook for the operator:

1. **GitHub Repository Secrets** — remove `RAILWAY_BACKEND_URL`; add `POSTGRES_PASSWORD`, `JWT_SECRET`, `ADMIN_SEED_EMAIL`, `ADMIN_SEED_PASSWORD` with generation commands and a write-only password-manager reminder
2. **GitHub Repository Variables** — add `BACKEND_URL`, `FRONTEND_URL`, `CORS_ORIGIN` with exact-match CORS warning (trailing slash breaks auth)
3. **GitHub Actions Environment** — create `production` environment; optional required-reviewer and wait-timer guidance
4. **Vercel Environment Variables** — update `NEXT_PUBLIC_API_URL` + `INTERNAL_API_URL` in Production + Preview scopes; mandatory redeploy with Build Cache OFF; build-time baking warning in blockquote
5. **End-to-End Verification** — 9 testable checkboxes covering tunnel /health, /api/reports, frontend load, admin login, cookie attributes, auth guard, report submission, GitHub Actions deploy, docker compose ps

Closes with a **Rollback** section noting that once the Railway project is deleted, rollback to Railway is permanently unavailable.

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Author 02.4-CHECKLIST.md with five numbered sections | 1f69c22 |

## Verification Results

All plan acceptance checks passed:
- File exists
- Title and all 5 `## N.` headings present
- `RAILWAY_BACKEND_URL` in remove context
- All 4 new secrets in Section 1 with generation commands
- All 3 variables in Section 2 with CORS exact-match callout
- `production` environment in Section 3
- `NEXT_PUBLIC_API_URL`, `INTERNAL_API_URL`, redeploy step, build-time warning in Section 4
- 9 E2E checkboxes in Section 5 (≥ 7 required)
- 21 total checkboxes across all sections (≥ 14 required)
- Rollback section present
- 159 lines (≥ 100 required)

## Deviations from Plan

None. Executed inline after worktree agent failed without Bash access.

## Known Stubs

None.

## Threat Flags

None. Documentation-only artifact.

## Self-Check: PASSED

- 02.4-CHECKLIST.md created and committed at `1f69c22`
- All plan acceptance criteria verified
- Every secret/var consumed by deploy.yml (Plan 02) is accounted for in Sections 1 and 2
- NEXT_PUBLIC_API_URL build-time gotcha is prominently called out in Section 4
