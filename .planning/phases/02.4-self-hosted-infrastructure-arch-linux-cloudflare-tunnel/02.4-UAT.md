---
status: complete
phase: 02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel
source: 02.4-01-SUMMARY.md, 02.4-02-SUMMARY.md, 02.4-03-SUMMARY.md, 02.4-04-SUMMARY.md
started: 2026-05-20T00:00:00Z
updated: 2026-05-20T12:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Kill any running docker compose services. Then start from scratch using the
  new server compose override:
    docker compose -f docker-compose.yml -f docker-compose.server.yml up -d db backend nginx
  The services should come up without errors, migrations should complete, and a
  health check to http://localhost/health should return a 200 response.
  Navigating to http://localhost/ (the catch-all) should return 404 — no frontend.
result: pass

### 2. Backend-only nginx config structure
expected: |
  Open nginx/nginx.server.conf. It should:
  - Have all 3 rate-limit zones: upload (5r/m), admin_login (5r/m), admin_api (60r/m)
  - Have "upstream backend { server backend:3001; }" present
  - Have NO "upstream frontend" block and NO "proxy_pass http://frontend"
  - Have all 5 backend location blocks: = /api/admin/auth/login, /api/admin/, /api/, /uploads/, /health
  - Have "location / { return 404; }" as the catch-all at the end
  - Have "client_max_body_size 20M" preserved
result: pass

### 3. Docker Compose server override validates
expected: |
  Run: docker compose -f docker-compose.yml -f docker-compose.server.yml config
  (use real env vars or pass dummy ones with env)
  It should exit successfully (no errors), and:
  - The merged config should show nginx using ./nginx/nginx.server.conf
  - Running: docker compose -f docker-compose.yml -f docker-compose.server.yml config --profiles
    should return "frontend-only"
result: pass

### 4. Frontend profile gate
expected: |
  With the server compose override, running:
    docker compose -f docker-compose.yml -f docker-compose.server.yml up -d db backend nginx
  should NOT start the frontend container. Running "docker compose ps" after this
  command should show db, backend, nginx running — and NO frontend container running
  or being started.
result: pass

### 5. Deploy workflow three-job pipeline
expected: |
  Open .github/workflows/deploy.yml. It should have exactly three jobs:
  - "ci" (reuses ci.yml via workflow_call)
  - "deploy" (runs on self-hosted runner labeled: self-hosted, linux, walkability-prod)
    and gates on the "production" GitHub Environment
  - "smoke-test" (runs on ubuntu-latest, tests public Cloudflare tunnel + Vercel URLs)
  The deploy job should have a 6-attempt local health-wait loop (curl with retries).
result: pass

### 6. Railway references removed
expected: |
  In .github/workflows/deploy.yml, there should be zero references to "railway",
  "RAILWAY_BACKEND_URL", or any Railway domain. The old single smoke-test job
  targeting Railway should be completely gone.
result: pass

### 7. DEPLOYMENT.md runbook at repo root
expected: |
  A file DEPLOYMENT.md exists at the repo root. Opening it should show 9 numbered
  sections in this order:
  1. Overview (with ASCII architecture diagram)
  2. Prerequisites
  3. Arch Linux Desktop Setup
  4. Cloudflare Tunnel Setup
  5. Production Environment Variables
  6. First Manual Deploy
  7. GitHub Actions Self-Hosted Runner
  8. Ongoing Operations
  9. Troubleshooting
  The troubleshooting section should have a table covering at least 7 symptoms.
result: pass

### 8. Cutover checklist is complete
expected: |
  Open .planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md.
  It should have 5 sections:
  1. GitHub Repository Secrets (with RAILWAY_BACKEND_URL removal + 4 new secrets)
  2. GitHub Repository Variables (BACKEND_URL, FRONTEND_URL, CORS_ORIGIN with CORS exact-match warning)
  3. GitHub Actions Environment (production environment creation)
  4. Vercel Environment Variables (NEXT_PUBLIC_API_URL, INTERNAL_API_URL, redeploy step)
  5. End-to-End Verification (9 test checkboxes)
  Plus a Rollback section noting Railway deletion is irreversible.
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
