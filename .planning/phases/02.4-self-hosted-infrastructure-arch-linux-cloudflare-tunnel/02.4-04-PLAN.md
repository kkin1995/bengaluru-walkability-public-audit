---
phase: 02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel
plan: 04
type: execute
wave: 2
depends_on:
  - 02.4-01
  - 02.4-02
files_modified:
  - .planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md
autonomous: true
requirements:
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Every external-dashboard change required to cut over from Railway to the self-hosted desktop is enumerated, in order, with no implicit knowledge required"
    - "The checklist surfaces the Vercel `NEXT_PUBLIC_API_URL` build-time gotcha prominently — a redeploy is MANDATORY after env var changes"
    - "An end-to-end verification section confirms the full Vercel ↔ Cloudflare Tunnel ↔ desktop backend flow before the cutover is considered done"
    - "Every required GitHub secret and variable consumed by deploy.yml (Plan 02) appears in this checklist"
  artifacts:
    - path: ".planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md"
      provides: "Step-by-step human checklist for updating GitHub Secrets, GitHub Variables, GitHub Environments, and Vercel env vars during the Railway → desktop cutover, followed by an end-to-end verification block"
      contains: "NEXT_PUBLIC_API_URL"
  key_links:
    - from: "Section 1 (GitHub Secrets)"
      to: ".github/workflows/deploy.yml"
      via: "secret names consumed by the deploy job"
      pattern: "POSTGRES_PASSWORD|JWT_SECRET|ADMIN_SEED_EMAIL|ADMIN_SEED_PASSWORD"
    - from: "Section 2 (GitHub Variables)"
      to: ".github/workflows/deploy.yml"
      via: "vars consumed by the smoke-test job"
      pattern: "BACKEND_URL|FRONTEND_URL|CORS_ORIGIN"
    - from: "Section 4 (Vercel)"
      to: "frontend/app/lib/config.ts"
      via: "NEXT_PUBLIC_API_URL + INTERNAL_API_URL — the only frontend env vars that change"
      pattern: "NEXT_PUBLIC_API_URL"
---

<objective>
Author `02.4-CHECKLIST.md` — a step-by-step human checklist enumerating every dashboard change required outside the code repository to complete the Railway → desktop cutover. This plan produces NO code; it produces the human-executable checklist that runs alongside Plan 03's DEPLOYMENT.md when the operator performs the actual cutover.

Purpose: Plans 01 + 02 produce code; Plan 03 produces the on-host runbook; this plan produces the dashboard-side checklist (GitHub repo settings + Vercel project settings) that ties everything together. After this checklist is followed in order, the live system runs on the desktop, Vercel calls the Cloudflare tunnel, and `git push origin main` auto-deploys without manual SSH.

Output: One file — `.planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md` — with five clearly numbered sections and copy-pasteable values throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

# Reference files (the executor MUST read these to keep the checklist accurate)
@.github/workflows/deploy.yml
@docker-compose.yml
@STAGING-SETUP.md
@frontend/app/lib/config.ts

<interfaces>
<!-- Required GitHub Secrets consumed by Plan 02's deploy.yml -->
GitHub Secrets to ADD:
- POSTGRES_PASSWORD
- JWT_SECRET
- ADMIN_SEED_EMAIL
- ADMIN_SEED_PASSWORD

GitHub Secrets to REMOVE:
- RAILWAY_BACKEND_URL (no longer referenced by any workflow)

GitHub Variables to ADD (Settings > Secrets and variables > Actions > Variables tab):
- BACKEND_URL — public Cloudflare tunnel URL (e.g. https://api-walkability.kinariwala.com)
- FRONTEND_URL — Vercel staging URL (https://staging-walkability.kinariwala.com)
- CORS_ORIGIN — exactly the Vercel URL, no trailing slash

GitHub Environment to CREATE (Settings > Environments):
- Name: "production" (required by deploy.yml `environment: production` gate)

Vercel env vars to UPDATE (Project > Settings > Environment Variables):
- NEXT_PUBLIC_API_URL — change from Railway URL to Cloudflare tunnel URL (Production + Preview scopes)
- INTERNAL_API_URL — change from Railway URL to Cloudflare tunnel URL (Production + Preview scopes)

Vercel post-change requirement:
- NEXT_PUBLIC_API_URL is baked at build time → manual redeploy (Project > Deployments > Redeploy latest) is MANDATORY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 02.4-CHECKLIST.md with five numbered sections</name>
  <files>.planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md</files>
  <read_first>
    - .github/workflows/deploy.yml (Plan 02 output — confirms exact secret/var names referenced)
    - docker-compose.yml (confirm which env vars the backend consumes)
    - STAGING-SETUP.md (existing Railway-era checklist — read to make sure no Railway artifact is left behind)
    - frontend/app/lib/config.ts (confirm NEXT_PUBLIC_API_URL and INTERNAL_API_URL are the only Vercel-side variables that change)
  </read_first>
  <action>
    Create `.planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md`.

    Open with:
    - Title: `# Phase 02.4 Cutover Checklist — Railway → Self-Hosted Desktop`
    - One-line subtitle: `Updates to external dashboards required to complete the cutover. Run AFTER Plans 01–03 are merged and the desktop runbook (DEPLOYMENT.md) has been completed.`
    - `**Last updated:** 2026-05-20`
    - A brief callout (`>` blockquote) noting: this checklist is the only manual / non-code step in Phase 02.4. Every item is human-only because the data lives in third-party dashboards. Do each section in order — skipping ahead will leave the system in a broken intermediate state.

    Then the following five sections (each a level-2 heading `##`):

    Section 1 — `## 1. GitHub Repository Secrets`
    - Path callout: `Path: GitHub repo → Settings → Secrets and variables → Actions → Secrets tab`
    - Subsection `### 1a. Remove`
      - One checklist row: `- [ ] Remove secret: RAILWAY_BACKEND_URL` (with a parenthetical note that the secret no longer has any consumer after Plan 02 merges)
    - Subsection `### 1b. Add`
      - Each item is a checkbox row. Use the same row format throughout: `- [ ] **<SECRET_NAME>** — <one-line purpose>. Generate with: \`<command>\`` (use backticks for inline code). The four required secrets:
        - `POSTGRES_PASSWORD` — postgres container password for the desktop deploy. Generate with: `openssl rand -base64 32`
        - `JWT_SECRET` — backend session-token signing key (32+ chars). Generate with: `openssl rand -hex 32`
        - `ADMIN_SEED_EMAIL` — initial super-admin email created on first boot. Use the operator's real email (or `admin@example.com` if seeding will be discarded).
        - `ADMIN_SEED_PASSWORD` — initial super-admin password (12+ chars). Generate with: `openssl rand -base64 24` and store securely; change via Admin UI after first login.
    - Footer note: a `>` blockquote reminding the operator that GitHub Secrets are write-only from the UI — once saved, the dashboard never re-displays the value. Save the generated values to a password manager BEFORE pasting.

    Section 2 — `## 2. GitHub Repository Variables`
    - Path callout: `Path: GitHub repo → Settings → Secrets and variables → Actions → Variables tab`
    - One paragraph explaining that GitHub "Variables" are non-secret values exposed via `${{ vars.* }}` in workflows — they are visible in run logs, so URLs go here, not in Secrets.
    - Three checkbox rows, one per variable. Same row format as Section 1:
      - `BACKEND_URL` — public Cloudflare tunnel URL for the desktop backend. Example value: `https://api-walkability.kinariwala.com` (use the actual tunnel hostname from DEPLOYMENT.md Section 4).
      - `FRONTEND_URL` — Vercel frontend URL. Example value: `https://staging-walkability.kinariwala.com`. Must NOT include a trailing slash.
      - `CORS_ORIGIN` — must equal FRONTEND_URL exactly. The Axum backend enables `allow_credentials(true)`, so wildcards are rejected and only one origin can be active. Trailing slash will break CORS.
    - A short callout (`>`) warning that if FRONTEND_URL and CORS_ORIGIN drift apart (e.g., one has a trailing slash, one does not), the browser will see CORS errors and admin login will silently fail. Re-check both values after entry.

    Section 3 — `## 3. GitHub Actions Environment`
    - Path callout: `Path: GitHub repo → Settings → Environments`
    - Checkbox row: `- [ ] Create environment named "production"` (lowercase, exact match to the `environment: production` line in deploy.yml).
    - Sub-bullets explaining:
      - The deploy job in `.github/workflows/deploy.yml` has `environment: production`. If the environment does not exist, the deploy job hangs in "Waiting" indefinitely.
      - Optional: add required reviewers (yourself) so deploys to main require a single-click approval before running on the desktop. This is a defense-in-depth control against accidental main pushes.
      - Optional: add wait timer (e.g. 1 minute) to allow last-minute revert by force-pushing before the deploy executes.

    Section 4 — `## 4. Vercel Environment Variables`
    - Path callout: `Path: Vercel project → Settings → Environment Variables`
    - A leading callout (`>`) in bold or warning style stating: **`NEXT_PUBLIC_API_URL` is baked into the JavaScript bundle at build time. Changing it WITHOUT triggering a Vercel redeploy will leave the production frontend pointing at the old (now-defunct) Railway URL and the entire app will fail.**
    - Two checkbox rows, each must mention both the Production and Preview scopes explicitly:
      - `- [ ] Update **NEXT_PUBLIC_API_URL** in Production AND Preview scopes: change from the Railway URL to the Cloudflare tunnel URL (matches GitHub var BACKEND_URL exactly).`
      - `- [ ] Update **INTERNAL_API_URL** in Production AND Preview scopes: change from the Railway URL to the Cloudflare tunnel URL (same value as NEXT_PUBLIC_API_URL above).`
    - A final checkbox row for the mandatory redeploy:
      - `- [ ] Trigger manual Vercel redeploy: Project → Deployments → click the three-dot menu on the latest deployment → "Redeploy" → confirm "Use existing Build Cache: OFF" (to ensure the new env vars are picked up).`
    - A note that previous Vercel deployments will continue to reference the old Railway URL until redeployed — there is no automatic propagation.

    Section 5 — `## 5. End-to-End Verification`
    - Lead paragraph: "Run through this list AFTER Sections 1–4 are complete AND the desktop deployment from DEPLOYMENT.md is up. Do not declare Phase 02.4 done until every item is checked."
    - A checkbox list. Each item must be unambiguously testable. Include AT LEAST these items, each as `- [ ] <test>` with the expected outcome:
      - `curl https://<tunnel-url>/health` returns `{"status":"ok"}` (replace `<tunnel-url>` with the BACKEND_URL value).
      - `curl https://<tunnel-url>/api/reports` returns a JSON array (may be empty `[]` on a fresh deploy).
      - The Vercel frontend loads at the FRONTEND_URL without blank page or browser console errors.
      - Navigating to `<frontend-url>/admin/login`, entering `ADMIN_SEED_EMAIL` + `ADMIN_SEED_PASSWORD`, and clicking submit redirects to `/admin/reports`.
      - In the browser DevTools (Application → Cookies after admin login), the `admin_token` cookie is present under the FRONTEND_URL domain with `Secure` AND `HttpOnly` attributes set.
      - In a fresh incognito window, navigating directly to `<frontend-url>/admin/reports` redirects to `/admin/login` (auth guard works).
      - Submitting a test report at `<frontend-url>/report` (with a photo + GPS) returns a success message and the new report appears on the public map at `<frontend-url>/map`.
      - On the next `git push origin main`, the GitHub Actions Deploy workflow runs, the `deploy` job runs on the `walkability-prod` self-hosted runner, the local-health step passes, the `smoke-test` job passes against `vars.BACKEND_URL` and `vars.FRONTEND_URL`, and the workflow completes green.
      - SSH'd into the desktop (or running locally), `docker compose -f docker-compose.yml -f docker-compose.server.yml ps` shows db, backend, and nginx all in "healthy" status.

    Closing block:
    - A short paragraph: "When every box above is ticked, Phase 02.4 is complete. Update STATE.md and REQUIREMENTS.md (INFRA-04, INFRA-05 → Complete) and mark the phase done on the ROADMAP."
    - Add a `## Rollback` section after the verification list with a single-paragraph plan: if the cutover fails (e.g. tunnel never connects), the rollback is (a) restore Railway service to "Running" state if it was paused (it was decommissioned in Plan 03, so this is a no-op if more than 30 days have passed and the Railway project was deleted), (b) revert NEXT_PUBLIC_API_URL + INTERNAL_API_URL in Vercel to the old Railway URL, (c) trigger Vercel redeploy. Note explicitly: once the Railway project is deleted (the trigger for this entire phase), rollback is no longer available — the desktop deploy MUST work.

    Formatting rules:
    - Use `- [ ]` for every actionable item (GitHub-flavored Markdown checkboxes).
    - Use level-2 headings (`##`) for the five sections; level-3 headings (`###`) for sub-sections (e.g., 1a, 1b).
    - Use `>` blockquotes for warnings and critical callouts.
    - Use fenced code blocks for any shell command or example value the operator must copy.
    - Use bold (`**...**`) for emphasis on env var names and warnings.
    - No tables required (every item is a checkbox row), but a table is acceptable for the env var summary if it improves clarity.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md
      F=.planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md
      # Title and five top-level sections
      grep -qE '^# Phase 02\.4 Cutover Checklist' "$F"
      grep -qE '^## 1\. GitHub Repository Secrets' "$F"
      grep -qE '^## 2\. GitHub Repository Variables' "$F"
      grep -qE '^## 3\. GitHub Actions Environment' "$F"
      grep -qE '^## 4\. Vercel Environment Variables' "$F"
      grep -qE '^## 5\. End-to-End Verification' "$F"
      # Removal of RAILWAY_BACKEND_URL is called out
      grep -q 'RAILWAY_BACKEND_URL' "$F"
      grep -qiE 'remove' "$F"
      # All four required new secrets appear under section 1
      awk '/^## 1\. /,/^## 2\. /' "$F" | grep -q 'POSTGRES_PASSWORD'
      awk '/^## 1\. /,/^## 2\. /' "$F" | grep -q 'JWT_SECRET'
      awk '/^## 1\. /,/^## 2\. /' "$F" | grep -q 'ADMIN_SEED_EMAIL'
      awk '/^## 1\. /,/^## 2\. /' "$F" | grep -q 'ADMIN_SEED_PASSWORD'
      # All three required new vars appear under section 2
      awk '/^## 2\. /,/^## 3\. /' "$F" | grep -q 'BACKEND_URL'
      awk '/^## 2\. /,/^## 3\. /' "$F" | grep -q 'FRONTEND_URL'
      awk '/^## 2\. /,/^## 3\. /' "$F" | grep -q 'CORS_ORIGIN'
      # Production environment creation appears under section 3
      awk '/^## 3\. /,/^## 4\. /' "$F" | grep -qi 'production'
      awk '/^## 3\. /,/^## 4\. /' "$F" | grep -qi 'environment'
      # Vercel section calls out both env vars and the redeploy requirement
      awk '/^## 4\. /,/^## 5\. /' "$F" | grep -q 'NEXT_PUBLIC_API_URL'
      awk '/^## 4\. /,/^## 5\. /' "$F" | grep -q 'INTERNAL_API_URL'
      awk '/^## 4\. /,/^## 5\. /' "$F" | grep -qi 'redeploy'
      awk '/^## 4\. /,/^## 5\. /' "$F" | grep -qi 'build.time\|build time'
      # E2E section contains at least 7 checkbox rows
      e2e_boxes=$(awk '/^## 5\. /,0' "$F" | grep -cE '^- \[ \]')
      test "$e2e_boxes" -ge 7 || { echo "Section 5 has only $e2e_boxes checkboxes, need >= 7"; exit 1; }
      # Total checkbox count >= 14 (4 sec1 + 3 sec2 + 1 sec3 + 3 sec4 + 7 sec5)
      total_boxes=$(grep -cE '^- \[ \]' "$F")
      test "$total_boxes" -ge 14 || { echo "Only $total_boxes total checkboxes, need >= 14"; exit 1; }
      # Rollback section present
      grep -qE '^## Rollback' "$F"
      # Document length sanity check
      lines=$(wc -l < "$F")
      test "$lines" -ge 100 || { echo "Checklist too short: $lines lines"; exit 1; }
    </automated>
  </verify>
  <done>
    .planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md exists with five numbered sections in order: (1) GitHub Secrets (remove RAILWAY_BACKEND_URL; add POSTGRES_PASSWORD, JWT_SECRET, ADMIN_SEED_EMAIL, ADMIN_SEED_PASSWORD with generation commands), (2) GitHub Variables (BACKEND_URL, FRONTEND_URL, CORS_ORIGIN with explanation of vars vs. secrets), (3) GitHub Actions Environment (create "production"), (4) Vercel env vars (update NEXT_PUBLIC_API_URL and INTERNAL_API_URL in Production+Preview scopes plus mandatory redeploy), (5) End-to-End Verification with at least 7 testable checkboxes. A Rollback section appears at the end. All check items use GitHub-flavored markdown `- [ ]` syntax. Critical warnings about build-time bundling and CORS exact-match are in blockquote callouts.
  </done>
  <acceptance_criteria>
    - File `.planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md` exists.
    - The file has level-2 headings matching exactly: `## 1. GitHub Repository Secrets`, `## 2. GitHub Repository Variables`, `## 3. GitHub Actions Environment`, `## 4. Vercel Environment Variables`, `## 5. End-to-End Verification`.
    - Section 1 contains the literal string `RAILWAY_BACKEND_URL` in a "remove" context and contains all four of: `POSTGRES_PASSWORD`, `JWT_SECRET`, `ADMIN_SEED_EMAIL`, `ADMIN_SEED_PASSWORD`.
    - Section 1 includes the generation commands `openssl rand -hex 32` (for JWT_SECRET) and at least one `openssl rand -base64` invocation.
    - Section 2 contains all three of: `BACKEND_URL`, `FRONTEND_URL`, `CORS_ORIGIN`, with a callout noting CORS_ORIGIN must equal FRONTEND_URL exactly (no trailing slash).
    - Section 3 instructs the operator to create a GitHub Environment named `production` (lowercase, matching the deploy.yml `environment:` value).
    - Section 4 contains both `NEXT_PUBLIC_API_URL` and `INTERNAL_API_URL`, explicitly says both must be set in Production AND Preview scopes, and contains a mandatory redeploy step plus a warning that NEXT_PUBLIC_API_URL is baked at build time.
    - Section 5 contains at least 7 distinct `- [ ]` checkbox rows covering: tunnel /health, tunnel /api/reports, Vercel frontend load, admin login, admin cookie attributes, auth guard incognito test, public report submission, GitHub Actions deploy on push to main, and `docker compose ps` showing healthy services.
    - The file has at least 14 total `- [ ]` checkbox rows across all sections.
    - A `## Rollback` section exists at the end describing how to revert to Railway and noting that rollback becomes unavailable once the Railway project is deleted.
    - The file is at least 100 lines long.
  </acceptance_criteria>
</task>

</tasks>

<verification>
After Task 1 completes:

1. `test -f .planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-CHECKLIST.md` succeeds.
2. The five `## N. Title` headings appear in numerical order.
3. Every secret/var referenced by `.github/workflows/deploy.yml` (Plan 02) appears in either Section 1 (secrets) or Section 2 (variables) of this checklist.
4. The Vercel build-time gotcha (`NEXT_PUBLIC_API_URL`) is called out in a warning blockquote in Section 4 plus has a mandatory redeploy checkbox.
5. Section 5 has at least 7 testable verification items.
</verification>

<success_criteria>
- Every secret consumed by `deploy.yml` is listed in Section 1 with a generation command
- Every variable consumed by `deploy.yml` is listed in Section 2 with an example value and the no-trailing-slash CORS warning
- The mandatory Vercel redeploy after env var changes is unmissable (blockquote callout + dedicated checkbox)
- End-to-end verification covers: tunnel health, public API, frontend load, admin login, cookie attributes, auth guard, report submission, GitHub Actions deploy, docker ps healthy
- A rollback section exists with the explicit caveat that rollback becomes impossible once Railway is deleted
- The checklist is purely documentation — no code changes, no executable scripts
</success_criteria>

<output>
Create `.planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-04-SUMMARY.md` when done.
</output>
