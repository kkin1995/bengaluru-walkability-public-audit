---
phase: 02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel
plan: 03
type: execute
wave: 2
depends_on:
  - 02.4-01
  - 02.4-02
files_modified:
  - DEPLOYMENT.md
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "A new developer cloning the repo can follow DEPLOYMENT.md top to bottom to stand up the desktop deployment with no missing steps"
    - "Cloudflare tunnel setup is documented end-to-end: cloudflared install, tunnel create, /etc/cloudflared/config.yml template, DNS CNAME, systemd enablement"
    - "Self-hosted GitHub Actions runner installation is documented including the walkability-prod label and docker group membership"
    - "The first manual deploy command (`docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --build db backend nginx`) is shown exactly as the deploy workflow will run it"
    - "An ASCII architecture diagram makes clear what runs on the desktop vs. on Vercel and how the tunnel routes traffic"
  artifacts:
    - path: "DEPLOYMENT.md"
      provides: "Production self-hosting runbook for the Arch Linux + Cloudflare Tunnel + self-hosted runner setup"
      contains: "Cloudflare tunnel"
  key_links:
    - from: "DEPLOYMENT.md cloudflared config example"
      to: "docker-compose.server.yml + nginx.server.conf"
      via: "ingress rule pointing https://<hostname> to http://localhost:80"
      pattern: "service: http://localhost:80"
    - from: "DEPLOYMENT.md GitHub runner section"
      to: ".github/workflows/deploy.yml"
      via: "matching runner label walkability-prod"
      pattern: "walkability-prod"
---

<objective>
Author `DEPLOYMENT.md` at the repository root — a self-contained, top-to-bottom runbook for standing up the production deployment on the Arch Linux desktop. After this document exists, a developer (or future-Claude) can recreate the entire production stack from scratch without consulting any other source.

Purpose: Plans 01 and 02 produce the code artifacts (compose override, nginx config, deploy workflow). This plan produces the human-executable runbook that turns those artifacts into a running system — installing Docker, configuring Cloudflare Tunnel, generating production secrets, performing the first manual deploy, and installing the GitHub Actions self-hosted runner.

Output: `DEPLOYMENT.md` at the repository root with 9 numbered sections covering overview, prerequisites, Arch Linux setup, Cloudflare tunnel, env vars, first manual deploy, GitHub runner install, ongoing operations, and troubleshooting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

# Reference files (the executor MUST read these to keep the runbook in sync with reality)
@docker-compose.yml
@nginx/nginx.conf
@nginx/nginx.server.conf
@docker-compose.server.yml
@.github/workflows/deploy.yml
@STAGING-SETUP.md

<interfaces>
<!-- Canonical commands and identifiers the runbook MUST use verbatim -->

Compose commands (must match Plan 02's deploy.yml exactly):
- Build + first up: `docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --build db backend nginx`
- Subsequent ups (no rebuild): `docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --remove-orphans db backend nginx`
- Status: `docker compose -f docker-compose.yml -f docker-compose.server.yml ps`
- Logs: `docker compose -f docker-compose.yml -f docker-compose.server.yml logs -f backend`

cloudflared identifiers:
- AUR package: `cloudflared` (community/AUR — yay -S cloudflared or paru -S cloudflared)
- Tunnel name: `walkability-prod`
- Config path: `/etc/cloudflared/config.yml`
- DNS target format: `<TUNNEL-UUID>.cfargotunnel.com`

Production env vars (subset from docker-compose.yml + Plan 02 deploy job):
- POSTGRES_PASSWORD — strong random
- POSTGRES_DB — defaults to `walkability`
- POSTGRES_USER — defaults to `walkability`
- JWT_SECRET — `openssl rand -hex 32`
- COOKIE_SECURE — `true`
- CORS_ORIGIN — exact frontend URL, no trailing slash
- ADMIN_SEED_EMAIL — initial admin email
- ADMIN_SEED_PASSWORD — initial admin password (min 12 chars)

GitHub runner labels (must match Plan 02 deploy job runs-on):
- self-hosted
- linux
- walkability-prod
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author DEPLOYMENT.md with 9 numbered sections</name>
  <files>DEPLOYMENT.md</files>
  <read_first>
    - docker-compose.yml (to confirm env var names, service names, port mappings)
    - nginx/nginx.server.conf (produced in Plan 01 — confirm the listen port and absence of TLS)
    - docker-compose.server.yml (produced in Plan 01 — confirm the override usage pattern)
    - .github/workflows/deploy.yml (produced in Plan 02 — confirm runner labels, secrets, and compose commands match)
    - STAGING-SETUP.md (existing operational doc — match tone, table styles, and depth)
  </read_first>
  <action>
    Create `DEPLOYMENT.md` at the repository root. Open with a title (`# Self-Hosted Deployment Guide`), a one-line tagline, and a `**Last updated:** 2026-05-20` line. Do NOT add the `<!-- generated-by: gsd-doc-writer -->` marker because this is a human-authored runbook, not a generated artifact.

    The body MUST contain the following nine sections in this exact order. Each is a level-2 heading (`##`).

    Section 1 — `## 1. Overview`
    - 1-2 paragraphs summarizing the architecture: backend + PostGIS + nginx run on the Arch Linux desktop via Docker Compose; the frontend runs on Vercel; a Cloudflare Tunnel exposes the desktop's port 80 publicly over HTTPS without opening firewall ports or needing a public IP.
    - An ASCII architecture diagram. Include all of these nodes and arrows (use box-drawing characters or ASCII art):
      - Citizen browser → Vercel (Next.js frontend)
      - Vercel → Cloudflare Tunnel HTTPS endpoint (api-walkability.kinariwala.com or similar)
      - Cloudflare Tunnel → cloudflared daemon on desktop
      - cloudflared → nginx:80 on desktop
      - nginx → backend:3001 (in docker network)
      - backend → db:5432 (PostGIS)
    - A small "what runs where" table (3 columns: Component | Where | Notes) listing: Frontend (Vercel), Backend (Desktop / Docker), Database (Desktop / Docker), nginx (Desktop / Docker), Cloudflare Tunnel (Cloudflare edge ↔ desktop cloudflared daemon).
    - A "why Cloudflare Tunnel" paragraph (3-5 sentences) noting: no public IP needed, no firewall holes, free TLS certificate management by Cloudflare, automatic DDoS protection, works behind NAT.

    Section 2 — `## 2. Prerequisites`
    - Bulleted list of host-side prerequisites:
      - Arch Linux (or Arch-based, e.g., Manjaro) with sudo
      - A Cloudflare account with the target zone (`kinariwala.com`) already added
      - A GitHub account with admin access to the `bengaluru-walkability-public-audit` repository
      - Outbound HTTPS (443) reachable from the desktop (no special inbound rules required)
    - Tooling that will be installed in Section 3 (no need to install in advance): docker, docker-compose-plugin, cloudflared, github runner

    Section 3 — `## 3. Arch Linux Desktop Setup`
    - Three labeled sub-steps (3a, 3b, 3c) as level-3 headings:
      - 3a. Install Docker + Compose plugin: `sudo pacman -S docker docker-compose` then `sudo systemctl enable --now docker.service` then `docker --version` + `docker compose version` to verify
      - 3b. Add user to docker group: `sudo usermod -aG docker $USER` then a callout (blockquote) noting that the user MUST log out and back in (or run `newgrp docker`) before docker commands work without sudo
      - 3c. (Optional but recommended) Configure log rotation for docker: brief note that journald/docker default rotation is sufficient for this workload; advanced users can edit `/etc/docker/daemon.json` to add `log-opts.max-size`/`max-file` — show the JSON snippet

    Section 4 — `## 4. Cloudflare Tunnel Setup`
    - Six labeled sub-steps (4a through 4f) as level-3 headings:
      - 4a. Install cloudflared from AUR: `yay -S cloudflared` (or `paru -S cloudflared`); verify with `cloudflared --version`
      - 4b. Authenticate: `cloudflared tunnel login` → prints a URL → opens browser → user picks the `kinariwala.com` zone → cert.pem written to `~/.cloudflared/cert.pem`
      - 4c. Create tunnel: `cloudflared tunnel create walkability-prod` → outputs a UUID and the credentials JSON path (e.g. `~/.cloudflared/<UUID>.json`); instruct the user to copy the UUID for the next step
      - 4d. Write `/etc/cloudflared/config.yml`. Show the exact YAML template (in a fenced code block) with placeholder values the user must replace:

        ```yaml
        tunnel: <TUNNEL-UUID>
        credentials-file: /etc/cloudflared/<TUNNEL-UUID>.json

        ingress:
          - hostname: api-walkability.kinariwala.com
            service: http://localhost:80
          - service: http_status:404
        ```

        Include a note that the credentials JSON file must be copied from `~/.cloudflared/<UUID>.json` to `/etc/cloudflared/<UUID>.json` with `sudo cp` and that ownership/permissions should be `root:root 0600`.
      - 4e. Create the DNS record. Two options:
        - Option A (recommended): `cloudflared tunnel route dns walkability-prod api-walkability.kinariwala.com` — creates the proxied CNAME automatically
        - Option B (manual): In the Cloudflare dashboard → DNS → Add record → Type: CNAME, Name: `api-walkability`, Target: `<TUNNEL-UUID>.cfargotunnel.com`, Proxy: Enabled (orange cloud)
      - 4f. Install + enable the systemd service: `sudo cloudflared service install` then `sudo systemctl enable --now cloudflared.service` then `sudo systemctl status cloudflared --no-pager` to confirm running; finally `cloudflared tunnel info walkability-prod` to confirm a connector is registered.

    Section 5 — `## 5. Production Environment Variables`
    - One paragraph explaining that production env vars live in a `.env` file at the repo root next to `docker-compose.yml`. The deploy workflow (`.github/workflows/deploy.yml`) sets them in the runner environment for CI deploys; manual deploys read them from `.env`.
    - A table with columns: Variable | Generation Command / Example | Notes. Include exactly these rows:
      - `POSTGRES_PASSWORD` — `openssl rand -base64 32` — Strong random; never share; rotating requires DB restart
      - `JWT_SECRET` — `openssl rand -hex 32` — Min 32 chars; rotating invalidates all admin sessions
      - `COOKIE_SECURE` — `true` (literal) — Cloudflare terminates TLS so cookies always travel over HTTPS; never set to false on production
      - `CORS_ORIGIN` — e.g. `https://staging-walkability.kinariwala.com` — Exact frontend URL, no trailing slash, no wildcard
      - `ADMIN_SEED_EMAIL` — e.g. `admin@example.com` — Initial super-admin created on first boot
      - `ADMIN_SEED_PASSWORD` — `openssl rand -base64 24` — Min 12 chars; change via admin UI after first login
      - `POSTGRES_DB` (optional) — `walkability` (default) — Override only if running multiple instances
      - `POSTGRES_USER` (optional) — `walkability` (default) — Same reason
    - A code block showing a complete `.env` example with placeholder values clearly marked as placeholders.

    Section 6 — `## 6. First Manual Deploy`
    - Four labeled sub-steps (6a through 6d) as level-3 headings:
      - 6a. Clone the repo: `git clone https://github.com/<owner>/bengaluru-walkability-public-audit.git` then `cd bengaluru-walkability-public-audit`
      - 6b. Place the `.env` file at the repo root with the values from Section 5
      - 6c. Run the canonical first-up command (must match Plan 02 exactly):
        ```bash
        docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --build db backend nginx
        ```
      - 6d. Verify locally and publicly:
        - Local: `docker compose -f docker-compose.yml -f docker-compose.server.yml ps` (all three services healthy)
        - Local: `curl -f http://localhost/health` (expect `{"status":"ok"}`)
        - Public via tunnel: `curl -f https://api-walkability.kinariwala.com/health` (expect same response from the public internet)
        - Logs (if anything fails): `docker compose -f docker-compose.yml -f docker-compose.server.yml logs -f backend`

    Section 7 — `## 7. GitHub Actions Self-Hosted Runner`
    - Five labeled sub-steps (7a through 7e) as level-3 headings:
      - 7a. Go to GitHub: Settings → Actions → Runners → "New self-hosted runner" → select Linux/x64. GitHub displays a download + configure command block.
      - 7b. Create a runner user and home directory (optional but recommended): show `sudo useradd -m -s /bin/bash gh-runner` and `sudo usermod -aG docker gh-runner`
      - 7c. Run the GitHub-provided download and `./config.sh ...` commands as the runner user. When prompted for labels, enter exactly: `walkability-prod` (the `self-hosted` and `linux` labels are auto-applied)
      - 7d. Install as a systemd service: `sudo ./svc.sh install` then `sudo ./svc.sh start` then `sudo ./svc.sh status` to confirm
      - 7e. Verify the runner shows up in GitHub: repo Settings → Actions → Runners → should show one Idle runner with labels `self-hosted, Linux, X64, walkability-prod`. Confirm membership in the docker group with `groups gh-runner` (must include `docker`).

    Section 8 — `## 8. Ongoing Operations`
    - A short list of common operations with the exact command for each:
      - "Deploy a new build (automatic)": push to `main` → GitHub Actions runs deploy.yml → no manual step
      - "Deploy manually (force redeploy)": run the canonical `docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --remove-orphans db backend nginx` command as the runner user, or trigger `workflow_dispatch` from the GitHub Actions UI
      - "View backend logs": `docker compose -f docker-compose.yml -f docker-compose.server.yml logs -f backend`
      - "View nginx logs": `docker compose -f docker-compose.yml -f docker-compose.server.yml logs -f nginx`
      - "View tunnel logs": `sudo journalctl -u cloudflared -f`
      - "Restart a single service": `docker compose -f docker-compose.yml -f docker-compose.server.yml restart backend`
      - "Update env vars": edit `.env` → re-run the up command → containers pick up the new env on restart
      - "Stop everything": `docker compose -f docker-compose.yml -f docker-compose.server.yml down` (volumes preserved)

    Section 9 — `## 9. Troubleshooting`
    - A table with columns: Symptom | Likely Cause | Fix. Include at minimum these rows:
      - "Tunnel not connecting / `cloudflared tunnel info` shows 0 connectors" — credentials file missing or wrong UUID in config.yml — verify `/etc/cloudflared/config.yml` matches the UUID printed by `cloudflared tunnel create` and the credentials JSON is at the path referenced
      - "`docker: permission denied`" — user not in docker group or not re-logged-in — run `groups` to verify; log out + back in or run `newgrp docker`
      - "GitHub runner offline" — svc.sh not started or runner crashed — `sudo ./svc.sh status`; if not running, `sudo ./svc.sh start`
      - "Deploy job fails at health-wait step" — backend not coming up healthy — check `docker compose logs backend`; common cause: missing/invalid env var (POSTGRES_PASSWORD, JWT_SECRET)
      - "curl https://<tunnel-url>/health hangs or 502" — nginx or backend not running, or tunnel ingress points to wrong port — verify `docker compose ps` shows nginx healthy on the desktop; verify `/etc/cloudflared/config.yml` ingress points to `http://localhost:80`
      - "Admin login redirects back to login on production" — COOKIE_SECURE not set to true or CORS_ORIGIN mismatch — check `.env` has `COOKIE_SECURE=true` and `CORS_ORIGIN` matches the Vercel URL exactly with no trailing slash
      - "Push to main does not trigger deploy" — Production environment not created, or required reviewers blocking — verify GitHub Settings → Environments → "production" exists; check the deploy job's "Waiting" status in the Actions tab

    Formatting rules across the whole document:
    - Use level-2 (`##`) for section headings, level-3 (`###`) for labeled sub-steps (e.g., `### 4a. Install cloudflared`)
    - Fenced code blocks for ALL shell commands; specify language (`bash`, `yaml`, or `json`) on the fence
    - Use `>` blockquotes for important callouts (the "log out and back in" warning, security warnings)
    - Use tables (pipe-syntax) for structured information; keep column count ≤ 3
    - Do NOT include any reference to Railway except inside Section 1's brief mention that Railway was the prior staging host (optional, one sentence)
  </action>
  <verify>
    <automated>
      test -f DEPLOYMENT.md
      # All nine section headings present (exact match on numbering)
      grep -qE '^## 1\. Overview' DEPLOYMENT.md
      grep -qE '^## 2\. Prerequisites' DEPLOYMENT.md
      grep -qE '^## 3\. Arch Linux Desktop Setup' DEPLOYMENT.md
      grep -qE '^## 4\. Cloudflare Tunnel Setup' DEPLOYMENT.md
      grep -qE '^## 5\. Production Environment Variables' DEPLOYMENT.md
      grep -qE '^## 6\. First Manual Deploy' DEPLOYMENT.md
      grep -qE '^## 7\. GitHub Actions Self-Hosted Runner' DEPLOYMENT.md
      grep -qE '^## 8\. Ongoing Operations' DEPLOYMENT.md
      grep -qE '^## 9\. Troubleshooting' DEPLOYMENT.md
      # ASCII diagram references the three key boundaries
      grep -q 'Vercel' DEPLOYMENT.md
      grep -q 'Cloudflare' DEPLOYMENT.md
      grep -q 'cloudflared' DEPLOYMENT.md
      # Canonical compose commands
      grep -q 'docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --build db backend nginx' DEPLOYMENT.md
      grep -q 'docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --remove-orphans db backend nginx' DEPLOYMENT.md
      # cloudflared identifiers
      grep -q 'cloudflared tunnel login' DEPLOYMENT.md
      grep -q 'cloudflared tunnel create walkability-prod' DEPLOYMENT.md
      grep -q '/etc/cloudflared/config.yml' DEPLOYMENT.md
      grep -q 'cfargotunnel.com' DEPLOYMENT.md
      grep -q 'cloudflared service install' DEPLOYMENT.md
      grep -q 'systemctl enable --now cloudflared' DEPLOYMENT.md
      # Runner label
      grep -q 'walkability-prod' DEPLOYMENT.md
      # Required env vars all listed
      grep -q 'POSTGRES_PASSWORD' DEPLOYMENT.md
      grep -q 'JWT_SECRET' DEPLOYMENT.md
      grep -q 'COOKIE_SECURE' DEPLOYMENT.md
      grep -q 'CORS_ORIGIN' DEPLOYMENT.md
      grep -q 'ADMIN_SEED_EMAIL' DEPLOYMENT.md
      grep -q 'ADMIN_SEED_PASSWORD' DEPLOYMENT.md
      # Generation commands present
      grep -q 'openssl rand -hex 32' DEPLOYMENT.md
      # Pacman commands present
      grep -q 'pacman -S docker' DEPLOYMENT.md
      grep -q 'usermod -aG docker' DEPLOYMENT.md
      # svc.sh runner install commands
      grep -q 'svc.sh install' DEPLOYMENT.md
      grep -q 'svc.sh start' DEPLOYMENT.md
      # Troubleshooting table present (look for at least 3 rows by counting pipe rows in the table area)
      awk '/^## 9\. Troubleshooting/,0' DEPLOYMENT.md | grep -cE '^\|' | awk '{ if ($1 < 5) { print "Need >= 5 table rows (header + separator + 3 data rows minimum), got " $1; exit 1 } }'
      # Document length sanity check (>= 200 lines for the runbook to be useful)
      lines=$(wc -l < DEPLOYMENT.md); test "$lines" -ge 200 || { echo "DEPLOYMENT.md too short: $lines lines"; exit 1; }
    </automated>
  </verify>
  <done>
    DEPLOYMENT.md exists at the repository root with all nine numbered top-level sections in the correct order. The Cloudflare Tunnel section walks through cloudflared install (AUR), tunnel login, tunnel create, config.yml template, DNS routing (both `cloudflared tunnel route dns` and manual CNAME options), and systemd service install. The compose commands match Plan 02's deploy.yml verbatim. The GitHub runner section specifies the `walkability-prod` label and docker group membership. All required env vars from docker-compose.yml are documented with generation commands. The troubleshooting table covers tunnel, docker permissions, runner offline, deploy failures, public-URL hangs, login redirect loop, and missing production environment.
  </done>
  <acceptance_criteria>
    - File `DEPLOYMENT.md` exists at the repository root.
    - The document has level-2 headings matching exactly: `## 1. Overview`, `## 2. Prerequisites`, `## 3. Arch Linux Desktop Setup`, `## 4. Cloudflare Tunnel Setup`, `## 5. Production Environment Variables`, `## 6. First Manual Deploy`, `## 7. GitHub Actions Self-Hosted Runner`, `## 8. Ongoing Operations`, `## 9. Troubleshooting`.
    - Section 1 contains an ASCII diagram that mentions Vercel, Cloudflare (Tunnel), cloudflared, nginx, backend, and db nodes.
    - Section 4 includes a YAML code block (fenced with `yaml`) showing the `/etc/cloudflared/config.yml` template with `tunnel:`, `credentials-file:`, and `ingress:` keys, where ingress contains a `service: http://localhost:80` line and a `service: http_status:404` catch-all.
    - Section 4 documents both `cloudflared tunnel route dns walkability-prod ...` and the manual Cloudflare dashboard CNAME flow.
    - Section 5 contains a table with at least 6 rows for the required env vars (POSTGRES_PASSWORD, JWT_SECRET, COOKIE_SECURE, CORS_ORIGIN, ADMIN_SEED_EMAIL, ADMIN_SEED_PASSWORD).
    - Section 5 shows `openssl rand -hex 32` for JWT_SECRET and at least one `openssl rand -base64` invocation for password/secret generation.
    - Section 6 quotes the first-deploy command exactly as: `docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --build db backend nginx`.
    - Section 7 specifies the runner label as `walkability-prod` and shows the `svc.sh install` + `svc.sh start` sequence.
    - Section 9 has a troubleshooting table with at least 3 data rows covering: tunnel not connecting, docker permission denied, GitHub runner offline (or equivalent runner issue).
    - The document is at least 200 lines long.
    - The document references the existence of nginx.server.conf and docker-compose.server.yml (the artifacts produced by Plan 01).
  </acceptance_criteria>
</task>

</tasks>

<verification>
After Task 1 completes:

1. `test -f DEPLOYMENT.md` succeeds.
2. The nine `## N. Title` headings appear in numerical order (verified by `grep -nE '^## [0-9]+\. ' DEPLOYMENT.md`).
3. The canonical compose commands match those used by `.github/workflows/deploy.yml`.
4. The runner label `walkability-prod` is referenced in Section 7 — matches the label used in `runs-on:` in deploy.yml.
5. All six required production env vars are present in Section 5 with generation commands.
</verification>

<success_criteria>
- DEPLOYMENT.md is a self-contained, top-to-bottom runbook a future user (or future Claude) can follow without external research
- Cloudflare Tunnel setup is documented with both the automatic (`cloudflared tunnel route dns`) and manual (dashboard CNAME) approaches
- All compose commands match Plan 02 deploy.yml verbatim
- The runner section references the `walkability-prod` label used by deploy.yml
- All production env vars from docker-compose.yml are documented with safe generation commands
- The troubleshooting table addresses the most likely failure modes
</success_criteria>

<output>
Create `.planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-03-SUMMARY.md` when done.
</output>
