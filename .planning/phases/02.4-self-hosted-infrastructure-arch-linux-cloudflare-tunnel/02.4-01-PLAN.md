---
phase: 02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nginx/nginx.server.conf
  - docker-compose.server.yml
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "nginx.server.conf serves backend traffic with identical rate-limiting behavior as nginx.conf"
    - "docker-compose.server.yml is a valid Docker Compose override that disables the frontend container and re-mounts nginx with the backend-only config"
    - "Running `docker compose -f docker-compose.yml -f docker-compose.server.yml config` resolves without errors and shows frontend service with profile 'frontend-only'"
    - "nginx.server.conf has no upstream frontend block and no proxy_pass to frontend"
    - "nginx.server.conf returns 404 for any path not matching /api/, /uploads/, or /health"
  artifacts:
    - path: "nginx/nginx.server.conf"
      provides: "Backend-only nginx config for desktop production"
      contains: "upstream backend"
    - path: "docker-compose.server.yml"
      provides: "Compose override removing frontend dependency, swapping nginx config volume"
      contains: "nginx.server.conf"
  key_links:
    - from: "docker-compose.server.yml nginx volumes"
      to: "nginx/nginx.server.conf"
      via: "bind mount to /etc/nginx/conf.d/default.conf"
      pattern: "nginx.server.conf:/etc/nginx/conf.d/default.conf:ro"
    - from: "docker-compose.server.yml nginx depends_on"
      to: "backend service health"
      via: "service_healthy condition with no frontend dependency"
      pattern: "condition: service_healthy"
---

<objective>
Create the two infrastructure files that let the existing docker-compose.yml run in backend-only mode on the Arch Linux desktop:
- `nginx/nginx.server.conf` — backend-only nginx config (no frontend upstream, no /admin or / proxies, catch-all returns 404)
- `docker-compose.server.yml` — Compose override that removes the frontend dependency from nginx, swaps the mounted nginx config, and gates the frontend service behind a profile so it never starts under this override

Purpose: The frontend stays on Vercel — only backend + DB + nginx run on the desktop. The existing `docker-compose.yml` and `nginx/nginx.conf` MUST remain unchanged (they continue to serve full-stack local development).

Output: Two new files committed to the repo. After this plan, `docker compose -f docker-compose.yml -f docker-compose.server.yml up -d db backend nginx` is the canonical production launch command (executed manually in Plan 03 and via CI in Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Existing files the executor MUST read before writing anything new
@nginx/nginx.conf
@docker-compose.yml

<interfaces>
<!-- Verbatim references the executor will copy from nginx/nginx.conf. -->
<!-- These are the EXACT directives, names, sizes, and rates that must be preserved in nginx.server.conf. -->

Rate-limiting zones (from nginx/nginx.conf lines 1-18):
- `map $request_method $upload_limit_key { POST $binary_remote_addr; default ""; }`
- `limit_req_zone $upload_limit_key zone=upload:10m rate=5r/m;`
- `limit_req_zone $binary_remote_addr zone=admin_login:10m rate=5r/m;`
- `limit_req_zone $binary_remote_addr zone=admin_api:10m rate=60r/m;`

Upstream to preserve:
- `upstream backend { server backend:3001; }`

Upstream to OMIT:
- `upstream frontend { server frontend:3000; }`

Log format to preserve (lines 32-43):
- `log_format json_combined escape=json ... ;`
- `access_log /var/log/nginx/access.log json_combined;`

Server block directives to preserve (lines 45-82):
- `listen 80; server_name _;`
- `client_max_body_size 20M;`
- `client_body_buffer_size 10m;`
- `client_body_temp_path /tmp/nginx_upload_temp;`
- `proxy_connect_timeout 5s;`
- `proxy_send_timeout 30s;`
- `proxy_read_timeout 30s;`

Location blocks to preserve verbatim:
- `location = /api/admin/auth/login` (zone=admin_login, burst=3 nodelay, limit_req_status 429, all proxy headers including Host, X-Real-IP, X-Forwarded-For, X-Request-ID)
- `location /api/admin/` (zone=admin_api, burst=10 nodelay, limit_req_status 429, full proxy header set incl. X-Forwarded-Proto)
- `location /api/` (zone=upload, burst=2 nodelay, full proxy header set incl. X-Forwarded-Proto)
- `location /uploads/` (proxy to backend, `expires 30d`, `add_header Cache-Control "public, no-transform"`)
- `location /health` (proxy to backend, Host header only)

Location blocks to OMIT:
- `location /admin { ... proxy_pass http://frontend; ... }`  — frontend is on Vercel, not running here
- `location / { proxy_pass http://frontend; ... }`  — same reason

Location block to ADD:
- `location / { return 404; }`  — catch-all for anything not matched above

docker-compose.yml interfaces (from lines 100-125, the nginx service):
- nginx currently has `depends_on: { backend: { condition: service_healthy }, frontend: { condition: service_healthy } }`
- nginx currently has `volumes: - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro`
- frontend service is defined at lines 61-98 with no `profiles:` key
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nginx/nginx.server.conf (backend-only nginx config)</name>
  <files>nginx/nginx.server.conf</files>
  <read_first>
    - nginx/nginx.conf (entire file — this is the source of every block being copied verbatim)
    - CLAUDE.md (project conventions, API endpoint table)
  </read_first>
  <action>
    Create `nginx/nginx.server.conf` as a new file. Do NOT modify `nginx/nginx.conf`.

    The file MUST be structured as follows (top to bottom):

    1. Header comment block (3-6 lines) explaining: this is the backend-only variant used in production self-hosting on the Arch Linux desktop behind a Cloudflare tunnel; frontend is served by Vercel; the file is selected by docker-compose.server.yml override; nginx.conf (full-stack) remains the canonical local-dev config.

    2. The `map $request_method $upload_limit_key { ... }` block — copy verbatim from nginx.conf lines 3-6.

    3. The three `limit_req_zone` directives — copy verbatim from nginx.conf lines 7, 12, 18. Identical zone names (upload, admin_login, admin_api), identical sizes (10m), identical rates (5r/m, 5r/m, 60r/m).

    4. The `upstream backend { server backend:3001; }` block — copy verbatim from nginx.conf lines 22-24.

    5. DO NOT include any `upstream frontend { ... }` block.

    6. The `log_format json_combined` block and `access_log` directive — copy verbatim from nginx.conf lines 32-43.

    7. A single `server { listen 80; server_name _; ... }` block containing (in order):
       - `client_max_body_size 20M;`
       - `client_body_buffer_size 10m;`
       - `client_body_temp_path /tmp/nginx_upload_temp;`
       - `proxy_connect_timeout 5s; proxy_send_timeout 30s; proxy_read_timeout 30s;`
       - A short comment block (2-4 lines) noting that TLS termination is handled upstream by the Cloudflare tunnel; this nginx serves plain HTTP on port 80 to the tunnel daemon on the same host.
       - `location = /api/admin/auth/login { ... }` — copy verbatim from nginx.conf lines 86-97 (admin_login zone, burst=3 nodelay, limit_req_status 429, proxy_pass http://backend, proxy headers: Host, X-Real-IP, X-Forwarded-For, X-Request-ID, plus the three proxy timeout directives).
       - `location /api/admin/ { ... }` — copy verbatim from nginx.conf lines 103-115 (admin_api zone, burst=10 nodelay, limit_req_status 429, proxy_pass http://backend, proxy headers including X-Forwarded-Proto, plus the three proxy timeout directives).
       - `location /api/ { ... }` — copy verbatim from nginx.conf lines 118-126 (upload zone, burst=2 nodelay, proxy_pass http://backend, full proxy header set).
       - `location /uploads/ { ... }` — copy verbatim from nginx.conf lines 129-137 (proxy_pass http://backend, proxy_cache_bypass, expires 30d, add_header Cache-Control "public, no-transform", all proxy headers).
       - `location /health { ... }` — copy verbatim from nginx.conf lines 140-143 (proxy_pass http://backend, Host header only).
       - DO NOT include any `location /admin { ... }` block.
       - Final block: `location / { return 404; }` — catch-all. Any request not matching the preceding locations gets HTTP 404, because there is no frontend to proxy to on this host.

    8. Close the server block.

    Preserve nginx's standard 4-space (or original) indentation. Preserve comments inside copied blocks where they document why a directive exists (e.g., the X-Forwarded-Proto note, the burst=10 dashboard fan-out note).

    Naming/identifier requirements that the executor MUST get right:
    - Zone names: exactly `upload`, `admin_login`, `admin_api` (not pluralized, not renamed)
    - Upstream name: exactly `backend` (lowercase)
    - All `proxy_pass` targets: exactly `http://backend` (no trailing slash, no port)
    - Listen directive: exactly `listen 80;` (no SSL, no http2)
  </action>
  <verify>
    <automated>
      # Structural assertions on the new file:
      test -f nginx/nginx.server.conf
      # Rate-limit zones present (3 of them, exact zone names)
      grep -q 'zone=upload:10m rate=5r/m' nginx/nginx.server.conf
      grep -q 'zone=admin_login:10m rate=5r/m' nginx/nginx.server.conf
      grep -q 'zone=admin_api:10m rate=60r/m' nginx/nginx.server.conf
      # Backend upstream present
      grep -q 'upstream backend' nginx/nginx.server.conf
      grep -q 'server backend:3001;' nginx/nginx.server.conf
      # Frontend upstream and proxy_pass ABSENT
      ! grep -q 'upstream frontend' nginx/nginx.server.conf
      ! grep -q 'proxy_pass http://frontend' nginx/nginx.server.conf
      # Required location blocks PRESENT
      grep -q 'location = /api/admin/auth/login' nginx/nginx.server.conf
      grep -q 'location /api/admin/' nginx/nginx.server.conf
      grep -q 'location /api/' nginx/nginx.server.conf
      grep -q 'location /uploads/' nginx/nginx.server.conf
      grep -q 'location /health' nginx/nginx.server.conf
      # Frontend location blocks ABSENT
      ! grep -q 'location /admin ' nginx/nginx.server.conf
      ! grep -qE 'location / \{[^}]*proxy_pass[[:space:]]+http://frontend' nginx/nginx.server.conf
      # Catch-all 404 present
      grep -qE 'location / \{[[:space:]]*return 404' nginx/nginx.server.conf
      # client_max_body_size preserved
      grep -q 'client_max_body_size 20M;' nginx/nginx.server.conf
      # Validate nginx syntax inside a transient container (no live reload)
      docker run --rm -v "$PWD/nginx/nginx.server.conf:/etc/nginx/conf.d/default.conf:ro" nginx:alpine nginx -t
    </automated>
  </verify>
  <done>
    nginx.server.conf exists with verbatim copies of all rate-limit zones, upstream backend, log_format, server block directives, and the five required location blocks (=/api/admin/auth/login, /api/admin/, /api/, /uploads/, /health). It contains NO upstream frontend, NO proxy_pass http://frontend, and NO location /admin block. The catch-all `location /` returns 404. `nginx -t` inside an nginx:alpine container passes with no errors. nginx/nginx.conf is unmodified.
  </done>
  <acceptance_criteria>
    - File `nginx/nginx.server.conf` exists in the repository.
    - File `nginx/nginx.conf` is byte-identical to its pre-task state (git diff shows zero changes).
    - All three rate-limiting zone declarations (upload, admin_login, admin_api) are present with the exact rates 5r/m, 5r/m, 60r/m respectively.
    - The string `upstream backend` appears exactly once and `upstream frontend` does not appear.
    - The following location selectors are present (one occurrence each): `location = /api/admin/auth/login`, `location /api/admin/`, `location /api/`, `location /uploads/`, `location /health`.
    - The string `location /admin ` (with trailing space, to match the prefix block) does not appear.
    - The string `proxy_pass http://frontend` does not appear anywhere in the file.
    - The catch-all `location /` exists and contains `return 404`.
    - `docker run --rm -v "$PWD/nginx/nginx.server.conf:/etc/nginx/conf.d/default.conf:ro" nginx:alpine nginx -t` exits 0 and prints "syntax is ok" + "test is successful".
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create docker-compose.server.yml (Compose override for backend-only desktop deploy)</name>
  <files>docker-compose.server.yml</files>
  <read_first>
    - docker-compose.yml (the base file this override targets — read the whole file to understand the nginx and frontend service definitions being overridden)
    - nginx/nginx.server.conf (produced in Task 1 — the new config the override mounts)
    - CLAUDE.md
  </read_first>
  <action>
    Create `docker-compose.server.yml` at the repository root as a Docker Compose override file. Do NOT modify `docker-compose.yml`.

    File MUST contain (in this order):

    1. A header comment block (5-10 lines) documenting:
       - Purpose: backend-only production override for self-hosted desktop deployment behind a Cloudflare tunnel
       - Usage: `docker compose -f docker-compose.yml -f docker-compose.server.yml up -d db backend nginx`
       - What it does: removes the `frontend` dependency from nginx, swaps nginx's mounted config to nginx.server.conf, and parks the frontend service behind a profile so it never starts under this override
       - What it does NOT do: redefine db or backend (they inherit unchanged from docker-compose.yml)

    2. Top-level key `services:` with exactly two child service overrides:

       a. `nginx:` override block containing:
          - `depends_on:` — REPLACE the base file's two-key map (backend + frontend) with a single-key map listing ONLY backend with `condition: service_healthy`. Use the long-form map syntax (key under `backend:`). Do NOT include any frontend key under depends_on.
          - `volumes:` — REPLACE the base mount. Use the syntax `- ./nginx/nginx.server.conf:/etc/nginx/conf.d/default.conf:ro` as the only volume entry. (Compose merges by list-replacement for service-level `volumes` only when the override redeclares the list, which is the intent here.)
          - Do NOT redefine image, ports, healthcheck, restart, or deploy — these all inherit from the base file.

       b. `frontend:` override block containing ONLY:
          - `profiles:` with a single list entry `- frontend-only`
          - No other keys. The base file's frontend service definition (build, depends_on, environment, healthcheck, restart, deploy) remains intact in docker-compose.yml. Adding the `frontend-only` profile here means: with no `--profile frontend-only` flag, the frontend service is excluded from `docker compose up`, even if its name is listed on the command line.

    3. DO NOT include any `db:` or `backend:` block — these services inherit from docker-compose.yml without changes.

    4. DO NOT include a top-level `version:` key (Docker Compose v2 ignores it and warns).

    5. DO NOT include any `volumes:` top-level key — named volumes are defined in docker-compose.yml.

    YAML formatting rules: 2-space indentation, no tabs, list dashes aligned with the parent key, no trailing whitespace.
  </action>
  <verify>
    <automated>
      # File exists
      test -f docker-compose.server.yml
      # Header comment indicates purpose
      grep -qi 'cloudflare\|self-host\|override\|backend-only' docker-compose.server.yml
      # nginx override redirects to nginx.server.conf
      grep -q 'nginx.server.conf:/etc/nginx/conf.d/default.conf:ro' docker-compose.server.yml
      # frontend service is gated behind a profile
      grep -A1 '^  frontend:' docker-compose.server.yml | grep -q 'profiles:'
      grep -q 'frontend-only' docker-compose.server.yml
      # No top-level version key
      ! grep -qE '^version:' docker-compose.server.yml
      # No db or backend service redefinition
      ! grep -qE '^  db:' docker-compose.server.yml
      ! grep -qE '^  backend:' docker-compose.server.yml
      # docker-compose.yml is unmodified
      git diff --quiet docker-compose.yml
      # Compose can resolve the merged config without errors
      POSTGRES_PASSWORD=dummy JWT_SECRET=dummy_at_least_32_characters_long_for_test \
        docker compose -f docker-compose.yml -f docker-compose.server.yml config > /tmp/merged.yml 2>&1
      # In merged config, nginx depends_on must NOT mention frontend
      ! grep -A5 'service: nginx' /tmp/merged.yml | grep -q 'frontend'
      # In merged config, nginx volumes must reference nginx.server.conf
      grep -q 'nginx.server.conf' /tmp/merged.yml
      # Starting up db + backend + nginx without --profile must NOT start frontend
      POSTGRES_PASSWORD=dummy JWT_SECRET=dummy_at_least_32_characters_long_for_test \
        docker compose -f docker-compose.yml -f docker-compose.server.yml config --services | sort > /tmp/services.txt
      # The frontend service IS listed (Compose still knows about it), but `up` would skip it without the profile.
      # Verify that without the profile flag, frontend is filtered out:
      POSTGRES_PASSWORD=dummy JWT_SECRET=dummy_at_least_32_characters_long_for_test \
        docker compose -f docker-compose.yml -f docker-compose.server.yml config --profiles | grep -q 'frontend-only'
    </automated>
  </verify>
  <done>
    docker-compose.server.yml exists at the repo root. `docker compose -f docker-compose.yml -f docker-compose.server.yml config` succeeds with dummy env vars and produces a merged manifest in which (a) nginx depends only on backend and mounts nginx.server.conf, (b) the frontend service is gated behind the `frontend-only` profile, (c) db and backend service definitions are inherited unchanged from docker-compose.yml. docker-compose.yml is byte-identical to its pre-task state.
  </done>
  <acceptance_criteria>
    - File `docker-compose.server.yml` exists at the repository root.
    - File `docker-compose.yml` shows zero diff after this task.
    - The file contains exactly two service override keys at depth 1 under `services:`: `nginx` and `frontend`. No `db` or `backend` block exists in this file.
    - The nginx override's `volumes:` list contains the entry `./nginx/nginx.server.conf:/etc/nginx/conf.d/default.conf:ro` and references no other nginx config path.
    - The nginx override's `depends_on:` map contains exactly one key: `backend:` with `condition: service_healthy`. The key `frontend:` does not appear anywhere inside the nginx override.
    - The frontend override declares `profiles: [frontend-only]` (or YAML list equivalent) and no other keys.
    - The file contains no top-level `version:` key.
    - `docker compose -f docker-compose.yml -f docker-compose.server.yml config` exits 0 when run with `POSTGRES_PASSWORD` and `JWT_SECRET` set to non-empty values.
    - In the merged config output, the nginx service's `depends_on` lists only backend.
    - In the merged config output, the nginx service's `volumes` references `nginx.server.conf`.
    - `docker compose -f docker-compose.yml -f docker-compose.server.yml config --profiles` lists `frontend-only`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks complete:

1. `git status` shows two new untracked files: `nginx/nginx.server.conf` and `docker-compose.server.yml`. No other files modified.
2. `git diff nginx/nginx.conf docker-compose.yml` is empty.
3. `docker run --rm -v "$PWD/nginx/nginx.server.conf:/etc/nginx/conf.d/default.conf:ro" nginx:alpine nginx -t` exits 0.
4. `POSTGRES_PASSWORD=dummy JWT_SECRET=dummy_secret_at_least_32_chars_long docker compose -f docker-compose.yml -f docker-compose.server.yml config` exits 0 and produces a merged manifest that:
   - Has nginx depending only on backend (no frontend in depends_on)
   - Mounts `./nginx/nginx.server.conf` to `/etc/nginx/conf.d/default.conf`
   - Lists `frontend-only` as a known profile
5. `grep -c '^location' nginx/nginx.server.conf` returns 6 (the five preserved location blocks plus the catch-all `location /`).
</verification>

<success_criteria>
- nginx.server.conf passes `nginx -t` syntax validation
- docker-compose.server.yml passes `docker compose config` validation when merged with docker-compose.yml
- No existing files (docker-compose.yml, nginx/nginx.conf) were modified
- The override correctly removes the frontend dependency from nginx while preserving all backend routing behavior
- All five backend-relevant location blocks (admin login, admin API, public API, uploads, health) preserve their rate-limit zones, burst values, and proxy header sets from nginx.conf verbatim
</success_criteria>

<output>
Create `.planning/phases/02.4-self-hosted-infrastructure-arch-linux-cloudflare-tunnel/02.4-01-SUMMARY.md` when done.
</output>
