---
phase: 03.1-admin-report-detail-split-layout
fixed_at: 2026-05-26T00:00:00Z
review_path: .planning/phases/03.1-admin-report-detail-split-layout/03.1-REVIEW.md
iteration: 1
findings_in_scope: 9
fixed: 9
skipped: 0
status: all_fixed
---

# Phase 03.1: Code Review Fix Report

**Fixed at:** 2026-05-26
**Source review:** .planning/phases/03.1-admin-report-detail-split-layout/03.1-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 9
- Fixed: 9
- Skipped: 0

## Fixed Issues

### CR-01: Delete failure is silent — user receives no error feedback

**Files modified:** `frontend/app/admin/reports/[id]/page.tsx`
**Commit:** 4519c8b
**Applied fix:** Added `deleteError` state (`useState<string | null>(null)`), cleared it at the start of `handleDelete`, set it to `"Failed to delete report. Please try again."` in the catch block, and rendered it as a `<p role="alert">` inside the delete modal before the Cancel/Delete button row.

---

### WR-01: "Zoom photo" button is non-functional interactive element

**Files modified:** `frontend/app/admin/reports/[id]/page.tsx`
**Commit:** d6a2165
**Applied fix:** Replaced the non-functional `<button aria-label="Zoom photo">` with a `<div aria-hidden="true">` so the element is removed from the accessibility tree until a lightbox is implemented (Option B from the review suggestion). Removed `cursor: pointer` and `border: none` styles that were button-specific.

---

### WR-02: "Open in Maps" URL uses unguarded latitude/longitude

**Files modified:** `frontend/app/admin/reports/[id]/page.tsx`
**Commit:** cba0b9f
**Applied fix:** Wrapped the `<a>` element in a conditional render `(report.latitude != null && report.longitude != null) ? <a ...> : <span>No coordinates</span>` to guard against null/undefined values producing a `?q=null,null` URL.

---

### WR-03: `getMe()` promise guard is test-accommodation logic in production code

**Files modified:** `frontend/app/admin/reports/[id]/page.tsx`, `frontend/app/admin/reports/[id]/__tests__/page.test.tsx`
**Commit:** 7b07a08
**Applied fix:** Removed the defensive `if (promise && typeof promise.then === "function")` guard from the component — `getMe()` is now called normally with `.then()/.catch()`. Added `(adminApiModule.getMe as jest.Mock).mockResolvedValue({ role: "admin" })` to all four `beforeEach` blocks in the test file (data fetching, content rendering, Phase 3.1 layout, and ISSUE-06 timeline describe blocks).

---

### WR-04: `GbaHierarchyPanel` nothing-state guard misses `undefined` ward_name

**Files modified:** `frontend/app/admin/components/GbaHierarchyPanel.tsx`
**Commit:** 37fcb6e
**Applied fix:** Changed `hierarchy.ward_name === null` to `!hierarchy.ward_name`, making the nothing-state guard fire for both `null` and `undefined` values.

---

### WR-05: Test fixture `FULL_REPORT` omits required Phase 3 fields from `AdminReport`

**Files modified:** `frontend/app/admin/reports/[id]/__tests__/page.test.tsx`
**Commit:** 5dc99f4
**Applied fix:** Added three missing Phase 03 fields to the `FULL_REPORT` fixture: `resolution_photo_url: null`, `resolution_notes: null`, and `assigned_org_id: null`.

---

### IN-01: Photo rendering code duplicated between mobile and desktop layouts

**Files modified:** `frontend/app/admin/reports/[id]/page.tsx`
**Commit:** 1dfe411
**Applied fix:** Extracted a `PhotoHero` component (placed before `ReportDetailPage`) with a `showOverlays` boolean prop. The mobile `photoSection` variable now uses `<PhotoHero report={report} categoryLabel={categoryLabel} showOverlays />`. The desktop left-column photo uses `<PhotoHero report={report} categoryLabel={categoryLabel} />` (no overlays). The no-photo fallback (gradient div + "NO PHOTO" text) is unified across both paths; the `minHeight: 400` is applied only when `showOverlays` is false (desktop). The `PhotoTile` import was removed as it is no longer used.

---

### IN-02: `status.replace("_", " ")` replaces only the first underscore

**Files modified:** `frontend/app/admin/reports/[id]/page.tsx`
**Commit:** da8be24
**Applied fix:** Changed `report.status.replace("_", " ")` to `report.status.replace(/_/g, " ")` to replace all underscores globally, making the status display future-proof for multi-underscore enum values.

---

### IN-03: Test coverage gap — Corporation row title not asserted in happy-path test

**Files modified:** `frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx`
**Commit:** ca058e6
**Applied fix:** Added `expect(screen.getByText("Chief Commissioner")).toBeInTheDocument()` to the "renders bureaucratic chain label/title pairs" test, alongside the existing `"Chief Commissioner, GBA"` assertion. `getByText("Chief Commissioner")` uniquely matches the Corporation row because the GBA row title is the distinct string `"Chief Commissioner, GBA"`.

---

_Fixed: 2026-05-26_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
