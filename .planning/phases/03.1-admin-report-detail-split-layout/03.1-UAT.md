---
status: complete
phase: 03.1-admin-report-detail-split-layout
source: [03.1-01-SUMMARY.md]
started: 2026-05-30T00:00:00Z
updated: 2026-05-30T08:40:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Desktop Split Layout
expected: On a viewport >= 768px wide, the admin report detail page shows a two-column layout. Left column: photo on top (fills remaining height), identity strip at the bottom with status badges + category + telemetry. Right column: action panels (StatusActionPanel, OrgAssignPanel, GbaHierarchyPanel, description, map, status history) with an independent scrollbar — scrolling the right rail does NOT scroll the left column. The left column stays fixed while you scroll the right rail.
result: pass

### 2. Mobile Single-Column Layout
expected: On a viewport < 768px wide (or browser devtools mobile simulation), the same page stacks vertically in this exact order: photo → identity strip (status badges, category, telemetry) → action panels. No two-column split. Content flows naturally in a single column.
result: pass

### 3. Ward Row Null Guard (ISSUE-01)
expected: Open a report where ward_number is null (or not assigned yet). In the GBA Hierarchy panel, the Ward row shows only the ward_name (e.g. "Shivajinagar") — no "undefined" prefix, no "undefined · Shivajinagar" string.
result: pass

### 4. Null Personnel Rows Hidden (ISSUE-03)
expected: For a report where ARO, RO, or Zone fields are null (most reports in dev), those rows are completely absent from the GBA Hierarchy panel — no row with a dash or placeholder. Only the Ward row and GBA row are always visible.
result: skipped
reason: All available dev reports have ARO, RO, and Zone populated — no null-hierarchy report available to test against.

### 5. AC Number and Constituency Name Visually Distinct (ISSUE-04)
expected: In the GBA Hierarchy panel, the Assembly Constituency section shows the AC number (e.g. "155") in a monospace/smaller font style, and the constituency name (e.g. "Shivajinagar") in a separate bolder sans-serif style — they are on separate visual elements, not a combined "155 – Shivajinagar" string.
result: pass

### 6. Disclaimer Text Wraps (ISSUE-05)
expected: The disclaimer text at the bottom of the GBA Hierarchy panel (the long legal/data disclaimer) wraps within the panel boundary. No horizontal overflow, no text getting cut off, no scrollbar on the disclaimer itself.
result: pass

### 7. Status Timeline Dot Color (ISSUE-06)
expected: In the status history section, each timeline entry has a colored circle dot. The dot color matches the report status: e.g. "open" → green/teal, "in_progress" → blue, "resolved" → purple, "closed" → grey. The dot is NOT transparent or invisible.
result: issue
reported: "Dot color correct. But timestamp shows 'Invalid Date' and attribution shows 'BY · SYSTEM' — should show the name/designation of who changed the status for audit trail."
severity: major

### 8. Delete Error Shown in Modal (CR-01)
expected: When a delete fails (simulate by temporarily going offline or using DevTools → Network → Block request URL for the delete endpoint), the delete confirmation modal shows an error message like "Failed to delete report. Please try again." rather than silently closing or doing nothing.
result: pass

### 9. Open in Maps — Null Coordinates Guard (WR-02)
expected: For a report that has no GPS coordinates (latitude/longitude are null), the location section shows "No coordinates" text instead of a broken "Open in Maps" link that would produce a ?q=null,null URL.
result: skipped
reason: Frontend always captures coordinates (EXIF or browser geolocation fallback) — null-coordinates reports cannot be submitted through normal flow. Unit test covers the guard.

### 10. Build Hygiene
expected: Running `npm run build` in the frontend directory completes successfully — "Compiled successfully" or "✓ Generating static pages", zero TypeScript errors, zero ESLint errors. All 873 tests pass with `npm test`.
result: pass

## Summary

total: 10
passed: 7
issues: 1
pending: 0
skipped: 2
blocked: 0

## Gaps

- truth: "Status history shows the date the status was last changed and who changed it (name/designation)"
  status: resolved
  reason: "Resolved by Phase 03.2: backend adds r.updated_at + status_history with admin attribution; frontend renders real history entries, eliminating 'Invalid Date' and 'SYSTEM'"
  severity: major
  test: 7
  root_cause: |
    Two distinct bugs:
    1. Invalid Date — backend get_admin_report_by_id query does not select `updated_at` from
       the reports table. Frontend AdminReport.updated_at is undefined at runtime.
       new Date(undefined) = "Invalid Date". Fix: add `r.updated_at` to the backend SQL query
       and JSON response in admin_queries.rs get_admin_report_by_id().
    2. BY SYSTEM — status_history API not implemented yet. The timeline entry is a hardcoded
       placeholder using `report.updated_at` (itself broken) and the literal string "SYSTEM".
       The status_history table exists in the DB (migration 002) but no endpoint exposes it.
       Fix: implement GET /api/admin/reports/:id/status-history or include status_history
       entries in the single-report response, with changed_by_id joined to admin_users.display_name.
  artifacts:
    - path: "backend/src/db/admin_queries.rs"
      issue: "get_admin_report_by_id SQL does not SELECT r.updated_at; not included in JSON response"
    - path: "frontend/app/admin/reports/[id]/page.tsx:502"
      issue: "formatDate(report.updated_at) — updated_at is undefined; hardcoded '· BY · SYSTEM' placeholder"
  missing:
    - "Add r.updated_at to get_admin_report_by_id SELECT and JSON response (AdminReport.updated_at type already declared)"
    - "Implement status_history API endpoint or inline history in single-report response with admin user attribution"
  debug_session: ""
