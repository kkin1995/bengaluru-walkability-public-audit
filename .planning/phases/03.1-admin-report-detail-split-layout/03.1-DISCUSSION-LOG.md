# Phase 3.1: Admin Report Detail — Split Layout - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-26
**Phase:** 03.1-admin-report-detail-split-layout
**Areas discussed:** Left column content, UAT bugs scope

---

## Left Column Content

### Q1: Which items should move from the right rail to the left column?

| Option | Description | Selected |
|--------|-------------|----------|
| Photo + identity row only | Left = photo + one compact row: status badge, severity, category label | |
| Photo + telemetry block | Left = photo + telemetry grid (LAT/LNG/WARD/SUBMITTED_AT/UUID etc.) | |
| Photo + all read-only info | Left = photo + telemetry + category + description + map thumbnail | |
| You decide | Claude determines best metadata partition based on Figma/VS Code pattern | ✓ |

**User's choice:** You decide  
**Notes:** User delegated partition decision to Claude. Claude determined: photo (flex:1) + identity strip (status badge, severity, ConfidencePill, DUP pill, category title, telemetry grid). Right column starts with action panels.

---

### Q2: Should duplicate signals stay in the identity strip?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, keep them in the identity strip | DUP_CONF and DUP +N are read-only severity signals — appropriate alongside status + severity | ✓ |
| No, move them to the right rail | Keeps left column minimal; dup signals surface during triage action |  |

**User's choice:** Yes — keep duplicate signals in the identity strip  
**Notes:** Consistent with existing telemetry block grouping.

---

## UAT Bugs Scope

### Q1: Should ISSUE-01..06 be fixed in Phase 3.1?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — fix all ISSUE-01..06 in this phase | Bundle all UAT bugs; Phase 3.1 is the UAT polish phase | ✓ |
| No — layout only (ISSUE-07 only) | Strict scope per roadmap; ISSUE-01..06 go to Phase 3.2 | |

**User's choice:** Yes — all 7 UAT bugs in Phase 3.1  
**Notes:** Phase 3.1 is the natural UAT polish phase; all bugs came from the same session.

---

### Q2: How should empty personnel columns in GbaHierarchyPanel be handled?

| Option | Description | Selected |
|--------|-------------|----------|
| Remove personnel columns entirely | Strip the structure; show only structural hierarchy + elected chain | |
| Keep columns, show "Not available" | Retain structure; display "—" where data is missing | |
| You decide | Claude determines best approach |  |
| Other (free text) | Hide if not available, show if it is available | ✓ |

**User's choice:** Conditional rendering — hide rows when field is null/empty, show when data is present  
**Notes:** User wants to eventually find/add this data from the web. Phase 3.1 adds null-guard rendering so the UI is clean now and will automatically show the data when it's added.

---

## Claude's Discretion

- Desktop split layout column proportions and exact flex CSS values
- Whether `rightPanel` JSX is split in-place or refactored into named subcomponents
- StatusBadge dot color map update in status history timeline (Phase 3 enum values replace old `submitted`/`under_review` references)
- CSS fixes for ISSUE-04/05 (AC label format, disclaimer overflow) in GbaHierarchyPanel
- Desktop breakpoint change: 1024px → 768px (aligned with roadmap success criteria; not discussed but decision is clear from the spec)

## Deferred Ideas

- **Personnel / officer data**: User wants to eventually source named contacts (Ward Supervisor, Area Engineer) from the web and populate the DB. Deferred — Phase 3.1 only adds conditional rendering.
- **Full status history timeline**: Current implementation shows only current status as fallback. Full history API expansion deferred to a future phase.
