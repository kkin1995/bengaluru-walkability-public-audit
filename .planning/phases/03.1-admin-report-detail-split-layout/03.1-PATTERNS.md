# Phase 3.1: Admin Report Detail — Split Layout - Pattern Map

**Mapped:** 2026-05-26
**Files analyzed:** 4 (2 source edits + 2 test edits)
**Analogs found:** 4 / 4

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `frontend/app/admin/reports/[id]/page.tsx` | component (page) | request-response | self (already exists; restructure only) | exact |
| `frontend/app/admin/components/GbaHierarchyPanel.tsx` | component (panel) | transform | self (already exists; bug fixes only) | exact |
| `frontend/app/admin/reports/[id]/__tests__/page.test.tsx` | test | — | self (already exists; extend with new assertions) | exact |
| `frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx` | test | — | self (already exists; extend with new assertions) | exact |

Both source files and both test files already exist. Phase 3.1 is exclusively a redistribution and bug-fix phase — no new files are created.

---

## Pattern Assignments

### `frontend/app/admin/reports/[id]/page.tsx` (component, request-response)

**Analog:** self — the file is being restructured, not replaced.

**Imports pattern** (lines 1-25):
```tsx
"use client";

import { useState, useEffect } from "react";
import { useRouter } from "next/navigation";
import {
  getAdminReport,
  deleteReport,
  updateReportStatus,
  getMe,
  type AdminReport,
} from "../../lib/adminApi";
import { getCategoryLabel } from "@/app/lib/translations";
import StatusBadge from "../../components/StatusBadge";
import { SeverityIndicator } from "../../components/SeverityIndicator";
import { Card } from "../../components/Card";
import { Btn } from "../../components/Btn";
import { Pill } from "../../components/Pill";
import { PhotoTile } from "../../components/PhotoTile";
import { SectionLabel } from "../../components/SectionLabel";
import { ConfidencePill } from "../../components/ConfidencePill";
import StatusActionPanel from "../../components/StatusActionPanel";
import OrgAssignPanel from "../../components/OrgAssignPanel";
import GbaHierarchyPanel from "../../components/GbaHierarchyPanel";
import ResolveModal from "../../components/ResolveModal";
```
No new imports needed. All component primitives (StatusBadge, SeverityIndicator, ConfidencePill, Pill, Card, SectionLabel) are already imported.

**isDesktop / matchMedia pattern** (lines 66-74) — change breakpoint string only:
```tsx
// CURRENT (line 68) — change to 768px:
const mq = window.matchMedia("(min-width: 1024px)");

// AFTER (D-04):
const mq = window.matchMedia("(min-width: 768px)");

// Full pattern — surrounding code is unchanged:
useEffect(() => {
  if (typeof window !== "undefined" && typeof window.matchMedia === "function") {
    const mq = window.matchMedia("(min-width: 768px)");
    setIsDesktop(mq.matches);
    const handler = (e: MediaQueryListEvent) => setIsDesktop(e.matches);
    mq.addEventListener("change", handler);
    return () => mq.removeEventListener("change", handler);
  }
}, []);
```

**Desktop grid pattern** (line 511) — grid ratio unchanged, only column content changes:
```tsx
// Source: page.tsx line 511 — NO CHANGE to this div
<div style={{
  display: "grid",
  gridTemplateColumns: "1.3fr 1fr",
  gap: 0,
  minHeight: "calc(100vh - 120px)"
}}>
```

**Left column — new flex-column pattern** (D-01, D-03):
```tsx
// Replace the current bare <div style={{ background: "var(--surface-2)", borderRadius: ... }}>
// with this flex-column structure:
<div style={{
  display: "flex",
  flexDirection: "column",
  height: "calc(100vh - 120px)",
  overflow: "hidden",
  background: "var(--surface-2)",
  borderRadius: "var(--r-lg)",
}}>
  {/* Photo hero — grows to fill remaining height */}
  <div style={{ flex: 1, overflow: "hidden", minHeight: 200 }}>
    {/* img or PhotoTile placeholder — same JSX as current lines 515-535 */}
  </div>

  {/* Identity strip — anchored below photo, never scrolls */}
  <div style={{ flexShrink: 0, padding: "12px 16px", borderTop: "1px solid var(--border)" }}>
    {identityStripContent}
  </div>
</div>
```

**Identity strip content variable** (content moved from rightPanel, D-02):
```tsx
// New named JSX variable; extracted from rightPanel lines 267-347
const identityStripContent = (
  <>
    {/* Status badges row */}
    <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 12 }}>
      <StatusBadge status={report.status} />
      <SeverityIndicator severity={sevValue} />
      {report.duplicate_confidence && (
        <ConfidencePill confidence={report.duplicate_confidence === "high" ? "high" : "low"} />
      )}
      {dupCount > 0 && (
        <Pill tone="warn" size="sm" style={{ fontFamily: "var(--font-mono)", fontSize: 10, textTransform: "uppercase" }}>
          DUP +{dupCount}
        </Pill>
      )}
    </div>

    {/* Category title */}
    <div style={{
      fontSize: 14,
      fontWeight: 600,
      fontFamily: "var(--font-sans)",
      color: "var(--ink)",
      marginBottom: 16,
    }}>
      {categoryLabel}
    </div>

    {/* Telemetry block — 2-column grid (lines 311-347 verbatim) */}
    <Card padded={false} style={{ marginBottom: 0, overflow: "hidden" }}>
      {/* ... telemetry grid unchanged ... */}
    </Card>
  </>
);
```

**Action rail content variable** (content that stays in right column, D-05):
```tsx
// New named JSX variable; content from rightPanel lines 281-452 minus the identity strip items
const actionRailContent = (
  <div style={{ padding: isDesktop ? "0" : "16px" }}>
    <StatusActionPanel ... />
    {statusUpdateError && <p role="alert" ...>{statusUpdateError}</p>}
    <div id="org-assign-panel"><OrgAssignPanel ... /></div>
    <GbaHierarchyPanel ... />
    {/* Citizen description card */}
    {/* Map thumbnail */}
    {/* Status history card — with updated dot color map */}
  </div>
);
```

**Right column scroll pattern** (line 539) — preserve exactly, replace `{rightPanel}` with `{actionRailContent}`:
```tsx
// Source: page.tsx line 539
<div style={{
  overflowY: "auto",
  maxHeight: "calc(100vh - 120px)",
  borderLeft: "1px solid var(--border)"
}}>
  {actionRailContent}   {/* was: {rightPanel} */}
</div>
```

**Mobile single-column path** (lines 543-578) — MUST include both variables after refactor:
```tsx
// Source: page.tsx line 576-577 — update after splitting rightPanel
{photoSection}
{identityStripContent}   {/* moved out of rightPanel */}
{actionRailContent}      {/* was: {rightPanel} */}
```

**Status dot color map fix** (ISSUE-06, lines 427-431):
```tsx
// CURRENT — broken (old Phase 2 enum values):
background:
  report.status === "submitted" ? "var(--status-submitted)" :
  report.status === "under_review" ? "var(--status-review)" :
  "var(--status-resolved)",

// AFTER — Phase 3 enum values (all 6 CSS tokens confirmed in admin.css lines 55-69):
background:
  report.status === "open"         ? "var(--status-open)" :
  report.status === "acknowledged" ? "var(--status-acknowledged)" :
  report.status === "assigned"     ? "var(--status-assigned)" :
  report.status === "in_progress"  ? "var(--status-in-progress)" :
  report.status === "resolved"     ? "var(--status-resolved)" :
  report.status === "closed"       ? "var(--status-closed)" :
  "var(--status-open)",  // fallback
```

**Inline style convention** (Direction B — applies to all new JSX):
```tsx
// All CSS via inline style={{}} with var(--*) tokens from admin.css
// NO Tailwind classes, NO external CSS modules
// Example from any existing element in the file:
style={{ fontFamily: "var(--font-mono)", fontSize: 12, color: "var(--ink-2)" }}
```

---

### `frontend/app/admin/components/GbaHierarchyPanel.tsx` (component, transform)

**Analog:** self — targeted bug fixes only.

**Imports pattern** (lines 1-6) — unchanged:
```tsx
"use client";

import type { CSSProperties } from "react";
import { Card } from "./Card";
import { SectionLabel } from "./SectionLabel";
import type { WardHierarchy } from "../lib/adminApi";
```

**ChainRow type — extend for alwaysShow flag** (ISSUE-03 / D-08):
```tsx
// CURRENT (line 16-19):
interface ChainRow {
  level: string;
  name: string | null;
  title: string;
}

// AFTER — add optional flag to mark institutional rows:
interface ChainRow {
  level: string;
  name: string | null;
  title: string;
  alwaysShow?: boolean;
}
```

**bureaucraticChain array — tag institutional rows** (lines 98-105):
```tsx
// CURRENT:
const bureaucraticChain: ChainRow[] = [
  { level: "Ward",          name: wardDisplay,                   title: "Ward Engineer" },
  { level: "ARO Sub Division", name: hierarchy.aro_sub_division, title: "Asst. Revenue Officer" },
  { level: "RO Division",   name: hierarchy.ro_division,         title: "Revenue Officer" },
  { level: "Zone",          name: hierarchy.zone_name,           title: "Zonal Commissioner" },
  { level: "Corporation",   name: corpDisplay,                   title: "Chief Commissioner" },
  { level: "GBA",           name: "Greater Bengaluru Authority", title: "Chief Commissioner, GBA" },
];

// AFTER — tag rows with personnel-vs-institutional distinction:
const bureaucraticChain: ChainRow[] = [
  { level: "Ward",             name: wardDisplay,                   title: "Ward Engineer",          alwaysShow: true },
  { level: "ARO Sub Division", name: hierarchy.aro_sub_division,   title: "Asst. Revenue Officer" },
  { level: "RO Division",      name: hierarchy.ro_division,         title: "Revenue Officer" },
  { level: "Zone",             name: hierarchy.zone_name,           title: "Zonal Commissioner" },
  { level: "Corporation",      name: corpDisplay,                   title: "Chief Commissioner" },
  { level: "GBA",              name: "Greater Bengaluru Authority", title: "Chief Commissioner, GBA", alwaysShow: true },
];

// Filter: keep rows that have a name OR are explicitly institutional
const visibleChain = bureaucraticChain.filter(
  (row) => row.alwaysShow || (row.name !== null && row.name !== undefined && row.name !== "")
);
```

**ChainRowItem isFirst after filter** (lines 118-126) — derive from filtered array:
```tsx
// CURRENT (uses unfiltered array index):
{bureaucraticChain.map((row, idx) => (
  <ChainRowItem key={row.level} ... isFirst={idx === 0} />
))}

// AFTER (map the filtered array so isFirst is always the first visible row):
{visibleChain.map((row, idx) => (
  <ChainRowItem key={row.level} level={row.level} name={row.name} title={row.title} isFirst={idx === 0} />
))}
```

**wardDisplay null-guard** (lines 89-91) — existing guard for ISSUE-01; verify + add string coercion guard:
```tsx
// CURRENT (line 89):
const wardDisplay = hierarchy.ward_number != null
  ? `${hierarchy.ward_number} · ${hierarchy.ward_name}`
  : hierarchy.ward_name;

// AFTER — add string "undefined" guard for ISSUE-01 (API may return string "undefined"):
const rawWardNumber = hierarchy.ward_number;
const wardNumberValid =
  rawWardNumber != null &&
  !(typeof rawWardNumber === "string" && rawWardNumber === "undefined");
const wardDisplay = wardNumberValid
  ? `${rawWardNumber} · ${hierarchy.ward_name}`
  : hierarchy.ward_name;
```

**AC label display fix** (ISSUE-04, lines 109-111):
```tsx
// CURRENT — combined string in one DOM element:
const acLabel = hierarchy.assembly_constituency_no !== null && hierarchy.assembly_constituency
  ? `${hierarchy.assembly_constituency_no} – ${hierarchy.assembly_constituency}`
  : hierarchy.assembly_constituency ?? "—";

// Rendered as a single <div> with combined text.

// AFTER — split into two DOM elements for distinct typography (Direction B pattern):
// Keep acLabel variable but render separately:
{hierarchy.assembly_constituency_no !== null && (
  <span style={{ fontFamily: "var(--font-mono)", fontSize: 12, color: "var(--muted)", marginRight: 6 }}>
    {hierarchy.assembly_constituency_no}
  </span>
)}
<span style={{ fontSize: 13, fontWeight: 600, color: "var(--ink)" }}>
  {hierarchy.assembly_constituency ?? "—"}
</span>
```

**Disclaimer overflow fix** (ISSUE-05, lines 167-175):
```tsx
// CURRENT (no overflow protection):
<div style={{
  fontSize: 11,
  color: "var(--muted)",
  marginTop: 10,
  fontFamily: "var(--font-mono)",
}}>
  Constituency boundaries may differ from ward boundaries.
</div>

// AFTER — add wordBreak to prevent overflow:
<div style={{
  fontSize: 11,
  color: "var(--muted)",
  marginTop: 10,
  fontFamily: "var(--font-mono)",
  wordBreak: "break-word",
  overflowWrap: "break-word",
}}>
  Constituency boundaries may differ from ward boundaries.
</div>
```

---

### `frontend/app/admin/reports/[id]/__tests__/page.test.tsx` (test)

**Analog:** self — extend existing 12-test file with new assertions.

**Test file header / mock pattern** (lines 1-46) — unchanged, copy this pattern for any new describe blocks:
```tsx
import React from "react";
import { render, screen, waitFor } from "@testing-library/react";

jest.mock("next/navigation", () => ({
  useRouter: () => ({ push: jest.fn(), replace: jest.fn(), back: jest.fn() }),
  usePathname: () => "/admin/reports/test-report-id",
  useSearchParams: () => new URLSearchParams(),
}));

jest.mock("@/app/admin/lib/adminApi", () => ({
  getAdminReports: jest.fn(),
  deleteReport: jest.fn(),
  getStats: jest.fn(),
  getUsers: jest.fn(),
  login: jest.fn(),
  logout: jest.fn(),
  getMe: jest.fn(),
  getAdminReport: jest.fn(),
  updateReportStatus: jest.fn(),
  createUser: jest.fn(),
  deactivateUser: jest.fn(),
  getDuplicatesForReport: jest.fn(),
}));

import * as adminApiModule from "@/app/admin/lib/adminApi";
import ReportDetailPage from "@/app/admin/reports/[id]/page";
```

**Fixture pattern for new test blocks** — extend FULL_REPORT to include Phase 3 fields:
```tsx
// Existing FULL_REPORT (lines 49-68) uses old "submitted" status.
// Add a Phase3 fixture for new tests:
const PHASE3_REPORT = {
  ...FULL_REPORT,
  status: "open",
  ward_hierarchy: {
    ward_name: "Shivajinagar",
    ward_number: 117,
    corporation: "West",
    zone_name: "West Zone",
    ro_division: "West Division",
    aro_sub_division: "Shivajinagar Sub Division",
    assembly_constituency: "Shivajinagar",
    assembly_constituency_no: 155,
    parliamentary_constituency: "Bangalore Central",
    mla_name: "Rizwan Arshad",
    mp_name: "P.C. Mohan",
  },
};
```

**describe/it pattern** — follow existing style exactly:
```tsx
describe("Phase 3.1 — Layout: identity strip in left column", () => {
  beforeEach(() => {
    (adminApiModule.getAdminReport as jest.Mock).mockResolvedValue(PHASE3_REPORT);
  });
  afterEach(() => { jest.clearAllMocks(); });

  it("renders StatusBadge in the document after report loads", async () => {
    render(<ReportDetailPage params={{ id: "test-report-id" }} />);
    await screen.findByTestId("report-detail");
    expect(screen.getByTestId("status-badge")).toBeInTheDocument();
  });
});
```

**Status dot test approach** — query the span with aria-hidden and check inline style:
```tsx
// The dot <span> has no testid; query by aria-hidden and check computed style
// Note: JSDOM does not compute CSS var() values — assert the style string instead
it("status timeline dot has inline background referencing a Phase 3 CSS token (not old submitted/under_review)", async () => {
  render(<ReportDetailPage params={{ id: "test-report-id" }} />);
  await screen.findByTestId("report-detail");
  // Find the aria-hidden dot span
  const dots = document.querySelectorAll('[aria-hidden="true"]');
  const dot = Array.from(dots).find(
    (el) => el.getAttribute("style")?.includes("border-radius: 999px") ||
             el.getAttribute("style")?.includes("border-radius:999px")
  ) as HTMLElement | undefined;
  // Assert it does NOT reference old tokens
  expect(dot?.getAttribute("style") ?? "").not.toContain("status-submitted");
  expect(dot?.getAttribute("style") ?? "").not.toContain("status-review");
});
```

---

### `frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx` (test)

**Analog:** self — already exists (135 lines); extend with new assertions for ISSUE-01, ISSUE-03, ISSUE-04.

**Existing fixture / helper pattern** (lines 16-31) — reuse `makeHierarchy` for all new tests:
```tsx
// Source: GbaHierarchyPanel.test.tsx lines 16-31
function makeHierarchy(overrides: Partial<WardHierarchy> = {}): WardHierarchy {
  return {
    ward_name: "Shivajinagar",
    ward_number: 117,
    corporation: "West",
    zone_name: "West Zone",
    ro_division: "West Division",
    aro_sub_division: "Shivajinagar Sub Division",
    assembly_constituency: "Shivajinagar",
    assembly_constituency_no: 155,
    parliamentary_constituency: "Bangalore Central",
    mla_name: "Rizwan Arshad",
    mp_name: "P.C. Mohan",
    ...overrides,
  };
}
```

**ISSUE-01 test — ward_number null guard**:
```tsx
describe("Phase 3.1 / ISSUE-01 — wardDisplay: null ward_number", () => {
  it("renders only ward_name without 'undefined' prefix when ward_number is null", () => {
    render(<GbaHierarchyPanel hierarchy={makeHierarchy({ ward_number: null })} />);
    // Must NOT contain "undefined"
    expect(screen.queryByText(/undefined/)).toBeNull();
    // Must render ward name alone
    expect(screen.getByText("Shivajinagar")).toBeInTheDocument();
  });

  it("renders only ward_name when ward_number is the string 'undefined'", () => {
    // @ts-expect-error: intentional bad runtime value to test guard
    render(<GbaHierarchyPanel hierarchy={makeHierarchy({ ward_number: "undefined" })} />);
    expect(screen.queryByText(/undefined/)).toBeNull();
  });
});
```

**ISSUE-03 test — conditional row rendering**:
```tsx
describe("Phase 3.1 / ISSUE-03 — bureaucraticChain: null-name rows hidden", () => {
  it("does not render a row for aro_sub_division when it is null", () => {
    render(<GbaHierarchyPanel hierarchy={makeHierarchy({ aro_sub_division: null })} />);
    // The title "Asst. Revenue Officer" should not appear in DOM
    expect(screen.queryByText("Asst. Revenue Officer")).toBeNull();
  });

  it("always renders GBA row even when institutional name is static", () => {
    render(<GbaHierarchyPanel hierarchy={makeHierarchy({ aro_sub_division: null })} />);
    expect(screen.getByText("Greater Bengaluru Authority")).toBeInTheDocument();
  });

  it("always renders Ward row even when aro_sub_division is null", () => {
    render(<GbaHierarchyPanel hierarchy={makeHierarchy({ aro_sub_division: null })} />);
    expect(screen.getByText(/Shivajinagar/)).toBeInTheDocument();
  });
});
```

**ISSUE-04 test — AC label rendered as separate elements**:
```tsx
describe("Phase 3.1 / ISSUE-04 — AC label display", () => {
  it("renders AC number and constituency name as separate queryable text nodes", () => {
    render(<GbaHierarchyPanel hierarchy={makeHierarchy()} />);
    // AC number as separate element (not combined string)
    expect(screen.getByText("155")).toBeInTheDocument();
    // Constituency name as separate element
    expect(screen.getByText("Shivajinagar")).toBeInTheDocument();
  });
});
```

**describe block style** — follow existing file pattern (describe with requirement ID prefix, then it-strings in plain English).

---

## Shared Patterns

### Inline Style with CSS Custom Properties
**Source:** `frontend/app/admin/reports/[id]/page.tsx` (all inline styles) and `frontend/app/admin/admin.css` (token definitions)
**Apply to:** All new JSX added in this phase

Project rule (from CLAUDE.md): no Tailwind. All CSS goes through inline `style={{}}` objects referencing `var(--*)` tokens defined in `admin.css`.

```tsx
// Pattern — always this form:
style={{ fontFamily: "var(--font-mono)", fontSize: 12, color: "var(--muted)" }}

// Token reference for this phase:
// Layout: --border, --surface-2, --surface-3, --r-lg, --r-md
// Text:   --ink, --ink-2, --muted, --font-mono, --font-sans
// Status: --status-open, --status-acknowledged, --status-assigned,
//         --status-in-progress, --status-resolved, --status-closed
```

### Null-guard with `?? "—"` Fallback
**Source:** `frontend/app/admin/reports/[id]/page.tsx` lines 318-325 (telemetry block)
**Apply to:** Any new display of nullable data fields

```tsx
// Pattern — telemetry values (lines 318-325):
{ key: "WARD", value: report.ward_name ?? "—" },
{ key: "LOCATION_SRC", value: (report.location_source ?? "—").toUpperCase() },
{ key: "DUP_CONF", value: report.duplicate_confidence?.toUpperCase() ?? "—" },
```

### Card Component Wrapper
**Source:** `frontend/app/admin/reports/[id]/page.tsx` lines 311, 361, 375, 416; `GbaHierarchyPanel.tsx` line 114
**Apply to:** Any new content section that needs a bordered surface container

```tsx
// Pattern — padded card:
<Card style={{ marginBottom: 16 }}>
  <SectionLabel style={{ marginBottom: 8 }}>SECTION_TITLE</SectionLabel>
  {/* content */}
</Card>

// Pattern — non-padded card with internal grid:
<Card padded={false} style={{ marginBottom: 16, overflow: "hidden" }}>
  {/* ... */}
</Card>
```

### SectionLabel for Section Headers
**Source:** `frontend/app/admin/reports/[id]/page.tsx` lines 335, 362, 417; `GbaHierarchyPanel.tsx` lines 116, 130
**Apply to:** Every new content section header

```tsx
// Pattern — always used with marginBottom:
<SectionLabel style={{ marginBottom: 8 }}>SECTION_KEY</SectionLabel>
// or
<SectionLabel style={{ marginBottom: 12 }}>Section Title</SectionLabel>
```

### matchMedia isDesktop Pattern
**Source:** `frontend/app/admin/reports/[id]/page.tsx` lines 66-74
**Apply to:** Any new responsive branching logic

```tsx
// Full pattern (lines 66-74) — SSR-safe, event-driven, cleanup included:
useEffect(() => {
  if (typeof window !== "undefined" && typeof window.matchMedia === "function") {
    const mq = window.matchMedia("(min-width: 768px)");  // 768px after D-04 fix
    setIsDesktop(mq.matches);
    const handler = (e: MediaQueryListEvent) => setIsDesktop(e.matches);
    mq.addEventListener("change", handler);
    return () => mq.removeEventListener("change", handler);
  }
}, []);
```

### Jest / RTL Test Structure
**Source:** `frontend/app/admin/reports/[id]/__tests__/page.test.tsx` and `frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx`
**Apply to:** All new test describe blocks in this phase

```tsx
// Pattern — mock at top, import AFTER mocks, fixtures, describe+it:
describe("Phase 3.1 / ISSUE-XX — ComponentName: behavior description", () => {
  beforeEach(() => {
    (apiMock as jest.Mock).mockResolvedValue(FIXTURE);
  });
  afterEach(() => { jest.clearAllMocks(); });

  it("describes one testable assertion in plain English", async () => {
    render(<Component props={...} />);
    await screen.findByTestId("root-testid");   // or just render() for pure components
    expect(screen.getByText("expected text")).toBeInTheDocument();
  });
});
```

---

## No Analog Found

None. All files modified in this phase already exist and have been fully read. The two test files already exist; they are extended, not created from scratch.

---

## Metadata

**Analog search scope:** `frontend/app/admin/reports/[id]/`, `frontend/app/admin/components/`, `frontend/app/admin/components/__tests__/`, `frontend/app/admin/lib/`, `frontend/app/admin/admin.css`
**Files scanned:** 8 (page.tsx, GbaHierarchyPanel.tsx, page.test.tsx, GbaHierarchyPanel.test.tsx, StatusBadge.test.tsx, adminApi.ts, admin.css, CLAUDE.md)
**Pattern extraction date:** 2026-05-26
