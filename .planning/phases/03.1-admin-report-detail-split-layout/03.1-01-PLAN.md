---
phase: 03.1-admin-report-detail-split-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/admin/reports/[id]/page.tsx
  - frontend/app/admin/components/GbaHierarchyPanel.tsx
  - frontend/app/admin/reports/[id]/__tests__/page.test.tsx
  - frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx
autonomous: false
requirements:
  - F-07
tags:
  - frontend
  - admin
  - layout
  - bugfix

must_haves:
  truths:
    - "On desktop (>=768px), /admin/reports/[id] renders two columns: left column (photo + identity strip) fixed-height non-scrolling; right column (action panels) scrolls independently within calc(100vh - 120px)"
    - "All action panels (StatusActionPanel, OrgAssignPanel, GbaHierarchyPanel, citizen description, map thumbnail, status history) are reachable from the right column without the page body scrolling (F-07 fix per D-01..D-06, D-09)"
    - "On mobile (<768px), the single-column layout renders photo, then identity strip content, then action rail content in order — no functionality removed"
    - "GbaHierarchyPanel Ward row never renders the literal string 'undefined' regardless of whether ward_number is JS null, undefined, or the JSON string 'undefined' (ISSUE-01 fix per D-07)"
    - "GbaHierarchyPanel hides bureaucratic chain rows where name is null AND the row is not flagged alwaysShow=true; Ward and GBA rows always render (ISSUE-03 fix per D-08)"
    - "GbaHierarchyPanel elected section renders AC number and constituency name as two separate DOM elements with distinct typography (ISSUE-04 fix per D-07 + Discretion)"
    - "GbaHierarchyPanel disclaimer wrapper has wordBreak/overflowWrap CSS so long text wraps within the panel (ISSUE-05 fix per D-07 + Discretion)"
    - "Status history timeline dot color map references Phase 3 enum values (open/acknowledged/assigned/in_progress/resolved/closed) via var(--status-*) tokens — no references to old 'submitted'/'under_review'/'status-submitted'/'status-review' remain (ISSUE-06 fix per Discretion)"
    - "isDesktop matchMedia query string is '(min-width: 768px)' — not 1024px (D-04)"
    - "npm run build passes with zero TypeScript errors; existing 12 page.test.tsx tests and existing 9 GbaHierarchyPanel.test.tsx tests all remain green"
  artifacts:
    - path: "frontend/app/admin/reports/[id]/page.tsx"
      provides: "Restructured desktop layout (flex-column left with photo+identity strip; right column with action rail) + Phase 3 status dot color map"
      contains: "min-width: 768px"
    - path: "frontend/app/admin/components/GbaHierarchyPanel.tsx"
      provides: "Conditional chain row filtering, string-undefined ward_number guard, split AC label, disclaimer word-break"
      contains: "alwaysShow"
    - path: "frontend/app/admin/reports/[id]/__tests__/page.test.tsx"
      provides: "Phase 3.1 layout + status-dot regression tests"
      contains: "Phase 3.1"
    - path: "frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx"
      provides: "ISSUE-01, ISSUE-03, ISSUE-04 regression tests; existing combined-AC-label assertion updated to match new split rendering"
      contains: "ISSUE-01"
  key_links:
    - from: "frontend/app/admin/reports/[id]/page.tsx"
      to: "frontend/app/admin/admin.css"
      via: "var(--status-open|acknowledged|assigned|in-progress|resolved|closed) CSS tokens"
      pattern: "var\\(--status-(open|acknowledged|assigned|in-progress|resolved|closed)\\)"
    - from: "frontend/app/admin/reports/[id]/page.tsx"
      to: "isDesktop matchMedia branch"
      via: "window.matchMedia('(min-width: 768px)')"
      pattern: "min-width:\\s*768px"
    - from: "frontend/app/admin/components/GbaHierarchyPanel.tsx"
      to: "visibleChain.filter"
      via: "alwaysShow flag on Ward and GBA rows"
      pattern: "alwaysShow"
---

<objective>
Restructure `/admin/reports/[id]` into the Figma-inspect / VS-Code-sidebar Option 4 split layout (left = photo + identity strip; right = action rail with independent scroll) and fix all six GbaHierarchyPanel / status-timeline display bugs surfaced in Phase 03 UAT.

Purpose: Resolve F-07 / ISSUE-07 (below-fold discoverability) by moving status badges + telemetry into a non-scrolling identity strip under the photo, freeing the right column to contain only action panels that scroll within the viewport. Simultaneously close ISSUE-01..06 since they all live in the same two files — single coordinated pass per CONTEXT.md D-07 (no separate micro-phase).

Output: Two source files restructured + two test files extended with regression assertions; full Jest suite green; `npm run build` clean.
</objective>

<execution_context>
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/workflows/execute-plan.md
@/home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-admin-report-detail-split-layout/03.1-CONTEXT.md
@.planning/phases/03.1-admin-report-detail-split-layout/03.1-RESEARCH.md
@.planning/phases/03.1-admin-report-detail-split-layout/03.1-PATTERNS.md
@.planning/phases/03-government-triage-workflow/03-UAT-ISSUES.md
@CLAUDE.md

# Source files being modified
@frontend/app/admin/reports/[id]/page.tsx
@frontend/app/admin/components/GbaHierarchyPanel.tsx
@frontend/app/admin/reports/[id]/__tests__/page.test.tsx
@frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx

# Design / token reference
@frontend/app/admin/admin.css
@frontend/app/admin/lib/adminApi.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Jest test suites with Phase 3.1 RED assertions (layout + 6 bug fixes)</name>
  <files>
    frontend/app/admin/reports/[id]/__tests__/page.test.tsx,
    frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx
  </files>
  <read_first>
    - frontend/app/admin/reports/[id]/__tests__/page.test.tsx (existing 12-test fixture FULL_REPORT + mock pattern at lines 1-46)
    - frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx (existing 9 tests + makeHierarchy helper at lines 16-31; existing assertion `screen.getByText(/155 – Shivajinagar/)` at line 99 MUST be replaced because ISSUE-04 fix splits this into two DOM nodes)
    - frontend/app/admin/reports/[id]/page.tsx (current rightPanel structure lines 265-454 — to understand what JSX must move into left column)
    - frontend/app/admin/components/GbaHierarchyPanel.tsx (current bureaucraticChain array lines 98-105, wardDisplay lines 89-91, acLabel lines 109-111, disclaimer wrapper lines 167-175)
    - .planning/phases/03.1-admin-report-detail-split-layout/03.1-RESEARCH.md (Phase Requirements -> Test Map section; Wave 0 Gaps section)
    - .planning/phases/03.1-admin-report-detail-split-layout/03.1-PATTERNS.md (test extension patterns lines 354-525)
  </read_first>
  <behavior>
    Extend the two existing test files with the following NEW describe blocks. Each new test MUST fail at the start of this task (RED) — they pass only after Task 2 implements the fixes.

    === In `frontend/app/admin/reports/[id]/__tests__/page.test.tsx` ===

    A. Add a `PHASE3_REPORT` fixture extending FULL_REPORT with status="open" and a populated `ward_hierarchy` object (use the makeHierarchy default shape from GbaHierarchyPanel.test.tsx). This new fixture is for all new tests in this file. Existing tests using FULL_REPORT (status="submitted") stay unchanged.

    B. New describe block: `describe("Phase 3.1 / F-07 — Layout: identity strip and action rail")` with these tests (all use PHASE3_REPORT via getAdminReport mock + await screen.findByTestId("report-detail")):
       - "renders StatusBadge (data-testid=status-badge) somewhere in the page" — `expect(screen.getByTestId("status-badge")).toBeInTheDocument()`
       - "renders the ward_name telemetry text" — `expect(screen.getByText("Ward 27")).toBeInTheDocument()` (telemetry row still rendered; just verifies the strip content is mounted on desktop OR mobile)
       - "renders the CATEGORY telemetry value with raw category slug" — `expect(screen.getByText("broken_footpath")).toBeInTheDocument()` (regression guard that telemetry survives the move)

    C. New describe block: `describe("Phase 3.1 / ISSUE-06 — Status timeline dot uses Phase 3 enum tokens")`:
       - "status dot does NOT reference legacy --status-submitted or --status-review tokens" — query all `aria-hidden="true"` spans, find the one with `border-radius: 999px`, assert `style.background` does NOT contain `"status-submitted"` and does NOT contain `"status-review"`.
       - "status dot uses --status-open token when report.status is 'open'" — same query, assert the style string contains `"status-open"`.

    === In `frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx` ===

    D. Add new describe block: `describe("Phase 3.1 / ISSUE-01 — wardDisplay null and string-undefined guard")`:
       - "renders only ward_name without 'undefined' prefix when ward_number is null" — render with `makeHierarchy({ ward_number: null })`; assert `screen.queryByText(/undefined/)` is `null`; assert `screen.getByText("Shivajinagar")` is in document.
       - "renders only ward_name when ward_number arrives as the string 'undefined'" — render with `makeHierarchy({ ward_number: "undefined" as unknown as number })` (cast to bypass TS); assert `screen.queryByText(/undefined/)` is `null`.

    E. Add new describe block: `describe("Phase 3.1 / ISSUE-03 — Bureaucratic chain hides null personnel rows")`:
       - "does not render the 'Asst. Revenue Officer' title when aro_sub_division is null" — render with `makeHierarchy({ aro_sub_division: null })`; assert `screen.queryByText("Asst. Revenue Officer")` is `null`.
       - "does not render the 'Revenue Officer' title when ro_division is null" — same pattern.
       - "does not render the 'Zonal Commissioner' title when zone_name is null" — same.
       - "does not render the 'Chief Commissioner' title when corporation is null" — render with `makeHierarchy({ corporation: null })`; assert `screen.queryByText(/Chief Commissioner$/)` is `null` (use `$` anchor so the GBA row's "Chief Commissioner, GBA" is NOT matched).
       - "always renders the Ward row even when other rows are null" — render with `makeHierarchy({ aro_sub_division: null, ro_division: null, zone_name: null, corporation: null })`; assert `screen.getByText("Ward Engineer")` is in document and `screen.getByText(/Shivajinagar/)` is in document.
       - "always renders the GBA row even when other rows are null" — same fixture; assert `screen.getByText("Greater Bengaluru Authority")` is in document.

    F. Add new describe block: `describe("Phase 3.1 / ISSUE-04 — AC number and constituency name render as separate elements")`:
       - "renders the AC number as its own text node" — render with default `makeHierarchy()`; assert `screen.getByText("155")` is in document (NOT the combined "155 – Shivajinagar" form).
       - "renders the assembly constituency name as its own text node" — same fixture; assert `screen.getByText("Shivajinagar")` is in document (assembly-constituency value; same string as ward_name in the default fixture, so use `getAllByText` if necessary — see implementation note below).

    G. UPDATE the existing test at line 99 (`"renders MLA name and assembly constituency when present"`): the assertion `expect(screen.getByText(/155 – Shivajinagar/)).toBeInTheDocument()` MUST be replaced with two assertions matching the new split rendering: `expect(screen.getByText("155")).toBeInTheDocument()` AND `expect(screen.getAllByText("Shivajinagar").length).toBeGreaterThanOrEqual(1)`. Leave the `expect(screen.getByText("Rizwan Arshad"))` assertion unchanged.

    Implementation notes for the executor:
    - In test (F), `"Shivajinagar"` may match BOTH the Ward row name (ward_name = "Shivajinagar") and the assembly_constituency name (also "Shivajinagar" in the default fixture). Use `screen.getAllByText("Shivajinagar")` and assert `.length >= 2`. Alternatively, change the default `assembly_constituency` to a distinct value like "Sampangirama Nagar" inside this test using overrides. Pick whichever keeps the test readable.
    - Use the existing test file mock setup, fixtures, and describe-style verbatim — DO NOT introduce a new mocking framework or fixture file.
    - All new assertions use only `@testing-library/react` APIs already imported in each file.

    After this task: `npm test -- --testPathPattern="admin/reports/\[id\]|GbaHierarchyPanel"` MUST show the new tests failing (RED). Existing tests remain green except the explicitly updated assertion in (G), which now also fails until Task 2 splits the rendering.
  </behavior>
  <action>
    Open `frontend/app/admin/reports/[id]/__tests__/page.test.tsx`. After the existing FULL_REPORT constant (line 68), add a new `PHASE3_REPORT` constant spreading FULL_REPORT and overriding `status: "open"`, plus a `ward_hierarchy` field matching the default `makeHierarchy()` shape used in GbaHierarchyPanel.test.tsx (ward_name "Shivajinagar", ward_number 117, corporation "West", zone_name "West Zone", ro_division "West Division", aro_sub_division "Shivajinagar Sub Division", assembly_constituency "Shivajinagar", assembly_constituency_no 155, parliamentary_constituency "Bangalore Central", mla_name "Rizwan Arshad", mp_name "P.C. Mohan").

    Append two new describe blocks at the bottom of the file (after the last existing describe): the F-07 layout describe (B) and the ISSUE-06 status-dot describe (C) as specified in <behavior>.

    For the status-dot test (C), follow the query approach from 03.1-PATTERNS.md line 426-442: query `document.querySelectorAll('[aria-hidden="true"]')`, filter to the one whose `getAttribute("style")` includes `"border-radius: 999px"` OR `"border-radius:999px"` (both Chromium/JSDOM serialization styles), then assert the style string membership of `"status-submitted"`/`"status-review"`/`"status-open"`.

    Open `frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx`. Update the existing test at line 99 per <behavior> point (G). Append four new describe blocks (D, E, F) at the bottom of the file using the existing `makeHierarchy` helper. The describe-block style must match the existing pattern: `describe("Phase 3.1 / ISSUE-XX — short summary", ...)` with `it("plain English assertion", () => { render(...); expect(...) })`.

    Run the test suite once to verify the new tests fail and existing tests (except the updated assertion in G) still pass. Do NOT proceed to Task 2 until the RED state is confirmed.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern="admin/reports/\[id\]|GbaHierarchyPanel" --passWithNoTests 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `frontend/app/admin/reports/[id]/__tests__/page.test.tsx` contains the string `Phase 3.1 / F-07` and the string `Phase 3.1 / ISSUE-06` (new describe block names).
    - Source assertion: `frontend/app/admin/reports/[id]/__tests__/page.test.tsx` contains the constant identifier `PHASE3_REPORT` and the property `ward_hierarchy:` initialised with `ward_number: 117`.
    - Source assertion: `frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx` contains the strings `Phase 3.1 / ISSUE-01`, `Phase 3.1 / ISSUE-03`, and `Phase 3.1 / ISSUE-04` (new describe block names).
    - Source assertion: the literal regex `/155 – Shivajinagar/` no longer appears in `GbaHierarchyPanel.test.tsx` (the existing assertion was replaced).
    - Test-command behaviour: running `npm test -- --testPathPattern="admin/reports/\[id\]|GbaHierarchyPanel" --passWithNoTests` from `frontend/` reports at least 6 failing tests across the two files (RED state confirms tests are exercising not-yet-implemented behaviour). Exit code is non-zero. The pre-existing tests (other than the updated one in G) remain in the "passed" set.
    - Grep gate: `grep -v '^#' frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx | grep -c 'Phase 3.1'` is >= 3.
  </acceptance_criteria>
  <done>
    Both test files contain new Phase 3.1 describe blocks covering layout, dot-color map update, ISSUE-01, ISSUE-03, ISSUE-04 regressions. New tests fail (RED). Pre-existing tests still pass (except the one assertion intentionally updated in point G of <behavior>). The combined "155 – Shivajinagar" assertion is removed and replaced with the two-element form. No production source files have been touched in this task.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement split layout in page.tsx + bug fixes in GbaHierarchyPanel.tsx (GREEN)</name>
  <files>
    frontend/app/admin/reports/[id]/page.tsx,
    frontend/app/admin/components/GbaHierarchyPanel.tsx
  </files>
  <read_first>
    - frontend/app/admin/reports/[id]/page.tsx (full file — restructure depends on knowing exact JSX of rightPanel lines 264-454, photoSection lines 200-262, desktop layout lines 510-541, mobile layout lines 543-578, and status timeline lines 415-452)
    - frontend/app/admin/components/GbaHierarchyPanel.tsx (full file — 182 lines, target of all ISSUE-01/03/04/05 fixes)
    - frontend/app/admin/admin.css (CSS tokens — confirm presence of --status-open, --status-acknowledged, --status-assigned, --status-in-progress, --status-resolved, --status-closed, --border, --surface-2, --r-lg, --r-md)
    - frontend/app/admin/lib/adminApi.ts (WardHierarchy interface — ward_number typed as `number | null`)
    - .planning/phases/03.1-admin-report-detail-split-layout/03.1-CONTEXT.md (D-01..D-09 locked decisions; Claude's Discretion section)
    - .planning/phases/03.1-admin-report-detail-split-layout/03.1-PATTERNS.md (lines 24-350: pattern assignments for both files with current vs. AFTER code excerpts)
    - .planning/phases/03.1-admin-report-detail-split-layout/03.1-RESEARCH.md (Common Pitfalls section — especially Pitfall 1 mobile path regression, Pitfall 2 isFirst recomputation, Pitfall 5 rightPanel mobile reference)
    - frontend/app/admin/reports/[id]/__tests__/page.test.tsx (Task 1's new tests — implementation must make them pass)
    - frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx (Task 1's new tests — implementation must make them pass)
  </read_first>
  <behavior>
    After this task, ALL tests added in Task 1 plus all pre-existing tests in both test files pass (GREEN). The roadmap success criteria for Phase 3.1 are satisfied: desktop split layout at >=768px, mobile unchanged, no functionality removed, build passes.

    Concrete behaviours per CONTEXT.md decisions (cite the decision ID in the change):

    1. **D-04 breakpoint:** Change the matchMedia string at page.tsx line 68 from `"(min-width: 1024px)"` to `"(min-width: 768px)"`. The surrounding useEffect, addEventListener, cleanup are NOT touched.

    2. **D-01, D-02, D-03 left column — flex column with photo hero + identity strip:** Replace the current bare left-panel div at page.tsx line 513 (`<div style={{ background: "var(--surface-2)", borderRadius: "var(--r-lg)", overflow: "hidden" }}>`) with a flex-column container:
       - Outer wrapper: `display: flex; flex-direction: column; height: calc(100vh - 120px); overflow: hidden; background: var(--surface-2); border-radius: var(--r-lg)`.
       - Inner photo region: `flex: 1; min-height: 200px; overflow: hidden`. Contains the existing `<img>` element OR the existing "NO PHOTO" placeholder — JSX content from current lines 514-535 is moved into this region UNCHANGED.
       - Inner identity strip region: `flex-shrink: 0; padding: 12px 16px; border-top: 1px solid var(--border)`. Contains the new `identityStripContent` JSX variable (see point 3).

    3. **D-02, D-05 split rightPanel into two named JSX variables:** Refactor the current `rightPanel` JSX variable (page.tsx lines 264-454) into two NEW named JSX `const` variables defined in the same function body (preferred per RESEARCH.md Open Questions #3 — keep inline, do NOT extract subcomponent files):
       - `identityStripContent` — contains the status badges row (StatusBadge, SeverityIndicator, ConfidencePill, DUP+N Pill) + category title + telemetry Card. This is what was at lines 268-347 of the current rightPanel, minus the wrapping `<div style={{ padding: ... }}>`.
       - `actionRailContent` — contains StatusActionPanel, the statusUpdateError <p>, OrgAssignPanel (id="org-assign-panel"), GbaHierarchyPanel, citizen description Card, map placeholder Card, status history Card. This is what was at lines 281-291 (StatusActionPanel) + 293-297 (error) + 349-453 (everything else) of the current rightPanel. Wrap in `<div style={{ padding: isDesktop ? "0" : "16px" }}>` so mobile padding is preserved.
       - The original single `rightPanel` const is REMOVED. Any later JSX reference to `{rightPanel}` must be replaced with `{identityStripContent}` or `{actionRailContent}` as appropriate.

    4. **D-05, D-06 right column — independent scroll:** At page.tsx line 539 (`<div style={{ overflowY: "auto", maxHeight: "calc(100vh - 120px)", borderLeft: "1px solid var(--border)" }}>`), the wrapping div and its styles are PRESERVED EXACTLY. Inside, replace `{rightPanel}` with `{actionRailContent}` (the new variable from point 3).

    5. **D-02 identity strip placement on desktop:** Inside the new flex-column left wrapper (point 2), the identity strip `<div style={{ flexShrink: 0, padding: "12px 16px", borderTop: "1px solid var(--border)" }}>` renders `{identityStripContent}`.

    6. **Mobile path (page.tsx line 576-577):** Current mobile renders `{photoSection}` then `{rightPanel}`. After the refactor, mobile MUST render `{photoSection}` then `{identityStripContent}` then `{actionRailContent}` so that mobile users see status badges + telemetry + all action panels. This addresses Pitfall 1 (mobile path regression) in RESEARCH.md.

    7. **ISSUE-06 status dot color map fix (page.tsx lines 427-431):** Replace the current ternary that references `report.status === "submitted"` / `report.status === "under_review"` and tokens `var(--status-submitted)` / `var(--status-review)` with a Phase 3 ternary chain covering all six values: open -> `var(--status-open)`, acknowledged -> `var(--status-acknowledged)`, assigned -> `var(--status-assigned)`, in_progress -> `var(--status-in-progress)`, resolved -> `var(--status-resolved)`, closed -> `var(--status-closed)`, fallback -> `var(--status-open)`. The surrounding aria-hidden span and its other inline styles (width 8, height 8, borderRadius 999, flexShrink 0, marginTop 2) are UNCHANGED.

    8. **ISSUE-01 ward_number guard (GbaHierarchyPanel.tsx lines 88-91):** Replace the current `wardDisplay` const with a guard that treats both JS `null`/`undefined` AND the JSON string `"undefined"` as missing. Suggested form:
       - `const rawWardNumber = hierarchy.ward_number;`
       - `const wardNumberValid = rawWardNumber != null && !(typeof rawWardNumber === "string" && rawWardNumber === "undefined");`
       - `const wardDisplay = wardNumberValid ? \`${rawWardNumber} · ${hierarchy.ward_name}\` : hierarchy.ward_name;`

    9. **ISSUE-03 conditional row rendering (GbaHierarchyPanel.tsx lines 16-19, 98-105, 117-126):**
       - Extend the `ChainRow` interface (line 16-19) with optional `alwaysShow?: boolean`.
       - In the `bureaucraticChain` array literal, tag the Ward row and the GBA row with `alwaysShow: true`. Leave ARO Sub Division, RO Division, Zone, and Corporation rows without the flag.
       - After the array literal, define `const visibleChain = bureaucraticChain.filter(row => row.alwaysShow || (row.name !== null && row.name !== undefined && row.name !== ""))`.
       - In the JSX, `bureaucraticChain.map(...)` becomes `visibleChain.map((row, idx) => <ChainRowItem ... isFirst={idx === 0} ... />)`. The `isFirst` derives from the FILTERED array's index (addresses Pitfall 2).

    10. **ISSUE-04 AC label split (GbaHierarchyPanel.tsx lines 109-141):** Keep the `acLabel` const string for backward compatibility if useful, but in the JSX `<div>` at line 140 (which currently renders `{acLabel}`), render the AC number and the constituency name as TWO separate sibling `<span>` elements:
        - First `<span>` (only rendered when `hierarchy.assembly_constituency_no !== null`): inline style `fontFamily: "var(--font-mono)", fontSize: 12, color: "var(--muted)", marginRight: 6`. Text content: `{hierarchy.assembly_constituency_no}` (raw number, no string template).
        - Second `<span>` (always rendered inside the wrapper, even if assembly_constituency is null — falls back to `"—"`): inline style `fontSize: 13, fontWeight: 600, color: "var(--ink)"`. Text content: `{hierarchy.assembly_constituency ?? "—"}`.
        - Both spans sit inside the existing `<div style={{ fontSize: 13, fontWeight: 600, color: "var(--ink)" }}>` at line 140; remove the inline text content `{acLabel}` from that div but keep the wrapper (or remove its styles since the inner spans now own typography — your call provided the test assertions pass).

    11. **ISSUE-05 disclaimer overflow (GbaHierarchyPanel.tsx lines 167-176):** Extend the disclaimer wrapper div's inline `style` object to include `wordBreak: "break-word"` and `overflowWrap: "break-word"`. The text content and other styles are unchanged.

    12. **Direction-B CSS rule (CLAUDE.md, 03-UI-SPEC.md):** All new style on new JSX is inline `style={{}}` with `var(--*)` tokens — no Tailwind classes, no className-driven CSS, no new CSS files.

    Anti-patterns to avoid:
    - Do NOT remove or rename any existing `data-testid` attribute (status-badge, report-detail, etc.) — the existing tests rely on them.
    - Do NOT change the desktop grid `gridTemplateColumns: "1.3fr 1fr"` at line 511 — only the content of the columns changes.
    - Do NOT extract `identityStripContent` or `actionRailContent` into separate component files; keep them as inline JSX `const` variables (Open Question #3 resolution).
    - Do NOT skip the GBA row when `name` is null — it has a hardcoded `name: "Greater Bengaluru Authority"` so its `name !== null` check is true regardless; explicit `alwaysShow: true` is added for defensiveness and documentation.
    - Do NOT touch backend files, the API, or admin.css — all tokens already exist.
  </behavior>
  <action>
    Open `frontend/app/admin/reports/[id]/page.tsx`. Change line 68's matchMedia string from `(min-width: 1024px)` to `(min-width: 768px)` per D-04. Inside the function body, refactor the single `const rightPanel = (...)` JSX variable at line 265 into two new constants `identityStripContent` and `actionRailContent` per <behavior> point 3 — splitting the JSX so the status badges row + category title + telemetry Card end up in identityStripContent, and StatusActionPanel through status history Card end up in actionRailContent (wrapped in `<div style={{ padding: isDesktop ? "0" : "16px" }}>`). Delete the original `const rightPanel`.

    Restructure the desktop layout starting at line 510. The outer grid (`gridTemplateColumns: "1.3fr 1fr"`) is preserved. Replace the current left-panel div (line 513) with the flex-column container described in <behavior> point 2 (height calc(100vh - 120px), overflow hidden, flex direction column), containing the photo region (flex 1, min-height 200, overflow hidden — holding the existing img/no-photo JSX from lines 514-535) and the identity strip region (flex-shrink 0, padding 12px 16px, border-top var(--border) — holding `{identityStripContent}`). Replace `{rightPanel}` at line 540 with `{actionRailContent}`.

    Update the mobile path at line 576-577: render `{photoSection}` then `{identityStripContent}` then `{actionRailContent}` (this preserves mobile feature parity per Pitfall 1).

    Update the status timeline dot color map (lines 427-431) per <behavior> point 7 — six-way ternary mapping report.status to var(--status-*) tokens, with `var(--status-open)` as the fallback.

    Open `frontend/app/admin/components/GbaHierarchyPanel.tsx`. Apply <behavior> point 8 to the wardDisplay derivation (add the string-"undefined" guard). Apply point 9 to the ChainRow interface, bureaucraticChain array (add alwaysShow: true on Ward and GBA rows), and the JSX map (introduce visibleChain filter; map filtered array; isFirst from filtered index). Apply point 10 to the assembly constituency section — replace the single `{acLabel}` rendering with two sibling `<span>` elements as specified. Apply point 11 to the disclaimer wrapper — add wordBreak and overflowWrap to its inline style.

    Run the targeted test suite to confirm GREEN: `cd frontend && npm test -- --testPathPattern="admin/reports/\[id\]|GbaHierarchyPanel" --passWithNoTests`. Then run the full suite + build in Task 3.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern="admin/reports/\[id\]|GbaHierarchyPanel" --passWithNoTests 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `frontend/app/admin/reports/[id]/page.tsx` contains the literal string `min-width: 768px` and does NOT contain the literal string `min-width: 1024px`.
    - Source assertion: `frontend/app/admin/reports/[id]/page.tsx` contains the identifiers `identityStripContent` and `actionRailContent` (new const variables) and does NOT contain a `const rightPanel =` declaration.
    - Source assertion: `frontend/app/admin/reports/[id]/page.tsx` contains the substring `var(--status-open)` AND `var(--status-acknowledged)` AND `var(--status-assigned)` AND `var(--status-in-progress)` AND `var(--status-resolved)` AND `var(--status-closed)`, AND does NOT contain `var(--status-submitted)` AND does NOT contain `var(--status-review)`.
    - Source assertion: `frontend/app/admin/reports/[id]/page.tsx` contains the substring `flex-direction: column` AND `height: "calc(100vh - 120px)"` (or `height:'calc(100vh - 120px)'`) inside the left-column wrapper that immediately precedes the right-column scroll wrapper.
    - Source assertion: `frontend/app/admin/components/GbaHierarchyPanel.tsx` contains the identifier `alwaysShow` AND the identifier `visibleChain` AND the substring `wordBreak` AND the substring `overflowWrap`.
    - Source assertion: `frontend/app/admin/components/GbaHierarchyPanel.tsx` `bureaucraticChain` array literal has `alwaysShow: true` on the Ward row and the GBA row, and does NOT have it on the ARO Sub Division, RO Division, Zone, or Corporation rows.
    - Behaviour assertion: the mobile-path JSX block (the `else` branch of the `isDesktop` ternary in page.tsx around lines 543-578) contains `{identityStripContent}` AND `{actionRailContent}` (both variable references) AND does NOT contain a bare `{rightPanel}` reference.
    - Test-command behaviour: `cd frontend && npm test -- --testPathPattern="admin/reports/\[id\]|GbaHierarchyPanel" --passWithNoTests` exits with code 0 and reports 0 failing tests across both files (all Phase 3.1 tests from Task 1 PLUS all pre-existing tests pass).
    - Grep gate: `grep -v '^#' frontend/app/admin/reports/\[id\]/page.tsx | grep -cE 'var\(--status-(open|acknowledged|assigned|in-progress|resolved|closed)\)'` is >= 6.
    - Grep gate: `grep -v '^#' frontend/app/admin/reports/\[id\]/page.tsx | grep -c 'min-width: 1024px'` is 0.
  </acceptance_criteria>
  <done>
    Both source files implement all 11 behaviour points. `npm test -- --testPathPattern="admin/reports/\[id\]|GbaHierarchyPanel"` is green. The desktop view at >=768px shows a fixed-height left column with the photo on top and the identity strip pinned beneath (no scroll), a right column with `overflow-y: auto` containing the action panels; the mobile view shows a single column with photo, identity strip, action rail in order. No Tailwind classes were introduced. Backend, API, admin.css, ResolveModal, and any other file outside the four declared `files` were NOT touched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full test suite + production build + human verification of ISSUE-05 and visual regressions</name>
  <files>
    (no file edits — verification only)
  </files>
  <read_first>
    - .planning/phases/03.1-admin-report-detail-split-layout/03.1-CONTEXT.md (success criteria for visual sanity check)
    - .planning/ROADMAP.md (Phase 3.1 Success Criteria — 4 items)
  </read_first>
  <what-built>
    Tasks 1 and 2 together delivered:
    1. Desktop split layout at /admin/reports/[id] with non-scrolling left column (photo + identity strip) and independently-scrolling right rail (action panels).
    2. 768px breakpoint replacing the previous 1024px breakpoint per D-04.
    3. Status timeline dot color map updated to Phase 3 enum values + var(--status-*) tokens.
    4. GbaHierarchyPanel ISSUE-01 fix (string-"undefined" guard on ward_number).
    5. GbaHierarchyPanel ISSUE-03 fix (conditional rendering of personnel rows with alwaysShow flag).
    6. GbaHierarchyPanel ISSUE-04 fix (AC number and constituency name as two separate DOM elements).
    7. GbaHierarchyPanel ISSUE-05 fix (wordBreak/overflowWrap on disclaimer wrapper).
    8. Test coverage extended for all of the above; full suite still green; PHASE3_REPORT fixture added; combined `155 – Shivajinagar` test assertion replaced.
  </what-built>
  <how-to-verify>
    Pre-verification automation (Claude runs these BEFORE pausing for the human):

    1. Run the FULL Jest suite from `frontend/`:
       `cd frontend && npm test -- --passWithNoTests --watchAll=false 2>&1 | tail -60`
       Expected: 0 failing tests. Both projects (middleware + jsdom) green.

    2. Run the production build:
       `cd frontend && npm run build 2>&1 | tail -40`
       Expected: Build completes with 0 TypeScript errors. New ESLint warning count must be 0.

    3. Print the verification summary to the human, then pause.

    Human verification steps (the user performs these on the running dev server):

    A. **Desktop layout** — On a screen at >=768px width, navigate to `http://localhost:3000/admin/reports/<any-id>`:
       - Confirm the page renders in TWO columns (photo on the left, action panels on the right).
       - Confirm the LEFT column does NOT scroll. The photo fills the upper portion; status badges + telemetry sit pinned at the bottom of the left column.
       - Confirm the RIGHT column has its own independent vertical scrollbar and can scroll through StatusActionPanel, OrgAssignPanel, GbaHierarchyPanel, Citizen Description, Map placeholder, and Status History.
       - Confirm the whole-page body does NOT scroll (only the right rail scrolls).
       - Resize the browser to 800px wide and confirm the split layout still renders (verifies the 768px breakpoint).
       - Resize to 760px and confirm the layout collapses to a single column (mobile path).

    B. **Mobile layout** — At <768px, confirm the single-column path renders photo, then status badges + telemetry, then all action panels in order. No content is missing.

    C. **GbaHierarchyPanel ISSUE-01** — Find a report whose `ward_hierarchy.ward_number` is null in the DB (or temporarily null it in the admin DB tool). Confirm the Ward row label shows ONLY the ward name (e.g. "Sampangirama Nagar") with no `undefined -` prefix.

    D. **GbaHierarchyPanel ISSUE-03** — On a report whose `ward_hierarchy.aro_sub_division` is null, confirm the "ARO Sub Division" row is COMPLETELY ABSENT from the bureaucratic chain (no row with `—` placeholder, no row title "Asst. Revenue Officer"). The Ward row and the GBA row remain visible.

    E. **GbaHierarchyPanel ISSUE-04** — On any report with a populated assembly constituency, confirm the AC number and constituency name are visually distinct (different typography — mono for the number, sans for the name) and NOT joined by a single em-dash string.

    F. **GbaHierarchyPanel ISSUE-05** — Inspect the elected-chain section disclaimer ("Constituency boundaries may differ from ward boundaries."). Confirm the text wraps within the panel and does NOT overflow horizontally. Resize the right column narrower if needed to force wrapping.

    G. **Status timeline ISSUE-06** — On the report detail page, scroll to the Status History card in the right rail. Confirm the colored dot to the left of the status label is present and visually distinct (a colored circle, not a black or transparent void). The status text below ("OPEN", "ACKNOWLEDGED", etc.) renders correctly.

    H. **Build hygiene** — `npm run build` output should have:
       - "Compiled successfully" (or equivalent green status).
       - Zero TypeScript errors.
       - Zero NEW ESLint warnings (compare against the pre-Task-1 baseline if needed).

    Resume signal once verified.
  </how-to-verify>
  <action>
    Automation phase (Claude performs without human interaction): from the repo root, run `cd frontend && npm test -- --passWithNoTests --watchAll=false 2>&1 | tail -60` and capture exit code; then run `cd frontend && npm run build 2>&1 | tail -40` and capture exit code. If either command exits non-zero, do NOT pause for the human — surface the failure and stop (the executor must re-open Task 2). If both exit zero, print a concise verification summary that includes: (a) full-suite test counts (passed / failed / skipped), (b) the final 5 lines of the build output, (c) a copy of the human verification steps A through H from <how-to-verify>. After printing the summary, pause and wait for the resume signal described in <resume-signal>. Do NOT edit any source files in this task — verification only.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --passWithNoTests --watchAll=false 2>&amp;1 | tail -10 &amp;&amp; cd /home/karankinariwala/KARAN/1-Projects/Active/bengaluru-walkability-public-audit/frontend &amp;&amp; npm run build 2>&amp;1 | tail -20</automated>
    <human-check>Steps A through H above performed on a running dev server.</human-check>
  </verify>
  <acceptance_criteria>
    - Test-command behaviour: `cd frontend && npm test -- --passWithNoTests --watchAll=false` exits with code 0 and "Tests: 0 failing" or equivalent green output across both jsdom and middleware projects.
    - CLI output: `cd frontend && npm run build` exits with code 0 and includes "Compiled successfully" (or Next.js 14 equivalent). Zero TypeScript errors in the build log.
    - Behaviour assertion (manual): At 1024px viewport width, the right column on /admin/reports/[id] has its own scrollbar; the page body does not scroll; the left column photo + identity strip stays fixed in place while the right rail scrolls.
    - Behaviour assertion (manual): At 760px viewport width, the layout is single-column with photo, identity strip content, and action rail content all visible in order.
    - Behaviour assertion (manual): For a report where aro_sub_division is null, the "ARO Sub Division" row is not present in the DOM (no row, not a placeholder).
    - Behaviour assertion (manual): For a report where ward_number is null, the Ward row displays only the ward name with no "undefined" prefix.
    - Behaviour assertion (manual): The disclaimer at the bottom of the elected-chain section wraps within the panel and does not overflow horizontally.
  </acceptance_criteria>
  <resume-signal>Type "approved" to confirm all 4 roadmap success criteria pass, or describe any visual / functional regressions observed.</resume-signal>
  <done>
    Full Jest suite green; `npm run build` green; human confirmed via steps A-H that the desktop split layout, mobile fallback, ISSUE-01 ward guard, ISSUE-03 conditional rows, ISSUE-04 split AC label, ISSUE-05 disclaimer wrap, and ISSUE-06 dot color map all behave as specified. All four Phase 3.1 ROADMAP success criteria satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ admin Next.js page | Admin user agent loads `/admin/reports/[id]` after passing the admin auth middleware. No new boundary introduced — the page was already auth-gated before this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Information disclosure | `GbaHierarchyPanel` conditional row rendering | accept | Hiding null personnel rows reduces, not increases, information surface. Rows are hidden via JS filter (not CSS), so screen readers and DOM inspectors agree on what is visible. No new data is rendered; existing fields whose values were already null are simply not displayed at all. |
| T-03.1-02 | Tampering | Inline JSX restructure of an authenticated page | accept | No new user input, API call, or persistence is introduced. The page remains behind the existing admin auth middleware. The restructure moves existing JSX between two already-rendered DOM regions; no new attack surface is added. |
| T-03.1-03 | Denial of service | CSS `calc(100vh - 120px)` viewport calculation | accept | Browser-evaluated CSS calc cannot be malformed by user input (the values are author-controlled literals). If the 120px offset is wrong for a given layout breakpoint, the symptom is a layout glitch, not a DoS. Verified visually in Task 3. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed in this phase (verified in RESEARCH.md "Standard Stack" section). The Package Legitimacy Audit table is not applicable; the slopcheck protocol triggers only when new third-party packages are added. |
</threat_model>

<verification>
End-of-phase verification gates (run by `/gsd-verify-work` after all three tasks):

1. **Frontend tests** — `cd frontend && npm test -- --passWithNoTests --watchAll=false` exits 0.
2. **Frontend build** — `cd frontend && npm run build` exits 0 with no TypeScript errors and no new ESLint warnings.
3. **Source assertions:**
   - `grep -v '^#' frontend/app/admin/reports/\[id\]/page.tsx | grep -c 'min-width: 1024px'` is 0.
   - `grep -v '^#' frontend/app/admin/reports/\[id\]/page.tsx | grep -c 'min-width: 768px'` is >= 1.
   - `grep -v '^#' frontend/app/admin/reports/\[id\]/page.tsx | grep -cE 'var\(--status-(open|acknowledged|assigned|in-progress|resolved|closed)\)'` is >= 6.
   - `grep -v '^#' frontend/app/admin/reports/\[id\]/page.tsx | grep -cE 'var\(--status-(submitted|review)\)'` is 0.
   - `grep -v '^#' frontend/app/admin/components/GbaHierarchyPanel.tsx | grep -c 'alwaysShow'` is >= 2 (Ward row + GBA row).
   - `grep -v '^#' frontend/app/admin/components/GbaHierarchyPanel.tsx | grep -c 'visibleChain'` is >= 2 (declaration + .map call).
   - `grep -v '^#' frontend/app/admin/components/GbaHierarchyPanel.tsx | grep -c 'wordBreak'` is >= 1.
4. **Phase 3.1 ROADMAP success criteria** confirmed visually in Task 3 checkpoint.
</verification>

<success_criteria>
Phase 3.1 succeeds when ALL of the following are true:

1. F-07 is closed: on >=768px viewports, the right-column action rail scrolls independently within `calc(100vh - 120px)`, and the left column (photo + identity strip) remains fixed — all panels are reachable without scrolling the page body.
2. ISSUE-01 is closed: GbaHierarchyPanel never renders the literal string "undefined" in the Ward row regardless of ward_number being null, undefined, or the string "undefined".
3. ISSUE-03 is closed: GbaHierarchyPanel hides bureaucratic-chain rows where the personnel name is null AND the row is not marked alwaysShow=true; Ward and GBA rows always render.
4. ISSUE-04 is closed: the assembly constituency number and constituency name render as two separate DOM elements with distinct typography.
5. ISSUE-05 is closed: the disclaimer text wraps within the panel and does not overflow horizontally.
6. ISSUE-06 is closed: the status history dot uses one of six Phase 3 `var(--status-*)` tokens; no references to legacy `--status-submitted` or `--status-review` remain in `page.tsx`.
7. Mobile layout (<768px) remains a single column showing photo, then identity strip content, then action rail content — no functionality removed.
8. `npm test` (full suite) and `npm run build` (production) both exit 0 with zero new errors or warnings.
</success_criteria>

<output>
Create `.planning/phases/03.1-admin-report-detail-split-layout/03.1-01-SUMMARY.md` after Task 3 confirms approved. The summary must include:

- What changed (two source files restructured + two test files extended)
- Decisions D-01..D-09 cited per change
- Status of each UAT ISSUE-01..07 (all should be closed)
- Test count delta (X new tests added; Y total now passing)
- Build output excerpt confirming zero errors
- Any deferred / discovered items (e.g., personnel data acquisition still deferred to Phase 4)
- Lessons learned: any pitfalls hit during implementation (especially Pitfalls 1, 2, 5 from RESEARCH.md)
</output>
