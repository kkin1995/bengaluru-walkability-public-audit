---
phase: 03.1
slug: admin-report-detail-split-layout
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-26
audited: 2026-06-01
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29.x (via `next/jest`) |
| **Config file** | `frontend/jest.config.js` |
| **Quick run command** | `cd frontend && npm test -- --testPathPattern="admin/reports"` |
| **Full suite command** | `cd frontend && npm test` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd frontend && npm test -- --testPathPattern="admin/reports"`
- **After every plan wave:** Run `cd frontend && npm test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.1-01-01 | 01 | 0 | F-07 | — | N/A | unit | `cd frontend && npm test -- --testPathPattern="admin/reports"` | ✅ | ✅ green |
| 03.1-01-02 | 01 | 1 | F-07 | — | N/A | unit | `cd frontend && npm test -- --testPathPattern="admin/reports/\\[id\\]"` | ✅ | ✅ green |
| 03.1-01-03 | 01 | 1 | F-07 | — | N/A | unit | `cd frontend && npm test -- --testPathPattern="GbaHierarchyPanel"` | ✅ | ✅ green |
| 03.1-01-04 | 01 | 2 | F-07 | — | N/A | build | `cd frontend && npm run build` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `frontend/app/admin/reports/[id]/__tests__/page.test.tsx` — extend existing 12 tests with: identity strip in left column, 768px breakpoint split, status dot Phase 3 enum values (now 24 tests: +5 Phase 3.1, +7 Phase 3.2)
- [x] `frontend/app/admin/components/__tests__/GbaHierarchyPanel.test.tsx` — null-row filtering, wardDisplay undefined guard, AC number+constituency separate display (19 tests)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Right column independently scrollable on desktop ≥768px | F-07 | Scroll behavior requires a browser | Load /admin/reports/[id] on Chromium at 1280px; scroll right column while confirming left column (photo) stays fixed |
| Mobile single-column layout unchanged at <768px | F-07 | Responsive layout requires viewport resize | DevTools → iPhone 12 viewport; verify single-column stack, photo + action panels in order |
| ISSUE-01 "undefined" ward number string gone | F-07 | Requires live report with null ward_number | Open a report in staging; verify Ward row shows ward name only, no "undefined" prefix |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** passed 2026-06-01

---

## Validation Audit 2026-06-01

| Metric | Count |
|--------|-------|
| Gaps found | 1 |
| Resolved | 1 |
| Escalated | 0 |

**Gap:** `listOrganizations` and `assignReportOrg` were added to `adminApi.ts` in Phase 3.4 but the `jest.mock("@/app/admin/lib/adminApi", ...)` in `page.test.tsx` was never updated. This caused a `TypeError` on mount that failed all 23 tests in the suite.

**Fix:** Added `listOrganizations: jest.fn()` and `assignReportOrg: jest.fn()` to the mock factory, and added `mockResolvedValue([])` for `listOrganizations` in each `beforeEach` block. 24/24 tests now pass. Pre-existing `ReportsMap.test.tsx` suite failure (unrelated Leaflet.heat mock issue) is not in scope.

**Final counts:** page.test.tsx 24/24 ✅ · GbaHierarchyPanel.test.tsx 19/19 ✅ · Build ✅
